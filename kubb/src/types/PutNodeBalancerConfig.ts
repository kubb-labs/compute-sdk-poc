/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export const putNodeBalancerConfigPathParamsApiVersionEnum = {
    "v4": "v4",
    "v4beta": "v4beta"
} as const;

export type PutNodeBalancerConfigPathParamsApiVersionEnumKey = (typeof putNodeBalancerConfigPathParamsApiVersionEnum)[keyof typeof putNodeBalancerConfigPathParamsApiVersionEnum];

export interface PutNodeBalancerConfigPathParams {
    /**
     * @description __Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta.
     * @type string
    */
    apiVersion: PutNodeBalancerConfigPathParamsApiVersionEnumKey;
    /**
     * @description The ID of the NodeBalancer.
     * @type integer
    */
    nodeBalancerId: number;
    /**
     * @description The ID of the Config to access.
     * @type integer
    */
    configId: number;
}

export const putNodeBalancerConfig200AlgorithmEnum = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "ring_hash": "ring_hash"
} as const;

export type PutNodeBalancerConfig200AlgorithmEnumKey = (typeof putNodeBalancerConfig200AlgorithmEnum)[keyof typeof putNodeBalancerConfig200AlgorithmEnum];

export const putNodeBalancerConfig200CheckEnum = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfig200CheckEnumKey = (typeof putNodeBalancerConfig200CheckEnum)[keyof typeof putNodeBalancerConfig200CheckEnum];

export const putNodeBalancerConfig200ProtocolEnum = {
    "udp": "udp"
} as const;

export type PutNodeBalancerConfig200ProtocolEnumKey = (typeof putNodeBalancerConfig200ProtocolEnum)[keyof typeof putNodeBalancerConfig200ProtocolEnum];

export const putNodeBalancerConfig200StickinessEnum = {
    "none": "none",
    "session": "session",
    "source_ip": "source_ip"
} as const;

export type PutNodeBalancerConfig200StickinessEnumKey = (typeof putNodeBalancerConfig200StickinessEnum)[keyof typeof putNodeBalancerConfig200StickinessEnum];

export const putNodeBalancerConfig200AlgorithmEnum2 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PutNodeBalancerConfig200AlgorithmEnum2Key = (typeof putNodeBalancerConfig200AlgorithmEnum2)[keyof typeof putNodeBalancerConfig200AlgorithmEnum2];

export const putNodeBalancerConfig200CheckEnum2 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfig200CheckEnum2Key = (typeof putNodeBalancerConfig200CheckEnum2)[keyof typeof putNodeBalancerConfig200CheckEnum2];

export const putNodeBalancerConfig200ProtocolEnum2 = {
    "tcp": "tcp"
} as const;

export type PutNodeBalancerConfig200ProtocolEnum2Key = (typeof putNodeBalancerConfig200ProtocolEnum2)[keyof typeof putNodeBalancerConfig200ProtocolEnum2];

export const putNodeBalancerConfig200ProxyProtocolEnum = {
    "none": "none",
    "v1": "v1",
    "v2": "v2"
} as const;

export type PutNodeBalancerConfig200ProxyProtocolEnumKey = (typeof putNodeBalancerConfig200ProxyProtocolEnum)[keyof typeof putNodeBalancerConfig200ProxyProtocolEnum];

export const putNodeBalancerConfig200StickinessEnum2 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PutNodeBalancerConfig200StickinessEnum2Key = (typeof putNodeBalancerConfig200StickinessEnum2)[keyof typeof putNodeBalancerConfig200StickinessEnum2];

export const putNodeBalancerConfig200AlgorithmEnum3 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PutNodeBalancerConfig200AlgorithmEnum3Key = (typeof putNodeBalancerConfig200AlgorithmEnum3)[keyof typeof putNodeBalancerConfig200AlgorithmEnum3];

export const putNodeBalancerConfig200CheckEnum3 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfig200CheckEnum3Key = (typeof putNodeBalancerConfig200CheckEnum3)[keyof typeof putNodeBalancerConfig200CheckEnum3];

export const putNodeBalancerConfig200ProtocolEnum3 = {
    "http": "http"
} as const;

export type PutNodeBalancerConfig200ProtocolEnum3Key = (typeof putNodeBalancerConfig200ProtocolEnum3)[keyof typeof putNodeBalancerConfig200ProtocolEnum3];

export const putNodeBalancerConfig200StickinessEnum3 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PutNodeBalancerConfig200StickinessEnum3Key = (typeof putNodeBalancerConfig200StickinessEnum3)[keyof typeof putNodeBalancerConfig200StickinessEnum3];

export const putNodeBalancerConfig200AlgorithmEnum4 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PutNodeBalancerConfig200AlgorithmEnum4Key = (typeof putNodeBalancerConfig200AlgorithmEnum4)[keyof typeof putNodeBalancerConfig200AlgorithmEnum4];

export const putNodeBalancerConfig200CheckEnum4 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfig200CheckEnum4Key = (typeof putNodeBalancerConfig200CheckEnum4)[keyof typeof putNodeBalancerConfig200CheckEnum4];

export const putNodeBalancerConfig200CipherSuiteEnum = {
    "recommended": "recommended",
    "legacy": "legacy"
} as const;

export type PutNodeBalancerConfig200CipherSuiteEnumKey = (typeof putNodeBalancerConfig200CipherSuiteEnum)[keyof typeof putNodeBalancerConfig200CipherSuiteEnum];

export const putNodeBalancerConfig200ProtocolEnum4 = {
    "https": "https"
} as const;

export type PutNodeBalancerConfig200ProtocolEnum4Key = (typeof putNodeBalancerConfig200ProtocolEnum4)[keyof typeof putNodeBalancerConfig200ProtocolEnum4];

export const putNodeBalancerConfig200StickinessEnum4 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PutNodeBalancerConfig200StickinessEnum4Key = (typeof putNodeBalancerConfig200StickinessEnum4)[keyof typeof putNodeBalancerConfig200StickinessEnum4];

/**
 * @description Config updated successfully.
*/
export type PutNodeBalancerConfig200 = ({
    /**
     * @description The algorithm this UDP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfig200AlgorithmEnumKey;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfig200CheckEnumKey;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value isn\'t present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description __Read-only__ Must be `false` for UDP.
     * @type boolean | undefined
    */
    readonly check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `udp` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfig200ProtocolEnumKey;
    /**
     * @description __Read-only__ Must be `none` for UDP.
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for UPD configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are assigned a backend server based on the algorithm configured.\n- If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.\n- If set to `source_ip`, the NodeBalancer uses the client\'s source IP address to route all packets from the same client to the same backend server.
     * @default "session"
     * @type string | undefined
    */
    stickiness?: PutNodeBalancerConfig200StickinessEnumKey;
    /**
     * @description UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    udp_check_port?: number;
    /**
     * @description __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
     * @minLength 1
     * @maxLength 300
     * @default 16
     * @type integer | undefined
    */
    readonly udp_session_timeout?: number;
} | {
    /**
     * @description The algorithm this TCP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfig200AlgorithmEnum2Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfig200CheckEnum2Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `tcp` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfig200ProtocolEnum2Key;
    /**
     * @description Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.\n\n- If omitted, or set to `none`, the NodeBalancer doesn\'t send any auxiliary data over TCP connections. This is the default.\n- If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.\n- If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
     * @default "none"
     * @type string | undefined
    */
    proxy_protocol?: PutNodeBalancerConfig200ProxyProtocolEnumKey;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description __Read-only__ Controls how session stickiness is handled on this port.\n\nNot applicable to TCP configurations.
     * @default "none"
     * @type string | undefined
    */
    readonly stickiness?: PutNodeBalancerConfig200StickinessEnum2Key;
} | {
    /**
     * @description The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfig200AlgorithmEnum3Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfig200CheckEnum3Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `http` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ Identifies the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced. You may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfig200ProtocolEnum3Key;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are always assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address are routed to the same backend.\n- If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PutNodeBalancerConfig200StickinessEnum3Key;
} | {
    /**
     * @description The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfig200AlgorithmEnum4Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- If `none` no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfig200CheckEnum4Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description The number of seconds between each check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
     * @default "recommended"
     * @type string | undefined
    */
    cipher_suite?: PutNodeBalancerConfig200CipherSuiteEnumKey;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfig200ProtocolEnum4Key;
    /**
     * @description __Read-only__ Not applicable for HTTPS configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description \nThe PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig\'s port.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\n[Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-cert
    */
    ssl_cert?: string | null;
    /**
     * @description __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\nThe contents of this field will not be shown in any responses that display\nthe NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field\nappears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig\nresponse are automatically derived from your certificate. Please refer to these fields to\nverify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-key
    */
    ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PutNodeBalancerConfig200StickinessEnum4Key;
});

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
*/
export interface PutNodeBalancerConfigError {
    /**
     * @type array | undefined
    */
    errors?: {
        /**
         * @description The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
         * @type string | undefined
        */
        field?: string;
        /**
         * @description What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
         * @type string | undefined
        */
        reason?: string;
    }[];
}

export const putNodeBalancerConfigMutationRequestAlgorithmEnum = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "ring_hash": "ring_hash"
} as const;

export type PutNodeBalancerConfigMutationRequestAlgorithmEnumKey = (typeof putNodeBalancerConfigMutationRequestAlgorithmEnum)[keyof typeof putNodeBalancerConfigMutationRequestAlgorithmEnum];

export const putNodeBalancerConfigMutationRequestCheckEnum = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfigMutationRequestCheckEnumKey = (typeof putNodeBalancerConfigMutationRequestCheckEnum)[keyof typeof putNodeBalancerConfigMutationRequestCheckEnum];

export const putNodeBalancerConfigMutationRequestProtocolEnum = {
    "udp": "udp"
} as const;

export type PutNodeBalancerConfigMutationRequestProtocolEnumKey = (typeof putNodeBalancerConfigMutationRequestProtocolEnum)[keyof typeof putNodeBalancerConfigMutationRequestProtocolEnum];

export const putNodeBalancerConfigMutationRequestStickinessEnum = {
    "none": "none",
    "session": "session",
    "source_ip": "source_ip"
} as const;

export type PutNodeBalancerConfigMutationRequestStickinessEnumKey = (typeof putNodeBalancerConfigMutationRequestStickinessEnum)[keyof typeof putNodeBalancerConfigMutationRequestStickinessEnum];

export const putNodeBalancerConfigMutationRequestAlgorithmEnum2 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PutNodeBalancerConfigMutationRequestAlgorithmEnum2Key = (typeof putNodeBalancerConfigMutationRequestAlgorithmEnum2)[keyof typeof putNodeBalancerConfigMutationRequestAlgorithmEnum2];

export const putNodeBalancerConfigMutationRequestCheckEnum2 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfigMutationRequestCheckEnum2Key = (typeof putNodeBalancerConfigMutationRequestCheckEnum2)[keyof typeof putNodeBalancerConfigMutationRequestCheckEnum2];

export const putNodeBalancerConfigMutationRequestProtocolEnum2 = {
    "tcp": "tcp"
} as const;

export type PutNodeBalancerConfigMutationRequestProtocolEnum2Key = (typeof putNodeBalancerConfigMutationRequestProtocolEnum2)[keyof typeof putNodeBalancerConfigMutationRequestProtocolEnum2];

export const putNodeBalancerConfigMutationRequestProxyProtocolEnum = {
    "none": "none",
    "v1": "v1",
    "v2": "v2"
} as const;

export type PutNodeBalancerConfigMutationRequestProxyProtocolEnumKey = (typeof putNodeBalancerConfigMutationRequestProxyProtocolEnum)[keyof typeof putNodeBalancerConfigMutationRequestProxyProtocolEnum];

export const putNodeBalancerConfigMutationRequestStickinessEnum2 = {
    "none": "none",
    "table": "table"
} as const;

export type PutNodeBalancerConfigMutationRequestStickinessEnum2Key = (typeof putNodeBalancerConfigMutationRequestStickinessEnum2)[keyof typeof putNodeBalancerConfigMutationRequestStickinessEnum2];

export const putNodeBalancerConfigMutationRequestAlgorithmEnum3 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PutNodeBalancerConfigMutationRequestAlgorithmEnum3Key = (typeof putNodeBalancerConfigMutationRequestAlgorithmEnum3)[keyof typeof putNodeBalancerConfigMutationRequestAlgorithmEnum3];

export const putNodeBalancerConfigMutationRequestCheckEnum3 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfigMutationRequestCheckEnum3Key = (typeof putNodeBalancerConfigMutationRequestCheckEnum3)[keyof typeof putNodeBalancerConfigMutationRequestCheckEnum3];

export const putNodeBalancerConfigMutationRequestProtocolEnum3 = {
    "http": "http"
} as const;

export type PutNodeBalancerConfigMutationRequestProtocolEnum3Key = (typeof putNodeBalancerConfigMutationRequestProtocolEnum3)[keyof typeof putNodeBalancerConfigMutationRequestProtocolEnum3];

export const putNodeBalancerConfigMutationRequestStickinessEnum3 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PutNodeBalancerConfigMutationRequestStickinessEnum3Key = (typeof putNodeBalancerConfigMutationRequestStickinessEnum3)[keyof typeof putNodeBalancerConfigMutationRequestStickinessEnum3];

export const putNodeBalancerConfigMutationRequestAlgorithmEnum4 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PutNodeBalancerConfigMutationRequestAlgorithmEnum4Key = (typeof putNodeBalancerConfigMutationRequestAlgorithmEnum4)[keyof typeof putNodeBalancerConfigMutationRequestAlgorithmEnum4];

export const putNodeBalancerConfigMutationRequestCheckEnum4 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PutNodeBalancerConfigMutationRequestCheckEnum4Key = (typeof putNodeBalancerConfigMutationRequestCheckEnum4)[keyof typeof putNodeBalancerConfigMutationRequestCheckEnum4];

export const putNodeBalancerConfigMutationRequestCipherSuiteEnum = {
    "recommended": "recommended",
    "legacy": "legacy"
} as const;

export type PutNodeBalancerConfigMutationRequestCipherSuiteEnumKey = (typeof putNodeBalancerConfigMutationRequestCipherSuiteEnum)[keyof typeof putNodeBalancerConfigMutationRequestCipherSuiteEnum];

export const putNodeBalancerConfigMutationRequestProtocolEnum4 = {
    "https": "https"
} as const;

export type PutNodeBalancerConfigMutationRequestProtocolEnum4Key = (typeof putNodeBalancerConfigMutationRequestProtocolEnum4)[keyof typeof putNodeBalancerConfigMutationRequestProtocolEnum4];

export const putNodeBalancerConfigMutationRequestStickinessEnum4 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PutNodeBalancerConfigMutationRequestStickinessEnum4Key = (typeof putNodeBalancerConfigMutationRequestStickinessEnum4)[keyof typeof putNodeBalancerConfigMutationRequestStickinessEnum4];

/**
 * @description The fields to update.
*/
export type PutNodeBalancerConfigMutationRequest = ({
    /**
     * @description The algorithm this UDP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfigMutationRequestAlgorithmEnumKey;
    /**
     * @description The type of check to perform against backends to ensure they\'re serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfigMutationRequestCheckEnumKey;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the check\'s response body for it to pass. Otherwise the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description __Read-only__ Must be `false` for UDP.
     * @type boolean | undefined
    */
    readonly check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren\'t in rotation, and aren\'t serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `udp` in this case.\n\nReview our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfigMutationRequestProtocolEnumKey;
    /**
     * @description __Read-only__ Must be `none` for UDP.
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for UPD configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections are assigned a back-end server based on the algorithm configured.\n- If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.\n- If set to `source_ip`, the NodeBalancer uses the client\'s source IP address to route all packets from the same client to the same backend server.
     * @default "session"
     * @type string | undefined
    */
    stickiness?: PutNodeBalancerConfigMutationRequestStickinessEnumKey;
    /**
     * @description UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    udp_check_port?: number;
    /**
     * @description __Read-only__ The maximum number of seconds a UDP session can be idle before it\'s closed.
     * @minLength 1
     * @maxLength 300
     * @default 16
     * @type integer | undefined
    */
    readonly udp_session_timeout?: number;
} | {
    /**
     * @description The algorithm this TCP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfigMutationRequestAlgorithmEnum2Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- Setting this to `none` skips the check.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfigMutationRequestCheckEnum2Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request it is considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren\'t in rotation, and aren\'t serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `tcp` in this case.\n\nReview our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfigMutationRequestProtocolEnum2Key;
    /**
     * @description Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.\n\n- If omitted, or set to `none`, the NodeBalancer doesn\'t send any auxiliary data over TCP connections. This is the default.\n- If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.\n- If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
     * @default "none"
     * @type string | undefined
    */
    proxy_protocol?: PutNodeBalancerConfigMutationRequestProxyProtocolEnumKey;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description __Read-only__ Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.
     * @default "none"
     * @type string | undefined
    */
    readonly stickiness?: PutNodeBalancerConfigMutationRequestStickinessEnum2Key;
} | {
    /**
     * @description The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfigMutationRequestAlgorithmEnum3Key;
    /**
     * @description The type of check to perform against backends to ensure they\'re serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfigMutationRequestCheckEnum3Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ Identifies the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren\'t in rotation, and aren\'t serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn\'t strictly enforced. You may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `http` in this case.\n\nReview our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfigMutationRequestProtocolEnum3Key;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PutNodeBalancerConfigMutationRequestStickinessEnum3Key;
} | {
    /**
     * @description The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PutNodeBalancerConfigMutationRequestAlgorithmEnum4Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- Setting this to `none` skips the check.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PutNodeBalancerConfigMutationRequestCheckEnum4Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description What ciphers to use for SSL connections served by this NodeBalancer.\n\n- `legacy` is considered insecure and should only be used if necessary.
     * @default "recommended"
     * @type string | undefined
    */
    cipher_suite?: PutNodeBalancerConfigMutationRequestCipherSuiteEnumKey;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren\'t in rotation, and aren\'t serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `https` in this case.\n\n- The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.\n\nReview our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PutNodeBalancerConfigMutationRequestProtocolEnum4Key;
    /**
     * @description __Read-only__ Not applicable for HTTPS configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig\'s port.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\n[Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-cert
    */
    ssl_cert?: string | null;
    /**
     * @description __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-key
    */
    ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PutNodeBalancerConfigMutationRequestStickinessEnum4Key;
});

export type PutNodeBalancerConfigMutationResponse = PutNodeBalancerConfig200;

export type PutNodeBalancerConfigMutation = {
    Response: PutNodeBalancerConfig200;
    Request: PutNodeBalancerConfigMutationRequest;
    PathParams: PutNodeBalancerConfigPathParams;
    Errors: any;
};