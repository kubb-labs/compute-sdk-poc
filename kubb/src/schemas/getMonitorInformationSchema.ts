/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const getMonitorInformationPathParamsSchema = z.object({
    "apiVersion": z.enum(["v4beta"]).describe("__Enum__ Call the `v4beta` URL for operations still only in beta."),
"serviceType": z.string().describe("The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.")
    })

export const getMonitorInformationHeaderParamsSchema = z.object({
    "X-Filter": z.optional(z.union([z.object({
    "is_alertable": z.optional(z.boolean().describe("Filter by whether the metric is alertable (`true` or `false`)."))
    }), z.object({
    "metric_type": z.optional(z.enum(["counter", "histogram", "gauge", "summary"]).describe("Filter by the metric type. Available values are `counter`, `histogram`, `gauge`, or `summary`."))
    }), z.object({
    "+and": z.optional(z.array(z.object({
    "is_alertable": z.optional(z.boolean()),
"metric_type": z.optional(z.enum(["counter", "histogram", "gauge", "summary"]))
    })).describe("All conditions need to be true. Include both `is_alertable` and `metric_type` conditions."))
    }), z.object({
    "+or": z.optional(z.array(z.object({
    "is_alertable": z.optional(z.boolean()),
"metric_type": z.optional(z.enum(["counter", "histogram", "gauge", "summary"]))
    })).describe("At least one condition needs to be true. Include both `is_alertable` and `metric_type` conditions."))
    })]).describe("Specifies the optional `X-Filter` header JSON object's filtering and sort criteria."))
    }).optional()

/**
 * @description Returns a paginated list of metric information.
 */
export const getMonitorInformation200Schema = z.object({
    "data": z.array(z.object({
    "available_aggregate_functions": z.optional(z.array(z.enum(["min", "max", "avg", "sum", "rate", "increase"])).describe("Available aggregate functions for the metric.")),
"dimensions": z.optional(z.array(z.object({
    "dimension_label": z.optional(z.string().describe("The label used to represent the dimension in metrics. Use this value to call out the service in other Monitor operations in the API.")),
"label": z.optional(z.string().describe("The label of the dimension. This is used for display purposes in Akamai Cloud Manager.")),
"values": z.optional(z.array(z.string()).describe("Available values for the `dimension_label`."))
    })).describe("Dimensions for the metric.")),
"is_alertable": z.optional(z.boolean().describe("Whether the metric is alertable.")),
"label": z.optional(z.string().describe("The name of the metric. This is used for display purposes in Akamai Cloud Manager.")),
"metric": z.optional(z.string().describe("The identifier for the metric. This is how the metric is called out in other Monitor operations in the API.")),
"metric_type": z.optional(z.enum(["counter", "histogram", "gauge", "summary"]).describe("The type of metric. A value of `counter` represents variable values and a set target for the count. A `histogram` represents the frequency distribution of data points across a continuous range of numerical values. A `gauge` represents the performance against a target goal, and `summary` presents and summarizes data from multiple sources.")),
"scrape_interval": z.optional(z.string().describe("How frequently a metric is scraped to gather data, using `s` for seconds, `m` for minutes, or `h` for hours. Set to `60s`, the metric would be scraped every 60 seconds. Set to `2m`, the scrape occurs every two minutes.")),
"unit": z.optional(z.enum(["%", "Bytes", "sec", "bps", "msec", "Bps", "KB", "MB", "GB", "rate", "percentile", "ratio", "OPS", "IOPS"]).describe("The unit of measurement for the metric."))
    })),
"page": z.int().describe("The current [page](https://techdocs.akamai.com/linode-api/reference/pagination)."),
"pages": z.int().describe("The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination)."),
"results": z.int().describe("The total number of results.")
    })

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const getMonitorInformationErrorSchema = z.object({
    "errors": z.optional(z.array(z.object({
    "field": z.optional(z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.")),
"reason": z.optional(z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully."))
    }).describe("An object for describing a single error that occurred during the processing of a request.")))
    })

export const getMonitorInformationQueryResponseSchema = z.lazy(() => getMonitorInformation200Schema)