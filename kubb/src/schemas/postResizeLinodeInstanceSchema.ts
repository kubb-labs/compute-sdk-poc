/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postResizeLinodeInstancePathParamsSchema = z.object({
    "apiVersion": z.enum(["v4", "v4beta"]).describe("__Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta."),
"linodeId": z.coerce.number().int().describe("ID of the Linode to resize.")
    })

/**
 * @description Resize started.
 */
export const postResizeLinodeInstance200Schema = z.object({
    
    }).describe("The API responds with an empty object.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postResizeLinodeInstanceErrorSchema = z.object({
    "errors": z.optional(z.array(z.object({
    "field": z.optional(z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.")),
"reason": z.optional(z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully."))
    }).describe("An object for describing a single error that occurred during the processing of a request.")))
    })

/**
 * @description The Type your current Linode will resize to, and whether to attempt to automatically resize the Linode\'s disks.
 */
export const postResizeLinodeInstanceMutationRequestSchema = z.object({
    "allow_auto_disk_resize": z.optional(z.boolean().default(true).describe("Automatically resize disks when resizing a Linode. When resizing down to a smaller plan your Linode's data must fit within the smaller disk size.")),
"migration_type": z.optional(z.enum(["warm", "cold"]).default("cold").describe("Type of migration used in moving to a new host or Linode type.\n\n`warm`: the Linode will not power down until the migration is complete.\nWarm migrations are not available for DC migrations.\n\n`cold`: the Linode will be powered down and migrated. When the migration\nis complete, the Linode will be powered on.")),
"type": z.string().describe("The ID representing the Linode Type.")
    })

export const postResizeLinodeInstanceMutationResponseSchema = z.lazy(() => postResizeLinodeInstance200Schema)