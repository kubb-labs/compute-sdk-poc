/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postAssignIpv4SPathParamsSchema = z.object({
    "apiVersion": z.enum(["v4", "v4beta"]).describe("__Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta.")
    })

/**
 * @description All assignments completed successfully.
 */
export const postAssignIpv4S200Schema = z.object({
    
    }).describe("The API responds with an empty object.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postAssignIpv4SErrorSchema = z.object({
    "errors": z.optional(z.array(z.object({
    "field": z.optional(z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.")),
"reason": z.optional(z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully."))
    }).describe("An object for describing a single error that occurred during the processing of a request.")))
    })

/**
 * @description Information about what IPv4 address to assign, and to which Linode.
 */
export const postAssignIpv4SMutationRequestSchema = z.object({
    "assignments": z.array(z.object({
    "address": z.string().describe("The IPv4 address or IPv6 range for this assignment.\n\n- Must be an IPv4 address or an IPv6 range you can access in the Region specified.\n- IPv6 ranges must include a prefix length of `/56` or `/64`, for example: `2001:db8:3c4d:15::/64`.\n- Assignment of an IPv6 range to a Linode updates the route target of the range to the assigned Linode's SLAAC address.\n- Can be a public or private address.\n- For Linode interfaces, public IPv4 addresses or public IPv6 ranges can be exchanged on public interfaces. Private IPv4 addresses are not supported."),
"linode_id": z.int().describe("The ID of the Linode to assign this address to. The IP's previous Linode will lose this address, and must end up with at least one public address and no more than one private address once all assignments have been made.")
    })).describe("The list of assignments to make. You must have read_write access to all IPs being assigned and all Linodes being assigned to in order for the assignments to succeed."),
"region": z.string().describe("The ID of the Region in which these assignments are to take place. All IPs and Linodes must exist in this Region.")
    }).describe("Request object for IP Addresses Assign (POST /networking/ips/assign).")

export const postAssignIpv4SMutationResponseSchema = z.lazy(() => postAssignIpv4S200Schema)