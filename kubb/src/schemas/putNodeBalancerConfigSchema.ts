/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const putNodeBalancerConfigPathParamsSchema = z.object({
    "apiVersion": z.enum(["v4", "v4beta"]).describe("__Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta."),
"nodeBalancerId": z.coerce.number().int().describe("The ID of the NodeBalancer."),
"configId": z.coerce.number().int().describe("The ID of the Config to access.")
    })

/**
 * @description Config updated successfully.
 */
export const putNodeBalancerConfig200Schema = z.union([z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "ring_hash"]).default("roundrobin").describe("The algorithm this UDP NodeBalancer uses to route traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().describe("__Read-only__ Must be `false` for UDP.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(30).describe("How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.string().describe("__Read-only__ Not applicable for `udp` configs.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ The ID for the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["udp"]).default("http").describe("The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.")),
"proxy_protocol": z.optional(z.string().describe("__Read-only__ Must be `none` for UDP.")),
"ssl_cert": z.string().describe("__Read-only__ Not applicable for UDP configs.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ Not applicable for UPD configs.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ Not applicable for UDP configs.")),
"ssl_key": z.string().describe("__Read-only__ Not applicable for UDP configs.").nullish(),
"stickiness": z.optional(z.enum(["none", "session", "source_ip"]).default("session").describe("Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are assigned a backend server based on the algorithm configured.\n- If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.\n- If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.")),
"udp_check_port": z.optional(z.int().min(1).max(65535).default(80).describe("UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.")),
"udp_session_timeout": z.optional(z.int().min(1).max(300).default(16).describe("__Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed."))
    }), z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "source"]).default("roundrobin").describe("The algorithm this TCP NodeBalancer uses to route traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().default(true).describe("If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(30).describe("How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.string().describe("__Read-only__ Not applicable for `tcp` configs.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ The ID for the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["tcp"]).default("http").describe("The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.")),
"proxy_protocol": z.optional(z.enum(["none", "v1", "v2"]).default("none").describe("Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.\n\n- If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.\n- If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.\n- If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.")),
"ssl_cert": z.string().describe("__Read-only__ Not applicable for TCP configs.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ Not applicable for TCP configs.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ Not applicable for TCP configs.")),
"ssl_key": z.string().describe("__Read-only__ Not applicable for HTTP configs.").nullish(),
"stickiness": z.optional(z.enum(["none", "table", "http_cookie"]).default("none").describe("__Read-only__ Controls how session stickiness is handled on this port.\n\nNot applicable to TCP configurations."))
    }), z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "source"]).default("roundrobin").describe("The algorithm this HTTP NodeBalancer uses for routing traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().default(true).describe("If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(3).describe("How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.string().describe("__Read-only__ Not applicable for `http` configs.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ Identifies the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["http"]).default("http").describe("The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.")),
"proxy_protocol": z.optional(z.string().default("none").describe("__Read-only__ Not applicable for HTTP configs.")),
"ssl_cert": z.string().describe("__Read-only__ Not applicable for HTTP configs.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ Not applicable for HTTP configs.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ Not applicable for HTTP configs.")),
"ssl_key": z.string().describe("__Read-only__ Not applicable for HTTP configs.").nullish(),
"stickiness": z.optional(z.enum(["none", "table", "http_cookie"]).default("table").describe("Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are always assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address are routed to the same backend.\n- If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer."))
    }), z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "source"]).default("roundrobin").describe("The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- If `none` no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("The number of seconds between each check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().default(true).describe("If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(3).describe("How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.enum(["recommended", "legacy"]).default("recommended").describe("What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ The ID for the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["https"]).default("http").describe("The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.")),
"proxy_protocol": z.optional(z.string().default("none").describe("__Read-only__ Not applicable for HTTPS configs.")),
"ssl_cert": z.string().describe("\nThe PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\n[Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.")),
"ssl_key": z.string().describe("The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\nThe contents of this field will not be shown in any responses that display\nthe NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field\nappears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig\nresponse are automatically derived from your certificate. Please refer to these fields to\nverify that the appropriate certificate was assigned to your NodeBalancerConfig.").nullish(),
"stickiness": z.optional(z.enum(["none", "table", "http_cookie"]).default("table").describe("Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer."))
    })]).describe("NodeBalancer `config` options for each protocol.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const putNodeBalancerConfigErrorSchema = z.object({
    "errors": z.optional(z.array(z.object({
    "field": z.optional(z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.")),
"reason": z.optional(z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully."))
    }).describe("An object for describing a single error that occurred during the processing of a request.")))
    })

/**
 * @description The fields to update.
 */
export const putNodeBalancerConfigMutationRequestSchema = z.union([z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "ring_hash"]).default("roundrobin").describe("The algorithm this UDP NodeBalancer uses to route traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they're serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("This value must be present in the check's response body for it to pass. Otherwise the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().describe("__Read-only__ Must be `false` for UDP.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(30).describe("Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.string().describe("__Read-only__ Not applicable for UDP configs.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ The ID for the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["udp"]).default("http").describe("The protocol the port is configured to serve, `udp` in this case.\n\nReview our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.")),
"proxy_protocol": z.optional(z.string().describe("__Read-only__ Must be `none` for UDP.")),
"ssl_cert": z.string().describe("__Read-only__ Not applicable for UDP configs.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ Not applicable for UPD configs.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ Not applicable for UDP configs.")),
"ssl_key": z.string().describe("__Read-only__ Not applicable for UDP configs.").nullish(),
"stickiness": z.optional(z.enum(["none", "session", "source_ip"]).default("session").describe("Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections are assigned a back-end server based on the algorithm configured.\n- If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.\n- If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.")),
"udp_check_port": z.optional(z.int().min(1).max(65535).default(80).describe("UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.")),
"udp_session_timeout": z.optional(z.int().min(1).max(300).default(16).describe("__Read-only__ The maximum number of seconds a UDP session can be idle before it's closed."))
    }), z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "source"]).default("roundrobin").describe("The algorithm this TCP NodeBalancer uses to route traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- Setting this to `none` skips the check.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().default(true).describe("If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. If the backend does not respond to this request it is considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(30).describe("Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.string().describe("__Read-only__ Not applicable for TCP configs.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ The ID for the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["tcp"]).default("http").describe("The protocol the port is configured to serve, `tcp` in this case.\n\nReview our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.")),
"proxy_protocol": z.optional(z.enum(["none", "v1", "v2"]).default("none").describe("Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.\n\n- If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.\n- If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.\n- If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.")),
"ssl_cert": z.string().describe("__Read-only__ Not applicable for TCP configs.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ Not applicable for TCP configs.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ Not applicable for TCP configs.")),
"ssl_key": z.string().describe("__Read-only__ Not applicable for HTTP configs.").nullish(),
"stickiness": z.optional(z.enum(["none", "table"]).default("none").describe("__Read-only__ Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend."))
    }), z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "source"]).default("roundrobin").describe("The algorithm this HTTP NodeBalancer uses for routing traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they're serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().default(true).describe("If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(3).describe("Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.string().describe("__Read-only__ Not applicable for HTTP configs.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ Identifies the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["http"]).default("http").describe("The protocol the port is configured to serve, `http` in this case.\n\nReview our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.")),
"proxy_protocol": z.optional(z.string().default("none").describe("__Read-only__ Not applicable for HTTP configs.")),
"ssl_cert": z.string().describe("__Read-only__ Not applicable for HTTP configs.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ Not applicable for HTTP configs.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ Not applicable for HTTP configs.")),
"ssl_key": z.string().describe("__Read-only__ Not applicable for HTTP configs.").nullish(),
"stickiness": z.optional(z.enum(["none", "table", "http_cookie"]).default("table").describe("Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer."))
    }), z.object({
    "algorithm": z.optional(z.enum(["roundrobin", "leastconn", "source"]).default("roundrobin").describe("The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.")),
"check": z.optional(z.enum(["none", "connection", "http", "http_body"]).default("none").describe("The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- Setting this to `none` skips the check.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.")),
"check_attempts": z.optional(z.int().min(1).max(30).default(3).describe("How many times to attempt a check before considering a backend to be down.")),
"check_body": z.optional(z.string().describe("Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.")),
"check_interval": z.optional(z.int().min(2).max(3600).default(5).describe("Number of seconds between checks to verify that backends are up and serving requests.\n\nMust be greater than `check_timeout`.")),
"check_passive": z.optional(z.boolean().default(true).describe("If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.")),
"check_path": z.optional(z.string().regex(/^[a-zA-Z0-9\/\-%?&=.]*$/).describe("The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.")),
"check_timeout": z.optional(z.int().min(1).max(30).default(3).describe("Number of seconds to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.")),
"cipher_suite": z.optional(z.enum(["recommended", "legacy"]).default("recommended").describe("What ciphers to use for SSL connections served by this NodeBalancer.\n\n- `legacy` is considered insecure and should only be used if necessary.")),
"id": z.optional(z.int().describe("__Read-only__ This config's unique ID.")),
"nodebalancer_id": z.optional(z.int().describe("__Read-only__ The ID for the NodeBalancer this config belongs to.")),
"nodes_status": z.optional(z.object({
    "down": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.")),
"up": z.optional(z.int().describe("__Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests."))
    }).describe("__Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.")),
"port": z.optional(z.int().min(1).max(65535).default(80).describe("This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.")),
"protocol": z.optional(z.enum(["https"]).default("http").describe("The protocol the port is configured to serve, `https` in this case.\n\n- The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.\n\nReview our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.")),
"proxy_protocol": z.optional(z.string().default("none").describe("__Read-only__ Not applicable for HTTPS configs.")),
"ssl_cert": z.string().describe("The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\n[Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.").nullish(),
"ssl_commonname": z.optional(z.string().describe("__Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.")),
"ssl_fingerprint": z.optional(z.string().describe("__Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.")),
"ssl_key": z.string().describe("The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.").nullish(),
"stickiness": z.optional(z.enum(["none", "table", "http_cookie"]).default("table").describe("Controls how session stickiness is handled on this port:\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer."))
    })]).describe("NodeBalancer `config` options for each protocol.")

export const putNodeBalancerConfigMutationResponseSchema = z.lazy(() => putNodeBalancerConfig200Schema)