/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postLinodeInstancePathParamsSchema = z.object({
    "apiVersion": z.enum(["v4", "v4beta"]).describe("__Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta.")
    })

/**
 * @description A new Linode is being created.
 */
export const postLinodeInstance200Schema = z.object({
    "alerts": z.optional(z.object({
    "cpu": z.optional(z.int().describe("The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.\n\nFor example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.\n\nThe default value is 90% multiplied by the number of cores.\n\nIf the value is set to `0` (zero), the alert is disabled.")),
"io": z.optional(z.int().describe("The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.")),
"network_in": z.optional(z.int().describe("The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.")),
"network_out": z.optional(z.int().describe("The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.")),
"transfer_quota": z.optional(z.int().describe("The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled."))
    })),
"backups": z.optional(z.object({
    "available": z.optional(z.boolean().describe("__Read-only__ Whether Backups for this Linode are available for restoration.\n\nBackups undergoing maintenance are not available for restoration.")),
"enabled": z.optional(z.boolean().describe("__Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).")),
"last_successful": z.optional(z.string().datetime().describe("__Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.")),
"schedule": z.optional(z.object({
    "day": z.enum(["Scheduling", "Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]).describe("The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.\n\nIf not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.").nullish(),
"window": z.enum(["Scheduling", "W0", "W2", "W4", "W6", "W8", "W10", "W12", "W14", "W16", "W18", "W20", "W22"]).describe("When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.\n\nFor example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.\n\nIf not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.").nullish()
    }))
    }).describe("Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).")),
"capabilities": z.optional(z.array(z.string()).describe("__Limited availability__, __Read-only__ A list of capabilities this compute instance supports.")),
"created": z.optional(z.string().datetime().describe("__Read-only__ When this Linode was created.")),
"disk_encryption": z.string().default("enabled").describe("__Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.").nullish(),
"group": z.optional(z.string().describe("__Deprecated__, __Filterable__ The group label for this Linode.")),
"has_user_data": z.optional(z.boolean().describe("__Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.")),
"host_uuid": z.optional(z.uuid().describe("__Read-only__ The Linode's host machine, as a UUID.")),
"hypervisor": z.optional(z.enum(["kvm"]).describe("__Read-only__ The virtualization software powering this Linode.")),
"id": z.optional(z.int().describe("__Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.")),
"image": z.optional(z.string()),
"interface_generation": z.optional(z.enum(["legacy_config", "linode"]).describe("__Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).")),
"ipv4": z.optional(z.url().describe("__Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.\n\nIPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.")),
"ipv6": z.string().describe("__Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.").nullish(),
"label": z.optional(z.string().regex(/^[a-zA-Z]((?!--|__|\.\.)[a-zA-Z0-9-_.])+$/).describe("__Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.\n\nLinode labels have the following constraints:\n\n- It needs to begin and end with an alphanumeric character.\n- It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).\n- Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.")),
"lke_cluster_id": z.int().describe("__Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.").nullish(),
"maintenance_policy": z.optional(z.enum(["linode/migrate", "linode/power_off_on"]).describe("__Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.")),
"placement_group": z.object({
    "id": z.optional(z.int().describe("The placement group's ID. You need to provide it for all operations that affect it.")),
"label": z.optional(z.string().min(1).describe("__Filterable__ The unique name set for the placement group. A label has these constraints:\n\n- It needs to begin and end with an alphanumeric character.\n- It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).")),
"placement_group_policy": z.optional(z.enum(["strict", "flexible"]).describe("How requests to add future compute instances to your placement group are handled, and whether it remains compliant:\n\n- `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).\n- `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.\n\n<<LB>>\n\n> 📘\n>\n> In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.")),
"placement_group_type": z.optional(z.enum(["anti_affinity:local"]).describe("__Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.\n\n> 📘\n>\n> Currently, only `anti_affinity:local` is available for `placement_group_type`."))
    }).describe("__Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.").nullish(),
"region": z.optional(z.string().describe("__Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).")),
"specs": z.optional(z.object({
    "disk": z.optional(z.int().describe("__Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.")),
"gpus": z.optional(z.int().describe("__Read-only__ The number of GPUs this Linode has access to.")),
"memory": z.optional(z.int().describe("__Read-only__ The amount of RAM, in MB, this Linode has access to.\n\nTypically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.")),
"transfer": z.optional(z.int().describe("__Read-only__ The amount of network transfer this Linode is allotted each month.")),
"vcpus": z.optional(z.int().describe("__Read-only__ The number of VCPUs this Linode has access to."))
    }).describe("__Read-only__ Information about the resources available to this Linode.")),
"status": z.optional(z.enum(["running", "offline", "booting", "busy", "rebooting", "shutting_down", "provisioning", "deleting", "migrating", "rebuilding", "cloning", "restoring", "stopped", "billing_suspension"]).describe("__Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.\n\n- `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.\n- `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.\n- `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.")),
"tags": z.optional(z.array(z.string()).describe("__Filterable__ Tags to help you organize your content.")),
"type": z.optional(z.string().describe("__Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).")),
"updated": z.optional(z.string().datetime().describe("__Read-only__ When this Linode was last updated.")),
"watchdog_enabled": z.optional(z.boolean().describe("The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes."))
    })

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postLinodeInstanceErrorSchema = z.object({
    "errors": z.optional(z.array(z.object({
    "field": z.optional(z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.")),
"reason": z.optional(z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully."))
    }).describe("An object for describing a single error that occurred during the processing of a request.")))
    })

/**
 * @description The requested initial state of a new Linode.
 */
export const postLinodeInstanceMutationRequestSchema = z.object({
    "authorized_keys": z.optional(z.array(z.string()).describe("__Write-only__ A list of public SSH keys that will be automatically appended to the root user's `~/.ssh/authorized_keys` file when deploying from an Image.")),
"authorized_users": z.optional(z.array(z.string()).describe("__Write-only__ A list of usernames for authorized users. Before you can add a user, it needs an SSH key assigned to its profile. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information. If the usernames already have associated SSH keys, the keys will be appended to the root users `~/.ssh/authorized_keys` file automatically when deploying from an image.")),
"booted": z.optional(z.boolean().default(true).describe("__Write-only__ This field defaults to `true` if the Linode is created with an Image or from a Backup. If it is deployed from an Image or a Backup and you wish it to remain `offline` after deployment, set this to `false`.")),
"disk_encryption": z.optional(z.enum(["enabled", "disabled"]).describe("Local disk encryption ensures that your data stored on Linodes is secured. Disk encryption protects against unauthorized data access by keeping the data encrypted if the disk is ever removed from the data center, decommissioned, or disposed of. The platform manages the encryption and decryption for you.\n\nBy default, encryption is `enabled` on all Linodes. If you opted out of encryption or if the Linode was created prior to local disk encryption support, you can encrypt your data using [Rebuild](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance).")),
"image": z.string().describe("An Image ID to deploy the Linode Disk from.\n\nRun the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.").nullish(),
"maintenance_policy": z.optional(z.enum(["linode/migrate", "linode/power_off_on"]).describe("__Beta__ Defines the maintenance policy for this Linode. Choose from the following policies:\n\n- `linode/migrate`: Migrates the Linode to a new host while it remains fully operational (with some performance degradation). Recommended for maximizing availability.\n- `linode/power_off_on`: Powers off the Linode at the start of the maintenance event and reboots it once the maintenance finishes. Recommended for maximizing performance.\n\nReview [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.")),
"metadata": z.optional(z.object({
    "user_data": z.optional(z.string().describe("Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.\n\nCannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.\n\nMust not be included when cloning to an existing Linode.\n\nUnencoded data must not exceed 65535 bytes, or about 16kb encoded."))
    }).describe("__Write-only__ An object containing user-defined data relevant to the creation of Linodes.")),
"root_pass": z.optional(z.string().min(7).max(128).describe("__Write-only__ This sets the root user's password on a newly created Linode Disk when deploying from an Image.\n\n- __Required__ when creating a Linode Disk from an Image, including when using a StackScript.\n\n- Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.")),
"stackscript_data": z.optional(z.object({
    
    }).describe("This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.\n\nThis field is required to be valid JSON.\n\nTotal length cannot exceed 65,535 characters.")),
"stackscript_id": z.optional(z.int().describe("A StackScript ID that will cause the referenced StackScript to be run during deployment of this Linode. A compatible `image` is required to use a StackScript. To get a list of available StackScript and their permitted Images, run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts). This field cannot be used when deploying from a Backup or a Private Image.")),
"backup_id": z.optional(z.int().describe("A Backup ID from another Linode's available backups. Your User must have `read_write` access to that Linode, the Backup must have a `status` of `successful`, and the Linode must be deployed to the same `region` as the Backup. Run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups) for a Linode's available backups.\n\nThis field and the `image` field are mutually exclusive.")),
"backups_enabled": z.optional(z.boolean().describe("If this field is set to `true`, the created Linode will automatically be enrolled in the Linode Backup service. This will incur an additional charge. The cost for the Backup service is dependent on the Type of Linode deployed.\n\nThis option is always treated as `true` if the account-wide `backups_enabled` setting is `true`.  See [Get account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings) for more information.\n\nBackup pricing is included in the response from [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types)")),
"firewall_id": z.optional(z.int().describe("The `id` of the Firewall to attach this Linode to upon creation. This `firewall_id` field is for Linodes using VLAN and legacy configuration interfaces only.")),
"group": z.optional(z.string().describe("__Deprecated__, __Filterable__ The group label for this Linode.")),
"interface_generation": z.enum(["legacy_config", "linode"]).describe("__Beta__ Specifies the interface type for the Linode. The value can be either `legacy_config` or `linode`. The default value is determined by the `interfaces_for_new_linodes` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). If the `interface_generation` option is set to `linode`, legacy configuration interfaces can no longer be used on the Linode.\n- If `interfaces_for_new_linodes` is set to `linode_only`, set `interface_generation` to `linode` or omit it for Linode interfaces.\n- If `interfaces_for_new_linodes` is set to `legacy_config_only`, set `interface_generation` to `legacy_config` or omit it for legacy configuration interfaces.\n- If `interfaces_for_new_linodes` is set to `linode_default_but_legacy_config_allowed`, set `interface_generation` to `linode` or omit it for Linode interfaces, and to `legacy_config` if the Linode uses legacy configuration interfaces.\n- If `interfaces_for_new_linodes` is set to `legacy_config_default_but_linode_allowed`, set `interface_generation` to `legacy_config` or omit it for legacy configuration interfaces, and to `linode` if the Linode uses Linode interfaces.").nullish(),
"interfaces": z.optional(z.array(z.union([z.union([z.object({
    "default_route": z.optional(z.object({
    "ipv4": z.boolean().describe("If set to `true`, the interface is used for the IPv4 `default_route`. Only one interface per Linode can be set as the IPv4 default route.").nullish(),
"ipv6": z.boolean().describe("If set to `true`, the interface is used for the IPv6 `default_route`. Only one interface per Linode can have the IPv6 default route.").nullish()
    }).describe("Indicates if the interface serves as the default route when multiple interfaces are eligible for this role. A public interface can have both an IPv4 `default_route` and an IPv6 `default_route`, provided it has at least one IP address of the corresponding type.")),
"firewall_id": z.int().describe("The enabled firewall to secure a VPC or public interface. Not allowed for VLAN interfaces. If a `firewall_id` is not provided for a VPC or a public interface, then its [default interface firewall](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-firewalls) is used. If a default firewall is not available, and `null` is not specified, the request fails. Setting the `firewall_id` as `null` indicates that no firewall device will be attached to the interface.").nullish(),
"public": z.object({
    "ipv4": z.optional(z.object({
    "addresses": z.optional(z.array(z.object({
    "address": z.optional(z.union([z.string(), z.string(), z.enum(["auto"])]).default("auto").describe("The interface's public IPv4 address. You can specify which public IPv4 address to configure for the interface. Setting this to `auto` automatically allocates a public address. To create a public interface that uses IPv6 and no IPv4, set the IPv4 address as an empty list `[]`. If the address is a reserved or automatically assigned, it needs to be reserved or already assigned to a single Linode, and it can’t be assigned to any other interface.")),
"primary": z.optional(z.boolean().default(false).describe("The IPv4 primary address configures the source address for routes within the Linode on the corresponding network interface.\n- Don't set this to `false` if there's only one address in the `addresses` array.\n- If more than one address is provided, primary can be set to `true` for one address.\n- If only one address is present in the `addresses` array, this address is automatically set as the primary address."))
    })).describe("List of IPv4 addresses to assign to this interface. Setting any to `auto` allocates a public IPv4 address. To create a public interface that uses IPv6 and no IPv4, set the IPv4 address as an empty list `[]`."))
    }).describe("IPv4 address settings for this public interface. If omitted, a public IPv4 address is automatically allocated.")),
"ipv6": z.optional(z.object({
    "ranges": z.optional(z.array(z.object({
    "range": z.string().describe("Your assigned IPv6 range in CIDR notation (`2001:0db8::1/64`) or prefix (`/64`).\n- The prefix of `/64` or `/56` block of IPv6 addresses.\n- If provided in CIDR notation, the prefix must be within the assigned ranges for the Linode.")
    })).describe("IPv6 address ranges to assign to this interface. If omitted, no ranges are assigned."))
    }).describe("IPv6 address settings for the public interface."))
    }).describe("Public interface settings. A Linode can have only one public interface. A public interface can have both IPv4 and IPv6 configurations.").nullish()
    }), z.object({
    "vlan": z.object({
    "ipam_address": z.optional(z.string().describe("This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.")),
"vlan_label": z.optional(z.string().regex(/[a-zA-Z0-9-]+/).describe("The VLAN's unique label. VLAN interfaces on the same Linode must have a unique `vlan_label`."))
    }).describe("VLAN interface settings. A Linode can have up to three VLAN interfaces, with a unique `vlan_label` for each.").nullish()
    }), z.object({
    "default_route": z.optional(z.object({
    "ipv4": z.boolean().describe("If set to `true`, the interface is used for the IPv4 `default_route`. Only one interface per Linode can be set as the IPv4 default route.").nullish()
    }).describe("Indicates if the interface is used as the default route when multiple interfaces are eligible for this role. A VPC interface can have an IPv4 `default_route`.")),
"firewall_id": z.int().describe("The enabled firewall to secure a VPC or public interface. Not allowed for VLAN interfaces. If a `firewall_id` is not provided for a VPC or a public interface, then its default interface firewall is used. If a default firewall is not available, and `null` is not specified, the request fails. Setting the `firewall_id` as `null` indicates that no firewall device will be attached to the interface.").nullish(),
"vpc": z.object({
    "ipv4": z.optional(z.object({
    "addresses": z.optional(z.array(z.object({
    "address": z.optional(z.union([z.string(), z.enum(["auto"])]).default("auto").describe("Specifies which IPv4 address to use in the VPC subnet. You can specify which VPC Ipv4 address in the subnet to configure for the interface. You can't use an IPv4 address taken from another Linode or interface, or the first two or last two addresses in the VPC subnet. When `address` is set to `auto`, an IP address from the subnet is automatically assigned.")),
"nat_1_1_address": z.union([z.string(), z.enum(["auto"])]).describe("The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.\n- You can set this to a specific public IPv4 address that's available on the Linode.\n- If set to `auto`, a public IPv4 address is automatically selected.\n- If a public IPv4 address or `auto` is specified, primary must be set to `true`.\n- The address can't be used on another Linode.\n- If the address is a reserved or an automatically assigned IP, the IP must be reserved or already assigned to a single Linode, and not assigned to an interface.\n- If this property is omitted or set to `null`, no 1:1 NAT configuration will be applied to the VPC interface.").nullish(),
"primary": z.boolean().default(false).describe("The IPv4 primary address is used to configure the source address for routes within the Linode on the corresponding network interface.\nShould not be set to `false` if there is only one address present in the `addresses` array.\nIf more than one address is provided, primary must be set to `true` for one address.\nIf only one address is present in the `addresses` array, this address is automatically set as the primary address.").nullish()
    })).describe("IPv4 address settings for this VPC interface.")),
"ranges": z.optional(z.array(z.object({
    "range": z.string().describe("CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).\n- When only the prefix is provided, then an available range of that size within the VPC's subnet is automatically selected.\n- If specified as CIDR notation, it must belong to the VPC subnet. All addresses in the range must not be taken by any other Linode or interfaces in the VPC subnet and must not include any of the first two or last two addresses of the VPC subnet.")
    })).describe("VPC IPv4 ranges."))
    }).describe("Interfaces can be configured with IPv4 addresses or ranges.")),
"subnet_id": z.int().describe("The VPC subnet identifier for this interface. Your subnet’s VPC must be in the same data center (region) as the Linode.")
    }).describe("VPC interface settings. A Linode can have one VPC interface. The maximum number of interfaces allowed on a Linode is three.").nullish()
    })]), z.any().and(z.object({
    "active": z.optional(z.boolean().describe("__Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.")),
"id": z.optional(z.int().describe("__Read-only__ The unique ID representing this interface.")),
"ip_ranges": z.array(z.string()).describe("IPv4 CIDR VPC subnet ranges that are routed to this interface.\n\nWhen included in a request:\n\n- A range can't include any addresses that are assigned to an active Linode or another VPC subnet.\n\n- When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.\n\n- Submit this as an empty array removes all existing values.\n\n- Omit this object to leave existing values as is.\n\n<<LB>>\n\n> 📘\n>\n> This is only supported for interfaces with a `purpose` of `vpc`.").nullish(),
"ipam_address": z.string().describe("This interface's private IP address in classless inter-domain routing (CIDR) notation.\n\nFor interfaces with a `purpose` of `public`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.\n\n- This should be unique among devices attached to the VLAN to avoid conflict.\n\n- If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).\n\nFor interfaces with a `purpose` of `vpc`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.").nullish(),
"ipv4": z.optional(z.object({
    "nat_1_1": z.union([z.string(), z.enum(["any"])]).describe("The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.\n\n- Only supported for interfaces with a `purpose` of `vpc`.\n\n- Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.\n\n- Returned as an empty string (`\"\"`) for non-`vpc` type interfaces.\n\nWhen included in a request:\n\n- You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.\n\n- A specified address can't be shared with another Linode.\n\n- Omit this object or include it and set it to `null` or an empty string (`\"\"`) to block creation of a 1:1 NAT.\n\n<<LB>>\n\n> 📘\n>\n> You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.").nullish(),
"vpc": z.string().describe("The VPC subnet IPv4 address for this interface.\n\n- This only applies to interfaces with a `purpose` of `vpc`.\n\n- Returned as an empty string (`\"\"`) for non-`vpc` type interfaces.\n\nWhen included in a request:\n\n- The `vpc` can't be assigned to an existing Linode as an address or in a range.\n\n- The target address can't be the first two or last two addresses in the subnet IPv4 range.\n\n- If omitted, a valid address within the Subnet IPv4 range is automatically assigned.").nullish()
    }).describe("IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.")),
"label": z.string().regex(/[a-zA-Z0-9-]+/).describe("__Filterable__ The name of this interface.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- Required.\n\n- This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.\n\n- This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).\n\n- If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.\n\nFor interfaces with a `purpose` of `public`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.\n\nFor interfaces with a `purpose` of `vpc`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.").nullish(),
"primary": z.optional(z.boolean().describe("The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.\n\n> 📘\n>\n> This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.")),
"purpose": z.enum(["public", "vlan", "vpc"]).describe("The type of interface. This can be `public`, `vlan`, or `vpc`.\n\nFor interfaces with a `purpose` of `public`:\n\n- You can only define one `public` interface per Linode.\n\n- The Linode's default public IPv4 address is assigned to the `public` interface.\n\n- A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.\n\n- If an `ipam_address` is configured, the Linode uses this address.\n\nFor interfaces with a `purpose` of `vpc`:\n\n- Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.\n\n- When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information."),
"subnet_id": z.int().describe("The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.\n\n- Required for `vpc` type interfaces.\n\n- Returned as `null` for non-`vpc` type interfaces.\n\n- Once you've assigned a VPC subnet to an interface, you can't update it.\n\n- You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.").nullish(),
"vpc_id": z.int().describe("__Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.").nullish()
    }))])).describe("Interfaces for the Linode. This can be a Linode interface or legacy configuration interface.")),
"label": z.optional(z.string().regex(/^[a-zA-Z]((?!--|__|\.\.)[a-zA-Z0-9-_.])+$/).describe("__Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.\n\nLinode labels have the following constraints:\n\n- It needs to begin and end with an alphanumeric character.\n- It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).\n- Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.")),
"network_helper": z.boolean().describe("Enables the Network Helper feature. The default value is determined by the `network_helper` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). This `network_helper` field is for Linodes using Linode interfaces only.").nullish(),
"placement_group": z.optional(z.object({
    "id": z.optional(z.int().describe("The placement group's ID. You need to provide it for all operations that affect it."))
    }).describe("Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/). These constraints apply:\n\n- The target placement group needs to be in the same `region` set for this Linode.\n- The placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation and note either the `maximum_linodes_per_pg` (strict) or `maximum_linodes_per_flexible_pg` (flexible), based on your selected `placement_group_policy`. These represent the Linode limit per placement group, for each `placement_group_policy` type. You can then run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation to review the Linodes in that group.")),
"private_ip": z.optional(z.boolean().describe("If `true`, the created Linode will have private networking enabled and assigned a private IPv4 address.")),
"region": z.string().describe("The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode will be located."),
"swap_size": z.optional(z.int().default(512).describe("When deploying from an Image, this field is optional, otherwise it is ignored. This is used to set the swap disk size for the newly created Linode.")),
"tags": z.optional(z.array(z.string()).describe("__Filterable__ Tags to help you organize your content.")),
"type": z.string().describe("The [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) of the Linode you are creating.")
    })

export const postLinodeInstanceMutationResponseSchema = z.lazy(() => postLinodeInstance200Schema)