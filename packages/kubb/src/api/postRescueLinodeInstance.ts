/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from "../.kubb/fetch.ts";
import type { PostRescueLinodeInstanceMutationRequest, PostRescueLinodeInstanceMutationResponse, PostRescueLinodeInstancePathParams } from "../types/PostRescueLinodeInstance.ts";
import { fetch } from "../.kubb/fetch.ts";

function getPostRescueLinodeInstanceUrl(linodeId: PostRescueLinodeInstancePathParams["linodeId"]) {
  const res = { method: 'POST', url: `/linode/instances/${linodeId}/rescue` as const }  
  return res
}

/**
 * @description Rescue Mode is a safe environment for performing many system recovery and disk management tasks. Rescue Mode is based on the Finnix recovery distribution, a self-contained and bootable Linux distribution. You can also use Rescue Mode for tasks other than disaster recovery, such as formatting disks to use different filesystems, copying data between disks, and downloading files from a disk via SSH and SFTP.Linodes with legacy configuration interfaces receive a public IP and boot into the recovery Linux distribution. Linodes with Linode interfaces still boot into recovery mode with the recovery Linux distribution, but they retain their original network interfaces and settings from before entering rescue mode.- Note that `sdh` is reserved and unavailable during rescue.<<LB>>---- __CLI__.    ```    linode-cli linodes rescue 123 \  --devices.sda.disk_id 124458    ```    [Learn more...](https://techdocs.akamai.com/cloud-computing/docs/getting-started-with-the-linode-cli)- __OAuth scopes__.    ```    linodes:read_write    ```    [Learn more...](https://techdocs.akamai.com/linode-api/reference/get-started#oauth)
 * @summary Boot a Linode into rescue mode
 * {@link /linode/instances/:linodeId/rescue}
 */
export async function postRescueLinodeInstance(linodeId: PostRescueLinodeInstancePathParams["linodeId"], data?: PostRescueLinodeInstanceMutationRequest, config: Partial<RequestConfig<PostRescueLinodeInstanceMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<PostRescueLinodeInstanceMutationResponse, ResponseErrorConfig<Error>, PostRescueLinodeInstanceMutationRequest>({ method : "POST", url : getPostRescueLinodeInstanceUrl(linodeId).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}