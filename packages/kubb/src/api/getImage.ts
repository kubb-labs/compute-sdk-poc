/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from "../.kubb/fetch.ts";
import type { GetImageQueryResponse, GetImagePathParams } from "../types/GetImage.ts";
import { fetch } from "../.kubb/fetch.ts";

function getGetImageUrl(imageId: GetImagePathParams["imageId"]) {
  const res = { method: 'GET', url: `/images/${imageId}` as const }  
  return res
}

/**
 * @description Get information about a single image. An image can be one of two types:- **Public image**. The `id` for these images begins with `linode/`. These images are generally available to all users. To limit the response to public images, don't include [authentication](https://techdocs.akamai.com/linode-api/reference/get-started#authentication) when calling this operation.- **Private image**. The `id` for these images begins with `private/`. These images are account-specific and only accessible to users with appropriate [grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants). To view private images, you need to include authentication when calling this operation. The response will also include public images.- **Shared image**. The `id` for these images begins with `shared/`. These are the images that are shared with the user.<<LB>>---- __CLI__.    ```    linode-cli images view linode/debian9    ```    [Learn more...](https://techdocs.akamai.com/cloud-computing/docs/getting-started-with-the-linode-cli)- __OAuth scopes__.    ```    images:read_only    ```    [Learn more...](https://techdocs.akamai.com/linode-api/reference/get-started#oauth)
 * @summary Get an image
 * {@link /images/:imageId}
 */
export async function getImage(imageId: GetImagePathParams["imageId"], config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetImageQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetImageUrl(imageId).url.toString(), ... requestConfig })  
  return res.data
}