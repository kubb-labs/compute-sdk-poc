/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@kubb/plugin-client/clients/fetch";
import type { PostPromoCreditMutationRequest, PostPromoCreditMutationResponse, PostPromoCreditPathParams } from "../types/PostPromoCredit.ts";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/fetch";

function getPostPromoCreditUrl(apiVersion: PostPromoCreditPathParams["apiVersion"]) {
  const res = { method: 'POST', url: `/${apiVersion}/account/promo-codes` as const }  
  return res
}

/**
 * @description Adds an expiring Promo Credit to your account. The following restrictions apply:- Your account needs to be less than 90 days old.- You can't already have a Promo Credit on your account.- The user making the request needs to be unrestricted. You can run the [Update a user](https://techdocs.akamai.com/linode-api/reference/put-user) operation to change a user's restricted status.- The `promo_code` needs to be valid and unexpired.__Parent and child accounts__In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, the following apply:- Child account users can't run this operation. These users don't have access to billing-related operations.<<LB>>---- __CLI__.    ```    linode-cli account \  promo-add \  --promo-code abcdefABCDEF1234567890    ```    [Learn more...](https://techdocs.akamai.com/cloud-computing/docs/getting-started-with-the-linode-cli)- __OAuth scopes__.    ```    account:read_only    ```    [Learn more...](https://techdocs.akamai.com/linode-api/reference/get-started#oauth)
 * @summary Add a promo credit
 * {@link /:apiVersion/account/promo-codes}
 */
export async function postPromoCredit(apiVersion: PostPromoCreditPathParams["apiVersion"], data: PostPromoCreditMutationRequest, config: Partial<RequestConfig<PostPromoCreditMutationRequest>> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const requestData = data  
  
  const res = await request<PostPromoCreditMutationResponse, ResponseErrorConfig<Error>, PostPromoCreditMutationRequest>({ method : "POST", url : getPostPromoCreditUrl(apiVersion).url.toString(), data : requestData, ... requestConfig })  
  return res.data
}