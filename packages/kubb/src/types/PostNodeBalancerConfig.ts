/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export const postNodeBalancerConfigPathParamsApiVersionEnum = {
    "v4": "v4",
    "v4beta": "v4beta"
} as const;

export type PostNodeBalancerConfigPathParamsApiVersionEnumKey = (typeof postNodeBalancerConfigPathParamsApiVersionEnum)[keyof typeof postNodeBalancerConfigPathParamsApiVersionEnum];

export interface PostNodeBalancerConfigPathParams {
    /**
     * @description __Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta.
     * @type string
    */
    apiVersion: PostNodeBalancerConfigPathParamsApiVersionEnumKey;
    /**
     * @description The ID of the NodeBalancer.
     * @type integer
    */
    nodeBalancerId: number;
}

export const postNodeBalancerConfig200AlgorithmEnum = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "ring_hash": "ring_hash"
} as const;

export type PostNodeBalancerConfig200AlgorithmEnumKey = (typeof postNodeBalancerConfig200AlgorithmEnum)[keyof typeof postNodeBalancerConfig200AlgorithmEnum];

export const postNodeBalancerConfig200CheckEnum = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfig200CheckEnumKey = (typeof postNodeBalancerConfig200CheckEnum)[keyof typeof postNodeBalancerConfig200CheckEnum];

export const postNodeBalancerConfig200ProtocolEnum = {
    "udp": "udp"
} as const;

export type PostNodeBalancerConfig200ProtocolEnumKey = (typeof postNodeBalancerConfig200ProtocolEnum)[keyof typeof postNodeBalancerConfig200ProtocolEnum];

export const postNodeBalancerConfig200StickinessEnum = {
    "none": "none",
    "session": "session",
    "source_ip": "source_ip"
} as const;

export type PostNodeBalancerConfig200StickinessEnumKey = (typeof postNodeBalancerConfig200StickinessEnum)[keyof typeof postNodeBalancerConfig200StickinessEnum];

export const postNodeBalancerConfig200AlgorithmEnum2 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PostNodeBalancerConfig200AlgorithmEnum2Key = (typeof postNodeBalancerConfig200AlgorithmEnum2)[keyof typeof postNodeBalancerConfig200AlgorithmEnum2];

export const postNodeBalancerConfig200CheckEnum2 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfig200CheckEnum2Key = (typeof postNodeBalancerConfig200CheckEnum2)[keyof typeof postNodeBalancerConfig200CheckEnum2];

export const postNodeBalancerConfig200ProtocolEnum2 = {
    "tcp": "tcp"
} as const;

export type PostNodeBalancerConfig200ProtocolEnum2Key = (typeof postNodeBalancerConfig200ProtocolEnum2)[keyof typeof postNodeBalancerConfig200ProtocolEnum2];

export const postNodeBalancerConfig200ProxyProtocolEnum = {
    "none": "none",
    "v1": "v1",
    "v2": "v2"
} as const;

export type PostNodeBalancerConfig200ProxyProtocolEnumKey = (typeof postNodeBalancerConfig200ProxyProtocolEnum)[keyof typeof postNodeBalancerConfig200ProxyProtocolEnum];

export const postNodeBalancerConfig200StickinessEnum2 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PostNodeBalancerConfig200StickinessEnum2Key = (typeof postNodeBalancerConfig200StickinessEnum2)[keyof typeof postNodeBalancerConfig200StickinessEnum2];

export const postNodeBalancerConfig200AlgorithmEnum3 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PostNodeBalancerConfig200AlgorithmEnum3Key = (typeof postNodeBalancerConfig200AlgorithmEnum3)[keyof typeof postNodeBalancerConfig200AlgorithmEnum3];

export const postNodeBalancerConfig200CheckEnum3 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfig200CheckEnum3Key = (typeof postNodeBalancerConfig200CheckEnum3)[keyof typeof postNodeBalancerConfig200CheckEnum3];

export const postNodeBalancerConfig200ProtocolEnum3 = {
    "http": "http"
} as const;

export type PostNodeBalancerConfig200ProtocolEnum3Key = (typeof postNodeBalancerConfig200ProtocolEnum3)[keyof typeof postNodeBalancerConfig200ProtocolEnum3];

export const postNodeBalancerConfig200StickinessEnum3 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PostNodeBalancerConfig200StickinessEnum3Key = (typeof postNodeBalancerConfig200StickinessEnum3)[keyof typeof postNodeBalancerConfig200StickinessEnum3];

export const postNodeBalancerConfig200AlgorithmEnum4 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PostNodeBalancerConfig200AlgorithmEnum4Key = (typeof postNodeBalancerConfig200AlgorithmEnum4)[keyof typeof postNodeBalancerConfig200AlgorithmEnum4];

export const postNodeBalancerConfig200CheckEnum4 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfig200CheckEnum4Key = (typeof postNodeBalancerConfig200CheckEnum4)[keyof typeof postNodeBalancerConfig200CheckEnum4];

export const postNodeBalancerConfig200CipherSuiteEnum = {
    "recommended": "recommended",
    "legacy": "legacy"
} as const;

export type PostNodeBalancerConfig200CipherSuiteEnumKey = (typeof postNodeBalancerConfig200CipherSuiteEnum)[keyof typeof postNodeBalancerConfig200CipherSuiteEnum];

export const postNodeBalancerConfig200ProtocolEnum4 = {
    "https": "https"
} as const;

export type PostNodeBalancerConfig200ProtocolEnum4Key = (typeof postNodeBalancerConfig200ProtocolEnum4)[keyof typeof postNodeBalancerConfig200ProtocolEnum4];

export const postNodeBalancerConfig200StickinessEnum4 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PostNodeBalancerConfig200StickinessEnum4Key = (typeof postNodeBalancerConfig200StickinessEnum4)[keyof typeof postNodeBalancerConfig200StickinessEnum4];

/**
 * @description Config created successfully.
*/
export type PostNodeBalancerConfig200 = ({
    /**
     * @description The algorithm this UDP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfig200AlgorithmEnumKey;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfig200CheckEnumKey;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value isn\'t present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description __Read-only__ Must be `false` for UDP.
     * @type boolean | undefined
    */
    readonly check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `udp` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfig200ProtocolEnumKey;
    /**
     * @description __Read-only__ Must be `none` for UDP.
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for UPD configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are assigned a backend server based on the algorithm configured.\n- If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.\n- If set to `source_ip`, the NodeBalancer uses the client\'s source IP address to route all packets from the same client to the same backend server.
     * @default "session"
     * @type string | undefined
    */
    stickiness?: PostNodeBalancerConfig200StickinessEnumKey;
    /**
     * @description UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    udp_check_port?: number;
    /**
     * @description __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
     * @minLength 1
     * @maxLength 300
     * @default 16
     * @type integer | undefined
    */
    readonly udp_session_timeout?: number;
} | {
    /**
     * @description The algorithm this TCP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfig200AlgorithmEnum2Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfig200CheckEnum2Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `tcp` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfig200ProtocolEnum2Key;
    /**
     * @description Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.\n\n- If omitted, or set to `none`, the NodeBalancer doesn\'t send any auxiliary data over TCP connections. This is the default.\n- If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.\n- If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
     * @default "none"
     * @type string | undefined
    */
    proxy_protocol?: PostNodeBalancerConfig200ProxyProtocolEnumKey;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description __Read-only__ Controls how session stickiness is handled on this port.\n\nNot applicable to TCP configurations.
     * @default "none"
     * @type string | undefined
    */
    readonly stickiness?: PostNodeBalancerConfig200StickinessEnum2Key;
} | {
    /**
     * @description The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfig200AlgorithmEnum3Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfig200CheckEnum3Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `http` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ Identifies the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced. You may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfig200ProtocolEnum3Key;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are always assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address are routed to the same backend.\n- If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PostNodeBalancerConfig200StickinessEnum3Key;
} | {
    /**
     * @description The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfig200AlgorithmEnum4Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- If `none` no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfig200CheckEnum4Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description The number of seconds between each check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
     * @default "recommended"
     * @type string | undefined
    */
    cipher_suite?: PostNodeBalancerConfig200CipherSuiteEnumKey;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfig200ProtocolEnum4Key;
    /**
     * @description __Read-only__ Not applicable for HTTPS configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description \nThe PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig\'s port.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\n[Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-cert
    */
    ssl_cert?: string | null;
    /**
     * @description __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\nThe contents of this field will not be shown in any responses that display\nthe NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field\nappears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig\nresponse are automatically derived from your certificate. Please refer to these fields to\nverify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-key
    */
    ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PostNodeBalancerConfig200StickinessEnum4Key;
});

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
*/
export interface PostNodeBalancerConfigError {
    /**
     * @type array | undefined
    */
    errors?: {
        /**
         * @description The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
         * @type string | undefined
        */
        field?: string;
        /**
         * @description What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
         * @type string | undefined
        */
        reason?: string;
    }[];
}

export const postNodeBalancerConfigMutationRequestAlgorithmEnum = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "ring_hash": "ring_hash"
} as const;

export type PostNodeBalancerConfigMutationRequestAlgorithmEnumKey = (typeof postNodeBalancerConfigMutationRequestAlgorithmEnum)[keyof typeof postNodeBalancerConfigMutationRequestAlgorithmEnum];

export const postNodeBalancerConfigMutationRequestCheckEnum = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfigMutationRequestCheckEnumKey = (typeof postNodeBalancerConfigMutationRequestCheckEnum)[keyof typeof postNodeBalancerConfigMutationRequestCheckEnum];

export const nodesModeEnum5 = {
    "none": "none"
} as const;

export type NodesModeEnum5Key = (typeof nodesModeEnum5)[keyof typeof nodesModeEnum5];

export const nodesStatusEnum9 = {
    "unknown": "unknown",
    "UP": "UP",
    "DOWN": "DOWN"
} as const;

export type NodesStatusEnum9Key = (typeof nodesStatusEnum9)[keyof typeof nodesStatusEnum9];

export const postNodeBalancerConfigMutationRequestProtocolEnum = {
    "udp": "udp"
} as const;

export type PostNodeBalancerConfigMutationRequestProtocolEnumKey = (typeof postNodeBalancerConfigMutationRequestProtocolEnum)[keyof typeof postNodeBalancerConfigMutationRequestProtocolEnum];

export const postNodeBalancerConfigMutationRequestStickinessEnum = {
    "none": "none",
    "session": "session",
    "source_ip": "source_ip"
} as const;

export type PostNodeBalancerConfigMutationRequestStickinessEnumKey = (typeof postNodeBalancerConfigMutationRequestStickinessEnum)[keyof typeof postNodeBalancerConfigMutationRequestStickinessEnum];

export const postNodeBalancerConfigMutationRequestAlgorithmEnum2 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PostNodeBalancerConfigMutationRequestAlgorithmEnum2Key = (typeof postNodeBalancerConfigMutationRequestAlgorithmEnum2)[keyof typeof postNodeBalancerConfigMutationRequestAlgorithmEnum2];

export const postNodeBalancerConfigMutationRequestCheckEnum2 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfigMutationRequestCheckEnum2Key = (typeof postNodeBalancerConfigMutationRequestCheckEnum2)[keyof typeof postNodeBalancerConfigMutationRequestCheckEnum2];

export const nodesModeEnum6 = {
    "accept": "accept",
    "reject": "reject",
    "drain": "drain",
    "backup": "backup"
} as const;

export type NodesModeEnum6Key = (typeof nodesModeEnum6)[keyof typeof nodesModeEnum6];

export const nodesStatusEnum10 = {
    "unknown": "unknown",
    "UP": "UP",
    "DOWN": "DOWN"
} as const;

export type NodesStatusEnum10Key = (typeof nodesStatusEnum10)[keyof typeof nodesStatusEnum10];

export const postNodeBalancerConfigMutationRequestProtocolEnum2 = {
    "tcp": "tcp"
} as const;

export type PostNodeBalancerConfigMutationRequestProtocolEnum2Key = (typeof postNodeBalancerConfigMutationRequestProtocolEnum2)[keyof typeof postNodeBalancerConfigMutationRequestProtocolEnum2];

export const postNodeBalancerConfigMutationRequestProxyProtocolEnum = {
    "none": "none",
    "v1": "v1",
    "v2": "v2"
} as const;

export type PostNodeBalancerConfigMutationRequestProxyProtocolEnumKey = (typeof postNodeBalancerConfigMutationRequestProxyProtocolEnum)[keyof typeof postNodeBalancerConfigMutationRequestProxyProtocolEnum];

export const postNodeBalancerConfigMutationRequestStickinessEnum2 = {
    "none": "none",
    "table": "table"
} as const;

export type PostNodeBalancerConfigMutationRequestStickinessEnum2Key = (typeof postNodeBalancerConfigMutationRequestStickinessEnum2)[keyof typeof postNodeBalancerConfigMutationRequestStickinessEnum2];

export const postNodeBalancerConfigMutationRequestAlgorithmEnum3 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PostNodeBalancerConfigMutationRequestAlgorithmEnum3Key = (typeof postNodeBalancerConfigMutationRequestAlgorithmEnum3)[keyof typeof postNodeBalancerConfigMutationRequestAlgorithmEnum3];

export const postNodeBalancerConfigMutationRequestCheckEnum3 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfigMutationRequestCheckEnum3Key = (typeof postNodeBalancerConfigMutationRequestCheckEnum3)[keyof typeof postNodeBalancerConfigMutationRequestCheckEnum3];

export const nodesModeEnum7 = {
    "accept": "accept",
    "reject": "reject",
    "drain": "drain",
    "backup": "backup"
} as const;

export type NodesModeEnum7Key = (typeof nodesModeEnum7)[keyof typeof nodesModeEnum7];

export const nodesStatusEnum11 = {
    "unknown": "unknown",
    "UP": "UP",
    "DOWN": "DOWN"
} as const;

export type NodesStatusEnum11Key = (typeof nodesStatusEnum11)[keyof typeof nodesStatusEnum11];

export const postNodeBalancerConfigMutationRequestProtocolEnum3 = {
    "http": "http"
} as const;

export type PostNodeBalancerConfigMutationRequestProtocolEnum3Key = (typeof postNodeBalancerConfigMutationRequestProtocolEnum3)[keyof typeof postNodeBalancerConfigMutationRequestProtocolEnum3];

export const postNodeBalancerConfigMutationRequestStickinessEnum3 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PostNodeBalancerConfigMutationRequestStickinessEnum3Key = (typeof postNodeBalancerConfigMutationRequestStickinessEnum3)[keyof typeof postNodeBalancerConfigMutationRequestStickinessEnum3];

export const postNodeBalancerConfigMutationRequestAlgorithmEnum4 = {
    "roundrobin": "roundrobin",
    "leastconn": "leastconn",
    "source": "source"
} as const;

export type PostNodeBalancerConfigMutationRequestAlgorithmEnum4Key = (typeof postNodeBalancerConfigMutationRequestAlgorithmEnum4)[keyof typeof postNodeBalancerConfigMutationRequestAlgorithmEnum4];

export const postNodeBalancerConfigMutationRequestCheckEnum4 = {
    "none": "none",
    "connection": "connection",
    "http": "http",
    "http_body": "http_body"
} as const;

export type PostNodeBalancerConfigMutationRequestCheckEnum4Key = (typeof postNodeBalancerConfigMutationRequestCheckEnum4)[keyof typeof postNodeBalancerConfigMutationRequestCheckEnum4];

export const postNodeBalancerConfigMutationRequestCipherSuiteEnum = {
    "recommended": "recommended",
    "legacy": "legacy"
} as const;

export type PostNodeBalancerConfigMutationRequestCipherSuiteEnumKey = (typeof postNodeBalancerConfigMutationRequestCipherSuiteEnum)[keyof typeof postNodeBalancerConfigMutationRequestCipherSuiteEnum];

export const nodesModeEnum8 = {
    "accept": "accept",
    "reject": "reject",
    "drain": "drain",
    "backup": "backup"
} as const;

export type NodesModeEnum8Key = (typeof nodesModeEnum8)[keyof typeof nodesModeEnum8];

export const nodesStatusEnum12 = {
    "unknown": "unknown",
    "UP": "UP",
    "DOWN": "DOWN"
} as const;

export type NodesStatusEnum12Key = (typeof nodesStatusEnum12)[keyof typeof nodesStatusEnum12];

export const postNodeBalancerConfigMutationRequestProtocolEnum4 = {
    "https": "https"
} as const;

export type PostNodeBalancerConfigMutationRequestProtocolEnum4Key = (typeof postNodeBalancerConfigMutationRequestProtocolEnum4)[keyof typeof postNodeBalancerConfigMutationRequestProtocolEnum4];

export const postNodeBalancerConfigMutationRequestStickinessEnum4 = {
    "none": "none",
    "table": "table",
    "http_cookie": "http_cookie"
} as const;

export type PostNodeBalancerConfigMutationRequestStickinessEnum4Key = (typeof postNodeBalancerConfigMutationRequestStickinessEnum4)[keyof typeof postNodeBalancerConfigMutationRequestStickinessEnum4];

export const nodesModeEnum9 = {
    "accept": "accept",
    "reject": "reject",
    "drain": "drain",
    "backup": "backup"
} as const;

export type NodesModeEnum9Key = (typeof nodesModeEnum9)[keyof typeof nodesModeEnum9];

/**
 * @description NodeBalancer configuration details for the port based on the routing protocol.
*/
export type PostNodeBalancerConfigMutationRequest = (({
    /**
     * @description The algorithm this UDP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfigMutationRequestAlgorithmEnumKey;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfigMutationRequestCheckEnumKey;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value isn\'t present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description __Read-only__ Must be `false` for UDP.
     * @type boolean | undefined
    */
    readonly check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description The NodeBalancer nodes that serve this configuration.
     * @type array
    */
    nodes: {
        /**
         * @description Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:\n  - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.\n  - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.\n  - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         * @type string | undefined, ip
        */
        address?: string;
        /**
         * @description __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly config_id?: number;
        /**
         * @description __Read-only__ This node\'s unique ID.
         * @type integer | undefined
        */
        readonly id?: number;
        /**
         * @description The label for this node.  This is for display purposes only.
         * @minLength 3
         * @maxLength 32
         * @pattern [a-zA-Z0-9-_.]{3,32}
         * @type string | undefined
        */
        label?: string;
        /**
         * @description __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn\'t apply, so the value is `none`.
         * @type string | undefined
        */
        readonly mode?: NodesModeEnum5Key;
        /**
         * @description __Read-only__ The NodeBalancer ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly nodebalancer_id?: number;
        /**
         * @description __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
         * @type string | undefined
        */
        readonly status?: NodesStatusEnum9Key;
        /**
         * @description Required for VPC backend nodes only. The VPC\'s subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         * @type integer | undefined
        */
        subnet_id?: number;
        /**
         * @description Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         * @minLength 1
         * @maxLength 255
         * @type integer | undefined
        */
        weight?: number;
    }[];
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfigMutationRequestProtocolEnumKey;
    /**
     * @description __Read-only__ Must be `none` for UDP.
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for UPD configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for UDP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections are assigned a backend server based on the algorithm configured.\n- If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.\n- If set to `source_ip`, the NodeBalancer uses the client\'s source IP address to route all packets from the same client to the same backend server.
     * @default "session"
     * @type string | undefined
    */
    stickiness?: PostNodeBalancerConfigMutationRequestStickinessEnumKey;
    /**
     * @description UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    udp_check_port?: number;
    /**
     * @description __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
     * @minLength 1
     * @maxLength 300
     * @default 16
     * @type integer | undefined
    */
    readonly udp_session_timeout?: number;
} | {
    /**
     * @description The algorithm this TCP NodeBalancer uses to route traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfigMutationRequestAlgorithmEnum2Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfigMutationRequestCheckEnum2Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. If the backend does not respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 30
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `tcp` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description The NodeBalancer nodes that serve this configuration.
     * @type array
    */
    nodes: {
        /**
         * @description Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:\n  - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.\n  - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.\n  - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         * @type string | undefined, ip
        */
        address?: string;
        /**
         * @description __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly config_id?: number;
        /**
         * @description __Read-only__ This node\'s unique ID.
         * @type integer | undefined
        */
        readonly id?: number;
        /**
         * @description The label for this node.  This is for display purposes only.
         * @minLength 3
         * @maxLength 32
         * @pattern [a-zA-Z0-9-_.]{3,32}
         * @type string | undefined
        */
        label?: string;
        /**
         * @description The mode this NodeBalancer should use when sending traffic to this backend.\n\n- If set to `accept` this backend is accepting traffic.\n- If set to `reject` this backend will not receive traffic.\n- If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.\n- If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         * @type string | undefined
        */
        mode?: NodesModeEnum6Key;
        /**
         * @description __Read-only__ The NodeBalancer ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly nodebalancer_id?: number;
        /**
         * @description __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         * @type string | undefined
        */
        readonly status?: NodesStatusEnum10Key;
        /**
         * @description Required for VPC backend nodes only. The VPC\'s subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         * @type integer | undefined
        */
        subnet_id?: number;
        /**
         * @description Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         * @minLength 1
         * @maxLength 255
         * @type integer | undefined
        */
        weight?: number;
    }[];
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfigMutationRequestProtocolEnum2Key;
    /**
     * @description Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.\n\n- If omitted, or set to `none`, the NodeBalancer doesn\'t send any auxiliary data over TCP connections. This is the default.\n- If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.\n- If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
     * @default "none"
     * @type string | undefined
    */
    proxy_protocol?: PostNodeBalancerConfigMutationRequestProxyProtocolEnumKey;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for TCP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description __Read-only__ Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.
     * @default "none"
     * @type string | undefined
    */
    readonly stickiness?: PostNodeBalancerConfigMutationRequestStickinessEnum2Key;
} | {
    /**
     * @description The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfigMutationRequestAlgorithmEnum3Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.\n\n- If `none`, no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfigMutationRequestCheckEnum3Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description __Read-only__ Not applicable for `http` configs.
     * @type string | undefined
    */
    readonly cipher_suite?: string;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ Identifies the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description The NodeBalancer nodes that serve this configuration.
     * @type array
    */
    nodes: {
        /**
         * @description Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:\n  - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.\n  - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.\n  - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         * @type string | undefined, ip
        */
        address?: string;
        /**
         * @description __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly config_id?: number;
        /**
         * @description __Read-only__ This node\'s unique ID.
         * @type integer | undefined
        */
        readonly id?: number;
        /**
         * @description The label for this node.  This is for display purposes only.
         * @minLength 3
         * @maxLength 32
         * @pattern [a-zA-Z0-9-_.]{3,32}
         * @type string | undefined
        */
        label?: string;
        /**
         * @description The mode this NodeBalancer should use when sending traffic to this backend.\n\n- If set to `accept` this backend is accepting traffic.\n- If set to `reject` this backend will not receive traffic.\n- If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.\n- If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         * @type string | undefined
        */
        mode?: NodesModeEnum7Key;
        /**
         * @description __Read-only__ The NodeBalancer ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly nodebalancer_id?: number;
        /**
         * @description __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         * @type string | undefined
        */
        readonly status?: NodesStatusEnum11Key;
        /**
         * @description Required for VPC backend nodes only. The VPC\'s subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         * @type integer | undefined
        */
        subnet_id?: number;
        /**
         * @description Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         * @minLength 1
         * @maxLength 255
         * @type integer | undefined
        */
        weight?: number;
    }[];
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced. You may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfigMutationRequestProtocolEnum3Key;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_cert?: string | null;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description __Read-only__ Not applicable for HTTP configs.
     * @type string
    */
    readonly ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PostNodeBalancerConfigMutationRequestStickinessEnum3Key;
} | {
    /**
     * @description The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
     * @default "roundrobin"
     * @type string | undefined
    */
    algorithm?: PostNodeBalancerConfigMutationRequestAlgorithmEnum4Key;
    /**
     * @description The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.\n\n- If `none` no check is performed.\n- `connection` requires only a connection to the backend to succeed.\n- `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
     * @default "none"
     * @type string | undefined
    */
    check?: PostNodeBalancerConfigMutationRequestCheckEnum4Key;
    /**
     * @description How many times to attempt a check before considering a backend to be down.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_attempts?: number;
    /**
     * @description Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
     * @type string | undefined
    */
    check_body?: string;
    /**
     * @description How often, in seconds, to check that backends are up and serving requests.\n\nMust be greater than `check_timeout`.
     * @minLength 2
     * @maxLength 3600
     * @default 5
     * @type integer | undefined
    */
    check_interval?: number;
    /**
     * @description If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
     * @default true
     * @type boolean | undefined
    */
    check_passive?: boolean;
    /**
     * @description The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn\'t respond to this request, it\'s considered to be down.
     * @pattern ^[a-zA-Z0-9\/\-%?&=.]*$
     * @type string | undefined
    */
    check_path?: string;
    /**
     * @description How long, in seconds, to wait for a check attempt before considering it failed.\n\nMust be less than `check_interval`.
     * @minLength 1
     * @maxLength 30
     * @default 3
     * @type integer | undefined
    */
    check_timeout?: number;
    /**
     * @description What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
     * @default "recommended"
     * @type string | undefined
    */
    cipher_suite?: PostNodeBalancerConfigMutationRequestCipherSuiteEnumKey;
    /**
     * @description __Read-only__ This config\'s unique ID.
     * @type integer | undefined
    */
    readonly id?: number;
    /**
     * @description __Read-only__ The ID for the NodeBalancer this config belongs to.
     * @type integer | undefined
    */
    readonly nodebalancer_id?: number;
    /**
     * @description The NodeBalancer nodes that serve this configuration.
     * @type array
    */
    nodes: {
        /**
         * @description Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:\n  - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.\n  - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.\n  - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         * @type string | undefined, ip
        */
        address?: string;
        /**
         * @description __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly config_id?: number;
        /**
         * @description __Read-only__ This node\'s unique ID.
         * @type integer | undefined
        */
        readonly id?: number;
        /**
         * @description The label for this node.  This is for display purposes only.
         * @minLength 3
         * @maxLength 32
         * @pattern [a-zA-Z0-9-_.]{3,32}
         * @type string | undefined
        */
        label?: string;
        /**
         * @description The mode this NodeBalancer should use when sending traffic to this backend.\n\n- If set to `accept` this backend is accepting traffic.\n- If set to `reject` this backend will not receive traffic.\n- If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.\n- If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         * @type string | undefined
        */
        mode?: NodesModeEnum8Key;
        /**
         * @description __Read-only__ The NodeBalancer ID that this Node belongs to.
         * @type integer | undefined
        */
        readonly nodebalancer_id?: number;
        /**
         * @description __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         * @type string | undefined
        */
        readonly status?: NodesStatusEnum12Key;
        /**
         * @description Required for VPC backend nodes only. The VPC\'s subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         * @type integer | undefined
        */
        subnet_id?: number;
        /**
         * @description Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         * @minLength 1
         * @maxLength 255
         * @type integer | undefined
        */
        weight?: number;
    }[];
    /**
     * @description __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
     * @type object | undefined
    */
    readonly nodes_status?: {
        /**
         * @description __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
         * @type integer | undefined
        */
        readonly down?: number;
        /**
         * @description __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
         * @type integer | undefined
        */
        readonly up?: number;
    };
    /**
     * @description This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can\'t be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn\'t strictly enforced, and you may configure your NodeBalancer however you find useful.
     * @minLength 1
     * @maxLength 65535
     * @default 80
     * @type integer | undefined
    */
    port?: number;
    /**
     * @description The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
     * @default "http"
     * @type string | undefined
    */
    protocol?: PostNodeBalancerConfigMutationRequestProtocolEnum4Key;
    /**
     * @description __Read-only__ Not applicable for HTTPS configs.
     * @default "none"
     * @type string | undefined
    */
    readonly proxy_protocol?: string;
    /**
     * @description \nThe PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig\'s port.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\n[Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.\n\nThe contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-cert
    */
    ssl_cert?: string | null;
    /**
     * @description __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
     * @type string | undefined
    */
    readonly ssl_commonname?: string;
    /**
     * @description __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
     * @type string | undefined
    */
    readonly ssl_fingerprint?: string;
    /**
     * @description The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.\n\nLine breaks must be represented as `\\n` in the string for requests (but not when using the Linode CLI).\n\nThe contents of this field will not be shown in any responses that display\nthe NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field\nappears.\n\nThe read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig\nresponse are automatically derived from your certificate. Please refer to these fields to\nverify that the appropriate certificate was assigned to your NodeBalancerConfig.
     * @type string, ssl-key
    */
    ssl_key?: string | null;
    /**
     * @description Controls how session stickiness is handled on this port.\n\n- If set to `none`, connections will always be assigned a backend based on the algorithm configured.\n- If set to `table`, sessions from the same remote address will be routed to the same backend.\n- For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
     * @default "table"
     * @type string | undefined
    */
    stickiness?: PostNodeBalancerConfigMutationRequestStickinessEnum4Key;
}) & {
    /**
     * @description The NodeBalancer nodes that serve this config.\n\nSome considerations for Nodes when rebuilding a config:\n\n  - Current Nodes excluded from the request body will be deleted from the Config.\n  - Current Nodes (identified by their Node ID) will be updated.\n  - New Nodes (included without a Node ID) will be created.
     * @type array
    */
    nodes: {
        /**
         * @description Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:\n  - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.\n  - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.\n  - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         * @type string | undefined, ip
        */
        address?: string;
        /**
         * @description The unique ID of the Node to update.
         * @type integer | undefined
        */
        id?: number;
        /**
         * @description The label for this node.  This is for display purposes only.
         * @minLength 3
         * @maxLength 32
         * @pattern [a-zA-Z0-9-_.]{3,32}
         * @type string | undefined
        */
        label?: string;
        /**
         * @description The mode this NodeBalancer should use when sending traffic to this backend.\n\n- If set to `accept` this backend is accepting traffic.\n- If set to `reject` this backend will not receive traffic.\n- If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.\n- If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         * @type string | undefined
        */
        mode?: NodesModeEnum9Key;
        /**
         * @description Required for VPC backend nodes only. The VPC\'s subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         * @type integer | undefined
        */
        subnet_id?: number;
        /**
         * @description Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         * @minLength 1
         * @maxLength 255
         * @type integer | undefined
        */
        weight?: number;
    }[];
});

export type PostNodeBalancerConfigMutationResponse = PostNodeBalancerConfig200;

export type PostNodeBalancerConfigMutation = {
    Response: PostNodeBalancerConfig200;
    Request: PostNodeBalancerConfigMutationRequest;
    PathParams: PostNodeBalancerConfigPathParams;
    Errors: any;
};