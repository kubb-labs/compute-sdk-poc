/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export interface PostLkeClusterPoolsPathParams {
    /**
     * @description ID of the Kubernetes cluster to look up.
     * @type integer
    */
    clusterId: number;
}

export const postLkeClusterPools200DiskEncryptionEnum = {
    "enabled": "enabled",
    "disabled": "disabled"
} as const;

export type PostLkeClusterPools200DiskEncryptionEnumKey = (typeof postLkeClusterPools200DiskEncryptionEnum)[keyof typeof postLkeClusterPools200DiskEncryptionEnum];

export const disksTypeEnum2 = {
    "raw": "raw",
    "ext4": "ext4"
} as const;

export type DisksTypeEnum2Key = (typeof disksTypeEnum2)[keyof typeof disksTypeEnum2];

export const nodesStatusEnum = {
    "ready": "ready",
    "not_ready": "not_ready"
} as const;

export type NodesStatusEnumKey = (typeof nodesStatusEnum)[keyof typeof nodesStatusEnum];

export const taintsEffectEnum2 = {
    "NoSchedule": "NoSchedule",
    "PreferNoSchedule": "PreferNoSchedule",
    "NoExecute": "NoExecute"
} as const;

export type TaintsEffectEnum2Key = (typeof taintsEffectEnum2)[keyof typeof taintsEffectEnum2];

export const postLkeClusterPools200UpdateStrategyEnum = {
    "rolling_update": "rolling_update",
    "on_recycle": "on_recycle"
} as const;

export type PostLkeClusterPools200UpdateStrategyEnumKey = (typeof postLkeClusterPools200UpdateStrategyEnum)[keyof typeof postLkeClusterPools200UpdateStrategyEnum];

/**
 * @description Node Pool has been created.
*/
export interface PostLkeClusterPools200 {
    /**
     * @description When enabled, the number of nodes autoscales within the defined minimum and maximum values.
     * @type object
    */
    autoscaler: {
        /**
         * @description Whether autoscaling is enabled for this node pool. Defaults to `false`.
         * @type boolean
        */
        enabled: boolean;
        /**
         * @description The maximum number of nodes to autoscale to. Defaults to the node pool\'s `count`.
         * @minLength 1
         * @maxLength 100
         * @type integer
        */
        max: number;
        /**
         * @description The minimum number of nodes to autoscale to. Defaults to the node pool\'s `count`.
         * @minLength 1
         * @maxLength 100
         * @type integer
        */
        min: number;
    };
    /**
     * @description The number of nodes in the node pool.
     * @minLength 1
     * @maxLength 100
     * @type integer
    */
    count: number;
    /**
     * @description Indicates the local disk encryption setting for this LKE node pool.
     * @type string
    */
    disk_encryption: PostLkeClusterPools200DiskEncryptionEnumKey;
    /**
     * @description This node pool\'s custom disk layout.
     * @type array
    */
    disks: {
        /**
         * @description The size of this custom disk partition in MB. The size of this disk partition can\'t exceed the capacity of the node\'s plan type.
         * @type integer
        */
        size: number;
        /**
         * @description This custom disk partition\'s filesystem type.
         * @type string
        */
        type: DisksTypeEnum2Key;
    }[];
    /**
     * @description __Filterable__ This node pool\'s unique ID.
     * @type integer
    */
    id: number;
    /**
     * @description __Beta__ The Kubernetes version used for the worker nodes within this node pool.\n> 🚧\n>\n> This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
     * @type string
    */
    k8s_version: string;
    /**
     * @description __Beta__ The optional label defined for this node Pool.\n\n> 🚧\n>\n> This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
     * @minLength 1
     * @maxLength 32
     * @type string
    */
    label: string;
    /**
     * @description Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
     * @type object
    */
    labels: {
        [key: string]: string;
    };
    /**
     * @description Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`.
     * @type array
    */
    nodes: {
        /**
         * @description The Node\'s ID.
         * @type string
        */
        id: string;
        /**
         * @description The Linode\'s ID. When no Linode is currently provisioned for this Node, this will be `null`.
         * @type integer
        */
        instance_id: number | null;
        /**
         * @description The creation status of this Node. This status is distinct from this Node\'s readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.\n\n`not_ready` indicates that the Linode is still being created.\n\n`ready` indicates that the Linode has successfully been created and is running Kubernetes software.
         * @type string
        */
        status: NodesStatusEnumKey;
    }[];
    /**
     * @description __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
     * @type array
    */
    tags: string[];
    /**
     * @description Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods.
     * @type array
    */
    taints: {
        /**
         * @description The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
         * @type string | undefined
        */
        effect?: TaintsEffectEnum2Key;
        /**
         * @description The Kubernetes taint key.
         * @minLength 1
         * @maxLength 63
         * @pattern ^([A-Za-z0-9][-A-Za-z0-9_.]*)?(\/)?([A-Za-z0-9][-A-Za-z0-9_.]{1,63})?$
         * @type string | undefined
        */
        key?: string;
        /**
         * @description The Kubernetes taint value.
         * @minLength 0
         * @maxLength 63
         * @pattern ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?
         * @type string | undefined
        */
        value?: string;
    }[];
    /**
     * @description The Linode Type for all of the nodes in the node pool.
     * @type string
    */
    type: string;
    /**
     * @description __Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.
     * @type string
    */
    update_strategy: PostLkeClusterPools200UpdateStrategyEnumKey;
}

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
*/
export interface PostLkeClusterPoolsError {
    /**
     * @type array
    */
    errors: {
        /**
         * @description The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
         * @type string
        */
        field: string;
        /**
         * @description What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
         * @type string
        */
        reason: string;
    }[];
}

export const postLkeClusterPoolsMutationRequestDiskEncryptionEnum = {
    "enabled": "enabled",
    "disabled": "disabled"
} as const;

export type PostLkeClusterPoolsMutationRequestDiskEncryptionEnumKey = (typeof postLkeClusterPoolsMutationRequestDiskEncryptionEnum)[keyof typeof postLkeClusterPoolsMutationRequestDiskEncryptionEnum];

export const disksTypeEnum3 = {
    "raw": "raw",
    "ext4": "ext4"
} as const;

export type DisksTypeEnum3Key = (typeof disksTypeEnum3)[keyof typeof disksTypeEnum3];

export const taintsEffectEnum3 = {
    "NoSchedule": "NoSchedule",
    "PreferNoSchedule": "PreferNoSchedule",
    "NoExecute": "NoExecute"
} as const;

export type TaintsEffectEnum3Key = (typeof taintsEffectEnum3)[keyof typeof taintsEffectEnum3];

export const postLkeClusterPoolsMutationRequestUpdateStrategyEnum = {
    "rolling_update": "rolling_update",
    "on_recycle": "on_recycle"
} as const;

export type PostLkeClusterPoolsMutationRequestUpdateStrategyEnumKey = (typeof postLkeClusterPoolsMutationRequestUpdateStrategyEnum)[keyof typeof postLkeClusterPoolsMutationRequestUpdateStrategyEnum];

/**
 * @description Configuration for the Node Pool.
*/
export interface PostLkeClusterPoolsMutationRequest {
    /**
     * @description When enabled, the number of nodes automatically scales within the defined minimum and maximum values. When making a request, `max` and `min` require each other.
     * @type object | undefined
    */
    autoscaler?: {
        /**
         * @description Whether automatic scaling is enabled for this node pool. Defaults to `false`.
         * @type boolean | undefined
        */
        enabled?: boolean;
        /**
         * @description The maximum number of nodes to automatically scale to. Defaults to the value provided by the `count` field.
         * @minLength 1
         * @maxLength 100
         * @type integer | undefined
        */
        max?: number;
        /**
         * @description The minimum number of nodes to automatically scale to. Defaults to the node pool\'s `count`.
         * @minLength 1
         * @maxLength 100
         * @type integer | undefined
        */
        min?: number;
    };
    /**
     * @description The number of nodes in the node pool.
     * @minLength 1
     * @maxLength 100
     * @type integer
    */
    count: number;
    /**
     * @description Local disk encryption setting for this LKE node pool. Defaults to `disabled`.
     * @default "disabled"
     * @type string | undefined
    */
    disk_encryption?: PostLkeClusterPoolsMutationRequestDiskEncryptionEnumKey;
    /**
     * @description This node pool\'s custom disk layout. Each item in this array will create a new disk partition for each node in this node pool.\n\n> 📘\n>\n> Omit this field, except for special use cases. The disks specified here are partitions in _addition_ to the primary partition and reduce the size of the primary partition. This can lead to stability problems for the node.\n\n  - The custom disk layout is applied to each node in this node pool.\n\n  - The maximum number of custom disk partitions that can be configured is 7.\n\n  - Once the requested disk partitions are allocated, the remaining disk space is allocated to the node\'s boot disk.\n\n  - A node pool\'s custom disk layout is immutable over the lifetime of the node pool.
     * @type array | undefined
    */
    disks?: {
        /**
         * @description The size of this custom disk partition in MB. The size of this disk partition can\'t exceed the capacity of the node\'s plan type.
         * @type integer | undefined
        */
        size?: number;
        /**
         * @description This custom disk partition\'s filesystem type.
         * @type string | undefined
        */
        type?: DisksTypeEnum3Key;
    }[];
    /**
     * @description __Beta__, __LKE Enterprise__ The LKE-specific Kubernetes version to use for the worker nodes within this node pool. This field is required when creating node pools on LKE Enterprise clusters.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
     * @type string | undefined
    */
    k8s_version?: string;
    /**
     * @description __Beta__ An optional label for this node pool. Labels can be up to 32 characters long, containing lowercase alphanumeric characters optionally interspersed with dashes.\n\nA node pool\'s label also cascades down to its nodes (Linodes). If a label is defined on the node pool, the label for each node is `<cluster-id>-<nodepool-label>-<hash>`. If not, the label is `<cluster-id>-<nodepool-id>-<hash>`.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
     * @minLength 1
     * @maxLength 32
     * @pattern ^[a-z0-9]([-a-z0-9]*[a-z0-9])$
     * @type string | undefined
    */
    label?: string;
    /**
     * @description Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).\n\n**Label key:**\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the maximum total length of the label key is 128 characters. Domain labels can be up to 62 characters after the \'/\'. The prefix needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions.\n\n- If the key doesn\'t begin with a DNS subdomain prefix, the maximum key length is 63 characters.\n\nSpecifying an empty object removes all previously set labels.\n\n**Label value:**\n\n- The label\'s value can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- Can be specified as an empty string value with `\"\"`.
     * @type object | undefined
    */
    labels?: {
        [key: string]: string;
    };
    /**
     * @description __Filterable__ Organizational tags to apply to this node pool. When you apply tags to LKE Enterprise node pools, they also apply to the nodes (Linodes) within the node pool. Tags don\'t cascade down to nodes in non-Enterprise node pools.
     * @type array | undefined
    */
    tags?: string[];
    /**
     * @description Kubernetes taints to add to node pool nodes. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods. To learn more, review [Add labels and taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).\n\nSpecifying an empty array (`[]`) removes all previously set taints.
     * @type array | undefined
    */
    taints?: {
        /**
         * @description The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
         * @type string
        */
        effect: TaintsEffectEnum3Key;
        /**
         * @description The Kubernetes taint key.\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the prefix part needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions and be a maximum of 253 characters.
         * @minLength 1
         * @maxLength 63
         * @pattern ^([A-Za-z0-9][-A-Za-z0-9_.]*)?(\/)?([A-Za-z0-9][-A-Za-z0-9_.]{1,63})?$
         * @type string
        */
        key: string;
        /**
         * @description The Kubernetes taint value.\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- Can be specified as an empty string value with `\"\"`.
         * @minLength 0
         * @maxLength 63
         * @pattern ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?
         * @type string
        */
        value: string;
    }[];
    /**
     * @description The Linode Type for all of the nodes in the node pool.
     * @type string
    */
    type: string;
    /**
     * @description __Beta__, __LKE Enterprise__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required when creating node pools on LKE Enterprise clusters.\n\n- `rolling_update`: Immediately triggers a recycle of this node pool when the Kubernetes version is updated.\n- `on_recycle` (default): Does not trigger any immediate recycle. New worker nodes are created with the new Kubernetes version. Existing worker nodes will be upgraded when a recycle is manually triggered.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
     * @type string | undefined
    */
    update_strategy?: PostLkeClusterPoolsMutationRequestUpdateStrategyEnumKey;
}

export type PostLkeClusterPoolsMutationResponse = PostLkeClusterPools200;

export type PostLkeClusterPoolsMutation = {
    Response: PostLkeClusterPools200;
    Request: PostLkeClusterPoolsMutationRequest;
    PathParams: PostLkeClusterPoolsPathParams;
    Errors: any;
};