/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export interface PutLkeNodePoolPathParams {
    /**
     * @description ID of the Kubernetes cluster to look up.
     * @type integer
    */
    clusterId: number;
    /**
     * @description ID of the Pool to look up.
     * @type integer
    */
    poolId: number;
}

export const putLkeNodePool200DiskEncryptionEnum = {
    "enabled": "enabled",
    "disabled": "disabled"
} as const;

export type PutLkeNodePool200DiskEncryptionEnumKey = (typeof putLkeNodePool200DiskEncryptionEnum)[keyof typeof putLkeNodePool200DiskEncryptionEnum];

export const disksTypeEnum6 = {
    "raw": "raw",
    "ext4": "ext4"
} as const;

export type DisksTypeEnum6Key = (typeof disksTypeEnum6)[keyof typeof disksTypeEnum6];

export const nodesStatusEnum4 = {
    "ready": "ready",
    "not_ready": "not_ready"
} as const;

export type NodesStatusEnum4Key = (typeof nodesStatusEnum4)[keyof typeof nodesStatusEnum4];

export const taintsEffectEnum6 = {
    "NoSchedule": "NoSchedule",
    "PreferNoSchedule": "PreferNoSchedule",
    "NoExecute": "NoExecute"
} as const;

export type TaintsEffectEnum6Key = (typeof taintsEffectEnum6)[keyof typeof taintsEffectEnum6];

export const putLkeNodePool200UpdateStrategyEnum = {
    "rolling_update": "rolling_update",
    "on_recycle": "on_recycle"
} as const;

export type PutLkeNodePool200UpdateStrategyEnumKey = (typeof putLkeNodePool200UpdateStrategyEnum)[keyof typeof putLkeNodePool200UpdateStrategyEnum];

/**
 * @description Node Pool was successfully modified.
*/
export interface PutLkeNodePool200 {
    /**
     * @description When enabled, the number of nodes autoscales within the defined minimum and maximum values.
     * @type object
    */
    autoscaler: {
        /**
         * @description Whether autoscaling is enabled for this node pool. Defaults to `false`.
         * @type boolean
        */
        enabled: boolean;
        /**
         * @description The maximum number of nodes to autoscale to. Defaults to the node pool\'s `count`.
         * @minLength 1
         * @maxLength 100
         * @type integer
        */
        max: number;
        /**
         * @description The minimum number of nodes to autoscale to. Defaults to the node pool\'s `count`.
         * @minLength 1
         * @maxLength 100
         * @type integer
        */
        min: number;
    };
    /**
     * @description The number of nodes in the node pool.
     * @minLength 1
     * @maxLength 100
     * @type integer
    */
    count: number;
    /**
     * @description Indicates the local disk encryption setting for this LKE node pool.
     * @type string
    */
    disk_encryption: PutLkeNodePool200DiskEncryptionEnumKey;
    /**
     * @description This node pool\'s custom disk layout.
     * @type array
    */
    disks: {
        /**
         * @description The size of this custom disk partition in MB. The size of this disk partition can\'t exceed the capacity of the node\'s plan type.
         * @type integer
        */
        size: number;
        /**
         * @description This custom disk partition\'s filesystem type.
         * @type string
        */
        type: DisksTypeEnum6Key;
    }[];
    /**
     * @description __Filterable__ This node pool\'s unique ID.
     * @type integer
    */
    id: number;
    /**
     * @description __Beta__ The Kubernetes version used for the worker nodes within this node pool.\n> 🚧\n>\n> This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
     * @type string
    */
    k8s_version: string;
    /**
     * @description __Beta__ The optional label defined for this node Pool.\n\n> 🚧\n>\n> This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
     * @minLength 1
     * @maxLength 32
     * @type string
    */
    label: string;
    /**
     * @description Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
     * @type object
    */
    labels: {
        [key: string]: string;
    };
    /**
     * @description Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`.
     * @type array
    */
    nodes: {
        /**
         * @description The Node\'s ID.
         * @type string
        */
        id: string;
        /**
         * @description The Linode\'s ID. When no Linode is currently provisioned for this Node, this will be `null`.
         * @type integer
        */
        instance_id: number | null;
        /**
         * @description The creation status of this Node. This status is distinct from this Node\'s readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.\n\n`not_ready` indicates that the Linode is still being created.\n\n`ready` indicates that the Linode has successfully been created and is running Kubernetes software.
         * @type string
        */
        status: NodesStatusEnum4Key;
    }[];
    /**
     * @description __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
     * @type array
    */
    tags: string[];
    /**
     * @description Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods.
     * @type array
    */
    taints: {
        /**
         * @description The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
         * @type string | undefined
        */
        effect?: TaintsEffectEnum6Key;
        /**
         * @description The Kubernetes taint key.
         * @minLength 1
         * @maxLength 63
         * @pattern ^([A-Za-z0-9][-A-Za-z0-9_.]*)?(\/)?([A-Za-z0-9][-A-Za-z0-9_.]{1,63})?$
         * @type string | undefined
        */
        key?: string;
        /**
         * @description The Kubernetes taint value.
         * @minLength 0
         * @maxLength 63
         * @pattern ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?
         * @type string | undefined
        */
        value?: string;
    }[];
    /**
     * @description The Linode Type for all of the nodes in the node pool.
     * @type string
    */
    type: string;
    /**
     * @description __Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.
     * @type string
    */
    update_strategy: PutLkeNodePool200UpdateStrategyEnumKey;
}

export const taintsEffectEnum7 = {
    "NoSchedule": "NoSchedule",
    "PreferNoSchedule": "PreferNoSchedule",
    "NoExecute": "NoExecute"
} as const;

export type TaintsEffectEnum7Key = (typeof taintsEffectEnum7)[keyof typeof taintsEffectEnum7];

/**
 * @description The fields to update.
*/
export interface PutLkeNodePoolMutationRequest {
    /**
     * @description When enabled, the number of nodes automatically scales within the defined minimum and maximum values. When making a request, `max` and `min` require each other.
     * @type object | undefined
    */
    autoscaler?: {
        /**
         * @description Whether automatic scaling is enabled for this node pool. Defaults to `false`.
         * @type boolean | undefined
        */
        enabled?: boolean;
        /**
         * @description The maximum number of nodes to automatically scale to. Defaults to the value provided by the `count` field.
         * @minLength 1
         * @maxLength 100
         * @type integer | undefined
        */
        max?: number;
        /**
         * @description The minimum number of nodes to automatically scale to. Defaults to the node pool\'s `count`.
         * @minLength 1
         * @maxLength 100
         * @type integer | undefined
        */
        min?: number;
    };
    /**
     * @description The number of nodes in the node pool.
     * @minLength 1
     * @maxLength 100
     * @type integer | undefined
    */
    count?: number;
    /**
     * @description Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).\n\n**Label key:**\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the maximum total length of the label key is 128 characters. Domain labels can be up to 62 characters after the \'/\'. The prefix needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions.\n\n- If the key doesn\'t begin with a DNS subdomain prefix, the maximum key length is 63 characters.\n\nSpecifying an empty object removes all previously set labels.\n\n**Label value:**\n\n- The label\'s value can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- Can be specified as an empty string value with `\"\"`.
     * @type object | undefined
    */
    labels?: {
        [key: string]: string;
    };
    /**
     * @description __Filterable__ Organizational tags to apply to this node pool. When you apply tags to LKE Enterprise node pools, they also apply to the nodes (Linodes) within the node pool. Tags don\'t cascade down to nodes in non-Enterprise node pools.
     * @type array | undefined
    */
    tags?: string[];
    /**
     * @description Kubernetes taints to add to node pool nodes. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods. To learn more, review [Add labels and taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).\n\nSpecifying an empty array (`[]`) removes all previously set taints.
     * @type array | undefined
    */
    taints?: {
        /**
         * @description The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
         * @type string
        */
        effect: TaintsEffectEnum7Key;
        /**
         * @description The Kubernetes taint key.\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the prefix part needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions and be a maximum of 253 characters.
         * @minLength 1
         * @maxLength 63
         * @pattern ^([A-Za-z0-9][-A-Za-z0-9_.]*)?(\/)?([A-Za-z0-9][-A-Za-z0-9_.]{1,63})?$
         * @type string
        */
        key: string;
        /**
         * @description The Kubernetes taint value.\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- Can be specified as an empty string value with `\"\"`.
         * @minLength 0
         * @maxLength 63
         * @pattern ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?
         * @type string
        */
        value: string;
    }[];
}

export type PutLkeNodePoolMutationResponse = PutLkeNodePool200;

export type PutLkeNodePoolMutation = {
    Response: PutLkeNodePool200;
    Request: PutLkeNodePoolMutationRequest;
    PathParams: PutLkeNodePoolPathParams;
    Errors: any;
};