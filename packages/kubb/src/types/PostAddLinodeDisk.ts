/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export interface PostAddLinodeDiskPathParams {
    /**
     * @description ID of the Linode to look up.
     * @type integer
    */
    linodeId: number;
}

export const postAddLinodeDisk200FilesystemEnum = {
    "raw": "raw",
    "swap": "swap",
    "ext2": "ext2",
    "ext3": "ext3",
    "ext4": "ext4",
    "initrd": "initrd"
} as const;

export type PostAddLinodeDisk200FilesystemEnumKey = (typeof postAddLinodeDisk200FilesystemEnum)[keyof typeof postAddLinodeDisk200FilesystemEnum];

export const postAddLinodeDisk200StatusEnum = {
    "ready": "ready",
    "not ready": "not ready",
    "deleting": "deleting"
} as const;

export type PostAddLinodeDisk200StatusEnumKey = (typeof postAddLinodeDisk200StatusEnum)[keyof typeof postAddLinodeDisk200StatusEnum];

/**
 * @description Disk created.
*/
export interface PostAddLinodeDisk200 {
    /**
     * @description __Read-only__ When this disk was created.
     * @type string, date-time
    */
    readonly created: string;
    /**
     * @description __Read-only__ Displays if encryption is enabled on this disk. This setting is based on the `disk_encryption` setting of the Linode.
     * @default "enabled"
     * @type string
    */
    readonly disk_encryption: string;
    /**
     * @description The disk\'s format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.\n\n> 📘\n>\n> The `ext2` file system doesn\'t properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
     * @type string
    */
    filesystem: PostAddLinodeDisk200FilesystemEnumKey;
    /**
     * @description __Read-only__ This disk\'s ID. You need this value to run other operations that interact with the disk.
     * @type integer
    */
    readonly id: number;
    /**
     * @description __Filterable__ The name of the disk. This is for display purposes only.
     * @minLength 1
     * @maxLength 48
     * @type string
    */
    label: string;
    /**
     * @description __Filterable__ The size of the disk in MB.
     * @type integer
    */
    size: number;
    /**
     * @description __Read-only__ The current state of the disk.
     * @type string
    */
    readonly status: PostAddLinodeDisk200StatusEnumKey;
    /**
     * @description __Read-only__ When this disk was last updated.
     * @type string, date-time
    */
    readonly updated: string;
}

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
*/
export interface PostAddLinodeDiskError {
    /**
     * @type array
    */
    errors: {
        /**
         * @description The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
         * @type string
        */
        field: string;
        /**
         * @description What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
         * @type string
        */
        reason: string;
    }[];
}

export const postAddLinodeDiskMutationRequestFilesystemEnum = {
    "raw": "raw",
    "swap": "swap",
    "ext2": "ext2",
    "ext3": "ext3",
    "ext4": "ext4",
    "initrd": "initrd"
} as const;

export type PostAddLinodeDiskMutationRequestFilesystemEnumKey = (typeof postAddLinodeDiskMutationRequestFilesystemEnum)[keyof typeof postAddLinodeDiskMutationRequestFilesystemEnum];

/**
 * @description The parameters to set when creating the Disk.
*/
export interface PostAddLinodeDiskMutationRequest {
    /**
     * @description __Write-only__ A list of public SSH keys that will be automatically appended to the root user\'s `~/.ssh/authorized_keys` file when deploying from an Image.
     * @type array | undefined
    */
    authorized_keys?: string[];
    /**
     * @description __Write-only__ A list of usernames for authorized users. Before you can add a user, it needs an SSH key assigned to its profile. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information. If the usernames already have associated SSH keys, the keys will be appended to the root users `~/.ssh/authorized_keys` file automatically when deploying from an image.
     * @type array | undefined
    */
    authorized_users?: string[];
    /**
     * @description The disk\'s format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.\n\n> 📘\n>\n> The `ext2` file system doesn\'t properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
     * @type string | undefined
    */
    filesystem?: PostAddLinodeDiskMutationRequestFilesystemEnumKey;
    /**
     * @description An Image ID to deploy the Linode Disk from.\n\nRun the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account\'s Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user\'s grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
     * @type string
    */
    image?: string | null;
    /**
     * @description __Filterable__ The name of the disk. This is for display purposes only.
     * @minLength 1
     * @maxLength 48
     * @type string | undefined
    */
    label?: string;
    /**
     * @description __Write-only__ This sets the root user\'s password on a newly created Linode Disk when deploying from an Image.\n\n- __Required__ when creating a Linode Disk from an Image, including when using a StackScript.\n\n- Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
     * @minLength 7
     * @maxLength 128
     * @type string | undefined, password
    */
    root_pass?: string;
    /**
     * @description __Filterable__ The size of the Disk in MB.\n\nImages require a minimum size. Run the [Get an image](https://techdocs.akamai.com/linode-api/reference/get-image) operation to view its size.
     * @type integer
    */
    size: number;
    /**
     * @description This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.\n\nThis field is required to be valid JSON.\n\nTotal length cannot exceed 65,535 characters.
     * @maxLength 65535
     * @type object | undefined
    */
    stackscript_data?: object;
    /**
     * @description A StackScript ID that will cause the referenced StackScript to be run during deployment of this Linode. A compatible `image` is required to use a StackScript. To get a list of available StackScript and their permitted Images, run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts). This field cannot be used when deploying from a Backup or a Private Image.
     * @type integer | undefined
    */
    stackscript_id?: number;
}

export type PostAddLinodeDiskMutationResponse = PostAddLinodeDisk200;

export type PostAddLinodeDiskMutation = {
    Response: PostAddLinodeDisk200;
    Request: PostAddLinodeDiskMutationRequest;
    PathParams: PostAddLinodeDiskPathParams;
    Errors: any;
};