/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/


export const postRescueLinodeInstancePathParamsApiVersionEnum = {
    "v4": "v4",
    "v4beta": "v4beta"
} as const;

export type PostRescueLinodeInstancePathParamsApiVersionEnumKey = (typeof postRescueLinodeInstancePathParamsApiVersionEnum)[keyof typeof postRescueLinodeInstancePathParamsApiVersionEnum];

export interface PostRescueLinodeInstancePathParams {
    /**
     * @description __Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta.
     * @type string
    */
    apiVersion: PostRescueLinodeInstancePathParamsApiVersionEnumKey;
    /**
     * @description ID of the Linode to rescue.
     * @type integer
    */
    linodeId: number;
}

/**
 * @description Rescue started.
*/
export type PostRescueLinodeInstance200 = object;

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
*/
export interface PostRescueLinodeInstanceError {
    /**
     * @type array | undefined
    */
    errors?: {
        /**
         * @description The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
         * @type string | undefined
        */
        field?: string;
        /**
         * @description What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
         * @type string | undefined
        */
        reason?: string;
    }[];
}

/**
 * @description Optional object of devices to be mounted.
*/
export interface PostRescueLinodeInstanceMutationRequest {
    /**
     * @type object | undefined
    */
    devices?: {
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sda?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sdb?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sdc?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sdd?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sde?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sdf?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
        /**
         * @description Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
         * @type object | undefined
        */
        sdg?: {
            /**
             * @description The Disk ID, or `null` if a Volume is assigned to this slot.
             * @type integer | undefined
            */
            disk_id?: number;
            /**
             * @description The Volume ID, or `null` if a Disk is assigned to this slot.
             * @type integer
            */
            volume_id?: number | null;
        };
    };
}

export type PostRescueLinodeInstanceMutationResponse = PostRescueLinodeInstance200;

export type PostRescueLinodeInstanceMutation = {
    Response: PostRescueLinodeInstance200;
    Request: PostRescueLinodeInstanceMutationRequest;
    PathParams: PostRescueLinodeInstancePathParams;
    Errors: any;
};