/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const getAccountAvailabilityPathParamsSchema = z.object({
    "regionId": z.string().describe("The abbreviated value (\"slug\") for the applicable data center. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the slug for each data center.")
    })

/**
 * @description The services available in the specified region.
 */
export const getAccountAvailability200Schema = z.object({
    "available": z.array(z.string()).describe("__Read-only__ A list of services _available_ to your account in the `region`."),
"region": z.string().describe("__Read-only__ The Akamai cloud computing data center (region), represented by a slug value. You can view a full list of regions and their associated slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation."),
"unavailable": z.array(z.string()).describe("__Read-only__ A list of services _unavailable_ to your account in the `region`.")
    }).describe("Account Service Availability object.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const getAccountAvailabilityErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

export const getAccountAvailabilityQueryResponseSchema = z.lazy(() => getAccountAvailability200Schema)