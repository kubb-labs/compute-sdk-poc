/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postUpgradeLinodeInterfacesPathParamsSchema = z.object({
    "linodeId": z.coerce.number().int().describe("The `id` of the Linode.")
    })

/**
 * @description Upgrade or `dry_run` upgrade completed.
 */
export const postUpgradeLinodeInterfaces200Schema = z.object({
    "config_id": z.int().describe("The Linode's `config_id` that was upgraded."),
"dry_run": z.boolean().describe("Indicates if this was a `dry_run` upgrade that didn't commit any changes and didn't generate a unique interface ID. If `dry_run` is `false`, the upgrade completed."),
"interfaces": z.array(z.union([z.object({
    "created": z.string().datetime().describe("When the interface was created."),
"default_route": z.nullable(z.object({
    "ipv4": z.boolean().default(false).describe("Indicates if the interface is used for the IPv4 default route. Only one interface per Linode can have the IPv4 default route."),
"ipv6": z.boolean().default(false).describe("Indicates if the interface is used for the IPv6 default route. Only one interface per Linode can have the IPv6 default route.")
    }).describe("Indicates if the interface is used as a default route.")),
"id": z.int().describe("__Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0."),
"mac_address": z.string().regex(/[a-zA-Z0-9-]+/).describe("A 48-bit MAC address used to identify the Linode’s interface. A public interface's `mac_address` does not change, even if the public interface is deleted and then recreated."),
"public": z.object({
    "ipv4": z.object({
    "addresses": z.array(z.object({
    "address": z.string().describe("The public IPv4 address assigned to this interface."),
"primary": z.boolean().describe("Indicates if the public IPv4 address serves as the source address for traffic routing within the Linode and other corresponding network interfaces and services.")
    })).describe("The public IPv4 addresses and primary settings for this public interface."),
"shared": z.array(z.object({
    "address": z.string().describe("Shared IPv4 address."),
"linode_id": z.string().describe("The ID of the Linode this address currently belongs to. For IPv4 addresses, by default this is the Linode this address was assigned when created.")
    })).describe("The IPv4 address assigned to this Linode interface, which is also shared with another Linode.")
    }).describe("The interface's public IPv4 `addresses`."),
"ipv6": z.object({
    "ranges": z.array(z.object({
    "range": z.string().describe("IPv6 range in CIDR notation (`2600:0db8::1/64`) or prefix-only (`/64`)."),
"route_target": z.string().describe("The public IPv6 address that the `range` is routed to.")
    })).describe("List of IPv6 ranges assigned to this interface."),
"shared": z.array(z.object({
    "range": z.string().describe("The IPv6 address range."),
"route_target": z.string().describe("The public IPv6 address that the `range` is routed to.")
    })).describe("The IPv6 address assigned to this Linode interface, which is also shared with another Linode."),
"slaac": z.array(z.object({
    "address": z.string().describe("Public IPv6 addresses assigned to this interface."),
"prefix": z.int().describe("The prefix length advertised for SLAAC to use. Only the specific (`/128`) EUI-64 address derived from the interface's MAC address is supported. To ensure the MAC-based EUI-64 address is used, privacy addressing needs to be disabled. Network Helper automatically configures the MAC-derived EUI-64 address. If you disable Network Helper or use an unsupported operating system, follow the specific instructions for your OS.")
    })).describe("The public `slaac` and subnet prefix settings for this public interface that is used to communicate over the public internet, and with other services in the same data center.")
    }).describe("The interface's public IPv6 configuration.")
    }).describe("Public interface type."),
"updated": z.string().datetime().describe("When the interface was last updated."),
"version": z.int().describe("Interface version number that increments when the interface updates."),
"vlan": z.nullable(z.object({
    
    }).describe("The value is `null` if this is not a VLAN interface.")),
"vpc": z.nullable(z.object({
    
    }).describe("The value is `null` if this is not a VPC interface."))
    }), z.object({
    "created": z.string().datetime().describe("When the interface was created."),
"id": z.int().describe("__Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0."),
"mac_address": z.string().regex(/[a-zA-Z0-9-]+/).describe("A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change."),
"public": z.nullable(z.object({
    
    }).describe("The value is `null` if this isn't a public interface.")),
"updated": z.string().datetime().describe("When the interface was last updated."),
"version": z.int().describe("Interface version number that is incremented when the interface is updated."),
"vlan": z.object({
    "ipam_address": z.nullable(z.string().describe("This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.")),
"vlan_label": z.string().describe("The VLAN's label. VLAN interfaces on the same Linode must have a unique `vlan_label`.")
    }).describe("VLAN interface type."),
"vpc": z.nullable(z.object({
    
    }).describe("The value is `null` if this isn't a VPC interface."))
    }), z.object({
    "created": z.string().datetime().describe("When the interface was created."),
"default_route": z.object({
    "ipv4": z.boolean().default(false).describe("Indicates if the interface serves as the IPv4 default route. Only one interface per Linode can have the IPv4 default route.")
    }).describe("Indicates if the interface serves as a default route."),
"id": z.int().describe("__Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0."),
"mac_address": z.string().regex(/[a-zA-Z0-9-]+/).describe("A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change."),
"public": z.nullable(z.object({
    
    }).describe("The value is `null` if this is not a public interface.")),
"updated": z.string().datetime().describe("When the interface last updated."),
"version": z.int().describe("The version number of the interface configuration, incremented each time the interface is updated."),
"vlan": z.nullable(z.object({
    
    }).describe("The value is `null` if this is not a VLAN interface.")),
"vpc": z.object({
    "ipv4": z.object({
    "addresses": z.array(z.object({
    "address": z.string().describe("The VPC subnet IPv4 address."),
"nat_1_1_address": z.nullable(z.string().describe("The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.")),
"primary": z.boolean().describe("Indicates if the IPv4 address is used to set up a source address for routes inside the Linode for the corresponding network interface.")
    })).describe("IPv4 address settings for this VPC interface."),
"ranges": z.array(z.object({
    "range": z.string().describe("CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).")
    })).describe("VPC IPv4 ranges.")
    }).describe("The interface's IPv4 `addresses` and `ranges` configuration."),
"subnet_id": z.int().describe("VPC subnet's unique identifier."),
"vpc_id": z.int().describe("VPC's unique identifier.")
    }).describe("VPC interface type.")
    })]).describe("One of the following interface types: VPC, public, or VLAN."))
    }).describe("Upgraded interfaces")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postUpgradeLinodeInterfacesErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

/**
 * @description Upgrade to a Linode interface or perform a dry run upgrade.
 */
export const postUpgradeLinodeInterfacesMutationRequestSchema = z.object({
    "config_id": z.int().describe("The Linode's `config_id`. Only one `config_id` can be specified:\n- If there are no legacy configuration interfaces or configurations defined in the config_id, a public interface is created using the Linode's automatically assigned public IPv4 address.\n- If a config_id is not provided and the Linode has only one configuration, the upgrade automatically uses that config_id.\n- If the Linode has multiple configurations and a config_id is not specified, an error is returned.").nullish(),
"dry_run": z.optional(z.boolean().default(true).describe("Before you upgrade interfaces, you can preview the new Linode interface by performing a `dry_run`:\n- Either omit `dry_run` or set it to `true` to simulate the upgrade process. The response data shows what the Linode interface will look like after the upgrade, but without committing any changes. Since the interface doesn't exist yet, the interface `id` value is 0.\n- If `dry_run` is set to `false`, the Linode undergoes the actual upgrade, but note you need to first shut down the Linode."))
    }).describe("Upgrades a legacy configuration interface on a Linode to a matching Linode interface.").nullable()

export const postUpgradeLinodeInterfacesMutationResponseSchema = z.lazy(() => postUpgradeLinodeInterfaces200Schema)