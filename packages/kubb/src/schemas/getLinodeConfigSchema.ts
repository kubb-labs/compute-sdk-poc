/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const getLinodeConfigPathParamsSchema = z.object({
    "apiVersion": z.enum(["v4", "v4beta"]).describe("__Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta."),
"linodeId": z.coerce.number().int().describe("The `id` of the Linode."),
"configId": z.coerce.number().int().describe("The `id` of the Configuration Profile.")
    })

/**
 * @description A configuration profile object.
 */
export const getLinodeConfig200Schema = z.object({
    "comments": z.string().describe("Optional field for arbitrary user comments on this configuration.").nullish(),
"devices": z.optional(z.object({
    "sda": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdb": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdc": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdd": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sde": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdf": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdg": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdh": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode."))
    }).describe("A dictionary of device disks to use as a device map in a Linode's configuration profile.\n\n- An empty device disk dictionary or a dictionary with empty values for device slots is allowed.\n- If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.")),
"helpers": z.optional(z.object({
    "devtmpfs_automount": z.optional(z.boolean().default(false).describe("Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.")),
"distro": z.optional(z.boolean().describe("Helps maintain correct `inittab` or `upstart` console device.")),
"modules_dep": z.optional(z.boolean().describe("Creates a modules dependency file for the kernel you run.")),
"network": z.boolean().describe("Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).").nullish(),
"updatedb_disabled": z.optional(z.boolean().describe("Set to `true` to disable the `updatedb` cron job to avoid disk thrashing."))
    }).describe("Helpers enabled when booting to this Linode configuration.")),
"id": z.optional(z.int().describe("__Read-only__ The ID of this Config.")),
"interfaces": z.optional(z.array(z.object({
    "active": z.optional(z.boolean().describe("__Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.")),
"id": z.optional(z.int().describe("__Read-only__ The unique ID representing this interface.")),
"ip_ranges": z.array(z.string()).describe("IPv4 CIDR VPC subnet ranges that are routed to this interface.\n\nWhen included in a request:\n\n- A range can't include any addresses that are assigned to an active Linode or another VPC subnet.\n\n- When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.\n\n- Submit this as an empty array removes all existing values.\n\n- Omit this object to leave existing values as is.\n\n<<LB>>\n\n> 📘\n>\n> This is only supported for interfaces with a `purpose` of `vpc`.").nullish(),
"ipam_address": z.string().describe("This interface's private IP address in classless inter-domain routing (CIDR) notation.\n\nFor interfaces with a `purpose` of `public`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.\n\n- This should be unique among devices attached to the VLAN to avoid conflict.\n\n- If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).\n\nFor interfaces with a `purpose` of `vpc`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.").nullish(),
"ipv4": z.optional(z.object({
    "nat_1_1": z.union([z.string(), z.enum(["any"])]).describe("The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.\n\n- Only supported for interfaces with a `purpose` of `vpc`.\n\n- Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.\n\n- Returned as an empty string (`\"\"`) for non-`vpc` type interfaces.\n\nWhen included in a request:\n\n- You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.\n\n- A specified address can't be shared with another Linode.\n\n- Omit this object or include it and set it to `null` or an empty string (`\"\"`) to block creation of a 1:1 NAT.\n\n<<LB>>\n\n> 📘\n>\n> You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.").nullish(),
"vpc": z.string().describe("The VPC subnet IPv4 address for this interface.\n\n- This only applies to interfaces with a `purpose` of `vpc`.\n\n- Returned as an empty string (`\"\"`) for non-`vpc` type interfaces.\n\nWhen included in a request:\n\n- The `vpc` can't be assigned to an existing Linode as an address or in a range.\n\n- The target address can't be the first two or last two addresses in the subnet IPv4 range.\n\n- If omitted, a valid address within the Subnet IPv4 range is automatically assigned.").nullish()
    }).describe("IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.")),
"label": z.string().regex(/[a-zA-Z0-9-]+/).describe("__Filterable__ The name of this interface.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- Required.\n\n- This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.\n\n- This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).\n\n- If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.\n\nFor interfaces with a `purpose` of `public`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.\n\nFor interfaces with a `purpose` of `vpc`:\n\n- If you include this in a request, set it to an empty string (`\"\"`) or `null`.\n\n- Returned as `null` in a response.").nullish(),
"primary": z.optional(z.boolean().describe("The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.\n\n> 📘\n>\n> This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.")),
"purpose": z.enum(["public", "vlan", "vpc"]).describe("The type of interface. This can be `public`, `vlan`, or `vpc`.\n\nFor interfaces with a `purpose` of `public`:\n\n- You can only define one `public` interface per Linode.\n\n- The Linode's default public IPv4 address is assigned to the `public` interface.\n\n- A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.\n\n- If an `ipam_address` is configured, the Linode uses this address.\n\nFor interfaces with a `purpose` of `vpc`:\n\n- Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.\n\n- When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information."),
"subnet_id": z.int().describe("The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.\n\n- Required for `vpc` type interfaces.\n\n- Returned as `null` for non-`vpc` type interfaces.\n\n- Once you've assigned a VPC subnet to an interface, you can't update it.\n\n- You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.").nullish(),
"vpc_id": z.int().describe("__Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.").nullish()
    }).describe("The network interface to apply to this Linode's configuration profile.")).min(1).max(3).refine(items => new Set(items).size === items.length, { message: "Array entries must be unique" }).describe("`interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).\n\nFrom one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:\n\n- First [0]:  `eth0`\n- Second [1]: `eth1`\n- Third [2]:  `eth2`\n\nWhen updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.\n\nIf no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.\n\n> 📘\n>\n> Changes to Linode Interface configurations can be enabled by rebooting the Linode.\n\n`vpc` details\n\nSee the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.\n\n`vlan` details\n\n- Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.\n- See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.")),
"kernel": z.optional(z.string().default("linode/latest-64bit").describe("The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:\n\n- `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.\n\n- `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.\n\n- `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.")),
"label": z.optional(z.string().min(1).max(48).describe("__Filterable__ The name of the configuration for display in Akamai Cloud Manager.")),
"memory_limit": z.optional(z.int().describe("Defaults to the total RAM of the Linode.")),
"root_device": z.optional(z.string().regex(/a-z, A-Z, 0-9, \/, _, -/).describe("The root device to boot.\n\n> 📘\n\n- If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.\n\n- If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.")),
"run_level": z.optional(z.enum(["default", "single", "binbash"]).describe("Defines the state of your Linode after booting. Defaults to `default`.")),
"virt_mode": z.optional(z.enum(["paravirt", "fullvirt"]).describe("Controls the virtualization mode. Defaults to `paravirt`.\n\n- `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.\n\n- `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized."))
    })

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const getLinodeConfigErrorSchema = z.object({
    "errors": z.optional(z.array(z.object({
    "field": z.optional(z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.")),
"reason": z.optional(z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully."))
    }).describe("An object for describing a single error that occurred during the processing of a request.")))
    })

export const getLinodeConfigQueryResponseSchema = z.lazy(() => getLinodeConfig200Schema)