/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const putLinodeConfigInterfacePathParamsSchema = z.object({
    "linodeId": z.coerce.number().int().describe("The `id` of the Linode."),
"configId": z.coerce.number().int().describe("The `id` of the Configuration Profile."),
"interfaceId": z.coerce.number().int().describe("The `id` of the Linode Configuration Profile Interface.")
    })

/**
 * @description Configuration profile interface successfully updated.
 */
export const putLinodeConfigInterface200Schema = z.object({
    "active": z.optional(z.boolean().describe("__Read-only__ Returned as `true` if the interface has been booted using the configuration profile to which the interface belongs.")),
"id": z.optional(z.int().describe("__Read-only__ The unique ID representing this interface.")),
"ip_ranges": z.array(z.string()).describe("IPv4 CIDR VPC subnet ranges that are routed to this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as an empty string (`\"\"`) if the interface has a `purpose` of `public` or `vlan`.").nullish(),
"ipam_address": z.string().describe("The interface's private IP address, in classless inter-domain routing (CIDR) notation. Only applies to interfaces with a `purpose` of `vlan`. Returned as `null` if the interface has a `purpose` of `public` or `vpc`.\n\n> 📘\n>\n> If Network Helper is enabled, the Linode's interface is automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).").nullish(),
"ipv4": z.optional(z.object({
    "nat_1_1": z.nullable(z.string().describe("The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no 1:1 NAT is set for a `vpc` interface. Returned as an empty string (`\"\"`) if the interface has a `purpose` of `public` or `vlan`.")),
"vpc": z.nullable(z.string().describe("The VPC subnet IPv4 address for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as an empty string (`\"\"`) if the interface has a `purpose` of `public` or `vlan`."))
    }).describe("IPv4 addresses configured for this interface. Only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if the interface has a `purpose` of `public` or `vlan`.")),
"label": z.string().regex(/[a-zA-Z0-9-]+/).describe("__Filterable__ The name set for an interface with a `purpose` of `vlan`. Returned as `null` if the interface has a `purpose` of `public` or `vpc`.").nullish(),
"primary": z.optional(z.boolean().describe("The default route to the Linode. Each Linode can have one interface set as its `primary`. Defaults to the first non-`vlan` type interface, if you haven't specifically set a `primary`.\n\n> 📘\n>\n> The value needs to `false` for any interface that uses `vlan` as its `purpose`.")),
"purpose": z.enum(["public", "vlan", "vpc"]).describe("The type of interface. This can be `public`, `vlan`, or `vpc`.\n\nFor interfaces with a `purpose` of `public`:\n\n- You can only define one `public` interface per Linode.\n\n- The Linode's default public IPv4 address is assigned to the `public` interface.\n\n- A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.\n\nFor interfaces with a `purpose` of `vlan`:\n\n- The target Linode is attached to the VLAN with the specified `label`.\n\n- If an `ipam_address` is configured, the Linode uses this address.\n\nFor interfaces with a `purpose` of `vpc`:\n\n- The target Linode is attached to an existing VPC subnet with the specified `subnet_id`.\n\n- When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information."),
"subnet_id": z.int().describe("The `id` of the VPC subnet for this interface. Only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if the interface has a `purpose` of `public` or `vlan`.\n\n> 📘\n>\n> You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.").nullish(),
"vpc_id": z.int().describe("__Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.").nullish()
    }).describe("The network interface to apply to this Linode's configuration profile.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const putLinodeConfigInterfaceErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

export const putLinodeConfigInterfaceMutationRequestSchema = z.object({
    "ip_ranges": z.optional(z.array(z.string()).describe("IPv4 CIDR VPC subnet ranges that are routed to this interface.\n\n- A range can't include any addresses that are assigned to an active Linode or another VPC subnet.\n\n- When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.\n\n- Include this as an empty array (`[]`) to remove all existing `nat_1_1` values.\n\n- Omit this array to leave all existing `ip_ranges` as is.\n<<LB>>\n> 📘\n>\n> This only applies to interfaces with a `purpose` of `vpc`.")),
"ipv4": z.optional(z.object({
    "nat_1_1": z.union([z.string(), z.enum(["any"])]).describe("The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.\n\n- You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.\n\n- A specified address can't be used on another Linode.\n\n- Include this as an empty object (`\"\"`) to remove an existing `nat_1_1` value.\n\n- Omit this object or include it and set it to `null` to leave an existing `nat_1_1` value as is.").nullish(),
"vpc": z.string().describe("The VPC subnet IPv4 address for this interface.\n\n- The `vpc` can't be assigned to an existing Linode as an address or in a range.\n\n- The target address can't be the first two or last two addresses in the subnet IPv4 range.\n\n- If omitted, a valid address within the subnet IPv4 range is automatically assigned.").nullish()
    }).describe("IPv4 addresses configured for this interface.\n\n> 📘\n>\n> This only applies to interfaces with a `purpose` of `vpc`.")),
"primary": z.optional(z.boolean().describe("Set to `true` to label this configuration profile interface as the default route to the Linode.\n\n- Each Linode can have one interface set as its `primary`.\n\n- If you don't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary."))
    }).describe("Linode configuration profile interface update request object.")

export const putLinodeConfigInterfaceMutationResponseSchema = z.lazy(() => putLinodeConfigInterface200Schema)