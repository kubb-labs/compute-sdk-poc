/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postLkeClusterPoolsPathParamsSchema = z.object({
    "clusterId": z.coerce.number().int().describe("ID of the Kubernetes cluster to look up.")
    })

/**
 * @description Node Pool has been created.
 */
export const postLkeClusterPools200Schema = z.object({
    "autoscaler": z.object({
    "enabled": z.boolean().describe("Whether autoscaling is enabled for this node pool. Defaults to `false`."),
"max": z.int().min(1).max(100).describe("The maximum number of nodes to autoscale to. Defaults to the node pool's `count`."),
"min": z.int().min(1).max(100).describe("The minimum number of nodes to autoscale to. Defaults to the node pool's `count`.")
    }).describe("When enabled, the number of nodes autoscales within the defined minimum and maximum values."),
"count": z.int().min(1).max(100).describe("The number of nodes in the node pool."),
"disk_encryption": z.enum(["enabled", "disabled"]).describe("Indicates the local disk encryption setting for this LKE node pool."),
"disks": z.array(z.object({
    "size": z.int().describe("The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type."),
"type": z.enum(["raw", "ext4"]).describe("This custom disk partition's filesystem type.")
    }).describe("The values to assign to each partition in this node pool's custom disk layout.")).describe("This node pool's custom disk layout."),
"id": z.int().describe("__Filterable__ This node pool's unique ID."),
"k8s_version": z.string().describe("__Beta__ The Kubernetes version used for the worker nodes within this node pool.\n> 🚧\n>\n> This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA."),
"label": z.string().min(1).max(32).describe("__Beta__ The optional label defined for this node Pool.\n\n> 🚧\n>\n> This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA."),
"labels": z.object({
    
    }).catchall(z.string().regex(/^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?/)).describe("Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools)."),
"nodes": z.array(z.object({
    "id": z.string().describe("The Node's ID."),
"instance_id": z.nullable(z.int().describe("The Linode's ID. When no Linode is currently provisioned for this Node, this will be `null`.")),
"status": z.enum(["ready", "not_ready"]).describe("The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.\n\n`not_ready` indicates that the Linode is still being created.\n\n`ready` indicates that the Linode has successfully been created and is running Kubernetes software.")
    }).describe("__Read-only__ Status information for a Node which is a member of a Kubernetes cluster.")).describe("Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`."),
"tags": z.array(z.string()).describe("__Filterable__ An array of tags applied to this object. Tags are for organizational purposes only."),
"taints": z.array(z.object({
    "effect": z.optional(z.enum(["NoSchedule", "PreferNoSchedule", "NoExecute"]).describe("The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).")),
"key": z.optional(z.string().regex(/^([A-Za-z0-9][-A-Za-z0-9_.]*)?(\/)?([A-Za-z0-9][-A-Za-z0-9_.]{1,63})?$/).describe("The Kubernetes taint key.")),
"value": z.optional(z.string().regex(/^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?/).describe("The Kubernetes taint value."))
    })).describe("Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods."),
"type": z.string().describe("The Linode Type for all of the nodes in the node pool."),
"update_strategy": z.enum(["rolling_update", "on_recycle"]).describe("__Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.")
    }).describe("The set of node pools that are members of the Kubernetes cluster. Node pools consist of a Linode type, the number of Linodes to deploy of that type, and additional status information.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postLkeClusterPoolsErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

/**
 * @description Configuration for the Node Pool.
 */
export const postLkeClusterPoolsMutationRequestSchema = z.object({
    "autoscaler": z.optional(z.object({
    "enabled": z.optional(z.boolean().describe("Whether automatic scaling is enabled for this node pool. Defaults to `false`.")),
"max": z.optional(z.int().min(1).max(100).describe("The maximum number of nodes to automatically scale to. Defaults to the value provided by the `count` field.")),
"min": z.optional(z.int().min(1).max(100).describe("The minimum number of nodes to automatically scale to. Defaults to the node pool's `count`."))
    }).describe("When enabled, the number of nodes automatically scales within the defined minimum and maximum values. When making a request, `max` and `min` require each other.")),
"count": z.int().min(1).max(100).describe("The number of nodes in the node pool."),
"disk_encryption": z.optional(z.enum(["enabled", "disabled"]).default("disabled").describe("Local disk encryption setting for this LKE node pool. Defaults to `disabled`.")),
"disks": z.optional(z.array(z.object({
    "size": z.optional(z.int().describe("The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.")),
"type": z.optional(z.enum(["raw", "ext4"]).describe("This custom disk partition's filesystem type."))
    }).describe("The values to assign to each partition in this node pool's custom disk layout.")).describe("This node pool's custom disk layout. Each item in this array will create a new disk partition for each node in this node pool.\n\n> 📘\n>\n> Omit this field, except for special use cases. The disks specified here are partitions in _addition_ to the primary partition and reduce the size of the primary partition. This can lead to stability problems for the node.\n\n  - The custom disk layout is applied to each node in this node pool.\n\n  - The maximum number of custom disk partitions that can be configured is 7.\n\n  - Once the requested disk partitions are allocated, the remaining disk space is allocated to the node's boot disk.\n\n  - A node pool's custom disk layout is immutable over the lifetime of the node pool.")),
"k8s_version": z.optional(z.string().describe("__Beta__, __LKE Enterprise__ The LKE-specific Kubernetes version to use for the worker nodes within this node pool. This field is required when creating node pools on LKE Enterprise clusters.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"label": z.optional(z.string().regex(/^[a-z0-9]([-a-z0-9]*[a-z0-9])$/).describe("__Beta__ An optional label for this node pool. Labels can be up to 32 characters long, containing lowercase alphanumeric characters optionally interspersed with dashes.\n\nA node pool's label also cascades down to its nodes (Linodes). If a label is defined on the node pool, the label for each node is `<cluster-id>-<nodepool-label>-<hash>`. If not, the label is `<cluster-id>-<nodepool-id>-<hash>`.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"labels": z.optional(z.object({
    
    }).catchall(z.string().regex(/^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?/)).describe("Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).\n\n**Label key:**\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the maximum total length of the label key is 128 characters. Domain labels can be up to 62 characters after the '/'. The prefix needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions.\n\n- If the key doesn't begin with a DNS subdomain prefix, the maximum key length is 63 characters.\n\nSpecifying an empty object removes all previously set labels.\n\n**Label value:**\n\n- The label's value can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- Can be specified as an empty string value with `\"\"`.")),
"tags": z.optional(z.array(z.string()).describe("__Filterable__ Organizational tags to apply to this node pool. When you apply tags to LKE Enterprise node pools, they also apply to the nodes (Linodes) within the node pool. Tags don't cascade down to nodes in non-Enterprise node pools.")),
"taints": z.optional(z.array(z.object({
    "effect": z.enum(["NoSchedule", "PreferNoSchedule", "NoExecute"]).describe("The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/)."),
"key": z.string().regex(/^([A-Za-z0-9][-A-Za-z0-9_.]*)?(\/)?([A-Za-z0-9][-A-Za-z0-9_.]{1,63})?$/).describe("The Kubernetes taint key.\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the prefix part needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions and be a maximum of 253 characters."),
"value": z.string().regex(/^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?/).describe("The Kubernetes taint value.\n\n- A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.\n\n- Can be specified as an empty string value with `\"\"`.")
    })).min(0).describe("Kubernetes taints to add to node pool nodes. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods. To learn more, review [Add labels and taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).\n\nSpecifying an empty array (`[]`) removes all previously set taints.")),
"type": z.string().describe("The Linode Type for all of the nodes in the node pool."),
"update_strategy": z.optional(z.enum(["rolling_update", "on_recycle"]).describe("__Beta__, __LKE Enterprise__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required when creating node pools on LKE Enterprise clusters.\n\n- `rolling_update`: Immediately triggers a recycle of this node pool when the Kubernetes version is updated.\n- `on_recycle` (default): Does not trigger any immediate recycle. New worker nodes are created with the new Kubernetes version. Existing worker nodes will be upgraded when a recycle is manually triggered.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`."))
    })

export const postLkeClusterPoolsMutationResponseSchema = z.lazy(() => postLkeClusterPools200Schema)