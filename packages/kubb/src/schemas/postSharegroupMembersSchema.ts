/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postSharegroupMembersPathParamsSchema = z.object({
    "sharegroupId": z.coerce.number().int().describe("The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.")
    })

/**
 * @description Member added successfully.
 */
export const postSharegroupMembers200Schema = z.object({
    "created": z.string().datetime().describe("__Read-only__ When the user became the share group's member."),
"expiry": z.nullable(z.string().datetime().describe("__Read-only__ When the user's share group membership expires.")),
"label": z.string().describe("The share group member's descriptive name."),
"status": z.enum(["active", "revoked"]).describe("__Read-only__ The current membership status. Share group members can be `active` or `revoked`. Revoked members' data remains available for two weeks after they left a share group."),
"token_uuid": z.string().describe("__Read-only__ The membership token's unique identifier. Not to be confused with the `token` needed to add a member to the share group."),
"updated": z.nullable(z.string().datetime().describe("__Read-only__ When the user's share group membership was last updated."))
    }).describe("The shared group membership's details.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postSharegroupMembersErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

export const postSharegroupMembersMutationRequestSchema = z.object({
    "label": z.string().describe("The share group member's descriptive name."),
"token": z.string().describe("The single-use JWT membership token encoded as a string. It consists of three parts: header, payload, and signature. If needed, the user should [Create a token](https://techdocs.akamai.com/linode-api/reference/post-sharegroup-tokens) and share it with the group owner before joining.")
    })

export const postSharegroupMembersMutationResponseSchema = z.lazy(() => postSharegroupMembers200Schema)