/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postLinodeConfigInterfacesPathParamsSchema = z.object({
    "linodeId": z.coerce.number().int().describe("The `id` of the Linode."),
"configId": z.coerce.number().int().describe("The `id` of the Configuration Profile.")
    })

/**
 * @description Interfaces successfully reordered.
 */
export const postLinodeConfigInterfaces200Schema = z.object({
    
    }).describe("The API responds with an empty object.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postLinodeConfigInterfacesErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

/**
 * @description The desired Interface order for the Configuration Profile.
 */
export const postLinodeConfigInterfacesMutationRequestSchema = z.object({
    "ids": z.array(z.int().describe("__Read-only__ The unique ID representing this interface.")).describe("An ordered array of existing Configuration Profile Interface `id`s.\n\n- All current Interface `id`s must be present in the array.\n- If the Configuration Profile contains Interfaces and is active on the Linode, the Linode must first be shut down prior to running this operation.\n- Reordering takes effect after rebooting the Linode with this Configuration Profile.\n\nThe position in the array determines which of the Linode's network Interfaces is configured:\n\n- First [0]:  eth0\n- Second [1]: eth1\n- Third [2]:  eth2")
    }).describe("Linode Configuration Interfaces Order request object.")

export const postLinodeConfigInterfacesMutationResponseSchema = z.lazy(() => postLinodeConfigInterfaces200Schema)