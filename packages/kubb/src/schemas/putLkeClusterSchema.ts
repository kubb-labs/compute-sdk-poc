/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const putLkeClusterPathParamsSchema = z.object({
    "apiVersion": z.enum(["v4", "v4beta"]).describe("__Enum__ Call either the `v4` URL, or `v4beta` for operations still in Beta."),
"clusterId": z.coerce.number().int().describe("ID of the Kubernetes cluster to look up.")
    })

/**
 * @description Returns a single Kubernetes cluster.
 */
export const putLkeCluster200Schema = z.object({
    "apl_enabled": z.optional(z.boolean().describe("__Write-once__ Indicates whether the Akamai App Platform is installed during creation of the LKE cluster. It defaults to `false`. If set to `true`, `control_plane.high_availability` also needs to be `true`. Automatic installation of the App Platform is only possible when creating a new cluster (not when modifying existing clusters).")),
"control_plane": z.optional(z.object({
    "audit_logs_enabled": z.optional(z.boolean().default(false).describe("__Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"high_availability": z.optional(z.boolean().default(false).describe("Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change."))
    }).describe("Defines settings for the Kubernetes control plane, including enabling High Availability (HA) for the control plane.")),
"created": z.optional(z.string().datetime().describe("__Read-only__ When this Kubernetes cluster was created.")),
"id": z.optional(z.int().describe("__Read-only__ This Kubernetes cluster's unique ID.")),
"k8s_version": z.optional(z.string().describe("__Filterable__ The desired Kubernetes version for this Kubernetes cluster in the format of `<major>.<minor>`. The latest supported patch version is deployed.")),
"label": z.optional(z.string().min(1).max(32).describe("__Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:\n\n  - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`\"\\u30ab\"`).\n\n  - 4 byte UTF-8 characters are not supported.\n\n  - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.")),
"region": z.optional(z.string().describe("__Filterable__ This Kubernetes cluster's location.")),
"stack_type": z.optional(z.enum(["ipv4", "ipv4-ipv6"]).default("ipv4").describe("__Beta__, __LKE Enterprise__ The networking stack type of the Kubernetes cluster. This specifies that the cluster is IPv4 only (default) or supports both IPv4 and IPv6 (dual-stack).\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"subnet_id": z.optional(z.int().describe("__Beta__, __LKE Enterprise__ The ID of the VPC subnet to use for the Kubernetes cluster. This subnet must have both IPv4 and IPv6 enabled (dual-stack). When this field is specified, the cluster is deployed to the given subnet and its corresponding VPC. To specify a VPC and have a subnet auto-allocated, use `vpc_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"tags": z.optional(z.array(z.string()).describe("__Filterable__ An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only.")),
"tier": z.optional(z.enum(["standard", "enterprise"]).describe("__Beta__, __Filterable__ The desired Kubernetes tier, either `standard` or `enterprise`.\n\n> 🚧\n>\n> This field is available as part of the beta API. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"updated": z.optional(z.string().datetime().describe("__Read-only__ When this Kubernetes cluster was updated.")),
"vpc_id": z.optional(z.int().describe("__Beta__, __LKE Enterprise__ The ID of the VPC to use for the Kubernetes cluster. An appropriately sized subnet is auto-allocated. To specify an existing subnet, use `subnet_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`."))
    }).describe("A Kubernetes cluster.")

/**
 * @description The fields to update the Kubernetes cluster.
 */
export const putLkeClusterMutationRequestSchema = z.object({
    "control_plane": z.optional(z.object({
    "acl": z.optional(z.object({
    "addresses": z.optional(z.object({
    "ipv4": z.optional(z.array(z.string()).describe("A list of individual ipv4 addresses or CIDRs to ALLOW. Defaults to `[]`.")),
"ipv6": z.optional(z.array(z.string()).describe("A list of individual ipv6 addresses or CIDRs to ALLOW. Defaults to `[]`."))
    }).describe("Supports keys `ipv4` and `ipv6`. Defaults to `{}`.")),
"enabled": z.optional(z.boolean().describe("Defines a default policy. A value of `true` results in a default policy of `DENY`. A value of `false` results in a default policy of `ALLOW`, such as for disabled access controls. It defaults to `true`. Creating a cluster with ACL, or upgrading a cluster to use ACL for LKE, is an irreversible change. Once upgraded, you can only toggle access controls with this field.")),
"revision-id": z.optional(z.string().describe("Enables clients to track events related to ACL update requests and enforcements. Optional field. If omitted, defaults to a randomly generated string."))
    }).describe("Defines settings related to the IP-based ACL of the LKE cluster. The object requires the `enabled` and `addresses` keys. It also supports the optional key `revision-id`. The default policy is set to `ALLOW`, so that access controls are disabled. An empty object value (`{}`) sets default elements.")),
"audit_logs_enabled": z.optional(z.boolean().default(false).describe("__Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.\n\n> 🚧\n>\n> This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.")),
"high_availability": z.optional(z.boolean().default(false).describe("Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change."))
    }).describe("Defines settings for the Kubernetes control plane, including High Availability (HA) and an IP-based Access Control List (ACL) for the control plane components.\n\nWhen upgrading pre-existing LKE clusters to use the HA Control Plane, the following changes will additionally occur:\n\n- All nodes will be deleted and new nodes will be created to replace them.\n\n- Any local storage (such as `hostPath` volumes) will be erased.\n\n- The upgrade process may take several minutes to complete, as nodes will be replaced on a rolling basis.\n\nWhen upgrading pre-existing LKE clusters to use the control plane ACL for the first time, it may take several hours for external clients to respect the access control settings. This is partly due to delays from DNS propagation.")),
"k8s_version": z.optional(z.string().describe("The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;. New and recycled Nodes in this cluster will be installed with the latest available patch for the Cluster's Kubernetes version.\n\nWhen upgrading the Kubernetes version, only the next latest minor version following the current version can be deployed. For example, a cluster with Kubernetes version 1.29 can be upgraded to version 1.30, but not directly to 1.31.\n\nThe Kubernetes version of a cluster can not be downgraded.")),
"label": z.optional(z.string().min(1).max(32).describe("__Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:\n\n  - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`\"\\u30ab\"`).\n\n  - 4 byte UTF-8 characters are not supported.\n\n  - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.")),
"tags": z.optional(z.array(z.string()).describe("An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only. To delete a tag, exclude it from your `tags` array."))
    })

export const putLkeClusterMutationResponseSchema = z.lazy(() => putLkeCluster200Schema)