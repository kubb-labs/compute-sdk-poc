/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

/**
 * @description The bucket created successfully.
 */
export const postObjectStorageBucket200Schema = z.object({
    "cluster": z.string().describe("__Deprecated__ The legacy `clusterId` equivalent for the `regionId` where this bucket lives. The API maintains this for backward compatibility.\n\n> 📘\n>\n> - This value and the `regionId` are interchangeable when used in requests. Best practice is to use the `regionId`.\n>\n> - This value is empty for newer regions that don't have a legacy `clusterId`."),
"created": z.string().datetime().describe("When this bucket was created."),
"endpoint_type": z.enum(["E0", "E1", "E2", "E3"]).describe("The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information."),
"hostname": z.string().describe("The hostname where this bucket can be accessed. This hostname can be accessed through a browser if the bucket is made public."),
"label": z.string().describe("The name of this bucket."),
"objects": z.int().describe("The number of objects stored in this bucket."),
"region": z.string().describe("The `id` of the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where this Object Storage bucket lives."),
"s3_endpoint": z.string().describe("The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`."),
"size": z.int().describe("The size of the bucket in bytes.")
    }).describe("An Object Storage bucket.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postObjectStorageBucketErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

/**
 * @description Information about the bucket you want to create.
 */
export const postObjectStorageBucketMutationRequestSchema = z.object({
    "acl": z.optional(z.enum(["private", "public-read", "authenticated-read", "public-read-write"]).default("private").describe("The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).")),
"cors_enabled": z.optional(z.boolean().describe("If set to `false`, cross-origin resource sharing (CORS) is disabled for all origins in the bucket.")),
"endpoint_type": z.optional(z.enum(["E0", "E1", "E2", "E3"]).describe("The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.")),
"label": z.string().regex(/^(?=.{3,63}$)[a-z0-9]+(?:-[a-z0-9]+)*(?:[.][a-z0-9]+(?:-[a-z0-9]+)*)*$/).describe("The name for this bucket.\n\n* A bucket name can contain from 3 to 63 alphanumeric characters, dashes (`-`), or dots (`.`).\n* A bucket name can't end in a dash and you can't use two consecutive dashes.\n* A bucket name can't start or end in a dot, and you can't use two consecutive dots. As a best practice, only use dots if a certificate you're using with your bucket requires it. (For example, if you're using a custom TLS certificate.)\n* A bucket name needs to be unique in the `region` where you're creating the bucket. The API only reserves labels for the `region` where active buckets are created and stored. If you want to reserve this bucket's label in another `region`, create a new bucket with the same label in the new `region`."),
"region": z.optional(z.string().describe("The `id` assigned to the data center ([region](https://techdocs.akamai.com/linode-api/reference/get-regions)) where this Object Storage bucket should be created.\n\n> 📘\n>\n> This supports legacy `clusterId` values that represented a specific region. For example, `us-east-1` is the legacy reference for the `us-east` region.")),
"s3_endpoint": z.optional(z.string().describe("The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`."))
    })

export const postObjectStorageBucketMutationResponseSchema = z.lazy(() => postObjectStorageBucket200Schema)