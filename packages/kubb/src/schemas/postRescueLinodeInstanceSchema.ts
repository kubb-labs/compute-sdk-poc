/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

export const postRescueLinodeInstancePathParamsSchema = z.object({
    "linodeId": z.coerce.number().int().describe("ID of the Linode to rescue.")
    })

/**
 * @description Rescue started.
 */
export const postRescueLinodeInstance200Schema = z.object({
    
    }).describe("The API responds with an empty object.")

/**
 * @description See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
 */
export const postRescueLinodeInstanceErrorSchema = z.object({
    "errors": z.array(z.object({
    "field": z.string().describe("The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request."),
"reason": z.string().describe("What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.")
    }).describe("An object for describing a single error that occurred during the processing of a request."))
    })

/**
 * @description Optional object of devices to be mounted.
 */
export const postRescueLinodeInstanceMutationRequestSchema = z.object({
    "devices": z.optional(z.object({
    "sda": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdb": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdc": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdd": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sde": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdf": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.")),
"sdg": z.optional(z.object({
    "disk_id": z.optional(z.int().describe("The Disk ID, or `null` if a Volume is assigned to this slot.")),
"volume_id": z.int().describe("The Volume ID, or `null` if a Disk is assigned to this slot.").nullish()
    }).describe("Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode."))
    }))
    })

export const postRescueLinodeInstanceMutationResponseSchema = z.lazy(() => postRescueLinodeInstance200Schema)