// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://api.linode.com/v4/' | (string & {});
};

/**
 * Account object.
 */
export type Account = {
    readonly active_promotions: Array<{
        /**
         * The amount available to spend per month.
         */
        credit_monthly_cap: string;
        /**
         * The total amount of credit left for this promotion.
         */
        credit_remaining: string;
        /**
         * A detailed description of this promotion.
         */
        description: string;
        /**
         * When this promotion's credits expire.
         */
        expire_dt: string;
        /**
         * The location of an image for this promotion.
         */
        image_url: string;
        /**
         * The service to which this promotion applies.
         */
        service_type: 'all' | 'backup' | 'blockstorage' | 'db_mysql' | 'ip_v4' | 'linode' | 'linode_disk' | 'linode_memory' | 'longview' | 'managed' | 'nodebalancer' | 'objectstorage' | 'placement_group' | 'transfer_tx';
        /**
         * Short details of this promotion.
         */
        summary: string;
        /**
         * The amount of credit left for this month for this promotion.
         */
        this_month_credit_remaining: string;
    }>;
    /**
     * __Read-only__ The date and time the account was activated.
     */
    readonly active_since: string;
    /**
     * The first line of this account's billing address.
     */
    address_1: string;
    /**
     * The second line of this account's billing address.
     */
    address_2: string;
    /**
     * __Read-only__ This account's balance, in US dollars.
     */
    readonly balance: number;
    /**
     * __Read-only__ This account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
     */
    readonly balance_uninvoiced: number;
    /**
     * __Read-only__ The source of service charges for this account. Accounts that are associated with Akamai-specific customers return a value of `akamai`. All other accounts return a value of `linode`.
     */
    billing_source: 'akamai' | 'linode';
    /**
     * __Read-only__ The Akamai Cloud Computing services your account supports.
     */
    readonly capabilities: Array<string>;
    /**
     * The city for this account's `address`.
     */
    city: string;
    /**
     * The company name assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
     */
    company: string;
    /**
     * The two-letter ISO 3166 country code for this account's `address`.
     */
    country: string;
    /**
     * __Read-only__ The credit card information assigned to this account.
     */
    readonly credit_card: {
        /**
         * The expiration month and year of the `credit_card`.
         */
        expiry: string;
        /**
         * The last four digits of the `credit_card` assigned to this account.
         */
        last_four: string;
    };
    /**
     * The email address of the person assigned to this account.
     */
    email: string;
    /**
     * __Read-only__ An external unique identifier for this account.
     */
    readonly euuid: string;
    /**
     * The first name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
     */
    first_name: string;
    /**
     * The last name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
     */
    last_name: string;
    /**
     * The phone number assigned to this account.
     */
    phone: string;
    /**
     * The state or province for the `address` set for your account, if applicable.
     *
     * - If the `address` is in the United States (US) or Canada (CA), this is the two-letter ISO 3166 code for the state or province.
     *
     * - If it's a US military `address`, this is the abbreviation for that territory. This includes `AA` for Armed Forces Americas (excluding Canada), `AE` for Armed Forces Africa, Europe, Middle East, and Canada, or `AP` for Armed Forces Pacific.
     *
     * - If outside the US or CA, this is the province associated with the account's `address`.
     */
    state: string;
    /**
     * The tax identification number (TIN) assigned to this account, used for tax calculations. A TIN is set by the national authorities in your `country`, based on your `address_1`, and it may be named differently between countries. Set to an empty string (`""`) if a TIN doesn't apply or for countries that don't collect tax.
     *
     * > 📘
     * >
     * > This value is externally validated. If the validation is successful, a `tax_id_valid` [event](https://techdocs.akamai.com/linode-api/reference/get-events) is triggered. If unsuccessful, a `tax_id_invalid` event is triggered and an error response is issued for an operation that included it.
     */
    tax_id: string;
    /**
     * The zip code for this account's `address`.
     *
     * - It can only contain ASCII letters, numbers, and dashes (`-`).
     *
     * - It can't contain more than nine letter or number characters.
     */
    zip: string;
};

/**
 * Account Service Availability object.
 */
export type AccountAvailability = {
    /**
     * __Read-only__ A list of services _available_ to your account in the `region`.
     */
    readonly available: Array<string>;
    /**
     * __Read-only__ The Akamai cloud computing data center (region), represented by a slug value. You can view a full list of regions and their associated slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation.
     */
    readonly region: string;
    /**
     * __Read-only__ A list of services _unavailable_ to your account in the `region`.
     */
    readonly unavailable: Array<string>;
};

/**
 * Account Settings object.
 */
export type AccountSettings = {
    /**
     * Account-wide backups default.  If `true`, all Linodes created will automatically be enrolled in the Backups service.  If `false`, Linodes will not be enrolled by default, but may still be enrolled on creation or later.
     */
    backups_enabled: boolean;
    /**
     * __Beta__ Defines if new Linodes can use legacy configuration interfaces:
     * - `legacy_config_only`. All new Linodes need to use legacy configuration interfaces. Prevously created Linodes with Linode Interfaces can still exist. Linodes using legacy configuration interfaces can't be upgraded to use Linode Interfaces.
     * - `legacy_config_default_but_linode_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use legacy configuration interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode Interfaces. This is the default setting for existing accounts.
     * - `linode_default_but_legacy_config_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use Linode Interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode interfaces. This is the default setting for new accounts.
     * - `linode_only`. All new Linodes need to use Linode Interfaces. Prevously created Linodes with legacy configuration profile interfaces can still exist if they were created under a previous setting. Linodes using legacy configuration interfaces can be upgraded to Linode Interfaces.
     */
    interfaces_for_new_linodes: 'legacy_config_only' | 'legacy_config_default_but_linode_allowed' | 'linode_default_but_legacy_config_allowed' | 'linode_only';
    /**
     * __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be a [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions) ID or `null` for Longview Free.
     */
    readonly longview_subscription: string;
    /**
     * __Beta__ Defines the default maintenance policy for new Linodes created on this account. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
     */
    maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
    /**
     * __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring system distributes monitoring checks to ensure that your servers remain online and available at all times. Linode Managed can monitor any service or software stack reachable over TCP or HTTP. Once you add a service to Linode Managed, we'll monitor it for connectivity, response, and total request time.
     */
    readonly managed: boolean;
    /**
     * Enables network helper across all users by default for new Linodes and Linode Configs.
     */
    network_helper: boolean;
    /**
     * __Read-only__ A string describing the status of this account's Object Storage service enrollment.
     */
    object_storage: 'disabled' | 'suspended' | 'active';
};

/**
 * The API responds with an empty object.
 */
export type AddedEmptyObj = {
    [key: string]: unknown;
};

export type AddedGetAccountLogins200 = {
    data: Array<{
        /**
         * __Read-only__ When the login was initiated.
         */
        readonly datetime: string;
        /**
         * __Read-only__ The unique ID of this login object.
         */
        readonly id: number;
        /**
         * __Read-only__ The remote IP address that requested the login.
         */
        readonly ip: string;
        /**
         * __Read-only__ True if the User that attempted the login was a restricted User, false otherwise.
         */
        readonly restricted: boolean;
        /**
         * __Read-only__ Whether the login attempt succeeded or failed.
         */
        status: 'successful' | 'failed';
        /**
         * __Read-only__ The username of the User that attempted the login.
         */
        readonly username: string;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetAvailability200 = {
    data: Array<{
        /**
         * __Read-only__ A list of services _available_ to your account in the `region`.
         */
        readonly available: Array<string>;
        /**
         * __Read-only__ The Akamai cloud computing data center (region), represented by a slug value. You can view a full list of regions and their associated slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation.
         */
        readonly region: string;
        /**
         * __Read-only__ A list of services _unavailable_ to your account in the `region`.
         */
        readonly unavailable: Array<string>;
    }>;
} & {
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetChildAccounts200 = {
    data: Array<{
        /**
         * __Read-only__ The activation date and time for the child account.
         */
        readonly active_since: string;
        /**
         * __Filterable__ First line of this child account's billing address.
         */
        address_1: string;
        /**
         * __Filterable__ Second line of this child account's billing address, if applicable.
         */
        address_2: string;
        /**
         * __Read-only__ This child account's balance, in US dollars.
         */
        readonly balance: number;
        /**
         * __Read-only__ This child account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
         */
        readonly balance_uninvoiced: number;
        /**
         * __Read-only__ The source of service charges for this account, as determined by its relationship with Akamai. The API returns a value of `external` to describe a child account in a parent-child account environment.
         */
        billing_source: 'external';
        /**
         * __Read-only__ A list of the capabilities the child account supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Filterable__ The city for this child account's billing address.
         */
        city: string;
        /**
         * __Filterable__ The company name for the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`. You can't change this value yourself. We use it to create the proxy users that a parent account uses to access a child account. Talk to your account team if you need to change this value.
         */
        company: string;
        /**
         * __Filterable__ The two-letter ISO 3166 country code for this child account's billing address.
         */
        country: string;
        /**
         * __Read-only__ Information for the credit card you've assigned to this child account.
         */
        readonly credit_card: {
            /**
             * The expiration month and year of the credit card.
             */
            expiry: string;
            /**
             * The last four digits of the credit card.
             */
            last_four: string;
        };
        /**
         * __Filterable__ The email address of the owner of this child account.
         */
        email: string;
        /**
         * __Read-only__ An external, unique identifier that Akamai assigned to the child account.
         */
        readonly euuid: string;
        /**
         * __Filterable__ The first name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
         */
        first_name: string;
        /**
         * __Filterable__ The last name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
         */
        last_name: string;
        /**
         * __Filterable__ The phone number for the owner of this child account.
         */
        phone: string;
        /**
         * __Filterable__ The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.
         *
         * > 📘
         * >
         * > If this is a US military address, use state abbreviations (AA, AE, AP).
         */
        state: string;
        /**
         * The tax identification number for this child account. Use this for tax calculations in some countries. If you live in a country that doesn't collect taxes, ensure this is an empty string (`""`).
         */
        tax_id: string;
        /**
         * __Filterable__ The zip code of this Account's billing address. The following restrictions apply:
         *
         * - Can only contain ASCII letters, numbers, and hyphens (`-`).
         * - Can't contain more than 9 letter or number characters.
         */
        zip: string;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetClients200 = {
    data: Array<{
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetEnrolledBetaPrograms200 = {
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
} & {
    data: Array<{
        /**
         * __Read-only__ Additional details regarding the Beta Program.
         */
        readonly description: string | null;
        /**
         * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
         *
         * `null` indicates that the Beta Program is ongoing.
         */
        readonly ended: string | null;
        /**
         * __Filterable__, __Read-only__ The date-time of Account enrollment to the Beta Program.
         */
        readonly enrolled: string;
        /**
         * The unique identifier of the Beta Program.
         */
        id: string;
        /**
         * __Filterable__, __Read-only__ The name of the Beta Program.
         */
        readonly label: string;
        /**
         * __Filterable__, __Read-only__ The start date-time of the Beta Program.
         */
        readonly started: string;
    }>;
};

export type AddedGetEntityTransfers200 = {
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
} & {
    data?: Array<{
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the entities to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request:
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
         * `canceled`: The transfer has been canceled by the sender.
         * `completed`: The transfer has completed successfully.
         * `failed`: The transfer has failed after initiation.
         * `pending`: The transfer is ready to be accepted.
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    }>;
};

export type AddedGetInvoiceItems200 = {
    data: Array<{
        /**
         * __Read-only__ The price, in US dollars, of the Invoice Item. Equal to the unit price multiplied by quantity.
         */
        readonly amount: number;
        /**
         * __Read-only__ The date the Invoice Item started, based on month.
         */
        readonly from: string;
        /**
         * __Read-only__ The Invoice Item's display label.
         */
        readonly label: string;
        /**
         * __Read-only__ The quantity of this Item for the specified Invoice.
         */
        readonly quantity: number;
        /**
         * __Read-only__ The ID of the applicable Region associated with this Invoice Item.
         *
         * `null` if there is no applicable Region.
         */
        readonly region: string | null;
        /**
         * __Read-only__ The amount of tax levied on this Item in US Dollars.
         */
        readonly tax: number;
        /**
         * __Read-only__ The date the Invoice Item ended, based on month.
         */
        readonly to: string;
        /**
         * __Read-only__ The price of this Item after taxes in US Dollars.
         */
        readonly total: number;
        /**
         * __Read-only__ The type of service, ether `hourly` or `misc`.
         */
        type: 'hourly' | 'misc';
        /**
         * __Read-only__ The monthly service fee in US Dollars for this Item.
         */
        readonly unit_price: string;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetInvoices200 = {
    data: Array<{
        /**
         * __Filterable__, __Read-only__ The source of service charges for this invoice. A value of `akamai` indicates an invoice generated according to the terms of an agreement between the customer and Akamai. A value of `linode` indicates an invoice was generated according to the default terms, prices, and discounts.
         */
        billing_source: 'akamai' | 'linode';
        /**
         * __Filterable__, __Read-only__ When this Invoice was generated.
         */
        readonly date: string;
        /**
         * __Read-only__ The Invoice's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ The Invoice's display label.
         */
        readonly label: string;
        /**
         * __Read-only__ The amount of the Invoice before taxes in US Dollars.
         */
        readonly subtotal: number;
        /**
         * __Read-only__ The amount of tax levied on the Invoice in US Dollars.
         */
        readonly tax: number;
        /**
         * __Read-only__ The amount of tax broken down into subtotals by source.
         */
        readonly tax_summary: Array<{
            /**
             * The source of this tax subtotal.
             */
            name: string;
            /**
             * The amount of tax subtotal attributable to this source.
             */
            tax: number;
        }>;
        /**
         * __Filterable__, __Read-only__ The amount of the Invoice after taxes in US Dollars.
         */
        readonly total: number;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetNotifications200 = {
    data: Array<{
        /**
         * A full description of this notification, in markdown format. Not all notifications include a `body`. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        body: string | null;
        /**
         * Detailed information about the notification. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        entity: {
            /**
             * The unique ID of the notification's entity, based on the entity type. Returns `null` for an `account` or `promotion` entity.
             */
            id: number | null;
            /**
             * The current name of this notification's entity. Returns `null` for the following `entity` types:
             *
             * - `entity_transfer`
             *
             * - `promotion`
             *
             * - `region`
             */
            label: string | null;
            /**
             * __Filterable__ The type of entity this is related to. An entity can be product or service-specific, such as a `linode`, `loadbalancers`, or `nodebalancers`. It can apply to a specific component, such as your `account`, a specific `promotion` your participating in, a data center (`region`) where you're using services, a transfer from one component to another (an `entity_transfer`), a support `ticket` you've opened, or a `volume` on a specific Linode.
             */
            type: 'account' | 'entity_transfer' | 'linode' | 'loadbalancers' | 'nodebalancer' | 'promotion' | 'region' | 'ticket' | 'volume';
            /**
             * The URL where you can access the notification's object. The URL is relative to the domain where you retrieved the notification. This value is `null` for the `promotion` entity type.
             */
            url: string | null;
        } | null;
        /**
         * A short description of this notification.
         */
        label: string;
        /**
         * A human-readable description of the notification.
         */
        message: string;
        /**
         * The severity of this notification. This field determines how prominently the notification is displayed and the color of the display text.
         */
        severity: 'minor' | 'major' | 'critical';
        /**
         * __Filterable__ The type of notification.
         *
         * > 📘
         * >
         * > A `security_reboot_maintenance_scheduled` event is a global notice that a Linode needs to be rebooted for QEMU upgrade maintenance. Have a look at [this workflow](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance) for guidance on reboooting your Linodes for this maintenance.
         */
        type: 'migration_scheduled' | 'migration_imminent' | 'migration_pending' | 'reboot_scheduled' | 'outage' | 'payment_due' | 'ticket_important' | 'ticket_abuse' | 'notice' | 'maintenance' | 'maintenance_scheduled' | 'promotion' | 'security_reboot_maintenance_scheduled' | 'tax_id_verifying';
        /**
         * If this notification has a duration, this is when the event or action will complete. For example, if there's scheduled maintenance for one of our systems, `until` represents the end of the maintenance window. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        until: string | null;
        /**
         * If this notification is for an event in the future, this specifies when the action occurs. For example, if a compute instance needs to migrate in response to a security advisory, this field sets the approximate time the compute instance will be taken offline for migration. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        when: string | null;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetPaymentMethods200 = {
    data: Array<{
        /**
         * __Read-only__ When the Payment Method was added to the Account.
         */
        readonly created: string;
        data: {
            /**
             * __Read-only__ The type of credit card.
             */
            readonly card_type: string;
            /**
             * __Read-only__ The expiration month and year of the credit card.
             */
            readonly expiry: string;
            /**
             * __Read-only__ The last four digits of the credit card number.
             */
            readonly last_four: string;
        } | {
            /**
             * __Read-only__ The type of credit card.
             */
            readonly card_type: string;
            /**
             * __Read-only__ The expiration month and year of the credit card.
             */
            readonly expiry: string;
            /**
             * __Read-only__ The last four digits of the credit card number.
             */
            readonly last_four: string;
        } | {
            /**
             * __Read-only__ The email address associated with your PayPal account.
             */
            readonly email: string;
            /**
             * __Read-only__ PayPal Merchant ID associated with your PayPal account.
             */
            readonly paypal_id: string;
        };
        /**
         * The unique ID of this Payment Method.
         */
        id: number;
        /**
         * Whether this Payment Method is the default method for automatically processing service charges.
         */
        is_default: boolean;
        /**
         * The type of Payment Method.
         */
        type: 'credit_card' | 'google_pay' | 'paypal';
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetPayments200 = {
    data: Array<{
        /**
         * __Filterable__, __Read-only__ When the payment was made.
         */
        readonly date: string;
        /**
         * __Read-only__ The unique ID of the payment.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ The amount, in US dollars, of the payment.
         */
        readonly usd: number;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetServiceTransfers200 = {
    data: Array<{
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the services to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request.
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress.
         * Transfers can take up to 3 hours to complete.
         *
         * `canceled`: The transfer has been canceled by the sender.
         *
         * `completed`: The transfer has completed successfully.
         *
         * `failed`: The transfer has failed after initiation.
         *
         * `pending`: The transfer is ready to be accepted.
         *
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
         * canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

export type AddedGetUser200 = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
     */
    readonly last_login: {
        /**
         * __Read-only__ The date and time of this user's most recent login attempt.
         */
        readonly login_datetime: string;
        /**
         * __Read-only__ The result of this user's most recent login attempt.
         */
        status: 'successful' | 'failed';
    } | null;
    /**
     * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
     */
    readonly password_created: string | null;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
     *
     * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
     *
     * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
     *
     * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
     */
    readonly ssh_keys: Array<string>;
    /**
     * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
     */
    readonly tfa_enabled: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
    /**
     * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
     */
    readonly verified_phone_number: string | null;
} & {
    /**
     * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
     *
     * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
     *
     * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
     *
     * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
     *
     * - `default`. This applies to all regular, non-parent-child account users.
     */
    user_type: 'parent' | 'child' | 'proxy' | 'default';
};

export type AddedGetUsers200 = {
    data: Array<{
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
         */
        readonly last_login: {
            /**
             * __Read-only__ The date and time of this user's most recent login attempt.
             */
            readonly login_datetime: string;
            /**
             * __Read-only__ The result of this user's most recent login attempt.
             */
            status: 'successful' | 'failed';
        } | null;
        /**
         * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
         */
        readonly password_created: string | null;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
         *
         * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
         *
         * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
         *
         * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
         */
        readonly ssh_keys: Array<string>;
        /**
         * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
         */
        readonly tfa_enabled: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
        /**
         * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    } & {
        /**
         * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
         *
         * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
         *
         * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
         *
         * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
         *
         * - `default`. This applies to all regular, non-parent-child account users.
         */
        user_type: 'parent' | 'child' | 'proxy' | 'default';
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

/**
 * The Beta Program ID to enroll in for your Account.
 */
export type AddedPostBetaProgram = {
    /**
     * The unique identifier of the Beta Program.
     */
    id: string;
};

export type AddedPostCancelAccount = {
    /**
     * Any reason for cancelling the account, and any other comments you might have about your Linode service.
     */
    comments: string;
};

export type AddedPostCancelAccount200 = {
    /**
     * A link to Linode's exit survey.
     */
    survey_link: string;
};

export type AddedPostCancelAccount409 = {
    errors: Array<{
        /**
         * A string explaining that the account could not be canceled because there is an outstanding balance on the account that must be paid first.
         */
        reason: string;
    }>;
};

export type AddedPostCancelAccount504 = {
    errors: Array<{
        /**
         * A string explaining that the account is taking longer to close than expected.
         */
        reason: string;
    }>;
};

/**
 * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
 */
export type AddedPostClient = {
    /**
     * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
     */
    readonly id: string;
    /**
     * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
     */
    label: string;
    /**
     * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
     */
    public: boolean;
    /**
     * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
     */
    redirect_uri: string;
    /**
     * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
     */
    readonly secret: string;
    /**
     * __Read-only__ The status of this application.  `active` by default.
     */
    status: 'active' | 'disabled' | 'suspended';
    /**
     * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
     */
    readonly thumbnail_url: string | null;
};

export type AddedPostEntityTransfer = {
    /**
     * A collection of the entities to include in this transfer request, separated by type.
     */
    entities: {
        /**
         * An array containing the IDs of each of the Linodes included in this transfer.
         */
        linodes: Array<number>;
    };
};

export type AddedPostPayPalPayment200 = {
    /**
     * __Read-only__ The checkout token generated for this Payment.
     */
    readonly checkout_token: string;
    /**
     * The paypal-generated ID for this Payment. Used when authorizing the Payment in PayPal's interface.
     */
    payment_id: string;
};

export type AddedPostPayment = {
    /**
     * The ID of the Payment Method to apply to the Payment.
     */
    payment_method_id: number;
    /**
     * The amount in US Dollars of the Payment.
     *
     * - Can begin with or without `$`.
     * - Commas (`,`) are not accepted.
     * - Must end with a decimal expression, such as `.00` or `.99`.
     * - Minimum: `$5.00` or the Account balance, whichever is lower.
     * - Maximum: `$2000.00` or the Account balance up to `$50000.00`, whichever is greater.
     */
    usd: string;
};

/**
 * Payment Method Request Object.
 */
export type AddedPostPaymentMethod = {
    /**
     * An object representing the credit card information you have on file with Linode to make Payments against your Account.
     */
    data: {
        /**
         * Your credit card number. No spaces or hyphens (`-`) allowed.
         */
        card_number: string;
        /**
         * CVV (Card Verification Value) of the credit card, typically found on the back of the card.
         */
        cvv: string;
        /**
         * A value from 1-12 representing the expiration month of your credit card.
         *
         * - 1 = January
         * - 2 = February
         * - 3 = March
         * - Etc.
         */
        expiry_month: number;
        /**
         * A four-digit integer representing the expiration year of your credit card.
         *
         * The combination of `expiry_month` and `expiry_year` must result in a month/year combination of the current month or in the future. An expiration date set in the past is invalid.
         */
        expiry_year: number;
    };
    /**
     * Whether this Payment Method is the default method for automatically processing service charges.
     */
    is_default: boolean;
    /**
     * The type of Payment Method.
     *
     * Alternative Payment Methods including Google Pay and PayPal can be added using the Cloud Manager. See the [Manage Payment Methods](https://www.linode.com/docs/products/platform/billing/guides/payment-methods/) guide
     * for details and instructions.
     */
    type: 'credit_card';
};

export type AddedPostPromoCredit = {
    /**
     * The Promo Code.
     */
    promo_code: string;
};

export type AddedPostServiceTransfer = {
    /**
     * A collection of the services to include in this transfer request, separated by type.
     */
    entities: {
        /**
         * An array containing the IDs of each of the Linodes included in this transfer.
         */
        linodes: Array<number>;
    };
};

/**
 * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
 */
export type AddedPostUser = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
     */
    readonly last_login: {
        /**
         * __Read-only__ The date and time of this user's most recent login attempt.
         */
        readonly login_datetime: string;
        /**
         * __Read-only__ The result of this user's most recent login attempt.
         */
        status: 'successful' | 'failed';
    } | null;
    /**
     * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
     */
    readonly password_created: string | null;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
     *
     * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
     *
     * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
     *
     * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
     */
    readonly ssh_keys: Array<string>;
    /**
     * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
     */
    readonly tfa_enabled: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
    /**
     * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
     */
    readonly verified_phone_number: string | null;
};

export type AddedPutUser200 = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
     */
    readonly last_login: {
        /**
         * __Read-only__ The date and time of this user's most recent login attempt.
         */
        readonly login_datetime: string;
        /**
         * __Read-only__ The result of this user's most recent login attempt.
         */
        status: 'successful' | 'failed';
    } | null;
    /**
     * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
     */
    readonly password_created: string | null;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
     *
     * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
     *
     * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
     *
     * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
     */
    readonly ssh_keys: Array<string>;
    /**
     * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
     */
    readonly tfa_enabled: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
    /**
     * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
     */
    readonly verified_phone_number: string | null;
} & {
    /**
     * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
     *
     * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
     *
     * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
     *
     * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
     *
     * - `default`. This applies to all regular, non-parent-child account users.
     */
    user_type: 'parent' | 'child' | 'proxy' | 'default';
};

/**
 * Acknowledgment status for agreements on your account. When acknowledging any agreements, set them to `true` and omit any remainders.
 */
export type Agreements = {
    /**
     * Certain regions require that you share your tax identification number (TIN) with Akamai. When you do, you need to acknowledge Akamai's [privacy statement](https://www.akamai.com/legal/privacy-statement) agreement, in regards to its protection. When set to `true`, you've acknowledged this agreement.
     */
    billing_agreement: boolean;
    /**
     * The acknowledgement status for the [cross-border data transfer](https://www.akamai.com/legal/compliance/privacy-trust-center/cross-border-data-transfer-statement) agreement.
     */
    eu_model: boolean;
    /**
     * The acknowledgement status for Akamai's [master service agreement](https://www.linode.com/legal-msa/).
     */
    master_service_agreement: boolean;
    /**
     * The acknowledgement status for Akamai's [privacy statement](https://www.akamai.com/legal/privacy-statement).
     */
    privacy_policy: boolean;
};

/**
 * An object representing an enrolled Beta Program for the Account.
 */
export type BetaProgramEnrolled = {
    /**
     * __Read-only__ Additional details regarding the Beta Program.
     */
    readonly description: string | null;
    /**
     * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
     *
     * `null` indicates that the Beta Program is ongoing.
     */
    readonly ended: string | null;
    /**
     * __Filterable__, __Read-only__ The date-time of Account enrollment to the Beta Program.
     */
    readonly enrolled: string;
    /**
     * The unique identifier of the Beta Program.
     */
    id: string;
    /**
     * __Filterable__, __Read-only__ The name of the Beta Program.
     */
    readonly label: string;
    /**
     * __Filterable__, __Read-only__ The start date-time of the Beta Program.
     */
    readonly started: string;
};

/**
 * Child account object.
 */
export type ChildAccount = {
    /**
     * __Read-only__ The activation date and time for the child account.
     */
    readonly active_since: string;
    /**
     * __Filterable__ First line of this child account's billing address.
     */
    address_1: string;
    /**
     * __Filterable__ Second line of this child account's billing address, if applicable.
     */
    address_2: string;
    /**
     * __Read-only__ This child account's balance, in US dollars.
     */
    readonly balance: number;
    /**
     * __Read-only__ This child account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
     */
    readonly balance_uninvoiced: number;
    /**
     * __Read-only__ The source of service charges for this account, as determined by its relationship with Akamai. The API returns a value of `external` to describe a child account in a parent-child account environment.
     */
    billing_source: 'external';
    /**
     * __Read-only__ A list of the capabilities the child account supports.
     */
    readonly capabilities: Array<string>;
    /**
     * __Filterable__ The city for this child account's billing address.
     */
    city: string;
    /**
     * __Filterable__ The company name for the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`. You can't change this value yourself. We use it to create the proxy users that a parent account uses to access a child account. Talk to your account team if you need to change this value.
     */
    company: string;
    /**
     * __Filterable__ The two-letter ISO 3166 country code for this child account's billing address.
     */
    country: string;
    /**
     * __Read-only__ Information for the credit card you've assigned to this child account.
     */
    readonly credit_card: {
        /**
         * The expiration month and year of the credit card.
         */
        expiry: string;
        /**
         * The last four digits of the credit card.
         */
        last_four: string;
    };
    /**
     * __Filterable__ The email address of the owner of this child account.
     */
    email: string;
    /**
     * __Read-only__ An external, unique identifier that Akamai assigned to the child account.
     */
    readonly euuid: string;
    /**
     * __Filterable__ The first name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
     */
    first_name: string;
    /**
     * __Filterable__ The last name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
     */
    last_name: string;
    /**
     * __Filterable__ The phone number for the owner of this child account.
     */
    phone: string;
    /**
     * __Filterable__ The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.
     *
     * > 📘
     * >
     * > If this is a US military address, use state abbreviations (AA, AE, AP).
     */
    state: string;
    /**
     * The tax identification number for this child account. Use this for tax calculations in some countries. If you live in a country that doesn't collect taxes, ensure this is an empty string (`""`).
     */
    tax_id: string;
    /**
     * __Filterable__ The zip code of this Account's billing address. The following restrictions apply:
     *
     * - Can only contain ASCII letters, numbers, and hyphens (`-`).
     * - Can't contain more than 9 letter or number characters.
     */
    zip: string;
};

/**
 * An object representing the credit card information you have on file with Linode to make Payments against your Account.
 */
export type CreditCard = {
    /**
     * Your credit card number. No spaces or hyphens (`-`) allowed.
     */
    card_number: string;
    /**
     * CVV (Card Verification Value) of the credit card, typically found on the back of the card.
     */
    cvv: string;
    /**
     * A value from 1-12 representing the expiration month of your credit card.
     *
     * - 1 = January
     * - 2 = February
     * - 3 = March
     * - Etc.
     */
    expiry_month: number;
    /**
     * A four-digit integer representing the expiration year of your credit card.
     *
     * The combination of `expiry_month` and `expiry_year` must result in a month/year combination of the current month or in the future. An expiration date set in the past is invalid.
     */
    expiry_year: number;
};

/**
 * Credit card
 *
 * Credit card information.
 */
export type CreditCardData = {
    /**
     * __Read-only__ The type of credit card.
     */
    readonly card_type: string;
    /**
     * __Read-only__ The expiration month and year of the credit card.
     */
    readonly expiry: string;
    /**
     * __Read-only__ The last four digits of the credit card number.
     */
    readonly last_four: string;
};

/**
 * __Read-only__ Additional details regarding the Beta Program.
 */
export type Description = string | null;

/**
 * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
 *
 * `null` indicates that the Beta Program is ongoing.
 */
export type Ended = string | null;

/**
 * A collection of the entities to include in this transfer request, separated by type.
 */
export type Entities = {
    /**
     * An array containing the IDs of each of the Linodes included in this transfer.
     */
    linodes: Array<number>;
};

/**
 * An object representing an Entity Transfer.
 */
export type EntityTransfer = {
    /**
     * When this transfer was created.
     */
    created: string;
    /**
     * A collection of the entities to include in this transfer request, separated by type.
     */
    entities: {
        /**
         * An array containing the IDs of each of the Linodes included in this transfer.
         */
        linodes: Array<number>;
    };
    /**
     * When this transfer expires. Transfers will automatically expire 24 hours after creation.
     */
    expiry: string;
    /**
     * __Filterable__ If the requesting account created this transfer.
     */
    is_sender: boolean;
    /**
     * __Filterable__ The status of the transfer request:
     *
     * `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
     * `canceled`: The transfer has been canceled by the sender.
     * `completed`: The transfer has completed successfully.
     * `failed`: The transfer has failed after initiation.
     * `pending`: The transfer is ready to be accepted.
     * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
     */
    status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
    /**
     * The token used to identify and accept or cancel this transfer.
     */
    token: string;
    /**
     * When this transfer was last updated.
     */
    updated: string;
};

/**
 * An object for describing a single error that occurred during the processing of a request.
 */
export type ErrorObject = {
    /**
     * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
     */
    field: string;
    /**
     * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
     */
    reason: string;
};

/**
 * A specific event object. An event is an action taken against an entity related to your account. For example, if you boot a Linode, the system creates an event. The events returned depend on your user grants.
 */
export type Event = {
    /**
     * __Read-only__ The action that caused this event. New actions may be added in the future.
     */
    action: 'account_agreement_eu_model' | 'account_promo_apply' | 'account_update' | 'account_settings_update' | 'backups_enable' | 'backups_cancel' | 'backups_restore' | 'community_question_reply' | 'community_like' | 'community_mention' | 'credit_card_updated' | 'database_create' | 'database_delete' | 'database_update' | 'database_failed' | 'database_degraded' | 'database_create_failed' | 'database_update_failed' | 'database_backup_create' | 'database_backup_restore' | 'database_backup_delete' | 'database_credentials_reset' | 'database_low_disk_space' | 'database_scale' | 'database_resize' | 'database_resize_create' | 'database_migrate' | 'database_upgrade' | 'database_suspend' | 'database_resume' | 'disk_create' | 'disk_delete' | 'disk_update' | 'disk_duplicate' | 'disk_imagize' | 'disk_resize' | 'dns_record_create' | 'dns_record_delete' | 'dns_record_update' | 'dns_zone_create' | 'dns_zone_delete' | 'dns_zone_import' | 'dns_zone_update' | 'entity_transfer_accept' | 'entity_transfer_accept_recipient' | 'entity_transfer_cancel' | 'entity_transfer_create' | 'entity_transfer_fail' | 'entity_transfer_stale' | 'firewall_apply' | 'firewall_create' | 'firewall_delete' | 'firewall_disable' | 'firewall_enable' | 'firewall_update' | 'firewall_device_add' | 'firewall_device_remove' | 'firewall_rules_update' | 'host_reboot' | 'image_delete' | 'image_update' | 'image_upload' | 'interface_create' | 'interface_delete' | 'interface_update' | 'ipaddress_update' | 'ipv6pool_add' | 'ipv6pool_delete' | 'lassie_reboot' | 'lish_boot' | 'linode_addip' | 'linode_boot' | 'linode_clone' | 'linode_create' | 'linode_delete' | 'linode_update' | 'linode_deleteip' | 'linode_kvmify' | 'linode_migrate' | 'linode_migrate_datacenter' | 'linode_migrate_datacenter_create' | 'linode_mutate' | 'linode_mutate_create' | 'linode_poweroff_on' | 'linode_reboot' | 'linode_rebuild' | 'linode_resize' | 'linode_resize_create' | 'linode_resize_warm_create' | 'linode_shutdown' | 'linode_snapshot' | 'linode_config_create' | 'linode_config_delete' | 'linode_config_update' | 'lke_control_plane_acl_create' | 'lke_control_plane_acl_update' | 'lke_control_plane_acl_delete' | 'lke_cluster_create' | 'lke_cluster_update' | 'lke_cluster_delete' | 'lke_cluster_recycle' | 'lke_cluster_regenerate' | 'lke_node_create' | 'lke_node_delete' | 'lke_node_recycle' | 'lke_pool_create' | 'lke_pool_delete' | 'lke_pool_recycle' | 'lke_kubeconfig_regenerate' | 'lke_token_rotate' | 'longviewclient_create' | 'longviewclient_delete' | 'longviewclient_update' | 'managed_disabled' | 'managed_enabled' | 'managed_service_create' | 'managed_service_delete' | 'nodebalancer_create' | 'nodebalancer_delete' | 'nodebalancer_update' | 'nodebalancer_config_create' | 'nodebalancer_config_delete' | 'nodebalancer_config_update' | 'nodebalancer_node_create' | 'nodebalancer_node_delete' | 'nodebalancer_node_update' | 'oauth_client_create' | 'oauth_client_delete' | 'oauth_client_secret_reset' | 'oauth_client_update' | 'obj_access_key_create' | 'obj_access_key_delete' | 'obj_access_key_update' | 'password_reset' | 'payment_method_add' | 'payment_submitted' | 'placement_group_assign' | 'placement_group_became_compliant' | 'placement_group_became_non_compliant' | 'placement_group_create' | 'placement_group_delete' | 'placement_group_unassign' | 'placement_group_update' | 'profile_update' | 'stackscript_create' | 'stackscript_delete' | 'stackscript_update' | 'stackscript_publicize' | 'stackscript_revise' | 'subnet_create' | 'subnet_delete' | 'subnet_update' | 'tag_create' | 'tag_delete' | 'tag_update' | 'tax_id_valid' | 'tax_id_invalid' | 'tfa_disabled' | 'tfa_enabled' | 'ticket_attachment_upload' | 'ticket_create' | 'ticket_update' | 'token_create' | 'token_delete' | 'token_update' | 'user_create' | 'user_update' | 'user_delete' | 'user_ssh_key_add' | 'user_ssh_key_delete' | 'user_ssh_key_update' | 'vlan_attach' | 'vlan_detach' | 'volume_attach' | 'volume_clone' | 'volume_create' | 'volume_delete' | 'volume_update' | 'volume_detach' | 'volume_resize' | 'volume_migrate' | 'volume_migrate_scheduled' | 'vpc_create' | 'vpc_delete' | 'vpc_update';
    /**
     * __Read-only__ When the system created this event.
     */
    readonly created: string;
    /**
     * __Beta__, __Read-only__ Maintenance details for this event, if any.
     */
    readonly details: {
        /**
         * The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        complete_time: string;
        /**
         * Differentiates between scheduled and emergency maintenance.
         */
        description: 'Scheduled Maintenance' | 'Emergency Maintenance';
        /**
         * The maintenance policy the user configures for this event.
         */
        maintenance_policy_set: string;
        /**
         * The scheduled start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        not_before: string;
        /**
         * The origin of the event. A `platform` source indicates that the event was initiated by Akamai. A `user` source indicates that the event was initiated by the user.
         */
        source: 'platform' | 'user';
        /**
         * The actual start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        start_time: string;
    };
    /**
     * __Read-only__ The number of seconds that it takes for the event to complete.
     */
    readonly duration: number;
    /**
     * __Read-only__ Detailed information about the entity that triggered this event.
     */
    readonly entity: {
        /**
         * The unique identifier assigned to the entity.
         */
        id: number;
        /**
         * The name of the entity. The label may reflect changes that occur with this event.
         */
        label: string;
        /**
         * __Read-only__ The type of entity that is being referenced by the event.
         */
        type: 'account' | 'backups' | 'community' | 'disks' | 'domain' | 'entity_transfer' | 'firewall' | 'image' | 'ipaddress' | 'linode' | 'longview' | 'loadbalancer' | 'managed_service' | 'nodebalancer' | 'oauth_client' | 'profile' | 'stackscript' | 'tag' | 'ticket' | 'token' | 'user' | 'user_ssh_key' | 'volume';
        /**
         * The URL where you can access this event's entity. If it's a relative URL, it's relative to the domain where you retrieved the event.
         */
        url: string;
    };
    /**
     * __Read-only__ The unique ID of this event.
     */
    readonly id: number;
    /**
     * Additional information about the event. This can be a more detailed representation of an event that can help you diagnose non-obvious failures.
     */
    message: string | null;
    /**
     * __Read-only__ A percentage estimating the amount of time remaining for an event. Returned as `null` for notification events.
     */
    readonly percent_complete: number | null;
    /**
     * __Read-only__ The rate of completion of the event. Only some events return a `rate`, such as the `migration` and `resize` events.
     */
    readonly rate: string | null;
    /**
     * __Read-only__ Detailed information about the event's secondary entity, if applicable.
     */
    readonly secondary_entity: {
        /**
         * The identifier for the secondary entity object.
         */
        id: string;
        /**
         * The name of the secondary entity object.
         */
        label: string;
        /**
         * __Read-only__ The type of secondary entity object that's being referenced by the event.
         */
        readonly type: string;
        /**
         * The URL where you can access this event's secondary entity object. If it's a relative URL, it's relative to the domain where you retrieved the event.
         */
        url: string;
    };
    /**
     * __Read-only__ If a user on your account has [marked an event as seen](https://techdocs.akamai.com/linode-api/reference/post-event-seen).
     */
    readonly seen: boolean;
    /**
     * __Read-only__ The current status of this event.
     */
    status: 'failed' | 'finished' | 'notification' | 'scheduled' | 'started';
    /**
     * __Read-only__ The estimated time remaining until the event completes. This is only returned for some in-progress migration events. Otherwise, `percent_complete` indicates how long until completion.
     */
    readonly time_remaining: string | null;
    /**
     * __Read-only__ The name of the user whose action caused the event.
     */
    readonly username: string | null;
};

export type Events = {
    data: Array<{
        /**
         * __Filterable__ The action that caused this event. New actions may be added in the future.
         */
        action: 'account_agreement_eu_model' | 'account_promo_apply' | 'account_update' | 'account_settings_update' | 'backups_enable' | 'backups_cancel' | 'backups_restore' | 'community_question_reply' | 'community_like' | 'community_mention' | 'credit_card_updated' | 'database_create' | 'database_delete' | 'database_update' | 'database_failed' | 'database_degraded' | 'database_create_failed' | 'database_update_failed' | 'database_backup_create' | 'database_backup_restore' | 'database_backup_delete' | 'database_credentials_reset' | 'database_low_disk_space' | 'database_scale' | 'database_resize' | 'database_resize_create' | 'database_migrate' | 'database_upgrade' | 'database_suspend' | 'database_resume' | 'disk_create' | 'disk_delete' | 'disk_update' | 'disk_duplicate' | 'disk_imagize' | 'disk_resize' | 'dns_record_create' | 'dns_record_delete' | 'dns_record_update' | 'dns_zone_create' | 'dns_zone_delete' | 'dns_zone_import' | 'dns_zone_update' | 'entity_transfer_accept' | 'entity_transfer_accept_recipient' | 'entity_transfer_cancel' | 'entity_transfer_create' | 'entity_transfer_fail' | 'entity_transfer_stale' | 'firewall_apply' | 'firewall_create' | 'firewall_delete' | 'firewall_disable' | 'firewall_enable' | 'firewall_update' | 'firewall_device_add' | 'firewall_device_remove' | 'firewall_rules_update' | 'host_reboot' | 'image_delete' | 'image_update' | 'image_upload' | 'interface_create' | 'interface_delete' | 'interface_update' | 'ipaddress_update' | 'ipv6pool_add' | 'ipv6pool_delete' | 'lassie_reboot' | 'lish_boot' | 'linode_addip' | 'linode_boot' | 'linode_clone' | 'linode_create' | 'linode_delete' | 'linode_update' | 'linode_deleteip' | 'linode_kvmify' | 'linode_migrate' | 'linode_migrate_datacenter' | 'linode_migrate_datacenter_create' | 'linode_mutate' | 'linode_mutate_create' | 'linode_poweroff_on' | 'linode_reboot' | 'linode_rebuild' | 'linode_resize' | 'linode_resize_create' | 'linode_resize_warm_create' | 'linode_shutdown' | 'linode_snapshot' | 'linode_config_create' | 'linode_config_delete' | 'linode_config_update' | 'lke_cluster_create' | 'lke_cluster_update' | 'lke_cluster_delete' | 'lke_cluster_recycle' | 'lke_cluster_regenerate' | 'lke_control_plane_acl_create' | 'lke_control_plane_acl_update' | 'lke_control_plane_acl_delete' | 'lke_node_create' | 'lke_node_delete' | 'lke_node_recycle' | 'lke_pool_create' | 'lke_pool_delete' | 'lke_pool_recycle' | 'lke_kubeconfig_regenerate' | 'lke_token_rotate' | 'longviewclient_create' | 'longviewclient_delete' | 'longviewclient_update' | 'managed_disabled' | 'managed_enabled' | 'managed_service_create' | 'managed_service_delete' | 'nodebalancer_create' | 'nodebalancer_delete' | 'nodebalancer_update' | 'nodebalancer_config_create' | 'nodebalancer_config_delete' | 'nodebalancer_config_update' | 'nodebalancer_node_create' | 'nodebalancer_node_delete' | 'nodebalancer_node_update' | 'oauth_client_create' | 'oauth_client_delete' | 'oauth_client_secret_reset' | 'oauth_client_update' | 'obj_access_key_create' | 'obj_access_key_delete' | 'obj_access_key_update' | 'password_reset' | 'payment_method_add' | 'payment_submitted' | 'placement_group_assign' | 'placement_group_became_compliant' | 'placement_group_became_non_compliant' | 'placement_group_create' | 'placement_group_delete' | 'placement_group_unassign' | 'placement_group_update' | 'profile_update' | 'stackscript_create' | 'stackscript_delete' | 'stackscript_update' | 'stackscript_publicize' | 'stackscript_revise' | 'subnet_create' | 'subnet_delete' | 'subnet_update' | 'tag_create' | 'tag_delete' | 'tag_update' | 'tax_id_valid' | 'tax_id_invalid' | 'tfa_disabled' | 'tfa_enabled' | 'ticket_attachment_upload' | 'ticket_create' | 'ticket_update' | 'token_create' | 'token_delete' | 'token_update' | 'user_create' | 'user_update' | 'user_delete' | 'user_ssh_key_add' | 'user_ssh_key_delete' | 'user_ssh_key_update' | 'vlan_attach' | 'vlan_detach' | 'volume_attach' | 'volume_clone' | 'volume_create' | 'volume_delete' | 'volume_update' | 'volume_detach' | 'volume_resize' | 'volume_migrate' | 'volume_migrate_scheduled' | 'vpc_create' | 'vpc_delete' | 'vpc_update';
        /**
         * __Filterable__ When the system created this event.
         */
        created: string;
        /**
         * __Beta__ Maintenance details for this event, if any.
         */
        details: {
            /**
             * __Filterable__ The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            complete_time: string;
            /**
             * __Filterable__ Differentiates between scheduled and emergency maintenance.
             */
            description: 'Scheduled Maintenance' | 'Emergency Maintenance';
            /**
             * __Filterable__ The maintenance policy the user configures for this event.
             */
            maintenance_policy_set: string;
            /**
             * __Filterable__ The scheduled start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            not_before: string;
            /**
             * __Filterable__ The origin of the event. A `platform` source indicates that the event was initiated by Akamai. A `user` source indicates that the event was initiated by the user.
             */
            source: 'platform' | 'user';
            /**
             * __Filterable__ The actual start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            start_time: string;
        };
        /**
         * The number of seconds that it takes for the event to complete.
         */
        duration: number;
        /**
         * Detailed information about the entity that triggered this event.
         */
        entity: {
            /**
             * __Filterable__ The unique identifier assigned to the entity.
             *
             * > 📘
             * >
             * > Consider these points when filtering by `id`:
             * >
             * > - The `disks` and `backups` entities use the `id` of their parent Linode when filtering for events.
             * >
             * > - The `account` and `profile` entities don't have an `id`. To filter these entities, use the `type` object instead.
             * >
             * > - The `tag` entity uses the tag's name as its `id`. Set this value to the tag's name to filter.
             */
            id: number;
            /**
             * The name of the entity. The label may reflect changes that occur with this event.
             */
            label: string;
            /**
             * __Filterable__ The type of entity that is being referenced by the event.
             */
            type: 'account' | 'backups' | 'community' | 'disks' | 'domain' | 'entity_transfer' | 'firewall' | 'image' | 'ipaddress' | 'linode' | 'longview' | 'loadbalancer' | 'managed_service' | 'nodebalancer' | 'oauth_client' | 'profile' | 'stackscript' | 'tag' | 'ticket' | 'token' | 'user' | 'user_ssh_key' | 'volume';
            /**
             * The URL where you can access this event's entity. If it's a relative URL, it's relative to the domain where you retrieved the event.
             */
            url: string;
        };
        /**
         * __Filterable__ The unique ID of this event.
         */
        id: number;
        /**
         * Additional information about the event. This can be a more detailed representation of an event that can help you diagnose non-obvious failures.
         */
        message: string | null;
        /**
         * A percentage estimating the amount of time remaining for an event. Returned as `null` for notification events.
         */
        percent_complete: number | null;
        /**
         * The rate of completion of the event. Only some events return a `rate`, such as the `migration` and `resize` events.
         */
        rate: string | null;
        /**
         * Detailed information about the event's secondary entity, if applicable.
         */
        secondary_entity: {
            /**
             * The identifier for the secondary entity object.
             */
            id: string;
            /**
             * The name of the secondary entity object.
             */
            label: string;
            /**
             * The type of secondary entity object that's being referenced by the event.
             */
            type: string;
            /**
             * The URL where you can access this event's secondary entity object. If it's a relative URL, it's relative to the domain where you retrieved the event.
             */
            url: string;
        };
        /**
         * If a user on your account has [marked an event as seen](https://techdocs.akamai.com/linode-api/reference/post-event-seen).
         */
        seen: boolean;
        /**
         * The current status of this event.
         */
        status: 'completed' | 'failed' | 'finished' | 'in_progress' | 'notification' | 'scheduled' | 'started';
        /**
         * The estimated time remaining until the event completes. This is only returned for some in-progress migration events. Otherwise, the `percent_complete` attribute indicates how long until completion.
         */
        time_remaining: string | null;
        /**
         * The name of the user whose action caused the event.
         */
        username: string | null;
    }>;
    /**
     * The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    page: number;
    /**
     * The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    pages: number;
    /**
     * The total number of results.
     */
    results: number;
};

export type GetMaintenance200 = {
    data: Array<{
        /**
         * __Beta__, __Filterable__ The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        complete_time: string;
        /**
         * __Beta__ Differentiates between scheduled and emergency maintenance.
         */
        description: 'Scheduled Maintenance' | 'Emergency Maintenance';
        /**
         * The entity affected by the maintenance.
         */
        entity: {
            /**
             * The unique identifier of the entity targeted by the maintenance.
             */
            id: number;
            /**
             * The name of the entity targeted by the maintenance.
             */
            label: string;
            /**
             * The type of entity.
             */
            type: string;
            /**
             * A combination of the API operation prefix and the entity's `id` that can be used to review the entity.
             */
            url: string;
        };
        /**
         * __Beta__, __Filterable__ The maintenance policy configured by the user.
         */
        maintenance_policy_set: string;
        /**
         * __Beta__, __Filterable__ The earliest time when the maintenance can start. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        not_before: string;
        /**
         * The reason maintenance is being performed.
         */
        reason: string;
        /**
         * __Beta__ The origin of the maintenance. A `platform` source indicates that the maintenance was initiated by Akamai. A `user` source indicates that the maintenance was initiated by the user.
         */
        source: 'platform' | 'user';
        /**
         * __Beta__, __Filterable__ The time the maintenance started. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        start_time: string;
        /**
         * __Filterable__ The maintenance status. Maintenance progress follows the sequence `pending`, `scheduled`, `started`, and `completed`. A `scheduled` status is unique to Linodes that require a reboot for [QEMU maintenance](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance).
         */
        status: 'pending' | 'scheduled' | 'started' | 'in-progress' | 'completed' | 'canceled';
        /**
         * __Filterable__ The type of maintenance.
         */
        type: 'reboot' | 'cold_migration' | 'live_migration' | 'migrate' | 'power_off_on';
        /**
         * __Filterable__ The start time for the maintenance. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (yyyy-mm-ddThh:mm:ss), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        when: string;
    }>;
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

/**
 * Google Pay
 *
 * Google Pay information.
 */
export type GooglePayData = {
    /**
     * __Read-only__ The type of credit card.
     */
    readonly card_type: string;
    /**
     * __Read-only__ The expiration month and year of the credit card.
     */
    readonly expiry: string;
    /**
     * __Read-only__ The last four digits of the credit card number.
     */
    readonly last_four: string;
};

/**
 * Represents the level of access a restricted user has to a specific resource on the account.
 */
export type Grant = {
    /**
     * The unique identifier of the resource this grant applies to.
     */
    id: number;
    /**
     * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
     */
    readonly label: string;
    /**
     * The level of access this user has to this entity. If `null`, this user has no access.
     */
    permissions: 'read_only' | 'read_write';
};

export type GrantsResponse = {
    /**
     * The grants this user has for individual Managed Databases on this account.
     */
    database: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual domains on this account.
     */
    domain: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual firewalls on this account.
     */
    firewall: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants the user has to all resources on your account.
     */
    global: {
        /**
         * The level of access this user has to account-level actions, like billing information and user management.
         *
         * > 📘
         * >
         * > A `restricted` user can't be used to manage users, even if this is set to `read-write`. Only unrestricted users can manage other users on an account.
         *
         * __Parent and child accounts__
         *
         * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
         */
        account_access: 'read_only' | 'read_write';
        /**
         * Whether this user can add Managed Databases on the account.
         */
        add_databases: boolean;
        /**
         * Whether this user can add domains on the account.
         */
        add_domains: boolean;
        /**
         * Whether this user can add Firewalls on the account.
         */
        add_firewalls: boolean;
        /**
         * Whether this user can create images from disks on your Linodes, on the account.
         */
        add_images: boolean;
        /**
         * Whether this user can create Linodes.
         */
        add_linodes: boolean;
        /**
         * Whether this user can create Longview clients and view the current plan.
         */
        add_longview: boolean;
        /**
         * Whether this user can add NodeBalancers on the account.
         */
        add_nodebalancers: boolean;
        /**
         * Whether this user can add StackScripts on the account.
         */
        add_stackscripts: boolean;
        /**
         * Whether this user can add volumes on the account.
         */
        add_volumes: boolean;
        /**
         * Whether this user can add Virtual Private Clouds (VPCs) on the account.
         */
        add_vpcs: boolean;
        /**
         * Whether this user can cancel the entire account.
         */
        cancel_account: boolean;
        /**
         * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this gives a parent account access to operations that can be used to manage child accounts. Unrestricted parent account users have access to this grant, while restricted parent users don't. An unrestricted parent user can set this to `true` to add this grant to a restricted parent user. Displayed as `null` for all non-parent accounts.
         */
        child_account_access: boolean | null;
        /**
         * Whether this user can manage your account's Longview subscription.
         */
        longview_subscription: boolean;
    };
    /**
     * The grants this user has for individual images on this account.
     */
    image: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual Linodes on this account.
     */
    linode: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual Longview Clients on this account.
     */
    longview: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual NodeBalancers on this account.
     */
    nodebalancer: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this User has for individual StackScripts on this account.
     */
    stackscript: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has individual Block Storage Volumes on this account.
     */
    volume: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has individual Virtual Private Clouds (VPCs) on this account.
     */
    vpc: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
         */
        readonly label: string;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
};

/**
 * The unique identifier of the Beta Program.
 */
export type Id = string;

/**
 * Account Invoice object.
 */
export type Invoice = {
    /**
     * __Filterable__, __Read-only__ The source of service charges for this invoice. A value of `akamai` indicates an invoice generated according to the terms of an agreement between the customer and Akamai. A value of `linode` indicates an invoice was generated according to the default terms, prices, and discounts.
     */
    billing_source: 'akamai' | 'linode';
    /**
     * __Filterable__, __Read-only__ When this Invoice was generated.
     */
    readonly date: string;
    /**
     * __Read-only__ The Invoice's unique ID.
     */
    readonly id: number;
    /**
     * __Filterable__, __Read-only__ The Invoice's display label.
     */
    readonly label: string;
    /**
     * __Read-only__ The amount of the Invoice before taxes in US Dollars.
     */
    readonly subtotal: number;
    /**
     * __Read-only__ The amount of tax levied on the Invoice in US Dollars.
     */
    readonly tax: number;
    /**
     * __Read-only__ The amount of tax broken down into subtotals by source.
     */
    readonly tax_summary: Array<{
        /**
         * The source of this tax subtotal.
         */
        name: string;
        /**
         * The amount of tax subtotal attributable to this source.
         */
        tax: number;
    }>;
    /**
     * __Filterable__, __Read-only__ The amount of the Invoice after taxes in US Dollars.
     */
    readonly total: number;
};

/**
 * An InvoiceItem object.
 */
export type InvoiceItem = {
    /**
     * __Read-only__ The price, in US dollars, of the Invoice Item. Equal to the unit price multiplied by quantity.
     */
    readonly amount: number;
    /**
     * __Read-only__ The date the Invoice Item started, based on month.
     */
    readonly from: string;
    /**
     * __Read-only__ The Invoice Item's display label.
     */
    readonly label: string;
    /**
     * __Read-only__ The quantity of this Item for the specified Invoice.
     */
    readonly quantity: number;
    /**
     * __Read-only__ The ID of the applicable Region associated with this Invoice Item.
     *
     * `null` if there is no applicable Region.
     */
    readonly region: string | null;
    /**
     * __Read-only__ The amount of tax levied on this Item in US Dollars.
     */
    readonly tax: number;
    /**
     * __Read-only__ The date the Invoice Item ended, based on month.
     */
    readonly to: string;
    /**
     * __Read-only__ The price of this Item after taxes in US Dollars.
     */
    readonly total: number;
    /**
     * __Read-only__ The type of service, ether `hourly` or `misc`.
     */
    type: 'hourly' | 'misc';
    /**
     * __Read-only__ The monthly service fee in US Dollars for this Item.
     */
    readonly unit_price: string;
};

/**
 * Whether this Payment Method is the default method for automatically processing service charges.
 */
export type IsDefault = boolean;

/**
 * __Filterable__, __Read-only__ The name of the Beta Program.
 */
export type Label = string;

/**
 * An object representing a previous successful login for a User.
 */
export type Login = {
    /**
     * __Read-only__ When the login was initiated.
     */
    readonly datetime: string;
    /**
     * __Read-only__ The unique ID of this login object.
     */
    readonly id: number;
    /**
     * __Read-only__ The remote IP address that requested the login.
     */
    readonly ip: string;
    /**
     * __Read-only__ True if the User that attempted the login was a restricted User, false otherwise.
     */
    readonly restricted: boolean;
    /**
     * __Read-only__ Whether the login attempt succeeded or failed.
     */
    status: 'successful' | 'failed';
    /**
     * __Read-only__ The username of the User that attempted the login.
     */
    readonly username: string;
};

/**
 * Information about maintenance affecting an entity.
 */
export type Maintenance = {
    /**
     * __Beta__, __Filterable__ The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
     *
     * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
     *
     * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
     */
    complete_time: string;
    /**
     * __Beta__ Differentiates between scheduled and emergency maintenance.
     */
    description: 'Scheduled Maintenance' | 'Emergency Maintenance';
    /**
     * The entity affected by the maintenance.
     */
    entity: {
        /**
         * The unique identifier of the entity targeted by the maintenance.
         */
        id: number;
        /**
         * The name of the entity targeted by the maintenance.
         */
        label: string;
        /**
         * The type of entity.
         */
        type: string;
        /**
         * A combination of the API operation prefix and the entity's `id` that can be used to review the entity.
         */
        url: string;
    };
    /**
     * __Beta__, __Filterable__ The maintenance policy configured by the user.
     */
    maintenance_policy_set: string;
    /**
     * __Beta__, __Filterable__ The earliest time when the maintenance can start. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
     *
     * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
     *
     * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
     */
    not_before: string;
    /**
     * The reason maintenance is being performed.
     */
    reason: string;
    /**
     * __Beta__ The origin of the maintenance. A `platform` source indicates that the maintenance was initiated by Akamai. A `user` source indicates that the maintenance was initiated by the user.
     */
    source: 'platform' | 'user';
    /**
     * __Beta__, __Filterable__ The time the maintenance started. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
     *
     * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
     *
     * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
     */
    start_time: string;
    /**
     * __Filterable__ The maintenance status. Maintenance progress follows the sequence `pending`, `scheduled`, `started`, and `completed`. A `scheduled` status is unique to Linodes that require a reboot for [QEMU maintenance](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance).
     */
    status: 'pending' | 'scheduled' | 'started' | 'in-progress' | 'completed' | 'canceled';
    /**
     * __Filterable__ The type of maintenance.
     */
    type: 'reboot' | 'cold_migration' | 'live_migration' | 'migrate' | 'power_off_on';
    /**
     * __Filterable__ The start time for the maintenance. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
     *
     * - A single ISO 8601 timestamp (yyyy-mm-ddThh:mm:ss), which returns only matches for that value.
     *
     * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
     */
    when: string;
};

/**
 * An important, often time-sensitive item related to your account.
 */
export type Notification = {
    /**
     * A full description of this notification, in markdown format. Not all notifications include a `body`. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
     */
    body: string | null;
    /**
     * Detailed information about the notification. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
     */
    entity: {
        /**
         * The unique ID of the notification's entity, based on the entity type. Returns `null` for an `account` or `promotion` entity.
         */
        id: number | null;
        /**
         * The current name of this notification's entity. Returns `null` for the following `entity` types:
         *
         * - `entity_transfer`
         *
         * - `promotion`
         *
         * - `region`
         */
        label: string | null;
        /**
         * __Filterable__ The type of entity this is related to. An entity can be product or service-specific, such as a `linode`, `loadbalancers`, or `nodebalancers`. It can apply to a specific component, such as your `account`, a specific `promotion` your participating in, a data center (`region`) where you're using services, a transfer from one component to another (an `entity_transfer`), a support `ticket` you've opened, or a `volume` on a specific Linode.
         */
        type: 'account' | 'entity_transfer' | 'linode' | 'loadbalancers' | 'nodebalancer' | 'promotion' | 'region' | 'ticket' | 'volume';
        /**
         * The URL where you can access the notification's object. The URL is relative to the domain where you retrieved the notification. This value is `null` for the `promotion` entity type.
         */
        url: string | null;
    } | null;
    /**
     * A short description of this notification.
     */
    label: string;
    /**
     * A human-readable description of the notification.
     */
    message: string;
    /**
     * The severity of this notification. This field determines how prominently the notification is displayed and the color of the display text.
     */
    severity: 'minor' | 'major' | 'critical';
    /**
     * __Filterable__ The type of notification.
     *
     * > 📘
     * >
     * > A `security_reboot_maintenance_scheduled` event is a global notice that a Linode needs to be rebooted for QEMU upgrade maintenance. Have a look at [this workflow](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance) for guidance on reboooting your Linodes for this maintenance.
     */
    type: 'migration_scheduled' | 'migration_imminent' | 'migration_pending' | 'reboot_scheduled' | 'outage' | 'payment_due' | 'ticket_important' | 'ticket_abuse' | 'notice' | 'maintenance' | 'maintenance_scheduled' | 'promotion' | 'security_reboot_maintenance_scheduled' | 'tax_id_verifying';
    /**
     * If this notification has a duration, this is when the event or action will complete. For example, if there's scheduled maintenance for one of our systems, `until` represents the end of the maintenance window. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
     */
    until: string | null;
    /**
     * If this notification is for an event in the future, this specifies when the action occurs. For example, if a compute instance needs to migrate in response to a security advisory, this field sets the approximate time the compute instance will be taken offline for migration. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
     */
    when: string | null;
};

/**
 * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
 */
export type OauthClient = {
    /**
     * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
     */
    readonly id: string;
    /**
     * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
     */
    label: string;
    /**
     * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
     */
    public: boolean;
    /**
     * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
     */
    redirect_uri: string;
    /**
     * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
     */
    readonly secret: string;
    /**
     * __Read-only__ The status of this application.  `active` by default.
     */
    status: 'active' | 'disabled' | 'suspended';
    /**
     * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
     */
    readonly thumbnail_url: string | null;
};

/**
 * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
 */
export type Page = number;

/**
 * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
 */
export type Pages = number;

/**
 * An envelope for paginated response. When accessing a collection through a GET endpoint, the results are wrapped in this envelope which includes metadata about those results. Results are presented within a `data` array. See [Pagination](https://techdocs.akamai.com/linode-api/reference/pagination) for more information.
 */
export type PaginationEnvelope = {
    /**
     * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly page: number;
    /**
     * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
     */
    readonly pages: number;
    /**
     * __Read-only__ The total number of results.
     */
    readonly results: number;
};

/**
 * Payment object response.
 */
export type Payment = {
    /**
     * __Filterable__, __Read-only__ When the payment was made.
     */
    readonly date: string;
    /**
     * __Read-only__ The unique ID of the payment.
     */
    readonly id: number;
    /**
     * __Filterable__, __Read-only__ The amount, in US dollars, of the payment.
     */
    readonly usd: number;
};

/**
 * Payment Method Response Object.
 */
export type PaymentMethod = {
    /**
     * __Read-only__ When the Payment Method was added to the Account.
     */
    readonly created: string;
    data: {
        /**
         * __Read-only__ The type of credit card.
         */
        readonly card_type: string;
        /**
         * __Read-only__ The expiration month and year of the credit card.
         */
        readonly expiry: string;
        /**
         * __Read-only__ The last four digits of the credit card number.
         */
        readonly last_four: string;
    } | {
        /**
         * __Read-only__ The type of credit card.
         */
        readonly card_type: string;
        /**
         * __Read-only__ The expiration month and year of the credit card.
         */
        readonly expiry: string;
        /**
         * __Read-only__ The last four digits of the credit card number.
         */
        readonly last_four: string;
    } | {
        /**
         * __Read-only__ The email address associated with your PayPal account.
         */
        readonly email: string;
        /**
         * __Read-only__ PayPal Merchant ID associated with your PayPal account.
         */
        readonly paypal_id: string;
    };
    /**
     * The unique ID of this Payment Method.
     */
    id: number;
    /**
     * Whether this Payment Method is the default method for automatically processing service charges.
     */
    is_default: boolean;
    /**
     * The type of Payment Method.
     */
    type: 'credit_card' | 'google_pay' | 'paypal';
};

/**
 * An object representing the staging of a Payment via PayPal.
 */
export type Paypal = {
    /**
     * The URL to have PayPal redirect to when Payment is canceled.
     */
    cancel_url: string;
    /**
     * The URL to have PayPal redirect to when Payment is approved.
     */
    redirect_url: string;
    /**
     * The payment amount in USD. Minimum accepted value of $5 USD. Maximum accepted value of $500 USD or credit card payment limit; whichever value is highest. PayPal's maximum transaction limit is $10,000 USD.
     */
    usd: string;
};

/**
 * Paypal
 *
 * PayPal information.
 */
export type PaypalData = {
    /**
     * __Read-only__ The email address associated with your PayPal account.
     */
    readonly email: string;
    /**
     * __Read-only__ PayPal Merchant ID associated with your PayPal account.
     */
    readonly paypal_id: string;
};

/**
 * An object representing an execution of Payment to PayPal to capture the funds and credit your Linode Account.
 */
export type PaypalExecute = {
    /**
     * The PayerID returned by PayPal during the transaction authorization process.
     */
    payer_id: string;
    /**
     * The PaymentID returned from [Stage a PayPal payment](https://techdocs.akamai.com/linode-api/reference/post-pay-pal-payment) that has been approved with PayPal.
     */
    payment_id: string;
};

/**
 * __Read-only__ Promotions generally offer a set amount of credit that can be used toward your Linode services, and the promotion expires after a specified date. As well, a monthly cap on the promotional offer is set.
 *
 * Simply put, a promotion offers a certain amount of credit  month, until either the expiration date is passed, or until the total promotional credit is used, whichever comes first.
 */
export type Promotion = {
    /**
     * The amount available to spend per month.
     */
    credit_monthly_cap: string;
    /**
     * The total amount of credit left for this promotion.
     */
    credit_remaining: string;
    /**
     * A detailed description of this promotion.
     */
    description: string;
    /**
     * When this promotion's credits expire.
     */
    expire_dt: string;
    /**
     * The location of an image for this promotion.
     */
    image_url: string;
    /**
     * The service to which this promotion applies.
     */
    service_type: 'all' | 'backup' | 'blockstorage' | 'db_mysql' | 'ip_v4' | 'linode' | 'linode_disk' | 'linode_memory' | 'longview' | 'managed' | 'nodebalancer' | 'objectstorage' | 'placement_group' | 'transfer_tx';
    /**
     * Short details of this promotion.
     */
    summary: string;
    /**
     * The amount of credit left for this month for this promotion.
     */
    this_month_credit_remaining: string;
};

/**
 * A collection of the services to include in this transfer request, separated by type.
 */
export type PropertiesEntities = {
    /**
     * An array containing the IDs of each of the Linodes included in this transfer.
     */
    linodes: Array<number>;
};

/**
 * The token generated manually for a child account so its proxy user can access the API and CLI without going through an OAuth login.
 */
export type ProxyUserToken = {
    /**
     * __Filterable__, __Read-only__ The date and time this token was created.
     */
    readonly created: string;
    /**
     * __Read-only__ When this token expires. This is default set to 15 minutes from the time of creation. Proxy user tokens can't be renewed. After this time, Akamai revokes the token and you need to generate a new one.
     */
    readonly expiry: string;
    /**
     * __Read-only__ The proxy user token's unique ID, which can be used to revoke it.
     */
    readonly id: number;
    /**
     * __Filterable__ The name of the token. The API automatically sets this to `<username>_<uid>_<time>`. It's composed of the `username` for your parent account user, the unique `uid` Akamai assigned to identify your user, and the `time` the API generated the token. This is for display purposes only, but you can use it to help track how you're using each proxy user token.
     */
    label: string;
    /**
     * __Read-only__ The scopes this token was created with. Defaults to `*`. Proxy user tokens automatically inherit all the permissions of the proxy user.
     */
    readonly scopes: string;
    /**
     * __Read-only__ The proxy user token that can be used to access the API and CLI. After you [create](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) a token, you can see the full token in the response. All other operations that contain this token only show the first 16 characters in their response.
     */
    readonly token: string;
};

/**
 * __Read-only__ The total number of results.
 */
export type Results = number;

/**
 * An object representing a Service Transfer.
 */
export type ServiceTransfer = {
    /**
     * When this transfer was created.
     */
    created: string;
    /**
     * A collection of the services to include in this transfer request, separated by type.
     */
    entities: {
        /**
         * An array containing the IDs of each of the Linodes included in this transfer.
         */
        linodes: Array<number>;
    };
    /**
     * When this transfer expires. Transfers will automatically expire 24 hours after creation.
     */
    expiry: string;
    /**
     * __Filterable__ If the requesting account created this transfer.
     */
    is_sender: boolean;
    /**
     * __Filterable__ The status of the transfer request.
     *
     * `accepted`: The transfer has been accepted by another user and is currently in progress.
     * Transfers can take up to 3 hours to complete.
     *
     * `canceled`: The transfer has been canceled by the sender.
     *
     * `completed`: The transfer has completed successfully.
     *
     * `failed`: The transfer has failed after initiation.
     *
     * `pending`: The transfer is ready to be accepted.
     *
     * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
     * canceled.
     */
    status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
    /**
     * The token used to identify and accept or cancel this transfer.
     */
    token: string;
    /**
     * When this transfer was last updated.
     */
    updated: string;
};

/**
 * __Filterable__, __Read-only__ The start date-time of the Beta Program.
 */
export type Started = string;

/**
 * An object representing your network utilization for the current month, in Gigabytes.
 *
 * Certain Regions have separate utilization quotas and rates. For Region-specific network utilization data, see `region_transfers`.
 */
export type Transfer = {
    /**
     * __Read-only__ The amount of your transfer pool that is billable this billing cycle.
     */
    readonly billable: number;
    /**
     * __Read-only__ The amount of network usage allowed this billing cycle.
     */
    readonly quota: number;
    region_transfers: Array<{
        /**
         * __Read-only__ The amount of your transfer pool that is billable this billing cycle for this Region.
         */
        readonly billable: number;
        /**
         * The Region ID for this network utilization data.
         */
        id: string;
        /**
         * __Read-only__ The amount of network usage allowed this billing cycle for this Region.
         */
        readonly quota: number;
        /**
         * __Read-only__ The amount of network usage you have used this billing cycle for this Region.
         */
        readonly used: number;
    }>;
    /**
     * __Read-only__ The amount of network usage you have used this billing cycle.
     */
    readonly used: number;
};

/**
 * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
 */
export type User = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
     */
    readonly last_login: {
        /**
         * __Read-only__ The date and time of this user's most recent login attempt.
         */
        readonly login_datetime: string;
        /**
         * __Read-only__ The result of this user's most recent login attempt.
         */
        status: 'successful' | 'failed';
    } | null;
    /**
     * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
     */
    readonly password_created: string | null;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
     *
     * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
     *
     * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
     *
     * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
     */
    readonly ssh_keys: Array<string>;
    /**
     * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
     */
    readonly tfa_enabled: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
    /**
     * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
     */
    readonly verified_phone_number: string | null;
};

/**
 * The type of user on an account. Mostly applies to the use of the parent and child accounts for Akamai partners functionality.
 */
export type UserType = {
    /**
     * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
     *
     * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
     *
     * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
     *
     * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
     *
     * - `default`. This applies to all regular, non-parent-child account users.
     */
    user_type: 'parent' | 'child' | 'proxy' | 'default';
};

/**
 * An object for describing a single warning associated with a response.
 */
export type WarningObject = {
    /**
     * Specific information related to the warning.
     */
    details: string;
    /**
     * The general warning message.
     */
    title: string;
};

/**
 * Specifies the `X-Filter` header JSON object's filtering and sort criteria.
 */
export type XFilter = {
    [key: string]: unknown;
} | {
    /**
     * All conditions need to be true.
     */
    '+and': Array<{
        [key: string]: unknown;
    }>;
    /**
     * The provided string needs to be in the value.
     */
    '+contains': string;
    /**
     * The value needs to be greater than the provided number.
     */
    '+gt': number;
    /**
     * The value needs to be greater than or equal to the provided number.
     */
    '+gte': number;
    /**
     * The value needs to be less than the provided number.
     */
    '+lt': number;
    /**
     * The value needs to be less than or equal to the provided number.
     */
    '+lte': number;
    /**
     * The provided string is left out of the results.
     */
    '+neq': string;
    /**
     * At least one condition needs to be true.
     */
    '+or': Array<{
        [key: string]: unknown;
    }>;
    /**
     * Sort in ascending (`asc`) or descending (`desc`) order. This defaults to `asc`. Requires `+order_by`.
     */
    '+order': 'asc' | 'desc';
    /**
     * Order results based on the provided attribute. The attribute needs to be filterable.
     */
    '+order_by': string;
};

/**
 * Simple filter
 *
 * Specify the name of the data field and the accompanying value.
 */
export type XFilterCriteria = {
    [key: string]: unknown;
};

/**
 * Account object.
 */
export type AccountWritable = {
    /**
     * The first line of this account's billing address.
     */
    address_1: string;
    /**
     * The second line of this account's billing address.
     */
    address_2: string;
    /**
     * The city for this account's `address`.
     */
    city: string;
    /**
     * The company name assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
     */
    company: string;
    /**
     * The two-letter ISO 3166 country code for this account's `address`.
     */
    country: string;
    /**
     * The email address of the person assigned to this account.
     */
    email: string;
    /**
     * The first name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
     */
    first_name: string;
    /**
     * The last name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
     */
    last_name: string;
    /**
     * The phone number assigned to this account.
     */
    phone: string;
    /**
     * The state or province for the `address` set for your account, if applicable.
     *
     * - If the `address` is in the United States (US) or Canada (CA), this is the two-letter ISO 3166 code for the state or province.
     *
     * - If it's a US military `address`, this is the abbreviation for that territory. This includes `AA` for Armed Forces Americas (excluding Canada), `AE` for Armed Forces Africa, Europe, Middle East, and Canada, or `AP` for Armed Forces Pacific.
     *
     * - If outside the US or CA, this is the province associated with the account's `address`.
     */
    state: string;
    /**
     * The tax identification number (TIN) assigned to this account, used for tax calculations. A TIN is set by the national authorities in your `country`, based on your `address_1`, and it may be named differently between countries. Set to an empty string (`""`) if a TIN doesn't apply or for countries that don't collect tax.
     *
     * > 📘
     * >
     * > This value is externally validated. If the validation is successful, a `tax_id_valid` [event](https://techdocs.akamai.com/linode-api/reference/get-events) is triggered. If unsuccessful, a `tax_id_invalid` event is triggered and an error response is issued for an operation that included it.
     */
    tax_id: string;
    /**
     * The zip code for this account's `address`.
     *
     * - It can only contain ASCII letters, numbers, and dashes (`-`).
     *
     * - It can't contain more than nine letter or number characters.
     */
    zip: string;
};

/**
 * Account Settings object.
 */
export type AccountSettingsWritable = {
    /**
     * Account-wide backups default.  If `true`, all Linodes created will automatically be enrolled in the Backups service.  If `false`, Linodes will not be enrolled by default, but may still be enrolled on creation or later.
     */
    backups_enabled: boolean;
    /**
     * __Beta__ Defines if new Linodes can use legacy configuration interfaces:
     * - `legacy_config_only`. All new Linodes need to use legacy configuration interfaces. Prevously created Linodes with Linode Interfaces can still exist. Linodes using legacy configuration interfaces can't be upgraded to use Linode Interfaces.
     * - `legacy_config_default_but_linode_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use legacy configuration interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode Interfaces. This is the default setting for existing accounts.
     * - `linode_default_but_legacy_config_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use Linode Interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode interfaces. This is the default setting for new accounts.
     * - `linode_only`. All new Linodes need to use Linode Interfaces. Prevously created Linodes with legacy configuration profile interfaces can still exist if they were created under a previous setting. Linodes using legacy configuration interfaces can be upgraded to Linode Interfaces.
     */
    interfaces_for_new_linodes: 'legacy_config_only' | 'legacy_config_default_but_linode_allowed' | 'linode_default_but_legacy_config_allowed' | 'linode_only';
    /**
     * __Beta__ Defines the default maintenance policy for new Linodes created on this account. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
     */
    maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
    /**
     * Enables network helper across all users by default for new Linodes and Linode Configs.
     */
    network_helper: boolean;
};

export type AddedGetAccountLogins200Writable = {
    data: Array<{
        [key: string]: never;
    }>;
};

export type AddedGetAvailability200Writable = {
    data: Array<{}>;
} & {};

export type AddedGetChildAccounts200Writable = {
    data: Array<{
        /**
         * __Filterable__ First line of this child account's billing address.
         */
        address_1: string;
        /**
         * __Filterable__ Second line of this child account's billing address, if applicable.
         */
        address_2: string;
        /**
         * __Filterable__ The city for this child account's billing address.
         */
        city: string;
        /**
         * __Filterable__ The company name for the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`. You can't change this value yourself. We use it to create the proxy users that a parent account uses to access a child account. Talk to your account team if you need to change this value.
         */
        company: string;
        /**
         * __Filterable__ The two-letter ISO 3166 country code for this child account's billing address.
         */
        country: string;
        /**
         * __Filterable__ The email address of the owner of this child account.
         */
        email: string;
        /**
         * __Filterable__ The first name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
         */
        first_name: string;
        /**
         * __Filterable__ The last name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
         */
        last_name: string;
        /**
         * __Filterable__ The phone number for the owner of this child account.
         */
        phone: string;
        /**
         * __Filterable__ The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.
         *
         * > 📘
         * >
         * > If this is a US military address, use state abbreviations (AA, AE, AP).
         */
        state: string;
        /**
         * The tax identification number for this child account. Use this for tax calculations in some countries. If you live in a country that doesn't collect taxes, ensure this is an empty string (`""`).
         */
        tax_id: string;
        /**
         * __Filterable__ The zip code of this Account's billing address. The following restrictions apply:
         *
         * - Can only contain ASCII letters, numbers, and hyphens (`-`).
         * - Can't contain more than 9 letter or number characters.
         */
        zip: string;
    }>;
};

export type AddedGetClients200Writable = {
    data: Array<{
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
    }>;
};

export type AddedGetEnrolledBetaPrograms200Writable = {} & {
    data: Array<{
        /**
         * The unique identifier of the Beta Program.
         */
        id: string;
    }>;
};

export type AddedGetEntityTransfers200Writable = {} & {
    data?: Array<{
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the entities to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request:
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
         * `canceled`: The transfer has been canceled by the sender.
         * `completed`: The transfer has completed successfully.
         * `failed`: The transfer has failed after initiation.
         * `pending`: The transfer is ready to be accepted.
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    }>;
};

export type AddedGetInvoiceItems200Writable = {
    data: Array<{
        [key: string]: never;
    }>;
};

export type AddedGetInvoices200Writable = {
    data: Array<{
        [key: string]: never;
    }>;
};

export type AddedGetNotifications200Writable = {
    data: Array<{
        /**
         * A full description of this notification, in markdown format. Not all notifications include a `body`. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        body: string | null;
        /**
         * Detailed information about the notification. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        entity: {
            /**
             * The unique ID of the notification's entity, based on the entity type. Returns `null` for an `account` or `promotion` entity.
             */
            id: number | null;
            /**
             * The current name of this notification's entity. Returns `null` for the following `entity` types:
             *
             * - `entity_transfer`
             *
             * - `promotion`
             *
             * - `region`
             */
            label: string | null;
            /**
             * __Filterable__ The type of entity this is related to. An entity can be product or service-specific, such as a `linode`, `loadbalancers`, or `nodebalancers`. It can apply to a specific component, such as your `account`, a specific `promotion` your participating in, a data center (`region`) where you're using services, a transfer from one component to another (an `entity_transfer`), a support `ticket` you've opened, or a `volume` on a specific Linode.
             */
            type: 'account' | 'entity_transfer' | 'linode' | 'loadbalancers' | 'nodebalancer' | 'promotion' | 'region' | 'ticket' | 'volume';
            /**
             * The URL where you can access the notification's object. The URL is relative to the domain where you retrieved the notification. This value is `null` for the `promotion` entity type.
             */
            url: string | null;
        } | null;
        /**
         * A short description of this notification.
         */
        label: string;
        /**
         * A human-readable description of the notification.
         */
        message: string;
        /**
         * The severity of this notification. This field determines how prominently the notification is displayed and the color of the display text.
         */
        severity: 'minor' | 'major' | 'critical';
        /**
         * __Filterable__ The type of notification.
         *
         * > 📘
         * >
         * > A `security_reboot_maintenance_scheduled` event is a global notice that a Linode needs to be rebooted for QEMU upgrade maintenance. Have a look at [this workflow](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance) for guidance on reboooting your Linodes for this maintenance.
         */
        type: 'migration_scheduled' | 'migration_imminent' | 'migration_pending' | 'reboot_scheduled' | 'outage' | 'payment_due' | 'ticket_important' | 'ticket_abuse' | 'notice' | 'maintenance' | 'maintenance_scheduled' | 'promotion' | 'security_reboot_maintenance_scheduled' | 'tax_id_verifying';
        /**
         * If this notification has a duration, this is when the event or action will complete. For example, if there's scheduled maintenance for one of our systems, `until` represents the end of the maintenance window. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        until: string | null;
        /**
         * If this notification is for an event in the future, this specifies when the action occurs. For example, if a compute instance needs to migrate in response to a security advisory, this field sets the approximate time the compute instance will be taken offline for migration. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
         */
        when: string | null;
    }>;
};

export type AddedGetPaymentMethods200Writable = {
    data: Array<{
        data: {
            [key: string]: never;
        } | {
            [key: string]: never;
        } | {
            [key: string]: never;
        };
        /**
         * The unique ID of this Payment Method.
         */
        id: number;
        /**
         * Whether this Payment Method is the default method for automatically processing service charges.
         */
        is_default: boolean;
        /**
         * The type of Payment Method.
         */
        type: 'credit_card' | 'google_pay' | 'paypal';
    }>;
};

export type AddedGetPayments200Writable = {
    data: Array<{
        [key: string]: never;
    }>;
};

export type AddedGetServiceTransfers200Writable = {
    data: Array<{
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the services to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request.
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress.
         * Transfers can take up to 3 hours to complete.
         *
         * `canceled`: The transfer has been canceled by the sender.
         *
         * `completed`: The transfer has completed successfully.
         *
         * `failed`: The transfer has failed after initiation.
         *
         * `pending`: The transfer is ready to be accepted.
         *
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
         * canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    }>;
};

export type AddedGetUser200Writable = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
} & {};

export type AddedGetUsers200Writable = {
    data: Array<{
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
    } & {}>;
};

/**
 * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
 */
export type AddedPostClientWritable = {
    /**
     * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
     */
    label: string;
    /**
     * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
     */
    public: boolean;
    /**
     * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
     */
    redirect_uri: string;
};

export type AddedPostPayPalPayment200Writable = {
    /**
     * The paypal-generated ID for this Payment. Used when authorizing the Payment in PayPal's interface.
     */
    payment_id: string;
};

/**
 * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
 */
export type AddedPostUserWritable = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
};

export type AddedPutUser200Writable = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
} & {};

/**
 * An object representing an enrolled Beta Program for the Account.
 */
export type BetaProgramEnrolledWritable = {
    /**
     * The unique identifier of the Beta Program.
     */
    id: string;
};

/**
 * Child account object.
 */
export type ChildAccountWritable = {
    /**
     * __Filterable__ First line of this child account's billing address.
     */
    address_1: string;
    /**
     * __Filterable__ Second line of this child account's billing address, if applicable.
     */
    address_2: string;
    /**
     * __Filterable__ The city for this child account's billing address.
     */
    city: string;
    /**
     * __Filterable__ The company name for the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`. You can't change this value yourself. We use it to create the proxy users that a parent account uses to access a child account. Talk to your account team if you need to change this value.
     */
    company: string;
    /**
     * __Filterable__ The two-letter ISO 3166 country code for this child account's billing address.
     */
    country: string;
    /**
     * __Filterable__ The email address of the owner of this child account.
     */
    email: string;
    /**
     * __Filterable__ The first name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
     */
    first_name: string;
    /**
     * __Filterable__ The last name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
     */
    last_name: string;
    /**
     * __Filterable__ The phone number for the owner of this child account.
     */
    phone: string;
    /**
     * __Filterable__ The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.
     *
     * > 📘
     * >
     * > If this is a US military address, use state abbreviations (AA, AE, AP).
     */
    state: string;
    /**
     * The tax identification number for this child account. Use this for tax calculations in some countries. If you live in a country that doesn't collect taxes, ensure this is an empty string (`""`).
     */
    tax_id: string;
    /**
     * __Filterable__ The zip code of this Account's billing address. The following restrictions apply:
     *
     * - Can only contain ASCII letters, numbers, and hyphens (`-`).
     * - Can't contain more than 9 letter or number characters.
     */
    zip: string;
};

/**
 * A specific event object. An event is an action taken against an entity related to your account. For example, if you boot a Linode, the system creates an event. The events returned depend on your user grants.
 */
export type EventWritable = {
    /**
     * Additional information about the event. This can be a more detailed representation of an event that can help you diagnose non-obvious failures.
     */
    message: string | null;
};

export type GetMaintenance200Writable = {
    data: Array<{
        /**
         * __Beta__, __Filterable__ The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        complete_time: string;
        /**
         * __Beta__ Differentiates between scheduled and emergency maintenance.
         */
        description: 'Scheduled Maintenance' | 'Emergency Maintenance';
        /**
         * The entity affected by the maintenance.
         */
        entity: {
            /**
             * The unique identifier of the entity targeted by the maintenance.
             */
            id: number;
            /**
             * The name of the entity targeted by the maintenance.
             */
            label: string;
            /**
             * The type of entity.
             */
            type: string;
            /**
             * A combination of the API operation prefix and the entity's `id` that can be used to review the entity.
             */
            url: string;
        };
        /**
         * __Beta__, __Filterable__ The maintenance policy configured by the user.
         */
        maintenance_policy_set: string;
        /**
         * __Beta__, __Filterable__ The earliest time when the maintenance can start. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        not_before: string;
        /**
         * The reason maintenance is being performed.
         */
        reason: string;
        /**
         * __Beta__ The origin of the maintenance. A `platform` source indicates that the maintenance was initiated by Akamai. A `user` source indicates that the maintenance was initiated by the user.
         */
        source: 'platform' | 'user';
        /**
         * __Beta__, __Filterable__ The time the maintenance started. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        start_time: string;
        /**
         * __Filterable__ The maintenance status. Maintenance progress follows the sequence `pending`, `scheduled`, `started`, and `completed`. A `scheduled` status is unique to Linodes that require a reboot for [QEMU maintenance](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance).
         */
        status: 'pending' | 'scheduled' | 'started' | 'in-progress' | 'completed' | 'canceled';
        /**
         * __Filterable__ The type of maintenance.
         */
        type: 'reboot' | 'cold_migration' | 'live_migration' | 'migrate' | 'power_off_on';
        /**
         * __Filterable__ The start time for the maintenance. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
         *
         * - A single ISO 8601 timestamp (yyyy-mm-ddThh:mm:ss), which returns only matches for that value.
         *
         * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
         */
        when: string;
    }>;
};

/**
 * Represents the level of access a restricted user has to a specific resource on the account.
 */
export type GrantWritable = {
    /**
     * The unique identifier of the resource this grant applies to.
     */
    id: number;
    /**
     * The level of access this user has to this entity. If `null`, this user has no access.
     */
    permissions: 'read_only' | 'read_write';
};

export type GrantsResponseWritable = {
    /**
     * The grants this user has for individual Managed Databases on this account.
     */
    database: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual domains on this account.
     */
    domain: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual firewalls on this account.
     */
    firewall: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants the user has to all resources on your account.
     */
    global: {
        /**
         * The level of access this user has to account-level actions, like billing information and user management.
         *
         * > 📘
         * >
         * > A `restricted` user can't be used to manage users, even if this is set to `read-write`. Only unrestricted users can manage other users on an account.
         *
         * __Parent and child accounts__
         *
         * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
         */
        account_access: 'read_only' | 'read_write';
        /**
         * Whether this user can add Managed Databases on the account.
         */
        add_databases: boolean;
        /**
         * Whether this user can add domains on the account.
         */
        add_domains: boolean;
        /**
         * Whether this user can add Firewalls on the account.
         */
        add_firewalls: boolean;
        /**
         * Whether this user can create images from disks on your Linodes, on the account.
         */
        add_images: boolean;
        /**
         * Whether this user can create Linodes.
         */
        add_linodes: boolean;
        /**
         * Whether this user can create Longview clients and view the current plan.
         */
        add_longview: boolean;
        /**
         * Whether this user can add NodeBalancers on the account.
         */
        add_nodebalancers: boolean;
        /**
         * Whether this user can add StackScripts on the account.
         */
        add_stackscripts: boolean;
        /**
         * Whether this user can add volumes on the account.
         */
        add_volumes: boolean;
        /**
         * Whether this user can add Virtual Private Clouds (VPCs) on the account.
         */
        add_vpcs: boolean;
        /**
         * Whether this user can cancel the entire account.
         */
        cancel_account: boolean;
        /**
         * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this gives a parent account access to operations that can be used to manage child accounts. Unrestricted parent account users have access to this grant, while restricted parent users don't. An unrestricted parent user can set this to `true` to add this grant to a restricted parent user. Displayed as `null` for all non-parent accounts.
         */
        child_account_access: boolean | null;
        /**
         * Whether this user can manage your account's Longview subscription.
         */
        longview_subscription: boolean;
    };
    /**
     * The grants this user has for individual images on this account.
     */
    image: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual Linodes on this account.
     */
    linode: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual Longview Clients on this account.
     */
    longview: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has for individual NodeBalancers on this account.
     */
    nodebalancer: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this User has for individual StackScripts on this account.
     */
    stackscript: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has individual Block Storage Volumes on this account.
     */
    volume: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
    /**
     * The grants this user has individual Virtual Private Clouds (VPCs) on this account.
     */
    vpc: Array<{
        /**
         * The unique identifier of the resource this grant applies to.
         */
        id: number;
        /**
         * The level of access this user has to this entity. If `null`, this user has no access.
         */
        permissions: 'read_only' | 'read_write';
    }>;
};

/**
 * Account Invoice object.
 */
export type InvoiceWritable = {
    [key: string]: never;
};

/**
 * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
 */
export type OauthClientWritable = {
    /**
     * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
     */
    label: string;
    /**
     * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
     */
    public: boolean;
    /**
     * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
     */
    redirect_uri: string;
};

/**
 * Payment Method Response Object.
 */
export type PaymentMethodWritable = {
    data: {
        [key: string]: never;
    } | {
        [key: string]: never;
    } | {
        [key: string]: never;
    };
    /**
     * The unique ID of this Payment Method.
     */
    id: number;
    /**
     * Whether this Payment Method is the default method for automatically processing service charges.
     */
    is_default: boolean;
    /**
     * The type of Payment Method.
     */
    type: 'credit_card' | 'google_pay' | 'paypal';
};

/**
 * The token generated manually for a child account so its proxy user can access the API and CLI without going through an OAuth login.
 */
export type ProxyUserTokenWritable = {
    /**
     * __Filterable__ The name of the token. The API automatically sets this to `<username>_<uid>_<time>`. It's composed of the `username` for your parent account user, the unique `uid` Akamai assigned to identify your user, and the `time` the API generated the token. This is for display purposes only, but you can use it to help track how you're using each proxy user token.
     */
    label: string;
};

/**
 * An object representing your network utilization for the current month, in Gigabytes.
 *
 * Certain Regions have separate utilization quotas and rates. For Region-specific network utilization data, see `region_transfers`.
 */
export type TransferWritable = {
    region_transfers: Array<{
        /**
         * The Region ID for this network utilization data.
         */
        id: string;
    }>;
};

/**
 * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
 */
export type UserWritable = {
    /**
     * This user's email address. Akamai uses this address for account management communications.
     */
    email: string;
    /**
     * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
     */
    restricted: boolean;
    /**
     * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
     */
    username: string;
};

/**
 * Call either the `v4` URL, or `v4beta` for operations still in Beta.
 */
export type ApiVersionPath = 'v4' | 'v4beta';

/**
 * The ID of the Beta Program.
 */
export type BetaId = string;

/**
 * The OAuth Client ID to look up.
 */
export type ClientIdPath = string;

/**
 * The OAuth Client ID to look up.
 */
export type ClientIdPathEcf807Fb = string;

/**
 * The child account to look up. You can run the [List child accounts](https://techdocs.akamai.com/linode-api/reference/get-child-accounts) operation to find the applicable account and store its `euuid` as your `euuId`.
 */
export type Eeuid = string;

/**
 * The ID of the Event to designate as seen.
 */
export type EventIdPath214Cd042 = number;

/**
 * The ID of the Event.
 */
export type EventIdPath39255Fcf = number;

/**
 * The ID of the Invoice.
 */
export type InvoiceIdPath = number;

/**
 * The ID of the login object to access.
 */
export type LoginIdPath = number;

/**
 * The page of a collection to return.
 */
export type PageOffset = number;

/**
 * The number of items to return per page.
 */
export type PageSize = number;

/**
 * The ID of the Payment to look up.
 */
export type PaymentIdPath = number;

/**
 * The ID of the Payment Method to look up.
 */
export type PaymentMethodIdPath6078Bc7b = number;

/**
 * The ID of the Payment Method to make default.
 */
export type PaymentMethodIdPathFb39A844 = number;

/**
 * The abbreviated value ("slug") for the applicable data center. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the slug for each data center.
 */
export type RegionIdPath = string;

/**
 * The UUID of the Service Transfer.
 */
export type TokenPathF857F5A2 = string;

/**
 * The UUID of the Entity Transfer.
 */
export type TokenPathFaf66B58 = string;

/**
 * The username to look up.
 */
export type UsernamePath = string;

/**
 * Specifies the `X-Filter` header JSON object's filtering and sort criteria.
 */
export type XFilterHeader = {
    [key: string]: unknown;
} | {
    /**
     * All conditions need to be true.
     */
    '+and': Array<{
        [key: string]: unknown;
    }>;
    /**
     * The provided string needs to be in the value.
     */
    '+contains': string;
    /**
     * The value needs to be greater than the provided number.
     */
    '+gt': number;
    /**
     * The value needs to be greater than or equal to the provided number.
     */
    '+gte': number;
    /**
     * The value needs to be less than the provided number.
     */
    '+lt': number;
    /**
     * The value needs to be less than or equal to the provided number.
     */
    '+lte': number;
    /**
     * The provided string is left out of the results.
     */
    '+neq': string;
    /**
     * At least one condition needs to be true.
     */
    '+or': Array<{
        [key: string]: unknown;
    }>;
    /**
     * Sort in ascending (`asc`) or descending (`desc`) order. This defaults to `asc`. Requires `+order_by`.
     */
    '+order': 'asc' | 'desc';
    /**
     * Order results based on the provided attribute. The attribute needs to be filterable.
     */
    '+order_by': string;
};

export type GetAccountData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account';
};

export type GetAccountErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAccountError = GetAccountErrors[keyof GetAccountErrors];

export type GetAccountResponses = {
    /**
     * Account object.
     */
    200: {
        readonly active_promotions: Array<{
            /**
             * The amount available to spend per month.
             */
            credit_monthly_cap: string;
            /**
             * The total amount of credit left for this promotion.
             */
            credit_remaining: string;
            /**
             * A detailed description of this promotion.
             */
            description: string;
            /**
             * When this promotion's credits expire.
             */
            expire_dt: string;
            /**
             * The location of an image for this promotion.
             */
            image_url: string;
            /**
             * The service to which this promotion applies.
             */
            service_type: 'all' | 'backup' | 'blockstorage' | 'db_mysql' | 'ip_v4' | 'linode' | 'linode_disk' | 'linode_memory' | 'longview' | 'managed' | 'nodebalancer' | 'objectstorage' | 'placement_group' | 'transfer_tx';
            /**
             * Short details of this promotion.
             */
            summary: string;
            /**
             * The amount of credit left for this month for this promotion.
             */
            this_month_credit_remaining: string;
        }>;
        /**
         * __Read-only__ The date and time the account was activated.
         */
        readonly active_since: string;
        /**
         * The first line of this account's billing address.
         */
        address_1: string;
        /**
         * The second line of this account's billing address.
         */
        address_2: string;
        /**
         * __Read-only__ This account's balance, in US dollars.
         */
        readonly balance: number;
        /**
         * __Read-only__ This account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
         */
        readonly balance_uninvoiced: number;
        /**
         * __Read-only__ The source of service charges for this account. Accounts that are associated with Akamai-specific customers return a value of `akamai`. All other accounts return a value of `linode`.
         */
        billing_source: 'akamai' | 'linode';
        /**
         * __Read-only__ The Akamai Cloud Computing services your account supports.
         */
        readonly capabilities: Array<string>;
        /**
         * The city for this account's `address`.
         */
        city: string;
        /**
         * The company name assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        company: string;
        /**
         * The two-letter ISO 3166 country code for this account's `address`.
         */
        country: string;
        /**
         * __Read-only__ The credit card information assigned to this account.
         */
        readonly credit_card: {
            /**
             * The expiration month and year of the `credit_card`.
             */
            expiry: string;
            /**
             * The last four digits of the `credit_card` assigned to this account.
             */
            last_four: string;
        };
        /**
         * The email address of the person assigned to this account.
         */
        email: string;
        /**
         * __Read-only__ An external unique identifier for this account.
         */
        readonly euuid: string;
        /**
         * The first name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        first_name: string;
        /**
         * The last name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        last_name: string;
        /**
         * The phone number assigned to this account.
         */
        phone: string;
        /**
         * The state or province for the `address` set for your account, if applicable.
         *
         * - If the `address` is in the United States (US) or Canada (CA), this is the two-letter ISO 3166 code for the state or province.
         *
         * - If it's a US military `address`, this is the abbreviation for that territory. This includes `AA` for Armed Forces Americas (excluding Canada), `AE` for Armed Forces Africa, Europe, Middle East, and Canada, or `AP` for Armed Forces Pacific.
         *
         * - If outside the US or CA, this is the province associated with the account's `address`.
         */
        state: string;
        /**
         * The tax identification number (TIN) assigned to this account, used for tax calculations. A TIN is set by the national authorities in your `country`, based on your `address_1`, and it may be named differently between countries. Set to an empty string (`""`) if a TIN doesn't apply or for countries that don't collect tax.
         *
         * > 📘
         * >
         * > This value is externally validated. If the validation is successful, a `tax_id_valid` [event](https://techdocs.akamai.com/linode-api/reference/get-events) is triggered. If unsuccessful, a `tax_id_invalid` event is triggered and an error response is issued for an operation that included it.
         */
        tax_id: string;
        /**
         * The zip code for this account's `address`.
         *
         * - It can only contain ASCII letters, numbers, and dashes (`-`).
         *
         * - It can't contain more than nine letter or number characters.
         */
        zip: string;
    };
};

export type GetAccountResponse = GetAccountResponses[keyof GetAccountResponses];

export type PutAccountData = {
    /**
     * Account object.
     */
    body: {
        readonly active_promotions: Array<{
            /**
             * The amount available to spend per month.
             */
            credit_monthly_cap: string;
            /**
             * The total amount of credit left for this promotion.
             */
            credit_remaining: string;
            /**
             * A detailed description of this promotion.
             */
            description: string;
            /**
             * When this promotion's credits expire.
             */
            expire_dt: string;
            /**
             * The location of an image for this promotion.
             */
            image_url: string;
            /**
             * The service to which this promotion applies.
             */
            service_type: 'all' | 'backup' | 'blockstorage' | 'db_mysql' | 'ip_v4' | 'linode' | 'linode_disk' | 'linode_memory' | 'longview' | 'managed' | 'nodebalancer' | 'objectstorage' | 'placement_group' | 'transfer_tx';
            /**
             * Short details of this promotion.
             */
            summary: string;
            /**
             * The amount of credit left for this month for this promotion.
             */
            this_month_credit_remaining: string;
        }>;
        /**
         * __Read-only__ The date and time the account was activated.
         */
        readonly active_since: string;
        /**
         * The first line of this account's billing address.
         */
        address_1: string;
        /**
         * The second line of this account's billing address.
         */
        address_2: string;
        /**
         * __Read-only__ This account's balance, in US dollars.
         */
        readonly balance: number;
        /**
         * __Read-only__ This account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
         */
        readonly balance_uninvoiced: number;
        /**
         * __Read-only__ The source of service charges for this account. Accounts that are associated with Akamai-specific customers return a value of `akamai`. All other accounts return a value of `linode`.
         */
        billing_source: 'akamai' | 'linode';
        /**
         * __Read-only__ The Akamai Cloud Computing services your account supports.
         */
        readonly capabilities: Array<string>;
        /**
         * The city for this account's `address`.
         */
        city: string;
        /**
         * The company name assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        company: string;
        /**
         * The two-letter ISO 3166 country code for this account's `address`.
         */
        country: string;
        /**
         * __Read-only__ The credit card information assigned to this account.
         */
        readonly credit_card: {
            /**
             * The expiration month and year of the `credit_card`.
             */
            expiry: string;
            /**
             * The last four digits of the `credit_card` assigned to this account.
             */
            last_four: string;
        };
        /**
         * The email address of the person assigned to this account.
         */
        email: string;
        /**
         * __Read-only__ An external unique identifier for this account.
         */
        readonly euuid: string;
        /**
         * The first name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        first_name: string;
        /**
         * The last name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        last_name: string;
        /**
         * The phone number assigned to this account.
         */
        phone: string;
        /**
         * The state or province for the `address` set for your account, if applicable.
         *
         * - If the `address` is in the United States (US) or Canada (CA), this is the two-letter ISO 3166 code for the state or province.
         *
         * - If it's a US military `address`, this is the abbreviation for that territory. This includes `AA` for Armed Forces Americas (excluding Canada), `AE` for Armed Forces Africa, Europe, Middle East, and Canada, or `AP` for Armed Forces Pacific.
         *
         * - If outside the US or CA, this is the province associated with the account's `address`.
         */
        state: string;
        /**
         * The tax identification number (TIN) assigned to this account, used for tax calculations. A TIN is set by the national authorities in your `country`, based on your `address_1`, and it may be named differently between countries. Set to an empty string (`""`) if a TIN doesn't apply or for countries that don't collect tax.
         *
         * > 📘
         * >
         * > This value is externally validated. If the validation is successful, a `tax_id_valid` [event](https://techdocs.akamai.com/linode-api/reference/get-events) is triggered. If unsuccessful, a `tax_id_invalid` event is triggered and an error response is issued for an operation that included it.
         */
        tax_id: string;
        /**
         * The zip code for this account's `address`.
         *
         * - It can only contain ASCII letters, numbers, and dashes (`-`).
         *
         * - It can't contain more than nine letter or number characters.
         */
        zip: string;
    };
    path?: never;
    query?: never;
    url: '/account';
};

export type PutAccountErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutAccountError = PutAccountErrors[keyof PutAccountErrors];

export type PutAccountResponses = {
    /**
     * Account object.
     */
    200: {
        readonly active_promotions: Array<{
            /**
             * The amount available to spend per month.
             */
            credit_monthly_cap: string;
            /**
             * The total amount of credit left for this promotion.
             */
            credit_remaining: string;
            /**
             * A detailed description of this promotion.
             */
            description: string;
            /**
             * When this promotion's credits expire.
             */
            expire_dt: string;
            /**
             * The location of an image for this promotion.
             */
            image_url: string;
            /**
             * The service to which this promotion applies.
             */
            service_type: 'all' | 'backup' | 'blockstorage' | 'db_mysql' | 'ip_v4' | 'linode' | 'linode_disk' | 'linode_memory' | 'longview' | 'managed' | 'nodebalancer' | 'objectstorage' | 'placement_group' | 'transfer_tx';
            /**
             * Short details of this promotion.
             */
            summary: string;
            /**
             * The amount of credit left for this month for this promotion.
             */
            this_month_credit_remaining: string;
        }>;
        /**
         * __Read-only__ The date and time the account was activated.
         */
        readonly active_since: string;
        /**
         * The first line of this account's billing address.
         */
        address_1: string;
        /**
         * The second line of this account's billing address.
         */
        address_2: string;
        /**
         * __Read-only__ This account's balance, in US dollars.
         */
        readonly balance: number;
        /**
         * __Read-only__ This account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
         */
        readonly balance_uninvoiced: number;
        /**
         * __Read-only__ The source of service charges for this account. Accounts that are associated with Akamai-specific customers return a value of `akamai`. All other accounts return a value of `linode`.
         */
        billing_source: 'akamai' | 'linode';
        /**
         * __Read-only__ The Akamai Cloud Computing services your account supports.
         */
        readonly capabilities: Array<string>;
        /**
         * The city for this account's `address`.
         */
        city: string;
        /**
         * The company name assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        company: string;
        /**
         * The two-letter ISO 3166 country code for this account's `address`.
         */
        country: string;
        /**
         * __Read-only__ The credit card information assigned to this account.
         */
        readonly credit_card: {
            /**
             * The expiration month and year of the `credit_card`.
             */
            expiry: string;
            /**
             * The last four digits of the `credit_card` assigned to this account.
             */
            last_four: string;
        };
        /**
         * The email address of the person assigned to this account.
         */
        email: string;
        /**
         * __Read-only__ An external unique identifier for this account.
         */
        readonly euuid: string;
        /**
         * The first name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        first_name: string;
        /**
         * The last name of the person assigned to this account. This value can't include the characters, `<` `>` `(` `)` `"` `=`.
         */
        last_name: string;
        /**
         * The phone number assigned to this account.
         */
        phone: string;
        /**
         * The state or province for the `address` set for your account, if applicable.
         *
         * - If the `address` is in the United States (US) or Canada (CA), this is the two-letter ISO 3166 code for the state or province.
         *
         * - If it's a US military `address`, this is the abbreviation for that territory. This includes `AA` for Armed Forces Americas (excluding Canada), `AE` for Armed Forces Africa, Europe, Middle East, and Canada, or `AP` for Armed Forces Pacific.
         *
         * - If outside the US or CA, this is the province associated with the account's `address`.
         */
        state: string;
        /**
         * The tax identification number (TIN) assigned to this account, used for tax calculations. A TIN is set by the national authorities in your `country`, based on your `address_1`, and it may be named differently between countries. Set to an empty string (`""`) if a TIN doesn't apply or for countries that don't collect tax.
         *
         * > 📘
         * >
         * > This value is externally validated. If the validation is successful, a `tax_id_valid` [event](https://techdocs.akamai.com/linode-api/reference/get-events) is triggered. If unsuccessful, a `tax_id_invalid` event is triggered and an error response is issued for an operation that included it.
         */
        tax_id: string;
        /**
         * The zip code for this account's `address`.
         *
         * - It can only contain ASCII letters, numbers, and dashes (`-`).
         *
         * - It can't contain more than nine letter or number characters.
         */
        zip: string;
    };
};

export type PutAccountResponse = PutAccountResponses[keyof PutAccountResponses];

export type GetAccountAgreementsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/agreements';
};

export type GetAccountAgreementsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAccountAgreementsError = GetAccountAgreementsErrors[keyof GetAccountAgreementsErrors];

export type GetAccountAgreementsResponses = {
    /**
     * Acknowledgment status for agreements on your account. When acknowledging any agreements, set them to `true` and omit any remainders.
     */
    200: {
        /**
         * Certain regions require that you share your tax identification number (TIN) with Akamai. When you do, you need to acknowledge Akamai's [privacy statement](https://www.akamai.com/legal/privacy-statement) agreement, in regards to its protection. When set to `true`, you've acknowledged this agreement.
         */
        billing_agreement: boolean;
        /**
         * The acknowledgement status for the [cross-border data transfer](https://www.akamai.com/legal/compliance/privacy-trust-center/cross-border-data-transfer-statement) agreement.
         */
        eu_model: boolean;
        /**
         * The acknowledgement status for Akamai's [master service agreement](https://www.linode.com/legal-msa/).
         */
        master_service_agreement: boolean;
        /**
         * The acknowledgement status for Akamai's [privacy statement](https://www.akamai.com/legal/privacy-statement).
         */
        privacy_policy: boolean;
    };
};

export type GetAccountAgreementsResponse = GetAccountAgreementsResponses[keyof GetAccountAgreementsResponses];

export type PostAccountAgreementsData = {
    /**
     * Acknowledgment status for agreements on your account. When acknowledging any agreements, set them to `true` and omit any remainders.
     */
    body: {
        /**
         * Certain regions require that you share your tax identification number (TIN) with Akamai. When you do, you need to acknowledge Akamai's [privacy statement](https://www.akamai.com/legal/privacy-statement) agreement, in regards to its protection. When set to `true`, you've acknowledged this agreement.
         */
        billing_agreement: boolean;
        /**
         * The acknowledgement status for the [cross-border data transfer](https://www.akamai.com/legal/compliance/privacy-trust-center/cross-border-data-transfer-statement) agreement.
         */
        eu_model: boolean;
        /**
         * The acknowledgement status for Akamai's [master service agreement](https://www.linode.com/legal-msa/).
         */
        master_service_agreement: boolean;
        /**
         * The acknowledgement status for Akamai's [privacy statement](https://www.akamai.com/legal/privacy-statement).
         */
        privacy_policy: boolean;
    };
    path?: never;
    query?: never;
    url: '/account/agreements';
};

export type PostAccountAgreementsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAccountAgreementsError = PostAccountAgreementsErrors[keyof PostAccountAgreementsErrors];

export type PostAccountAgreementsResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostAccountAgreementsResponse = PostAccountAgreementsResponses[keyof PostAccountAgreementsResponses];

export type GetAvailabilityData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/availability';
};

export type GetAvailabilityErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAvailabilityError = GetAvailabilityErrors[keyof GetAvailabilityErrors];

export type GetAvailabilityResponses = {
    /**
     * List of regions and the services available in each.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ A list of services _available_ to your account in the `region`.
             */
            readonly available: Array<string>;
            /**
             * __Read-only__ The Akamai cloud computing data center (region), represented by a slug value. You can view a full list of regions and their associated slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation.
             */
            readonly region: string;
            /**
             * __Read-only__ A list of services _unavailable_ to your account in the `region`.
             */
            readonly unavailable: Array<string>;
        }>;
    } & {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetAvailabilityResponse = GetAvailabilityResponses[keyof GetAvailabilityResponses];

export type GetAccountAvailabilityData = {
    body?: never;
    path: {
        /**
         * The abbreviated value ("slug") for the applicable data center. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the slug for each data center.
         */
        regionId: string;
    };
    query?: never;
    url: '/account/availability/{regionId}';
};

export type GetAccountAvailabilityErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAccountAvailabilityError = GetAccountAvailabilityErrors[keyof GetAccountAvailabilityErrors];

export type GetAccountAvailabilityResponses = {
    /**
     * Account Service Availability object.
     */
    200: {
        /**
         * __Read-only__ A list of services _available_ to your account in the `region`.
         */
        readonly available: Array<string>;
        /**
         * __Read-only__ The Akamai cloud computing data center (region), represented by a slug value. You can view a full list of regions and their associated slugs with the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation.
         */
        readonly region: string;
        /**
         * __Read-only__ A list of services _unavailable_ to your account in the `region`.
         */
        readonly unavailable: Array<string>;
    };
};

export type GetAccountAvailabilityResponse = GetAccountAvailabilityResponses[keyof GetAccountAvailabilityResponses];

export type GetEnrolledBetaProgramsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/betas';
};

export type GetEnrolledBetaProgramsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetEnrolledBetaProgramsError = GetEnrolledBetaProgramsErrors[keyof GetEnrolledBetaProgramsErrors];

export type GetEnrolledBetaProgramsResponses = {
    /**
     * Returns a paginated list of all enrolled Beta Program objects for the Account.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * __Read-only__ Additional details regarding the Beta Program.
             */
            readonly description: string | null;
            /**
             * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
             *
             * `null` indicates that the Beta Program is ongoing.
             */
            readonly ended: string | null;
            /**
             * __Filterable__, __Read-only__ The date-time of Account enrollment to the Beta Program.
             */
            readonly enrolled: string;
            /**
             * The unique identifier of the Beta Program.
             */
            id: string;
            /**
             * __Filterable__, __Read-only__ The name of the Beta Program.
             */
            readonly label: string;
            /**
             * __Filterable__, __Read-only__ The start date-time of the Beta Program.
             */
            readonly started: string;
        }>;
    };
};

export type GetEnrolledBetaProgramsResponse = GetEnrolledBetaProgramsResponses[keyof GetEnrolledBetaProgramsResponses];

export type PostBetaProgramData = {
    /**
     * The Beta Program ID to enroll in for your Account.
     */
    body: {
        /**
         * The unique identifier of the Beta Program.
         */
        id: string;
    };
    path?: never;
    query?: never;
    url: '/account/betas';
};

export type PostBetaProgramErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostBetaProgramError = PostBetaProgramErrors[keyof PostBetaProgramErrors];

export type PostBetaProgramResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostBetaProgramResponse = PostBetaProgramResponses[keyof PostBetaProgramResponses];

export type GetEnrolledBetaProgramData = {
    body?: never;
    path: {
        /**
         * The ID of the Beta Program.
         */
        betaId: string;
    };
    query?: never;
    url: '/account/betas/{betaId}';
};

export type GetEnrolledBetaProgramErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetEnrolledBetaProgramError = GetEnrolledBetaProgramErrors[keyof GetEnrolledBetaProgramErrors];

export type GetEnrolledBetaProgramResponses = {
    /**
     * An object representing an enrolled Beta Program for the Account.
     */
    200: {
        /**
         * __Read-only__ Additional details regarding the Beta Program.
         */
        readonly description: string | null;
        /**
         * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
         *
         * `null` indicates that the Beta Program is ongoing.
         */
        readonly ended: string | null;
        /**
         * __Filterable__, __Read-only__ The date-time of Account enrollment to the Beta Program.
         */
        readonly enrolled: string;
        /**
         * The unique identifier of the Beta Program.
         */
        id: string;
        /**
         * __Filterable__, __Read-only__ The name of the Beta Program.
         */
        readonly label: string;
        /**
         * __Filterable__, __Read-only__ The start date-time of the Beta Program.
         */
        readonly started: string;
    };
};

export type GetEnrolledBetaProgramResponse = GetEnrolledBetaProgramResponses[keyof GetEnrolledBetaProgramResponses];

export type PostCancelAccountData = {
    /**
     * Supply a comment stating the reason that you are cancelling your account.
     */
    body: {
        /**
         * Any reason for cancelling the account, and any other comments you might have about your Linode service.
         */
        comments: string;
    };
    path?: never;
    query?: never;
    url: '/account/cancel';
};

export type PostCancelAccountErrors = {
    /**
     * Could not charge the credit card on file.
     */
    409: {
        errors: Array<{
            /**
             * A string explaining that the account could not be canceled because there is an outstanding balance on the account that must be paid first.
             */
            reason: string;
        }>;
    };
    /**
     * Account is taking longer than expected to cancel.
     */
    504: {
        errors: Array<{
            /**
             * A string explaining that the account is taking longer to close than expected.
             */
            reason: string;
        }>;
    };
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCancelAccountError = PostCancelAccountErrors[keyof PostCancelAccountErrors];

export type PostCancelAccountResponses = {
    /**
     * Account canceled.
     */
    200: {
        /**
         * A link to Linode's exit survey.
         */
        survey_link: string;
    };
};

export type PostCancelAccountResponse = PostCancelAccountResponses[keyof PostCancelAccountResponses];

export type GetChildAccountsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/child-accounts';
};

export type GetChildAccountsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetChildAccountsError = GetChildAccountsErrors[keyof GetChildAccountsErrors];

export type GetChildAccountsResponses = {
    /**
     * Returns child-level accounts.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The activation date and time for the child account.
             */
            readonly active_since: string;
            /**
             * __Filterable__ First line of this child account's billing address.
             */
            address_1: string;
            /**
             * __Filterable__ Second line of this child account's billing address, if applicable.
             */
            address_2: string;
            /**
             * __Read-only__ This child account's balance, in US dollars.
             */
            readonly balance: number;
            /**
             * __Read-only__ This child account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
             */
            readonly balance_uninvoiced: number;
            /**
             * __Read-only__ The source of service charges for this account, as determined by its relationship with Akamai. The API returns a value of `external` to describe a child account in a parent-child account environment.
             */
            billing_source: 'external';
            /**
             * __Read-only__ A list of the capabilities the child account supports.
             */
            readonly capabilities: Array<string>;
            /**
             * __Filterable__ The city for this child account's billing address.
             */
            city: string;
            /**
             * __Filterable__ The company name for the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`. You can't change this value yourself. We use it to create the proxy users that a parent account uses to access a child account. Talk to your account team if you need to change this value.
             */
            company: string;
            /**
             * __Filterable__ The two-letter ISO 3166 country code for this child account's billing address.
             */
            country: string;
            /**
             * __Read-only__ Information for the credit card you've assigned to this child account.
             */
            readonly credit_card: {
                /**
                 * The expiration month and year of the credit card.
                 */
                expiry: string;
                /**
                 * The last four digits of the credit card.
                 */
                last_four: string;
            };
            /**
             * __Filterable__ The email address of the owner of this child account.
             */
            email: string;
            /**
             * __Read-only__ An external, unique identifier that Akamai assigned to the child account.
             */
            readonly euuid: string;
            /**
             * __Filterable__ The first name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
             */
            first_name: string;
            /**
             * __Filterable__ The last name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
             */
            last_name: string;
            /**
             * __Filterable__ The phone number for the owner of this child account.
             */
            phone: string;
            /**
             * __Filterable__ The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.
             *
             * > 📘
             * >
             * > If this is a US military address, use state abbreviations (AA, AE, AP).
             */
            state: string;
            /**
             * The tax identification number for this child account. Use this for tax calculations in some countries. If you live in a country that doesn't collect taxes, ensure this is an empty string (`""`).
             */
            tax_id: string;
            /**
             * __Filterable__ The zip code of this Account's billing address. The following restrictions apply:
             *
             * - Can only contain ASCII letters, numbers, and hyphens (`-`).
             * - Can't contain more than 9 letter or number characters.
             */
            zip: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetChildAccountsResponse = GetChildAccountsResponses[keyof GetChildAccountsResponses];

export type GetChildAccountData = {
    body?: never;
    path: {
        /**
         * The child account to look up. You can run the [List child accounts](https://techdocs.akamai.com/linode-api/reference/get-child-accounts) operation to find the applicable account and store its `euuid` as your `euuId`.
         */
        euuId: string;
    };
    query?: never;
    url: '/account/child-accounts/{euuId}';
};

export type GetChildAccountErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetChildAccountError = GetChildAccountErrors[keyof GetChildAccountErrors];

export type GetChildAccountResponses = {
    /**
     * Child account object.
     */
    200: {
        /**
         * __Read-only__ The activation date and time for the child account.
         */
        readonly active_since: string;
        /**
         * __Filterable__ First line of this child account's billing address.
         */
        address_1: string;
        /**
         * __Filterable__ Second line of this child account's billing address, if applicable.
         */
        address_2: string;
        /**
         * __Read-only__ This child account's balance, in US dollars.
         */
        readonly balance: number;
        /**
         * __Read-only__ This child account's current estimated invoice in US dollars. This is not your final invoice balance. Transfer charges are not included in the estimate.
         */
        readonly balance_uninvoiced: number;
        /**
         * __Read-only__ The source of service charges for this account, as determined by its relationship with Akamai. The API returns a value of `external` to describe a child account in a parent-child account environment.
         */
        billing_source: 'external';
        /**
         * __Read-only__ A list of the capabilities the child account supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Filterable__ The city for this child account's billing address.
         */
        city: string;
        /**
         * __Filterable__ The company name for the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`. You can't change this value yourself. We use it to create the proxy users that a parent account uses to access a child account. Talk to your account team if you need to change this value.
         */
        company: string;
        /**
         * __Filterable__ The two-letter ISO 3166 country code for this child account's billing address.
         */
        country: string;
        /**
         * __Read-only__ Information for the credit card you've assigned to this child account.
         */
        readonly credit_card: {
            /**
             * The expiration month and year of the credit card.
             */
            expiry: string;
            /**
             * The last four digits of the credit card.
             */
            last_four: string;
        };
        /**
         * __Filterable__ The email address of the owner of this child account.
         */
        email: string;
        /**
         * __Read-only__ An external, unique identifier that Akamai assigned to the child account.
         */
        readonly euuid: string;
        /**
         * __Filterable__ The first name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
         */
        first_name: string;
        /**
         * __Filterable__ The last name of the owner of this child account. It can't include any of these characters: `<` `>` `(` `)` `"` `=`.
         */
        last_name: string;
        /**
         * __Filterable__ The phone number for the owner of this child account.
         */
        phone: string;
        /**
         * __Filterable__ The state or province for the billing address (`address_1` and `address_2, if applicable`). If in the United States (US) or Canada (CA), this is the two-letter ISO 3166 State or Province code.
         *
         * > 📘
         * >
         * > If this is a US military address, use state abbreviations (AA, AE, AP).
         */
        state: string;
        /**
         * The tax identification number for this child account. Use this for tax calculations in some countries. If you live in a country that doesn't collect taxes, ensure this is an empty string (`""`).
         */
        tax_id: string;
        /**
         * __Filterable__ The zip code of this Account's billing address. The following restrictions apply:
         *
         * - Can only contain ASCII letters, numbers, and hyphens (`-`).
         * - Can't contain more than 9 letter or number characters.
         */
        zip: string;
    };
};

export type GetChildAccountResponse = GetChildAccountResponses[keyof GetChildAccountResponses];

export type PostChildAccountTokenData = {
    body?: never;
    path: {
        /**
         * The child account to look up. You can run the [List child accounts](https://techdocs.akamai.com/linode-api/reference/get-child-accounts) operation to find the applicable account and store its `euuid` as your `euuId`.
         */
        euuId: string;
    };
    query?: never;
    url: '/account/child-accounts/{euuId}/token';
};

export type PostChildAccountTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostChildAccountTokenError = PostChildAccountTokenErrors[keyof PostChildAccountTokenErrors];

export type PostChildAccountTokenResponses = {
    /**
     * The token generated manually for a child account so its proxy user can access the API and CLI without going through an OAuth login.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date and time this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token expires. This is default set to 15 minutes from the time of creation. Proxy user tokens can't be renewed. After this time, Akamai revokes the token and you need to generate a new one.
         */
        readonly expiry: string;
        /**
         * __Read-only__ The proxy user token's unique ID, which can be used to revoke it.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of the token. The API automatically sets this to `<username>_<uid>_<time>`. It's composed of the `username` for your parent account user, the unique `uid` Akamai assigned to identify your user, and the `time` the API generated the token. This is for display purposes only, but you can use it to help track how you're using each proxy user token.
         */
        label: string;
        /**
         * __Read-only__ The scopes this token was created with. Defaults to `*`. Proxy user tokens automatically inherit all the permissions of the proxy user.
         */
        readonly scopes: string;
        /**
         * __Read-only__ The proxy user token that can be used to access the API and CLI. After you [create](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) a token, you can see the full token in the response. All other operations that contain this token only show the first 16 characters in their response.
         */
        readonly token: string;
    };
};

export type PostChildAccountTokenResponse = PostChildAccountTokenResponses[keyof PostChildAccountTokenResponses];

export type PostCreditCardData = {
    /**
     * An object representing the credit card information you have on file with Linode to make Payments against your Account.
     */
    body: {
        /**
         * Your credit card number. No spaces or hyphens (`-`) allowed.
         */
        card_number: string;
        /**
         * CVV (Card Verification Value) of the credit card, typically found on the back of the card.
         */
        cvv: string;
        /**
         * A value from 1-12 representing the expiration month of your credit card.
         *
         * - 1 = January
         * - 2 = February
         * - 3 = March
         * - Etc.
         */
        expiry_month: number;
        /**
         * A four-digit integer representing the expiration year of your credit card.
         *
         * The combination of `expiry_month` and `expiry_year` must result in a month/year combination of the current month or in the future. An expiration date set in the past is invalid.
         */
        expiry_year: number;
    };
    path?: never;
    query?: never;
    url: '/account/credit-card';
};

export type PostCreditCardErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCreditCardError = PostCreditCardErrors[keyof PostCreditCardErrors];

export type PostCreditCardResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostCreditCardResponse = PostCreditCardResponses[keyof PostCreditCardResponses];

export type GetEntityTransfersData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/entity-transfers';
};

export type GetEntityTransfersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetEntityTransfersError = GetEntityTransfersErrors[keyof GetEntityTransfersErrors];

export type GetEntityTransfersResponses = {
    /**
     * Returns a paginated list of Entity Transfer objects containing the details of all transfers that have been created and accepted by this account.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data?: Array<{
            /**
             * When this transfer was created.
             */
            created: string;
            /**
             * A collection of the entities to include in this transfer request, separated by type.
             */
            entities: {
                /**
                 * An array containing the IDs of each of the Linodes included in this transfer.
                 */
                linodes: Array<number>;
            };
            /**
             * When this transfer expires. Transfers will automatically expire 24 hours after creation.
             */
            expiry: string;
            /**
             * __Filterable__ If the requesting account created this transfer.
             */
            is_sender: boolean;
            /**
             * __Filterable__ The status of the transfer request:
             *
             * `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
             * `canceled`: The transfer has been canceled by the sender.
             * `completed`: The transfer has completed successfully.
             * `failed`: The transfer has failed after initiation.
             * `pending`: The transfer is ready to be accepted.
             * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
             */
            status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
            /**
             * The token used to identify and accept or cancel this transfer.
             */
            token: string;
            /**
             * When this transfer was last updated.
             */
            updated: string;
        }>;
    };
};

export type GetEntityTransfersResponse = GetEntityTransfersResponses[keyof GetEntityTransfersResponses];

export type PostEntityTransferData = {
    /**
     * The entities to include in this transfer request.
     */
    body?: {
        /**
         * A collection of the entities to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
    };
    path?: never;
    query?: never;
    url: '/account/entity-transfers';
};

export type PostEntityTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostEntityTransferError = PostEntityTransferErrors[keyof PostEntityTransferErrors];

export type PostEntityTransferResponses = {
    /**
     * An object representing an Entity Transfer.
     */
    200: {
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the entities to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request:
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
         * `canceled`: The transfer has been canceled by the sender.
         * `completed`: The transfer has completed successfully.
         * `failed`: The transfer has failed after initiation.
         * `pending`: The transfer is ready to be accepted.
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    };
};

export type PostEntityTransferResponse = PostEntityTransferResponses[keyof PostEntityTransferResponses];

export type DeleteEntityTransferData = {
    body?: never;
    path: {
        /**
         * The UUID of the Entity Transfer.
         */
        token: string;
    };
    query?: never;
    url: '/account/entity-transfers/{token}';
};

export type DeleteEntityTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteEntityTransferError = DeleteEntityTransferErrors[keyof DeleteEntityTransferErrors];

export type DeleteEntityTransferResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteEntityTransferResponse = DeleteEntityTransferResponses[keyof DeleteEntityTransferResponses];

export type GetEntityTransferData = {
    body?: never;
    path: {
        /**
         * The UUID of the Entity Transfer.
         */
        token: string;
    };
    query?: never;
    url: '/account/entity-transfers/{token}';
};

export type GetEntityTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetEntityTransferError = GetEntityTransferErrors[keyof GetEntityTransferErrors];

export type GetEntityTransferResponses = {
    /**
     * An object representing an Entity Transfer.
     */
    200: {
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the entities to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request:
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress. Transfers can take up to 3 hours to complete.
         * `canceled`: The transfer has been canceled by the sender.
         * `completed`: The transfer has completed successfully.
         * `failed`: The transfer has failed after initiation.
         * `pending`: The transfer is ready to be accepted.
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    };
};

export type GetEntityTransferResponse = GetEntityTransferResponses[keyof GetEntityTransferResponses];

export type PostAcceptEntityTransferData = {
    body?: never;
    path: {
        /**
         * The UUID of the Entity Transfer.
         */
        token: string;
    };
    query?: never;
    url: '/account/entity-transfers/{token}/accept';
};

export type PostAcceptEntityTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAcceptEntityTransferError = PostAcceptEntityTransferErrors[keyof PostAcceptEntityTransferErrors];

export type PostAcceptEntityTransferResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostAcceptEntityTransferResponse = PostAcceptEntityTransferResponses[keyof PostAcceptEntityTransferResponses];

export type GetEventsData = {
    body?: never;
    headers?: {
        /**
         * Specifies the `X-Filter` header JSON object's filtering and sort criteria.
         */
        'X-Filter'?: {
            [key: string]: unknown;
        } | {
            /**
             * All conditions need to be true.
             */
            '+and': Array<{
                [key: string]: unknown;
            }>;
            /**
             * The provided string needs to be in the value.
             */
            '+contains': string;
            /**
             * The value needs to be greater than the provided number.
             */
            '+gt': number;
            /**
             * The value needs to be greater than or equal to the provided number.
             */
            '+gte': number;
            /**
             * The value needs to be less than the provided number.
             */
            '+lt': number;
            /**
             * The value needs to be less than or equal to the provided number.
             */
            '+lte': number;
            /**
             * The provided string is left out of the results.
             */
            '+neq': string;
            /**
             * At least one condition needs to be true.
             */
            '+or': Array<{
                [key: string]: unknown;
            }>;
            /**
             * Sort in ascending (`asc`) or descending (`desc`) order. This defaults to `asc`. Requires `+order_by`.
             */
            '+order': 'asc' | 'desc';
            /**
             * Order results based on the provided attribute. The attribute needs to be filterable.
             */
            '+order_by': string;
        };
    };
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/events';
};

export type GetEventsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetEventsError = GetEventsErrors[keyof GetEventsErrors];

export type GetEventsResponses = {
    /**
     * Returns a paginated list of Event objects from the last 90 days.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__ The action that caused this event. New actions may be added in the future.
             */
            action: 'account_agreement_eu_model' | 'account_promo_apply' | 'account_update' | 'account_settings_update' | 'backups_enable' | 'backups_cancel' | 'backups_restore' | 'community_question_reply' | 'community_like' | 'community_mention' | 'credit_card_updated' | 'database_create' | 'database_delete' | 'database_update' | 'database_failed' | 'database_degraded' | 'database_create_failed' | 'database_update_failed' | 'database_backup_create' | 'database_backup_restore' | 'database_backup_delete' | 'database_credentials_reset' | 'database_low_disk_space' | 'database_scale' | 'database_resize' | 'database_resize_create' | 'database_migrate' | 'database_upgrade' | 'database_suspend' | 'database_resume' | 'disk_create' | 'disk_delete' | 'disk_update' | 'disk_duplicate' | 'disk_imagize' | 'disk_resize' | 'dns_record_create' | 'dns_record_delete' | 'dns_record_update' | 'dns_zone_create' | 'dns_zone_delete' | 'dns_zone_import' | 'dns_zone_update' | 'entity_transfer_accept' | 'entity_transfer_accept_recipient' | 'entity_transfer_cancel' | 'entity_transfer_create' | 'entity_transfer_fail' | 'entity_transfer_stale' | 'firewall_apply' | 'firewall_create' | 'firewall_delete' | 'firewall_disable' | 'firewall_enable' | 'firewall_update' | 'firewall_device_add' | 'firewall_device_remove' | 'firewall_rules_update' | 'host_reboot' | 'image_delete' | 'image_update' | 'image_upload' | 'interface_create' | 'interface_delete' | 'interface_update' | 'ipaddress_update' | 'ipv6pool_add' | 'ipv6pool_delete' | 'lassie_reboot' | 'lish_boot' | 'linode_addip' | 'linode_boot' | 'linode_clone' | 'linode_create' | 'linode_delete' | 'linode_update' | 'linode_deleteip' | 'linode_kvmify' | 'linode_migrate' | 'linode_migrate_datacenter' | 'linode_migrate_datacenter_create' | 'linode_mutate' | 'linode_mutate_create' | 'linode_poweroff_on' | 'linode_reboot' | 'linode_rebuild' | 'linode_resize' | 'linode_resize_create' | 'linode_resize_warm_create' | 'linode_shutdown' | 'linode_snapshot' | 'linode_config_create' | 'linode_config_delete' | 'linode_config_update' | 'lke_cluster_create' | 'lke_cluster_update' | 'lke_cluster_delete' | 'lke_cluster_recycle' | 'lke_cluster_regenerate' | 'lke_control_plane_acl_create' | 'lke_control_plane_acl_update' | 'lke_control_plane_acl_delete' | 'lke_node_create' | 'lke_node_delete' | 'lke_node_recycle' | 'lke_pool_create' | 'lke_pool_delete' | 'lke_pool_recycle' | 'lke_kubeconfig_regenerate' | 'lke_token_rotate' | 'longviewclient_create' | 'longviewclient_delete' | 'longviewclient_update' | 'managed_disabled' | 'managed_enabled' | 'managed_service_create' | 'managed_service_delete' | 'nodebalancer_create' | 'nodebalancer_delete' | 'nodebalancer_update' | 'nodebalancer_config_create' | 'nodebalancer_config_delete' | 'nodebalancer_config_update' | 'nodebalancer_node_create' | 'nodebalancer_node_delete' | 'nodebalancer_node_update' | 'oauth_client_create' | 'oauth_client_delete' | 'oauth_client_secret_reset' | 'oauth_client_update' | 'obj_access_key_create' | 'obj_access_key_delete' | 'obj_access_key_update' | 'password_reset' | 'payment_method_add' | 'payment_submitted' | 'placement_group_assign' | 'placement_group_became_compliant' | 'placement_group_became_non_compliant' | 'placement_group_create' | 'placement_group_delete' | 'placement_group_unassign' | 'placement_group_update' | 'profile_update' | 'stackscript_create' | 'stackscript_delete' | 'stackscript_update' | 'stackscript_publicize' | 'stackscript_revise' | 'subnet_create' | 'subnet_delete' | 'subnet_update' | 'tag_create' | 'tag_delete' | 'tag_update' | 'tax_id_valid' | 'tax_id_invalid' | 'tfa_disabled' | 'tfa_enabled' | 'ticket_attachment_upload' | 'ticket_create' | 'ticket_update' | 'token_create' | 'token_delete' | 'token_update' | 'user_create' | 'user_update' | 'user_delete' | 'user_ssh_key_add' | 'user_ssh_key_delete' | 'user_ssh_key_update' | 'vlan_attach' | 'vlan_detach' | 'volume_attach' | 'volume_clone' | 'volume_create' | 'volume_delete' | 'volume_update' | 'volume_detach' | 'volume_resize' | 'volume_migrate' | 'volume_migrate_scheduled' | 'vpc_create' | 'vpc_delete' | 'vpc_update';
            /**
             * __Filterable__ When the system created this event.
             */
            created: string;
            /**
             * __Beta__ Maintenance details for this event, if any.
             */
            details: {
                /**
                 * __Filterable__ The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
                 *
                 * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
                 *
                 * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
                 */
                complete_time: string;
                /**
                 * __Filterable__ Differentiates between scheduled and emergency maintenance.
                 */
                description: 'Scheduled Maintenance' | 'Emergency Maintenance';
                /**
                 * __Filterable__ The maintenance policy the user configures for this event.
                 */
                maintenance_policy_set: string;
                /**
                 * __Filterable__ The scheduled start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
                 *
                 * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
                 *
                 * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
                 */
                not_before: string;
                /**
                 * __Filterable__ The origin of the event. A `platform` source indicates that the event was initiated by Akamai. A `user` source indicates that the event was initiated by the user.
                 */
                source: 'platform' | 'user';
                /**
                 * __Filterable__ The actual start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
                 *
                 * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
                 *
                 * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
                 */
                start_time: string;
            };
            /**
             * The number of seconds that it takes for the event to complete.
             */
            duration: number;
            /**
             * Detailed information about the entity that triggered this event.
             */
            entity: {
                /**
                 * __Filterable__ The unique identifier assigned to the entity.
                 *
                 * > 📘
                 * >
                 * > Consider these points when filtering by `id`:
                 * >
                 * > - The `disks` and `backups` entities use the `id` of their parent Linode when filtering for events.
                 * >
                 * > - The `account` and `profile` entities don't have an `id`. To filter these entities, use the `type` object instead.
                 * >
                 * > - The `tag` entity uses the tag's name as its `id`. Set this value to the tag's name to filter.
                 */
                id: number;
                /**
                 * The name of the entity. The label may reflect changes that occur with this event.
                 */
                label: string;
                /**
                 * __Filterable__ The type of entity that is being referenced by the event.
                 */
                type: 'account' | 'backups' | 'community' | 'disks' | 'domain' | 'entity_transfer' | 'firewall' | 'image' | 'ipaddress' | 'linode' | 'longview' | 'loadbalancer' | 'managed_service' | 'nodebalancer' | 'oauth_client' | 'profile' | 'stackscript' | 'tag' | 'ticket' | 'token' | 'user' | 'user_ssh_key' | 'volume';
                /**
                 * The URL where you can access this event's entity. If it's a relative URL, it's relative to the domain where you retrieved the event.
                 */
                url: string;
            };
            /**
             * __Filterable__ The unique ID of this event.
             */
            id: number;
            /**
             * Additional information about the event. This can be a more detailed representation of an event that can help you diagnose non-obvious failures.
             */
            message: string | null;
            /**
             * A percentage estimating the amount of time remaining for an event. Returned as `null` for notification events.
             */
            percent_complete: number | null;
            /**
             * The rate of completion of the event. Only some events return a `rate`, such as the `migration` and `resize` events.
             */
            rate: string | null;
            /**
             * Detailed information about the event's secondary entity, if applicable.
             */
            secondary_entity: {
                /**
                 * The identifier for the secondary entity object.
                 */
                id: string;
                /**
                 * The name of the secondary entity object.
                 */
                label: string;
                /**
                 * The type of secondary entity object that's being referenced by the event.
                 */
                type: string;
                /**
                 * The URL where you can access this event's secondary entity object. If it's a relative URL, it's relative to the domain where you retrieved the event.
                 */
                url: string;
            };
            /**
             * If a user on your account has [marked an event as seen](https://techdocs.akamai.com/linode-api/reference/post-event-seen).
             */
            seen: boolean;
            /**
             * The current status of this event.
             */
            status: 'completed' | 'failed' | 'finished' | 'in_progress' | 'notification' | 'scheduled' | 'started';
            /**
             * The estimated time remaining until the event completes. This is only returned for some in-progress migration events. Otherwise, the `percent_complete` attribute indicates how long until completion.
             */
            time_remaining: string | null;
            /**
             * The name of the user whose action caused the event.
             */
            username: string | null;
        }>;
        /**
         * The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        page: number;
        /**
         * The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        pages: number;
        /**
         * The total number of results.
         */
        results: number;
    };
};

export type GetEventsResponse = GetEventsResponses[keyof GetEventsResponses];

export type GetEventData = {
    body?: never;
    path: {
        /**
         * The ID of the Event.
         */
        eventId: number;
    };
    query?: never;
    url: '/account/events/{eventId}';
};

export type GetEventErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetEventError = GetEventErrors[keyof GetEventErrors];

export type GetEventResponses = {
    /**
     * A specific event object. An event is an action taken against an entity related to your account. For example, if you boot a Linode, the system creates an event. The events returned depend on your user grants.
     */
    200: {
        /**
         * __Read-only__ The action that caused this event. New actions may be added in the future.
         */
        action: 'account_agreement_eu_model' | 'account_promo_apply' | 'account_update' | 'account_settings_update' | 'backups_enable' | 'backups_cancel' | 'backups_restore' | 'community_question_reply' | 'community_like' | 'community_mention' | 'credit_card_updated' | 'database_create' | 'database_delete' | 'database_update' | 'database_failed' | 'database_degraded' | 'database_create_failed' | 'database_update_failed' | 'database_backup_create' | 'database_backup_restore' | 'database_backup_delete' | 'database_credentials_reset' | 'database_low_disk_space' | 'database_scale' | 'database_resize' | 'database_resize_create' | 'database_migrate' | 'database_upgrade' | 'database_suspend' | 'database_resume' | 'disk_create' | 'disk_delete' | 'disk_update' | 'disk_duplicate' | 'disk_imagize' | 'disk_resize' | 'dns_record_create' | 'dns_record_delete' | 'dns_record_update' | 'dns_zone_create' | 'dns_zone_delete' | 'dns_zone_import' | 'dns_zone_update' | 'entity_transfer_accept' | 'entity_transfer_accept_recipient' | 'entity_transfer_cancel' | 'entity_transfer_create' | 'entity_transfer_fail' | 'entity_transfer_stale' | 'firewall_apply' | 'firewall_create' | 'firewall_delete' | 'firewall_disable' | 'firewall_enable' | 'firewall_update' | 'firewall_device_add' | 'firewall_device_remove' | 'firewall_rules_update' | 'host_reboot' | 'image_delete' | 'image_update' | 'image_upload' | 'interface_create' | 'interface_delete' | 'interface_update' | 'ipaddress_update' | 'ipv6pool_add' | 'ipv6pool_delete' | 'lassie_reboot' | 'lish_boot' | 'linode_addip' | 'linode_boot' | 'linode_clone' | 'linode_create' | 'linode_delete' | 'linode_update' | 'linode_deleteip' | 'linode_kvmify' | 'linode_migrate' | 'linode_migrate_datacenter' | 'linode_migrate_datacenter_create' | 'linode_mutate' | 'linode_mutate_create' | 'linode_poweroff_on' | 'linode_reboot' | 'linode_rebuild' | 'linode_resize' | 'linode_resize_create' | 'linode_resize_warm_create' | 'linode_shutdown' | 'linode_snapshot' | 'linode_config_create' | 'linode_config_delete' | 'linode_config_update' | 'lke_control_plane_acl_create' | 'lke_control_plane_acl_update' | 'lke_control_plane_acl_delete' | 'lke_cluster_create' | 'lke_cluster_update' | 'lke_cluster_delete' | 'lke_cluster_recycle' | 'lke_cluster_regenerate' | 'lke_node_create' | 'lke_node_delete' | 'lke_node_recycle' | 'lke_pool_create' | 'lke_pool_delete' | 'lke_pool_recycle' | 'lke_kubeconfig_regenerate' | 'lke_token_rotate' | 'longviewclient_create' | 'longviewclient_delete' | 'longviewclient_update' | 'managed_disabled' | 'managed_enabled' | 'managed_service_create' | 'managed_service_delete' | 'nodebalancer_create' | 'nodebalancer_delete' | 'nodebalancer_update' | 'nodebalancer_config_create' | 'nodebalancer_config_delete' | 'nodebalancer_config_update' | 'nodebalancer_node_create' | 'nodebalancer_node_delete' | 'nodebalancer_node_update' | 'oauth_client_create' | 'oauth_client_delete' | 'oauth_client_secret_reset' | 'oauth_client_update' | 'obj_access_key_create' | 'obj_access_key_delete' | 'obj_access_key_update' | 'password_reset' | 'payment_method_add' | 'payment_submitted' | 'placement_group_assign' | 'placement_group_became_compliant' | 'placement_group_became_non_compliant' | 'placement_group_create' | 'placement_group_delete' | 'placement_group_unassign' | 'placement_group_update' | 'profile_update' | 'stackscript_create' | 'stackscript_delete' | 'stackscript_update' | 'stackscript_publicize' | 'stackscript_revise' | 'subnet_create' | 'subnet_delete' | 'subnet_update' | 'tag_create' | 'tag_delete' | 'tag_update' | 'tax_id_valid' | 'tax_id_invalid' | 'tfa_disabled' | 'tfa_enabled' | 'ticket_attachment_upload' | 'ticket_create' | 'ticket_update' | 'token_create' | 'token_delete' | 'token_update' | 'user_create' | 'user_update' | 'user_delete' | 'user_ssh_key_add' | 'user_ssh_key_delete' | 'user_ssh_key_update' | 'vlan_attach' | 'vlan_detach' | 'volume_attach' | 'volume_clone' | 'volume_create' | 'volume_delete' | 'volume_update' | 'volume_detach' | 'volume_resize' | 'volume_migrate' | 'volume_migrate_scheduled' | 'vpc_create' | 'vpc_delete' | 'vpc_update';
        /**
         * __Read-only__ When the system created this event.
         */
        readonly created: string;
        /**
         * __Beta__, __Read-only__ Maintenance details for this event, if any.
         */
        readonly details: {
            /**
             * The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            complete_time: string;
            /**
             * Differentiates between scheduled and emergency maintenance.
             */
            description: 'Scheduled Maintenance' | 'Emergency Maintenance';
            /**
             * The maintenance policy the user configures for this event.
             */
            maintenance_policy_set: string;
            /**
             * The scheduled start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            not_before: string;
            /**
             * The origin of the event. A `platform` source indicates that the event was initiated by Akamai. A `user` source indicates that the event was initiated by the user.
             */
            source: 'platform' | 'user';
            /**
             * The actual start time for the event. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and individual ISO 8601 timestamps. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            start_time: string;
        };
        /**
         * __Read-only__ The number of seconds that it takes for the event to complete.
         */
        readonly duration: number;
        /**
         * __Read-only__ Detailed information about the entity that triggered this event.
         */
        readonly entity: {
            /**
             * The unique identifier assigned to the entity.
             */
            id: number;
            /**
             * The name of the entity. The label may reflect changes that occur with this event.
             */
            label: string;
            /**
             * __Read-only__ The type of entity that is being referenced by the event.
             */
            type: 'account' | 'backups' | 'community' | 'disks' | 'domain' | 'entity_transfer' | 'firewall' | 'image' | 'ipaddress' | 'linode' | 'longview' | 'loadbalancer' | 'managed_service' | 'nodebalancer' | 'oauth_client' | 'profile' | 'stackscript' | 'tag' | 'ticket' | 'token' | 'user' | 'user_ssh_key' | 'volume';
            /**
             * The URL where you can access this event's entity. If it's a relative URL, it's relative to the domain where you retrieved the event.
             */
            url: string;
        };
        /**
         * __Read-only__ The unique ID of this event.
         */
        readonly id: number;
        /**
         * Additional information about the event. This can be a more detailed representation of an event that can help you diagnose non-obvious failures.
         */
        message: string | null;
        /**
         * __Read-only__ A percentage estimating the amount of time remaining for an event. Returned as `null` for notification events.
         */
        readonly percent_complete: number | null;
        /**
         * __Read-only__ The rate of completion of the event. Only some events return a `rate`, such as the `migration` and `resize` events.
         */
        readonly rate: string | null;
        /**
         * __Read-only__ Detailed information about the event's secondary entity, if applicable.
         */
        readonly secondary_entity: {
            /**
             * The identifier for the secondary entity object.
             */
            id: string;
            /**
             * The name of the secondary entity object.
             */
            label: string;
            /**
             * __Read-only__ The type of secondary entity object that's being referenced by the event.
             */
            readonly type: string;
            /**
             * The URL where you can access this event's secondary entity object. If it's a relative URL, it's relative to the domain where you retrieved the event.
             */
            url: string;
        };
        /**
         * __Read-only__ If a user on your account has [marked an event as seen](https://techdocs.akamai.com/linode-api/reference/post-event-seen).
         */
        readonly seen: boolean;
        /**
         * __Read-only__ The current status of this event.
         */
        status: 'failed' | 'finished' | 'notification' | 'scheduled' | 'started';
        /**
         * __Read-only__ The estimated time remaining until the event completes. This is only returned for some in-progress migration events. Otherwise, `percent_complete` indicates how long until completion.
         */
        readonly time_remaining: string | null;
        /**
         * __Read-only__ The name of the user whose action caused the event.
         */
        readonly username: string | null;
    };
};

export type GetEventResponse = GetEventResponses[keyof GetEventResponses];

export type PostEventSeenData = {
    body?: never;
    path: {
        /**
         * The ID of the Event to designate as seen.
         */
        eventId: number;
    };
    query?: never;
    url: '/account/events/{eventId}/seen';
};

export type PostEventSeenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostEventSeenError = PostEventSeenErrors[keyof PostEventSeenErrors];

export type PostEventSeenResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostEventSeenResponse = PostEventSeenResponses[keyof PostEventSeenResponses];

export type GetInvoicesData = {
    body?: never;
    headers?: {
        /**
         * Specifies the `X-Filter` header JSON object's filtering and sort criteria.
         */
        'X-Filter'?: {
            [key: string]: unknown;
        } | {
            /**
             * All conditions need to be true.
             */
            '+and': Array<{
                [key: string]: unknown;
            }>;
            /**
             * The provided string needs to be in the value.
             */
            '+contains': string;
            /**
             * The value needs to be greater than the provided number.
             */
            '+gt': number;
            /**
             * The value needs to be greater than or equal to the provided number.
             */
            '+gte': number;
            /**
             * The value needs to be less than the provided number.
             */
            '+lt': number;
            /**
             * The value needs to be less than or equal to the provided number.
             */
            '+lte': number;
            /**
             * The provided string is left out of the results.
             */
            '+neq': string;
            /**
             * At least one condition needs to be true.
             */
            '+or': Array<{
                [key: string]: unknown;
            }>;
            /**
             * Sort in ascending (`asc`) or descending (`desc`) order. This defaults to `asc`. Requires `+order_by`.
             */
            '+order': 'asc' | 'desc';
            /**
             * Order results based on the provided attribute. The attribute needs to be filterable.
             */
            '+order_by': string;
        };
    };
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/invoices';
};

export type GetInvoicesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetInvoicesError = GetInvoicesErrors[keyof GetInvoicesErrors];

export type GetInvoicesResponses = {
    /**
     * Returns a paginated list of Invoice objects.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ The source of service charges for this invoice. A value of `akamai` indicates an invoice generated according to the terms of an agreement between the customer and Akamai. A value of `linode` indicates an invoice was generated according to the default terms, prices, and discounts.
             */
            billing_source: 'akamai' | 'linode';
            /**
             * __Filterable__, __Read-only__ When this Invoice was generated.
             */
            readonly date: string;
            /**
             * __Read-only__ The Invoice's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__, __Read-only__ The Invoice's display label.
             */
            readonly label: string;
            /**
             * __Read-only__ The amount of the Invoice before taxes in US Dollars.
             */
            readonly subtotal: number;
            /**
             * __Read-only__ The amount of tax levied on the Invoice in US Dollars.
             */
            readonly tax: number;
            /**
             * __Read-only__ The amount of tax broken down into subtotals by source.
             */
            readonly tax_summary: Array<{
                /**
                 * The source of this tax subtotal.
                 */
                name: string;
                /**
                 * The amount of tax subtotal attributable to this source.
                 */
                tax: number;
            }>;
            /**
             * __Filterable__, __Read-only__ The amount of the Invoice after taxes in US Dollars.
             */
            readonly total: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetInvoicesResponse = GetInvoicesResponses[keyof GetInvoicesResponses];

export type GetInvoiceData = {
    body?: never;
    path: {
        /**
         * The ID of the Invoice.
         */
        invoiceId: number;
    };
    query?: never;
    url: '/account/invoices/{invoiceId}';
};

export type GetInvoiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetInvoiceError = GetInvoiceErrors[keyof GetInvoiceErrors];

export type GetInvoiceResponses = {
    /**
     * Account Invoice object.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The source of service charges for this invoice. A value of `akamai` indicates an invoice generated according to the terms of an agreement between the customer and Akamai. A value of `linode` indicates an invoice was generated according to the default terms, prices, and discounts.
         */
        billing_source: 'akamai' | 'linode';
        /**
         * __Filterable__, __Read-only__ When this Invoice was generated.
         */
        readonly date: string;
        /**
         * __Read-only__ The Invoice's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ The Invoice's display label.
         */
        readonly label: string;
        /**
         * __Read-only__ The amount of the Invoice before taxes in US Dollars.
         */
        readonly subtotal: number;
        /**
         * __Read-only__ The amount of tax levied on the Invoice in US Dollars.
         */
        readonly tax: number;
        /**
         * __Read-only__ The amount of tax broken down into subtotals by source.
         */
        readonly tax_summary: Array<{
            /**
             * The source of this tax subtotal.
             */
            name: string;
            /**
             * The amount of tax subtotal attributable to this source.
             */
            tax: number;
        }>;
        /**
         * __Filterable__, __Read-only__ The amount of the Invoice after taxes in US Dollars.
         */
        readonly total: number;
    };
};

export type GetInvoiceResponse = GetInvoiceResponses[keyof GetInvoiceResponses];

export type GetInvoiceItemsData = {
    body?: never;
    path: {
        /**
         * The ID of the Invoice.
         */
        invoiceId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/invoices/{invoiceId}/items';
};

export type GetInvoiceItemsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetInvoiceItemsError = GetInvoiceItemsErrors[keyof GetInvoiceItemsErrors];

export type GetInvoiceItemsResponses = {
    /**
     * A paginated list of InvoiceItem objects.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The price, in US dollars, of the Invoice Item. Equal to the unit price multiplied by quantity.
             */
            readonly amount: number;
            /**
             * __Read-only__ The date the Invoice Item started, based on month.
             */
            readonly from: string;
            /**
             * __Read-only__ The Invoice Item's display label.
             */
            readonly label: string;
            /**
             * __Read-only__ The quantity of this Item for the specified Invoice.
             */
            readonly quantity: number;
            /**
             * __Read-only__ The ID of the applicable Region associated with this Invoice Item.
             *
             * `null` if there is no applicable Region.
             */
            readonly region: string | null;
            /**
             * __Read-only__ The amount of tax levied on this Item in US Dollars.
             */
            readonly tax: number;
            /**
             * __Read-only__ The date the Invoice Item ended, based on month.
             */
            readonly to: string;
            /**
             * __Read-only__ The price of this Item after taxes in US Dollars.
             */
            readonly total: number;
            /**
             * __Read-only__ The type of service, ether `hourly` or `misc`.
             */
            type: 'hourly' | 'misc';
            /**
             * __Read-only__ The monthly service fee in US Dollars for this Item.
             */
            readonly unit_price: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetInvoiceItemsResponse = GetInvoiceItemsResponses[keyof GetInvoiceItemsResponses];

export type GetAccountLoginsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/logins';
};

export type GetAccountLoginsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAccountLoginsError = GetAccountLoginsErrors[keyof GetAccountLoginsErrors];

export type GetAccountLoginsResponses = {
    /**
     * A collection of successful logins for all users on the account during the last 90 days.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When the login was initiated.
             */
            readonly datetime: string;
            /**
             * __Read-only__ The unique ID of this login object.
             */
            readonly id: number;
            /**
             * __Read-only__ The remote IP address that requested the login.
             */
            readonly ip: string;
            /**
             * __Read-only__ True if the User that attempted the login was a restricted User, false otherwise.
             */
            readonly restricted: boolean;
            /**
             * __Read-only__ Whether the login attempt succeeded or failed.
             */
            status: 'successful' | 'failed';
            /**
             * __Read-only__ The username of the User that attempted the login.
             */
            readonly username: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetAccountLoginsResponse = GetAccountLoginsResponses[keyof GetAccountLoginsResponses];

export type GetAccountLoginData = {
    body?: never;
    path: {
        /**
         * The ID of the login object to access.
         */
        loginId: number;
    };
    query?: never;
    url: '/account/logins/{loginId}';
};

export type GetAccountLoginErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAccountLoginError = GetAccountLoginErrors[keyof GetAccountLoginErrors];

export type GetAccountLoginResponses = {
    /**
     * An object representing a previous successful login for a User.
     */
    200: {
        /**
         * __Read-only__ When the login was initiated.
         */
        readonly datetime: string;
        /**
         * __Read-only__ The unique ID of this login object.
         */
        readonly id: number;
        /**
         * __Read-only__ The remote IP address that requested the login.
         */
        readonly ip: string;
        /**
         * __Read-only__ True if the User that attempted the login was a restricted User, false otherwise.
         */
        readonly restricted: boolean;
        /**
         * __Read-only__ Whether the login attempt succeeded or failed.
         */
        status: 'successful' | 'failed';
        /**
         * __Read-only__ The username of the User that attempted the login.
         */
        readonly username: string;
    };
};

export type GetAccountLoginResponse = GetAccountLoginResponses[keyof GetAccountLoginResponses];

export type GetMaintenanceData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/maintenance';
};

export type GetMaintenanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetMaintenanceError = GetMaintenanceErrors[keyof GetMaintenanceErrors];

export type GetMaintenanceResponses = {
    /**
     * Returns a paginated list of Maintenance objects.
     */
    200: {
        data: Array<{
            /**
             * __Beta__, __Filterable__ The time the maintenance completed. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            complete_time: string;
            /**
             * __Beta__ Differentiates between scheduled and emergency maintenance.
             */
            description: 'Scheduled Maintenance' | 'Emergency Maintenance';
            /**
             * The entity affected by the maintenance.
             */
            entity: {
                /**
                 * The unique identifier of the entity targeted by the maintenance.
                 */
                id: number;
                /**
                 * The name of the entity targeted by the maintenance.
                 */
                label: string;
                /**
                 * The type of entity.
                 */
                type: string;
                /**
                 * A combination of the API operation prefix and the entity's `id` that can be used to review the entity.
                 */
                url: string;
            };
            /**
             * __Beta__, __Filterable__ The maintenance policy configured by the user.
             */
            maintenance_policy_set: string;
            /**
             * __Beta__, __Filterable__ The earliest time when the maintenance can start. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            not_before: string;
            /**
             * The reason maintenance is being performed.
             */
            reason: string;
            /**
             * __Beta__ The origin of the maintenance. A `platform` source indicates that the maintenance was initiated by Akamai. A `user` source indicates that the maintenance was initiated by the user.
             */
            source: 'platform' | 'user';
            /**
             * __Beta__, __Filterable__ The time the maintenance started. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (`yyyy-mm-ddThh:mm:ss`), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            start_time: string;
            /**
             * __Filterable__ The maintenance status. Maintenance progress follows the sequence `pending`, `scheduled`, `started`, and `completed`. A `scheduled` status is unique to Linodes that require a reboot for [QEMU maintenance](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance).
             */
            status: 'pending' | 'scheduled' | 'started' | 'in-progress' | 'completed' | 'canceled';
            /**
             * __Filterable__ The type of maintenance.
             */
            type: 'reboot' | 'cold_migration' | 'live_migration' | 'migrate' | 'power_off_on';
            /**
             * __Filterable__ The start time for the maintenance. This field is [filterable](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) based on these parameters:
             *
             * - A single ISO 8601 timestamp (yyyy-mm-ddThh:mm:ss), which returns only matches for that value.
             *
             * - Pairs of operator string keys (`+or`, `+gt`, `+gte`, `+lt`, `+lte`, or `+neq`) and single ISO 8601 timestamp. The `+or` operator accepts an array of values that can consist of single date-time strings or dictionaries of inequality operator pairs.
             */
            when: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetMaintenanceResponse = GetMaintenanceResponses[keyof GetMaintenanceResponses];

export type GetNotificationsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/notifications';
};

export type GetNotificationsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNotificationsError = GetNotificationsErrors[keyof GetNotificationsErrors];

export type GetNotificationsResponses = {
    /**
     * Returns a paginated list of notification objects.
     */
    200: {
        data: Array<{
            /**
             * A full description of this notification, in markdown format. Not all notifications include a `body`. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
             */
            body: string | null;
            /**
             * Detailed information about the notification. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
             */
            entity: {
                /**
                 * The unique ID of the notification's entity, based on the entity type. Returns `null` for an `account` or `promotion` entity.
                 */
                id: number | null;
                /**
                 * The current name of this notification's entity. Returns `null` for the following `entity` types:
                 *
                 * - `entity_transfer`
                 *
                 * - `promotion`
                 *
                 * - `region`
                 */
                label: string | null;
                /**
                 * __Filterable__ The type of entity this is related to. An entity can be product or service-specific, such as a `linode`, `loadbalancers`, or `nodebalancers`. It can apply to a specific component, such as your `account`, a specific `promotion` your participating in, a data center (`region`) where you're using services, a transfer from one component to another (an `entity_transfer`), a support `ticket` you've opened, or a `volume` on a specific Linode.
                 */
                type: 'account' | 'entity_transfer' | 'linode' | 'loadbalancers' | 'nodebalancer' | 'promotion' | 'region' | 'ticket' | 'volume';
                /**
                 * The URL where you can access the notification's object. The URL is relative to the domain where you retrieved the notification. This value is `null` for the `promotion` entity type.
                 */
                url: string | null;
            } | null;
            /**
             * A short description of this notification.
             */
            label: string;
            /**
             * A human-readable description of the notification.
             */
            message: string;
            /**
             * The severity of this notification. This field determines how prominently the notification is displayed and the color of the display text.
             */
            severity: 'minor' | 'major' | 'critical';
            /**
             * __Filterable__ The type of notification.
             *
             * > 📘
             * >
             * > A `security_reboot_maintenance_scheduled` event is a global notice that a Linode needs to be rebooted for QEMU upgrade maintenance. Have a look at [this workflow](https://techdocs.akamai.com/linode-api/reference/reboot-your-linodes-for-qemu-maintenance) for guidance on reboooting your Linodes for this maintenance.
             */
            type: 'migration_scheduled' | 'migration_imminent' | 'migration_pending' | 'reboot_scheduled' | 'outage' | 'payment_due' | 'ticket_important' | 'ticket_abuse' | 'notice' | 'maintenance' | 'maintenance_scheduled' | 'promotion' | 'security_reboot_maintenance_scheduled' | 'tax_id_verifying';
            /**
             * If this notification has a duration, this is when the event or action will complete. For example, if there's scheduled maintenance for one of our systems, `until` represents the end of the maintenance window. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
             */
            until: string | null;
            /**
             * If this notification is for an event in the future, this specifies when the action occurs. For example, if a compute instance needs to migrate in response to a security advisory, this field sets the approximate time the compute instance will be taken offline for migration. Returned as `null` for an event `type` of `security_reboot_maintenance_scheduled`.
             */
            when: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetNotificationsResponse = GetNotificationsResponses[keyof GetNotificationsResponses];

export type GetClientsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/oauth-clients';
};

export type GetClientsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetClientsError = GetClientsErrors[keyof GetClientsErrors];

export type GetClientsResponses = {
    /**
     * A paginated list of OAuth Clients.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
             */
            readonly id: string;
            /**
             * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
             */
            label: string;
            /**
             * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
             */
            public: boolean;
            /**
             * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
             */
            redirect_uri: string;
            /**
             * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
             */
            readonly secret: string;
            /**
             * __Read-only__ The status of this application.  `active` by default.
             */
            status: 'active' | 'disabled' | 'suspended';
            /**
             * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
             */
            readonly thumbnail_url: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetClientsResponse = GetClientsResponses[keyof GetClientsResponses];

export type PostClientData = {
    /**
     * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
     */
    body?: {
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    };
    path?: never;
    query?: never;
    url: '/account/oauth-clients';
};

export type PostClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostClientError = PostClientErrors[keyof PostClientErrors];

export type PostClientResponses = {
    /**
     * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
     */
    200: {
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    };
};

export type PostClientResponse = PostClientResponses[keyof PostClientResponses];

export type DeleteClientData = {
    body?: never;
    path: {
        /**
         * The OAuth Client ID to look up.
         */
        clientId: string;
    };
    query?: never;
    url: '/account/oauth-clients/{clientId}';
};

export type DeleteClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteClientError = DeleteClientErrors[keyof DeleteClientErrors];

export type DeleteClientResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteClientResponse = DeleteClientResponses[keyof DeleteClientResponses];

export type GetClientData = {
    body?: never;
    path: {
        /**
         * The OAuth Client ID to look up.
         */
        clientId: string;
    };
    query?: never;
    url: '/account/oauth-clients/{clientId}';
};

export type GetClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetClientError = GetClientErrors[keyof GetClientErrors];

export type GetClientResponses = {
    /**
     * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
     */
    200: {
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    };
};

export type GetClientResponse = GetClientResponses[keyof GetClientResponses];

export type PutClientData = {
    /**
     * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
     */
    body?: {
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    };
    path: {
        /**
         * The OAuth Client ID to look up.
         */
        clientId: string;
    };
    query?: never;
    url: '/account/oauth-clients/{clientId}';
};

export type PutClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutClientError = PutClientErrors[keyof PutClientErrors];

export type PutClientResponses = {
    /**
     * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
     */
    200: {
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    };
};

export type PutClientResponse = PutClientResponses[keyof PutClientResponses];

export type PostResetClientSecretData = {
    body?: never;
    path: {
        /**
         * The OAuth Client ID to look up.
         */
        clientId: string;
    };
    query?: never;
    url: '/account/oauth-clients/{clientId}/reset-secret';
};

export type PostResetClientSecretErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostResetClientSecretError = PostResetClientSecretErrors[keyof PostResetClientSecretErrors];

export type PostResetClientSecretResponses = {
    /**
     * A third-party application registered to Linode that users may log into with their Linode account through our authentication server at [login.linode.com](https://login.linode.com).  Using an OAuth Client, a third-party developer may be given access to some, or all, of a User's account for the purposes of their application.
     */
    200: {
        /**
         * __Read-only__ The OAuth Client ID.  This is used to identify the client, and is a publicly known value (it is not a secret).
         */
        readonly id: string;
        /**
         * __Filterable__ The name of this application.  This will be presented to users when they are asked to grant it access to their Account.
         */
        label: string;
        /**
         * __Filterable__ If this is a public or private OAuth Client.  Public clients have a slightly different authentication workflow than private clients.  See the [OAuth spec](https://oauth.net/2/) for more details.
         */
        public: boolean;
        /**
         * The location a successful log in from [login.linode.com](https://login.linode.com) should be redirected to for this client.  The receiver of this redirect should be ready to accept an OAuth exchange code and finish the OAuth exchange.
         */
        redirect_uri: string;
        /**
         * __Read-only__ The OAuth Client secret, used in the OAuth exchange.  This is returned as `<REDACTED>` except when an OAuth Client is created or its secret is reset.  This is a secret, and should not be shared or disclosed publicly.
         */
        readonly secret: string;
        /**
         * __Read-only__ The status of this application.  `active` by default.
         */
        status: 'active' | 'disabled' | 'suspended';
        /**
         * __Read-only__ The URL where this client's thumbnail may be viewed, or `null` if this client does not have a thumbnail set.
         */
        readonly thumbnail_url: string | null;
    };
};

export type PostResetClientSecretResponse = PostResetClientSecretResponses[keyof PostResetClientSecretResponses];

export type GetClientThumbnailData = {
    body?: never;
    path: {
        /**
         * The OAuth Client ID to look up.
         */
        clientId: string;
    };
    query?: never;
    url: '/account/oauth-clients/{clientId}/thumbnail';
};

export type GetClientThumbnailErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetClientThumbnailError = GetClientThumbnailErrors[keyof GetClientThumbnailErrors];

export type GetClientThumbnailResponses = {
    /**
     * The client's thumbnail.
     */
    200: Blob | File;
};

export type GetClientThumbnailResponse = GetClientThumbnailResponses[keyof GetClientThumbnailResponses];

export type PutClientThumbnailData = {
    /**
     * The image to set as the thumbnail.
     */
    body: Blob | File;
    path: {
        /**
         * The OAuth Client ID to look up.
         */
        clientId: string;
    };
    query?: never;
    url: '/account/oauth-clients/{clientId}/thumbnail';
};

export type PutClientThumbnailErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutClientThumbnailError = PutClientThumbnailErrors[keyof PutClientThumbnailErrors];

export type PutClientThumbnailResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PutClientThumbnailResponse = PutClientThumbnailResponses[keyof PutClientThumbnailResponses];

export type GetPaymentMethodsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/payment-methods';
};

export type GetPaymentMethodsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPaymentMethodsError = GetPaymentMethodsErrors[keyof GetPaymentMethodsErrors];

export type GetPaymentMethodsResponses = {
    /**
     * Returns a paginated list of Payment Method objects.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When the Payment Method was added to the Account.
             */
            readonly created: string;
            data: {
                /**
                 * __Read-only__ The type of credit card.
                 */
                readonly card_type: string;
                /**
                 * __Read-only__ The expiration month and year of the credit card.
                 */
                readonly expiry: string;
                /**
                 * __Read-only__ The last four digits of the credit card number.
                 */
                readonly last_four: string;
            } | {
                /**
                 * __Read-only__ The type of credit card.
                 */
                readonly card_type: string;
                /**
                 * __Read-only__ The expiration month and year of the credit card.
                 */
                readonly expiry: string;
                /**
                 * __Read-only__ The last four digits of the credit card number.
                 */
                readonly last_four: string;
            } | {
                /**
                 * __Read-only__ The email address associated with your PayPal account.
                 */
                readonly email: string;
                /**
                 * __Read-only__ PayPal Merchant ID associated with your PayPal account.
                 */
                readonly paypal_id: string;
            };
            /**
             * The unique ID of this Payment Method.
             */
            id: number;
            /**
             * Whether this Payment Method is the default method for automatically processing service charges.
             */
            is_default: boolean;
            /**
             * The type of Payment Method.
             */
            type: 'credit_card' | 'google_pay' | 'paypal';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetPaymentMethodsResponse = GetPaymentMethodsResponses[keyof GetPaymentMethodsResponses];

export type PostPaymentMethodData = {
    /**
     * Payment Method Request Object.
     */
    body: {
        /**
         * An object representing the credit card information you have on file with Linode to make Payments against your Account.
         */
        data: {
            /**
             * Your credit card number. No spaces or hyphens (`-`) allowed.
             */
            card_number: string;
            /**
             * CVV (Card Verification Value) of the credit card, typically found on the back of the card.
             */
            cvv: string;
            /**
             * A value from 1-12 representing the expiration month of your credit card.
             *
             * - 1 = January
             * - 2 = February
             * - 3 = March
             * - Etc.
             */
            expiry_month: number;
            /**
             * A four-digit integer representing the expiration year of your credit card.
             *
             * The combination of `expiry_month` and `expiry_year` must result in a month/year combination of the current month or in the future. An expiration date set in the past is invalid.
             */
            expiry_year: number;
        };
        /**
         * Whether this Payment Method is the default method for automatically processing service charges.
         */
        is_default: boolean;
        /**
         * The type of Payment Method.
         *
         * Alternative Payment Methods including Google Pay and PayPal can be added using the Cloud Manager. See the [Manage Payment Methods](https://www.linode.com/docs/products/platform/billing/guides/payment-methods/) guide
         * for details and instructions.
         */
        type: 'credit_card';
    };
    path?: never;
    query?: never;
    url: '/account/payment-methods';
};

export type PostPaymentMethodErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostPaymentMethodError = PostPaymentMethodErrors[keyof PostPaymentMethodErrors];

export type PostPaymentMethodResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostPaymentMethodResponse = PostPaymentMethodResponses[keyof PostPaymentMethodResponses];

export type DeletePaymentMethodData = {
    body?: never;
    path: {
        /**
         * The ID of the Payment Method to look up.
         */
        paymentMethodId: number;
    };
    query?: never;
    url: '/account/payment-methods/{paymentMethodId}';
};

export type DeletePaymentMethodErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeletePaymentMethodError = DeletePaymentMethodErrors[keyof DeletePaymentMethodErrors];

export type DeletePaymentMethodResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeletePaymentMethodResponse = DeletePaymentMethodResponses[keyof DeletePaymentMethodResponses];

export type GetPaymentMethodData = {
    body?: never;
    path: {
        /**
         * The ID of the Payment Method to look up.
         */
        paymentMethodId: number;
    };
    query?: never;
    url: '/account/payment-methods/{paymentMethodId}';
};

export type GetPaymentMethodErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPaymentMethodError = GetPaymentMethodErrors[keyof GetPaymentMethodErrors];

export type GetPaymentMethodResponses = {
    /**
     * Payment Method Response Object.
     */
    200: {
        /**
         * __Read-only__ When the Payment Method was added to the Account.
         */
        readonly created: string;
        data: {
            /**
             * __Read-only__ The type of credit card.
             */
            readonly card_type: string;
            /**
             * __Read-only__ The expiration month and year of the credit card.
             */
            readonly expiry: string;
            /**
             * __Read-only__ The last four digits of the credit card number.
             */
            readonly last_four: string;
        } | {
            /**
             * __Read-only__ The type of credit card.
             */
            readonly card_type: string;
            /**
             * __Read-only__ The expiration month and year of the credit card.
             */
            readonly expiry: string;
            /**
             * __Read-only__ The last four digits of the credit card number.
             */
            readonly last_four: string;
        } | {
            /**
             * __Read-only__ The email address associated with your PayPal account.
             */
            readonly email: string;
            /**
             * __Read-only__ PayPal Merchant ID associated with your PayPal account.
             */
            readonly paypal_id: string;
        };
        /**
         * The unique ID of this Payment Method.
         */
        id: number;
        /**
         * Whether this Payment Method is the default method for automatically processing service charges.
         */
        is_default: boolean;
        /**
         * The type of Payment Method.
         */
        type: 'credit_card' | 'google_pay' | 'paypal';
    };
};

export type GetPaymentMethodResponse = GetPaymentMethodResponses[keyof GetPaymentMethodResponses];

export type PostMakePaymentMethodDefaultData = {
    body?: never;
    path: {
        /**
         * The ID of the Payment Method to make default.
         */
        paymentMethodId: number;
    };
    query?: never;
    url: '/account/payment-methods/{paymentMethodId}/make-default';
};

export type PostMakePaymentMethodDefaultErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostMakePaymentMethodDefaultError = PostMakePaymentMethodDefaultErrors[keyof PostMakePaymentMethodDefaultErrors];

export type PostMakePaymentMethodDefaultResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostMakePaymentMethodDefaultResponse = PostMakePaymentMethodDefaultResponses[keyof PostMakePaymentMethodDefaultResponses];

export type GetPaymentsData = {
    body?: never;
    headers?: {
        /**
         * Specifies the `X-Filter` header JSON object's filtering and sort criteria.
         */
        'X-Filter'?: {
            [key: string]: unknown;
        } | {
            /**
             * All conditions need to be true.
             */
            '+and': Array<{
                [key: string]: unknown;
            }>;
            /**
             * The provided string needs to be in the value.
             */
            '+contains': string;
            /**
             * The value needs to be greater than the provided number.
             */
            '+gt': number;
            /**
             * The value needs to be greater than or equal to the provided number.
             */
            '+gte': number;
            /**
             * The value needs to be less than the provided number.
             */
            '+lt': number;
            /**
             * The value needs to be less than or equal to the provided number.
             */
            '+lte': number;
            /**
             * The provided string is left out of the results.
             */
            '+neq': string;
            /**
             * At least one condition needs to be true.
             */
            '+or': Array<{
                [key: string]: unknown;
            }>;
            /**
             * Sort in ascending (`asc`) or descending (`desc`) order. This defaults to `asc`. Requires `+order_by`.
             */
            '+order': 'asc' | 'desc';
            /**
             * Order results based on the provided attribute. The attribute needs to be filterable.
             */
            '+order_by': string;
        };
    };
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/payments';
};

export type GetPaymentsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPaymentsError = GetPaymentsErrors[keyof GetPaymentsErrors];

export type GetPaymentsResponses = {
    /**
     * Returns a paginated list of payment objects.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When the payment was made.
             */
            readonly date: string;
            /**
             * __Read-only__ The unique ID of the payment.
             */
            readonly id: number;
            /**
             * __Filterable__, __Read-only__ The amount, in US dollars, of the payment.
             */
            readonly usd: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetPaymentsResponse = GetPaymentsResponses[keyof GetPaymentsResponses];

export type PostPaymentData = {
    /**
     * Information about the payment you're making.
     */
    body: {
        /**
         * The ID of the Payment Method to apply to the Payment.
         */
        payment_method_id: number;
        /**
         * The amount in US Dollars of the Payment.
         *
         * - Can begin with or without `$`.
         * - Commas (`,`) are not accepted.
         * - Must end with a decimal expression, such as `.00` or `.99`.
         * - Minimum: `$5.00` or the Account balance, whichever is lower.
         * - Maximum: `$2000.00` or the Account balance up to `$50000.00`, whichever is greater.
         */
        usd: string;
    };
    path?: never;
    query?: never;
    url: '/account/payments';
};

export type PostPaymentErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostPaymentError = PostPaymentErrors[keyof PostPaymentErrors];

export type PostPaymentResponses = {
    /**
     * Payment object response.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When the payment was made.
         */
        readonly date: string;
        /**
         * __Read-only__ The unique ID of the payment.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ The amount, in US dollars, of the payment.
         */
        readonly usd: number;
    };
    /**
     * Accepted with warning.
     *
     * A warnings array is included with the standard 200 response body.
     */
    202: {
        warnings: Array<{
            /**
             * Specific information related to the warning.
             */
            details: string;
            /**
             * The general warning message.
             */
            title: string;
        }>;
    };
};

export type PostPaymentResponse = PostPaymentResponses[keyof PostPaymentResponses];

export type PostPayPalPaymentData = {
    /**
     * An object representing the staging of a Payment via PayPal.
     */
    body: {
        /**
         * The URL to have PayPal redirect to when Payment is canceled.
         */
        cancel_url: string;
        /**
         * The URL to have PayPal redirect to when Payment is approved.
         */
        redirect_url: string;
        /**
         * The payment amount in USD. Minimum accepted value of $5 USD. Maximum accepted value of $500 USD or credit card payment limit; whichever value is highest. PayPal's maximum transaction limit is $10,000 USD.
         */
        usd: string;
    };
    path?: never;
    query?: never;
    url: '/account/payments/paypal';
};

export type PostPayPalPaymentErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostPayPalPaymentError = PostPayPalPaymentErrors[keyof PostPayPalPaymentErrors];

export type PostPayPalPaymentResponses = {
    /**
     * PayPal Payment staged.
     */
    200: {
        /**
         * __Read-only__ The checkout token generated for this Payment.
         */
        readonly checkout_token: string;
        /**
         * The paypal-generated ID for this Payment. Used when authorizing the Payment in PayPal's interface.
         */
        payment_id: string;
    };
    /**
     * Request successful. This operation is deprecated and may be removed in a future release.
     *
     * A warnings array is included with the standard 200 response body.
     */
    299: {
        warnings: Array<{
            /**
             * Specific information related to the warning.
             */
            details: string;
            /**
             * The general warning message.
             */
            title: string;
        }>;
    };
};

export type PostPayPalPaymentResponse = PostPayPalPaymentResponses[keyof PostPayPalPaymentResponses];

export type PostExecutePayPalPaymentData = {
    /**
     * An object representing an execution of Payment to PayPal to capture the funds and credit your Linode Account.
     */
    body: {
        /**
         * The PayerID returned by PayPal during the transaction authorization process.
         */
        payer_id: string;
        /**
         * The PaymentID returned from [Stage a PayPal payment](https://techdocs.akamai.com/linode-api/reference/post-pay-pal-payment) that has been approved with PayPal.
         */
        payment_id: string;
    };
    path?: never;
    query?: never;
    url: '/account/payments/paypal/execute';
};

export type PostExecutePayPalPaymentErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostExecutePayPalPaymentError = PostExecutePayPalPaymentErrors[keyof PostExecutePayPalPaymentErrors];

export type PostExecutePayPalPaymentResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
    /**
     * Accepted with warning.
     *
     * A warnings array is included with the standard 200 response body.
     */
    202: {
        warnings: Array<{
            /**
             * Specific information related to the warning.
             */
            details: string;
            /**
             * The general warning message.
             */
            title: string;
        }>;
    };
    /**
     * Request successful. This operation is deprecated and may be removed in a future release.
     *
     * A warnings array is included with the standard 200 response body.
     */
    299: {
        warnings: Array<{
            /**
             * Specific information related to the warning.
             */
            details: string;
            /**
             * The general warning message.
             */
            title: string;
        }>;
    };
};

export type PostExecutePayPalPaymentResponse = PostExecutePayPalPaymentResponses[keyof PostExecutePayPalPaymentResponses];

export type GetPaymentData = {
    body?: never;
    path: {
        /**
         * The ID of the Payment to look up.
         */
        paymentId: number;
    };
    query?: never;
    url: '/account/payments/{paymentId}';
};

export type GetPaymentErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPaymentError = GetPaymentErrors[keyof GetPaymentErrors];

export type GetPaymentResponses = {
    /**
     * Payment object response.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When the payment was made.
         */
        readonly date: string;
        /**
         * __Read-only__ The unique ID of the payment.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ The amount, in US dollars, of the payment.
         */
        readonly usd: number;
    };
};

export type GetPaymentResponse = GetPaymentResponses[keyof GetPaymentResponses];

export type PostPromoCreditData = {
    /**
     * Enter a Promo Code to add its associated credit to your Account.
     */
    body?: {
        /**
         * The Promo Code.
         */
        promo_code: string;
    };
    path?: never;
    query?: never;
    url: '/account/promo-codes';
};

export type PostPromoCreditErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostPromoCreditError = PostPromoCreditErrors[keyof PostPromoCreditErrors];

export type PostPromoCreditResponses = {
    /**
     * __Read-only__ Promotions generally offer a set amount of credit that can be used toward your Linode services, and the promotion expires after a specified date. As well, a monthly cap on the promotional offer is set.
     *
     * Simply put, a promotion offers a certain amount of credit  month, until either the expiration date is passed, or until the total promotional credit is used, whichever comes first.
     */
    readonly 200: {
        /**
         * The amount available to spend per month.
         */
        credit_monthly_cap: string;
        /**
         * The total amount of credit left for this promotion.
         */
        credit_remaining: string;
        /**
         * A detailed description of this promotion.
         */
        description: string;
        /**
         * When this promotion's credits expire.
         */
        expire_dt: string;
        /**
         * The location of an image for this promotion.
         */
        image_url: string;
        /**
         * The service to which this promotion applies.
         */
        service_type: 'all' | 'backup' | 'blockstorage' | 'db_mysql' | 'ip_v4' | 'linode' | 'linode_disk' | 'linode_memory' | 'longview' | 'managed' | 'nodebalancer' | 'objectstorage' | 'placement_group' | 'transfer_tx';
        /**
         * Short details of this promotion.
         */
        summary: string;
        /**
         * The amount of credit left for this month for this promotion.
         */
        this_month_credit_remaining: string;
    };
};

export type PostPromoCreditResponse = PostPromoCreditResponses[keyof PostPromoCreditResponses];

export type GetServiceTransfersData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/service-transfers';
};

export type GetServiceTransfersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetServiceTransfersError = GetServiceTransfersErrors[keyof GetServiceTransfersErrors];

export type GetServiceTransfersResponses = {
    /**
     * Returns a paginated list of Service Transfer objects containing the details of all transfers that have been created and accepted by this account.
     */
    200: {
        data: Array<{
            /**
             * When this transfer was created.
             */
            created: string;
            /**
             * A collection of the services to include in this transfer request, separated by type.
             */
            entities: {
                /**
                 * An array containing the IDs of each of the Linodes included in this transfer.
                 */
                linodes: Array<number>;
            };
            /**
             * When this transfer expires. Transfers will automatically expire 24 hours after creation.
             */
            expiry: string;
            /**
             * __Filterable__ If the requesting account created this transfer.
             */
            is_sender: boolean;
            /**
             * __Filterable__ The status of the transfer request.
             *
             * `accepted`: The transfer has been accepted by another user and is currently in progress.
             * Transfers can take up to 3 hours to complete.
             *
             * `canceled`: The transfer has been canceled by the sender.
             *
             * `completed`: The transfer has completed successfully.
             *
             * `failed`: The transfer has failed after initiation.
             *
             * `pending`: The transfer is ready to be accepted.
             *
             * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
             * canceled.
             */
            status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
            /**
             * The token used to identify and accept or cancel this transfer.
             */
            token: string;
            /**
             * When this transfer was last updated.
             */
            updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetServiceTransfersResponse = GetServiceTransfersResponses[keyof GetServiceTransfersResponses];

export type PostServiceTransferData = {
    /**
     * The services to include in this transfer request.
     */
    body?: {
        /**
         * A collection of the services to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
    };
    path?: never;
    query?: never;
    url: '/account/service-transfers';
};

export type PostServiceTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostServiceTransferError = PostServiceTransferErrors[keyof PostServiceTransferErrors];

export type PostServiceTransferResponses = {
    /**
     * An object representing a Service Transfer.
     */
    200: {
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the services to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request.
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress.
         * Transfers can take up to 3 hours to complete.
         *
         * `canceled`: The transfer has been canceled by the sender.
         *
         * `completed`: The transfer has completed successfully.
         *
         * `failed`: The transfer has failed after initiation.
         *
         * `pending`: The transfer is ready to be accepted.
         *
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
         * canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    };
};

export type PostServiceTransferResponse = PostServiceTransferResponses[keyof PostServiceTransferResponses];

export type DeleteServiceTransferData = {
    body?: never;
    path: {
        /**
         * The UUID of the Service Transfer.
         */
        token: string;
    };
    query?: never;
    url: '/account/service-transfers/{token}';
};

export type DeleteServiceTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteServiceTransferError = DeleteServiceTransferErrors[keyof DeleteServiceTransferErrors];

export type DeleteServiceTransferResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteServiceTransferResponse = DeleteServiceTransferResponses[keyof DeleteServiceTransferResponses];

export type GetServiceTransferData = {
    body?: never;
    path: {
        /**
         * The UUID of the Service Transfer.
         */
        token: string;
    };
    query?: never;
    url: '/account/service-transfers/{token}';
};

export type GetServiceTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetServiceTransferError = GetServiceTransferErrors[keyof GetServiceTransferErrors];

export type GetServiceTransferResponses = {
    /**
     * An object representing a Service Transfer.
     */
    200: {
        /**
         * When this transfer was created.
         */
        created: string;
        /**
         * A collection of the services to include in this transfer request, separated by type.
         */
        entities: {
            /**
             * An array containing the IDs of each of the Linodes included in this transfer.
             */
            linodes: Array<number>;
        };
        /**
         * When this transfer expires. Transfers will automatically expire 24 hours after creation.
         */
        expiry: string;
        /**
         * __Filterable__ If the requesting account created this transfer.
         */
        is_sender: boolean;
        /**
         * __Filterable__ The status of the transfer request.
         *
         * `accepted`: The transfer has been accepted by another user and is currently in progress.
         * Transfers can take up to 3 hours to complete.
         *
         * `canceled`: The transfer has been canceled by the sender.
         *
         * `completed`: The transfer has completed successfully.
         *
         * `failed`: The transfer has failed after initiation.
         *
         * `pending`: The transfer is ready to be accepted.
         *
         * `stale`: The transfer has exceeded its expiration date. It can no longer be accepted or
         * canceled.
         */
        status: 'accepted' | 'canceled' | 'completed' | 'failed' | 'pending' | 'stale';
        /**
         * The token used to identify and accept or cancel this transfer.
         */
        token: string;
        /**
         * When this transfer was last updated.
         */
        updated: string;
    };
};

export type GetServiceTransferResponse = GetServiceTransferResponses[keyof GetServiceTransferResponses];

export type PostAcceptServiceTransferData = {
    body?: never;
    path: {
        /**
         * The UUID of the Service Transfer.
         */
        token: string;
    };
    query?: never;
    url: '/account/service-transfers/{token}/accept';
};

export type PostAcceptServiceTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAcceptServiceTransferError = PostAcceptServiceTransferErrors[keyof PostAcceptServiceTransferErrors];

export type PostAcceptServiceTransferResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostAcceptServiceTransferResponse = PostAcceptServiceTransferResponses[keyof PostAcceptServiceTransferResponses];

export type GetAccountSettingsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/settings';
};

export type GetAccountSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAccountSettingsError = GetAccountSettingsErrors[keyof GetAccountSettingsErrors];

export type GetAccountSettingsResponses = {
    /**
     * Account Settings object.
     */
    200: {
        /**
         * Account-wide backups default.  If `true`, all Linodes created will automatically be enrolled in the Backups service.  If `false`, Linodes will not be enrolled by default, but may still be enrolled on creation or later.
         */
        backups_enabled: boolean;
        /**
         * __Beta__ Defines if new Linodes can use legacy configuration interfaces:
         * - `legacy_config_only`. All new Linodes need to use legacy configuration interfaces. Prevously created Linodes with Linode Interfaces can still exist. Linodes using legacy configuration interfaces can't be upgraded to use Linode Interfaces.
         * - `legacy_config_default_but_linode_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use legacy configuration interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode Interfaces. This is the default setting for existing accounts.
         * - `linode_default_but_legacy_config_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use Linode Interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode interfaces. This is the default setting for new accounts.
         * - `linode_only`. All new Linodes need to use Linode Interfaces. Prevously created Linodes with legacy configuration profile interfaces can still exist if they were created under a previous setting. Linodes using legacy configuration interfaces can be upgraded to Linode Interfaces.
         */
        interfaces_for_new_linodes: 'legacy_config_only' | 'legacy_config_default_but_linode_allowed' | 'linode_default_but_legacy_config_allowed' | 'linode_only';
        /**
         * __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be a [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions) ID or `null` for Longview Free.
         */
        readonly longview_subscription: string;
        /**
         * __Beta__ Defines the default maintenance policy for new Linodes created on this account. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring system distributes monitoring checks to ensure that your servers remain online and available at all times. Linode Managed can monitor any service or software stack reachable over TCP or HTTP. Once you add a service to Linode Managed, we'll monitor it for connectivity, response, and total request time.
         */
        readonly managed: boolean;
        /**
         * Enables network helper across all users by default for new Linodes and Linode Configs.
         */
        network_helper: boolean;
        /**
         * __Read-only__ A string describing the status of this account's Object Storage service enrollment.
         */
        object_storage: 'disabled' | 'suspended' | 'active';
    };
};

export type GetAccountSettingsResponse = GetAccountSettingsResponses[keyof GetAccountSettingsResponses];

export type PutAccountSettingsData = {
    /**
     * Account Settings object.
     */
    body: {
        /**
         * Account-wide backups default.  If `true`, all Linodes created will automatically be enrolled in the Backups service.  If `false`, Linodes will not be enrolled by default, but may still be enrolled on creation or later.
         */
        backups_enabled: boolean;
        /**
         * __Beta__ Defines if new Linodes can use legacy configuration interfaces:
         * - `legacy_config_only`. All new Linodes need to use legacy configuration interfaces. Prevously created Linodes with Linode Interfaces can still exist. Linodes using legacy configuration interfaces can't be upgraded to use Linode Interfaces.
         * - `legacy_config_default_but_linode_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use legacy configuration interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode Interfaces. This is the default setting for existing accounts.
         * - `linode_default_but_legacy_config_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use Linode Interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode interfaces. This is the default setting for new accounts.
         * - `linode_only`. All new Linodes need to use Linode Interfaces. Prevously created Linodes with legacy configuration profile interfaces can still exist if they were created under a previous setting. Linodes using legacy configuration interfaces can be upgraded to Linode Interfaces.
         */
        interfaces_for_new_linodes: 'legacy_config_only' | 'legacy_config_default_but_linode_allowed' | 'linode_default_but_legacy_config_allowed' | 'linode_only';
        /**
         * __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be a [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions) ID or `null` for Longview Free.
         */
        readonly longview_subscription: string;
        /**
         * __Beta__ Defines the default maintenance policy for new Linodes created on this account. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring system distributes monitoring checks to ensure that your servers remain online and available at all times. Linode Managed can monitor any service or software stack reachable over TCP or HTTP. Once you add a service to Linode Managed, we'll monitor it for connectivity, response, and total request time.
         */
        readonly managed: boolean;
        /**
         * Enables network helper across all users by default for new Linodes and Linode Configs.
         */
        network_helper: boolean;
        /**
         * __Read-only__ A string describing the status of this account's Object Storage service enrollment.
         */
        object_storage: 'disabled' | 'suspended' | 'active';
    };
    path?: never;
    query?: never;
    url: '/account/settings';
};

export type PutAccountSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutAccountSettingsError = PutAccountSettingsErrors[keyof PutAccountSettingsErrors];

export type PutAccountSettingsResponses = {
    /**
     * Account Settings object.
     */
    200: {
        /**
         * Account-wide backups default.  If `true`, all Linodes created will automatically be enrolled in the Backups service.  If `false`, Linodes will not be enrolled by default, but may still be enrolled on creation or later.
         */
        backups_enabled: boolean;
        /**
         * __Beta__ Defines if new Linodes can use legacy configuration interfaces:
         * - `legacy_config_only`. All new Linodes need to use legacy configuration interfaces. Prevously created Linodes with Linode Interfaces can still exist. Linodes using legacy configuration interfaces can't be upgraded to use Linode Interfaces.
         * - `legacy_config_default_but_linode_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use legacy configuration interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode Interfaces. This is the default setting for existing accounts.
         * - `linode_default_but_legacy_config_allowed`. New Linodes can use legacy configuration interfaces or Linode Interfaces, depending on the `interface_generation` setting specified when creating the Linode. By default, new Linodes use Linode Interfaces unless otherwise specified. Linodes that use legacy configuration interfaces can upgrade to Linode interfaces. This is the default setting for new accounts.
         * - `linode_only`. All new Linodes need to use Linode Interfaces. Prevously created Linodes with legacy configuration profile interfaces can still exist if they were created under a previous setting. Linodes using legacy configuration interfaces can be upgraded to Linode Interfaces.
         */
        interfaces_for_new_linodes: 'legacy_config_only' | 'legacy_config_default_but_linode_allowed' | 'linode_default_but_legacy_config_allowed' | 'linode_only';
        /**
         * __Read-only__ The Longview Pro tier you are currently subscribed to. The value must be a [Longview subscription](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions) ID or `null` for Longview Free.
         */
        readonly longview_subscription: string;
        /**
         * __Beta__ Defines the default maintenance policy for new Linodes created on this account. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Our 24/7 incident response service. This robust, multi-homed monitoring system distributes monitoring checks to ensure that your servers remain online and available at all times. Linode Managed can monitor any service or software stack reachable over TCP or HTTP. Once you add a service to Linode Managed, we'll monitor it for connectivity, response, and total request time.
         */
        readonly managed: boolean;
        /**
         * Enables network helper across all users by default for new Linodes and Linode Configs.
         */
        network_helper: boolean;
        /**
         * __Read-only__ A string describing the status of this account's Object Storage service enrollment.
         */
        object_storage: 'disabled' | 'suspended' | 'active';
    };
};

export type PutAccountSettingsResponse = PutAccountSettingsResponses[keyof PutAccountSettingsResponses];

export type PostEnableAccountManagedData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/settings/managed-enable';
};

export type PostEnableAccountManagedErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostEnableAccountManagedError = PostEnableAccountManagedErrors[keyof PostEnableAccountManagedErrors];

export type PostEnableAccountManagedResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostEnableAccountManagedResponse = PostEnableAccountManagedResponses[keyof PostEnableAccountManagedResponses];

export type GetTransferData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/account/transfer';
};

export type GetTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTransferError = GetTransferErrors[keyof GetTransferErrors];

export type GetTransferResponses = {
    /**
     * An object representing your network utilization for the current month, in Gigabytes.
     *
     * Certain Regions have separate utilization quotas and rates. For Region-specific network utilization data, see `region_transfers`.
     */
    200: {
        /**
         * __Read-only__ The amount of your transfer pool that is billable this billing cycle.
         */
        readonly billable: number;
        /**
         * __Read-only__ The amount of network usage allowed this billing cycle.
         */
        readonly quota: number;
        region_transfers: Array<{
            /**
             * __Read-only__ The amount of your transfer pool that is billable this billing cycle for this Region.
             */
            readonly billable: number;
            /**
             * The Region ID for this network utilization data.
             */
            id: string;
            /**
             * __Read-only__ The amount of network usage allowed this billing cycle for this Region.
             */
            readonly quota: number;
            /**
             * __Read-only__ The amount of network usage you have used this billing cycle for this Region.
             */
            readonly used: number;
        }>;
        /**
         * __Read-only__ The amount of network usage you have used this billing cycle.
         */
        readonly used: number;
    };
};

export type GetTransferResponse = GetTransferResponses[keyof GetTransferResponses];

export type GetUsersData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/account/users';
};

export type GetUsersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetUsersError = GetUsersErrors[keyof GetUsersErrors];

export type GetUsersResponses = {
    /**
     * A paginated list of users.
     */
    200: {
        data: Array<{
            /**
             * This user's email address. Akamai uses this address for account management communications.
             */
            email: string;
            /**
             * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
             */
            readonly last_login: {
                /**
                 * __Read-only__ The date and time of this user's most recent login attempt.
                 */
                readonly login_datetime: string;
                /**
                 * __Read-only__ The result of this user's most recent login attempt.
                 */
                status: 'successful' | 'failed';
            } | null;
            /**
             * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
             */
            readonly password_created: string | null;
            /**
             * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
             */
            restricted: boolean;
            /**
             * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
             *
             * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
             *
             * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
             *
             * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
             */
            readonly ssh_keys: Array<string>;
            /**
             * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
             */
            readonly tfa_enabled: boolean;
            /**
             * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
             */
            username: string;
            /**
             * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
             */
            readonly verified_phone_number: string | null;
        } & {
            /**
             * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
             *
             * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
             *
             * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
             *
             * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
             *
             * - `default`. This applies to all regular, non-parent-child account users.
             */
            user_type: 'parent' | 'child' | 'proxy' | 'default';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetUsersResponse = GetUsersResponses[keyof GetUsersResponses];

export type PostUserData = {
    /**
     * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
     */
    body?: {
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
         */
        readonly last_login: {
            /**
             * __Read-only__ The date and time of this user's most recent login attempt.
             */
            readonly login_datetime: string;
            /**
             * __Read-only__ The result of this user's most recent login attempt.
             */
            status: 'successful' | 'failed';
        } | null;
        /**
         * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
         */
        readonly password_created: string | null;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
         *
         * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
         *
         * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
         *
         * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
         */
        readonly ssh_keys: Array<string>;
        /**
         * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
         */
        readonly tfa_enabled: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
        /**
         * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    };
    path?: never;
    query?: never;
    url: '/account/users';
};

export type PostUserErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostUserError = PostUserErrors[keyof PostUserErrors];

export type PostUserResponses = {
    /**
     * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
     */
    200: {
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
         */
        readonly last_login: {
            /**
             * __Read-only__ The date and time of this user's most recent login attempt.
             */
            readonly login_datetime: string;
            /**
             * __Read-only__ The result of this user's most recent login attempt.
             */
            status: 'successful' | 'failed';
        } | null;
        /**
         * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
         */
        readonly password_created: string | null;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
         *
         * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
         *
         * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
         *
         * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
         */
        readonly ssh_keys: Array<string>;
        /**
         * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
         */
        readonly tfa_enabled: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
        /**
         * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    };
};

export type PostUserResponse = PostUserResponses[keyof PostUserResponses];

export type DeleteUserData = {
    body?: never;
    path: {
        /**
         * The username to look up.
         */
        username: string;
    };
    query?: never;
    url: '/account/users/{username}';
};

export type DeleteUserErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteUserError = DeleteUserErrors[keyof DeleteUserErrors];

export type DeleteUserResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteUserResponse = DeleteUserResponses[keyof DeleteUserResponses];

export type GetUserData = {
    body?: never;
    path: {
        /**
         * The username to look up.
         */
        username: string;
    };
    query?: never;
    url: '/account/users/{username}';
};

export type GetUserErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetUserError = GetUserErrors[keyof GetUserErrors];

export type GetUserResponses = {
    /**
     * The requested User object.
     */
    200: {
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
         */
        readonly last_login: {
            /**
             * __Read-only__ The date and time of this user's most recent login attempt.
             */
            readonly login_datetime: string;
            /**
             * __Read-only__ The result of this user's most recent login attempt.
             */
            status: 'successful' | 'failed';
        } | null;
        /**
         * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
         */
        readonly password_created: string | null;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
         *
         * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
         *
         * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
         *
         * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
         */
        readonly ssh_keys: Array<string>;
        /**
         * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
         */
        readonly tfa_enabled: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
        /**
         * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    } & {
        /**
         * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
         *
         * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
         *
         * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
         *
         * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
         *
         * - `default`. This applies to all regular, non-parent-child account users.
         */
        user_type: 'parent' | 'child' | 'proxy' | 'default';
    };
};

export type GetUserResponse = GetUserResponses[keyof GetUserResponses];

export type PutUserData = {
    /**
     * A user on your account. Unrestricted users can log in and access information about your account, while restricted users may only access entities or perform actions they've been granted access to.
     */
    body?: {
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
         */
        readonly last_login: {
            /**
             * __Read-only__ The date and time of this user's most recent login attempt.
             */
            readonly login_datetime: string;
            /**
             * __Read-only__ The result of this user's most recent login attempt.
             */
            status: 'successful' | 'failed';
        } | null;
        /**
         * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
         */
        readonly password_created: string | null;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
         *
         * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
         *
         * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
         *
         * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
         */
        readonly ssh_keys: Array<string>;
        /**
         * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
         */
        readonly tfa_enabled: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
        /**
         * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    };
    path: {
        /**
         * The username to look up.
         */
        username: string;
    };
    query?: never;
    url: '/account/users/{username}';
};

export type PutUserErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutUserError = PutUserErrors[keyof PutUserErrors];

export type PutUserResponses = {
    /**
     * User updated successfully.
     */
    200: {
        /**
         * This user's email address. Akamai uses this address for account management communications.
         */
        email: string;
        /**
         * __Read-only__ Details on this user's last login attempt. Returned as `null` if this user hasn't attempted a login since it was created. You can run the [List user logins](https://techdocs.akamai.com/linode-api/reference/get-account-logins) operation for additional login information.
         */
        readonly last_login: {
            /**
             * __Read-only__ The date and time of this user's most recent login attempt.
             */
            readonly login_datetime: string;
            /**
             * __Read-only__ The result of this user's most recent login attempt.
             */
            status: 'successful' | 'failed';
        } | null;
        /**
         * __Read-only__ When this user's current password was created. You initially create a password during the account sign-up process, and you can update it using the [Reset Password](https://login.linode.com/forgot/password) webpage. Returned as `null` if this user doesn't have a password set.
         */
        readonly password_created: string | null;
        /**
         * If `true`, this user needs specific access granted to perform actions or access entities on your account. Run [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) for details on how to configure grants for a restricted user.
         */
        restricted: boolean;
        /**
         * __Read-only__ A list of the labels for SSH keys added by this user. Users can add keys with the [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) operation. These keys are deployed when this user is included in the `authorized_users` field of the following requests:
         *
         * - [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance)
         *
         * - [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance)
         *
         * - [Create a disk](https://techdocs.akamai.com/linode-api/reference/post-add-linode-disk)
         */
        readonly ssh_keys: Array<string>;
        /**
         * __Read-only__ Whether this user has Two Factor Authentication (TFA) enabled. Run the [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) operation to enable TFA.
         */
        readonly tfa_enabled: boolean;
        /**
         * __Filterable__ The name of this user. This user needs to use this value to log in. It may also display alongside actions this user performs, including events or public StackScripts.
         */
        username: string;
        /**
         * __Read-only__ The [verified](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) phone number for this user profile. Returned as `null` if the user doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    } & {
        /**
         * __Read-only__ If the user belongs to a [parent or child account](https://www.linode.com/docs/guides/parent-child-accounts/) relationship, this defines the user type in the respective account. Possible values include:
         *
         * - `parent`. This is a user in an Akamai partner account. Akamai partners have a contractual relationship with their end customers, to sell Akamai services. This user can either have full access (a parent account admin user) or limited access. Limited users don't have access to manage child accounts, but they can be granted this access by an admin user.
         *
         * - `child`. This is an Akamai partner's end customer user, in a child account. A child user can have either full or limited access. Full access lets the user manage other child users and the proxy user in a child account.
         *
         * - `proxy`. This is a user on a child account that gives parent account users access to that child account. A parent account user with the `child_account_access` grant can [Create a proxy user token](https://techdocs.akamai.com/linode-api/reference/post-child-account-token) from the parent account. The parent user can use this token to run API operations from the child account, as if they were a child user.
         *
         * - `default`. This applies to all regular, non-parent-child account users.
         */
        user_type: 'parent' | 'child' | 'proxy' | 'default';
    };
};

export type PutUserResponse = PutUserResponses[keyof PutUserResponses];

export type GetUserGrantsData = {
    body?: never;
    path: {
        /**
         * The username to look up.
         */
        username: string;
    };
    query?: never;
    url: '/account/users/{username}/grants';
};

export type GetUserGrantsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetUserGrantsError = GetUserGrantsErrors[keyof GetUserGrantsErrors];

export type GetUserGrantsResponses = {
    /**
     * The User's grants.
     */
    200: {
        /**
         * The grants this user has for individual Managed Databases on this account.
         */
        database: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual domains on this account.
         */
        domain: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual firewalls on this account.
         */
        firewall: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants the user has to all resources on your account.
         */
        global: {
            /**
             * The level of access this user has to account-level actions, like billing information and user management.
             *
             * > 📘
             * >
             * > A `restricted` user can't be used to manage users, even if this is set to `read-write`. Only unrestricted users can manage other users on an account.
             *
             * __Parent and child accounts__
             *
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
             */
            account_access: 'read_only' | 'read_write';
            /**
             * Whether this user can add Managed Databases on the account.
             */
            add_databases: boolean;
            /**
             * Whether this user can add domains on the account.
             */
            add_domains: boolean;
            /**
             * Whether this user can add Firewalls on the account.
             */
            add_firewalls: boolean;
            /**
             * Whether this user can create images from disks on your Linodes, on the account.
             */
            add_images: boolean;
            /**
             * Whether this user can create Linodes.
             */
            add_linodes: boolean;
            /**
             * Whether this user can create Longview clients and view the current plan.
             */
            add_longview: boolean;
            /**
             * Whether this user can add NodeBalancers on the account.
             */
            add_nodebalancers: boolean;
            /**
             * Whether this user can add StackScripts on the account.
             */
            add_stackscripts: boolean;
            /**
             * Whether this user can add volumes on the account.
             */
            add_volumes: boolean;
            /**
             * Whether this user can add Virtual Private Clouds (VPCs) on the account.
             */
            add_vpcs: boolean;
            /**
             * Whether this user can cancel the entire account.
             */
            cancel_account: boolean;
            /**
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this gives a parent account access to operations that can be used to manage child accounts. Unrestricted parent account users have access to this grant, while restricted parent users don't. An unrestricted parent user can set this to `true` to add this grant to a restricted parent user. Displayed as `null` for all non-parent accounts.
             */
            child_account_access: boolean | null;
            /**
             * Whether this user can manage your account's Longview subscription.
             */
            longview_subscription: boolean;
        };
        /**
         * The grants this user has for individual images on this account.
         */
        image: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Linodes on this account.
         */
        linode: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Longview Clients on this account.
         */
        longview: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual NodeBalancers on this account.
         */
        nodebalancer: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this User has for individual StackScripts on this account.
         */
        stackscript: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Block Storage Volumes on this account.
         */
        volume: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Virtual Private Clouds (VPCs) on this account.
         */
        vpc: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
    };
    /**
     * This is an unrestricted user. These users don't use grants. They can access everything on the account and perform all actions.
     */
    204: void;
};

export type GetUserGrantsResponse = GetUserGrantsResponses[keyof GetUserGrantsResponses];

export type PutUserGrantsData = {
    body: {
        /**
         * The grants this user has for individual Managed Databases on this account.
         */
        database: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual domains on this account.
         */
        domain: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual firewalls on this account.
         */
        firewall: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants the user has to all resources on your account.
         */
        global: {
            /**
             * The level of access this user has to account-level actions, like billing information and user management.
             *
             * > 📘
             * >
             * > A `restricted` user can't be used to manage users, even if this is set to `read-write`. Only unrestricted users can manage other users on an account.
             *
             * __Parent and child accounts__
             *
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
             */
            account_access: 'read_only' | 'read_write';
            /**
             * Whether this user can add Managed Databases on the account.
             */
            add_databases: boolean;
            /**
             * Whether this user can add domains on the account.
             */
            add_domains: boolean;
            /**
             * Whether this user can add Firewalls on the account.
             */
            add_firewalls: boolean;
            /**
             * Whether this user can create images from disks on your Linodes, on the account.
             */
            add_images: boolean;
            /**
             * Whether this user can create Linodes.
             */
            add_linodes: boolean;
            /**
             * Whether this user can create Longview clients and view the current plan.
             */
            add_longview: boolean;
            /**
             * Whether this user can add NodeBalancers on the account.
             */
            add_nodebalancers: boolean;
            /**
             * Whether this user can add StackScripts on the account.
             */
            add_stackscripts: boolean;
            /**
             * Whether this user can add volumes on the account.
             */
            add_volumes: boolean;
            /**
             * Whether this user can add Virtual Private Clouds (VPCs) on the account.
             */
            add_vpcs: boolean;
            /**
             * Whether this user can cancel the entire account.
             */
            cancel_account: boolean;
            /**
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this gives a parent account access to operations that can be used to manage child accounts. Unrestricted parent account users have access to this grant, while restricted parent users don't. An unrestricted parent user can set this to `true` to add this grant to a restricted parent user. Displayed as `null` for all non-parent accounts.
             */
            child_account_access: boolean | null;
            /**
             * Whether this user can manage your account's Longview subscription.
             */
            longview_subscription: boolean;
        };
        /**
         * The grants this user has for individual images on this account.
         */
        image: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Linodes on this account.
         */
        linode: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Longview Clients on this account.
         */
        longview: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual NodeBalancers on this account.
         */
        nodebalancer: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this User has for individual StackScripts on this account.
         */
        stackscript: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Block Storage Volumes on this account.
         */
        volume: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Virtual Private Clouds (VPCs) on this account.
         */
        vpc: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
    };
    path: {
        /**
         * The username to look up.
         */
        username: string;
    };
    query?: never;
    url: '/account/users/{username}/grants';
};

export type PutUserGrantsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutUserGrantsError = PutUserGrantsErrors[keyof PutUserGrantsErrors];

export type PutUserGrantsResponses = {
    /**
     * Grants updated successfully.
     */
    200: {
        /**
         * The grants this user has for individual Managed Databases on this account.
         */
        database: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual domains on this account.
         */
        domain: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual firewalls on this account.
         */
        firewall: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants the user has to all resources on your account.
         */
        global: {
            /**
             * The level of access this user has to account-level actions, like billing information and user management.
             *
             * > 📘
             * >
             * > A `restricted` user can't be used to manage users, even if this is set to `read-write`. Only unrestricted users can manage other users on an account.
             *
             * __Parent and child accounts__
             *
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
             */
            account_access: 'read_only' | 'read_write';
            /**
             * Whether this user can add Managed Databases on the account.
             */
            add_databases: boolean;
            /**
             * Whether this user can add domains on the account.
             */
            add_domains: boolean;
            /**
             * Whether this user can add Firewalls on the account.
             */
            add_firewalls: boolean;
            /**
             * Whether this user can create images from disks on your Linodes, on the account.
             */
            add_images: boolean;
            /**
             * Whether this user can create Linodes.
             */
            add_linodes: boolean;
            /**
             * Whether this user can create Longview clients and view the current plan.
             */
            add_longview: boolean;
            /**
             * Whether this user can add NodeBalancers on the account.
             */
            add_nodebalancers: boolean;
            /**
             * Whether this user can add StackScripts on the account.
             */
            add_stackscripts: boolean;
            /**
             * Whether this user can add volumes on the account.
             */
            add_volumes: boolean;
            /**
             * Whether this user can add Virtual Private Clouds (VPCs) on the account.
             */
            add_vpcs: boolean;
            /**
             * Whether this user can cancel the entire account.
             */
            cancel_account: boolean;
            /**
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this gives a parent account access to operations that can be used to manage child accounts. Unrestricted parent account users have access to this grant, while restricted parent users don't. An unrestricted parent user can set this to `true` to add this grant to a restricted parent user. Displayed as `null` for all non-parent accounts.
             */
            child_account_access: boolean | null;
            /**
             * Whether this user can manage your account's Longview subscription.
             */
            longview_subscription: boolean;
        };
        /**
         * The grants this user has for individual images on this account.
         */
        image: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Linodes on this account.
         */
        linode: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Longview Clients on this account.
         */
        longview: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual NodeBalancers on this account.
         */
        nodebalancer: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this User has for individual StackScripts on this account.
         */
        stackscript: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Block Storage Volumes on this account.
         */
        volume: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Virtual Private Clouds (VPCs) on this account.
         */
        vpc: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
    };
};

export type PutUserGrantsResponse = PutUserGrantsResponses[keyof PutUserGrantsResponses];

export type GetBetaProgramsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/betas';
};

export type GetBetaProgramsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetBetaProgramsError = GetBetaProgramsErrors[keyof GetBetaProgramsErrors];

export type GetBetaProgramsResponses = {
    /**
     * Returns a paginated list of all available Beta Program objects.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * __Read-only__ Additional details regarding the Beta Program.
             */
            readonly description: string | null;
            /**
             * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
             *
             * `null` indicates that the Beta Program is ongoing.
             */
            readonly ended: string | null;
            /**
             * __Filterable__, __Read-only__ Whether the Beta Program requires [Green Light](https://www.linode.com/green-light/) participation for enrollment.
             */
            readonly greenlight_only: boolean;
            /**
             * The unique identifier of the Beta Program.
             */
            id: string;
            /**
             * __Filterable__, __Read-only__ The name of the Beta Program.
             */
            readonly label: string;
            /**
             * __Read-only__ Additional source of information for the Beta Program.
             */
            readonly more_info: string | null;
            /**
             * __Filterable__, __Read-only__ The start date-time of the Beta Program.
             */
            readonly started: string;
        }>;
    };
};

export type GetBetaProgramsResponse = GetBetaProgramsResponses[keyof GetBetaProgramsResponses];

export type GetBetaProgramData = {
    body?: never;
    path: {
        /**
         * The ID of the Beta Program.
         */
        betaId: string;
    };
    query?: never;
    url: '/betas/{betaId}';
};

export type GetBetaProgramErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetBetaProgramError = GetBetaProgramErrors[keyof GetBetaProgramErrors];

export type GetBetaProgramResponses = {
    /**
     * An object representing Beta Program information.
     */
    200: {
        /**
         * __Read-only__ Additional details regarding the Beta Program.
         */
        readonly description: string | null;
        /**
         * __Filterable__, __Read-only__ The date-time that the Beta Program ended.
         *
         * `null` indicates that the Beta Program is ongoing.
         */
        readonly ended: string | null;
        /**
         * __Filterable__, __Read-only__ Whether the Beta Program requires [Green Light](https://www.linode.com/green-light/) participation for enrollment.
         */
        readonly greenlight_only: boolean;
        /**
         * The unique identifier of the Beta Program.
         */
        id: string;
        /**
         * __Filterable__, __Read-only__ The name of the Beta Program.
         */
        readonly label: string;
        /**
         * __Read-only__ Additional source of information for the Beta Program.
         */
        readonly more_info: string | null;
        /**
         * __Filterable__, __Read-only__ The start date-time of the Beta Program.
         */
        readonly started: string;
    };
};

export type GetBetaProgramResponse = GetBetaProgramResponses[keyof GetBetaProgramResponses];

export type GetDatabasesEnginesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/engines';
};

export type GetDatabasesEnginesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesEnginesError = GetDatabasesEnginesErrors[keyof GetDatabasesEnginesErrors];

export type GetDatabasesEnginesResponses = {
    /**
     * Returns a paginated list of all available Managed Databases engines and versions.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * __Filterable__ The Managed Database engine type.
             */
            engine: string;
            /**
             * The Managed Database engine ID in engine/version format.
             */
            id: string;
            /**
             * __Filterable__ The Managed Database engine version.
             */
            version: string;
        }>;
    };
};

export type GetDatabasesEnginesResponse = GetDatabasesEnginesResponses[keyof GetDatabasesEnginesResponses];

export type GetDatabasesEngineData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed Database engine.
         */
        engineId: string;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/engines/{engineId}';
};

export type GetDatabasesEngineErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesEngineError = GetDatabasesEngineErrors[keyof GetDatabasesEngineErrors];

export type GetDatabasesEngineResponses = {
    /**
     * Managed Database engine object.
     */
    200: {
        /**
         * __Filterable__ The Managed Database engine type.
         */
        engine: string;
        /**
         * The Managed Database engine ID in engine/version format.
         */
        id: string;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type GetDatabasesEngineResponse = GetDatabasesEngineResponses[keyof GetDatabasesEngineResponses];

export type GetDatabasesInstancesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/instances';
};

export type GetDatabasesInstancesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesInstancesError = GetDatabasesInstancesErrors[keyof GetDatabasesInstancesErrors];

export type GetDatabasesInstancesResponses = {
    /**
     * Returns a paginated list of all accessible Managed Databases on your account.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * Controls access to the Managed Database.
             *
             * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
             *
             * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
             *
             * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
             */
            allow_list: Array<string>;
            /**
             * The number of Linode instance nodes deployed to the Managed Database.
             *
             * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
             *
             * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
             */
            cluster_size: 1 | 2 | 3;
            /**
             * __Read-only__ When this Managed Database was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
             */
            readonly encrypted: boolean;
            /**
             * __Filterable__, __Read-only__ The Managed Database engine type.
             */
            engine: 'mysql' | 'postgresql';
            /**
             * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
             */
            fork: {
                /**
                 * The database timestamp from which it was restored. This is _not_ when the fork was created.
                 */
                restore_time: string;
                /**
                 * The instance id of the database that was forked from.
                 */
                source: number;
            };
            /**
             * __Read-only__ The primary hostname and secondary read-only hostname for the Managed Database. The API assigns these hostnames after it successfully creates the Managed Database.
             */
            readonly hosts: {
                /**
                 * The primary hostname for the Managed Database.
                 *
                 * > 📘
                 * >
                 * > - If you've configured the Managed Database to use a Virtual Private Cloud (VPC) via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC.
                 * >
                 * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns _publicly_ routable IPv4 and IPv6 addresses.
                 */
                primary: string | null;
                /**
                 * The secondary, read-only hostname for the Managed Database. This can only be used to access a Managed Database cluster from Linodes in the same data center (region). Accessing the cluster through this hostname doesn't incur transfer costs.
                 *
                 * > 📘
                 * >
                 * > - The secondary hostname is publicly visible and accessible.
                 * >
                 * > - If you've configured the Managed Database to use a VPC via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC. You can also use this hostname for requests between clients within the same VPC to access the cluster over the private network, which avoids transfer costs.
                 * >
                 * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns publicly routable IPv4 and IPv6 addresses.
                 */
                secondary: string | null;
            };
            /**
             * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
             */
            readonly id: number;
            /**
             * __Read-only__ Append this to `https://api.linode.com` to run commands for the Managed Database.
             */
            readonly instance_uri: string;
            /**
             * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
             */
            label: string;
            /**
             * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
             */
            readonly members: {
                [key: string]: unknown;
            };
            /**
             * __Read-only__ The oldest time to which a database can be restored.
             */
            readonly oldest_restore_time: string;
            /**
             * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
             */
            platform: 'rdbms-legacy' | 'rdbms-default';
            /**
             * __Read-only__ The access port for this Managed Database.
             */
            readonly port: number;
            /**
             * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
             */
            region: string;
            /**
             * __Filterable__, __Read-only__ The operating status of the Managed Database.
             */
            status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
            /**
             * __Read-only__ The total disk size of the database, in GB.
             */
            readonly total_disk_size_gb: number;
            /**
             * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
             */
            type: string;
            /**
             * __Read-only__ When this Managed Database was last updated.
             */
            readonly updated: string;
            /**
             * Configuration settings for automated patch update maintenance for the Managed Database.
             */
            updates: {
                /**
                 * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
                 */
                day_of_week: number;
                /**
                 * The maximum maintenance window time in hours.
                 */
                duration: number;
                /**
                 * How frequently maintenance occurs. Currently can only be `weekly`.
                 */
                frequency: 'weekly';
                /**
                 * The hour to begin maintenance based in UTC time.
                 */
                hour_of_day: number;
                /**
                 * __Read-only__ An array of pending updates.
                 */
                readonly pending: Array<{
                    /**
                     * The time when a mandatory update needs to be applied.
                     */
                    deadline: string | null;
                    /**
                     * A description of the update.
                     */
                    description: string;
                    /**
                     * The date and time a maintenance update will be applied.
                     */
                    planned_for: string | null;
                }>;
            };
            /**
             * __Read-only__ The amount of space currently in use in the database, in GB.
             */
            readonly used_disk_size_gb: number;
            /**
             * __Filterable__ The Managed Database engine version.
             */
            version: string;
        }>;
    };
};

export type GetDatabasesInstancesResponse = GetDatabasesInstancesResponses[keyof GetDatabasesInstancesResponses];

export type GetDatabasesMysqlConfigData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/databases/mysql/config';
};

export type GetDatabasesMysqlConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesMysqlConfigError = GetDatabasesMysqlConfigErrors[keyof GetDatabasesMysqlConfigErrors];

export type GetDatabasesMysqlConfigResponses = {
    /**
     * Available [advanced parameters](https://aiven.io/docs/products/mysql/reference/advanced-params) for a MySQL Managed Database.
     */
    200: {
        /**
         * Settings available to configure a `binlog_retention_period`, per Aiven's specifications.
         */
        binlog_retention_period: number;
        /**
         * Parameters available to configure a MySQL Managed Database.
         */
        mysql: {
            /**
             * Settings available to configure the `connect_timeout`, per Aiven's specifications.
             */
            connect_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `connect_timeout`, per Aiven's specifications.
             */
            default_time_zone: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter, using the required `pattern`.
                 */
                example: string;
                /**
                 * The maximum character length for this parameter.
                 */
                maxLength: number;
                /**
                 * The minimum character length for this parameter.
                 */
                minLength: number;
                /**
                 * The pattern to follow when defining this parameter.
                 */
                pattern: string;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `group_concat_max_len`, per Aiven's specifications.
             */
            group_concat_max_len: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `information_schema_stats_expiry`, per Aiven's specifications.
             */
            information_schema_stats_expiry: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_change_buffer_max_size`, per Aiven's specifications.
             */
            innodb_change_buffer_max_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_flush_neighbors`, per Aiven's specifications.
             */
            innodb_flush_neighbors: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_ft_min_token_size`, per Aiven's specifications.
             */
            innodb_ft_min_token_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure your own `innodb_ft_server_stopword_table`, per Aiven's specifications. Set to `null` for no value.
             */
            innodb_ft_server_stopword_table: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter, using the required `pattern`, or `null` for no value.
                 */
                example: string;
                /**
                 * The maximum character length for this parameter.
                 */
                maxLength: number;
                /**
                 * The pattern to follow when defining this parameter.
                 */
                pattern: string;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            } | null;
            /**
             * Settings available to configure the `innodb_lock_wait_timeout`, per Aiven's specifications.
             */
            innodb_lock_wait_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_log_buffer_size`, per Aiven's specifications.
             */
            innodb_log_buffer_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_online_alter_log_max_size`, per Aiven's specifications.
             */
            innodb_online_alter_log_max_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_read_io_threads`, per Aiven's specifications.
             */
            innodb_read_io_threads: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_rollback_on_timeout`, per Aiven's specifications.
             */
            innodb_rollback_on_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example boolean value for this parameter.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_thread_concurrency`, per Aiven's specifications.
             */
            innodb_thread_concurrency: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `innodb_write_io_threads`, per Aiven's specifications.
             */
            innodb_write_io_threads: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `interactive_timeout`, per Aiven's specifications.
             */
            interactive_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `internal_tmp_mem_storage_engine`, per Aiven's specifications.
             */
            internal_tmp_mem_storage_engine: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_allowed_packet`, per Aiven's specifications.
             */
            max_allowed_packet: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_heap_table_size`, per Aiven's specifications.
             */
            max_heap_table_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `net_buffer_length`, per Aiven's specifications.
             */
            net_buffer_length: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `net_read_timeout`, per Aiven's specifications.
             */
            net_read_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `net_write_timeout`, per Aiven's specifications.
             */
            net_write_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `sql_mode`, per Aiven's specifications.
             */
            sql_mode: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter, using the required `pattern`.
                 */
                example: string;
                /**
                 * The maximum character length for this parameter.
                 */
                maxLength: number;
                /**
                 * The pattern to follow when defining this parameter.
                 */
                pattern: string;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `sql_require_primary_key`, per Aiven's specifications.
             */
            sql_require_primary_key: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example boolean value for this parameter.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `tmp_table_size`, per Aiven's specifications.
             */
            tmp_table_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `wait_timeout`, per Aiven's specifications.
             */
            wait_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the MySQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
        };
    };
};

export type GetDatabasesMysqlConfigResponse = GetDatabasesMysqlConfigResponses[keyof GetDatabasesMysqlConfigResponses];

export type GetDatabasesMysqlInstancesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/mysql/instances';
};

export type GetDatabasesMysqlInstancesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesMysqlInstancesError = GetDatabasesMysqlInstancesErrors[keyof GetDatabasesMysqlInstancesErrors];

export type GetDatabasesMysqlInstancesResponses = {
    /**
     * Returns a paginated list of all accessible MySQL Managed Databases on your account.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * Controls access to the Managed Database.
             *
             * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
             *
             * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
             *
             * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
             */
            allow_list: Array<string>;
            /**
             * The number of Linode instance nodes deployed to the Managed Database.
             *
             * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
             *
             * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
             */
            cluster_size: 1 | 2 | 3;
            /**
             * __Read-only__ When this Managed Database was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
             */
            readonly encrypted: boolean;
            /**
             * __Filterable__, __Read-only__ The Managed Database engine type.
             */
            readonly engine: string;
            /**
             * Advanced parameters you can apply to a MySQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/mysql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
             *
             * > 📘
             * >
             * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a MySQL Managed Database. You can also run the [List MySQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-config) operation to see an up-to-date list.
             */
            engine_config: {
                /**
                 * The minimum amount of time in seconds to keep `binlog` entries before deletion. This may be extended for services that require `binlog` entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
                 */
                binlog_retention_period: number;
                /**
                 * MySQL-specific advanced configuration parameters.
                 */
                mysql: {
                    /**
                     * The number of seconds that the `mysqld` server waits for a connect packet before responding with bad handshake.
                     */
                    connect_timeout: number;
                    /**
                     * Default server time zone as an offset from UTC (from -12:00 to +12:00), a time zone name, or `SYSTEM` to use the MySQL server default.
                     */
                    default_time_zone: string;
                    /**
                     * The maximum permitted result length in bytes for the `GROUP_CONCAT()` function.
                     */
                    group_concat_max_len: number;
                    /**
                     * The time, in seconds, before cached statistics expire.
                     */
                    information_schema_stats_expiry: number;
                    /**
                     * Maximum size for the InnoDB change buffer, as a percentage of the total size of the buffer pool.
                     */
                    innodb_change_buffer_max_size: number;
                    /**
                     * Specifies whether flushing a page from the InnoDB buffer pool also flushes other dirty pages in the same extent: `0` - dirty pages in the same extent are not flushed, `1` - flush contiguous dirty pages in the same extent, `2` - flush dirty pages in the same extent.
                     */
                    innodb_flush_neighbors: number;
                    /**
                     * Minimum length of words that are stored in an InnoDB `-1FULLTEXT` index. Changing this parameter will lead to a restart of the MySQL service.
                     */
                    innodb_ft_min_token_size: number;
                    /**
                     * This option is used to specify your own InnoDB `FULLTEXT` index `stopword` list for all InnoDB tables. Set to `null` for no value.
                     */
                    innodb_ft_server_stopword_table: string | null;
                    /**
                     * The length of time in seconds an InnoDB transaction waits for a row lock before giving up.
                     */
                    innodb_lock_wait_timeout: number;
                    /**
                     * The size in bytes of the buffer that InnoDB uses to write to the log files on disk.
                     */
                    innodb_log_buffer_size: number;
                    /**
                     * The upper limit in bytes on the size of the temporary log files used during online DDL operations for InnoDB tables.
                     */
                    innodb_online_alter_log_max_size: number;
                    /**
                     * The number of I/O threads for read operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                     */
                    innodb_read_io_threads: number;
                    /**
                     * When enabled a transaction timeout causes InnoDB to abort and roll back the entire transaction. Changing this parameter will lead to a restart of the MySQL service.
                     */
                    innodb_rollback_on_timeout: boolean;
                    /**
                     * Defines the maximum number of threads permitted inside of InnoDB. The default is `0` which indicates infinite concurrency, or no limit.
                     */
                    innodb_thread_concurrency: number;
                    /**
                     * The number of I/O threads for write operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                     */
                    innodb_write_io_threads: number;
                    /**
                     * The number of seconds the server waits for activity on an interactive connection before closing it.
                     */
                    interactive_timeout: number;
                    /**
                     * The storage engine for in-memory internal temporary tables.
                     */
                    internal_tmp_mem_storage_engine: 'TempTable' | 'MEMORY';
                    /**
                     * Size of the largest message in bytes that can be received by the server.
                     */
                    max_allowed_packet: number;
                    /**
                     * Limits the size of internal in-memory tables. Also set `tmp_table_size`.
                     */
                    max_heap_table_size: number;
                    /**
                     * Start sizes of connection buffer and result buffer. Changing this parameter will lead to a restart of the MySQL service.
                     */
                    net_buffer_length: number;
                    /**
                     * The number of seconds to wait for more data from a connection before aborting the read.
                     */
                    net_read_timeout: number;
                    /**
                     * The number of seconds to wait for a block to be written to a connection before aborting the write.
                     */
                    net_write_timeout: number;
                    /**
                     * Global SQL mode. Set to empty to use MySQL server defaults. When creating a new service and not setting this field, Akamai defaults to SQL mode which is strict, SQL standard compliant.
                     */
                    sql_mode: string;
                    /**
                     * Require primary key to be defined for new tables or old tables modified with `ALTER TABLE` and fail if missing. You should always have primary keys because various functionality may break if any large table is missing them.
                     */
                    sql_require_primary_key: boolean;
                    /**
                     * Limits the size of internal in-memory tables. This also requires a `max_heap_table_size`.
                     */
                    tmp_table_size: number;
                    /**
                     * The number of seconds the server waits for activity on a non-interactive connection before closing it.
                     */
                    wait_timeout: number;
                };
            };
            /**
             * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
             */
            fork: {
                /**
                 * The database timestamp from which it was restored. This is _not_ when the fork was created.
                 */
                restore_time: string;
                /**
                 * The instance id of the database that was forked from.
                 */
                source: number;
            };
            /**
             * __Read-only__ The primary hostname and secondary read-only hostname for the Managed Database. The API assigns these hostnames after it successfully creates the Managed Database.
             */
            readonly hosts: {
                /**
                 * The primary hostname for the Managed Database.
                 *
                 * > 📘
                 * >
                 * > - If you've configured the Managed Database to use a Virtual Private Cloud (VPC) via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC.
                 * >
                 * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns _publicly_ routable IPv4 and IPv6 addresses.
                 */
                primary: string | null;
                /**
                 * The secondary, read-only hostname for the Managed Database. This can only be used to access a Managed Database cluster from Linodes in the same data center (region). Accessing the cluster through this hostname doesn't incur transfer costs.
                 *
                 * > 📘
                 * >
                 * > - The secondary hostname is publicly visible and accessible.
                 * >
                 * > - If you've configured the Managed Database to use a VPC via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC. You can also use this hostname for requests between clients within the same VPC to access the cluster over the private network, which avoids transfer costs.
                 * >
                 * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns publicly routable IPv4 and IPv6 addresses.
                 */
                secondary: string | null;
            };
            /**
             * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
             */
            readonly id: number;
            /**
             * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
             */
            label: string;
            /**
             * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
             */
            readonly members: {
                [key: string]: unknown;
            };
            /**
             * __Read-only__ The oldest time to which a database can be restored.
             */
            readonly oldest_restore_time: string;
            /**
             * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
             */
            platform: 'rdbms-legacy' | 'rdbms-default';
            /**
             * __Read-only__ The access port for this Managed Database.
             */
            readonly port: number;
            /**
             * __Beta__ Restricts access to a MySQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
             */
            private_network: {
                /**
                 * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
                 *
                 * > 📘
                 * >
                 * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
                 */
                public_access: boolean;
                /**
                 * The unique identifier of a specific subnet, in the VPC (`vpc_id`) where the Managed Database can be accessed.
                 */
                subnet_id: number;
                /**
                 * The unique identifier of the VPC where this database exists.
                 */
                vpc_id: number;
            } | null;
            /**
             * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
             */
            region: string;
            /**
             * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
             */
            ssl_connection: boolean;
            /**
             * __Filterable__, __Read-only__ The operating status of the Managed Database.
             */
            status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
            /**
             * __Read-only__ The total disk size of the database, in GB.
             */
            readonly total_disk_size_gb: number;
            /**
             * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
             */
            type: string;
            /**
             * __Read-only__ When this Managed Database was last updated.
             */
            readonly updated: string;
            /**
             * Configuration settings for automated patch update maintenance for the Managed Database.
             */
            updates: {
                /**
                 * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
                 */
                day_of_week: number;
                /**
                 * The maximum maintenance window time in hours.
                 */
                duration: number;
                /**
                 * How frequently maintenance occurs. Currently can only be `weekly`.
                 */
                frequency: 'weekly';
                /**
                 * The hour to begin maintenance based in UTC time.
                 */
                hour_of_day: number;
                /**
                 * __Read-only__ An array of pending updates.
                 */
                readonly pending: Array<{
                    /**
                     * The time when a mandatory update needs to be applied.
                     */
                    deadline: string | null;
                    /**
                     * A description of the update.
                     */
                    description: string;
                    /**
                     * The date and time a maintenance update will be applied.
                     */
                    planned_for: string | null;
                }>;
            };
            /**
             * __Read-only__ The amount of space currently in use in the database, in GB.
             */
            readonly used_disk_size_gb: number;
            /**
             * __Filterable__ The Managed Database engine version.
             */
            version: string;
        }>;
    };
};

export type GetDatabasesMysqlInstancesResponse = GetDatabasesMysqlInstancesResponses[keyof GetDatabasesMysqlInstancesResponses];

export type PostDatabasesMysqlInstancesData = {
    /**
     * Managed MySQL Database request object.
     */
    body: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list?: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size?: 1 | 2 | 3;
        /**
         * The Managed Database engine in engine/version format.
         */
        engine: string;
        /**
         * Advanced parameters you can apply to a MySQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/mysql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a MySQL Managed Database. You can also run the [List MySQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-config) operation to see an up-to-date list.
         */
        engine_config?: {
            /**
             * The minimum amount of time in seconds to keep `binlog` entries before deletion. This may be extended for services that require `binlog` entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
             */
            binlog_retention_period: number;
            /**
             * MySQL-specific advanced configuration parameters.
             */
            mysql: {
                /**
                 * The number of seconds that the `mysqld` server waits for a connect packet before responding with bad handshake.
                 */
                connect_timeout: number;
                /**
                 * Default server time zone as an offset from UTC (from -12:00 to +12:00), a time zone name, or `SYSTEM` to use the MySQL server default.
                 */
                default_time_zone: string;
                /**
                 * The maximum permitted result length in bytes for the `GROUP_CONCAT()` function.
                 */
                group_concat_max_len: number;
                /**
                 * The time, in seconds, before cached statistics expire.
                 */
                information_schema_stats_expiry: number;
                /**
                 * Maximum size for the InnoDB change buffer, as a percentage of the total size of the buffer pool.
                 */
                innodb_change_buffer_max_size: number;
                /**
                 * Specifies whether flushing a page from the InnoDB buffer pool also flushes other dirty pages in the same extent: `0` - dirty pages in the same extent are not flushed, `1` - flush contiguous dirty pages in the same extent, `2` - flush dirty pages in the same extent.
                 */
                innodb_flush_neighbors: number;
                /**
                 * Minimum length of words that are stored in an InnoDB `-1FULLTEXT` index. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_ft_min_token_size: number;
                /**
                 * This option is used to specify your own InnoDB `FULLTEXT` index `stopword` list for all InnoDB tables. Set to `null` for no value.
                 */
                innodb_ft_server_stopword_table: string | null;
                /**
                 * The length of time in seconds an InnoDB transaction waits for a row lock before giving up.
                 */
                innodb_lock_wait_timeout: number;
                /**
                 * The size in bytes of the buffer that InnoDB uses to write to the log files on disk.
                 */
                innodb_log_buffer_size: number;
                /**
                 * The upper limit in bytes on the size of the temporary log files used during online DDL operations for InnoDB tables.
                 */
                innodb_online_alter_log_max_size: number;
                /**
                 * The number of I/O threads for read operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_read_io_threads: number;
                /**
                 * When enabled a transaction timeout causes InnoDB to abort and roll back the entire transaction. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_rollback_on_timeout: boolean;
                /**
                 * Defines the maximum number of threads permitted inside of InnoDB. The default is `0` which indicates infinite concurrency, or no limit.
                 */
                innodb_thread_concurrency: number;
                /**
                 * The number of I/O threads for write operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_write_io_threads: number;
                /**
                 * The number of seconds the server waits for activity on an interactive connection before closing it.
                 */
                interactive_timeout: number;
                /**
                 * The storage engine for in-memory internal temporary tables.
                 */
                internal_tmp_mem_storage_engine: 'TempTable' | 'MEMORY';
                /**
                 * Size of the largest message in bytes that can be received by the server.
                 */
                max_allowed_packet: number;
                /**
                 * Limits the size of internal in-memory tables. Also set `tmp_table_size`.
                 */
                max_heap_table_size: number;
                /**
                 * Start sizes of connection buffer and result buffer. Changing this parameter will lead to a restart of the MySQL service.
                 */
                net_buffer_length: number;
                /**
                 * The number of seconds to wait for more data from a connection before aborting the read.
                 */
                net_read_timeout: number;
                /**
                 * The number of seconds to wait for a block to be written to a connection before aborting the write.
                 */
                net_write_timeout: number;
                /**
                 * Global SQL mode. Set to empty to use MySQL server defaults. When creating a new service and not setting this field, Akamai defaults to SQL mode which is strict, SQL standard compliant.
                 */
                sql_mode: string;
                /**
                 * Require primary key to be defined for new tables or old tables modified with `ALTER TABLE` and fail if missing. You should always have primary keys because various functionality may break if any large table is missing them.
                 */
                sql_require_primary_key: boolean;
                /**
                 * Limits the size of internal in-memory tables. This also requires a `max_heap_table_size`.
                 */
                tmp_table_size: number;
                /**
                 * The number of seconds the server waits for activity on a non-interactive connection before closing it.
                 */
                wait_timeout: number;
            };
        };
        /**
         * Include this object to restore a Managed Database by forking from a backup.
         *
         * - If you include this object, all other fields are optional.
         *
         * - Don't include this object if you're creating a new Managed Database.
         */
        fork?: {
            /**
             * A specific database timestamp to restore from.
             */
            restore_time?: string;
            /**
             * The unique instance id for the database to fork from. Run the [List Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-instances) operation and store the unique `id` for the target Managed Database.
             */
            source: number;
        };
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Beta__ Restricts access on a MySQL Managed Database to a specific Virtual Private Cloud (VPC) configured for the cluster.
         */
        private_network?: {
            /**
             * Set to `true` to allow clients outside of the VPC to connect to the database using a public IP address. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * If the `vpc_id` includes multiple subnets, specify the `subnet_id` you want to use to control access to the database. Use the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation to find and store the `id` of the relevant subnet object.
             *
             * > 📘
             * >
             * > A VPC needs at least one subnet to assign a Manage Database instance.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC you want to use to enable private access to the Managed Database. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation and store the `id` for the applicable VPC.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
         */
        ssl_connection?: boolean;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
    };
    path?: never;
    query?: never;
    url: '/databases/mysql/instances';
};

export type PostDatabasesMysqlInstancesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDatabasesMysqlInstancesError = PostDatabasesMysqlInstancesErrors[keyof PostDatabasesMysqlInstancesErrors];

export type PostDatabasesMysqlInstancesResponses = {
    /**
     * Managed MySQL Databases object.
     */
    200: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size: 1 | 2 | 3;
        /**
         * __Read-only__ When this Managed Database was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
         */
        readonly encrypted: boolean;
        /**
         * __Filterable__, __Read-only__ The Managed Database engine type.
         */
        readonly engine: string;
        /**
         * Advanced parameters you can apply to a MySQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/mysql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a MySQL Managed Database. You can also run the [List MySQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * The minimum amount of time in seconds to keep `binlog` entries before deletion. This may be extended for services that require `binlog` entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
             */
            binlog_retention_period: number;
            /**
             * MySQL-specific advanced configuration parameters.
             */
            mysql: {
                /**
                 * The number of seconds that the `mysqld` server waits for a connect packet before responding with bad handshake.
                 */
                connect_timeout: number;
                /**
                 * Default server time zone as an offset from UTC (from -12:00 to +12:00), a time zone name, or `SYSTEM` to use the MySQL server default.
                 */
                default_time_zone: string;
                /**
                 * The maximum permitted result length in bytes for the `GROUP_CONCAT()` function.
                 */
                group_concat_max_len: number;
                /**
                 * The time, in seconds, before cached statistics expire.
                 */
                information_schema_stats_expiry: number;
                /**
                 * Maximum size for the InnoDB change buffer, as a percentage of the total size of the buffer pool.
                 */
                innodb_change_buffer_max_size: number;
                /**
                 * Specifies whether flushing a page from the InnoDB buffer pool also flushes other dirty pages in the same extent: `0` - dirty pages in the same extent are not flushed, `1` - flush contiguous dirty pages in the same extent, `2` - flush dirty pages in the same extent.
                 */
                innodb_flush_neighbors: number;
                /**
                 * Minimum length of words that are stored in an InnoDB `-1FULLTEXT` index. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_ft_min_token_size: number;
                /**
                 * This option is used to specify your own InnoDB `FULLTEXT` index `stopword` list for all InnoDB tables. Set to `null` for no value.
                 */
                innodb_ft_server_stopword_table: string | null;
                /**
                 * The length of time in seconds an InnoDB transaction waits for a row lock before giving up.
                 */
                innodb_lock_wait_timeout: number;
                /**
                 * The size in bytes of the buffer that InnoDB uses to write to the log files on disk.
                 */
                innodb_log_buffer_size: number;
                /**
                 * The upper limit in bytes on the size of the temporary log files used during online DDL operations for InnoDB tables.
                 */
                innodb_online_alter_log_max_size: number;
                /**
                 * The number of I/O threads for read operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_read_io_threads: number;
                /**
                 * When enabled a transaction timeout causes InnoDB to abort and roll back the entire transaction. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_rollback_on_timeout: boolean;
                /**
                 * Defines the maximum number of threads permitted inside of InnoDB. The default is `0` which indicates infinite concurrency, or no limit.
                 */
                innodb_thread_concurrency: number;
                /**
                 * The number of I/O threads for write operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_write_io_threads: number;
                /**
                 * The number of seconds the server waits for activity on an interactive connection before closing it.
                 */
                interactive_timeout: number;
                /**
                 * The storage engine for in-memory internal temporary tables.
                 */
                internal_tmp_mem_storage_engine: 'TempTable' | 'MEMORY';
                /**
                 * Size of the largest message in bytes that can be received by the server.
                 */
                max_allowed_packet: number;
                /**
                 * Limits the size of internal in-memory tables. Also set `tmp_table_size`.
                 */
                max_heap_table_size: number;
                /**
                 * Start sizes of connection buffer and result buffer. Changing this parameter will lead to a restart of the MySQL service.
                 */
                net_buffer_length: number;
                /**
                 * The number of seconds to wait for more data from a connection before aborting the read.
                 */
                net_read_timeout: number;
                /**
                 * The number of seconds to wait for a block to be written to a connection before aborting the write.
                 */
                net_write_timeout: number;
                /**
                 * Global SQL mode. Set to empty to use MySQL server defaults. When creating a new service and not setting this field, Akamai defaults to SQL mode which is strict, SQL standard compliant.
                 */
                sql_mode: string;
                /**
                 * Require primary key to be defined for new tables or old tables modified with `ALTER TABLE` and fail if missing. You should always have primary keys because various functionality may break if any large table is missing them.
                 */
                sql_require_primary_key: boolean;
                /**
                 * Limits the size of internal in-memory tables. This also requires a `max_heap_table_size`.
                 */
                tmp_table_size: number;
                /**
                 * The number of seconds the server waits for activity on a non-interactive connection before closing it.
                 */
                wait_timeout: number;
            };
        };
        /**
         * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
         */
        fork: {
            /**
             * The database timestamp from which it was restored. This is _not_ when the fork was created.
             */
            restore_time: string;
            /**
             * The instance id of the database that was forked from.
             */
            source: number;
        };
        /**
         * __Read-only__ The primary hostname and secondary read-only hostname for the Managed Database. The API assigns these hostnames after it successfully creates the Managed Database.
         */
        readonly hosts: {
            /**
             * The primary hostname for the Managed Database.
             *
             * > 📘
             * >
             * > - If you've configured the Managed Database to use a Virtual Private Cloud (VPC) via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC.
             * >
             * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns _publicly_ routable IPv4 and IPv6 addresses.
             */
            primary: string | null;
            /**
             * The secondary, read-only hostname for the Managed Database. This can only be used to access a Managed Database cluster from Linodes in the same data center (region). Accessing the cluster through this hostname doesn't incur transfer costs.
             *
             * > 📘
             * >
             * > - The secondary hostname is publicly visible and accessible.
             * >
             * > - If you've configured the Managed Database to use a VPC via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC. You can also use this hostname for requests between clients within the same VPC to access the cluster over the private network, which avoids transfer costs.
             * >
             * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns publicly routable IPv4 and IPv6 addresses.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
         */
        readonly id: number;
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
         */
        readonly members: {
            [key: string]: unknown;
        };
        /**
         * __Read-only__ The oldest time to which a database can be restored.
         */
        readonly oldest_restore_time: string;
        /**
         * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
         */
        platform: 'rdbms-legacy' | 'rdbms-default';
        /**
         * __Read-only__ The access port for this Managed Database.
         */
        readonly port: number;
        /**
         * __Beta__ Restricts access to a MySQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
         */
        private_network: {
            /**
             * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * The unique identifier of a specific subnet, in the VPC (`vpc_id`) where the Managed Database can be accessed.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC where this database exists.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
         */
        ssl_connection: boolean;
        /**
         * __Filterable__, __Read-only__ The operating status of the Managed Database.
         */
        status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
        /**
         * __Read-only__ The total disk size of the database, in GB.
         */
        readonly total_disk_size_gb: number;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
        /**
         * __Read-only__ When this Managed Database was last updated.
         */
        readonly updated: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Read-only__ The amount of space currently in use in the database, in GB.
         */
        readonly used_disk_size_gb: number;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type PostDatabasesMysqlInstancesResponse = PostDatabasesMysqlInstancesResponses[keyof PostDatabasesMysqlInstancesResponses];

export type DeleteDatabasesMysqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}';
};

export type DeleteDatabasesMysqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteDatabasesMysqlInstanceError = DeleteDatabasesMysqlInstanceErrors[keyof DeleteDatabasesMysqlInstanceErrors];

export type DeleteDatabasesMysqlInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteDatabasesMysqlInstanceResponse = DeleteDatabasesMysqlInstanceResponses[keyof DeleteDatabasesMysqlInstanceResponses];

export type GetDatabasesMysqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}';
};

export type GetDatabasesMysqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesMysqlInstanceError = GetDatabasesMysqlInstanceErrors[keyof GetDatabasesMysqlInstanceErrors];

export type GetDatabasesMysqlInstanceResponses = {
    /**
     * Managed MySQL Databases object.
     */
    200: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size: 1 | 2 | 3;
        /**
         * __Read-only__ When this Managed Database was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
         */
        readonly encrypted: boolean;
        /**
         * __Filterable__, __Read-only__ The Managed Database engine type.
         */
        readonly engine: string;
        /**
         * Advanced parameters you can apply to a MySQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/mysql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a MySQL Managed Database. You can also run the [List MySQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * The minimum amount of time in seconds to keep `binlog` entries before deletion. This may be extended for services that require `binlog` entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
             */
            binlog_retention_period: number;
            /**
             * MySQL-specific advanced configuration parameters.
             */
            mysql: {
                /**
                 * The number of seconds that the `mysqld` server waits for a connect packet before responding with bad handshake.
                 */
                connect_timeout: number;
                /**
                 * Default server time zone as an offset from UTC (from -12:00 to +12:00), a time zone name, or `SYSTEM` to use the MySQL server default.
                 */
                default_time_zone: string;
                /**
                 * The maximum permitted result length in bytes for the `GROUP_CONCAT()` function.
                 */
                group_concat_max_len: number;
                /**
                 * The time, in seconds, before cached statistics expire.
                 */
                information_schema_stats_expiry: number;
                /**
                 * Maximum size for the InnoDB change buffer, as a percentage of the total size of the buffer pool.
                 */
                innodb_change_buffer_max_size: number;
                /**
                 * Specifies whether flushing a page from the InnoDB buffer pool also flushes other dirty pages in the same extent: `0` - dirty pages in the same extent are not flushed, `1` - flush contiguous dirty pages in the same extent, `2` - flush dirty pages in the same extent.
                 */
                innodb_flush_neighbors: number;
                /**
                 * Minimum length of words that are stored in an InnoDB `-1FULLTEXT` index. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_ft_min_token_size: number;
                /**
                 * This option is used to specify your own InnoDB `FULLTEXT` index `stopword` list for all InnoDB tables. Set to `null` for no value.
                 */
                innodb_ft_server_stopword_table: string | null;
                /**
                 * The length of time in seconds an InnoDB transaction waits for a row lock before giving up.
                 */
                innodb_lock_wait_timeout: number;
                /**
                 * The size in bytes of the buffer that InnoDB uses to write to the log files on disk.
                 */
                innodb_log_buffer_size: number;
                /**
                 * The upper limit in bytes on the size of the temporary log files used during online DDL operations for InnoDB tables.
                 */
                innodb_online_alter_log_max_size: number;
                /**
                 * The number of I/O threads for read operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_read_io_threads: number;
                /**
                 * When enabled a transaction timeout causes InnoDB to abort and roll back the entire transaction. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_rollback_on_timeout: boolean;
                /**
                 * Defines the maximum number of threads permitted inside of InnoDB. The default is `0` which indicates infinite concurrency, or no limit.
                 */
                innodb_thread_concurrency: number;
                /**
                 * The number of I/O threads for write operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_write_io_threads: number;
                /**
                 * The number of seconds the server waits for activity on an interactive connection before closing it.
                 */
                interactive_timeout: number;
                /**
                 * The storage engine for in-memory internal temporary tables.
                 */
                internal_tmp_mem_storage_engine: 'TempTable' | 'MEMORY';
                /**
                 * Size of the largest message in bytes that can be received by the server.
                 */
                max_allowed_packet: number;
                /**
                 * Limits the size of internal in-memory tables. Also set `tmp_table_size`.
                 */
                max_heap_table_size: number;
                /**
                 * Start sizes of connection buffer and result buffer. Changing this parameter will lead to a restart of the MySQL service.
                 */
                net_buffer_length: number;
                /**
                 * The number of seconds to wait for more data from a connection before aborting the read.
                 */
                net_read_timeout: number;
                /**
                 * The number of seconds to wait for a block to be written to a connection before aborting the write.
                 */
                net_write_timeout: number;
                /**
                 * Global SQL mode. Set to empty to use MySQL server defaults. When creating a new service and not setting this field, Akamai defaults to SQL mode which is strict, SQL standard compliant.
                 */
                sql_mode: string;
                /**
                 * Require primary key to be defined for new tables or old tables modified with `ALTER TABLE` and fail if missing. You should always have primary keys because various functionality may break if any large table is missing them.
                 */
                sql_require_primary_key: boolean;
                /**
                 * Limits the size of internal in-memory tables. This also requires a `max_heap_table_size`.
                 */
                tmp_table_size: number;
                /**
                 * The number of seconds the server waits for activity on a non-interactive connection before closing it.
                 */
                wait_timeout: number;
            };
        };
        /**
         * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
         */
        fork: {
            /**
             * The database timestamp from which it was restored. This is _not_ when the fork was created.
             */
            restore_time: string;
            /**
             * The instance id of the database that was forked from.
             */
            source: number;
        };
        /**
         * __Read-only__ The primary hostname and secondary read-only hostname for the Managed Database. The API assigns these hostnames after it successfully creates the Managed Database.
         */
        readonly hosts: {
            /**
             * The primary hostname for the Managed Database.
             *
             * > 📘
             * >
             * > - If you've configured the Managed Database to use a Virtual Private Cloud (VPC) via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC.
             * >
             * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns _publicly_ routable IPv4 and IPv6 addresses.
             */
            primary: string | null;
            /**
             * The secondary, read-only hostname for the Managed Database. This can only be used to access a Managed Database cluster from Linodes in the same data center (region). Accessing the cluster through this hostname doesn't incur transfer costs.
             *
             * > 📘
             * >
             * > - The secondary hostname is publicly visible and accessible.
             * >
             * > - If you've configured the Managed Database to use a VPC via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC. You can also use this hostname for requests between clients within the same VPC to access the cluster over the private network, which avoids transfer costs.
             * >
             * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns publicly routable IPv4 and IPv6 addresses.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
         */
        readonly id: number;
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
         */
        readonly members: {
            [key: string]: unknown;
        };
        /**
         * __Read-only__ The oldest time to which a database can be restored.
         */
        readonly oldest_restore_time: string;
        /**
         * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
         */
        platform: 'rdbms-legacy' | 'rdbms-default';
        /**
         * __Read-only__ The access port for this Managed Database.
         */
        readonly port: number;
        /**
         * __Beta__ Restricts access to a MySQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
         */
        private_network: {
            /**
             * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * The unique identifier of a specific subnet, in the VPC (`vpc_id`) where the Managed Database can be accessed.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC where this database exists.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
         */
        ssl_connection: boolean;
        /**
         * __Filterable__, __Read-only__ The operating status of the Managed Database.
         */
        status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
        /**
         * __Read-only__ The total disk size of the database, in GB.
         */
        readonly total_disk_size_gb: number;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
        /**
         * __Read-only__ When this Managed Database was last updated.
         */
        readonly updated: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Read-only__ The amount of space currently in use in the database, in GB.
         */
        readonly used_disk_size_gb: number;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type GetDatabasesMysqlInstanceResponse = GetDatabasesMysqlInstanceResponses[keyof GetDatabasesMysqlInstanceResponses];

export type PutDatabasesMysqlInstanceData = {
    /**
     * Updated information for the Managed MySQL Database.
     */
    body: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * Advanced parameters you can apply to a MySQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/mysql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a MySQL Managed Database. You can also run the [List MySQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * The minimum amount of time in seconds to keep `binlog` entries before deletion. This may be extended for services that require `binlog` entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
             */
            binlog_retention_period: number;
            /**
             * MySQL-specific advanced configuration parameters.
             */
            mysql: {
                /**
                 * The number of seconds that the `mysqld` server waits for a connect packet before responding with bad handshake.
                 */
                connect_timeout: number;
                /**
                 * Default server time zone as an offset from UTC (from -12:00 to +12:00), a time zone name, or `SYSTEM` to use the MySQL server default.
                 */
                default_time_zone: string;
                /**
                 * The maximum permitted result length in bytes for the `GROUP_CONCAT()` function.
                 */
                group_concat_max_len: number;
                /**
                 * The time, in seconds, before cached statistics expire.
                 */
                information_schema_stats_expiry: number;
                /**
                 * Maximum size for the InnoDB change buffer, as a percentage of the total size of the buffer pool.
                 */
                innodb_change_buffer_max_size: number;
                /**
                 * Specifies whether flushing a page from the InnoDB buffer pool also flushes other dirty pages in the same extent: `0` - dirty pages in the same extent are not flushed, `1` - flush contiguous dirty pages in the same extent, `2` - flush dirty pages in the same extent.
                 */
                innodb_flush_neighbors: number;
                /**
                 * Minimum length of words that are stored in an InnoDB `-1FULLTEXT` index. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_ft_min_token_size: number;
                /**
                 * This option is used to specify your own InnoDB `FULLTEXT` index `stopword` list for all InnoDB tables. Set to `null` for no value.
                 */
                innodb_ft_server_stopword_table: string | null;
                /**
                 * The length of time in seconds an InnoDB transaction waits for a row lock before giving up.
                 */
                innodb_lock_wait_timeout: number;
                /**
                 * The size in bytes of the buffer that InnoDB uses to write to the log files on disk.
                 */
                innodb_log_buffer_size: number;
                /**
                 * The upper limit in bytes on the size of the temporary log files used during online DDL operations for InnoDB tables.
                 */
                innodb_online_alter_log_max_size: number;
                /**
                 * The number of I/O threads for read operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_read_io_threads: number;
                /**
                 * When enabled a transaction timeout causes InnoDB to abort and roll back the entire transaction. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_rollback_on_timeout: boolean;
                /**
                 * Defines the maximum number of threads permitted inside of InnoDB. The default is `0` which indicates infinite concurrency, or no limit.
                 */
                innodb_thread_concurrency: number;
                /**
                 * The number of I/O threads for write operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_write_io_threads: number;
                /**
                 * The number of seconds the server waits for activity on an interactive connection before closing it.
                 */
                interactive_timeout: number;
                /**
                 * The storage engine for in-memory internal temporary tables.
                 */
                internal_tmp_mem_storage_engine: 'TempTable' | 'MEMORY';
                /**
                 * Size of the largest message in bytes that can be received by the server.
                 */
                max_allowed_packet: number;
                /**
                 * Limits the size of internal in-memory tables. Also set `tmp_table_size`.
                 */
                max_heap_table_size: number;
                /**
                 * Start sizes of connection buffer and result buffer. Changing this parameter will lead to a restart of the MySQL service.
                 */
                net_buffer_length: number;
                /**
                 * The number of seconds to wait for more data from a connection before aborting the read.
                 */
                net_read_timeout: number;
                /**
                 * The number of seconds to wait for a block to be written to a connection before aborting the write.
                 */
                net_write_timeout: number;
                /**
                 * Global SQL mode. Set to empty to use MySQL server defaults. When creating a new service and not setting this field, Akamai defaults to SQL mode which is strict, SQL standard compliant.
                 */
                sql_mode: string;
                /**
                 * Require primary key to be defined for new tables or old tables modified with `ALTER TABLE` and fail if missing. You should always have primary keys because various functionality may break if any large table is missing them.
                 */
                sql_require_primary_key: boolean;
                /**
                 * Limits the size of internal in-memory tables. This also requires a `max_heap_table_size`.
                 */
                tmp_table_size: number;
                /**
                 * The number of seconds the server waits for activity on a non-interactive connection before closing it.
                 */
                wait_timeout: number;
            };
        };
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Beta__ Restricts access on a MySQL Managed Database to a specific Virtual Private Cloud (VPC) configured for the cluster.
         */
        private_network: {
            /**
             * Set to `true` to allow clients outside of the VPC to connect to the database using a public IP address. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * If the `vpc_id` includes multiple subnets, specify the `subnet_id` you want to use to control access to the database. Use the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation to find and store the `id` of the relevant subnet object.
             *
             * > 📘
             * >
             * > A VPC needs at least one subnet to assign a Manage Database instance.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC you want to use to enable private access to the Managed Database. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation and store the `id` for the applicable VPC.
             */
            vpc_id: number;
        } | null;
        /**
         * Request re-sizing of your cluster to a Linode Type with more disk space. For example, you could request a Linode Type that uses a higher plan.
         *
         * - Needs to be a Linode Type with more disk space than your current Linode.
         *
         * - Resizing to a larger Linode Type can accrue additional cost. Review the `price` output in the [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types) operation for more information.
         *
         * - You can't update the `allow_list` and set a new `type` in the same request.
         *
         * - Any active updates to your cluster need to complete before you can request a resize. The reverse is also true: An active resizing needs to complete before you can perform any other update.
         */
        type: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}';
};

export type PutDatabasesMysqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutDatabasesMysqlInstanceError = PutDatabasesMysqlInstanceErrors[keyof PutDatabasesMysqlInstanceErrors];

export type PutDatabasesMysqlInstanceResponses = {
    /**
     * Managed MySQL Databases object.
     */
    200: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size: 1 | 2 | 3;
        /**
         * __Read-only__ When this Managed Database was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
         */
        readonly encrypted: boolean;
        /**
         * __Filterable__, __Read-only__ The Managed Database engine type.
         */
        readonly engine: string;
        /**
         * Advanced parameters you can apply to a MySQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/mysql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a MySQL Managed Database. You can also run the [List MySQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * The minimum amount of time in seconds to keep `binlog` entries before deletion. This may be extended for services that require `binlog` entries for longer than the default, for example if using the MySQL Debezium Kafka connector.
             */
            binlog_retention_period: number;
            /**
             * MySQL-specific advanced configuration parameters.
             */
            mysql: {
                /**
                 * The number of seconds that the `mysqld` server waits for a connect packet before responding with bad handshake.
                 */
                connect_timeout: number;
                /**
                 * Default server time zone as an offset from UTC (from -12:00 to +12:00), a time zone name, or `SYSTEM` to use the MySQL server default.
                 */
                default_time_zone: string;
                /**
                 * The maximum permitted result length in bytes for the `GROUP_CONCAT()` function.
                 */
                group_concat_max_len: number;
                /**
                 * The time, in seconds, before cached statistics expire.
                 */
                information_schema_stats_expiry: number;
                /**
                 * Maximum size for the InnoDB change buffer, as a percentage of the total size of the buffer pool.
                 */
                innodb_change_buffer_max_size: number;
                /**
                 * Specifies whether flushing a page from the InnoDB buffer pool also flushes other dirty pages in the same extent: `0` - dirty pages in the same extent are not flushed, `1` - flush contiguous dirty pages in the same extent, `2` - flush dirty pages in the same extent.
                 */
                innodb_flush_neighbors: number;
                /**
                 * Minimum length of words that are stored in an InnoDB `-1FULLTEXT` index. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_ft_min_token_size: number;
                /**
                 * This option is used to specify your own InnoDB `FULLTEXT` index `stopword` list for all InnoDB tables. Set to `null` for no value.
                 */
                innodb_ft_server_stopword_table: string | null;
                /**
                 * The length of time in seconds an InnoDB transaction waits for a row lock before giving up.
                 */
                innodb_lock_wait_timeout: number;
                /**
                 * The size in bytes of the buffer that InnoDB uses to write to the log files on disk.
                 */
                innodb_log_buffer_size: number;
                /**
                 * The upper limit in bytes on the size of the temporary log files used during online DDL operations for InnoDB tables.
                 */
                innodb_online_alter_log_max_size: number;
                /**
                 * The number of I/O threads for read operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_read_io_threads: number;
                /**
                 * When enabled a transaction timeout causes InnoDB to abort and roll back the entire transaction. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_rollback_on_timeout: boolean;
                /**
                 * Defines the maximum number of threads permitted inside of InnoDB. The default is `0` which indicates infinite concurrency, or no limit.
                 */
                innodb_thread_concurrency: number;
                /**
                 * The number of I/O threads for write operations in InnoDB. Changing this parameter will lead to a restart of the MySQL service.
                 */
                innodb_write_io_threads: number;
                /**
                 * The number of seconds the server waits for activity on an interactive connection before closing it.
                 */
                interactive_timeout: number;
                /**
                 * The storage engine for in-memory internal temporary tables.
                 */
                internal_tmp_mem_storage_engine: 'TempTable' | 'MEMORY';
                /**
                 * Size of the largest message in bytes that can be received by the server.
                 */
                max_allowed_packet: number;
                /**
                 * Limits the size of internal in-memory tables. Also set `tmp_table_size`.
                 */
                max_heap_table_size: number;
                /**
                 * Start sizes of connection buffer and result buffer. Changing this parameter will lead to a restart of the MySQL service.
                 */
                net_buffer_length: number;
                /**
                 * The number of seconds to wait for more data from a connection before aborting the read.
                 */
                net_read_timeout: number;
                /**
                 * The number of seconds to wait for a block to be written to a connection before aborting the write.
                 */
                net_write_timeout: number;
                /**
                 * Global SQL mode. Set to empty to use MySQL server defaults. When creating a new service and not setting this field, Akamai defaults to SQL mode which is strict, SQL standard compliant.
                 */
                sql_mode: string;
                /**
                 * Require primary key to be defined for new tables or old tables modified with `ALTER TABLE` and fail if missing. You should always have primary keys because various functionality may break if any large table is missing them.
                 */
                sql_require_primary_key: boolean;
                /**
                 * Limits the size of internal in-memory tables. This also requires a `max_heap_table_size`.
                 */
                tmp_table_size: number;
                /**
                 * The number of seconds the server waits for activity on a non-interactive connection before closing it.
                 */
                wait_timeout: number;
            };
        };
        /**
         * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
         */
        fork: {
            /**
             * The database timestamp from which it was restored. This is _not_ when the fork was created.
             */
            restore_time: string;
            /**
             * The instance id of the database that was forked from.
             */
            source: number;
        };
        /**
         * __Read-only__ The primary hostname and secondary read-only hostname for the Managed Database. The API assigns these hostnames after it successfully creates the Managed Database.
         */
        readonly hosts: {
            /**
             * The primary hostname for the Managed Database.
             *
             * > 📘
             * >
             * > - If you've configured the Managed Database to use a Virtual Private Cloud (VPC) via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC.
             * >
             * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns _publicly_ routable IPv4 and IPv6 addresses.
             */
            primary: string | null;
            /**
             * The secondary, read-only hostname for the Managed Database. This can only be used to access a Managed Database cluster from Linodes in the same data center (region). Accessing the cluster through this hostname doesn't incur transfer costs.
             *
             * > 📘
             * >
             * > - The secondary hostname is publicly visible and accessible.
             * >
             * > - If you've configured the Managed Database to use a VPC via the `private_network` object ([MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) / [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances)), this hostname resolves to a private IPv4 address within that VPC subnet's address range. Currently, IPv6 addresses aren't supported in a VPC. You can also use this hostname for requests between clients within the same VPC to access the cluster over the private network, which avoids transfer costs.
             * >
             * > - With `public_access` enabled in a `private_network`, a public version of this hostname is also available. Resolving this hostname returns publicly routable IPv4 and IPv6 addresses.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
         */
        readonly id: number;
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
         */
        readonly members: {
            [key: string]: unknown;
        };
        /**
         * __Read-only__ The oldest time to which a database can be restored.
         */
        readonly oldest_restore_time: string;
        /**
         * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
         */
        platform: 'rdbms-legacy' | 'rdbms-default';
        /**
         * __Read-only__ The access port for this Managed Database.
         */
        readonly port: number;
        /**
         * __Beta__ Restricts access to a MySQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
         */
        private_network: {
            /**
             * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * The unique identifier of a specific subnet, in the VPC (`vpc_id`) where the Managed Database can be accessed.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC where this database exists.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed MySQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-mysql-instance-credentials) operation for access information.
         */
        ssl_connection: boolean;
        /**
         * __Filterable__, __Read-only__ The operating status of the Managed Database.
         */
        status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
        /**
         * __Read-only__ The total disk size of the database, in GB.
         */
        readonly total_disk_size_gb: number;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
        /**
         * __Read-only__ When this Managed Database was last updated.
         */
        readonly updated: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Read-only__ The amount of space currently in use in the database, in GB.
         */
        readonly used_disk_size_gb: number;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type PutDatabasesMysqlInstanceResponse = PutDatabasesMysqlInstanceResponses[keyof PutDatabasesMysqlInstanceResponses];

export type GetDatabasesMysqlInstanceCredentialsData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}/credentials';
};

export type GetDatabasesMysqlInstanceCredentialsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesMysqlInstanceCredentialsError = GetDatabasesMysqlInstanceCredentialsErrors[keyof GetDatabasesMysqlInstanceCredentialsErrors];

export type GetDatabasesMysqlInstanceCredentialsResponses = {
    /**
     * Managed Database object for database credentials.
     */
    200: {
        /**
         * __Read-only__ The randomly generated root password for the Managed Database instance.
         */
        readonly password: string;
        /**
         * __Read-only__ The root username for the Managed Database instance.
         */
        readonly username: string;
    };
};

export type GetDatabasesMysqlInstanceCredentialsResponse = GetDatabasesMysqlInstanceCredentialsResponses[keyof GetDatabasesMysqlInstanceCredentialsResponses];

export type PostDatabasesMysqlInstanceCredentialsResetData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed MySQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}/credentials/reset';
};

export type PostDatabasesMysqlInstanceCredentialsResetErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDatabasesMysqlInstanceCredentialsResetError = PostDatabasesMysqlInstanceCredentialsResetErrors[keyof PostDatabasesMysqlInstanceCredentialsResetErrors];

export type PostDatabasesMysqlInstanceCredentialsResetResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostDatabasesMysqlInstanceCredentialsResetResponse = PostDatabasesMysqlInstanceCredentialsResetResponses[keyof PostDatabasesMysqlInstanceCredentialsResetResponses];

export type PostDatabasesMysqlInstancePatchData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}/patch';
};

export type PostDatabasesMysqlInstancePatchErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDatabasesMysqlInstancePatchError = PostDatabasesMysqlInstancePatchErrors[keyof PostDatabasesMysqlInstancePatchErrors];

export type PostDatabasesMysqlInstancePatchResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostDatabasesMysqlInstancePatchResponse = PostDatabasesMysqlInstancePatchResponses[keyof PostDatabasesMysqlInstancePatchResponses];

export type ResumeDatabasesMysqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}/resume';
};

export type ResumeDatabasesMysqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type ResumeDatabasesMysqlInstanceError = ResumeDatabasesMysqlInstanceErrors[keyof ResumeDatabasesMysqlInstanceErrors];

export type ResumeDatabasesMysqlInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type ResumeDatabasesMysqlInstanceResponse = ResumeDatabasesMysqlInstanceResponses[keyof ResumeDatabasesMysqlInstanceResponses];

export type GetDatabasesMysqlInstanceSslData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed MySQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}/ssl';
};

export type GetDatabasesMysqlInstanceSslErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesMysqlInstanceSslError = GetDatabasesMysqlInstanceSslErrors[keyof GetDatabasesMysqlInstanceSslErrors];

export type GetDatabasesMysqlInstanceSslResponses = {
    /**
     * Managed Database SSL object.
     */
    200: {
        /**
         * The base64-encoded SSL CA certificate for the Managed Database instance.
         */
        ca_certificate: string;
    };
};

export type GetDatabasesMysqlInstanceSslResponse = GetDatabasesMysqlInstanceSslResponses[keyof GetDatabasesMysqlInstanceSslResponses];

export type SuspendDatabasesMysqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/mysql/instances/{instanceId}/suspend';
};

export type SuspendDatabasesMysqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type SuspendDatabasesMysqlInstanceError = SuspendDatabasesMysqlInstanceErrors[keyof SuspendDatabasesMysqlInstanceErrors];

export type SuspendDatabasesMysqlInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type SuspendDatabasesMysqlInstanceResponse = SuspendDatabasesMysqlInstanceResponses[keyof SuspendDatabasesMysqlInstanceResponses];

export type GetDatabasesPostgresqlConfigData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/databases/postgresql/config';
};

export type GetDatabasesPostgresqlConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesPostgresqlConfigError = GetDatabasesPostgresqlConfigErrors[keyof GetDatabasesPostgresqlConfigErrors];

export type GetDatabasesPostgresqlConfigResponses = {
    /**
     * Available [advanced parameters](https://aiven.io/docs/products/postgresql/reference/advanced-params) for a PostgreSQL Managed Database.
     */
    200: {
        /**
         * Configuration values available for a postgresql.conf.
         */
        pg: {
            /**
             * Settings available to configure the `autovacuum_analyze_scale_factor`, per Aiven's specifications.
             */
            autovacuum_analyze_scale_factor: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_analyze_threshold`, per Aiven's specifications.
             */
            autovacuum_analyze_threshold: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_max_workers`, per Aiven's specifications.
             */
            autovacuum_max_workers: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_naptime`, per Aiven's specifications.
             */
            autovacuum_naptime: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_vacuum_cost_delay`, per Aiven's specifications.
             */
            autovacuum_vacuum_cost_delay: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_vacuum_cost_limit`, per Aiven's specifications.
             */
            autovacuum_vacuum_cost_limit: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_vacuum_scale_factor`, per Aiven's specifications.
             */
            autovacuum_vacuum_scale_factor: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `autovacuum_vacuum_threshold`, per Aiven's specifications.
             */
            autovacuum_vacuum_threshold: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `bgwriter_delay`, per Aiven's specifications.
             */
            bgwriter_delay: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `bgwriter_flush_after`, per Aiven's specifications.
             */
            bgwriter_flush_after: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure `bgwriter_lru_maxpages`, per Aiven's specifications.
             */
            bgwriter_lru_maxpages: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `bgwriter_lru_multiplier`, per Aiven's specifications.
             */
            bgwriter_lru_multiplier: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `deadlock_timeout`, per Aiven's specifications.
             */
            deadlock_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `default_toast_compression`, per Aiven's specifications.
             */
            default_toast_compression: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `idle_in_transaction_session_timeout`, per Aiven's specifications.
             */
            idle_in_transaction_session_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Parameter used to enable the Just-In-Time (`jit`) compilation, per Aiven's specifications.
             */
            jit: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example boolean value for this parameter.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_files_per_process`, per Aiven's specifications.
             */
            max_files_per_process: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_locks_per_transaction`, per Aiven's specifications.
             */
            max_locks_per_transaction: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_logical_replication_workers`, per Aiven's specifications.
             */
            max_logical_replication_workers: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_parallel_workers`, per Aiven's specifications.
             */
            max_parallel_workers: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_parallel_workers_per_gather`, per Aiven's specifications.
             */
            max_parallel_workers_per_gather: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_pred_locks_per_transaction`, per Aiven's specifications.
             */
            max_pred_locks_per_transaction: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_replication_slots`, per Aiven's specifications.
             */
            max_replication_slots: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_slot_wal_keep_size`, per Aiven's specifications.
             */
            max_slot_wal_keep_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_stack_depth`, per Aiven's specifications.
             */
            max_stack_depth: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_standby_archive_delay`, per Aiven's specifications.
             */
            max_standby_archive_delay: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_standby_streaming_delay`, per Aiven's specifications.
             */
            max_standby_streaming_delay: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_wal_senders`, per Aiven's specifications.
             */
            max_wal_senders: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `max_worker_processes`, per Aiven's specifications.
             */
            max_worker_processes: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure `password_encryption`, per Aiven's specifications.
             */
            password_encryption: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `pg_partman_bgw.interval`, per Aiven's specifications.
             */
            'pg_partman_bgw.interval': {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `pg_partman_bgw.role`, per Aiven's specifications.
             */
            'pg_partman_bgw.role': {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter, using the required `pattern`.
                 */
                example: string;
                /**
                 * The maximum character length for this parameter.
                 */
                maxLength: number;
                /**
                 * The pattern to follow when defining this parameter.
                 */
                pattern: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Parameter used to enable query plan monitoring, per Aiven's specifications.
             */
            'pg_stat_monitor.pgsm_enable_query_plan': {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example boolean value for this parameter.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure `pg_stat_monitor.pgsm_max_buckets`, per Aiven's specifications.
             */
            'pg_stat_monitor.pgsm_max_buckets': {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure `password_encryption`, per Aiven's specifications.
             */
            'pg_stat_statements.track': {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: Array<number>;
            };
            /**
             * Settings available to configure the `temp_file_limit`, per Aiven's specifications.
             */
            temp_file_limit: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the PostgreSQL service time zone, per Aiven's specifications.
             */
            timezone: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter, using the required `pattern`.
                 */
                example: string;
                /**
                 * The maximum character length for this parameter.
                 */
                maxLength: number;
                /**
                 * The pattern to follow when defining this parameter.
                 */
                pattern: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `track_activity_query_size`, per Aiven's specifications.
             */
            track_activity_query_size: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `track_commit_timestamp`, per Aiven's specifications.
             */
            track_commit_timestamp: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure `track_functions`, per Aiven's specifications.
             */
            track_functions: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to enable `track_io_timing`, per Aiven's specifications.
             */
            track_io_timing: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * Specific values available for use as this parameter.
                 */
                enum: string;
                /**
                 * One of the `enum` values available for use.
                 */
                example: string;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `wal_sender_timeout`, per Aiven's specifications.
             */
            wal_sender_timeout: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
            /**
             * Settings available to configure the `wal_writer_delay`, per Aiven's specifications.
             */
            wal_writer_delay: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
        };
        /**
         * Parameter used to enable the `pg_stat_monitor` extension for a PostgreSQL cluster, per Aiven's specifications.
         */
        pg_stat_monitor_enable: {
            /**
             * The description for this parameter.
             */
            description: string;
            /**
             * Whether a change to this parameter requires a restart of the PostgreSQL database.
             */
            requires_restart: boolean;
            /**
             * The format of this object.
             */
            type: string;
        };
        /**
         * Parameter used to apply PGLookout settings, per Aiven's specifications.
         */
        pglookout: {
            /**
             * Settings available to configure the `max_failover_replication_time_lag`, per Aiven's specifications.
             */
            max_failover_replication_time_lag: {
                /**
                 * The description for this parameter.
                 */
                description: string;
                /**
                 * An example value for this parameter.
                 */
                example: number;
                /**
                 * The maximum value allowed for this parameter.
                 */
                maximum: number;
                /**
                 * The minimum value allowed for this parameter.
                 */
                minimum: number;
                /**
                 * Whether a change to this parameter requires a restart of the PostgreSQL database.
                 */
                requires_restart: boolean;
                /**
                 * The format of this object.
                 */
                type: string;
            };
        };
        /**
         * Parameters used to set up the `shared_buffers_percentage`, per Aiven's specifications.
         */
        shared_buffers_percentage: {
            /**
             * The description for this parameter.
             */
            description: string;
            /**
             * An example value for this parameter.
             */
            example: number;
            /**
             * The maximum value allowed for this parameter.
             */
            maximum: number;
            /**
             * The minimum value allowed for this parameter.
             */
            minimum: number;
            /**
             * Whether a change to this parameter requires a restart of the PostgreSQL database.
             */
            requires_restart: boolean;
            /**
             * The format of this object.
             */
            type: string;
        };
        /**
         * Parameters used to set up `work_mem`, per Aiven's specifications.
         */
        work_mem: {
            /**
             * The description for this parameter.
             */
            description: string;
            /**
             * An example value for this parameter.
             */
            example: number;
            /**
             * The maximum value allowed for this parameter.
             */
            maximum: number;
            /**
             * The minimum value allowed for this parameter.
             */
            minimum: number;
            /**
             * Whether a change to this parameter requires a restart of the PostgreSQL database.
             */
            requires_restart: boolean;
            /**
             * The format of this object.
             */
            type: string;
        };
    };
};

export type GetDatabasesPostgresqlConfigResponse = GetDatabasesPostgresqlConfigResponses[keyof GetDatabasesPostgresqlConfigResponses];

export type GetDatabasesPostgreSqlInstancesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/postgresql/instances';
};

export type GetDatabasesPostgreSqlInstancesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesPostgreSqlInstancesError = GetDatabasesPostgreSqlInstancesErrors[keyof GetDatabasesPostgreSqlInstancesErrors];

export type GetDatabasesPostgreSqlInstancesResponses = {
    /**
     * Returns a paginated list of all accessible PostgreSQL Managed Databases on your account.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * Controls access to the Managed Database.
             *
             * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
             *
             * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
             *
             * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
             */
            allow_list: Array<string>;
            /**
             * The number of Linode instance nodes deployed to the Managed Database.
             *
             * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
             *
             * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
             */
            cluster_size: 1 | 2 | 3;
            /**
             * __Read-only__ When this Managed Database was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
             */
            readonly encrypted: boolean;
            /**
             * __Filterable__, __Read-only__ The Managed Database engine type.
             */
            readonly engine: string;
            /**
             * Advanced parameters you can apply to a PostgreSQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/postgresql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
             *
             * > 📘
             * >
             * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a PostgreSQL Managed Database. You can also run the [List PostgreSQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-config) operation to see an up-to-date list.
             */
            engine_config: {
                /**
                 * PostgreSQL-specific advanced configuration parameters.
                 */
                pg: {
                    /**
                     * Specifies a fraction of the table size to add to `autovacuum_analyze_threshold` when deciding whether to trigger an `ANALYZE`.
                     */
                    autovacuum_analyze_scale_factor: number;
                    /**
                     * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an `ANALYZE` in any one table.
                     */
                    autovacuum_analyze_threshold: number;
                    /**
                     * Specifies the maximum number of `autovacuum` processes, other than the `autovacuum` launcher, that may be running at any one time. This parameter can only be set at server start.
                     */
                    autovacuum_max_workers: number;
                    /**
                     * Specifies the minimum delay between `autovacuum` runs on any given database. The delay is measured in seconds.
                     */
                    autovacuum_naptime: number;
                    /**
                     * Specifies the cost delay value that will be used in automatic `VACUUM` operations. If `-1` is specified, the regular `vacuum_cost_delay` value will be used.
                     */
                    autovacuum_vacuum_cost_delay: number;
                    /**
                     * Specifies the cost limit value that will be used in automatic `VACUUM` operations. The default of `-1` applies the regular `vacuum_cost_limit` value.
                     */
                    autovacuum_vacuum_cost_limit: number;
                    /**
                     * Specifies a fraction of the table size to add to `autovacuum_vacuum_threshold` when deciding whether to trigger a `VACUUM`.
                     */
                    autovacuum_vacuum_scale_factor: number;
                    /**
                     * Specifies the minimum number of updated or deleted tuples needed to trigger a `VACUUM` in any one table.
                     */
                    autovacuum_vacuum_threshold: number;
                    /**
                     * Specifies the delay between activity rounds for the background writer in milliseconds.
                     */
                    bgwriter_delay: number;
                    /**
                     * Whenever more than `bgwriter_flush_after` bytes have been written by the background writer, attempt to force the OS to issue these writes to the underlying storage. Specified in kilobytes. A setting of `0` disables forced writeback.
                     */
                    bgwriter_flush_after: number;
                    /**
                     * In each round, no more than this many buffers will be written by the background writer. Setting this to `0` disables background writing.
                     */
                    bgwriter_lru_maxpages: number;
                    /**
                     * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to `bgwriter_lru_maxpages`). `1.0` represents a `“just` in `time”` policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes.
                     */
                    bgwriter_lru_multiplier: number;
                    /**
                     * This is the amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
                     */
                    deadlock_timeout: number;
                    /**
                     * Specifies the default TOAST compression method for values of compressible columns.
                     */
                    default_toast_compression: 'lz4' | 'pglz';
                    /**
                     * Time out sessions with open transactions after this number of milliseconds.
                     */
                    idle_in_transaction_session_timeout: number;
                    /**
                     * Controls system-wide use of Just-in-Time Compilation (JIT).
                     */
                    jit: boolean;
                    /**
                     * PostgreSQL maximum number of files that can be open per process.
                     */
                    max_files_per_process: number;
                    /**
                     * PostgreSQL maximum locks per transaction.
                     */
                    max_locks_per_transaction: number;
                    /**
                     * PostgreSQL maximum logical replication workers, taken from the pool of `max_parallel_workers`.
                     */
                    max_logical_replication_workers: number;
                    /**
                     * Sets the maximum number of workers that the system can support for parallel queries.
                     */
                    max_parallel_workers: number;
                    /**
                     * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
                     */
                    max_parallel_workers_per_gather: number;
                    /**
                     * PostgreSQL maximum predicate locks per transaction.
                     */
                    max_pred_locks_per_transaction: number;
                    /**
                     * PostgreSQL maximum replication slots.
                     */
                    max_replication_slots: number;
                    /**
                     * PostgreSQL maximum write ahead log (WAL) size in MB, reserved for replication slots. A value of `-1` which indicates unlimited. The `wal_keep_size` minimum write ahead log (WAL) size setting takes precedence over this.
                     */
                    max_slot_wal_keep_size: number;
                    /**
                     * Maximum depth of the stack in bytes.
                     */
                    max_stack_depth: number;
                    /**
                     * Maximum standby archive delay in milliseconds.
                     */
                    max_standby_archive_delay: number;
                    /**
                     * Maximum standby streaming delay in milliseconds.
                     */
                    max_standby_streaming_delay: number;
                    /**
                     * PostgreSQL maximum write ahead log (WAL) senders.
                     */
                    max_wal_senders: number;
                    /**
                     * Maximum number of background processes that the system can support.
                     */
                    max_worker_processes: number;
                    /**
                     * Chooses the algorithm for encrypting passwords.
                     */
                    password_encryption: 'scram-sh-256' | 'md5';
                    /**
                     * Sets the time interval to run `pg_partman` scheduled tasks.
                     */
                    'pg_partman_bgw.interval': number;
                    /**
                     * Controls which role to use for `pg_partman` scheduled background tasks.
                     */
                    'pg_partman_bgw.role': string;
                    /**
                     * Enables query plan monitoring.
                     */
                    'pg_stat_monitor.pgsm_enable_query_plan': boolean;
                    /**
                     * Sets the maximum number of buckets.
                     */
                    'pg_stat_monitor.pgsm_max_buckets': number;
                    /**
                     * Controls which statements are counted. Specify `top` to track top-level statements that are issued directly by clients, `all` to also track nested statements, such as those invoked within functions, or `none` to disable statement statistics collection.
                     */
                    'pg_stat_statements.track': 'all' | 'top' | 'none';
                    /**
                     * PostgreSQL temporary file limit in KB. Set to `-1` for unlimited.
                     */
                    temp_file_limit: number;
                    /**
                     * PostgreSQL service time zone.
                     */
                    timezone: string;
                    /**
                     * Specifies the number of bytes reserved to track the currently executing command for each active session.
                     */
                    track_activity_query_size: number;
                    /**
                     * Record the commit time of transactions.
                     */
                    track_commit_timestamp: 'on' | 'off';
                    /**
                     * Enables tracking of function call counts and time used. Specify `pl` to track only procedural-language functions, `all` to also track SQL and C language functions, or `none` to disable function statistics tracking.
                     */
                    track_functions: 'all' | 'pl' | 'none';
                    /**
                     * Enables timing of database I/O calls. This parameter is `off` by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
                     */
                    track_io_timing: 'on' | 'off';
                    /**
                     * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to `0` disables the timeout.
                     */
                    wal_sender_timeout: number;
                    /**
                     * Write ahead log (WAL) flush interval in milliseconds. A value lower than 200 milliseconds may negatively impact performance.
                     */
                    wal_writer_delay: number;
                };
                /**
                 * Enable the `pg_stat_monitor` extension. When this extension is enabled, PostgreSQL restarts the cluster it's in. Additionally, `pg_stat_statements` results for utility commands are unreliable.
                 */
                pg_stat_monitor_enable: boolean;
                /**
                 * Parameter used to apply PGLookout settings.
                 */
                pglookout: {
                    /**
                     * Number of seconds of master unavailability before triggering database failover to standby.
                     */
                    max_failover_replication_time_lag: number;
                };
                /**
                 * Percentage of total RAM that the database server uses for shared memory buffers. Valid range is 20-60 (float), which corresponds to 20% - 60%. This setting adjusts the `shared_buffers` configuration value.
                 */
                shared_buffers_percentage: number;
                /**
                 * Sets the maximum amount of memory in MB to be used by a query operation, such as a sort or hash table, before writing to temporary disk files. Default is 1MB + 0.075% of total RAM, up to 32 MB.
                 */
                work_mem: number;
            };
            /**
             * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
             */
            fork: {
                /**
                 * The database timestamp from which it was restored. This is _not_ when the fork was created.
                 */
                restore_time: string;
                /**
                 * The instance id of the database that was forked from.
                 */
                source: number;
            };
            /**
             * __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned after provisioning is complete.
             */
            readonly hosts: {
                /**
                 * The primary host for the Managed Database.
                 */
                primary: string | null;
                /**
                 * The secondary/private network host for the Managed Database. A private network host and a private IP can only be used to access a database cluster from Linodes in the same data center and will not incur transfer costs.
                 *
                 * > 📘
                 * >
                 * > The secondary hostname is publicly visible and accessible.
                 */
                secondary: string | null;
            };
            /**
             * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
             */
            readonly id: number;
            /**
             * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
             */
            label: string;
            /**
             * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
             */
            readonly members: {
                [key: string]: unknown;
            };
            /**
             * __Read-only__ The oldest time to which a database can be restored.
             */
            readonly oldest_restore_time: string;
            /**
             * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
             */
            platform: 'rdbms-legacy' | 'rdbms-default';
            /**
             * __Read-only__ The access port for this Managed Database.
             */
            readonly port: number;
            /**
             * __Beta__ Restricts access to a PostgreSQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
             */
            private_network: {
                /**
                 * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
                 *
                 * > 📘
                 * >
                 * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
                 */
                public_access: boolean;
                /**
                 * The specific subnet, in the VPC (`vpc_id`) where the Managed Database cluster can be accessed.
                 */
                subnet_id: number;
                /**
                 * The unique identifier of the VPC where this database exists.
                 */
                vpc_id: number;
            } | null;
            /**
             * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
             */
            region: string;
            /**
             * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
             */
            ssl_connection: boolean;
            /**
             * __Filterable__, __Read-only__ The operating status of the Managed Database.
             */
            status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
            /**
             * __Read-only__ The total disk size of the database, in GB.
             */
            readonly total_disk_size_gb: number;
            /**
             * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
             */
            type: string;
            /**
             * __Read-only__ When this Managed Database was last updated.
             */
            readonly updated: string;
            /**
             * Configuration settings for automated patch update maintenance for the Managed Database.
             */
            updates: {
                /**
                 * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
                 */
                day_of_week: number;
                /**
                 * The maximum maintenance window time in hours.
                 */
                duration: number;
                /**
                 * How frequently maintenance occurs. Currently can only be `weekly`.
                 */
                frequency: 'weekly';
                /**
                 * The hour to begin maintenance based in UTC time.
                 */
                hour_of_day: number;
                /**
                 * __Read-only__ An array of pending updates.
                 */
                readonly pending: Array<{
                    /**
                     * The time when a mandatory update needs to be applied.
                     */
                    deadline: string | null;
                    /**
                     * A description of the update.
                     */
                    description: string;
                    /**
                     * The date and time a maintenance update will be applied.
                     */
                    planned_for: string | null;
                }>;
            };
            /**
             * __Read-only__ The amount of space currently in use in the database, in GB.
             */
            readonly used_disk_size_gb: number;
            /**
             * __Filterable__ The Managed Database engine version.
             */
            version: string;
        }>;
    };
};

export type GetDatabasesPostgreSqlInstancesResponse = GetDatabasesPostgreSqlInstancesResponses[keyof GetDatabasesPostgreSqlInstancesResponses];

export type PostDatabasesPostgreSqlInstancesData = {
    /**
     * Managed PostgreSQL Database request object.
     */
    body: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list?: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size?: 1 | 2 | 3;
        /**
         * The Managed Database engine in engine/version format.
         */
        engine: string;
        /**
         * Advanced parameters you can apply to a PostgreSQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/postgresql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a PostgreSQL Managed Database. You can also run the [List PostgreSQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-config) operation to see an up-to-date list.
         */
        engine_config?: {
            /**
             * PostgreSQL-specific advanced configuration parameters.
             */
            pg: {
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_analyze_threshold` when deciding whether to trigger an `ANALYZE`.
                 */
                autovacuum_analyze_scale_factor: number;
                /**
                 * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an `ANALYZE` in any one table.
                 */
                autovacuum_analyze_threshold: number;
                /**
                 * Specifies the maximum number of `autovacuum` processes, other than the `autovacuum` launcher, that may be running at any one time. This parameter can only be set at server start.
                 */
                autovacuum_max_workers: number;
                /**
                 * Specifies the minimum delay between `autovacuum` runs on any given database. The delay is measured in seconds.
                 */
                autovacuum_naptime: number;
                /**
                 * Specifies the cost delay value that will be used in automatic `VACUUM` operations. If `-1` is specified, the regular `vacuum_cost_delay` value will be used.
                 */
                autovacuum_vacuum_cost_delay: number;
                /**
                 * Specifies the cost limit value that will be used in automatic `VACUUM` operations. The default of `-1` applies the regular `vacuum_cost_limit` value.
                 */
                autovacuum_vacuum_cost_limit: number;
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_vacuum_threshold` when deciding whether to trigger a `VACUUM`.
                 */
                autovacuum_vacuum_scale_factor: number;
                /**
                 * Specifies the minimum number of updated or deleted tuples needed to trigger a `VACUUM` in any one table.
                 */
                autovacuum_vacuum_threshold: number;
                /**
                 * Specifies the delay between activity rounds for the background writer in milliseconds.
                 */
                bgwriter_delay: number;
                /**
                 * Whenever more than `bgwriter_flush_after` bytes have been written by the background writer, attempt to force the OS to issue these writes to the underlying storage. Specified in kilobytes. A setting of `0` disables forced writeback.
                 */
                bgwriter_flush_after: number;
                /**
                 * In each round, no more than this many buffers will be written by the background writer. Setting this to `0` disables background writing.
                 */
                bgwriter_lru_maxpages: number;
                /**
                 * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to `bgwriter_lru_maxpages`). `1.0` represents a `“just` in `time”` policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes.
                 */
                bgwriter_lru_multiplier: number;
                /**
                 * This is the amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
                 */
                deadlock_timeout: number;
                /**
                 * Specifies the default TOAST compression method for values of compressible columns.
                 */
                default_toast_compression: 'lz4' | 'pglz';
                /**
                 * Time out sessions with open transactions after this number of milliseconds.
                 */
                idle_in_transaction_session_timeout: number;
                /**
                 * Controls system-wide use of Just-in-Time Compilation (JIT).
                 */
                jit: boolean;
                /**
                 * PostgreSQL maximum number of files that can be open per process.
                 */
                max_files_per_process: number;
                /**
                 * PostgreSQL maximum locks per transaction.
                 */
                max_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum logical replication workers, taken from the pool of `max_parallel_workers`.
                 */
                max_logical_replication_workers: number;
                /**
                 * Sets the maximum number of workers that the system can support for parallel queries.
                 */
                max_parallel_workers: number;
                /**
                 * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
                 */
                max_parallel_workers_per_gather: number;
                /**
                 * PostgreSQL maximum predicate locks per transaction.
                 */
                max_pred_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum replication slots.
                 */
                max_replication_slots: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) size in MB, reserved for replication slots. A value of `-1` which indicates unlimited. The `wal_keep_size` minimum write ahead log (WAL) size setting takes precedence over this.
                 */
                max_slot_wal_keep_size: number;
                /**
                 * Maximum depth of the stack in bytes.
                 */
                max_stack_depth: number;
                /**
                 * Maximum standby archive delay in milliseconds.
                 */
                max_standby_archive_delay: number;
                /**
                 * Maximum standby streaming delay in milliseconds.
                 */
                max_standby_streaming_delay: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) senders.
                 */
                max_wal_senders: number;
                /**
                 * Maximum number of background processes that the system can support.
                 */
                max_worker_processes: number;
                /**
                 * Chooses the algorithm for encrypting passwords.
                 */
                password_encryption: 'scram-sh-256' | 'md5';
                /**
                 * Sets the time interval to run `pg_partman` scheduled tasks.
                 */
                'pg_partman_bgw.interval': number;
                /**
                 * Controls which role to use for `pg_partman` scheduled background tasks.
                 */
                'pg_partman_bgw.role': string;
                /**
                 * Enables query plan monitoring.
                 */
                'pg_stat_monitor.pgsm_enable_query_plan': boolean;
                /**
                 * Sets the maximum number of buckets.
                 */
                'pg_stat_monitor.pgsm_max_buckets': number;
                /**
                 * Controls which statements are counted. Specify `top` to track top-level statements that are issued directly by clients, `all` to also track nested statements, such as those invoked within functions, or `none` to disable statement statistics collection.
                 */
                'pg_stat_statements.track': 'all' | 'top' | 'none';
                /**
                 * PostgreSQL temporary file limit in KB. Set to `-1` for unlimited.
                 */
                temp_file_limit: number;
                /**
                 * PostgreSQL service time zone.
                 */
                timezone: string;
                /**
                 * Specifies the number of bytes reserved to track the currently executing command for each active session.
                 */
                track_activity_query_size: number;
                /**
                 * Record the commit time of transactions.
                 */
                track_commit_timestamp: 'on' | 'off';
                /**
                 * Enables tracking of function call counts and time used. Specify `pl` to track only procedural-language functions, `all` to also track SQL and C language functions, or `none` to disable function statistics tracking.
                 */
                track_functions: 'all' | 'pl' | 'none';
                /**
                 * Enables timing of database I/O calls. This parameter is `off` by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
                 */
                track_io_timing: 'on' | 'off';
                /**
                 * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to `0` disables the timeout.
                 */
                wal_sender_timeout: number;
                /**
                 * Write ahead log (WAL) flush interval in milliseconds. A value lower than 200 milliseconds may negatively impact performance.
                 */
                wal_writer_delay: number;
            };
            /**
             * Enable the `pg_stat_monitor` extension. When this extension is enabled, PostgreSQL restarts the cluster it's in. Additionally, `pg_stat_statements` results for utility commands are unreliable.
             */
            pg_stat_monitor_enable: boolean;
            /**
             * Parameter used to apply PGLookout settings.
             */
            pglookout: {
                /**
                 * Number of seconds of master unavailability before triggering database failover to standby.
                 */
                max_failover_replication_time_lag: number;
            };
            /**
             * Percentage of total RAM that the database server uses for shared memory buffers. Valid range is 20-60 (float), which corresponds to 20% - 60%. This setting adjusts the `shared_buffers` configuration value.
             */
            shared_buffers_percentage: number;
            /**
             * Sets the maximum amount of memory in MB to be used by a query operation, such as a sort or hash table, before writing to temporary disk files. Default is 1MB + 0.075% of total RAM, up to 32 MB.
             */
            work_mem: number;
        };
        /**
         * Include this object to restore a Managed Database by forking from a backup.
         *
         * - If you include this object, all other fields are optional.
         *
         * - Don't include this object if you're creating a new Managed Database.
         */
        fork?: {
            /**
             * A specific database timestamp to restore from.
             */
            restore_time?: string;
            /**
             * The unique instance id for the database to fork from. Run the [List Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-instances) operation and store the unique `id` for the target Managed Database.
             */
            source: number;
        };
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Beta__ Restricts access on a PostgreSQL Managed Database to a specific Virtual Private Cloud (VPC) configured for the cluster.
         */
        private_network?: {
            /**
             * Set to `true` to allow clients outside of the VPC to connect to the database using a public IP address. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * If the `vpc_id` includes subnets, you can include the one you want to limit access to the database. From the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation, store the `id` for the applicable `subnets` object.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC you want to use to enable private access to the Managed Database. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation and store the `id` for the applicable VPC.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
         */
        ssl_connection?: boolean;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
    };
    path?: never;
    query?: never;
    url: '/databases/postgresql/instances';
};

export type PostDatabasesPostgreSqlInstancesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDatabasesPostgreSqlInstancesError = PostDatabasesPostgreSqlInstancesErrors[keyof PostDatabasesPostgreSqlInstancesErrors];

export type PostDatabasesPostgreSqlInstancesResponses = {
    /**
     * Managed PostgreSQL Databases object.
     */
    200: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size: 1 | 2 | 3;
        /**
         * __Read-only__ When this Managed Database was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
         */
        readonly encrypted: boolean;
        /**
         * __Filterable__, __Read-only__ The Managed Database engine type.
         */
        readonly engine: string;
        /**
         * Advanced parameters you can apply to a PostgreSQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/postgresql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a PostgreSQL Managed Database. You can also run the [List PostgreSQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * PostgreSQL-specific advanced configuration parameters.
             */
            pg: {
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_analyze_threshold` when deciding whether to trigger an `ANALYZE`.
                 */
                autovacuum_analyze_scale_factor: number;
                /**
                 * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an `ANALYZE` in any one table.
                 */
                autovacuum_analyze_threshold: number;
                /**
                 * Specifies the maximum number of `autovacuum` processes, other than the `autovacuum` launcher, that may be running at any one time. This parameter can only be set at server start.
                 */
                autovacuum_max_workers: number;
                /**
                 * Specifies the minimum delay between `autovacuum` runs on any given database. The delay is measured in seconds.
                 */
                autovacuum_naptime: number;
                /**
                 * Specifies the cost delay value that will be used in automatic `VACUUM` operations. If `-1` is specified, the regular `vacuum_cost_delay` value will be used.
                 */
                autovacuum_vacuum_cost_delay: number;
                /**
                 * Specifies the cost limit value that will be used in automatic `VACUUM` operations. The default of `-1` applies the regular `vacuum_cost_limit` value.
                 */
                autovacuum_vacuum_cost_limit: number;
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_vacuum_threshold` when deciding whether to trigger a `VACUUM`.
                 */
                autovacuum_vacuum_scale_factor: number;
                /**
                 * Specifies the minimum number of updated or deleted tuples needed to trigger a `VACUUM` in any one table.
                 */
                autovacuum_vacuum_threshold: number;
                /**
                 * Specifies the delay between activity rounds for the background writer in milliseconds.
                 */
                bgwriter_delay: number;
                /**
                 * Whenever more than `bgwriter_flush_after` bytes have been written by the background writer, attempt to force the OS to issue these writes to the underlying storage. Specified in kilobytes. A setting of `0` disables forced writeback.
                 */
                bgwriter_flush_after: number;
                /**
                 * In each round, no more than this many buffers will be written by the background writer. Setting this to `0` disables background writing.
                 */
                bgwriter_lru_maxpages: number;
                /**
                 * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to `bgwriter_lru_maxpages`). `1.0` represents a `“just` in `time”` policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes.
                 */
                bgwriter_lru_multiplier: number;
                /**
                 * This is the amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
                 */
                deadlock_timeout: number;
                /**
                 * Specifies the default TOAST compression method for values of compressible columns.
                 */
                default_toast_compression: 'lz4' | 'pglz';
                /**
                 * Time out sessions with open transactions after this number of milliseconds.
                 */
                idle_in_transaction_session_timeout: number;
                /**
                 * Controls system-wide use of Just-in-Time Compilation (JIT).
                 */
                jit: boolean;
                /**
                 * PostgreSQL maximum number of files that can be open per process.
                 */
                max_files_per_process: number;
                /**
                 * PostgreSQL maximum locks per transaction.
                 */
                max_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum logical replication workers, taken from the pool of `max_parallel_workers`.
                 */
                max_logical_replication_workers: number;
                /**
                 * Sets the maximum number of workers that the system can support for parallel queries.
                 */
                max_parallel_workers: number;
                /**
                 * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
                 */
                max_parallel_workers_per_gather: number;
                /**
                 * PostgreSQL maximum predicate locks per transaction.
                 */
                max_pred_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum replication slots.
                 */
                max_replication_slots: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) size in MB, reserved for replication slots. A value of `-1` which indicates unlimited. The `wal_keep_size` minimum write ahead log (WAL) size setting takes precedence over this.
                 */
                max_slot_wal_keep_size: number;
                /**
                 * Maximum depth of the stack in bytes.
                 */
                max_stack_depth: number;
                /**
                 * Maximum standby archive delay in milliseconds.
                 */
                max_standby_archive_delay: number;
                /**
                 * Maximum standby streaming delay in milliseconds.
                 */
                max_standby_streaming_delay: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) senders.
                 */
                max_wal_senders: number;
                /**
                 * Maximum number of background processes that the system can support.
                 */
                max_worker_processes: number;
                /**
                 * Chooses the algorithm for encrypting passwords.
                 */
                password_encryption: 'scram-sh-256' | 'md5';
                /**
                 * Sets the time interval to run `pg_partman` scheduled tasks.
                 */
                'pg_partman_bgw.interval': number;
                /**
                 * Controls which role to use for `pg_partman` scheduled background tasks.
                 */
                'pg_partman_bgw.role': string;
                /**
                 * Enables query plan monitoring.
                 */
                'pg_stat_monitor.pgsm_enable_query_plan': boolean;
                /**
                 * Sets the maximum number of buckets.
                 */
                'pg_stat_monitor.pgsm_max_buckets': number;
                /**
                 * Controls which statements are counted. Specify `top` to track top-level statements that are issued directly by clients, `all` to also track nested statements, such as those invoked within functions, or `none` to disable statement statistics collection.
                 */
                'pg_stat_statements.track': 'all' | 'top' | 'none';
                /**
                 * PostgreSQL temporary file limit in KB. Set to `-1` for unlimited.
                 */
                temp_file_limit: number;
                /**
                 * PostgreSQL service time zone.
                 */
                timezone: string;
                /**
                 * Specifies the number of bytes reserved to track the currently executing command for each active session.
                 */
                track_activity_query_size: number;
                /**
                 * Record the commit time of transactions.
                 */
                track_commit_timestamp: 'on' | 'off';
                /**
                 * Enables tracking of function call counts and time used. Specify `pl` to track only procedural-language functions, `all` to also track SQL and C language functions, or `none` to disable function statistics tracking.
                 */
                track_functions: 'all' | 'pl' | 'none';
                /**
                 * Enables timing of database I/O calls. This parameter is `off` by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
                 */
                track_io_timing: 'on' | 'off';
                /**
                 * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to `0` disables the timeout.
                 */
                wal_sender_timeout: number;
                /**
                 * Write ahead log (WAL) flush interval in milliseconds. A value lower than 200 milliseconds may negatively impact performance.
                 */
                wal_writer_delay: number;
            };
            /**
             * Enable the `pg_stat_monitor` extension. When this extension is enabled, PostgreSQL restarts the cluster it's in. Additionally, `pg_stat_statements` results for utility commands are unreliable.
             */
            pg_stat_monitor_enable: boolean;
            /**
             * Parameter used to apply PGLookout settings.
             */
            pglookout: {
                /**
                 * Number of seconds of master unavailability before triggering database failover to standby.
                 */
                max_failover_replication_time_lag: number;
            };
            /**
             * Percentage of total RAM that the database server uses for shared memory buffers. Valid range is 20-60 (float), which corresponds to 20% - 60%. This setting adjusts the `shared_buffers` configuration value.
             */
            shared_buffers_percentage: number;
            /**
             * Sets the maximum amount of memory in MB to be used by a query operation, such as a sort or hash table, before writing to temporary disk files. Default is 1MB + 0.075% of total RAM, up to 32 MB.
             */
            work_mem: number;
        };
        /**
         * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
         */
        fork: {
            /**
             * The database timestamp from which it was restored. This is _not_ when the fork was created.
             */
            restore_time: string;
            /**
             * The instance id of the database that was forked from.
             */
            source: number;
        };
        /**
         * __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned after provisioning is complete.
         */
        readonly hosts: {
            /**
             * The primary host for the Managed Database.
             */
            primary: string | null;
            /**
             * The secondary/private network host for the Managed Database. A private network host and a private IP can only be used to access a database cluster from Linodes in the same data center and will not incur transfer costs.
             *
             * > 📘
             * >
             * > The secondary hostname is publicly viewable and accessible.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
         */
        readonly id: number;
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
         */
        readonly members: {
            [key: string]: unknown;
        };
        /**
         * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
         */
        platform: 'rdbms-legacy' | 'rdbms-default';
        /**
         * __Read-only__ The access port for this Managed Database.
         */
        readonly port: number;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database.
         *
         * Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
         */
        ssl_connection: boolean;
        /**
         * __Filterable__, __Read-only__ The operating status of the Managed Database.
         */
        status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
        /**
         * __Read-only__ The total disk size of the database, in GB.
         */
        readonly total_disk_size_gb: number;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
        /**
         * __Read-only__ When this Managed Database was last updated.
         */
        readonly updated: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Read-only__ The amount of space currently in use in the database, in GB.
         */
        readonly used_disk_size_gb: number;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type PostDatabasesPostgreSqlInstancesResponse = PostDatabasesPostgreSqlInstancesResponses[keyof PostDatabasesPostgreSqlInstancesResponses];

export type DeleteDatabasesPostgreSqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}';
};

export type DeleteDatabasesPostgreSqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteDatabasesPostgreSqlInstanceError = DeleteDatabasesPostgreSqlInstanceErrors[keyof DeleteDatabasesPostgreSqlInstanceErrors];

export type DeleteDatabasesPostgreSqlInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteDatabasesPostgreSqlInstanceResponse = DeleteDatabasesPostgreSqlInstanceResponses[keyof DeleteDatabasesPostgreSqlInstanceResponses];

export type GetDatabasesPostgreSqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}';
};

export type GetDatabasesPostgreSqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesPostgreSqlInstanceError = GetDatabasesPostgreSqlInstanceErrors[keyof GetDatabasesPostgreSqlInstanceErrors];

export type GetDatabasesPostgreSqlInstanceResponses = {
    /**
     * Managed PostgreSQL Databases object.
     */
    200: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size: 1 | 2 | 3;
        /**
         * __Read-only__ When this Managed Database was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
         */
        readonly encrypted: boolean;
        /**
         * __Filterable__, __Read-only__ The Managed Database engine type.
         */
        readonly engine: string;
        /**
         * Advanced parameters you can apply to a PostgreSQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/postgresql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a PostgreSQL Managed Database. You can also run the [List PostgreSQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * PostgreSQL-specific advanced configuration parameters.
             */
            pg: {
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_analyze_threshold` when deciding whether to trigger an `ANALYZE`.
                 */
                autovacuum_analyze_scale_factor: number;
                /**
                 * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an `ANALYZE` in any one table.
                 */
                autovacuum_analyze_threshold: number;
                /**
                 * Specifies the maximum number of `autovacuum` processes, other than the `autovacuum` launcher, that may be running at any one time. This parameter can only be set at server start.
                 */
                autovacuum_max_workers: number;
                /**
                 * Specifies the minimum delay between `autovacuum` runs on any given database. The delay is measured in seconds.
                 */
                autovacuum_naptime: number;
                /**
                 * Specifies the cost delay value that will be used in automatic `VACUUM` operations. If `-1` is specified, the regular `vacuum_cost_delay` value will be used.
                 */
                autovacuum_vacuum_cost_delay: number;
                /**
                 * Specifies the cost limit value that will be used in automatic `VACUUM` operations. The default of `-1` applies the regular `vacuum_cost_limit` value.
                 */
                autovacuum_vacuum_cost_limit: number;
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_vacuum_threshold` when deciding whether to trigger a `VACUUM`.
                 */
                autovacuum_vacuum_scale_factor: number;
                /**
                 * Specifies the minimum number of updated or deleted tuples needed to trigger a `VACUUM` in any one table.
                 */
                autovacuum_vacuum_threshold: number;
                /**
                 * Specifies the delay between activity rounds for the background writer in milliseconds.
                 */
                bgwriter_delay: number;
                /**
                 * Whenever more than `bgwriter_flush_after` bytes have been written by the background writer, attempt to force the OS to issue these writes to the underlying storage. Specified in kilobytes. A setting of `0` disables forced writeback.
                 */
                bgwriter_flush_after: number;
                /**
                 * In each round, no more than this many buffers will be written by the background writer. Setting this to `0` disables background writing.
                 */
                bgwriter_lru_maxpages: number;
                /**
                 * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to `bgwriter_lru_maxpages`). `1.0` represents a `“just` in `time”` policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes.
                 */
                bgwriter_lru_multiplier: number;
                /**
                 * This is the amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
                 */
                deadlock_timeout: number;
                /**
                 * Specifies the default TOAST compression method for values of compressible columns.
                 */
                default_toast_compression: 'lz4' | 'pglz';
                /**
                 * Time out sessions with open transactions after this number of milliseconds.
                 */
                idle_in_transaction_session_timeout: number;
                /**
                 * Controls system-wide use of Just-in-Time Compilation (JIT).
                 */
                jit: boolean;
                /**
                 * PostgreSQL maximum number of files that can be open per process.
                 */
                max_files_per_process: number;
                /**
                 * PostgreSQL maximum locks per transaction.
                 */
                max_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum logical replication workers, taken from the pool of `max_parallel_workers`.
                 */
                max_logical_replication_workers: number;
                /**
                 * Sets the maximum number of workers that the system can support for parallel queries.
                 */
                max_parallel_workers: number;
                /**
                 * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
                 */
                max_parallel_workers_per_gather: number;
                /**
                 * PostgreSQL maximum predicate locks per transaction.
                 */
                max_pred_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum replication slots.
                 */
                max_replication_slots: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) size in MB, reserved for replication slots. A value of `-1` which indicates unlimited. The `wal_keep_size` minimum write ahead log (WAL) size setting takes precedence over this.
                 */
                max_slot_wal_keep_size: number;
                /**
                 * Maximum depth of the stack in bytes.
                 */
                max_stack_depth: number;
                /**
                 * Maximum standby archive delay in milliseconds.
                 */
                max_standby_archive_delay: number;
                /**
                 * Maximum standby streaming delay in milliseconds.
                 */
                max_standby_streaming_delay: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) senders.
                 */
                max_wal_senders: number;
                /**
                 * Maximum number of background processes that the system can support.
                 */
                max_worker_processes: number;
                /**
                 * Chooses the algorithm for encrypting passwords.
                 */
                password_encryption: 'scram-sh-256' | 'md5';
                /**
                 * Sets the time interval to run `pg_partman` scheduled tasks.
                 */
                'pg_partman_bgw.interval': number;
                /**
                 * Controls which role to use for `pg_partman` scheduled background tasks.
                 */
                'pg_partman_bgw.role': string;
                /**
                 * Enables query plan monitoring.
                 */
                'pg_stat_monitor.pgsm_enable_query_plan': boolean;
                /**
                 * Sets the maximum number of buckets.
                 */
                'pg_stat_monitor.pgsm_max_buckets': number;
                /**
                 * Controls which statements are counted. Specify `top` to track top-level statements that are issued directly by clients, `all` to also track nested statements, such as those invoked within functions, or `none` to disable statement statistics collection.
                 */
                'pg_stat_statements.track': 'all' | 'top' | 'none';
                /**
                 * PostgreSQL temporary file limit in KB. Set to `-1` for unlimited.
                 */
                temp_file_limit: number;
                /**
                 * PostgreSQL service time zone.
                 */
                timezone: string;
                /**
                 * Specifies the number of bytes reserved to track the currently executing command for each active session.
                 */
                track_activity_query_size: number;
                /**
                 * Record the commit time of transactions.
                 */
                track_commit_timestamp: 'on' | 'off';
                /**
                 * Enables tracking of function call counts and time used. Specify `pl` to track only procedural-language functions, `all` to also track SQL and C language functions, or `none` to disable function statistics tracking.
                 */
                track_functions: 'all' | 'pl' | 'none';
                /**
                 * Enables timing of database I/O calls. This parameter is `off` by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
                 */
                track_io_timing: 'on' | 'off';
                /**
                 * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to `0` disables the timeout.
                 */
                wal_sender_timeout: number;
                /**
                 * Write ahead log (WAL) flush interval in milliseconds. A value lower than 200 milliseconds may negatively impact performance.
                 */
                wal_writer_delay: number;
            };
            /**
             * Enable the `pg_stat_monitor` extension. When this extension is enabled, PostgreSQL restarts the cluster it's in. Additionally, `pg_stat_statements` results for utility commands are unreliable.
             */
            pg_stat_monitor_enable: boolean;
            /**
             * Parameter used to apply PGLookout settings.
             */
            pglookout: {
                /**
                 * Number of seconds of master unavailability before triggering database failover to standby.
                 */
                max_failover_replication_time_lag: number;
            };
            /**
             * Percentage of total RAM that the database server uses for shared memory buffers. Valid range is 20-60 (float), which corresponds to 20% - 60%. This setting adjusts the `shared_buffers` configuration value.
             */
            shared_buffers_percentage: number;
            /**
             * Sets the maximum amount of memory in MB to be used by a query operation, such as a sort or hash table, before writing to temporary disk files. Default is 1MB + 0.075% of total RAM, up to 32 MB.
             */
            work_mem: number;
        };
        /**
         * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
         */
        fork: {
            /**
             * The database timestamp from which it was restored. This is _not_ when the fork was created.
             */
            restore_time: string;
            /**
             * The instance id of the database that was forked from.
             */
            source: number;
        };
        /**
         * __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned after provisioning is complete.
         */
        readonly hosts: {
            /**
             * The primary host for the Managed Database.
             */
            primary: string | null;
            /**
             * The secondary/private network host for the Managed Database. A private network host and a private IP can only be used to access a database cluster from Linodes in the same data center and will not incur transfer costs.
             *
             * > 📘
             * >
             * > The secondary hostname is publicly visible and accessible.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
         */
        readonly id: number;
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
         */
        readonly members: {
            [key: string]: unknown;
        };
        /**
         * __Read-only__ The oldest time to which a database can be restored.
         */
        readonly oldest_restore_time: string;
        /**
         * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
         */
        platform: 'rdbms-legacy' | 'rdbms-default';
        /**
         * __Read-only__ The access port for this Managed Database.
         */
        readonly port: number;
        /**
         * __Beta__ Restricts access to a PostgreSQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
         */
        private_network: {
            /**
             * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * The specific subnet, in the VPC (`vpc_id`) where the Managed Database cluster can be accessed.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC where this database exists.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
         */
        ssl_connection: boolean;
        /**
         * __Filterable__, __Read-only__ The operating status of the Managed Database.
         */
        status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
        /**
         * __Read-only__ The total disk size of the database, in GB.
         */
        readonly total_disk_size_gb: number;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
        /**
         * __Read-only__ When this Managed Database was last updated.
         */
        readonly updated: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Read-only__ The amount of space currently in use in the database, in GB.
         */
        readonly used_disk_size_gb: number;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type GetDatabasesPostgreSqlInstanceResponse = GetDatabasesPostgreSqlInstanceResponses[keyof GetDatabasesPostgreSqlInstanceResponses];

export type PutDatabasesPostgreSqlInstanceData = {
    /**
     * Updated information for the Managed PostgreSQL Database.
     */
    body: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * Advanced parameters you can apply to a PostgreSQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/postgresql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a PostgreSQL Managed Database. You can also run the [List PostgreSQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * PostgreSQL-specific advanced configuration parameters.
             */
            pg: {
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_analyze_threshold` when deciding whether to trigger an `ANALYZE`.
                 */
                autovacuum_analyze_scale_factor: number;
                /**
                 * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an `ANALYZE` in any one table.
                 */
                autovacuum_analyze_threshold: number;
                /**
                 * Specifies the maximum number of `autovacuum` processes, other than the `autovacuum` launcher, that may be running at any one time. This parameter can only be set at server start.
                 */
                autovacuum_max_workers: number;
                /**
                 * Specifies the minimum delay between `autovacuum` runs on any given database. The delay is measured in seconds.
                 */
                autovacuum_naptime: number;
                /**
                 * Specifies the cost delay value that will be used in automatic `VACUUM` operations. If `-1` is specified, the regular `vacuum_cost_delay` value will be used.
                 */
                autovacuum_vacuum_cost_delay: number;
                /**
                 * Specifies the cost limit value that will be used in automatic `VACUUM` operations. The default of `-1` applies the regular `vacuum_cost_limit` value.
                 */
                autovacuum_vacuum_cost_limit: number;
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_vacuum_threshold` when deciding whether to trigger a `VACUUM`.
                 */
                autovacuum_vacuum_scale_factor: number;
                /**
                 * Specifies the minimum number of updated or deleted tuples needed to trigger a `VACUUM` in any one table.
                 */
                autovacuum_vacuum_threshold: number;
                /**
                 * Specifies the delay between activity rounds for the background writer in milliseconds.
                 */
                bgwriter_delay: number;
                /**
                 * Whenever more than `bgwriter_flush_after` bytes have been written by the background writer, attempt to force the OS to issue these writes to the underlying storage. Specified in kilobytes. A setting of `0` disables forced writeback.
                 */
                bgwriter_flush_after: number;
                /**
                 * In each round, no more than this many buffers will be written by the background writer. Setting this to `0` disables background writing.
                 */
                bgwriter_lru_maxpages: number;
                /**
                 * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to `bgwriter_lru_maxpages`). `1.0` represents a `“just` in `time”` policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes.
                 */
                bgwriter_lru_multiplier: number;
                /**
                 * This is the amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
                 */
                deadlock_timeout: number;
                /**
                 * Specifies the default TOAST compression method for values of compressible columns.
                 */
                default_toast_compression: 'lz4' | 'pglz';
                /**
                 * Time out sessions with open transactions after this number of milliseconds.
                 */
                idle_in_transaction_session_timeout: number;
                /**
                 * Controls system-wide use of Just-in-Time Compilation (JIT).
                 */
                jit: boolean;
                /**
                 * PostgreSQL maximum number of files that can be open per process.
                 */
                max_files_per_process: number;
                /**
                 * PostgreSQL maximum locks per transaction.
                 */
                max_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum logical replication workers, taken from the pool of `max_parallel_workers`.
                 */
                max_logical_replication_workers: number;
                /**
                 * Sets the maximum number of workers that the system can support for parallel queries.
                 */
                max_parallel_workers: number;
                /**
                 * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
                 */
                max_parallel_workers_per_gather: number;
                /**
                 * PostgreSQL maximum predicate locks per transaction.
                 */
                max_pred_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum replication slots.
                 */
                max_replication_slots: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) size in MB, reserved for replication slots. A value of `-1` which indicates unlimited. The `wal_keep_size` minimum write ahead log (WAL) size setting takes precedence over this.
                 */
                max_slot_wal_keep_size: number;
                /**
                 * Maximum depth of the stack in bytes.
                 */
                max_stack_depth: number;
                /**
                 * Maximum standby archive delay in milliseconds.
                 */
                max_standby_archive_delay: number;
                /**
                 * Maximum standby streaming delay in milliseconds.
                 */
                max_standby_streaming_delay: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) senders.
                 */
                max_wal_senders: number;
                /**
                 * Maximum number of background processes that the system can support.
                 */
                max_worker_processes: number;
                /**
                 * Chooses the algorithm for encrypting passwords.
                 */
                password_encryption: 'scram-sh-256' | 'md5';
                /**
                 * Sets the time interval to run `pg_partman` scheduled tasks.
                 */
                'pg_partman_bgw.interval': number;
                /**
                 * Controls which role to use for `pg_partman` scheduled background tasks.
                 */
                'pg_partman_bgw.role': string;
                /**
                 * Enables query plan monitoring.
                 */
                'pg_stat_monitor.pgsm_enable_query_plan': boolean;
                /**
                 * Sets the maximum number of buckets.
                 */
                'pg_stat_monitor.pgsm_max_buckets': number;
                /**
                 * Controls which statements are counted. Specify `top` to track top-level statements that are issued directly by clients, `all` to also track nested statements, such as those invoked within functions, or `none` to disable statement statistics collection.
                 */
                'pg_stat_statements.track': 'all' | 'top' | 'none';
                /**
                 * PostgreSQL temporary file limit in KB. Set to `-1` for unlimited.
                 */
                temp_file_limit: number;
                /**
                 * PostgreSQL service time zone.
                 */
                timezone: string;
                /**
                 * Specifies the number of bytes reserved to track the currently executing command for each active session.
                 */
                track_activity_query_size: number;
                /**
                 * Record the commit time of transactions.
                 */
                track_commit_timestamp: 'on' | 'off';
                /**
                 * Enables tracking of function call counts and time used. Specify `pl` to track only procedural-language functions, `all` to also track SQL and C language functions, or `none` to disable function statistics tracking.
                 */
                track_functions: 'all' | 'pl' | 'none';
                /**
                 * Enables timing of database I/O calls. This parameter is `off` by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
                 */
                track_io_timing: 'on' | 'off';
                /**
                 * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to `0` disables the timeout.
                 */
                wal_sender_timeout: number;
                /**
                 * Write ahead log (WAL) flush interval in milliseconds. A value lower than 200 milliseconds may negatively impact performance.
                 */
                wal_writer_delay: number;
            };
            /**
             * Enable the `pg_stat_monitor` extension. When this extension is enabled, PostgreSQL restarts the cluster it's in. Additionally, `pg_stat_statements` results for utility commands are unreliable.
             */
            pg_stat_monitor_enable: boolean;
            /**
             * Parameter used to apply PGLookout settings.
             */
            pglookout: {
                /**
                 * Number of seconds of master unavailability before triggering database failover to standby.
                 */
                max_failover_replication_time_lag: number;
            };
            /**
             * Percentage of total RAM that the database server uses for shared memory buffers. Valid range is 20-60 (float), which corresponds to 20% - 60%. This setting adjusts the `shared_buffers` configuration value.
             */
            shared_buffers_percentage: number;
            /**
             * Sets the maximum amount of memory in MB to be used by a query operation, such as a sort or hash table, before writing to temporary disk files. Default is 1MB + 0.075% of total RAM, up to 32 MB.
             */
            work_mem: number;
        };
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Beta__ Restricts access on a PostgreSQL Managed Database to a specific Virtual Private Cloud (VPC) configured for the cluster.
         */
        private_network: {
            /**
             * Set to `true` to allow clients outside of the VPC to connect to the database using a public IP address. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * If the `vpc_id` includes subnets, you can include the one you want to limit access to the database. From the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation, store the `id` for the applicable `subnets` object.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC you want to use to enable private access to the Managed Database. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation and store the `id` for the applicable VPC.
             */
            vpc_id: number;
        } | null;
        /**
         * Request re-sizing of your cluster to a Linode Type with more disk space. For example, you could request a Linode Type that uses a higher plan.
         *
         * - Needs to be a Linode Type with more disk space than your current Linode.
         *
         * - Resizing to a larger Linode Type can accrue additional cost. Review the `price` output from the [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types) operation for more information.
         *
         * - You can't update the `allow_list` and set a new `type` in the same request.
         *
         * - Any active updates to your cluster need to complete before you can request a resize. The reverse is also true: An active resizing needs to complete before you can perform any other update.
         */
        type: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}';
};

export type PutDatabasesPostgreSqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutDatabasesPostgreSqlInstanceError = PutDatabasesPostgreSqlInstanceErrors[keyof PutDatabasesPostgreSqlInstanceErrors];

export type PutDatabasesPostgreSqlInstanceResponses = {
    /**
     * Managed PostgreSQL Databases object.
     */
    200: {
        /**
         * Controls access to the Managed Database.
         *
         * - Individually included IP addresses or CIDR ranges can access the Managed Database while all other sources are blocked.
         *
         * - A standalone value of `0.0.0.0/0` allows all IP addresses access to the Managed Database.
         *
         * - An empty array (`[]`) blocks all public and private connections to the Managed Database.
         */
        allow_list: Array<string>;
        /**
         * The number of Linode instance nodes deployed to the Managed Database.
         *
         * - Choose `3` nodes to create a high availability cluster that consists of one primary node and two replica nodes.
         *
         * - A `2` node cluster is only available with a dedicated plan. It consists of one primary node and one replica node.
         */
        cluster_size: 1 | 2 | 3;
        /**
         * __Read-only__ When this Managed Database was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether the Managed Databases is encrypted. Currently required to be `true`.
         */
        readonly encrypted: boolean;
        /**
         * __Filterable__, __Read-only__ The Managed Database engine type.
         */
        readonly engine: string;
        /**
         * Advanced parameters you can apply to a PostgreSQL Managed Database, via our partner [Aiven's specification](https://aiven.io/docs/products/postgresql/reference/advanced-params). Only include the objects for parameters you want to set in your database. Omit objects for parameters you don't want to define or change.
         *
         * > 📘
         * >
         * > Aiven may offer additional parameters in their specification. Currently, only those listed here are supported for use in a PostgreSQL Managed Database. You can also run the [List PostgreSQL Managed Database advanced parameters](https://techdocs.akamai.com/linode-api/reference/get-databases-postgresql-config) operation to see an up-to-date list.
         */
        engine_config: {
            /**
             * PostgreSQL-specific advanced configuration parameters.
             */
            pg: {
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_analyze_threshold` when deciding whether to trigger an `ANALYZE`.
                 */
                autovacuum_analyze_scale_factor: number;
                /**
                 * Specifies the minimum number of inserted, updated, or deleted tuples needed to trigger an `ANALYZE` in any one table.
                 */
                autovacuum_analyze_threshold: number;
                /**
                 * Specifies the maximum number of `autovacuum` processes, other than the `autovacuum` launcher, that may be running at any one time. This parameter can only be set at server start.
                 */
                autovacuum_max_workers: number;
                /**
                 * Specifies the minimum delay between `autovacuum` runs on any given database. The delay is measured in seconds.
                 */
                autovacuum_naptime: number;
                /**
                 * Specifies the cost delay value that will be used in automatic `VACUUM` operations. If `-1` is specified, the regular `vacuum_cost_delay` value will be used.
                 */
                autovacuum_vacuum_cost_delay: number;
                /**
                 * Specifies the cost limit value that will be used in automatic `VACUUM` operations. The default of `-1` applies the regular `vacuum_cost_limit` value.
                 */
                autovacuum_vacuum_cost_limit: number;
                /**
                 * Specifies a fraction of the table size to add to `autovacuum_vacuum_threshold` when deciding whether to trigger a `VACUUM`.
                 */
                autovacuum_vacuum_scale_factor: number;
                /**
                 * Specifies the minimum number of updated or deleted tuples needed to trigger a `VACUUM` in any one table.
                 */
                autovacuum_vacuum_threshold: number;
                /**
                 * Specifies the delay between activity rounds for the background writer in milliseconds.
                 */
                bgwriter_delay: number;
                /**
                 * Whenever more than `bgwriter_flush_after` bytes have been written by the background writer, attempt to force the OS to issue these writes to the underlying storage. Specified in kilobytes. A setting of `0` disables forced writeback.
                 */
                bgwriter_flush_after: number;
                /**
                 * In each round, no more than this many buffers will be written by the background writer. Setting this to `0` disables background writing.
                 */
                bgwriter_lru_maxpages: number;
                /**
                 * The average recent need for new buffers is multiplied by bgwriter_lru_multiplier to arrive at an estimate of the number that will be needed during the next round, (up to `bgwriter_lru_maxpages`). `1.0` represents a `“just` in `time”` policy of writing exactly the number of buffers predicted to be needed. Larger values provide some cushion against spikes in demand, while smaller values intentionally leave writes to be done by server processes.
                 */
                bgwriter_lru_multiplier: number;
                /**
                 * This is the amount of time, in milliseconds, to wait on a lock before checking to see if there is a deadlock condition.
                 */
                deadlock_timeout: number;
                /**
                 * Specifies the default TOAST compression method for values of compressible columns.
                 */
                default_toast_compression: 'lz4' | 'pglz';
                /**
                 * Time out sessions with open transactions after this number of milliseconds.
                 */
                idle_in_transaction_session_timeout: number;
                /**
                 * Controls system-wide use of Just-in-Time Compilation (JIT).
                 */
                jit: boolean;
                /**
                 * PostgreSQL maximum number of files that can be open per process.
                 */
                max_files_per_process: number;
                /**
                 * PostgreSQL maximum locks per transaction.
                 */
                max_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum logical replication workers, taken from the pool of `max_parallel_workers`.
                 */
                max_logical_replication_workers: number;
                /**
                 * Sets the maximum number of workers that the system can support for parallel queries.
                 */
                max_parallel_workers: number;
                /**
                 * Sets the maximum number of workers that can be started by a single Gather or Gather Merge node.
                 */
                max_parallel_workers_per_gather: number;
                /**
                 * PostgreSQL maximum predicate locks per transaction.
                 */
                max_pred_locks_per_transaction: number;
                /**
                 * PostgreSQL maximum replication slots.
                 */
                max_replication_slots: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) size in MB, reserved for replication slots. A value of `-1` which indicates unlimited. The `wal_keep_size` minimum write ahead log (WAL) size setting takes precedence over this.
                 */
                max_slot_wal_keep_size: number;
                /**
                 * Maximum depth of the stack in bytes.
                 */
                max_stack_depth: number;
                /**
                 * Maximum standby archive delay in milliseconds.
                 */
                max_standby_archive_delay: number;
                /**
                 * Maximum standby streaming delay in milliseconds.
                 */
                max_standby_streaming_delay: number;
                /**
                 * PostgreSQL maximum write ahead log (WAL) senders.
                 */
                max_wal_senders: number;
                /**
                 * Maximum number of background processes that the system can support.
                 */
                max_worker_processes: number;
                /**
                 * Chooses the algorithm for encrypting passwords.
                 */
                password_encryption: 'scram-sh-256' | 'md5';
                /**
                 * Sets the time interval to run `pg_partman` scheduled tasks.
                 */
                'pg_partman_bgw.interval': number;
                /**
                 * Controls which role to use for `pg_partman` scheduled background tasks.
                 */
                'pg_partman_bgw.role': string;
                /**
                 * Enables query plan monitoring.
                 */
                'pg_stat_monitor.pgsm_enable_query_plan': boolean;
                /**
                 * Sets the maximum number of buckets.
                 */
                'pg_stat_monitor.pgsm_max_buckets': number;
                /**
                 * Controls which statements are counted. Specify `top` to track top-level statements that are issued directly by clients, `all` to also track nested statements, such as those invoked within functions, or `none` to disable statement statistics collection.
                 */
                'pg_stat_statements.track': 'all' | 'top' | 'none';
                /**
                 * PostgreSQL temporary file limit in KB. Set to `-1` for unlimited.
                 */
                temp_file_limit: number;
                /**
                 * PostgreSQL service time zone.
                 */
                timezone: string;
                /**
                 * Specifies the number of bytes reserved to track the currently executing command for each active session.
                 */
                track_activity_query_size: number;
                /**
                 * Record the commit time of transactions.
                 */
                track_commit_timestamp: 'on' | 'off';
                /**
                 * Enables tracking of function call counts and time used. Specify `pl` to track only procedural-language functions, `all` to also track SQL and C language functions, or `none` to disable function statistics tracking.
                 */
                track_functions: 'all' | 'pl' | 'none';
                /**
                 * Enables timing of database I/O calls. This parameter is `off` by default, because it will repeatedly query the operating system for the current time, which may cause significant overhead on some platforms.
                 */
                track_io_timing: 'on' | 'off';
                /**
                 * Terminate replication connections that are inactive for longer than this amount of time, in milliseconds. Setting this value to `0` disables the timeout.
                 */
                wal_sender_timeout: number;
                /**
                 * Write ahead log (WAL) flush interval in milliseconds. A value lower than 200 milliseconds may negatively impact performance.
                 */
                wal_writer_delay: number;
            };
            /**
             * Enable the `pg_stat_monitor` extension. When this extension is enabled, PostgreSQL restarts the cluster it's in. Additionally, `pg_stat_statements` results for utility commands are unreliable.
             */
            pg_stat_monitor_enable: boolean;
            /**
             * Parameter used to apply PGLookout settings.
             */
            pglookout: {
                /**
                 * Number of seconds of master unavailability before triggering database failover to standby.
                 */
                max_failover_replication_time_lag: number;
            };
            /**
             * Percentage of total RAM that the database server uses for shared memory buffers. Valid range is 20-60 (float), which corresponds to 20% - 60%. This setting adjusts the `shared_buffers` configuration value.
             */
            shared_buffers_percentage: number;
            /**
             * Sets the maximum amount of memory in MB to be used by a query operation, such as a sort or hash table, before writing to temporary disk files. Default is 1MB + 0.075% of total RAM, up to 32 MB.
             */
            work_mem: number;
        };
        /**
         * Details on the database that was the target of the fork. This only exists if the database was restored by creating a fork from another [MySQL](https://techdocs.akamai.com/linode-api/reference/post-databases-mysql-instances) or [PostgreSQL](https://techdocs.akamai.com/linode-api/reference/post-databases-postgre-sql-instances) database.
         */
        fork: {
            /**
             * The database timestamp from which it was restored. This is _not_ when the fork was created.
             */
            restore_time: string;
            /**
             * The instance id of the database that was forked from.
             */
            source: number;
        };
        /**
         * __Read-only__ The primary and secondary hosts for the Managed Database. These are assigned after provisioning is complete.
         */
        readonly hosts: {
            /**
             * The primary host for the Managed Database.
             */
            primary: string | null;
            /**
             * The secondary/private network host for the Managed Database. A private network host and a private IP can only be used to access a database cluster from Linodes in the same data center and will not incur transfer costs.
             *
             * > 📘
             * >
             * > The secondary hostname is publicly visible and accessible.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ A unique ID that can be used to identify and reference the Managed Database.
         */
        readonly id: number;
        /**
         * __Filterable__ A unique, user-defined string referring to the Managed Database. This string needs to be unique per Managed Database engine type.
         */
        label: string;
        /**
         * __Read-only__ A mapping between IP addresses and strings designating them as `primary` or `failover`.
         */
        readonly members: {
            [key: string]: unknown;
        };
        /**
         * __Read-only__ The oldest time to which a database can be restored.
         */
        readonly oldest_restore_time: string;
        /**
         * __Filterable__, __Read-only__ The back-end platform for relational databases used by the service.
         */
        platform: 'rdbms-legacy' | 'rdbms-default';
        /**
         * __Read-only__ The access port for this Managed Database.
         */
        readonly port: number;
        /**
         * __Beta__ Restricts access to a PostgreSQL Managed Database using a Virtual Private Cloud (VPC). Displayed as `null` if no VPC is configured.
         */
        private_network: {
            /**
             * When set to `true`, clients outside of the VPC can connect to the database. Defaults to `false`, where only nodes within the specified `vpc_id` can access the Managed Database cluster.
             *
             * > 📘
             * >
             * > If your Managed Database is also configured using an `allow_list`, only IP addresses set in it can access that database, even if this object is set to `true`.
             */
            public_access: boolean;
            /**
             * The specific subnet, in the VPC (`vpc_id`) where the Managed Database cluster can be accessed.
             */
            subnet_id: number;
            /**
             * The unique identifier of the VPC where this database exists.
             */
            vpc_id: number;
        } | null;
        /**
         * __Filterable__ The unique identifier for the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Managed Database lives.
         */
        region: string;
        /**
         * Currently required to be `true`. Whether to require SSL credentials to establish a connection to the Managed Database. Run the [Get managed PostgreSQL database credentials](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instance-credentials) operation for access information.
         */
        ssl_connection: boolean;
        /**
         * __Filterable__, __Read-only__ The operating status of the Managed Database.
         */
        status: 'provisioning' | 'active' | 'suspending' | 'suspended' | 'resuming' | 'failed' | 'degraded' | 'updating' | 'resizing';
        /**
         * __Read-only__ The total disk size of the database, in GB.
         */
        readonly total_disk_size_gb: number;
        /**
         * __Filterable__ The Linode Instance type used by the Managed Database for its nodes.
         */
        type: string;
        /**
         * __Read-only__ When this Managed Database was last updated.
         */
        readonly updated: string;
        /**
         * Configuration settings for automated patch update maintenance for the Managed Database.
         */
        updates: {
            /**
             * The numeric reference for the day of the week to perform maintenance. `1` is Monday, `2` is Tuesday, through to `7` which is Sunday.
             */
            day_of_week: number;
            /**
             * The maximum maintenance window time in hours.
             */
            duration: number;
            /**
             * How frequently maintenance occurs. Currently can only be `weekly`.
             */
            frequency: 'weekly';
            /**
             * The hour to begin maintenance based in UTC time.
             */
            hour_of_day: number;
            /**
             * __Read-only__ An array of pending updates.
             */
            readonly pending: Array<{
                /**
                 * The time when a mandatory update needs to be applied.
                 */
                deadline: string | null;
                /**
                 * A description of the update.
                 */
                description: string;
                /**
                 * The date and time a maintenance update will be applied.
                 */
                planned_for: string | null;
            }>;
        };
        /**
         * __Read-only__ The amount of space currently in use in the database, in GB.
         */
        readonly used_disk_size_gb: number;
        /**
         * __Filterable__ The Managed Database engine version.
         */
        version: string;
    };
};

export type PutDatabasesPostgreSqlInstanceResponse = PutDatabasesPostgreSqlInstanceResponses[keyof PutDatabasesPostgreSqlInstanceResponses];

export type GetDatabasesPostgreSqlInstanceCredentialsData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}/credentials';
};

export type GetDatabasesPostgreSqlInstanceCredentialsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesPostgreSqlInstanceCredentialsError = GetDatabasesPostgreSqlInstanceCredentialsErrors[keyof GetDatabasesPostgreSqlInstanceCredentialsErrors];

export type GetDatabasesPostgreSqlInstanceCredentialsResponses = {
    /**
     * Managed Database object for database credentials.
     */
    200: {
        /**
         * __Read-only__ The randomly generated root password for the Managed Database instance.
         */
        readonly password: string;
        /**
         * __Read-only__ The root username for the Managed Database instance.
         */
        readonly username: string;
    };
};

export type GetDatabasesPostgreSqlInstanceCredentialsResponse = GetDatabasesPostgreSqlInstanceCredentialsResponses[keyof GetDatabasesPostgreSqlInstanceCredentialsResponses];

export type PostDatabasesPostgreSqlInstanceCredentialsResetData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}/credentials/reset';
};

export type PostDatabasesPostgreSqlInstanceCredentialsResetErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDatabasesPostgreSqlInstanceCredentialsResetError = PostDatabasesPostgreSqlInstanceCredentialsResetErrors[keyof PostDatabasesPostgreSqlInstanceCredentialsResetErrors];

export type PostDatabasesPostgreSqlInstanceCredentialsResetResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostDatabasesPostgreSqlInstanceCredentialsResetResponse = PostDatabasesPostgreSqlInstanceCredentialsResetResponses[keyof PostDatabasesPostgreSqlInstanceCredentialsResetResponses];

export type PostDatabasesPostgreSqlInstancePatchData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}/patch';
};

export type PostDatabasesPostgreSqlInstancePatchErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDatabasesPostgreSqlInstancePatchError = PostDatabasesPostgreSqlInstancePatchErrors[keyof PostDatabasesPostgreSqlInstancePatchErrors];

export type PostDatabasesPostgreSqlInstancePatchResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostDatabasesPostgreSqlInstancePatchResponse = PostDatabasesPostgreSqlInstancePatchResponses[keyof PostDatabasesPostgreSqlInstancePatchResponses];

export type ResumeDatabasesPostgreSqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}/resume';
};

export type ResumeDatabasesPostgreSqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type ResumeDatabasesPostgreSqlInstanceError = ResumeDatabasesPostgreSqlInstanceErrors[keyof ResumeDatabasesPostgreSqlInstanceErrors];

export type ResumeDatabasesPostgreSqlInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type ResumeDatabasesPostgreSqlInstanceResponse = ResumeDatabasesPostgreSqlInstanceResponses[keyof ResumeDatabasesPostgreSqlInstanceResponses];

export type GetDatabasesPostgresqlInstanceSslData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}/ssl';
};

export type GetDatabasesPostgresqlInstanceSslErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesPostgresqlInstanceSslError = GetDatabasesPostgresqlInstanceSslErrors[keyof GetDatabasesPostgresqlInstanceSslErrors];

export type GetDatabasesPostgresqlInstanceSslResponses = {
    /**
     * Managed Database SSL object.
     */
    200: {
        /**
         * The base64-encoded SSL CA certificate for the Managed Database instance.
         */
        ca_certificate: string;
    };
};

export type GetDatabasesPostgresqlInstanceSslResponse = GetDatabasesPostgresqlInstanceSslResponses[keyof GetDatabasesPostgresqlInstanceSslResponses];

export type SuspendDatabasesPostgreSqlInstanceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed PostgreSQL Database.
         */
        instanceId: number;
    };
    query?: never;
    url: '/databases/postgresql/instances/{instanceId}/suspend';
};

export type SuspendDatabasesPostgreSqlInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type SuspendDatabasesPostgreSqlInstanceError = SuspendDatabasesPostgreSqlInstanceErrors[keyof SuspendDatabasesPostgreSqlInstanceErrors];

export type SuspendDatabasesPostgreSqlInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type SuspendDatabasesPostgreSqlInstanceResponse = SuspendDatabasesPostgreSqlInstanceResponses[keyof SuspendDatabasesPostgreSqlInstanceResponses];

export type GetDatabasesTypesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/types';
};

export type GetDatabasesTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesTypesError = GetDatabasesTypesErrors[keyof GetDatabasesTypesErrors];

export type GetDatabasesTypesResponses = {
    /**
     * Returns a paginated list of all Managed Databases types.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * The Linode class category. This can be [dedicated](https://techdocs.akamai.com/cloud-computing/docs/dedicated-cpu-compute-instances), [shared](https://techdocs.akamai.com/cloud-computing/docs/shared-cpu-compute-instances), or [premium](https://techdocs.akamai.com/cloud-computing/docs/premium-compute-instances).
             *
             * > 📘
             * >
             * > Premium plan Linodes are available in limited regions.
             */
            class: 'dedicated' | 'shared' | 'premium';
            /**
             * The amount of disk space set aside for Databases of this plan type. The value is represented in megabytes.
             */
            disk: number;
            /**
             * Information for the supported third-party databases that can be used with Managed Databases.
             */
            engines: {
                /**
                 * Pricing details for MySQL Managed Databases.
                 */
                mysql: Array<{
                    /**
                     * Cost in US dollars, broken down into hourly and monthly charges.
                     */
                    price: {
                        /**
                         * Cost (in US dollars) per hour for this subscription tier.
                         */
                        hourly: number;
                        /**
                         * Maximum cost (in US dollars) per month for this subscription tier.
                         */
                        monthly: number;
                    };
                    /**
                     * The number of nodes for the Managed Database cluster for this subscription tier.
                     */
                    quantity: 1 | 2 | 3;
                }>;
                /**
                 * Pricing details for PostgreSQL Managed Databases.
                 */
                postgresql: Array<{
                    /**
                     * Cost in US dollars, broken down into hourly and monthly charges.
                     */
                    price: {
                        /**
                         * Cost (in US dollars) per hour for this subscription tier.
                         */
                        hourly: number;
                        /**
                         * Maximum cost (in US dollars) per month for this subscription tier.
                         */
                        monthly: number;
                    };
                    /**
                     * The number of nodes for the Managed Database cluster for this subscription tier.
                     */
                    quantity: 1 | 2 | 3;
                }>;
            };
            /**
             * __Read-only__ The ID representing the Managed Database node plan type.
             */
            readonly id: string;
            /**
             * __Read-only__ A human-readable string that describes each plan type. For display purposes only.
             */
            readonly label: string;
            /**
             * The amount of RAM allocated to Database created of this plan type. The value is represented in megabytes.
             */
            memory: number;
            /**
             * The number of CPUs allocated to databases of this plan type.
             */
            vcpus: number;
        }>;
    };
};

export type GetDatabasesTypesResponse = GetDatabasesTypesResponses[keyof GetDatabasesTypesResponses];

export type GetDatabasesTypeData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed Database type.
         */
        typeId: string;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/databases/types/{typeId}';
};

export type GetDatabasesTypeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDatabasesTypeError = GetDatabasesTypeErrors[keyof GetDatabasesTypeErrors];

export type GetDatabasesTypeResponses = {
    /**
     * Managed Database plan type object.
     */
    200: {
        /**
         * The Linode class category. This can be [dedicated](https://techdocs.akamai.com/cloud-computing/docs/dedicated-cpu-compute-instances), [shared](https://techdocs.akamai.com/cloud-computing/docs/shared-cpu-compute-instances), or [premium](https://techdocs.akamai.com/cloud-computing/docs/premium-compute-instances).
         *
         * > 📘
         * >
         * > Premium plan Linodes are available in limited regions.
         */
        class: 'dedicated' | 'shared' | 'premium';
        /**
         * The amount of disk space set aside for Databases of this plan type. The value is represented in megabytes.
         */
        disk: number;
        /**
         * Information for the supported third-party databases that can be used with Managed Databases.
         */
        engines: {
            /**
             * Pricing details for MySQL Managed Databases.
             */
            mysql: Array<{
                /**
                 * Cost in US dollars, broken down into hourly and monthly charges.
                 */
                price: {
                    /**
                     * Cost (in US dollars) per hour for this subscription tier.
                     */
                    hourly: number;
                    /**
                     * Maximum cost (in US dollars) per month for this subscription tier.
                     */
                    monthly: number;
                };
                /**
                 * The number of nodes for the Managed Database cluster for this subscription tier.
                 */
                quantity: 1 | 2 | 3;
            }>;
            /**
             * Pricing details for PostgreSQL Managed Databases.
             */
            postgresql: Array<{
                /**
                 * Cost in US dollars, broken down into hourly and monthly charges.
                 */
                price: {
                    /**
                     * Cost (in US dollars) per hour for this subscription tier.
                     */
                    hourly: number;
                    /**
                     * Maximum cost (in US dollars) per month for this subscription tier.
                     */
                    monthly: number;
                };
                /**
                 * The number of nodes for the Managed Database cluster for this subscription tier.
                 */
                quantity: 1 | 2 | 3;
            }>;
        };
        /**
         * __Read-only__ The ID representing the Managed Database node plan type.
         */
        readonly id: string;
        /**
         * __Read-only__ A human-readable string that describes each plan type. For display purposes only.
         */
        readonly label: string;
        /**
         * The amount of RAM allocated to Database created of this plan type. The value is represented in megabytes.
         */
        memory: number;
        /**
         * The number of CPUs allocated to databases of this plan type.
         */
        vcpus: number;
    };
};

export type GetDatabasesTypeResponse = GetDatabasesTypeResponses[keyof GetDatabasesTypeResponses];

export type GetDomainsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/domains';
};

export type GetDomainsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDomainsError = GetDomainsErrors[keyof GetDomainsErrors];

export type GetDomainsResponses = {
    /**
     * A paginated list of Domains you have registered.
     */
    200: {
        data: Array<{
            /**
             * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
             *
             * > 📘
             * >
             * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
             */
            axfr_ips: Array<string>;
            /**
             * A description for this domain. This is for display purposes only.
             */
            description: string | null;
            /**
             * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
             */
            domain: string;
            /**
             * The amount of time in seconds that may pass before this domain is no longer authoritative.
             *
             * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
             *
             * - Any other value is rounded up to the nearest valid value.
             *
             * - A value of 0 is equivalent to the default value of 1209600.
             */
            expire_sec: number;
            /**
             * __Filterable__ The group this domain belongs to.  This is for display purposes only.
             *
             * @deprecated
             */
            group: string | null;
            /**
             * __Read-only__ This domain's unique ID.
             */
            readonly id: number;
            /**
             * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
             */
            master_ips: Array<string>;
            /**
             * The amount of time in seconds before this domain should be refreshed.
             *
             * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
             *
             * - Any other value is rounded up to the nearest valid value.
             *
             * - A value of 0 is equivalent to the default value of 14400.
             */
            refresh_sec: number;
            /**
             * The interval, in seconds, at which a failed refresh should be retried.
             *
             * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
             *
             * - Any other value is rounded up to the nearest valid value.
             *
             * - A value of 0 is equivalent to the default value of 14400.
             */
            retry_sec: number;
            /**
             * Start of Authority email address. This is required for `type` master domains.
             */
            soa_email: string;
            /**
             * Used to control whether this domain is currently being rendered.
             */
            status: 'disabled' | 'active';
            /**
             * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
             *
             * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
             *
             * - Any other value is rounded up to the nearest valid value.
             *
             * - A value of 0 is equivalent to the default value of 86400.
             */
            ttl_sec: number;
            /**
             * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
             */
            type: 'master' | 'slave';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetDomainsResponse = GetDomainsResponses[keyof GetDomainsResponses];

export type PostDomainData = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    body: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
    path?: never;
    query?: never;
    url: '/domains';
};

export type PostDomainErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDomainError = PostDomainErrors[keyof PostDomainErrors];

export type PostDomainResponses = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    200: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
};

export type PostDomainResponse = PostDomainResponses[keyof PostDomainResponses];

export type PostImportDomainData = {
    /**
     * Information about the Domain to import.
     */
    body?: {
        /**
         * The domain to import.
         */
        domain: string;
        /**
         * The remote nameserver that allows zone transfers (AXFR).
         */
        remote_nameserver: string;
    };
    path?: never;
    query?: never;
    url: '/domains/import';
};

export type PostImportDomainErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostImportDomainError = PostImportDomainErrors[keyof PostImportDomainErrors];

export type PostImportDomainResponses = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    200: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
};

export type PostImportDomainResponse = PostImportDomainResponses[keyof PostImportDomainResponses];

export type DeleteDomainData = {
    body?: never;
    path: {
        /**
         * The ID of the Domain to access.
         */
        domainId: number;
    };
    query?: never;
    url: '/domains/{domainId}';
};

export type DeleteDomainErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteDomainError = DeleteDomainErrors[keyof DeleteDomainErrors];

export type DeleteDomainResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteDomainResponse = DeleteDomainResponses[keyof DeleteDomainResponses];

export type GetDomainData = {
    body?: never;
    path: {
        /**
         * The ID of the Domain to access.
         */
        domainId: number;
    };
    query?: never;
    url: '/domains/{domainId}';
};

export type GetDomainErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDomainError = GetDomainErrors[keyof GetDomainErrors];

export type GetDomainResponses = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    200: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
};

export type GetDomainResponse = GetDomainResponses[keyof GetDomainResponses];

export type PutDomainData = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    body: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
    path: {
        /**
         * The ID of the Domain to access.
         */
        domainId: number;
    };
    query?: never;
    url: '/domains/{domainId}';
};

export type PutDomainErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutDomainError = PutDomainErrors[keyof PutDomainErrors];

export type PutDomainResponses = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    200: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
};

export type PutDomainResponse = PutDomainResponses[keyof PutDomainResponses];

export type PostCloneDomainData = {
    /**
     * Information about the Domain to clone.
     */
    body: {
        /**
         * __Filterable__ The new domain for the clone. Domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). Domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two Domains representing the same domain.
         */
        domain: string;
    };
    path: {
        /**
         * ID of the Domain to clone.
         */
        domainId: string;
    };
    query?: never;
    url: '/domains/{domainId}/clone';
};

export type PostCloneDomainErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCloneDomainError = PostCloneDomainErrors[keyof PostCloneDomainErrors];

export type PostCloneDomainResponses = {
    /**
     * Domain
     *
     * A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.
     */
    200: {
        /**
         * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
         *
         * > 📘
         * >
         * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
         */
        axfr_ips: Array<string>;
        /**
         * A description for this domain. This is for display purposes only.
         */
        description: string | null;
        /**
         * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
         */
        domain: string;
        /**
         * The amount of time in seconds that may pass before this domain is no longer authoritative.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 1209600.
         */
        expire_sec: number;
        /**
         * __Filterable__ The group this domain belongs to.  This is for display purposes only.
         *
         * @deprecated
         */
        group: string | null;
        /**
         * __Read-only__ This domain's unique ID.
         */
        readonly id: number;
        /**
         * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
         */
        master_ips: Array<string>;
        /**
         * The amount of time in seconds before this domain should be refreshed.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        refresh_sec: number;
        /**
         * The interval, in seconds, at which a failed refresh should be retried.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 14400.
         */
        retry_sec: number;
        /**
         * Start of Authority email address. This is required for `type` master domains.
         */
        soa_email: string;
        /**
         * Used to control whether this domain is currently being rendered.
         */
        status: 'disabled' | 'active';
        /**
         * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
         *
         * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
         *
         * - Any other value is rounded up to the nearest valid value.
         *
         * - A value of 0 is equivalent to the default value of 86400.
         */
        ttl_sec: number;
        /**
         * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
         */
        type: 'master' | 'slave';
    };
};

export type PostCloneDomainResponse = PostCloneDomainResponses[keyof PostCloneDomainResponses];

export type GetDomainRecordsData = {
    body?: never;
    path: {
        /**
         * The ID of the Domain we are accessing Records for.
         */
        domainId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/domains/{domainId}/records';
};

export type GetDomainRecordsResponses = {
    /**
     * A list of Domain Records.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this Domain Record was created.
             */
            readonly created: string;
            /**
             * __Read-only__ This Record's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
             *
             * `A` and `AAAA`: The hostname or FQDN of the Record.
             *
             * `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.
             *
             * `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.
             *
             * - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
             * - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
             * - Must be an empty string (`""`) for a Null MX Record.
             *
             * `CNAME`: The hostname. Must be unique. Required.
             *
             * `TXT`: The hostname.
             *
             * `SRV`: Unused. Use the `service` property to set the service name for this record.
             *
             * `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.
             *
             * `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
             * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
             */
            name: string;
            /**
             * The port this Record points to. Only valid and required for SRV record requests.
             */
            port: number;
            /**
             * The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.
             *
             * Defaults to `0` for MX record requests. Must be `0` for Null MX records.
             */
            priority: number;
            /**
             * The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically to the submitted value for this property. Only valid for SRV record requests.
             */
            protocol: string | null;
            /**
             * The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended automatically to the submitted value for this property. Only valid and required for SRV record requests.
             */
            service: string | null;
            /**
             * __Filterable__ The tag portion of a CAA record. Only valid and required for CAA record requests.
             */
            tag: 'issue' | 'issuewild' | 'iodef';
            /**
             * __Filterable__ The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
             *
             * `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.
             *
             * `NS`: The name server. Must be a valid domain. Required.
             *
             * `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.
             *
             * - Must have less than 254 total characters.
             * - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
             * - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
             * - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.
             *
             * `CNAME`: The alias. Must be a valid domain. Required.
             *
             * `TXT`: The value. Required.
             *
             * `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
             * To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
             * `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
             * target set to `sample.com`. Required.
             *
             * `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
             * tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:
             *
             * - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
             * - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
             * - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.
             *
             * `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
             * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
             *
             * With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
             */
            target: string;
            /**
             * "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. Valid values are 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200 - any other value will be rounded to the nearest valid value.
             */
            ttl_sec: number;
            /**
             * __Filterable__ The type of Record this is in the DNS system. For example, A records associate a domain name with an IPv4 address, and AAAA records associate a domain name with an IPv6 address. For more information, see the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
             */
            type: 'A' | 'AAAA' | 'NS' | 'MX' | 'CNAME' | 'TXT' | 'SRV' | 'PTR' | 'CAA';
            /**
             * __Read-only__ When this Domain Record was last updated.
             */
            readonly updated: string;
            /**
             * The relative weight of this Record used in the case of identical priority. Higher values are preferred. Only valid and required for SRV record requests.
             */
            weight: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetDomainRecordsResponse = GetDomainRecordsResponses[keyof GetDomainRecordsResponses];

export type PostDomainRecordData = {
    /**
     * A single record on a Domain.
     */
    body: {
        /**
         * __Read-only__ When this Domain Record was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Record's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The hostname or FQDN of the Record.
         *
         * `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.
         *
         * `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.
         *
         * - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
         * - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - Must be an empty string (`""`) for a Null MX Record.
         *
         * `CNAME`: The hostname. Must be unique. Required.
         *
         * `TXT`: The hostname.
         *
         * `SRV`: Unused. Use the `service` property to set the service name for this record.
         *
         * `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.
         *
         * `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         */
        name: string;
        /**
         * The port this Record points to. Only valid and required for SRV record requests.
         */
        port: number;
        /**
         * The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.
         *
         * Defaults to `0` for MX record requests. Must be `0` for Null MX records.
         */
        priority: number;
        /**
         * The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically to the submitted value for this property. Only valid for SRV record requests.
         */
        protocol: string | null;
        /**
         * The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended automatically to the submitted value for this property. Only valid and required for SRV record requests.
         */
        service: string | null;
        /**
         * __Filterable__ The tag portion of a CAA record. Only valid and required for CAA record requests.
         */
        tag: 'issue' | 'issuewild' | 'iodef';
        /**
         * __Filterable__ The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.
         *
         * `NS`: The name server. Must be a valid domain. Required.
         *
         * `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.
         *
         * - Must have less than 254 total characters.
         * - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
         * - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.
         *
         * `CNAME`: The alias. Must be a valid domain. Required.
         *
         * `TXT`: The value. Required.
         *
         * `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
         * To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
         * `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
         * target set to `sample.com`. Required.
         *
         * `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
         * tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:
         *
         * - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
         * - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
         * - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.
         *
         * `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         *
         * With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
         */
        target: string;
        /**
         * "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. Valid values are 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200 - any other value will be rounded to the nearest valid value.
         */
        ttl_sec: number;
        /**
         * __Filterable__ The type of Record this is in the DNS system. For example, A records associate a domain name with an IPv4 address, and AAAA records associate a domain name with an IPv6 address. For more information, see the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
         */
        type: 'A' | 'AAAA' | 'NS' | 'MX' | 'CNAME' | 'TXT' | 'SRV' | 'PTR' | 'CAA';
        /**
         * __Read-only__ When this Domain Record was last updated.
         */
        readonly updated: string;
        /**
         * The relative weight of this Record used in the case of identical priority. Higher values are preferred. Only valid and required for SRV record requests.
         */
        weight: number;
    };
    path: {
        /**
         * The ID of the Domain we are accessing Records for.
         */
        domainId: number;
    };
    query?: never;
    url: '/domains/{domainId}/records';
};

export type PostDomainRecordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDomainRecordError = PostDomainRecordErrors[keyof PostDomainRecordErrors];

export type PostDomainRecordResponses = {
    /**
     * A single record on a Domain.
     */
    200: {
        /**
         * __Read-only__ When this Domain Record was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Record's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The hostname or FQDN of the Record.
         *
         * `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.
         *
         * `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.
         *
         * - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
         * - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - Must be an empty string (`""`) for a Null MX Record.
         *
         * `CNAME`: The hostname. Must be unique. Required.
         *
         * `TXT`: The hostname.
         *
         * `SRV`: Unused. Use the `service` property to set the service name for this record.
         *
         * `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.
         *
         * `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         */
        name: string;
        /**
         * The port this Record points to. Only valid and required for SRV record requests.
         */
        port: number;
        /**
         * The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.
         *
         * Defaults to `0` for MX record requests. Must be `0` for Null MX records.
         */
        priority: number;
        /**
         * The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically to the submitted value for this property. Only valid for SRV record requests.
         */
        protocol: string | null;
        /**
         * The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended automatically to the submitted value for this property. Only valid and required for SRV record requests.
         */
        service: string | null;
        /**
         * __Filterable__ The tag portion of a CAA record. Only valid and required for CAA record requests.
         */
        tag: 'issue' | 'issuewild' | 'iodef';
        /**
         * __Filterable__ The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.
         *
         * `NS`: The name server. Must be a valid domain. Required.
         *
         * `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.
         *
         * - Must have less than 254 total characters.
         * - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
         * - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.
         *
         * `CNAME`: The alias. Must be a valid domain. Required.
         *
         * `TXT`: The value. Required.
         *
         * `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
         * To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
         * `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
         * target set to `sample.com`. Required.
         *
         * `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
         * tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:
         *
         * - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
         * - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
         * - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.
         *
         * `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         *
         * With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
         */
        target: string;
        /**
         * "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. Valid values are 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200 - any other value will be rounded to the nearest valid value.
         */
        ttl_sec: number;
        /**
         * __Filterable__ The type of Record this is in the DNS system. For example, A records associate a domain name with an IPv4 address, and AAAA records associate a domain name with an IPv6 address. For more information, see the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
         */
        type: 'A' | 'AAAA' | 'NS' | 'MX' | 'CNAME' | 'TXT' | 'SRV' | 'PTR' | 'CAA';
        /**
         * __Read-only__ When this Domain Record was last updated.
         */
        readonly updated: string;
        /**
         * The relative weight of this Record used in the case of identical priority. Higher values are preferred. Only valid and required for SRV record requests.
         */
        weight: number;
    };
};

export type PostDomainRecordResponse = PostDomainRecordResponses[keyof PostDomainRecordResponses];

export type DeleteDomainRecordData = {
    body?: never;
    path: {
        /**
         * The ID of the Domain whose Record you are accessing.
         */
        domainId: number;
        /**
         * The ID of the Record you are accessing.
         */
        recordId: number;
    };
    query?: never;
    url: '/domains/{domainId}/records/{recordId}';
};

export type DeleteDomainRecordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteDomainRecordError = DeleteDomainRecordErrors[keyof DeleteDomainRecordErrors];

export type DeleteDomainRecordResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteDomainRecordResponse = DeleteDomainRecordResponses[keyof DeleteDomainRecordResponses];

export type GetDomainRecordData = {
    body?: never;
    path: {
        /**
         * The ID of the Domain whose Record you are accessing.
         */
        domainId: number;
        /**
         * The ID of the Record you are accessing.
         */
        recordId: number;
    };
    query?: never;
    url: '/domains/{domainId}/records/{recordId}';
};

export type GetDomainRecordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDomainRecordError = GetDomainRecordErrors[keyof GetDomainRecordErrors];

export type GetDomainRecordResponses = {
    /**
     * A single record on a Domain.
     */
    200: {
        /**
         * __Read-only__ When this Domain Record was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Record's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The hostname or FQDN of the Record.
         *
         * `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.
         *
         * `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.
         *
         * - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
         * - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - Must be an empty string (`""`) for a Null MX Record.
         *
         * `CNAME`: The hostname. Must be unique. Required.
         *
         * `TXT`: The hostname.
         *
         * `SRV`: Unused. Use the `service` property to set the service name for this record.
         *
         * `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.
         *
         * `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         */
        name: string;
        /**
         * The port this Record points to. Only valid and required for SRV record requests.
         */
        port: number;
        /**
         * The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.
         *
         * Defaults to `0` for MX record requests. Must be `0` for Null MX records.
         */
        priority: number;
        /**
         * The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically to the submitted value for this property. Only valid for SRV record requests.
         */
        protocol: string | null;
        /**
         * The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended automatically to the submitted value for this property. Only valid and required for SRV record requests.
         */
        service: string | null;
        /**
         * __Filterable__ The tag portion of a CAA record. Only valid and required for CAA record requests.
         */
        tag: 'issue' | 'issuewild' | 'iodef';
        /**
         * __Filterable__ The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.
         *
         * `NS`: The name server. Must be a valid domain. Required.
         *
         * `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.
         *
         * - Must have less than 254 total characters.
         * - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
         * - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.
         *
         * `CNAME`: The alias. Must be a valid domain. Required.
         *
         * `TXT`: The value. Required.
         *
         * `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
         * To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
         * `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
         * target set to `sample.com`. Required.
         *
         * `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
         * tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:
         *
         * - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
         * - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
         * - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.
         *
         * `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         *
         * With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
         */
        target: string;
        /**
         * "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. Valid values are 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200 - any other value will be rounded to the nearest valid value.
         */
        ttl_sec: number;
        /**
         * __Filterable__ The type of Record this is in the DNS system. For example, A records associate a domain name with an IPv4 address, and AAAA records associate a domain name with an IPv6 address. For more information, see the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
         */
        type: 'A' | 'AAAA' | 'NS' | 'MX' | 'CNAME' | 'TXT' | 'SRV' | 'PTR' | 'CAA';
        /**
         * __Read-only__ When this Domain Record was last updated.
         */
        readonly updated: string;
        /**
         * The relative weight of this Record used in the case of identical priority. Higher values are preferred. Only valid and required for SRV record requests.
         */
        weight: number;
    };
};

export type GetDomainRecordResponse = GetDomainRecordResponses[keyof GetDomainRecordResponses];

export type PutDomainRecordData = {
    /**
     * A Domain Record Update request object.
     */
    body: {
        /**
         * __Filterable__ The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The hostname or FQDN of the Record.
         *
         * `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.
         *
         * `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.
         *
         * - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
         * - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - Must be an empty string (`""`) for a Null MX Record.
         *
         * `CNAME`: The hostname. Must be unique. Required.
         *
         * `TXT`: The hostname.
         *
         * `SRV`: Unused. Use the `service` property to set the service name for this record.
         *
         * `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.
         *
         * `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         */
        name: string;
        /**
         * The port this Record points to. Only valid and required for SRV record requests.
         */
        port: number;
        /**
         * The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.
         *
         * Defaults to `0` for MX record requests. Must be `0` for Null MX records.
         */
        priority: number;
        /**
         * The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically to the submitted value for this property. Only valid for SRV record requests.
         */
        protocol: string | null;
        /**
         * The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended automatically to the submitted value for this property. Only valid and required for SRV record requests.
         */
        service: string | null;
        /**
         * __Filterable__ The tag portion of a CAA record. Only valid and required for CAA record requests.
         */
        tag: 'issue' | 'issuewild' | 'iodef';
        /**
         * __Filterable__ The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.
         *
         * `NS`: The name server. Must be a valid domain. Required.
         *
         * `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.
         *
         * - Must have less than 254 total characters.
         * - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
         * - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.
         *
         * `CNAME`: The alias. Must be a valid domain. Required.
         *
         * `TXT`: The value. Required.
         *
         * `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
         * To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
         * `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
         * target set to `sample.com`. Required.
         *
         * `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
         * tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:
         *
         * - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
         * - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
         * - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.
         *
         * `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         *
         * With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
         */
        target: string;
        /**
         * "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. Valid values are 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200 - any other value will be rounded to the nearest valid value.
         */
        ttl_sec: number;
        /**
         * The relative weight of this Record used in the case of identical priority. Higher values are preferred. Only valid and required for SRV record requests.
         */
        weight: number;
    };
    path: {
        /**
         * The ID of the Domain whose Record you are accessing.
         */
        domainId: number;
        /**
         * The ID of the Record you are accessing.
         */
        recordId: number;
    };
    query?: never;
    url: '/domains/{domainId}/records/{recordId}';
};

export type PutDomainRecordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutDomainRecordError = PutDomainRecordErrors[keyof PutDomainRecordErrors];

export type PutDomainRecordResponses = {
    /**
     * A single record on a Domain.
     */
    200: {
        /**
         * __Read-only__ When this Domain Record was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Record's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The hostname or FQDN of the Record.
         *
         * `NS`: The subdomain, if any, to use with the Domain of the Record. Wildcard NS records (`*`) are not supported.
         *
         * `MX`: The mail subdomain. For example, `sub` for the address `user@sub.example.com` under the `example.com` Domain.
         *
         * - The left-most subdomain component may be an asterisk (`*`) to designate a wildcard subdomain.
         * - Other subdomain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - Must be an empty string (`""`) for a Null MX Record.
         *
         * `CNAME`: The hostname. Must be unique. Required.
         *
         * `TXT`: The hostname.
         *
         * `SRV`: Unused. Use the `service` property to set the service name for this record.
         *
         * `CAA`: The subdomain. Omit or enter an empty string (`""`) to apply to the entire Domain.
         *
         * `PTR`: See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         */
        name: string;
        /**
         * The port this Record points to. Only valid and required for SRV record requests.
         */
        port: number;
        /**
         * The priority of the target host for this Record. Lower values are preferred. Only valid for MX and SRV record requests. Required for SRV record requests.
         *
         * Defaults to `0` for MX record requests. Must be `0` for Null MX records.
         */
        priority: number;
        /**
         * The protocol this Record's service communicates with. An underscore (`_`) is prepended automatically to the submitted value for this property. Only valid for SRV record requests.
         */
        protocol: string | null;
        /**
         * The name of the service. An underscore (`_`) is prepended and a period (`.`) is appended automatically to the submitted value for this property. Only valid and required for SRV record requests.
         */
        service: string | null;
        /**
         * __Filterable__ The tag portion of a CAA record. Only valid and required for CAA record requests.
         */
        tag: 'issue' | 'issuewild' | 'iodef';
        /**
         * __Filterable__ The target for this Record. For requests, this property's actual usage and whether it is required depends on the type of record this represents:
         *
         * `A` and `AAAA`: The IP address. Use `[remote_addr]` to submit the IPv4 address of the request. Required.
         *
         * `NS`: The name server. Must be a valid domain. Required.
         *
         * `MX`: The mail server. Must be a valid domain unless creating a Null MX Record. Required.
         *
         * - Must have less than 254 total characters.
         * - The left-most domain component may be an asterisk (`*`) to designate a wildcard domain.
         * - Other domain components must only contain letters, digits, and hyphens, start with a letter, end with a letter or digit, and contain less than 64 characters.
         * - To create a [Null MX Record](https://datatracker.ietf.org/doc/html/rfc7505), first [remove](https://techdocs.akamai.com/linode-api/reference/delete-domain-record) any additional MX records, then create an MX record with empty strings (`""`) for the `target` and `name`. If a Domain has a Null MX record, new MX records cannot be created.
         *
         * `CNAME`: The alias. Must be a valid domain. Required.
         *
         * `TXT`: The value. Required.
         *
         * `SRV`: The target domain or subdomain. If a subdomain is entered, it is automatically used with the Domain.
         * To configure for a different domain, enter a valid FQDN. For example, the value `www` with a Domain for
         * `example.com` results in a target set to `www.example.com`, whereas the value `sample.com` results in a
         * target set to `sample.com`. Required.
         *
         * `CAA`: The value. For `issue` or `issuewild` tags, the domain of your certificate issuer. For the `iodef`
         * tag, a contact or submission URL (domain, http, https, or mailto). Requirements depend on the tag for this record:
         *
         * - `issue`: The domain of your certificate issuer. Must include a valid domain. May include additional parameters separated with semicolons (`;`), for example: `www.example.com; foo=bar`
         * - `issuewild`: The domain of your wildcard certificate issuer. Must be a valid domain and must not start with an asterisk (`*`).
         * - `iodef`: Must be either (1) a valid domain, (2) a valid domain prepended with `http://` or `https://`, or (3) a valid email address prepended with `mailto:`.
         *
         * `PTR`: Required. See our guide on how to [Configure Your Linode for Reverse DNS
         * (rDNS)](https://www.linode.com/docs/guides/configure-rdns/).
         *
         * With the exception of A, AAAA, and CAA records, this field accepts a trailing period.
         */
        target: string;
        /**
         * "Time to Live" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. Valid values are 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200 - any other value will be rounded to the nearest valid value.
         */
        ttl_sec: number;
        /**
         * __Filterable__ The type of Record this is in the DNS system. For example, A records associate a domain name with an IPv4 address, and AAAA records associate a domain name with an IPv6 address. For more information, see the guides on [DNS Record Types](https://www.linode.com/docs/products/networking/dns-manager/guides/#dns-record-types).
         */
        type: 'A' | 'AAAA' | 'NS' | 'MX' | 'CNAME' | 'TXT' | 'SRV' | 'PTR' | 'CAA';
        /**
         * __Read-only__ When this Domain Record was last updated.
         */
        readonly updated: string;
        /**
         * The relative weight of this Record used in the case of identical priority. Higher values are preferred. Only valid and required for SRV record requests.
         */
        weight: number;
    };
};

export type PutDomainRecordResponse = PutDomainRecordResponses[keyof PutDomainRecordResponses];

export type GetDomainZoneData = {
    body?: never;
    path: {
        /**
         * ID of the Domain.
         */
        domainId: string;
    };
    query?: never;
    url: '/domains/{domainId}/zone-file';
};

export type GetDomainZoneErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDomainZoneError = GetDomainZoneErrors[keyof GetDomainZoneErrors];

export type GetDomainZoneResponses = {
    /**
     * An array containing the lines of the domain zone file.
     */
    200: {
        /**
         * The lines of the zone file for the last rendered zone for this domain.
         */
        zone_file: Array<string>;
    };
};

export type GetDomainZoneResponse = GetDomainZoneResponses[keyof GetDomainZoneResponses];

export type GetImagesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/images';
};

export type GetImagesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetImagesError = GetImagesErrors[keyof GetImagesErrors];

export type GetImagesResponses = {
    /**
     * A paginated list of images.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ A list of the possible capabilities of this image.
             *
             * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
             *
             * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
             */
            readonly capabilities: Array<string>;
            /**
             * __Read-only__ When this image was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The name of the user who created this image, or `linode` for public images.
             */
            readonly created_by: string;
            /**
             * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
             */
            readonly deprecated: boolean;
            /**
             * A detailed description of this image.
             */
            description: string | null;
            /**
             * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
             */
            readonly eol: string | null;
            /**
             * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
             */
            readonly expiry: string | null;
            /**
             * __Read-only__ The unique identifier for each image.
             */
            readonly id: string;
            /**
             * Details about image sharing.
             */
            image_sharing: {
                /**
                 * This field is `null` in this case, meaning the image is not shared by any group. See the `shared_with` field for details on the image's share group.
                 */
                shared_by: {
                    [key: string]: unknown;
                } | null;
                /**
                 * Specifies the group that the image is shared with, including the number of share groups and a URL to view the share groups' list.
                 */
                shared_with: {
                    /**
                     * The number of share groups the image is shared with.
                     */
                    sharegroup_count: number;
                    /**
                     * A URL to view the list of share groups the image is shared with.
                     */
                    sharegroup_list_url: string;
                };
            } | {
                /**
                 * Specifies the details of the share group that is sharing the image.
                 */
                shared_by: {
                    /**
                     * The share group's identifier.
                     */
                    sharegroup_id: number;
                    /**
                     * The share group's descriptive name.
                     */
                    sharegroup_label: string;
                    /**
                     * The share group's unique identifier.
                     */
                    sharegroup_uuid: string;
                    /**
                     * The source image's slug identifier. `null` if no specific source image is referenced.
                     */
                    source_image_id: string | null;
                };
                /**
                 * This field is `null` in this case, meaning the image is not shared with anyone. See the `shared_by` fields for additional details about this shared image.
                 */
                shared_with: {
                    [key: string]: unknown;
                } | null;
            };
            /**
             * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
             */
            readonly is_public: boolean;
            /**
             * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
             */
            is_shared: true | false | 'none';
            /**
             * __Filterable__ A short description of the image.
             */
            label: string;
            /**
             * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
             */
            readonly regions: Array<{
                /**
                 * The unique identifier for the core compute region where this image is stored.
                 */
                region: string;
                /**
                 * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
                 */
                status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
            }>;
            /**
             * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
             */
            readonly size: number;
            /**
             * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
             *
             * > 📘
             * >
             * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
             */
            status: 'creating' | 'pending_upload' | 'available';
            /**
             * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
             */
            tags: Array<string>;
            /**
             * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
             *
             * > 📘
             * >
             * > This object is empty for existing images. It's intended for use with future functionality.
             */
            readonly total_size: number;
            /**
             * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
             */
            type: 'manual' | 'automatic' | 'shared';
            /**
             * __Read-only__ When this image was last updated.
             */
            readonly updated: string;
            /**
             * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
             */
            readonly vendor: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetImagesResponse = GetImagesResponses[keyof GetImagesResponses];

export type PostImageData = {
    body?: {
        /**
         * Whether this image supports [cloud-init](https://www.linode.com/docs/guides/write-files-with-cloud-init/).
         */
        cloud_init?: boolean;
        /**
         * A detailed description of this image.
         */
        description?: string;
        /**
         * The ID of the target Linode disk for this image.
         */
        disk_id: number;
        /**
         * The short title for this image. If not provided, the disk's current label is used.
         */
        label?: string;
        /**
         * Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags?: Array<string>;
    };
    path?: never;
    query?: never;
    url: '/images';
};

export type PostImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostImageError = PostImageErrors[keyof PostImageErrors];

export type PostImageResponses = {
    /**
     * Image object.
     */
    200: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         *
         * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
         *
         * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The name of the user who created this image, or `linode` for public images.
         */
        readonly created_by: string;
        /**
         * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * A detailed description of this image.
         */
        description: string | null;
        /**
         * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The unique identifier for each image.
         */
        readonly id: string;
        /**
         * Details about image sharing.
         */
        image_sharing: {
            /**
             * This field is `null` in this case, meaning the image is not shared by any group. See the `shared_with` field for details on the image's share group.
             */
            shared_by: {
                [key: string]: unknown;
            } | null;
            /**
             * Specifies the group that the image is shared with, including the number of share groups and a URL to view the share groups' list.
             */
            shared_with: {
                /**
                 * The number of share groups the image is shared with.
                 */
                sharegroup_count: number;
                /**
                 * A URL to view the list of share groups the image is shared with.
                 */
                sharegroup_list_url: string;
            };
        } | {
            /**
             * Specifies the details of the share group that is sharing the image.
             */
            shared_by: {
                /**
                 * The share group's identifier.
                 */
                sharegroup_id: number;
                /**
                 * The share group's descriptive name.
                 */
                sharegroup_label: string;
                /**
                 * The share group's unique identifier.
                 */
                sharegroup_uuid: string;
                /**
                 * The source image's slug identifier. `null` if no specific source image is referenced.
                 */
                source_image_id: string | null;
            };
            /**
             * This field is `null` in this case, meaning the image is not shared with anyone. See the `shared_by` fields for additional details about this shared image.
             */
            shared_with: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * __Filterable__ A short description of the image.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         *
         * > 📘
         * >
         * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         *
         * > 📘
         * >
         * > This object is empty for existing images. It's intended for use with future functionality.
         */
        readonly total_size: number;
        /**
         * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
         */
        type: 'manual' | 'automatic' | 'shared';
        /**
         * __Read-only__ When this image was last updated.
         */
        readonly updated: string;
        /**
         * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
         */
        readonly vendor: string | null;
    };
};

export type PostImageResponse = PostImageResponses[keyof PostImageResponses];

export type GetSharegroupsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/images/sharegroups';
};

export type GetSharegroupsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupsError = GetSharegroupsErrors[keyof GetSharegroupsErrors];

export type GetSharegroupsResponses = {
    /**
     * A paginated list of sharegroups.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this share group was created.
             */
            readonly created: string;
            /**
             * The share group's detailed description.
             */
            description: string | null;
            /**
             * __Read-only__ When the share group expires.
             */
            readonly expiry: string | null;
            /**
             * The share group's numeric identifier, used primarily as path parameters in URLs.
             */
            id: number;
            /**
             * __Read-only__ The total number of images currently belonging to the share group.
             */
            readonly images_count: number;
            /**
             * __Read-only__ Indicates whether the share group is currently suspended.
             */
            readonly is_suspended: boolean;
            /**
             * __Filterable__ The share group's descriptive name.
             */
            label: string;
            /**
             * __Read-only__ The number of users with access to the share group.
             */
            readonly members_count: number;
            /**
             * __Read-only__ When this share group was last updated.
             */
            readonly updated: string | null;
            /**
             * The share group's unique identifier used for membership token management.
             */
            uuid: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetSharegroupsResponse = GetSharegroupsResponses[keyof GetSharegroupsResponses];

export type PostSharegroupsData = {
    body?: {
        /**
         * A detailed description of this share group.
         */
        description?: string;
        /**
         * A list of image's details, including the ID, label, and description.
         */
        images?: Array<{
            /**
             * The image's description within the share group.
             */
            description?: string;
            /**
             * The private image’s unique slug identifier.
             */
            id: string;
            /**
             * The image's name within the share group.
             */
            label?: string;
        }>;
        /**
         * The share group's descriptive name.
         */
        label: string;
    };
    path?: never;
    query?: never;
    url: '/images/sharegroups';
};

export type PostSharegroupsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostSharegroupsError = PostSharegroupsErrors[keyof PostSharegroupsErrors];

export type PostSharegroupsResponses = {
    /**
     * The share group's details.
     */
    200: {
        /**
         * __Read-only__ When this share group was created.
         */
        readonly created: string;
        /**
         * A detailed description of this share group.
         */
        description: string | null;
        /**
         * __Read-only__ When the share group expires.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
         */
        readonly id: number;
        /**
         * __Read-only__ The total number of images currently belonging to the share group.
         */
        readonly images_count: number;
        /**
         * __Read-only__ If `true`, the share group is currently suspended.
         */
        readonly is_suspended: boolean;
        /**
         * The share group's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ The number of users with access to the share group.
         */
        readonly members_count: number;
        /**
         * __Read-only__ When this share group was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The share group's unique identifier used for membership token management.
         */
        readonly uuid: string;
    };
};

export type PostSharegroupsResponse = PostSharegroupsResponses[keyof PostSharegroupsResponses];

export type GetUserTokensData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/images/sharegroups/tokens';
};

export type GetUserTokensErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetUserTokensError = GetUserTokensErrors[keyof GetUserTokensErrors];

export type GetUserTokensResponses = {
    /**
     * Returns a paginated list of the tokens.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The creation time of this token.
             */
            readonly created: string;
            /**
             * __Read-only__ The time when this token expires.
             */
            readonly expiry: string | null;
            /**
             * __Filterable__ A short description of the token.
             */
            label: string;
            /**
             * __Filterable__, __Read-only__ Associated share group label.
             */
            readonly sharegroup_label: string;
            /**
             * __Filterable__, __Read-only__ The unique identifier of the share group you created a token for.
             */
            readonly sharegroup_uuid: string;
            /**
             * __Filterable__, __Read-only__ Represents the current token status.
             *
             * - `pending`: The token has been created but is not yet active.
             * - `active`: The token is currently active and usable.
             * - `revoked`: The token has been deleted and is no longer valid.
             * - `expired`: The token has passed its validity period and is no longer usable.
             */
            status: 'pending' | 'active' | 'revoked' | 'expired';
            /**
             * __Read-only__ A unique identifier for the token, used to reference it after creation.
             */
            readonly token_uuid: string;
            /**
             * __Read-only__ The time when this token was last updated.
             */
            readonly updated: string | null;
            /**
             * __Filterable__, __Read-only__ UUID identifying the associated share group.
             */
            readonly valid_for_sharegroup_uuid: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetUserTokensResponse = GetUserTokensResponses[keyof GetUserTokensResponses];

export type PostSharegroupTokensData = {
    body?: {
        /**
         * The token's descriptive name.
         */
        label?: string;
        /**
         * The unique identifier of the share group you want to create a token for.
         */
        valid_for_sharegroup_uuid: string;
    };
    path?: never;
    query?: never;
    url: '/images/sharegroups/tokens';
};

export type PostSharegroupTokensErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostSharegroupTokensError = PostSharegroupTokensErrors[keyof PostSharegroupTokensErrors];

export type PostSharegroupTokensResponses = {
    /**
     * The share group membership token's details.
     */
    200: {
        /**
         * __Read-only__ When the token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token expires.
         */
        readonly expiry: string | null;
        /**
         * The token's descriptive name.
         */
        label: string | null;
        /**
         * __Read-only__ The share group's descriptive name.
         */
        readonly sharegroup_label: string;
        /**
         * __Read-only__ The share group's unique identifier.
         */
        readonly sharegroup_uuid: string;
        /**
         * __Read-only__ The membership token's status:
         *
         * - `pending`: The token has been created but is not yet active.
         * - `active`: The token is currently active and usable.
         * - `revoked`: The token has been deleted and is no longer valid.
         * - `expired`: The token has passed its validity period and is no longer usable.
         */
        status: 'pending' | 'active' | 'revoked' | 'expired';
        /**
         * __Read-only__ The single-use JWT membership token encoded as a string. It consists of three parts: header, payload, and signature. Shared image groups owners can use it to [Add members to a share group](https://techdocs.akamai.com/linode-api/reference/post-sharegroup-members).
         */
        readonly token: string;
        /**
         * __Read-only__ The membership token's unique identifier.
         */
        readonly token_uuid: string;
        /**
         * __Read-only__ When the token was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The unique identifier of the share group you created a token for.
         */
        readonly valid_for_sharegroup_uuid: string;
    };
};

export type PostSharegroupTokensResponse = PostSharegroupTokensResponses[keyof PostSharegroupTokensResponses];

export type DeleteSharegroupTokenData = {
    body?: never;
    path: {
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/tokens/{tokenUuid}';
};

export type DeleteSharegroupTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteSharegroupTokenError = DeleteSharegroupTokenErrors[keyof DeleteSharegroupTokenErrors];

export type DeleteSharegroupTokenResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteSharegroupTokenResponse = DeleteSharegroupTokenResponses[keyof DeleteSharegroupTokenResponses];

export type GetSharegroupTokenData = {
    body?: never;
    path: {
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/tokens/{tokenUuid}';
};

export type GetSharegroupTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupTokenError = GetSharegroupTokenErrors[keyof GetSharegroupTokenErrors];

export type GetSharegroupTokenResponses = {
    /**
     * ImageShareGroupMemberTokens object.
     */
    200: {
        /**
         * __Read-only__ When this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token expires.
         */
        readonly expiry: string | null;
        /**
         * A short description of the token.
         */
        label: string;
        /**
         * __Read-only__ The share group's descriptive name.
         */
        readonly sharegroup_label: string;
        /**
         * __Read-only__ The share group's unique identifier.
         */
        readonly sharegroup_uuid: string;
        /**
         * __Read-only__ Represents the current token status.
         *
         * - `pending`: The token has been created but is not yet active.
         * - `active`: The token is currently active and usable.
         * - `revoked`: The token has been deleted and is no longer valid.
         * - `expired`: The token has passed its validity period and is no longer usable.
         */
        status: 'pending' | 'active' | 'revoked' | 'expired';
        /**
         * __Read-only__ A unique identifier for the token, used to reference it after creation.
         */
        readonly token_uuid: string;
        /**
         * __Read-only__ When this token was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The unique identifier of the share group you created a token for.
         */
        readonly valid_for_sharegroup_uuid: string;
    };
};

export type GetSharegroupTokenResponse = GetSharegroupTokenResponses[keyof GetSharegroupTokenResponses];

export type PutSharegroupTokenData = {
    /**
     * The membership token's details to update.
     */
    body?: {
        /**
         * The descriptive name of the token.
         */
        label: string;
    };
    path: {
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/tokens/{tokenUuid}';
};

export type PutSharegroupTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutSharegroupTokenError = PutSharegroupTokenErrors[keyof PutSharegroupTokenErrors];

export type PutSharegroupTokenResponses = {
    /**
     * ImageShareGroupMemberTokens object.
     */
    200: {
        /**
         * __Read-only__ When this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token expires.
         */
        readonly expiry: string | null;
        /**
         * A short description of the token.
         */
        label: string;
        /**
         * __Read-only__ The share group's descriptive name.
         */
        readonly sharegroup_label: string;
        /**
         * __Read-only__ The share group's unique identifier.
         */
        readonly sharegroup_uuid: string;
        /**
         * __Read-only__ Represents the current token status.
         *
         * - `pending`: The token has been created but is not yet active.
         * - `active`: The token is currently active and usable.
         * - `revoked`: The token has been deleted and is no longer valid.
         * - `expired`: The token has passed its validity period and is no longer usable.
         */
        status: 'pending' | 'active' | 'revoked' | 'expired';
        /**
         * __Read-only__ A unique identifier for the token, used to reference it after creation.
         */
        readonly token_uuid: string;
        /**
         * __Read-only__ When this token was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The unique identifier of the share group you created a token for.
         */
        readonly valid_for_sharegroup_uuid: string;
    };
};

export type PutSharegroupTokenResponse = PutSharegroupTokenResponses[keyof PutSharegroupTokenResponses];

export type GetSharegroupByTokenData = {
    body?: never;
    path: {
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/tokens/{tokenUuid}/sharegroup';
};

export type GetSharegroupByTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupByTokenError = GetSharegroupByTokenErrors[keyof GetSharegroupByTokenErrors];

export type GetSharegroupByTokenResponses = {
    /**
     * The share group's details.
     */
    200: {
        /**
         * __Read-only__ When this share group was created.
         */
        readonly created: string;
        /**
         * A detailed description of this share group.
         */
        description: string | null;
        /**
         * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
         */
        readonly id: number;
        /**
         * __Read-only__ If `true`, the share group is currently suspended.
         */
        readonly is_suspended: boolean;
        /**
         * The share group's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ When this share group was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The share group's unique identifier used for membership token management.
         */
        readonly uuid: string;
    };
};

export type GetSharegroupByTokenResponse = GetSharegroupByTokenResponses[keyof GetSharegroupByTokenResponses];

export type GetSharegroupImagesByTokenData = {
    body?: never;
    path: {
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/tokens/{tokenUuid}/sharegroup/images';
};

export type GetSharegroupImagesByTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupImagesByTokenError = GetSharegroupImagesByTokenErrors[keyof GetSharegroupImagesByTokenErrors];

export type GetSharegroupImagesByTokenResponses = {
    /**
     * A paginated list of shared images in a share group.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ A list of the possible capabilities of this image.
             */
            readonly capabilities: Array<string>;
            /**
             * __Read-only__ When this image share was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The shared image creator's username.
             */
            readonly created_by: string | null;
            /**
             * __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
             */
            readonly deprecated: boolean;
            /**
             * The shared image's detailed description.
             */
            description: string;
            /**
             * __Read-only__ The time of the public image's planned removal from service. `null` for private images.
             */
            readonly eol: string | null;
            /**
             * __Read-only__ Only images created automatically from a deleted compute instance (`type=automatic`) expire, `null` for private images.
             */
            readonly expiry: string | null;
            /**
             * __Read-only__ The shared image's unique idenfifier.
             */
            readonly id: string;
            /**
             * __Read-only__ Sharing information for the image, including who it was shared by and with.
             */
            readonly image_sharing: {
                shared_by: {
                    /**
                     * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
                     */
                    readonly sharegroup_id: number;
                    /**
                     * The share group's descriptive name.
                     */
                    sharegroup_label: string;
                    /**
                     * __Read-only__ The share group's unique identifier used for membership token management.
                     */
                    readonly sharegroup_uuid: string;
                    /**
                     * __Read-only__ The source image's slug identifier.
                     */
                    readonly source_image_id: string;
                };
                /**
                 * Details about who this image was shared with.
                 */
                shared_with: {
                    [key: string]: unknown;
                } | null;
            };
            /**
             * __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
             */
            readonly is_public: boolean;
            /**
             * __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
             */
            is_shared: true | false | 'none';
            /**
             * __Filterable__ The shared image's name.
             */
            label: string;
            /**
             * __Filterable__, __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
             */
            readonly regions: Array<{
                /**
                 * The unique identifier for the core compute region where this image is stored.
                 */
                region: string;
                /**
                 * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
                 */
                status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
            }>;
            /**
             * __Read-only__ The minimum size in MB this image needs to deploy.
             */
            readonly size: number;
            /**
             * __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
             */
            status: 'creating' | 'pending_upload' | 'available';
            /**
             * __Filterable__, __Read-only__ The shared image's organizational tags. A tag can be from 3 to 100 characters long, and each image can include up to 500 tags.
             */
            readonly tags: Array<string>;
            /**
             * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
             */
            readonly total_size: number;
            /**
             * __Filterable__, __Read-only__ Indicates how the image was created. It's always `shared` for images in a shared group. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
             */
            type: 'manual' | 'automatic' | 'shared';
            /**
             * __Read-only__ When this image share was last updated.
             */
            readonly updated: string | null;
            /**
             * __Read-only__ The upstream distribution vendor, `null` for private and shared images.
             */
            readonly vendor: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetSharegroupImagesByTokenResponse = GetSharegroupImagesByTokenResponses[keyof GetSharegroupImagesByTokenResponses];

export type DeleteSharegroupData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}';
};

export type DeleteSharegroupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteSharegroupError = DeleteSharegroupErrors[keyof DeleteSharegroupErrors];

export type DeleteSharegroupResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteSharegroupResponse = DeleteSharegroupResponses[keyof DeleteSharegroupResponses];

export type GetSharegroupData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}';
};

export type GetSharegroupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupError = GetSharegroupErrors[keyof GetSharegroupErrors];

export type GetSharegroupResponses = {
    /**
     * The share group's details.
     */
    200: {
        /**
         * __Read-only__ When this share group was created.
         */
        readonly created: string;
        /**
         * A detailed description of this share group.
         */
        description: string | null;
        /**
         * __Read-only__ When the share group expires.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
         */
        readonly id: number;
        /**
         * __Read-only__ The total number of images currently belonging to the share group.
         */
        readonly images_count: number;
        /**
         * __Read-only__ If `true`, the share group is currently suspended.
         */
        readonly is_suspended: boolean;
        /**
         * The share group's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ The number of users with access to the share group.
         */
        readonly members_count: number;
        /**
         * __Read-only__ When this share group was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The share group's unique identifier used for membership token management.
         */
        readonly uuid: string;
    };
};

export type GetSharegroupResponse = GetSharegroupResponses[keyof GetSharegroupResponses];

export type PutSharegroupData = {
    /**
     * The share group's details to update.
     */
    body: {
        /**
         * A detailed description of this share group.
         */
        description: string;
        /**
         * The share group's descriptive name.
         */
        label: string;
    };
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}';
};

export type PutSharegroupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutSharegroupError = PutSharegroupErrors[keyof PutSharegroupErrors];

export type PutSharegroupResponses = {
    /**
     * The share group's details.
     */
    200: {
        /**
         * __Read-only__ When this share group was created.
         */
        readonly created: string;
        /**
         * A detailed description of this share group.
         */
        description: string | null;
        /**
         * __Read-only__ When the share group expires.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
         */
        readonly id: number;
        /**
         * __Read-only__ The total number of images currently belonging to the share group.
         */
        readonly images_count: number;
        /**
         * __Read-only__ If `true`, the share group is currently suspended.
         */
        readonly is_suspended: boolean;
        /**
         * The share group's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ The number of users with access to the share group.
         */
        readonly members_count: number;
        /**
         * __Read-only__ When this share group was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The share group's unique identifier used for membership token management.
         */
        readonly uuid: string;
    };
};

export type PutSharegroupResponse = PutSharegroupResponses[keyof PutSharegroupResponses];

export type GetSharegroupImagesData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/images';
};

export type GetSharegroupImagesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupImagesError = GetSharegroupImagesErrors[keyof GetSharegroupImagesErrors];

export type GetSharegroupImagesResponses = {
    /**
     * Returns a paginated list of shared images within a given group.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ A list of the possible capabilities of this image.
             */
            readonly capabilities: Array<string>;
            /**
             * __Read-only__ When this image share was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The shared image creator's username.
             */
            readonly created_by: string | null;
            /**
             * __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
             */
            readonly deprecated: boolean;
            /**
             * The shared image's detailed description.
             */
            description: string;
            /**
             * __Read-only__ The time of the public image's planned removal from service. `null` for private images.
             */
            readonly eol: string | null;
            /**
             * __Read-only__ Only images created automatically from a deleted compute instance (`type=automatic`) expire, `null` for private images.
             */
            readonly expiry: string | null;
            /**
             * __Read-only__ The shared image's unique idenfifier.
             */
            readonly id: string;
            /**
             * __Read-only__ Sharing information for the image, including who it was shared by and with.
             */
            readonly image_sharing: {
                shared_by: {
                    /**
                     * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
                     */
                    readonly sharegroup_id: number;
                    /**
                     * The share group's descriptive name.
                     */
                    sharegroup_label: string;
                    /**
                     * __Read-only__ The share group's unique identifier used for membership token management.
                     */
                    readonly sharegroup_uuid: string;
                    /**
                     * __Read-only__ The source image's slug identifier.
                     */
                    readonly source_image_id: string;
                };
                /**
                 * Details about who this image was shared with.
                 */
                shared_with: {
                    [key: string]: unknown;
                } | null;
            };
            /**
             * __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
             */
            readonly is_public: boolean;
            /**
             * __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
             */
            is_shared: true | false | 'none';
            /**
             * __Filterable__ The shared image's name.
             */
            label: string;
            /**
             * __Filterable__, __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
             */
            readonly regions: Array<{
                /**
                 * The unique identifier for the core compute region where this image is stored.
                 */
                region: string;
                /**
                 * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
                 */
                status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
            }>;
            /**
             * __Read-only__ The minimum size in MB this image needs to deploy.
             */
            readonly size: number;
            /**
             * __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
             */
            status: 'creating' | 'pending_upload' | 'available';
            /**
             * __Filterable__, __Read-only__ The shared image's organizational tags. A tag can be from 3 to 100 characters long, and each image can include up to 500 tags.
             */
            readonly tags: Array<string>;
            /**
             * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
             */
            readonly total_size: number;
            /**
             * __Filterable__, __Read-only__ Indicates how the image was created. It's always `shared` for images in a shared group. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
             */
            type: 'manual' | 'automatic' | 'shared';
            /**
             * __Read-only__ When this image share was last updated.
             */
            readonly updated: string | null;
            /**
             * __Read-only__ The upstream distribution vendor, `null` for private and shared images.
             */
            readonly vendor: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetSharegroupImagesResponse = GetSharegroupImagesResponses[keyof GetSharegroupImagesResponses];

export type PostSharegroupImagesData = {
    body?: {
        images: Array<{
            /**
             * The image's description within the share group.
             */
            description?: string;
            /**
             * The private image’s unique slug identifier.
             */
            id: string;
            /**
             * The image's name within the share group.
             */
            label?: string;
        }>;
    };
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/images';
};

export type PostSharegroupImagesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostSharegroupImagesError = PostSharegroupImagesErrors[keyof PostSharegroupImagesErrors];

export type PostSharegroupImagesResponses = {
    /**
     * The shared image's details.
     */
    200: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was shared.
         */
        readonly created: string;
        /**
         * __Read-only__ The shared image creator's username.
         */
        readonly created_by: string | null;
        /**
         * __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * The shared image's detailed description.
         */
        description: string;
        /**
         * __Read-only__ The time of the public image's planned removal from service. `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (`type=automatic`) expire, `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The shared image’s unique slug identifier.
         */
        readonly id: string;
        /**
         * __Read-only__ The image's sharing details, including the share group and source image data.
         */
        readonly image_sharing: {
            shared_by: {
                /**
                 * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
                 */
                readonly sharegroup_id: number;
                /**
                 * The share group's descriptive name.
                 */
                sharegroup_label: string;
                /**
                 * __Read-only__ The share group's unique identifier used for membership token management.
                 */
                readonly sharegroup_uuid: string;
                /**
                 * __Read-only__ The source image's slug identifier.
                 */
                readonly source_image_id: string;
            };
            /**
             * The share group's and source image details.
             */
            shared_with: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * The shared image's name.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Read-only__ The shared image's organizational tags. A tag can be from 3 to 100 characters long, and each image can include up to 500 tags.
         */
        readonly tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         */
        readonly total_size: number;
        /**
         * __Read-only__ Indicates how the image was created. It's always `shared` for images in a shared group. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
         */
        type: 'manual' | 'automatic' | 'shared';
        /**
         * __Read-only__ When this shared image was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The upstream distribution vendor, `null` for private and shared images.
         */
        readonly vendor: string | null;
    };
};

export type PostSharegroupImagesResponse = PostSharegroupImagesResponses[keyof PostSharegroupImagesResponses];

export type DeleteSharegroupImageshareData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
        /**
         * Slug identifier assigned to the shared image upon sharing.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/images/{imageId}';
};

export type DeleteSharegroupImageshareErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteSharegroupImageshareError = DeleteSharegroupImageshareErrors[keyof DeleteSharegroupImageshareErrors];

export type DeleteSharegroupImageshareResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteSharegroupImageshareResponse = DeleteSharegroupImageshareResponses[keyof DeleteSharegroupImageshareResponses];

export type PutSharegroupImageshareData = {
    /**
     * The shared image's details you want to update.
     */
    body: {
        /**
         * A detailed description of this image share.
         */
        description: string;
        /**
         * The shared image's descriptive name.
         */
        label: string;
    };
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
        /**
         * Slug identifier assigned to the shared image upon sharing.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/images/{imageId}';
};

export type PutSharegroupImageshareErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutSharegroupImageshareError = PutSharegroupImageshareErrors[keyof PutSharegroupImageshareErrors];

export type PutSharegroupImageshareResponses = {
    /**
     * The shared image's details.
     */
    200: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was shared.
         */
        readonly created: string;
        /**
         * __Read-only__ The shared image creator's username.
         */
        readonly created_by: string | null;
        /**
         * __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * The shared image's detailed description.
         */
        description: string;
        /**
         * __Read-only__ The time of the public image's planned removal from service. `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (`type=automatic`) expire, `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The shared image’s unique slug identifier.
         */
        readonly id: string;
        /**
         * __Read-only__ The image's sharing details, including the share group and source image data.
         */
        readonly image_sharing: {
            shared_by: {
                /**
                 * __Read-only__ The share group's numeric identifier, used primarily as path parameters in URLs.
                 */
                readonly sharegroup_id: number;
                /**
                 * The share group's descriptive name.
                 */
                sharegroup_label: string;
                /**
                 * __Read-only__ The share group's unique identifier used for membership token management.
                 */
                readonly sharegroup_uuid: string;
                /**
                 * __Read-only__ The source image's slug identifier.
                 */
                readonly source_image_id: string;
            };
            /**
             * The share group's and source image details.
             */
            shared_with: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * The shared image's name.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Read-only__ The shared image's organizational tags. A tag can be from 3 to 100 characters long, and each image can include up to 500 tags.
         */
        readonly tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         */
        readonly total_size: number;
        /**
         * __Read-only__ Indicates how the image was created. It's always `shared` for images in a shared group. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
         */
        type: 'manual' | 'automatic' | 'shared';
        /**
         * __Read-only__ When this shared image was last updated.
         */
        readonly updated: string | null;
        /**
         * __Read-only__ The upstream distribution vendor, `null` for private and shared images.
         */
        readonly vendor: string | null;
    };
};

export type PutSharegroupImageshareResponse = PutSharegroupImageshareResponses[keyof PutSharegroupImageshareResponses];

export type GetSharegroupMembersData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/members';
};

export type GetSharegroupMembersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupMembersError = GetSharegroupMembersErrors[keyof GetSharegroupMembersErrors];

export type GetSharegroupMembersResponses = {
    /**
     * Returns a paginated list of the current members linked to the targeted share group.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When the user became the share group's member.
             */
            readonly created: string;
            /**
             * __Read-only__ When the user's share group membership expires.
             */
            readonly expiry: string | null;
            /**
             * __Filterable__ The share group member's descriptive name.
             */
            label: string;
            /**
             * __Filterable__, __Read-only__ The current membership status. Share group members can be `active` or `revoked`. Revoked members' data remains available for two weeks after they left a share group.
             */
            status: 'active' | 'revoked';
            /**
             * __Filterable__, __Read-only__ The membership token's unique identifier. Not to be confused with the `token` needed to add a member to the share group.
             */
            readonly token_uuid: string;
            /**
             * __Read-only__ When the user's share group membership was last updated.
             */
            readonly updated: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetSharegroupMembersResponse = GetSharegroupMembersResponses[keyof GetSharegroupMembersResponses];

export type PostSharegroupMembersData = {
    body?: {
        /**
         * The share group member's descriptive name.
         */
        label: string;
        /**
         * The single-use JWT membership token encoded as a string. It consists of three parts: header, payload, and signature. If needed, the user should [Create a token](https://techdocs.akamai.com/linode-api/reference/post-sharegroup-tokens) and share it with the group owner before joining.
         */
        token: string;
    };
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/members';
};

export type PostSharegroupMembersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostSharegroupMembersError = PostSharegroupMembersErrors[keyof PostSharegroupMembersErrors];

export type PostSharegroupMembersResponses = {
    /**
     * The shared group membership's details.
     */
    200: {
        /**
         * __Read-only__ When the user became the share group's member.
         */
        readonly created: string;
        /**
         * __Read-only__ When the user's share group membership expires.
         */
        readonly expiry: string | null;
        /**
         * The share group member's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ The current membership status. Share group members can be `active` or `revoked`. Revoked members' data remains available for two weeks after they left a share group.
         */
        status: 'active' | 'revoked';
        /**
         * __Read-only__ The membership token's unique identifier. Not to be confused with the `token` needed to add a member to the share group.
         */
        readonly token_uuid: string;
        /**
         * __Read-only__ When the user's share group membership was last updated.
         */
        readonly updated: string | null;
    };
};

export type PostSharegroupMembersResponse = PostSharegroupMembersResponses[keyof PostSharegroupMembersResponses];

export type DeleteSharegroupMemberTokenData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/members/{tokenUuid}';
};

export type DeleteSharegroupMemberTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteSharegroupMemberTokenError = DeleteSharegroupMemberTokenErrors[keyof DeleteSharegroupMemberTokenErrors];

export type DeleteSharegroupMemberTokenResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteSharegroupMemberTokenResponse = DeleteSharegroupMemberTokenResponses[keyof DeleteSharegroupMemberTokenResponses];

export type GetSharegroupMemberTokenData = {
    body?: never;
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/members/{tokenUuid}';
};

export type GetSharegroupMemberTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSharegroupMemberTokenError = GetSharegroupMemberTokenErrors[keyof GetSharegroupMemberTokenErrors];

export type GetSharegroupMemberTokenResponses = {
    /**
     * The shared group membership's details.
     */
    200: {
        /**
         * __Read-only__ When the user became the share group's member.
         */
        readonly created: string;
        /**
         * __Read-only__ When the user's share group membership expires.
         */
        readonly expiry: string | null;
        /**
         * The share group member's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ The current membership status. Share group members can be `active` or `revoked`. Revoked members' data remains available for two weeks after they left a share group.
         */
        status: 'active' | 'revoked';
        /**
         * __Read-only__ The membership token's unique identifier. Not to be confused with the `token` needed to add a member to the share group.
         */
        readonly token_uuid: string;
        /**
         * __Read-only__ When the user's share group membership was last updated.
         */
        readonly updated: string | null;
    };
};

export type GetSharegroupMemberTokenResponse = GetSharegroupMemberTokenResponses[keyof GetSharegroupMemberTokenResponses];

export type PutSharegroupMemberTokenData = {
    /**
     * The updated membership token's details.
     */
    body: {
        /**
         * The membership token's descriptive name.
         */
        label: string;
    };
    path: {
        /**
         * The share group's unique identifier assigned after creating it. Not to be confused with the group's `uuid`.
         */
        sharegroupId: number;
        /**
         * A unique identifier for the token, used to reference it after creation.
         */
        tokenUuid: string;
    };
    query?: never;
    url: '/images/sharegroups/{sharegroupId}/members/{tokenUuid}';
};

export type PutSharegroupMemberTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutSharegroupMemberTokenError = PutSharegroupMemberTokenErrors[keyof PutSharegroupMemberTokenErrors];

export type PutSharegroupMemberTokenResponses = {
    /**
     * The shared group membership's details.
     */
    200: {
        /**
         * __Read-only__ When the user became the share group's member.
         */
        readonly created: string;
        /**
         * __Read-only__ When the user's share group membership expires.
         */
        readonly expiry: string | null;
        /**
         * The share group member's descriptive name.
         */
        label: string;
        /**
         * __Read-only__ The current membership status. Share group members can be `active` or `revoked`. Revoked members' data remains available for two weeks after they left a share group.
         */
        status: 'active' | 'revoked';
        /**
         * __Read-only__ The membership token's unique identifier. Not to be confused with the `token` needed to add a member to the share group.
         */
        readonly token_uuid: string;
        /**
         * __Read-only__ When the user's share group membership was last updated.
         */
        readonly updated: string | null;
    };
};

export type PutSharegroupMemberTokenResponse = PutSharegroupMemberTokenResponses[keyof PutSharegroupMemberTokenResponses];

export type PostUploadImageData = {
    /**
     * The uploaded image details.
     */
    body?: {
        /**
         * Whether the uploaded Image supports cloud-init.
         */
        cloud_init?: boolean;
        /**
         * Description for the uploaded image.
         */
        description?: string;
        /**
         * Label for the uploaded image.
         */
        label: string;
        /**
         * The region to upload to. Once uploaded, the image can be used in any region.
         *
         * > 📘
         * >
         * > Only core regions that support our [Object Storage](https://techdocs.akamai.com/cloud-computing/reference/how-to-choose-a-data-center#product-availability) service can store an uploaded image.
         */
        region: string;
        /**
         * Tags you can use to organize images. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags?: Array<string>;
    };
    path?: never;
    query?: never;
    url: '/images/upload';
};

export type PostUploadImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostUploadImageError = PostUploadImageErrors[keyof PostUploadImageErrors];

export type PostUploadImageResponses = {
    /**
     * Image Upload object including the upload URL and image object.
     */
    200: {
        /**
         * Image object.
         */
        image: {
            /**
             * __Read-only__ A list of the possible capabilities of this image.
             *
             * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
             *
             * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
             */
            readonly capabilities: Array<string>;
            /**
             * __Read-only__ When this image was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The name of the user who created this image, or `linode` for public images.
             */
            readonly created_by: string;
            /**
             * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
             */
            readonly deprecated: boolean;
            /**
             * A detailed description of this image.
             */
            description: string | null;
            /**
             * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
             */
            readonly eol: string | null;
            /**
             * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
             */
            readonly expiry: string | null;
            /**
             * __Read-only__ The unique identifier for each image.
             */
            readonly id: string;
            /**
             * Details about image sharing.
             */
            image_sharing: {
                /**
                 * This field is `null` in this case, meaning the image is not shared by any group. See the `shared_with` field for details on the image's share group.
                 */
                shared_by: {
                    [key: string]: unknown;
                } | null;
                /**
                 * Specifies the group that the image is shared with, including the number of share groups and a URL to view the share groups' list.
                 */
                shared_with: {
                    /**
                     * The number of share groups the image is shared with.
                     */
                    sharegroup_count: number;
                    /**
                     * A URL to view the list of share groups the image is shared with.
                     */
                    sharegroup_list_url: string;
                };
            } | {
                /**
                 * Specifies the details of the share group that is sharing the image.
                 */
                shared_by: {
                    /**
                     * The share group's identifier.
                     */
                    sharegroup_id: number;
                    /**
                     * The share group's descriptive name.
                     */
                    sharegroup_label: string;
                    /**
                     * The share group's unique identifier.
                     */
                    sharegroup_uuid: string;
                    /**
                     * The source image's slug identifier. `null` if no specific source image is referenced.
                     */
                    source_image_id: string | null;
                };
                /**
                 * This field is `null` in this case, meaning the image is not shared with anyone. See the `shared_by` fields for additional details about this shared image.
                 */
                shared_with: {
                    [key: string]: unknown;
                } | null;
            };
            /**
             * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
             */
            readonly is_public: boolean;
            /**
             * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
             */
            is_shared: true | false | 'none';
            /**
             * __Filterable__ A short description of the image.
             */
            label: string;
            /**
             * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
             */
            readonly regions: Array<{
                /**
                 * The unique identifier for the core compute region where this image is stored.
                 */
                region: string;
                /**
                 * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
                 */
                status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
            }>;
            /**
             * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
             */
            readonly size: number;
            /**
             * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
             *
             * > 📘
             * >
             * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
             */
            status: 'creating' | 'pending_upload' | 'available';
            /**
             * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
             */
            tags: Array<string>;
            /**
             * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
             *
             * > 📘
             * >
             * > This object is empty for existing images. It's intended for use with future functionality.
             */
            readonly total_size: number;
            /**
             * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
             */
            type: 'manual' | 'automatic' | 'shared';
            /**
             * __Read-only__ When this image was last updated.
             */
            readonly updated: string;
            /**
             * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
             */
            readonly vendor: string | null;
        };
        /**
         * The URL to upload the Image to.
         */
        upload_to: string;
    };
};

export type PostUploadImageResponse = PostUploadImageResponses[keyof PostUploadImageResponses];

export type DeleteImageData = {
    body?: never;
    path: {
        /**
         * The unique identifier assigned to the image after creation.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/{imageId}';
};

export type DeleteImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteImageError = DeleteImageErrors[keyof DeleteImageErrors];

export type DeleteImageResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteImageResponse = DeleteImageResponses[keyof DeleteImageResponses];

export type GetImageData = {
    body?: never;
    path: {
        /**
         * The unique identifier assigned to the image after creation.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/{imageId}';
};

export type GetImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetImageError = GetImageErrors[keyof GetImageErrors];

export type GetImageResponses = {
    /**
     * Image object.
     */
    200: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         *
         * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
         *
         * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The name of the user who created this image, or `linode` for public images.
         */
        readonly created_by: string;
        /**
         * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * A detailed description of this image.
         */
        description: string | null;
        /**
         * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The unique identifier for each image.
         */
        readonly id: string;
        /**
         * Details about image sharing.
         */
        image_sharing: {
            /**
             * This field is `null` in this case, meaning the image is not shared by any group. See the `shared_with` field for details on the image's share group.
             */
            shared_by: {
                [key: string]: unknown;
            } | null;
            /**
             * Specifies the group that the image is shared with, including the number of share groups and a URL to view the share groups' list.
             */
            shared_with: {
                /**
                 * The number of share groups the image is shared with.
                 */
                sharegroup_count: number;
                /**
                 * A URL to view the list of share groups the image is shared with.
                 */
                sharegroup_list_url: string;
            };
        } | {
            /**
             * Specifies the details of the share group that is sharing the image.
             */
            shared_by: {
                /**
                 * The share group's identifier.
                 */
                sharegroup_id: number;
                /**
                 * The share group's descriptive name.
                 */
                sharegroup_label: string;
                /**
                 * The share group's unique identifier.
                 */
                sharegroup_uuid: string;
                /**
                 * The source image's slug identifier. `null` if no specific source image is referenced.
                 */
                source_image_id: string | null;
            };
            /**
             * This field is `null` in this case, meaning the image is not shared with anyone. See the `shared_by` fields for additional details about this shared image.
             */
            shared_with: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * __Filterable__ A short description of the image.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         *
         * > 📘
         * >
         * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         *
         * > 📘
         * >
         * > This object is empty for existing images. It's intended for use with future functionality.
         */
        readonly total_size: number;
        /**
         * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
         */
        type: 'manual' | 'automatic' | 'shared';
        /**
         * __Read-only__ When this image was last updated.
         */
        readonly updated: string;
        /**
         * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
         */
        readonly vendor: string | null;
    };
};

export type GetImageResponse = GetImageResponses[keyof GetImageResponses];

export type PutImageData = {
    body: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         *
         * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
         *
         * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The name of the user who created this image, or `linode` for public images.
         */
        readonly created_by: string;
        /**
         * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * A detailed description of this image.
         */
        description: string | null;
        /**
         * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The unique identifier for each image.
         */
        readonly id: string;
        /**
         * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * __Filterable__ A short description of the image.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         *
         * > 📘
         * >
         * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         *
         * > 📘
         * >
         * > This object is empty for existing images. It's intended for use with future functionality.
         */
        readonly total_size: number;
        /**
         * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance.
         */
        type: 'manual' | 'automatic';
        /**
         * __Read-only__ When this image was last updated.
         */
        readonly updated: string;
        /**
         * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
         */
        readonly vendor: string | null;
    };
    path: {
        /**
         * The unique identifier assigned to the image after creation.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/{imageId}';
};

export type PutImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutImageError = PutImageErrors[keyof PutImageErrors];

export type PutImageResponses = {
    /**
     * Image object.
     */
    200: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         *
         * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
         *
         * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The name of the user who created this image, or `linode` for public images.
         */
        readonly created_by: string;
        /**
         * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * A detailed description of this image.
         */
        description: string | null;
        /**
         * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The unique identifier for each image.
         */
        readonly id: string;
        /**
         * Details about image sharing.
         */
        image_sharing: {
            /**
             * This field is `null` in this case, meaning the image is not shared by any group. See the `shared_with` field for details on the image's share group.
             */
            shared_by: {
                [key: string]: unknown;
            } | null;
            /**
             * Specifies the group that the image is shared with, including the number of share groups and a URL to view the share groups' list.
             */
            shared_with: {
                /**
                 * The number of share groups the image is shared with.
                 */
                sharegroup_count: number;
                /**
                 * A URL to view the list of share groups the image is shared with.
                 */
                sharegroup_list_url: string;
            };
        } | {
            /**
             * Specifies the details of the share group that is sharing the image.
             */
            shared_by: {
                /**
                 * The share group's identifier.
                 */
                sharegroup_id: number;
                /**
                 * The share group's descriptive name.
                 */
                sharegroup_label: string;
                /**
                 * The share group's unique identifier.
                 */
                sharegroup_uuid: string;
                /**
                 * The source image's slug identifier. `null` if no specific source image is referenced.
                 */
                source_image_id: string | null;
            };
            /**
             * This field is `null` in this case, meaning the image is not shared with anyone. See the `shared_by` fields for additional details about this shared image.
             */
            shared_with: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * __Filterable__ A short description of the image.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         *
         * > 📘
         * >
         * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         *
         * > 📘
         * >
         * > This object is empty for existing images. It's intended for use with future functionality.
         */
        readonly total_size: number;
        /**
         * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
         */
        type: 'manual' | 'automatic' | 'shared';
        /**
         * __Read-only__ When this image was last updated.
         */
        readonly updated: string;
        /**
         * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
         */
        readonly vendor: string | null;
    };
};

export type PutImageResponse = PutImageResponses[keyof PutImageResponses];

export type PostReplicateImageData = {
    /**
     * List of regions where the image should be replicated to.
     */
    body: {
        /**
         * The unique identifier for each compute `region`. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        regions: Array<string>;
    };
    path: {
        /**
         * The unique identifier assigned to the image after creation.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/{imageId}/regions';
};

export type PostReplicateImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostReplicateImageError = PostReplicateImageErrors[keyof PostReplicateImageErrors];

export type PostReplicateImageResponses = {
    /**
     * Image object.
     */
    200: {
        /**
         * __Read-only__ A list of the possible capabilities of this image.
         *
         * - `cloud-init`. The image supports the cloud-init multi-distribution method with our [Metadata service](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata/#troubleshoot-metadata-and-cloud-init). This only applies to public images.
         *
         * - `distributed-sites`. Whether the image can be used in distributed compute regions. Compared to a core compute region, distributed compute regions offer limited functionality, but they're globally distributed. Your image can be geographically closer to you, potentially letting you deploy it quicker. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for complete details.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this image was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The name of the user who created this image, or `linode` for public images.
         */
        readonly created_by: string;
        /**
         * __Filterable__, __Read-only__ A `true` value indicates a deprecated image. Only public images can be deprecated.
         */
        readonly deprecated: boolean;
        /**
         * A detailed description of this image.
         */
        description: string | null;
        /**
         * __Read-only__ The time of the public image's planned removal from service. This is `null` for private images.
         */
        readonly eol: string | null;
        /**
         * __Read-only__ Only images created automatically from a deleted compute instance (type=automatic) expire. This is `null` for private images.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ The unique identifier for each image.
         */
        readonly id: string;
        /**
         * Details about image sharing.
         */
        image_sharing: {
            /**
             * This field is `null` in this case, meaning the image is not shared by any group. See the `shared_with` field for details on the image's share group.
             */
            shared_by: {
                [key: string]: unknown;
            } | null;
            /**
             * Specifies the group that the image is shared with, including the number of share groups and a URL to view the share groups' list.
             */
            shared_with: {
                /**
                 * The number of share groups the image is shared with.
                 */
                sharegroup_count: number;
                /**
                 * A URL to view the list of share groups the image is shared with.
                 */
                sharegroup_list_url: string;
            };
        } | {
            /**
             * Specifies the details of the share group that is sharing the image.
             */
            shared_by: {
                /**
                 * The share group's identifier.
                 */
                sharegroup_id: number;
                /**
                 * The share group's descriptive name.
                 */
                sharegroup_label: string;
                /**
                 * The share group's unique identifier.
                 */
                sharegroup_uuid: string;
                /**
                 * The source image's slug identifier. `null` if no specific source image is referenced.
                 */
                source_image_id: string | null;
            };
            /**
             * This field is `null` in this case, meaning the image is not shared with anyone. See the `shared_by` fields for additional details about this shared image.
             */
            shared_with: {
                [key: string]: unknown;
            } | null;
        };
        /**
         * __Filterable__, __Read-only__ A `true` value if the image is a public distribution image. A `false` value indicates private, account-specific images.
         */
        readonly is_public: boolean;
        /**
         * __Filterable__, __Read-only__ A `true` value for shared private images. `none` for images shared within a group.
         */
        is_shared: true | false | 'none';
        /**
         * __Filterable__ A short description of the image.
         */
        label: string;
        /**
         * __Read-only__ Details on the regions where this image is stored. See [Regions and images](https://techdocs.akamai.com/cloud-computing/docs/images#regions-and-images) for full details on support for `regions`.
         */
        readonly regions: Array<{
            /**
             * The unique identifier for the core compute region where this image is stored.
             */
            region: string;
            /**
             * The status of the image in this `region`. Possible values are `available`, `creating`, `pending`, `pending deletion`, `pending replication`, or `replicating`.
             */
            status: 'available' | 'creating' | 'pending' | 'pending deletion' | 'pending replication' | 'replicating';
        }>;
        /**
         * __Filterable__, __Read-only__ The minimum size in MB this image needs to deploy.
         */
        readonly size: number;
        /**
         * __Filterable__, __Read-only__ The current status of the image. Possible values are `available`, `creating`, and `pending_upload`.
         *
         * > 📘
         * >
         * > The `+order_by` and `+order` operators are not available when [filtering](https://techdocs.akamai.com/linode-api/reference/filtering-and-sorting) on this key.
         */
        status: 'creating' | 'pending_upload' | 'available';
        /**
         * __Filterable__ Tags used for organizational purposes. A tag can be from 3 to 100 characters long, and an image can have a maximum of 500 total tags.
         */
        tags: Array<string>;
        /**
         * __Read-only__ The total size in bytes of all instances of this image, in all `regions`.
         *
         * > 📘
         * >
         * > This object is empty for existing images. It's intended for use with future functionality.
         */
        readonly total_size: number;
        /**
         * __Filterable__, __Read-only__ How the image was created. Create a `manual` image at any time. An `automatic` image is created automatically from a deleted compute instance. Other users within share groups can access a `shared` image.
         */
        type: 'manual' | 'automatic' | 'shared';
        /**
         * __Read-only__ When this image was last updated.
         */
        readonly updated: string;
        /**
         * __Filterable__, __Read-only__ The upstream distribution vendor. This is `null` for private images.
         */
        readonly vendor: string | null;
    };
};

export type PostReplicateImageResponse = PostReplicateImageResponses[keyof PostReplicateImageResponses];

export type GetImagesSharegroupsImageData = {
    body?: never;
    path: {
        /**
         * Slug identifier assigned to the private image upon creation. This identifier includes a slash (`/`), which must be URL-encoded in requests to prevent breaking the URL structure.
         */
        imageId: string;
    };
    query?: never;
    url: '/images/{imageId}/sharegroups';
};

export type GetImagesSharegroupsImageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetImagesSharegroupsImageError = GetImagesSharegroupsImageErrors[keyof GetImagesSharegroupsImageErrors];

export type GetImagesSharegroupsImageResponses = {
    /**
     * Returns a paginated list of share groups.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this share group was created.
             */
            readonly created: string;
            /**
             * The share group's detailed description.
             */
            description: string | null;
            /**
             * __Read-only__ When the share group expires.
             */
            readonly expiry: string | null;
            /**
             * The share group's numeric identifier, used primarily as path parameters in URLs.
             */
            id: number;
            /**
             * __Read-only__ The total number of images currently belonging to the share group.
             */
            readonly images_count: number;
            /**
             * __Read-only__ Indicates whether the share group is currently suspended.
             */
            readonly is_suspended: boolean;
            /**
             * __Filterable__ The share group's descriptive name.
             */
            label: string;
            /**
             * __Read-only__ The number of users with access to the share group.
             */
            readonly members_count: number;
            /**
             * __Read-only__ When this share group was last updated.
             */
            readonly updated: string | null;
            /**
             * The share group's unique identifier used for membership token management.
             */
            uuid: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetImagesSharegroupsImageResponse = GetImagesSharegroupsImageResponses[keyof GetImagesSharegroupsImageResponses];

export type GetLinodeInstancesData = {
    body?: never;
    headers?: {
        /**
         * Specifies the `X-Filter` header JSON object's filtering and sort criteria.
         */
        'X-Filter'?: {
            [key: string]: unknown;
        } | {
            /**
             * All conditions need to be true.
             */
            '+and': Array<{
                [key: string]: unknown;
            }>;
            /**
             * The provided string needs to be in the value.
             */
            '+contains': string;
            /**
             * The value needs to be greater than the provided number.
             */
            '+gt': number;
            /**
             * The value needs to be greater than or equal to the provided number.
             */
            '+gte': number;
            /**
             * The value needs to be less than the provided number.
             */
            '+lt': number;
            /**
             * The value needs to be less than or equal to the provided number.
             */
            '+lte': number;
            /**
             * The provided string is left out of the results.
             */
            '+neq': string;
            /**
             * At least one condition needs to be true.
             */
            '+or': Array<{
                [key: string]: unknown;
            }>;
            /**
             * Sort in ascending (`asc`) or descending (`desc`) order. This defaults to `asc`. Requires `+order_by`.
             */
            '+order': 'asc' | 'desc';
            /**
             * Order results based on the provided attribute. The attribute needs to be filterable.
             */
            '+order_by': string;
        };
    };
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/instances';
};

export type GetLinodeInstancesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInstancesError = GetLinodeInstancesErrors[keyof GetLinodeInstancesErrors];

export type GetLinodeInstancesResponses = {
    /**
     * Returns an array of all Linodes on your Account.
     */
    200: {
        data: Array<{
            alerts: {
                /**
                 * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
                 *
                 * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
                 *
                 * The default value is 90% multiplied by the number of cores.
                 *
                 * If the value is set to `0` (zero), the alert is disabled.
                 */
                cpu: number;
                /**
                 * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
                 */
                io: number;
                /**
                 * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
                 */
                network_in: number;
                /**
                 * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
                 */
                network_out: number;
                /**
                 * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
                 */
                transfer_quota: number;
            };
            /**
             * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
             */
            backups: {
                /**
                 * __Read-only__ Whether Backups for this Linode are available for restoration.
                 *
                 * Backups undergoing maintenance are not available for restoration.
                 */
                readonly available: boolean;
                /**
                 * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                 */
                readonly enabled: boolean;
                /**
                 * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
                 */
                readonly last_successful: string;
                schedule: {
                    /**
                     * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                     *
                     * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                     */
                    day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                    /**
                     * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                     *
                     * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                     *
                     * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                     */
                    window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
                };
            };
            /**
             * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
             */
            readonly capabilities: Array<string>;
            /**
             * __Read-only__ When this Linode was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
             */
            disk_encryption: 'enabled' | 'disabled';
            /**
             * __Deprecated__, __Filterable__ The group label for this Linode.
             *
             * @deprecated
             */
            group: string;
            /**
             * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
             */
            readonly has_user_data: boolean;
            /**
             * __Read-only__ The Linode's host machine, as a UUID.
             */
            readonly host_uuid: string;
            /**
             * __Read-only__ The virtualization software powering this Linode.
             */
            hypervisor: 'kvm';
            /**
             * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
             */
            readonly id: number;
            /**
             * An Image ID to deploy the Linode Disk from.
             *
             * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
             */
            image: string | null;
            /**
             * __Beta__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
             */
            interface_generation: 'legacy_config' | 'linode';
            /**
             * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
             *
             * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
             */
            readonly ipv4: Array<string>;
            /**
             * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
             */
            readonly ipv6: string | null;
            /**
             * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
             *
             * Linode labels have the following constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             */
            label: string;
            /**
             * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
             */
            readonly lke_cluster_id: number | null;
            /**
             * __Beta__, __Read-only__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
             */
            maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
            /**
             * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
             */
            readonly placement_group: {
                /**
                 * The placement group's ID. You need to provide it for all operations that affect it.
                 */
                id: number;
                /**
                 * __Filterable__ The unique name set for the placement group. A label has these constraints:
                 *
                 * - It needs to begin and end with an alphanumeric character.
                 * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                 */
                label: string;
                /**
                 * __Read-only__ The unique identifier for the [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups) to which this Linode is being migrated. Displayed as `null` if the Linode is not being migrated to a new placement group.
                 */
                readonly migrating_to: number | null;
                /**
                 * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
                 *
                 * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
                 * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
                 */
                placement_group_policy: 'strict' | 'flexible';
                /**
                 * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
                 *
                 * > 📘
                 * >
                 * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
                 */
                placement_group_type: 'anti_affinity:local';
            } | null;
            /**
             * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
             */
            readonly region: string;
            /**
             * __Read-only__ Information about the resources available to this Linode.
             */
            readonly specs: {
                /**
                 * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
                 */
                readonly disk: number;
                /**
                 * __Read-only__ The number of GPUs this Linode has access to.
                 */
                readonly gpus: number;
                /**
                 * __Read-only__ The amount of RAM, in MB, this Linode has access to.
                 *
                 * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
                 */
                readonly memory: number;
                /**
                 * __Read-only__ The amount of network transfer this Linode is allotted each month.
                 */
                readonly transfer: number;
                /**
                 * __Read-only__ The number of VCPUs this Linode has access to.
                 */
                readonly vcpus: number;
            };
            /**
             * __Read-only__ A brief description of the compute instance's current state. This value can change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
             *
             * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
             * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
             * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
             */
            status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
            /**
             * __Filterable__ Tags to help you organize your content.
             */
            tags: Array<string>;
            /**
             * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
             */
            readonly type: string;
            /**
             * __Read-only__ When this Linode was last updated.
             */
            readonly updated: string;
            /**
             * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
             */
            watchdog_enabled: boolean;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeInstancesResponse = GetLinodeInstancesResponses[keyof GetLinodeInstancesResponses];

export type PostLinodeInstanceData = {
    /**
     * The requested initial state of a new Linode.
     */
    body: {
        /**
         * __Write-only__ A list of public SSH keys that will be automatically appended to the root user's `~/.ssh/authorized_keys` file when deploying from an Image.
         */
        authorized_keys: Array<string>;
        /**
         * __Write-only__ A list of usernames for authorized users. Before you can add a user, it needs an SSH key assigned to its profile. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information. If the usernames already have associated SSH keys, the keys will be appended to the root users `~/.ssh/authorized_keys` file automatically when deploying from an image.
         */
        authorized_users: Array<string>;
        /**
         * __Write-only__ This field defaults to `true` if the Linode is created with an Image or from a Backup. If it is deployed from an Image or a Backup and you wish it to remain `offline` after deployment, set this to `false`.
         */
        booted: boolean;
        /**
         * Local disk encryption ensures that your data stored on Linodes is secured. Disk encryption protects against unauthorized data access by keeping the data encrypted if the disk is ever removed from the data center, decommissioned, or disposed of. The platform manages the encryption and decryption for you.
         *
         * By default, encryption is `enabled` on all Linodes. If you opted out of encryption or if the Linode was created prior to local disk encryption support, you can encrypt your data using [Rebuild](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance).
         */
        disk_encryption: 'enabled' | 'disabled';
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Beta__ Defines the maintenance policy for this Linode. Choose from the following policies:
         *
         * - `linode/migrate`: Migrates the Linode to a new host while it remains fully operational (with some performance degradation). Recommended for maximizing availability.
         * - `linode/power_off_on`: Powers off the Linode at the start of the maintenance event and reboots it once the maintenance finishes. Recommended for maximizing performance.
         *
         * Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Write-only__ An object containing user-defined data relevant to the creation of Linodes.
         */
        metadata: {
            /**
             * Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.
             *
             * Cannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.
             *
             * Must not be included when cloning to an existing Linode.
             *
             * Unencoded data must not exceed 65535 bytes, or about 16kb encoded.
             */
            user_data: string;
        };
        /**
         * __Write-only__ This sets the root user's password on a newly created Linode Disk when deploying from an Image.
         *
         * - __Required__ when creating a Linode Disk from an Image, including when using a StackScript.
         *
         * - Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
         */
        root_pass: string;
        /**
         * This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.
         *
         * This field is required to be valid JSON.
         *
         * Total length cannot exceed 65,535 characters.
         */
        stackscript_data: {
            [key: string]: unknown;
        };
        /**
         * A StackScript ID that will cause the referenced StackScript to be run during deployment of this Linode. A compatible `image` is required to use a StackScript. To get a list of available StackScript and their permitted Images, run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts). This field cannot be used when deploying from a Backup or a Private Image.
         */
        stackscript_id: number;
    } & {
        /**
         * A Backup ID from another Linode's available backups. Your User must have `read_write` access to that Linode, the Backup must have a `status` of `successful`, and the Linode must be deployed to the same `region` as the Backup. Run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups) for a Linode's available backups.
         *
         * This field and the `image` field are mutually exclusive.
         */
        backup_id?: number;
        /**
         * If this field is set to `true`, the created Linode will automatically be enrolled in the Linode Backup service. This will incur an additional charge. The cost for the Backup service is dependent on the Type of Linode deployed.
         *
         * This option is always treated as `true` if the account-wide `backups_enabled` setting is `true`.  See [Get account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings) for more information.
         *
         * Backup pricing is included in the response from [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types)
         */
        backups_enabled?: boolean;
        /**
         * The `id` of the Firewall to attach this Linode to upon creation. This `firewall_id` field is for Linodes using VLAN and legacy configuration interfaces only.
         */
        firewall_id?: number;
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group?: string;
        /**
         * __Beta__ Specifies the interface type for the Linode. The value can be either `legacy_config` or `linode`. The default value is determined by the `interfaces_for_new_linodes` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). If the `interface_generation` option is set to `linode`, legacy configuration interfaces can no longer be used on the Linode.
         * - If `interfaces_for_new_linodes` is set to `linode_only`, set `interface_generation` to `linode` or omit it for Linode interfaces.
         * - If `interfaces_for_new_linodes` is set to `legacy_config_only`, set `interface_generation` to `legacy_config` or omit it for legacy configuration interfaces.
         * - If `interfaces_for_new_linodes` is set to `linode_default_but_legacy_config_allowed`, set `interface_generation` to `linode` or omit it for Linode interfaces, and to `legacy_config` if the Linode uses legacy configuration interfaces.
         * - If `interfaces_for_new_linodes` is set to `legacy_config_default_but_linode_allowed`, set `interface_generation` to `legacy_config` or omit it for legacy configuration interfaces, and to `linode` if the Linode uses Linode interfaces.
         */
        interface_generation?: 'legacy_config' | 'linode';
        /**
         * Interfaces for the Linode. This can be a Linode interface or legacy configuration interface.
         */
        interfaces?: Array<{
            /**
             * Indicates if the interface serves as the default route when multiple interfaces are eligible for this role. A public interface can have both an IPv4 `default_route` and an IPv6 `default_route`, provided it has at least one IP address of the corresponding type.
             */
            default_route: {
                /**
                 * If set to `true`, the interface is used for the IPv4 `default_route`. Only one interface per Linode can be set as the IPv4 default route.
                 */
                ipv4: boolean | null;
                /**
                 * If set to `true`, the interface is used for the IPv6 `default_route`. Only one interface per Linode can have the IPv6 default route.
                 */
                ipv6: boolean | null;
            };
            /**
             * The enabled firewall to secure a VPC or public interface. Not allowed for VLAN interfaces. If a `firewall_id` is not provided for a VPC or a public interface, then its [default interface firewall](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-firewalls) is used. If a default firewall is not available, and `null` is not specified, the request fails. Setting the `firewall_id` as `null` indicates that no firewall device will be attached to the interface.
             */
            firewall_id: number | null;
            /**
             * Public interface settings. A Linode can have only one public interface. A public interface can have both IPv4 and IPv6 configurations.
             */
            public: {
                /**
                 * IPv4 address settings for this public interface. If omitted, a public IPv4 address is automatically allocated.
                 */
                ipv4: {
                    /**
                     * List of IPv4 addresses to assign to this interface. Setting any to `auto` allocates a public IPv4 address. To create a public interface that uses IPv6 and no IPv4, set the IPv4 address as an empty list `[]`.
                     */
                    addresses: Array<{
                        /**
                         * The interface's public IPv4 address. You can specify which public IPv4 address to configure for the interface. Setting this to `auto` automatically allocates a public address. To create a public interface that uses IPv6 and no IPv4, set the IPv4 address as an empty list `[]`. If the address is a reserved or automatically assigned, it needs to be reserved or already assigned to a single Linode, and it can’t be assigned to any other interface.
                         */
                        address: string | 'auto' | string;
                        /**
                         * The IPv4 primary address configures the source address for routes within the Linode on the corresponding network interface.
                         * - Don't set this to `false` if there's only one address in the `addresses` array.
                         * - If more than one address is provided, primary can be set to `true` for one address.
                         * - If only one address is present in the `addresses` array, this address is automatically set as the primary address.
                         */
                        primary: boolean;
                    }>;
                };
                /**
                 * IPv6 address settings for the public interface.
                 */
                ipv6: {
                    /**
                     * IPv6 address ranges to assign to this interface. If omitted, no ranges are assigned.
                     */
                    ranges: Array<{
                        /**
                         * Your assigned IPv6 range in CIDR notation (`2001:0db8::1/64`) or prefix (`/64`).
                         * - The prefix of `/64` or `/56` block of IPv6 addresses.
                         * - If provided in CIDR notation, the prefix must be within the assigned ranges for the Linode.
                         */
                        range: string;
                    }>;
                };
            } | null;
        } | {
            /**
             * VLAN interface settings. A Linode can have up to three VLAN interfaces, with a unique `vlan_label` for each.
             */
            vlan: {
                /**
                 * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
                 */
                ipam_address: string;
                /**
                 * The VLAN's unique label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
                 */
                vlan_label: string;
            } | null;
        } | {
            /**
             * Indicates if the interface is used as the default route when multiple interfaces are eligible for this role. A VPC interface can have an IPv4 `default_route`.
             */
            default_route: {
                /**
                 * If set to `true`, the interface is used for the IPv4 `default_route`. Only one interface per Linode can be set as the IPv4 default route.
                 */
                ipv4: boolean | null;
            };
            /**
             * The enabled firewall to secure a VPC or public interface. Not allowed for VLAN interfaces. If a `firewall_id` is not provided for a VPC or a public interface, then its default interface firewall is used. If a default firewall is not available, and `null` is not specified, the request fails. Setting the `firewall_id` as `null` indicates that no firewall device will be attached to the interface.
             */
            firewall_id: number | null;
            /**
             * VPC interface settings. A Linode can have one VPC interface. The maximum number of interfaces allowed on a Linode is three.
             */
            vpc: {
                /**
                 * Interfaces can be configured with IPv4 addresses or ranges.
                 */
                ipv4?: {
                    /**
                     * IPv4 address settings for this VPC interface.
                     */
                    addresses: Array<{
                        /**
                         * Specifies which IPv4 address to use in the VPC subnet. You can specify which VPC Ipv4 address in the subnet to configure for the interface. You can't use an IPv4 address taken from another Linode or interface, or the first two or last two addresses in the VPC subnet. When `address` is set to `auto`, an IP address from the subnet is automatically assigned.
                         */
                        address: string | 'auto';
                        /**
                         * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                         * - You can set this to a specific public IPv4 address that's available on the Linode.
                         * - If set to `auto`, a public IPv4 address is automatically selected.
                         * - If a public IPv4 address or `auto` is specified, primary must be set to `true`.
                         * - The address can't be used on another Linode.
                         * - If the address is a reserved or an automatically assigned IP, the IP must be reserved or already assigned to a single Linode, and not assigned to an interface.
                         * - If this property is omitted or set to `null`, no 1:1 NAT configuration will be applied to the VPC interface.
                         */
                        nat_1_1_address: string | 'auto' | null;
                        /**
                         * The IPv4 primary address is used to configure the source address for routes within the Linode on the corresponding network interface.
                         * Should not be set to `false` if there is only one address present in the `addresses` array.
                         * If more than one address is provided, primary must be set to `true` for one address.
                         * If only one address is present in the `addresses` array, this address is automatically set as the primary address.
                         */
                        primary: boolean | null;
                    }>;
                    /**
                     * VPC IPv4 ranges.
                     */
                    ranges: Array<{
                        /**
                         * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                         * - When only the prefix is provided, then an available range of that size within the VPC's subnet is automatically selected.
                         * - If specified as CIDR notation, it must belong to the VPC subnet. All addresses in the range must not be taken by any other Linode or interfaces in the VPC subnet and must not include any of the first two or last two addresses of the VPC subnet.
                         */
                        range: string;
                    }>;
                };
                /**
                 * The VPC subnet identifier for this interface. Your subnet’s VPC must be in the same data center (region) as the Linode.
                 */
                subnet_id: number;
            } | null;
        } | (unknown & {
            /**
             * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
             */
            readonly active?: boolean;
            /**
             * __Read-only__ The unique ID representing this interface.
             */
            readonly id?: number;
            /**
             * IPv4 CIDR VPC subnet ranges that are routed to this interface.
             *
             * When included in a request:
             *
             * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
             *
             * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
             *
             * - Submit this as an empty array removes all existing values.
             *
             * - Omit this object to leave existing values as is.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > This is only supported for interfaces with a `purpose` of `vpc`.
             */
            ip_ranges?: Array<string> | null;
            /**
             * This interface's private IP address in classless inter-domain routing (CIDR) notation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
             *
             * - This should be unique among devices attached to the VLAN to avoid conflict.
             *
             * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            ipam_address?: string | null;
            /**
             * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
             */
            ipv4?: {
                /**
                 * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                 *
                 * - Only supported for interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                 *
                 * - A specified address can't be shared with another Linode.
                 *
                 * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                 */
                nat_1_1: string | 'any' | null;
                /**
                 * The VPC subnet IPv4 address for this interface.
                 *
                 * - This only applies to interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                 *
                 * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                 *
                 * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                 */
                vpc: string | null;
            };
            /**
             * __Filterable__ The name of this interface.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Required.
             *
             * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
             *
             * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
             *
             * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            label?: string | null;
            /**
             * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
             *
             * > 📘
             * >
             * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
             */
            primary?: boolean;
            /**
             * The type of interface. This can be `public`, `vlan`, or `vpc`.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - You can only define one `public` interface per Linode.
             *
             * - The Linode's default public IPv4 address is assigned to the `public` interface.
             *
             * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
             *
             * - If an `ipam_address` is configured, the Linode uses this address.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
             *
             * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
             */
            purpose: 'public' | 'vlan' | 'vpc';
            /**
             * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
             *
             * - Required for `vpc` type interfaces.
             *
             * - Returned as `null` for non-`vpc` type interfaces.
             *
             * - Once you've assigned a VPC subnet to an interface, you can't update it.
             *
             * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
             */
            subnet_id?: number | null;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
             */
            readonly vpc_id?: number | null;
        })>;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label?: string;
        /**
         * Enables the Network Helper feature. The default value is determined by the `network_helper` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). This `network_helper` field is for Linodes using Linode interfaces only.
         */
        network_helper?: boolean | null;
        /**
         * Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/). These constraints apply:
         *
         * - The target placement group needs to be in the same `region` set for this Linode.
         * - The placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation and note either the `maximum_linodes_per_pg` (strict) or `maximum_linodes_per_flexible_pg` (flexible), based on your selected `placement_group_policy`. These represent the Linode limit per placement group, for each `placement_group_policy` type. You can then run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation to review the Linodes in that group.
         */
        placement_group?: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
        };
        /**
         * If `true`, the created Linode will have private networking enabled and assigned a private IPv4 address.
         */
        private_ip?: boolean;
        /**
         * The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode will be located.
         */
        region: string;
        /**
         * When deploying from an Image, this field is optional, otherwise it is ignored. This is used to set the swap disk size for the newly created Linode.
         */
        swap_size?: number;
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags?: Array<string>;
        /**
         * The [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) of the Linode you are creating.
         */
        type: string;
    };
    path?: never;
    query?: never;
    url: '/linode/instances';
};

export type PostLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLinodeInstanceError = PostLinodeInstanceErrors[keyof PostLinodeInstanceErrors];

export type PostLinodeInstanceResponses = {
    /**
     * Linode
     *
     * A new Linode is being created.
     */
    200: {
        alerts: {
            /**
             * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
             *
             * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
             *
             * The default value is 90% multiplied by the number of cores.
             *
             * If the value is set to `0` (zero), the alert is disabled.
             */
            cpu: number;
            /**
             * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
             */
            io: number;
            /**
             * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_in: number;
            /**
             * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_out: number;
            /**
             * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
             */
            transfer_quota: number;
        };
        /**
         * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
         */
        backups: {
            /**
             * __Read-only__ Whether Backups for this Linode are available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
             */
            readonly enabled: boolean;
            /**
             * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
             */
            readonly last_successful: string;
            schedule: {
                /**
                 * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                 *
                 * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                 */
                day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                /**
                 * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                 *
                 * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                 *
                 * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                 */
                window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
            };
        };
        /**
         * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this Linode was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
         */
        readonly disk_encryption: string | null;
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group: string;
        /**
         * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
         */
        readonly has_user_data: boolean;
        /**
         * __Read-only__ The Linode's host machine, as a UUID.
         */
        readonly host_uuid: string;
        /**
         * __Read-only__ The virtualization software powering this Linode.
         */
        hypervisor: 'kvm';
        /**
         * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
         */
        readonly id: number;
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
         */
        interface_generation: 'legacy_config' | 'linode';
        /**
         * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
         *
         * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
         */
        readonly ipv4: Array<string>;
        /**
         * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label: string;
        /**
         * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
         */
        readonly lke_cluster_id: number | null;
        /**
         * __Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
         */
        readonly placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
        } | null;
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
         */
        readonly region: string;
        /**
         * __Read-only__ Information about the resources available to this Linode.
         */
        readonly specs: {
            /**
             * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
             */
            readonly disk: number;
            /**
             * __Read-only__ The number of GPUs this Linode has access to.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The amount of RAM, in MB, this Linode has access to.
             *
             * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
             */
            readonly memory: number;
            /**
             * __Read-only__ The amount of network transfer this Linode is allotted each month.
             */
            readonly transfer: number;
            /**
             * __Read-only__ The number of VCPUs this Linode has access to.
             */
            readonly vcpus: number;
        };
        /**
         * __Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
         *
         * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
         * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
         * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
         */
        status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags: Array<string>;
        /**
         * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
         */
        readonly type: string;
        /**
         * __Read-only__ When this Linode was last updated.
         */
        readonly updated: string;
        /**
         * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
         */
        watchdog_enabled: boolean;
    };
};

export type PostLinodeInstanceResponse = PostLinodeInstanceResponses[keyof PostLinodeInstanceResponses];

export type DeleteLinodeInstanceData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}';
};

export type DeleteLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLinodeInstanceError = DeleteLinodeInstanceErrors[keyof DeleteLinodeInstanceErrors];

export type DeleteLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLinodeInstanceResponse = DeleteLinodeInstanceResponses[keyof DeleteLinodeInstanceResponses];

export type GetLinodeInstanceData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}';
};

export type GetLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInstanceError = GetLinodeInstanceErrors[keyof GetLinodeInstanceErrors];

export type GetLinodeInstanceResponses = {
    /**
     * Linode
     *
     * Returns a single Linode object.
     */
    200: {
        alerts: {
            /**
             * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
             *
             * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
             *
             * The default value is 90% multiplied by the number of cores.
             *
             * If the value is set to `0` (zero), the alert is disabled.
             */
            cpu: number;
            /**
             * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
             */
            io: number;
            /**
             * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_in: number;
            /**
             * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_out: number;
            /**
             * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
             */
            transfer_quota: number;
        };
        /**
         * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
         */
        backups: {
            /**
             * __Read-only__ Whether Backups for this Linode are available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
             */
            readonly enabled: boolean;
            /**
             * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
             */
            readonly last_successful: string;
            schedule: {
                /**
                 * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                 *
                 * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                 */
                day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                /**
                 * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                 *
                 * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                 *
                 * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                 */
                window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
            };
        };
        /**
         * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this Linode was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
         */
        disk_encryption: 'enabled' | 'disabled';
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group: string;
        /**
         * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
         */
        readonly has_user_data: boolean;
        /**
         * __Read-only__ The Linode's host machine, as a UUID.
         */
        readonly host_uuid: string;
        /**
         * __Read-only__ The virtualization software powering this Linode.
         */
        hypervisor: 'kvm';
        /**
         * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
         */
        readonly id: number;
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Beta__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
         */
        interface_generation: 'legacy_config' | 'linode';
        /**
         * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
         *
         * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
         */
        readonly ipv4: Array<string>;
        /**
         * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label: string;
        /**
         * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
         */
        readonly lke_cluster_id: number | null;
        /**
         * __Beta__, __Read-only__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
         */
        readonly placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * __Read-only__ The unique identifier for the [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups) to which this Linode is being migrated. Displayed as `null` if the Linode is not being migrated to a new placement group.
             */
            readonly migrating_to: number | null;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
        } | null;
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
         */
        readonly region: string;
        /**
         * __Read-only__ Information about the resources available to this Linode.
         */
        readonly specs: {
            /**
             * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
             */
            readonly disk: number;
            /**
             * __Read-only__ The number of GPUs this Linode has access to.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The amount of RAM, in MB, this Linode has access to.
             *
             * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
             */
            readonly memory: number;
            /**
             * __Read-only__ The amount of network transfer this Linode is allotted each month.
             */
            readonly transfer: number;
            /**
             * __Read-only__ The number of VCPUs this Linode has access to.
             */
            readonly vcpus: number;
        };
        /**
         * __Read-only__ A brief description of the compute instance's current state. This value can change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
         *
         * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
         * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
         * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
         */
        status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags: Array<string>;
        /**
         * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
         */
        readonly type: string;
        /**
         * __Read-only__ When this Linode was last updated.
         */
        readonly updated: string;
        /**
         * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
         */
        watchdog_enabled: boolean;
    };
};

export type GetLinodeInstanceResponse = GetLinodeInstanceResponses[keyof GetLinodeInstanceResponses];

export type PutLinodeInstanceData = {
    /**
     * Linode
     *
     * Any field that is not marked as `readOnly` may be updated. Fields that are marked `readOnly` will be ignored. If any updated field fails to pass validation, the Linode will not be updated.
     */
    body: {
        alerts: {
            /**
             * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
             *
             * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
             *
             * The default value is 90% multiplied by the number of cores.
             *
             * If the value is set to `0` (zero), the alert is disabled.
             */
            cpu: number;
            /**
             * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
             */
            io: number;
            /**
             * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_in: number;
            /**
             * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_out: number;
            /**
             * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
             */
            transfer_quota: number;
        };
        /**
         * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
         */
        backups: {
            /**
             * __Read-only__ Whether Backups for this Linode are available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
             */
            readonly enabled: boolean;
            /**
             * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
             */
            readonly last_successful: string;
            schedule: {
                /**
                 * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                 *
                 * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                 */
                day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                /**
                 * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                 *
                 * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                 *
                 * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                 */
                window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
            };
        };
        /**
         * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this Linode was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
         */
        readonly disk_encryption: string | null;
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group: string;
        /**
         * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
         */
        readonly has_user_data: boolean;
        /**
         * __Read-only__ The Linode's host machine, as a UUID.
         */
        readonly host_uuid: string;
        /**
         * __Read-only__ The virtualization software powering this Linode.
         */
        hypervisor: 'kvm';
        /**
         * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
         */
        readonly id: number;
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
         */
        interface_generation: 'legacy_config' | 'linode';
        /**
         * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
         *
         * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
         */
        readonly ipv4: Array<string>;
        /**
         * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label: string;
        /**
         * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
         */
        readonly lke_cluster_id: number | null;
        /**
         * __Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
         */
        readonly placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
        } | null;
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
         */
        readonly region: string;
        /**
         * __Read-only__ Information about the resources available to this Linode.
         */
        readonly specs: {
            /**
             * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
             */
            readonly disk: number;
            /**
             * __Read-only__ The number of GPUs this Linode has access to.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The amount of RAM, in MB, this Linode has access to.
             *
             * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
             */
            readonly memory: number;
            /**
             * __Read-only__ The amount of network transfer this Linode is allotted each month.
             */
            readonly transfer: number;
            /**
             * __Read-only__ The number of VCPUs this Linode has access to.
             */
            readonly vcpus: number;
        };
        /**
         * __Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
         *
         * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
         * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
         * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
         */
        status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags: Array<string>;
        /**
         * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
         */
        readonly type: string;
        /**
         * __Read-only__ When this Linode was last updated.
         */
        readonly updated: string;
        /**
         * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
         */
        watchdog_enabled: boolean;
    };
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}';
};

export type PutLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeInstanceError = PutLinodeInstanceErrors[keyof PutLinodeInstanceErrors];

export type PutLinodeInstanceResponses = {
    /**
     * Linode
     *
     * The updated Linode.
     */
    200: {
        alerts: {
            /**
             * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
             *
             * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
             *
             * The default value is 90% multiplied by the number of cores.
             *
             * If the value is set to `0` (zero), the alert is disabled.
             */
            cpu: number;
            /**
             * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
             */
            io: number;
            /**
             * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_in: number;
            /**
             * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_out: number;
            /**
             * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
             */
            transfer_quota: number;
        };
        /**
         * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
         */
        backups: {
            /**
             * __Read-only__ Whether Backups for this Linode are available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
             */
            readonly enabled: boolean;
            /**
             * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
             */
            readonly last_successful: string;
            schedule: {
                /**
                 * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                 *
                 * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                 */
                day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                /**
                 * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                 *
                 * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                 *
                 * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                 */
                window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
            };
        };
        /**
         * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this Linode was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
         */
        readonly disk_encryption: string | null;
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group: string;
        /**
         * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
         */
        readonly has_user_data: boolean;
        /**
         * __Read-only__ The Linode's host machine, as a UUID.
         */
        readonly host_uuid: string;
        /**
         * __Read-only__ The virtualization software powering this Linode.
         */
        hypervisor: 'kvm';
        /**
         * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
         */
        readonly id: number;
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
         */
        interface_generation: 'legacy_config' | 'linode';
        /**
         * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
         *
         * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
         */
        readonly ipv4: Array<string>;
        /**
         * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label: string;
        /**
         * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
         */
        readonly lke_cluster_id: number | null;
        /**
         * __Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
         */
        readonly placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
        } | null;
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
         */
        readonly region: string;
        /**
         * __Read-only__ Information about the resources available to this Linode.
         */
        readonly specs: {
            /**
             * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
             */
            readonly disk: number;
            /**
             * __Read-only__ The number of GPUs this Linode has access to.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The amount of RAM, in MB, this Linode has access to.
             *
             * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
             */
            readonly memory: number;
            /**
             * __Read-only__ The amount of network transfer this Linode is allotted each month.
             */
            readonly transfer: number;
            /**
             * __Read-only__ The number of VCPUs this Linode has access to.
             */
            readonly vcpus: number;
        };
        /**
         * __Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
         *
         * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
         * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
         * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
         */
        status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags: Array<string>;
        /**
         * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
         */
        readonly type: string;
        /**
         * __Read-only__ When this Linode was last updated.
         */
        readonly updated: string;
        /**
         * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
         */
        watchdog_enabled: boolean;
    };
};

export type PutLinodeInstanceResponse = PutLinodeInstanceResponses[keyof PutLinodeInstanceResponses];

export type GetBackupsData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode the backups belong to.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/backups';
};

export type GetBackupsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetBackupsError = GetBackupsErrors[keyof GetBackupsErrors];

export type GetBackupsResponses = {
    /**
     * A collection of the specified Linode's available backups.
     */
    200: {
        automatic: Array<{
            /**
             * __Read-only__ Whether this Backup is available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ A list of the labels of the Configuration profiles that are part of the Backup.
             */
            readonly configs: Array<string>;
            /**
             * __Read-only__ The date the Backup was taken.
             */
            readonly created: string;
            /**
             * __Read-only__ A list of the disks that are part of the Backup.
             */
            readonly disks: Array<{
                /**
                 * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
                 *
                 * > 📘
                 * >
                 * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
                 */
                filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
                label: string;
                size: number;
            }>;
            /**
             * __Read-only__ The date the Backup completed.
             */
            readonly finished: string;
            /**
             * __Read-only__ The unique ID of this Backup.
             */
            readonly id: number;
            /**
             * A label for Backups that are of type `snapshot`.
             */
            label: string | null;
            /**
             * __Read-only__ The current state of a specific Backup.
             */
            status: 'paused' | 'pending' | 'running' | 'needsPostProcessing' | 'successful' | 'failed' | 'userAborted';
            /**
             * __Read-only__ This indicates whether the Backup is an automatic Backup or manual snapshot taken by the User at a specific point in time.
             */
            type: 'auto' | 'snapshot';
            /**
             * __Read-only__ The date the Backup was most recently updated.
             */
            readonly updated: string;
        } & {
            type?: string;
        }>;
        snapshot: {
            /**
             * An object representing a Backup or snapshot for a Linode with Backup service enabled.
             */
            current: {
                /**
                 * __Read-only__ Whether this Backup is available for restoration.
                 *
                 * Backups undergoing maintenance are not available for restoration.
                 */
                readonly available: boolean;
                /**
                 * __Read-only__ A list of the labels of the Configuration profiles that are part of the Backup.
                 */
                readonly configs: Array<string>;
                /**
                 * __Read-only__ The date the Backup was taken.
                 */
                readonly created: string;
                /**
                 * __Read-only__ A list of the disks that are part of the Backup.
                 */
                readonly disks: Array<{
                    /**
                     * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
                     *
                     * > 📘
                     * >
                     * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
                     */
                    filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
                    label: string;
                    size: number;
                }>;
                /**
                 * __Read-only__ The date the Backup completed.
                 */
                readonly finished: string;
                /**
                 * __Read-only__ The unique ID of this Backup.
                 */
                readonly id: number;
                /**
                 * A label for Backups that are of type `snapshot`.
                 */
                label: string | null;
                /**
                 * __Read-only__ The current state of a specific Backup.
                 */
                status: 'paused' | 'pending' | 'running' | 'needsPostProcessing' | 'successful' | 'failed' | 'userAborted';
                /**
                 * __Read-only__ This indicates whether the Backup is an automatic Backup or manual snapshot taken by the User at a specific point in time.
                 */
                type: 'auto' | 'snapshot';
                /**
                 * __Read-only__ The date the Backup was most recently updated.
                 */
                readonly updated: string;
            };
            /**
             * An object representing a Backup or snapshot for a Linode with Backup service enabled.
             */
            in_progress: {
                /**
                 * __Read-only__ Whether this Backup is available for restoration.
                 *
                 * Backups undergoing maintenance are not available for restoration.
                 */
                readonly available: boolean;
                /**
                 * __Read-only__ A list of the labels of the Configuration profiles that are part of the Backup.
                 */
                readonly configs: Array<string>;
                /**
                 * __Read-only__ The date the Backup was taken.
                 */
                readonly created: string;
                /**
                 * __Read-only__ A list of the disks that are part of the Backup.
                 */
                readonly disks: Array<{
                    /**
                     * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
                     *
                     * > 📘
                     * >
                     * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
                     */
                    filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
                    label: string;
                    size: number;
                }>;
                /**
                 * __Read-only__ The date the Backup completed.
                 */
                readonly finished: string;
                /**
                 * __Read-only__ The unique ID of this Backup.
                 */
                readonly id: number;
                /**
                 * A label for Backups that are of type `snapshot`.
                 */
                label: string | null;
                /**
                 * __Read-only__ The current state of a specific Backup.
                 */
                status: 'paused' | 'pending' | 'running' | 'needsPostProcessing' | 'successful' | 'failed' | 'userAborted';
                /**
                 * __Read-only__ This indicates whether the Backup is an automatic Backup or manual snapshot taken by the User at a specific point in time.
                 */
                type: 'auto' | 'snapshot';
                /**
                 * __Read-only__ The date the Backup was most recently updated.
                 */
                readonly updated: string;
            };
        };
    };
};

export type GetBackupsResponse = GetBackupsResponses[keyof GetBackupsResponses];

export type PostSnapshotData = {
    body: {
        /**
         * The label for the new snapshot.
         */
        label: string;
    };
    path: {
        /**
         * The ID of the Linode the backups belong to.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/backups';
};

export type PostSnapshotErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostSnapshotError = PostSnapshotErrors[keyof PostSnapshotErrors];

export type PostSnapshotResponses = {
    /**
     * An object representing a Backup or snapshot for a Linode with Backup service enabled.
     */
    200: {
        /**
         * __Read-only__ Whether this Backup is available for restoration.
         *
         * Backups undergoing maintenance are not available for restoration.
         */
        readonly available: boolean;
        /**
         * __Read-only__ A list of the labels of the Configuration profiles that are part of the Backup.
         */
        readonly configs: Array<string>;
        /**
         * __Read-only__ The date the Backup was taken.
         */
        readonly created: string;
        /**
         * __Read-only__ A list of the disks that are part of the Backup.
         */
        readonly disks: Array<{
            /**
             * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
             *
             * > 📘
             * >
             * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
             */
            filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
            label: string;
            size: number;
        }>;
        /**
         * __Read-only__ The date the Backup completed.
         */
        readonly finished: string;
        /**
         * __Read-only__ The unique ID of this Backup.
         */
        readonly id: number;
        /**
         * A label for Backups that are of type `snapshot`.
         */
        label: string | null;
        /**
         * __Read-only__ The current state of a specific Backup.
         */
        status: 'paused' | 'pending' | 'running' | 'needsPostProcessing' | 'successful' | 'failed' | 'userAborted';
        /**
         * __Read-only__ This indicates whether the Backup is an automatic Backup or manual snapshot taken by the User at a specific point in time.
         */
        type: 'auto' | 'snapshot';
        /**
         * __Read-only__ The date the Backup was most recently updated.
         */
        readonly updated: string;
    };
};

export type PostSnapshotResponse = PostSnapshotResponses[keyof PostSnapshotResponses];

export type PostCancelBackupsData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode to cancel backup service for.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/backups/cancel';
};

export type PostCancelBackupsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCancelBackupsError = PostCancelBackupsErrors[keyof PostCancelBackupsErrors];

export type PostCancelBackupsResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostCancelBackupsResponse = PostCancelBackupsResponses[keyof PostCancelBackupsResponses];

export type PostEnableBackupsData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode to enable backup service for.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/backups/enable';
};

export type PostEnableBackupsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostEnableBackupsError = PostEnableBackupsErrors[keyof PostEnableBackupsErrors];

export type PostEnableBackupsResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostEnableBackupsResponse = PostEnableBackupsResponses[keyof PostEnableBackupsResponses];

export type GetBackupData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode the Backup belongs to.
         */
        linodeId: number;
        /**
         * The ID of the Backup to look up.
         */
        backupId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/backups/{backupId}';
};

export type GetBackupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetBackupError = GetBackupErrors[keyof GetBackupErrors];

export type GetBackupResponses = {
    /**
     * An object representing a Backup or snapshot for a Linode with Backup service enabled.
     */
    200: {
        /**
         * __Read-only__ Whether this Backup is available for restoration.
         *
         * Backups undergoing maintenance are not available for restoration.
         */
        readonly available: boolean;
        /**
         * __Read-only__ A list of the labels of the Configuration profiles that are part of the Backup.
         */
        readonly configs: Array<string>;
        /**
         * __Read-only__ The date the Backup was taken.
         */
        readonly created: string;
        /**
         * __Read-only__ A list of the disks that are part of the Backup.
         */
        readonly disks: Array<{
            /**
             * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
             *
             * > 📘
             * >
             * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
             */
            filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
            label: string;
            size: number;
        }>;
        /**
         * __Read-only__ The date the Backup completed.
         */
        readonly finished: string;
        /**
         * __Read-only__ The unique ID of this Backup.
         */
        readonly id: number;
        /**
         * A label for Backups that are of type `snapshot`.
         */
        label: string | null;
        /**
         * __Read-only__ The current state of a specific Backup.
         */
        status: 'paused' | 'pending' | 'running' | 'needsPostProcessing' | 'successful' | 'failed' | 'userAborted';
        /**
         * __Read-only__ This indicates whether the Backup is an automatic Backup or manual snapshot taken by the User at a specific point in time.
         */
        type: 'auto' | 'snapshot';
        /**
         * __Read-only__ The date the Backup was most recently updated.
         */
        readonly updated: string;
    };
};

export type GetBackupResponse = GetBackupResponses[keyof GetBackupResponses];

export type PostRestoreBackupData = {
    /**
     * Parameters to provide when restoring the Backup.
     */
    body: {
        /**
         * The ID of the Linode to restore a Backup to.
         */
        linode_id: number;
        /**
         * If `true`, deletes all Disks and Configs on the target Linode before restoring.
         *
         * If `false`, and the Disk image size is larger than the available space on the Linode, an error message indicating insufficient space is returned.
         */
        overwrite?: boolean;
    };
    path: {
        /**
         * The ID of the Linode that the Backup belongs to.
         */
        linodeId: number;
        /**
         * The ID of the Backup to restore.
         */
        backupId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/backups/{backupId}/restore';
};

export type PostRestoreBackupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostRestoreBackupError = PostRestoreBackupErrors[keyof PostRestoreBackupErrors];

export type PostRestoreBackupResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostRestoreBackupResponse = PostRestoreBackupResponses[keyof PostRestoreBackupResponses];

export type PostBootLinodeInstanceData = {
    /**
     * Optional configuration to boot into (see above).
     */
    body?: {
        /**
         * The Linode Config ID to boot into.
         */
        config_id: number | null;
    };
    path: {
        /**
         * The ID of the Linode to boot.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/boot';
};

export type PostBootLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostBootLinodeInstanceError = PostBootLinodeInstanceErrors[keyof PostBootLinodeInstanceErrors];

export type PostBootLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostBootLinodeInstanceResponse = PostBootLinodeInstanceResponses[keyof PostBootLinodeInstanceResponses];

export type PostCloneLinodeInstanceData = {
    /**
     * The requested state your Linode will be cloned into.
     */
    body: {
        /**
         * If this field is set to `true`, the created Linode will automatically be enrolled in the Linode Backup service. This will incur an additional charge. Pricing is included in the response from [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types).
         *
         * - Can only be included when cloning to a new Linode.
         */
        backups_enabled: boolean;
        /**
         * An array of configuration profile IDs.
         *
         * - If the `configs` parameter __is not provided__, then __all configuration profiles and their associated disks will be cloned__ from the source Linode. Any disks specified by the `disks` parameter will also be cloned.
         * - __If an empty array is provided__ for the `configs` parameter, then __no configuration profiles (nor their associated disks) will be cloned__ from the source Linode. Any disks specified by the `disks` parameter will still be cloned.
         * - __If a non-empty array is provided__ for the `configs` parameter, then __the configuration profiles specified in the array (and their associated disks) will be cloned__ from the source Linode. Any disks specified by the `disks` parameter will also be cloned.
         */
        configs: Array<number>;
        /**
         * An array of disk IDs.
         *
         * - If the `disks` parameter __is not provided__, then __no extra disks will be cloned__ from the source Linode. All disks associated with the configuration profiles specified by the `configs` parameter will still be cloned.
         * - __If an empty array is provided__ for the `disks` parameter, then __no extra disks will be cloned__ from the source Linode. All disks associated with the configuration profiles specified by the `configs` parameter will still be cloned.
         * - __If a non-empty array is provided__ for the `disks` parameter, then __the disks specified in the array will be cloned__ from the source Linode, in addition to any disks associated with the configuration profiles specified by the `configs` parameter.
         */
        disks: Array<number>;
        /**
         * A label used to group Linodes for display. Linodes are not required to have a group.
         *
         * @deprecated
         */
        group: string;
        /**
         * The label to assign this Linode when cloning to a new Linode.
         *
         * - Can only be provided when cloning to a new Linode.
         * - Defaults to `linode`.
         */
        label: string;
        /**
         * If an existing Linode is the target for the clone, the ID of that Linode. The existing Linode must have enough resources to accept the clone.
         */
        linode_id: number;
        /**
         * __Beta__ Defines the maintenance policy for the new Linode. If you don't provide it, the new Linode inherits the maintenance policy from the original Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Write-only__ An object containing user-defined data relevant to the creation of Linodes.
         */
        metadata: {
            /**
             * Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.
             *
             * Cannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.
             *
             * Must not be included when cloning to an existing Linode.
             *
             * Unencoded data must not exceed 65535 bytes, or about 16kb encoded.
             */
            user_data: string;
        };
        /**
         * Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/). Consider these points when cloning:
         *
         * - If the Linode you're cloning exists in a placement group, the API won't automatically add the cloned instance to the same placement group. You need to specify a placement group to add the clone to.
         *
         * - The target placement group needs to be in the same `region` set for this Linode.
         *
         * - The placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation and note either the `maximum_linodes_per_pg` (strict) or `maximum_linodes_per_flexible_pg` (flexible), based on your selected `placement_group_policy`. These represent the Linode limit per placement group, for each `placement_group_policy` type. You can then run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation to review the Linodes in that group.
         */
        placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
        };
        /**
         * If `true`, the created Linode will have private networking enabled and assigned a private IPv4 address.
         *
         * - Can only be provided when cloning to a new Linode.
         */
        private_ip: boolean;
        /**
         * This is the Region where the Linode will be deployed. To view all available Regions you can deploy to, run [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions).
         *
         * - Region can only be provided and is required when cloning to a new Linode.
         */
        region: string;
        /**
         * A Linode's Type determines what resources are available to it, including disk space, memory, and virtual cpus. The amounts available to a specific Linode are returned as `specs` on the Linode object.
         *
         * To view all available Linode Types you can deploy with, run [List types](https://techdocs.akamai.com/linode-api/reference/get-linode-types).
         *
         * - Type can only be provided and is required when cloning to a new Linode.
         */
        type: string;
    };
    path: {
        /**
         * ID of the Linode to clone.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/clone';
};

export type PostCloneLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCloneLinodeInstanceError = PostCloneLinodeInstanceErrors[keyof PostCloneLinodeInstanceErrors];

export type PostCloneLinodeInstanceResponses = {
    /**
     * Linode
     *
     * Clone started.
     */
    200: {
        alerts: {
            /**
             * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
             *
             * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
             *
             * The default value is 90% multiplied by the number of cores.
             *
             * If the value is set to `0` (zero), the alert is disabled.
             */
            cpu: number;
            /**
             * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
             */
            io: number;
            /**
             * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_in: number;
            /**
             * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_out: number;
            /**
             * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
             */
            transfer_quota: number;
        };
        /**
         * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
         */
        backups: {
            /**
             * __Read-only__ Whether Backups for this Linode are available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
             */
            readonly enabled: boolean;
            /**
             * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
             */
            readonly last_successful: string;
            schedule: {
                /**
                 * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                 *
                 * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                 */
                day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                /**
                 * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                 *
                 * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                 *
                 * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                 */
                window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
            };
        };
        /**
         * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this Linode was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
         */
        readonly disk_encryption: string | null;
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group: string;
        /**
         * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
         */
        readonly has_user_data: boolean;
        /**
         * __Read-only__ The Linode's host machine, as a UUID.
         */
        readonly host_uuid: string;
        /**
         * __Read-only__ The virtualization software powering this Linode.
         */
        hypervisor: 'kvm';
        /**
         * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
         */
        readonly id: number;
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
         */
        interface_generation: 'legacy_config' | 'linode';
        /**
         * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
         *
         * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
         */
        readonly ipv4: Array<string>;
        /**
         * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label: string;
        /**
         * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
         */
        readonly lke_cluster_id: number | null;
        /**
         * __Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
         */
        readonly placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
        } | null;
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
         */
        readonly region: string;
        /**
         * __Read-only__ Information about the resources available to this Linode.
         */
        readonly specs: {
            /**
             * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
             */
            readonly disk: number;
            /**
             * __Read-only__ The number of GPUs this Linode has access to.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The amount of RAM, in MB, this Linode has access to.
             *
             * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
             */
            readonly memory: number;
            /**
             * __Read-only__ The amount of network transfer this Linode is allotted each month.
             */
            readonly transfer: number;
            /**
             * __Read-only__ The number of VCPUs this Linode has access to.
             */
            readonly vcpus: number;
        };
        /**
         * __Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
         *
         * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
         * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
         * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
         */
        status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags: Array<string>;
        /**
         * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
         */
        readonly type: string;
        /**
         * __Read-only__ When this Linode was last updated.
         */
        readonly updated: string;
        /**
         * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
         */
        watchdog_enabled: boolean;
    };
};

export type PostCloneLinodeInstanceResponse = PostCloneLinodeInstanceResponses[keyof PostCloneLinodeInstanceResponses];

export type GetLinodeConfigsData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up Configuration profiles for.
         */
        linodeId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/instances/{linodeId}/configs';
};

export type GetLinodeConfigsResponses = {
    /**
     * Returns the configuration profiles associated with this Linode.
     */
    200: {
        data: Array<{
            /**
             * Optional field for arbitrary user comments on this configuration.
             */
            comments: string | null;
            /**
             * A dictionary of device disks to use as a device map in a Linode's configuration profile.
             *
             * - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
             * - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
             */
            devices: {
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sda: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sdb: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sdc: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sdd: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sde: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sdf: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sdg: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
                /**
                 * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
                 */
                sdh: {
                    /**
                     * The Disk ID, or `null` if a Volume is assigned to this slot.
                     */
                    disk_id: number;
                    /**
                     * The Volume ID, or `null` if a Disk is assigned to this slot.
                     */
                    volume_id: number | null;
                };
            };
            /**
             * Helpers enabled when booting to this Linode configuration.
             */
            helpers: {
                /**
                 * Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.
                 */
                devtmpfs_automount: boolean;
                /**
                 * Helps maintain correct `inittab` or `upstart` console device.
                 */
                distro: boolean;
                /**
                 * Creates a modules dependency file for the kernel you run.
                 */
                modules_dep: boolean;
                /**
                 * Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
                 */
                network: boolean | null;
                /**
                 * Set to `true` to disable the `updatedb` cron job to avoid disk thrashing.
                 */
                updatedb_disabled: boolean;
            };
            /**
             * __Read-only__ The ID of this Config.
             */
            readonly id: number;
            /**
             * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
             *
             * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
             *
             * - First [0]:  `eth0`
             * - Second [1]: `eth1`
             * - Third [2]:  `eth2`
             *
             * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
             *
             * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
             *
             * > 📘
             * >
             * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
             *
             * `vpc` details
             *
             * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
             *
             * `vlan` details
             *
             * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
             * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
             */
            interfaces: Array<{
                /**
                 * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
                 */
                readonly active?: boolean;
                /**
                 * __Read-only__ The unique ID representing this interface.
                 */
                readonly id?: number;
                /**
                 * IPv4 CIDR VPC subnet ranges that are routed to this interface.
                 *
                 * When included in a request:
                 *
                 * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
                 *
                 * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
                 *
                 * - Submit this as an empty array removes all existing values.
                 *
                 * - Omit this object to leave existing values as is.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > This is only supported for interfaces with a `purpose` of `vpc`.
                 */
                ip_ranges?: Array<string> | null;
                /**
                 * This interface's private IP address in classless inter-domain routing (CIDR) notation.
                 *
                 * For interfaces with a `purpose` of `public`:
                 *
                 * - If you include this in a request, set it to an empty string (`""`) or `null`.
                 *
                 * - Returned as `null` in a response.
                 *
                 * For interfaces with a `purpose` of `vlan`:
                 *
                 * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
                 *
                 * - This should be unique among devices attached to the VLAN to avoid conflict.
                 *
                 * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
                 *
                 * For interfaces with a `purpose` of `vpc`:
                 *
                 * - If you include this in a request, set it to an empty string (`""`) or `null`.
                 *
                 * - Returned as `null` in a response.
                 */
                ipam_address?: string | null;
                /**
                 * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
                 */
                ipv4?: {
                    /**
                     * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                     *
                     * - Only supported for interfaces with a `purpose` of `vpc`.
                     *
                     * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                     *
                     * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                     *
                     * When included in a request:
                     *
                     * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                     *
                     * - A specified address can't be shared with another Linode.
                     *
                     * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                     *
                     * <<LB>>
                     *
                     * > 📘
                     * >
                     * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                     */
                    nat_1_1: string | 'any' | null;
                    /**
                     * The VPC subnet IPv4 address for this interface.
                     *
                     * - This only applies to interfaces with a `purpose` of `vpc`.
                     *
                     * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                     *
                     * When included in a request:
                     *
                     * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                     *
                     * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                     *
                     * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                     */
                    vpc: string | null;
                };
                /**
                 * __Filterable__ The name of this interface.
                 *
                 * For interfaces with a `purpose` of `vlan`:
                 *
                 * - Required.
                 *
                 * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
                 *
                 * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
                 *
                 * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
                 *
                 * For interfaces with a `purpose` of `public`:
                 *
                 * - If you include this in a request, set it to an empty string (`""`) or `null`.
                 *
                 * - Returned as `null` in a response.
                 *
                 * For interfaces with a `purpose` of `vpc`:
                 *
                 * - If you include this in a request, set it to an empty string (`""`) or `null`.
                 *
                 * - Returned as `null` in a response.
                 */
                label?: string | null;
                /**
                 * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
                 *
                 * > 📘
                 * >
                 * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
                 */
                primary?: boolean;
                /**
                 * The type of interface. This can be `public`, `vlan`, or `vpc`.
                 *
                 * For interfaces with a `purpose` of `public`:
                 *
                 * - You can only define one `public` interface per Linode.
                 *
                 * - The Linode's default public IPv4 address is assigned to the `public` interface.
                 *
                 * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
                 *
                 * For interfaces with a `purpose` of `vlan`:
                 *
                 * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
                 *
                 * - If an `ipam_address` is configured, the Linode uses this address.
                 *
                 * For interfaces with a `purpose` of `vpc`:
                 *
                 * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
                 *
                 * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
                 */
                purpose: 'public' | 'vlan' | 'vpc';
                /**
                 * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
                 *
                 * - Required for `vpc` type interfaces.
                 *
                 * - Returned as `null` for non-`vpc` type interfaces.
                 *
                 * - Once you've assigned a VPC subnet to an interface, you can't update it.
                 *
                 * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
                 */
                subnet_id?: number | null;
                /**
                 * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
                 */
                readonly vpc_id?: number | null;
            }>;
            /**
             * The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:
             *
             * - `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.
             *
             * - `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.
             *
             * - `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.
             */
            kernel: string;
            /**
             * __Filterable__ The name of the configuration for display in Akamai Cloud Manager.
             */
            label: string;
            /**
             * Defaults to the total RAM of the Linode.
             */
            memory_limit: number;
            /**
             * The root device to boot.
             *
             * > 📘
             *
             * - If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.
             *
             * - If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.
             */
            root_device: string;
            /**
             * Defines the state of your Linode after booting. Defaults to `default`.
             */
            run_level: 'default' | 'single' | 'binbash';
            /**
             * Controls the virtualization mode. Defaults to `paravirt`.
             *
             * - `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
             *
             * - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
             */
            virt_mode: 'paravirt' | 'fullvirt';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeConfigsResponse = GetLinodeConfigsResponses[keyof GetLinodeConfigsResponses];

export type PostAddLinodeConfigData = {
    /**
     * The parameters to set when creating the configuration profile. This determines things like the kernel, devices, and how much memory a Linode boots with.
     */
    body: {
        /**
         * Optional field for arbitrary user comments on this configuration.
         */
        comments: string | null;
        /**
         * A dictionary of device disks to use as a device map in a Linode's configuration profile.
         *
         * - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
         * - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
         */
        devices: {
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sda: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdb: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdc: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdd: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sde: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdf: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdg: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdh: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
        };
        /**
         * Helpers enabled when booting to this Linode configuration.
         */
        helpers: {
            /**
             * Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.
             */
            devtmpfs_automount: boolean;
            /**
             * Helps maintain correct `inittab` or `upstart` console device.
             */
            distro: boolean;
            /**
             * Creates a modules dependency file for the kernel you run.
             */
            modules_dep: boolean;
            /**
             * Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
             */
            network: boolean | null;
            /**
             * Set to `true` to disable the `updatedb` cron job to avoid disk thrashing.
             */
            updatedb_disabled: boolean;
        };
        /**
         * __Read-only__ The ID of this Config.
         */
        readonly id: number;
        /**
         * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
         *
         * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
         *
         * - First [0]:  `eth0`
         * - Second [1]: `eth1`
         * - Third [2]:  `eth2`
         *
         * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
         *
         * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
         *
         * > 📘
         * >
         * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
         *
         * `vpc` details
         *
         * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
         *
         * `vlan` details
         *
         * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
         * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
         */
        interfaces: Array<{
            /**
             * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
             */
            readonly active?: boolean;
            /**
             * __Read-only__ The unique ID representing this interface.
             */
            readonly id?: number;
            /**
             * IPv4 CIDR VPC subnet ranges that are routed to this interface.
             *
             * When included in a request:
             *
             * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
             *
             * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
             *
             * - Submit this as an empty array removes all existing values.
             *
             * - Omit this object to leave existing values as is.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > This is only supported for interfaces with a `purpose` of `vpc`.
             */
            ip_ranges?: Array<string> | null;
            /**
             * This interface's private IP address in classless inter-domain routing (CIDR) notation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
             *
             * - This should be unique among devices attached to the VLAN to avoid conflict.
             *
             * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            ipam_address?: string | null;
            /**
             * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
             */
            ipv4?: {
                /**
                 * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                 *
                 * - Only supported for interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                 *
                 * - A specified address can't be shared with another Linode.
                 *
                 * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                 */
                nat_1_1: string | 'any' | null;
                /**
                 * The VPC subnet IPv4 address for this interface.
                 *
                 * - This only applies to interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                 *
                 * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                 *
                 * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                 */
                vpc: string | null;
            };
            /**
             * __Filterable__ The name of this interface.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Required.
             *
             * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
             *
             * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
             *
             * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            label?: string | null;
            /**
             * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
             *
             * > 📘
             * >
             * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
             */
            primary?: boolean;
            /**
             * The type of interface. This can be `public`, `vlan`, or `vpc`.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - You can only define one `public` interface per Linode.
             *
             * - The Linode's default public IPv4 address is assigned to the `public` interface.
             *
             * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
             *
             * - If an `ipam_address` is configured, the Linode uses this address.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
             *
             * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
             */
            purpose: 'public' | 'vlan' | 'vpc';
            /**
             * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
             *
             * - Required for `vpc` type interfaces.
             *
             * - Returned as `null` for non-`vpc` type interfaces.
             *
             * - Once you've assigned a VPC subnet to an interface, you can't update it.
             *
             * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
             */
            subnet_id?: number | null;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
             */
            readonly vpc_id?: number | null;
        }>;
        /**
         * The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:
         *
         * - `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.
         *
         * - `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.
         *
         * - `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.
         */
        kernel: string;
        /**
         * __Filterable__ The name of the configuration for display in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Defaults to the total RAM of the Linode.
         */
        memory_limit: number;
        /**
         * The root device to boot.
         *
         * > 📘
         *
         * - If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.
         *
         * - If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.
         */
        root_device: string;
        /**
         * Defines the state of your Linode after booting. Defaults to `default`.
         */
        run_level: 'default' | 'single' | 'binbash';
        /**
         * Controls the virtualization mode. Defaults to `paravirt`.
         *
         * - `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
         *
         * - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
         */
        virt_mode: 'paravirt' | 'fullvirt';
    };
    path: {
        /**
         * ID of the Linode to look up Configuration profiles for.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs';
};

export type PostAddLinodeConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAddLinodeConfigError = PostAddLinodeConfigErrors[keyof PostAddLinodeConfigErrors];

export type PostAddLinodeConfigResponses = {
    /**
     * A configuration profile was created.
     */
    200: {
        /**
         * Optional field for arbitrary user comments on this configuration.
         */
        comments: string | null;
        /**
         * A dictionary of device disks to use as a device map in a Linode's configuration profile.
         *
         * - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
         * - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
         */
        devices: {
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sda: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdb: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdc: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdd: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sde: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdf: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdg: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdh: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
        };
        /**
         * Helpers enabled when booting to this Linode configuration.
         */
        helpers: {
            /**
             * Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.
             */
            devtmpfs_automount: boolean;
            /**
             * Helps maintain correct `inittab` or `upstart` console device.
             */
            distro: boolean;
            /**
             * Creates a modules dependency file for the kernel you run.
             */
            modules_dep: boolean;
            /**
             * Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
             */
            network: boolean | null;
            /**
             * Set to `true` to disable the `updatedb` cron job to avoid disk thrashing.
             */
            updatedb_disabled: boolean;
        };
        /**
         * __Read-only__ The ID of this Config.
         */
        readonly id: number;
        /**
         * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
         *
         * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
         *
         * - First [0]:  `eth0`
         * - Second [1]: `eth1`
         * - Third [2]:  `eth2`
         *
         * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
         *
         * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
         *
         * > 📘
         * >
         * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
         *
         * `vpc` details
         *
         * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
         *
         * `vlan` details
         *
         * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
         * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
         */
        interfaces: Array<{
            /**
             * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
             */
            readonly active?: boolean;
            /**
             * __Read-only__ The unique ID representing this interface.
             */
            readonly id?: number;
            /**
             * IPv4 CIDR VPC subnet ranges that are routed to this interface.
             *
             * When included in a request:
             *
             * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
             *
             * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
             *
             * - Submit this as an empty array removes all existing values.
             *
             * - Omit this object to leave existing values as is.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > This is only supported for interfaces with a `purpose` of `vpc`.
             */
            ip_ranges?: Array<string> | null;
            /**
             * This interface's private IP address in classless inter-domain routing (CIDR) notation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
             *
             * - This should be unique among devices attached to the VLAN to avoid conflict.
             *
             * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            ipam_address?: string | null;
            /**
             * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
             */
            ipv4?: {
                /**
                 * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                 *
                 * - Only supported for interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                 *
                 * - A specified address can't be shared with another Linode.
                 *
                 * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                 */
                nat_1_1: string | 'any' | null;
                /**
                 * The VPC subnet IPv4 address for this interface.
                 *
                 * - This only applies to interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                 *
                 * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                 *
                 * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                 */
                vpc: string | null;
            };
            /**
             * __Filterable__ The name of this interface.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Required.
             *
             * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
             *
             * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
             *
             * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            label?: string | null;
            /**
             * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
             *
             * > 📘
             * >
             * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
             */
            primary?: boolean;
            /**
             * The type of interface. This can be `public`, `vlan`, or `vpc`.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - You can only define one `public` interface per Linode.
             *
             * - The Linode's default public IPv4 address is assigned to the `public` interface.
             *
             * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
             *
             * - If an `ipam_address` is configured, the Linode uses this address.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
             *
             * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
             */
            purpose: 'public' | 'vlan' | 'vpc';
            /**
             * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
             *
             * - Required for `vpc` type interfaces.
             *
             * - Returned as `null` for non-`vpc` type interfaces.
             *
             * - Once you've assigned a VPC subnet to an interface, you can't update it.
             *
             * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
             */
            subnet_id?: number | null;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
             */
            readonly vpc_id?: number | null;
        }>;
        /**
         * The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:
         *
         * - `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.
         *
         * - `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.
         *
         * - `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.
         */
        kernel: string;
        /**
         * __Filterable__ The name of the configuration for display in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Defaults to the total RAM of the Linode.
         */
        memory_limit: number;
        /**
         * The root device to boot.
         *
         * > 📘
         *
         * - If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.
         *
         * - If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.
         */
        root_device: string;
        /**
         * Defines the state of your Linode after booting. Defaults to `default`.
         */
        run_level: 'default' | 'single' | 'binbash';
        /**
         * Controls the virtualization mode. Defaults to `paravirt`.
         *
         * - `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
         *
         * - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
         */
        virt_mode: 'paravirt' | 'fullvirt';
    };
};

export type PostAddLinodeConfigResponse = PostAddLinodeConfigResponses[keyof PostAddLinodeConfigResponses];

export type DeleteLinodeConfigData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}';
};

export type DeleteLinodeConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLinodeConfigError = DeleteLinodeConfigErrors[keyof DeleteLinodeConfigErrors];

export type DeleteLinodeConfigResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLinodeConfigResponse = DeleteLinodeConfigResponses[keyof DeleteLinodeConfigResponses];

export type GetLinodeConfigData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}';
};

export type GetLinodeConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeConfigError = GetLinodeConfigErrors[keyof GetLinodeConfigErrors];

export type GetLinodeConfigResponses = {
    /**
     * A configuration profile object.
     */
    200: {
        /**
         * Optional field for arbitrary user comments on this configuration.
         */
        comments: string | null;
        /**
         * A dictionary of device disks to use as a device map in a Linode's configuration profile.
         *
         * - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
         * - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
         */
        devices: {
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sda: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdb: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdc: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdd: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sde: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdf: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdg: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdh: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
        };
        /**
         * Helpers enabled when booting to this Linode configuration.
         */
        helpers: {
            /**
             * Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.
             */
            devtmpfs_automount: boolean;
            /**
             * Helps maintain correct `inittab` or `upstart` console device.
             */
            distro: boolean;
            /**
             * Creates a modules dependency file for the kernel you run.
             */
            modules_dep: boolean;
            /**
             * Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
             */
            network: boolean | null;
            /**
             * Set to `true` to disable the `updatedb` cron job to avoid disk thrashing.
             */
            updatedb_disabled: boolean;
        };
        /**
         * __Read-only__ The ID of this Config.
         */
        readonly id: number;
        /**
         * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
         *
         * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
         *
         * - First [0]:  `eth0`
         * - Second [1]: `eth1`
         * - Third [2]:  `eth2`
         *
         * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
         *
         * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
         *
         * > 📘
         * >
         * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
         *
         * `vpc` details
         *
         * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
         *
         * `vlan` details
         *
         * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
         * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
         */
        interfaces: Array<{
            /**
             * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
             */
            readonly active?: boolean;
            /**
             * __Read-only__ The unique ID representing this interface.
             */
            readonly id?: number;
            /**
             * IPv4 CIDR VPC subnet ranges that are routed to this interface.
             *
             * When included in a request:
             *
             * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
             *
             * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
             *
             * - Submit this as an empty array removes all existing values.
             *
             * - Omit this object to leave existing values as is.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > This is only supported for interfaces with a `purpose` of `vpc`.
             */
            ip_ranges?: Array<string> | null;
            /**
             * This interface's private IP address in classless inter-domain routing (CIDR) notation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
             *
             * - This should be unique among devices attached to the VLAN to avoid conflict.
             *
             * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            ipam_address?: string | null;
            /**
             * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
             */
            ipv4?: {
                /**
                 * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                 *
                 * - Only supported for interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                 *
                 * - A specified address can't be shared with another Linode.
                 *
                 * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                 */
                nat_1_1: string | 'any' | null;
                /**
                 * The VPC subnet IPv4 address for this interface.
                 *
                 * - This only applies to interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                 *
                 * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                 *
                 * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                 */
                vpc: string | null;
            };
            /**
             * __Filterable__ The name of this interface.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Required.
             *
             * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
             *
             * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
             *
             * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            label?: string | null;
            /**
             * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
             *
             * > 📘
             * >
             * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
             */
            primary?: boolean;
            /**
             * The type of interface. This can be `public`, `vlan`, or `vpc`.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - You can only define one `public` interface per Linode.
             *
             * - The Linode's default public IPv4 address is assigned to the `public` interface.
             *
             * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
             *
             * - If an `ipam_address` is configured, the Linode uses this address.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
             *
             * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
             */
            purpose: 'public' | 'vlan' | 'vpc';
            /**
             * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
             *
             * - Required for `vpc` type interfaces.
             *
             * - Returned as `null` for non-`vpc` type interfaces.
             *
             * - Once you've assigned a VPC subnet to an interface, you can't update it.
             *
             * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
             */
            subnet_id?: number | null;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
             */
            readonly vpc_id?: number | null;
        }>;
        /**
         * The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:
         *
         * - `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.
         *
         * - `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.
         *
         * - `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.
         */
        kernel: string;
        /**
         * __Filterable__ The name of the configuration for display in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Defaults to the total RAM of the Linode.
         */
        memory_limit: number;
        /**
         * The root device to boot.
         *
         * > 📘
         *
         * - If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.
         *
         * - If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.
         */
        root_device: string;
        /**
         * Defines the state of your Linode after booting. Defaults to `default`.
         */
        run_level: 'default' | 'single' | 'binbash';
        /**
         * Controls the virtualization mode. Defaults to `paravirt`.
         *
         * - `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
         *
         * - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
         */
        virt_mode: 'paravirt' | 'fullvirt';
    };
};

export type GetLinodeConfigResponse = GetLinodeConfigResponses[keyof GetLinodeConfigResponses];

export type PutLinodeConfigData = {
    body: {
        /**
         * Optional field for arbitrary user comments on this configuration.
         */
        comments: string | null;
        /**
         * A dictionary of device disks to use as a device map in a Linode's configuration profile.
         *
         * - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
         * - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
         */
        devices: {
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sda: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdb: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdc: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdd: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sde: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdf: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdg: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdh: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
        };
        /**
         * Helpers enabled when booting to this Linode configuration.
         */
        helpers: {
            /**
             * Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.
             */
            devtmpfs_automount: boolean;
            /**
             * Helps maintain correct `inittab` or `upstart` console device.
             */
            distro: boolean;
            /**
             * Creates a modules dependency file for the kernel you run.
             */
            modules_dep: boolean;
            /**
             * Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
             */
            network: boolean | null;
            /**
             * Set to `true` to disable the `updatedb` cron job to avoid disk thrashing.
             */
            updatedb_disabled: boolean;
        };
        /**
         * __Read-only__ The ID of this Config.
         */
        readonly id: number;
        /**
         * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
         *
         * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
         *
         * - First [0]:  `eth0`
         * - Second [1]: `eth1`
         * - Third [2]:  `eth2`
         *
         * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
         *
         * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
         *
         * > 📘
         * >
         * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
         *
         * `vpc` details
         *
         * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
         *
         * `vlan` details
         *
         * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
         * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
         */
        interfaces: Array<{
            /**
             * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
             */
            readonly active?: boolean;
            /**
             * __Read-only__ The unique ID representing this interface.
             */
            readonly id?: number;
            /**
             * IPv4 CIDR VPC subnet ranges that are routed to this interface.
             *
             * When included in a request:
             *
             * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
             *
             * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
             *
             * - Submit this as an empty array removes all existing values.
             *
             * - Omit this object to leave existing values as is.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > This is only supported for interfaces with a `purpose` of `vpc`.
             */
            ip_ranges?: Array<string> | null;
            /**
             * This interface's private IP address in classless inter-domain routing (CIDR) notation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
             *
             * - This should be unique among devices attached to the VLAN to avoid conflict.
             *
             * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            ipam_address?: string | null;
            /**
             * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
             */
            ipv4?: {
                /**
                 * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                 *
                 * - Only supported for interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                 *
                 * - A specified address can't be shared with another Linode.
                 *
                 * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                 */
                nat_1_1: string | 'any' | null;
                /**
                 * The VPC subnet IPv4 address for this interface.
                 *
                 * - This only applies to interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                 *
                 * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                 *
                 * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                 */
                vpc: string | null;
            };
            /**
             * __Filterable__ The name of this interface.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Required.
             *
             * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
             *
             * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
             *
             * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            label?: string | null;
            /**
             * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
             *
             * > 📘
             * >
             * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
             */
            primary?: boolean;
            /**
             * The type of interface. This can be `public`, `vlan`, or `vpc`.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - You can only define one `public` interface per Linode.
             *
             * - The Linode's default public IPv4 address is assigned to the `public` interface.
             *
             * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
             *
             * - If an `ipam_address` is configured, the Linode uses this address.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
             *
             * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
             */
            purpose: 'public' | 'vlan' | 'vpc';
            /**
             * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
             *
             * - Required for `vpc` type interfaces.
             *
             * - Returned as `null` for non-`vpc` type interfaces.
             *
             * - Once you've assigned a VPC subnet to an interface, you can't update it.
             *
             * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
             */
            subnet_id?: number | null;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
             */
            readonly vpc_id?: number | null;
        }>;
        /**
         * The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:
         *
         * - `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.
         *
         * - `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.
         *
         * - `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.
         */
        kernel: string;
        /**
         * __Filterable__ The name of the configuration for display in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Defaults to the total RAM of the Linode.
         */
        memory_limit: number;
        /**
         * The root device to boot.
         *
         * > 📘
         *
         * - If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.
         *
         * - If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.
         */
        root_device: string;
        /**
         * Defines the state of your Linode after booting. Defaults to `default`.
         */
        run_level: 'default' | 'single' | 'binbash';
        /**
         * Controls the virtualization mode. Defaults to `paravirt`.
         *
         * - `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
         *
         * - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
         */
        virt_mode: 'paravirt' | 'fullvirt';
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}';
};

export type PutLinodeConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeConfigError = PutLinodeConfigErrors[keyof PutLinodeConfigErrors];

export type PutLinodeConfigResponses = {
    /**
     * Configuration profile successfully updated.
     */
    200: {
        /**
         * Optional field for arbitrary user comments on this configuration.
         */
        comments: string | null;
        /**
         * A dictionary of device disks to use as a device map in a Linode's configuration profile.
         *
         * - An empty device disk dictionary or a dictionary with empty values for device slots is allowed.
         * - If no devices are specified, booting from this configuration will hold until a device exists that allows the boot process to start.
         */
        devices: {
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sda: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdb: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdc: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdd: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sde: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdf: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdg: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdh: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
        };
        /**
         * Helpers enabled when booting to this Linode configuration.
         */
        helpers: {
            /**
             * Populates the `/dev` directory early during boot without `udev`.  Defaults to `false`.
             */
            devtmpfs_automount: boolean;
            /**
             * Helps maintain correct `inittab` or `upstart` console device.
             */
            distro: boolean;
            /**
             * Creates a modules dependency file for the kernel you run.
             */
            modules_dep: boolean;
            /**
             * Set to `true` to automatically configure static networking. The `network` option applies only to legacy configuration profile interfaces and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
             */
            network: boolean | null;
            /**
             * Set to `true` to disable the `updatedb` cron job to avoid disk thrashing.
             */
            updatedb_disabled: boolean;
        };
        /**
         * __Read-only__ The ID of this Config.
         */
        readonly id: number;
        /**
         * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
         *
         * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
         *
         * - First [0]:  `eth0`
         * - Second [1]: `eth1`
         * - Third [2]:  `eth2`
         *
         * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
         *
         * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
         *
         * > 📘
         * >
         * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
         *
         * `vpc` details
         *
         * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
         *
         * `vlan` details
         *
         * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
         * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
         */
        interfaces: Array<{
            /**
             * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
             */
            readonly active?: boolean;
            /**
             * __Read-only__ The unique ID representing this interface.
             */
            readonly id?: number;
            /**
             * IPv4 CIDR VPC subnet ranges that are routed to this interface.
             *
             * When included in a request:
             *
             * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
             *
             * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
             *
             * - Submit this as an empty array removes all existing values.
             *
             * - Omit this object to leave existing values as is.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > This is only supported for interfaces with a `purpose` of `vpc`.
             */
            ip_ranges?: Array<string> | null;
            /**
             * This interface's private IP address in classless inter-domain routing (CIDR) notation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
             *
             * - This should be unique among devices attached to the VLAN to avoid conflict.
             *
             * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            ipam_address?: string | null;
            /**
             * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
             */
            ipv4?: {
                /**
                 * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                 *
                 * - Only supported for interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
                 *
                 * - A specified address can't be shared with another Linode.
                 *
                 * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
                 *
                 * <<LB>>
                 *
                 * > 📘
                 * >
                 * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
                 */
                nat_1_1: string | 'any' | null;
                /**
                 * The VPC subnet IPv4 address for this interface.
                 *
                 * - This only applies to interfaces with a `purpose` of `vpc`.
                 *
                 * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
                 *
                 * When included in a request:
                 *
                 * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
                 *
                 * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
                 *
                 * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
                 */
                vpc: string | null;
            };
            /**
             * __Filterable__ The name of this interface.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Required.
             *
             * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
             *
             * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
             *
             * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - If you include this in a request, set it to an empty string (`""`) or `null`.
             *
             * - Returned as `null` in a response.
             */
            label?: string | null;
            /**
             * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
             *
             * > 📘
             * >
             * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
             */
            primary?: boolean;
            /**
             * The type of interface. This can be `public`, `vlan`, or `vpc`.
             *
             * For interfaces with a `purpose` of `public`:
             *
             * - You can only define one `public` interface per Linode.
             *
             * - The Linode's default public IPv4 address is assigned to the `public` interface.
             *
             * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
             *
             * For interfaces with a `purpose` of `vlan`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
             *
             * - If an `ipam_address` is configured, the Linode uses this address.
             *
             * For interfaces with a `purpose` of `vpc`:
             *
             * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
             *
             * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
             */
            purpose: 'public' | 'vlan' | 'vpc';
            /**
             * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
             *
             * - Required for `vpc` type interfaces.
             *
             * - Returned as `null` for non-`vpc` type interfaces.
             *
             * - Once you've assigned a VPC subnet to an interface, you can't update it.
             *
             * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
             */
            subnet_id?: number | null;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
             */
            readonly vpc_id?: number | null;
        }>;
        /**
         * The ID of the kernel used to boot a Linode. Run the [List kernels](https://techdocs.akamai.com/linode-api/reference/get-kernels) operation to see all available kernels. Here are some commonly used kernels:
         *
         * - `linode/latest-64bit`. This is the default, our latest kernel at the time of an instance boot or reboot.
         *
         * - `linode/grub2`. The upstream distribution-supplied kernel that's installed on the primary disk, or a custom kernel if installed.
         *
         * - `linode/direct-disk`. The master boot record (MBR) of the primary disk or root device. Use this in place of a Linux kernel.
         */
        kernel: string;
        /**
         * __Filterable__ The name of the configuration for display in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Defaults to the total RAM of the Linode.
         */
        memory_limit: number;
        /**
         * The root device to boot.
         *
         * > 📘
         *
         * - If you leave this empty or set an invalid value, the root device defaults to `/dev/sda`.
         *
         * - If you specify a device at the root device location and it's not mounted, the Linode won't boot until a device is mounted.
         */
        root_device: string;
        /**
         * Defines the state of your Linode after booting. Defaults to `default`.
         */
        run_level: 'default' | 'single' | 'binbash';
        /**
         * Controls the virtualization mode. Defaults to `paravirt`.
         *
         * - `paravirt` is suitable for most cases. Linodes running in `paravirt` mode share some qualities with the host, ultimately making it run faster since there is less transition between it and the host.
         *
         * - `fullvirt` affords more customization, but is slower because 100% of the VM is virtualized.
         */
        virt_mode: 'paravirt' | 'fullvirt';
    };
};

export type PutLinodeConfigResponse = PutLinodeConfigResponses[keyof PutLinodeConfigResponses];

export type GetLinodeConfigInterfacesData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}/interfaces';
};

export type GetLinodeConfigInterfacesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeConfigInterfacesError = GetLinodeConfigInterfacesErrors[keyof GetLinodeConfigInterfacesErrors];

export type GetLinodeConfigInterfacesResponses = {
    /**
     * `interfaces` is applicable only to legacy configuration profiles and does not apply to [Linode interfaces](https://techdocs.akamai.com/linode-api/reference/post-linode-interface).
     *
     * From one to three network interfaces to add to this Linode's configuration profile. The position in the array determines which of the Linode's network interfaces is configured:
     *
     * - First [0]:  `eth0`
     * - Second [1]: `eth1`
     * - Third [2]:  `eth2`
     *
     * When updating a Linode's legacy interfaces, _each interface must be redefined_. An empty `interfaces` array results in a default `public` type interface configuration only.
     *
     * If no public Interface is configured, public IP addresses are still assigned to the Linode but will not be usable without manual configuration.
     *
     * > 📘
     * >
     * > Changes to Linode Interface configurations can be enabled by rebooting the Linode.
     *
     * `vpc` details
     *
     * See the [VPC documentation](https://www.linode.com/docs/products/networking/vpc/#technical-specifications) guide for its specifications and limitations.
     *
     * `vlan` details
     *
     * - Only Next Generation Network (NGN) data centers support VLANs. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the capabilities of data center regions. If a VLAN is attached to your Linode and you attempt to migrate or clone it to a non-NGN data center, the migration or cloning will not initiate. If a Linode cannot be migrated or cloned because of an incompatibility, you will be prompted to select a different data center or contact support.
     * - See the [VLANs Overview](https://www.linode.com/docs/products/networking/vlans/#technical-specifications) guide to view additional specifications and limitations.
     */
    200: Array<{
        /**
         * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
         */
        readonly active?: boolean;
        /**
         * __Read-only__ The unique ID representing this interface.
         */
        readonly id?: number;
        /**
         * IPv4 CIDR VPC subnet ranges that are routed to this interface.
         *
         * When included in a request:
         *
         * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
         *
         * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
         *
         * - Submit this as an empty array removes all existing values.
         *
         * - Omit this object to leave existing values as is.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > This is only supported for interfaces with a `purpose` of `vpc`.
         */
        ip_ranges?: Array<string> | null;
        /**
         * This interface's private IP address in classless inter-domain routing (CIDR) notation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
         *
         * - This should be unique among devices attached to the VLAN to avoid conflict.
         *
         * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        ipam_address?: string | null;
        /**
         * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
         */
        ipv4?: {
            /**
             * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
             *
             * - Only supported for interfaces with a `purpose` of `vpc`.
             *
             * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
             *
             * - A specified address can't be shared with another Linode.
             *
             * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
             */
            nat_1_1: string | 'any' | null;
            /**
             * The VPC subnet IPv4 address for this interface.
             *
             * - This only applies to interfaces with a `purpose` of `vpc`.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
             *
             * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
             *
             * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
             */
            vpc: string | null;
        };
        /**
         * __Filterable__ The name of this interface.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Required.
         *
         * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
         *
         * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
         *
         * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        label?: string | null;
        /**
         * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
         *
         * > 📘
         * >
         * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
         */
        primary?: boolean;
        /**
         * The type of interface. This can be `public`, `vlan`, or `vpc`.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - You can only define one `public` interface per Linode.
         *
         * - The Linode's default public IPv4 address is assigned to the `public` interface.
         *
         * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
         *
         * - If an `ipam_address` is configured, the Linode uses this address.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
         *
         * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
         */
        purpose: 'public' | 'vlan' | 'vpc';
        /**
         * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
         *
         * - Required for `vpc` type interfaces.
         *
         * - Returned as `null` for non-`vpc` type interfaces.
         *
         * - Once you've assigned a VPC subnet to an interface, you can't update it.
         *
         * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
         */
        subnet_id?: number | null;
        /**
         * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
         */
        readonly vpc_id?: number | null;
    }>;
};

export type GetLinodeConfigInterfacesResponse = GetLinodeConfigInterfacesResponses[keyof GetLinodeConfigInterfacesResponses];

export type PostLinodeConfigInterfaceData = {
    body: unknown & {
        /**
         * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
         */
        readonly active?: boolean;
        /**
         * __Read-only__ The unique ID representing this interface.
         */
        readonly id?: number;
        /**
         * IPv4 CIDR VPC subnet ranges that are routed to this interface.
         *
         * When included in a request:
         *
         * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
         *
         * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
         *
         * - Submit this as an empty array removes all existing values.
         *
         * - Omit this object to leave existing values as is.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > This is only supported for interfaces with a `purpose` of `vpc`.
         */
        ip_ranges?: Array<string> | null;
        /**
         * This interface's private IP address in classless inter-domain routing (CIDR) notation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
         *
         * - This should be unique among devices attached to the VLAN to avoid conflict.
         *
         * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        ipam_address?: string | null;
        /**
         * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
         */
        ipv4?: {
            /**
             * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
             *
             * - Only supported for interfaces with a `purpose` of `vpc`.
             *
             * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
             *
             * - A specified address can't be shared with another Linode.
             *
             * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
             */
            nat_1_1: string | 'any' | null;
            /**
             * The VPC subnet IPv4 address for this interface.
             *
             * - This only applies to interfaces with a `purpose` of `vpc`.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
             *
             * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
             *
             * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
             */
            vpc: string | null;
        };
        /**
         * __Filterable__ The name of this interface.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Required.
         *
         * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
         *
         * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
         *
         * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        label?: string | null;
        /**
         * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
         *
         * > 📘
         * >
         * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
         */
        primary?: boolean;
        /**
         * The type of interface. This can be `public`, `vlan`, or `vpc`.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - You can only define one `public` interface per Linode.
         *
         * - The Linode's default public IPv4 address is assigned to the `public` interface.
         *
         * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
         *
         * - If an `ipam_address` is configured, the Linode uses this address.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
         *
         * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
         */
        purpose: 'public' | 'vlan' | 'vpc';
        /**
         * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
         *
         * - Required for `vpc` type interfaces.
         *
         * - Returned as `null` for non-`vpc` type interfaces.
         *
         * - Once you've assigned a VPC subnet to an interface, you can't update it.
         *
         * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
         */
        subnet_id?: number | null;
        /**
         * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
         */
        readonly vpc_id?: number | null;
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}/interfaces';
};

export type PostLinodeConfigInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLinodeConfigInterfaceError = PostLinodeConfigInterfaceErrors[keyof PostLinodeConfigInterfaceErrors];

export type PostLinodeConfigInterfaceResponses = {
    /**
     * The network interface to apply to this Linode's configuration profile.
     */
    200: {
        /**
         * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
         */
        readonly active?: boolean;
        /**
         * __Read-only__ The unique ID representing this interface.
         */
        readonly id?: number;
        /**
         * IPv4 CIDR VPC subnet ranges that are routed to this interface.
         *
         * When included in a request:
         *
         * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
         *
         * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
         *
         * - Submit this as an empty array removes all existing values.
         *
         * - Omit this object to leave existing values as is.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > This is only supported for interfaces with a `purpose` of `vpc`.
         */
        ip_ranges?: Array<string> | null;
        /**
         * This interface's private IP address in classless inter-domain routing (CIDR) notation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
         *
         * - This should be unique among devices attached to the VLAN to avoid conflict.
         *
         * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        ipam_address?: string | null;
        /**
         * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
         */
        ipv4?: {
            /**
             * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
             *
             * - Only supported for interfaces with a `purpose` of `vpc`.
             *
             * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
             *
             * - A specified address can't be shared with another Linode.
             *
             * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
             */
            nat_1_1: string | 'any' | null;
            /**
             * The VPC subnet IPv4 address for this interface.
             *
             * - This only applies to interfaces with a `purpose` of `vpc`.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
             *
             * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
             *
             * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
             */
            vpc: string | null;
        };
        /**
         * __Filterable__ The name of this interface.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Required.
         *
         * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
         *
         * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
         *
         * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        label?: string | null;
        /**
         * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
         *
         * > 📘
         * >
         * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
         */
        primary?: boolean;
        /**
         * The type of interface. This can be `public`, `vlan`, or `vpc`.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - You can only define one `public` interface per Linode.
         *
         * - The Linode's default public IPv4 address is assigned to the `public` interface.
         *
         * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
         *
         * - If an `ipam_address` is configured, the Linode uses this address.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
         *
         * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
         */
        purpose: 'public' | 'vlan' | 'vpc';
        /**
         * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
         *
         * - Required for `vpc` type interfaces.
         *
         * - Returned as `null` for non-`vpc` type interfaces.
         *
         * - Once you've assigned a VPC subnet to an interface, you can't update it.
         *
         * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
         */
        subnet_id?: number | null;
        /**
         * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
         */
        readonly vpc_id?: number | null;
    };
};

export type PostLinodeConfigInterfaceResponse = PostLinodeConfigInterfaceResponses[keyof PostLinodeConfigInterfaceResponses];

export type PostLinodeConfigInterfacesData = {
    /**
     * Linode Configuration Interfaces Order request object.
     */
    body: {
        /**
         * An ordered array of existing Configuration Profile Interface `id`s.
         *
         * - All current Interface `id`s must be present in the array.
         * - If the Configuration Profile contains Interfaces and is active on the Linode, the Linode must first be shut down prior to running this operation.
         * - Reordering takes effect after rebooting the Linode with this Configuration Profile.
         *
         * The position in the array determines which of the Linode's network Interfaces is configured:
         *
         * - First [0]:  eth0
         * - Second [1]: eth1
         * - Third [2]:  eth2
         */
        ids: Array<number>;
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}/interfaces/order';
};

export type PostLinodeConfigInterfacesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLinodeConfigInterfacesError = PostLinodeConfigInterfacesErrors[keyof PostLinodeConfigInterfacesErrors];

export type PostLinodeConfigInterfacesResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostLinodeConfigInterfacesResponse = PostLinodeConfigInterfacesResponses[keyof PostLinodeConfigInterfacesResponses];

export type DeleteLinodeConfigInterfaceData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
        /**
         * The `id` of the Linode Configuration Profile Interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}/interfaces/{interfaceId}';
};

export type DeleteLinodeConfigInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLinodeConfigInterfaceError = DeleteLinodeConfigInterfaceErrors[keyof DeleteLinodeConfigInterfaceErrors];

export type DeleteLinodeConfigInterfaceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLinodeConfigInterfaceResponse = DeleteLinodeConfigInterfaceResponses[keyof DeleteLinodeConfigInterfaceResponses];

export type GetLinodeConfigInterfaceData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
        /**
         * The `id` of the Linode Configuration Profile Interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}/interfaces/{interfaceId}';
};

export type GetLinodeConfigInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeConfigInterfaceError = GetLinodeConfigInterfaceErrors[keyof GetLinodeConfigInterfaceErrors];

export type GetLinodeConfigInterfaceResponses = {
    /**
     * The network interface to apply to this Linode's configuration profile.
     */
    200: {
        /**
         * __Read-only__ Returns `true` if the interface is in use, meaning that the Linode has been booted using the configuration profile to which the interface belongs.
         */
        readonly active?: boolean;
        /**
         * __Read-only__ The unique ID representing this interface.
         */
        readonly id?: number;
        /**
         * IPv4 CIDR VPC subnet ranges that are routed to this interface.
         *
         * When included in a request:
         *
         * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
         *
         * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
         *
         * - Submit this as an empty array removes all existing values.
         *
         * - Omit this object to leave existing values as is.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > This is only supported for interfaces with a `purpose` of `vpc`.
         */
        ip_ranges?: Array<string> | null;
        /**
         * This interface's private IP address in classless inter-domain routing (CIDR) notation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - To avoid conflicting addresses, make sure this value is unique for each `vlan` interface.
         *
         * - This should be unique among devices attached to the VLAN to avoid conflict.
         *
         * - If Network Helper is enabled, the Linode's interface will be automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        ipam_address?: string | null;
        /**
         * IPv4 addresses configured for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no `vpc` interface is assigned.
         */
        ipv4?: {
            /**
             * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
             *
             * - Only supported for interfaces with a `purpose` of `vpc`.
             *
             * - Returned as `null` if no 1:1 NAT is set for a `vpc` type interface.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
             *
             * - A specified address can't be shared with another Linode.
             *
             * - Omit this object or include it and set it to `null` or an empty string (`""`) to block creation of a 1:1 NAT.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > You can't set this to a specific IPv4 address when creating a new Linode. During the creation process, the network automatically establishes a public IPv4 address for the Linode. Since this address doesn't exist yet, you can't include a custom IPv4 address to change it. After your Linode is created, you can [update your configuration profile interface](https://www.linode.com/docs/api/linode-instances/#configuration-profile-interface-update) to change the `nat_1_1` address.
             */
            nat_1_1: string | 'any' | null;
            /**
             * The VPC subnet IPv4 address for this interface.
             *
             * - This only applies to interfaces with a `purpose` of `vpc`.
             *
             * - Returned as an empty string (`""`) for non-`vpc` type interfaces.
             *
             * When included in a request:
             *
             * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
             *
             * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
             *
             * - If omitted, a valid address within the Subnet IPv4 range is automatically assigned.
             */
            vpc: string | null;
        };
        /**
         * __Filterable__ The name of this interface.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Required.
         *
         * - This needs to be unique among a Linode's interfaces. A Linode can't be attached to the same VLAN multiple times.
         *
         * - This can only contain ASCII letters, numbers, and dashes (`-`). You can't use two consecutive dashes (`--`).
         *
         * - If the VLAN label is new, a VLAN is created. Up to 10 VLANs can be created in each data center `region`. To view your active VLANs, run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - If you include this in a request, set it to an empty string (`""`) or `null`.
         *
         * - Returned as `null` in a response.
         */
        label?: string | null;
        /**
         * The default route to the Linode. Each Linode can have one interface set as its `primary`. If you haven't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
         *
         * > 📘
         * >
         * > This needs to be set to `false` for any interface that uses `vlan` as its `purpose`.
         */
        primary?: boolean;
        /**
         * The type of interface. This can be `public`, `vlan`, or `vpc`.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - You can only define one `public` interface per Linode.
         *
         * - The Linode's default public IPv4 address is assigned to the `public` interface.
         *
         * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to the VLAN with the specified `label`.
         *
         * - If an `ipam_address` is configured, the Linode uses this address.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - Configuring this `purpose` of interface attaches a Linode to an existing VPC subnet with the specified `subnet_id`.
         *
         * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
         */
        purpose: 'public' | 'vlan' | 'vpc';
        /**
         * The `id` of the VPC subnet for this interface. Use this value in a request to assign a Linode to a VPC subnet.
         *
         * - Required for `vpc` type interfaces.
         *
         * - Returned as `null` for non-`vpc` type interfaces.
         *
         * - Once you've assigned a VPC subnet to an interface, you can't update it.
         *
         * - You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
         */
        subnet_id?: number | null;
        /**
         * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
         */
        readonly vpc_id?: number | null;
    };
};

export type GetLinodeConfigInterfaceResponse = GetLinodeConfigInterfaceResponses[keyof GetLinodeConfigInterfaceResponses];

export type PutLinodeConfigInterfaceData = {
    /**
     * Linode configuration profile interface update request object.
     */
    body: {
        /**
         * IPv4 CIDR VPC subnet ranges that are routed to this interface.
         *
         * - A range can't include any addresses that are assigned to an active Linode or another VPC subnet.
         *
         * - When updating, you need to include any existing ranges to maintain them. If a range is left out, it will be removed.
         *
         * - Include this as an empty array (`[]`) to remove all existing `nat_1_1` values.
         *
         * - Omit this array to leave all existing `ip_ranges` as is.
         * <<LB>>
         * > 📘
         * >
         * > This only applies to interfaces with a `purpose` of `vpc`.
         */
        ip_ranges: Array<string>;
        /**
         * IPv4 addresses configured for this interface.
         *
         * > 📘
         * >
         * > This only applies to interfaces with a `purpose` of `vpc`.
         */
        ipv4: {
            /**
             * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
             *
             * - You can set this to a specific, public IPv4 address that's available on the Linode. You can also use the `any` keyword to enable the Linode's assigned public IPv4 address.
             *
             * - A specified address can't be used on another Linode.
             *
             * - Include this as an empty object (`""`) to remove an existing `nat_1_1` value.
             *
             * - Omit this object or include it and set it to `null` to leave an existing `nat_1_1` value as is.
             */
            nat_1_1: string | 'any' | null;
            /**
             * The VPC subnet IPv4 address for this interface.
             *
             * - The `vpc` can't be assigned to an existing Linode as an address or in a range.
             *
             * - The target address can't be the first two or last two addresses in the subnet IPv4 range.
             *
             * - If omitted, a valid address within the subnet IPv4 range is automatically assigned.
             */
            vpc: string | null;
        };
        /**
         * Set to `true` to label this configuration profile interface as the default route to the Linode.
         *
         * - Each Linode can have one interface set as its `primary`.
         *
         * - If you don't specifically set a `primary`, the first non-`vlan` type interface is automatically treated as the primary.
         */
        primary: boolean;
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Configuration Profile.
         */
        configId: number;
        /**
         * The `id` of the Linode Configuration Profile Interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/configs/{configId}/interfaces/{interfaceId}';
};

export type PutLinodeConfigInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeConfigInterfaceError = PutLinodeConfigInterfaceErrors[keyof PutLinodeConfigInterfaceErrors];

export type PutLinodeConfigInterfaceResponses = {
    /**
     * The network interface to apply to this Linode's configuration profile.
     */
    200: {
        /**
         * __Read-only__ Returned as `true` if the interface has been booted using the configuration profile to which the interface belongs.
         */
        readonly active?: boolean;
        /**
         * __Read-only__ The unique ID representing this interface.
         */
        readonly id?: number;
        /**
         * IPv4 CIDR VPC subnet ranges that are routed to this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as an empty string (`""`) if the interface has a `purpose` of `public` or `vlan`.
         */
        ip_ranges?: Array<string> | null;
        /**
         * The interface's private IP address, in classless inter-domain routing (CIDR) notation. Only applies to interfaces with a `purpose` of `vlan`. Returned as `null` if the interface has a `purpose` of `public` or `vpc`.
         *
         * > 📘
         * >
         * > If Network Helper is enabled, the Linode's interface is automatically configured to use this address after the Linode is rebooted. If Network Helper is disabled, enable the address using [manual static IP configuration](https://www.linode.com/docs/guides/manual-network-configuration/).
         */
        ipam_address?: string | null;
        /**
         * IPv4 addresses configured for this interface. Only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if the interface has a `purpose` of `public` or `vlan`.
         */
        ipv4?: {
            /**
             * The 1:1 NAT IPv4 address, used to associate a public IPv4 address with the interface's VPC subnet IPv4 address. This only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if no 1:1 NAT is set for a `vpc` interface. Returned as an empty string (`""`) if the interface has a `purpose` of `public` or `vlan`.
             */
            nat_1_1: string | null;
            /**
             * The VPC subnet IPv4 address for this interface. This only applies to interfaces with a `purpose` of `vpc`. Returned as an empty string (`""`) if the interface has a `purpose` of `public` or `vlan`.
             */
            vpc: string | null;
        };
        /**
         * __Filterable__ The name set for an interface with a `purpose` of `vlan`. Returned as `null` if the interface has a `purpose` of `public` or `vpc`.
         */
        label?: string | null;
        /**
         * The default route to the Linode. Each Linode can have one interface set as its `primary`. Defaults to the first non-`vlan` type interface, if you haven't specifically set a `primary`.
         *
         * > 📘
         * >
         * > The value needs to `false` for any interface that uses `vlan` as its `purpose`.
         */
        primary?: boolean;
        /**
         * The type of interface. This can be `public`, `vlan`, or `vpc`.
         *
         * For interfaces with a `purpose` of `public`:
         *
         * - You can only define one `public` interface per Linode.
         *
         * - The Linode's default public IPv4 address is assigned to the `public` interface.
         *
         * - A Linode needs a `public` interface in the first or `eth0` position to be reachable via the public internet, after it boots. If no `public` interface is configured, you can only access the Linode through [LISH](https://www.linode.com/docs/products/compute/compute-instances/guides/lish/), or through another Linode that's connected to the same VLAN or VPC.
         *
         * For interfaces with a `purpose` of `vlan`:
         *
         * - The target Linode is attached to the VLAN with the specified `label`.
         *
         * - If an `ipam_address` is configured, the Linode uses this address.
         *
         * For interfaces with a `purpose` of `vpc`:
         *
         * - The target Linode is attached to an existing VPC subnet with the specified `subnet_id`.
         *
         * - When the interface is activated, the Linode is configured to use an IP address from the range in the assigned VPC subnet. See `ipv4.vpc` for more information.
         */
        purpose: 'public' | 'vlan' | 'vpc';
        /**
         * The `id` of the VPC subnet for this interface. Only applies to interfaces with a `purpose` of `vpc`. Returned as `null` if the interface has a `purpose` of `public` or `vlan`.
         *
         * > 📘
         * >
         * > You need to reboot a Linode using the interface's configuration profile to assign the Linode to a VPC subnet.
         */
        subnet_id?: number | null;
        /**
         * __Read-only__ The `id` of the VPC configured for this interface. Returned as `null` for non-`vpc` type interfaces.
         */
        readonly vpc_id?: number | null;
    };
};

export type PutLinodeConfigInterfaceResponse = PutLinodeConfigInterfaceResponses[keyof PutLinodeConfigInterfaceResponses];

export type GetLinodeDisksData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/instances/{linodeId}/disks';
};

export type GetLinodeDisksErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeDisksError = GetLinodeDisksErrors[keyof GetLinodeDisksErrors];

export type GetLinodeDisksResponses = {
    /**
     * Returns a paginated list of disks associated with this Linode.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this disk was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Displays if encryption is enabled on this disk. This setting is based on the `disk_encryption` setting of the Linode.
             */
            readonly disk_encryption: string;
            /**
             * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
             *
             * > 📘
             * >
             * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
             */
            filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
            /**
             * __Read-only__ This disk's ID. You need this value to run other operations that interact with the disk.
             */
            readonly id: number;
            /**
             * __Filterable__ The name of the disk. This is for display purposes only.
             */
            label: string;
            /**
             * __Filterable__ The size of the disk in MB.
             */
            size: number;
            /**
             * __Read-only__ The current state of the disk.
             */
            status: 'ready' | 'not ready' | 'deleting';
            /**
             * __Read-only__ When this disk was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeDisksResponse = GetLinodeDisksResponses[keyof GetLinodeDisksResponses];

export type PostAddLinodeDiskData = {
    /**
     * Disk object request.
     */
    body: {
        /**
         * __Write-only__ A list of public SSH keys that will be automatically appended to the root user's `~/.ssh/authorized_keys` file when deploying from an Image.
         */
        authorized_keys: Array<string>;
        /**
         * __Write-only__ A list of usernames for authorized users. Before you can add a user, it needs an SSH key assigned to its profile. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information. If the usernames already have associated SSH keys, the keys will be appended to the root users `~/.ssh/authorized_keys` file automatically when deploying from an image.
         */
        authorized_users: Array<string>;
        /**
         * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
         *
         * > 📘
         * >
         * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
         */
        filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Filterable__ The name of the disk. This is for display purposes only.
         */
        label: string;
        /**
         * __Write-only__ This sets the root user's password on a newly created Linode Disk when deploying from an Image.
         *
         * - __Required__ when creating a Linode Disk from an Image, including when using a StackScript.
         *
         * - Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
         */
        root_pass: string;
        /**
         * __Filterable__ The size of the Disk in MB.
         *
         * Images require a minimum size. Run the [Get an image](https://techdocs.akamai.com/linode-api/reference/get-image) operation to view its size.
         */
        size: number;
        /**
         * This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.
         *
         * This field is required to be valid JSON.
         *
         * Total length cannot exceed 65,535 characters.
         */
        stackscript_data: {
            [key: string]: unknown;
        };
        /**
         * A StackScript ID that will cause the referenced StackScript to be run during deployment of this Linode. A compatible `image` is required to use a StackScript. To get a list of available StackScript and their permitted Images, run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts). This field cannot be used when deploying from a Backup or a Private Image.
         */
        stackscript_id: number;
    };
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks';
};

export type PostAddLinodeDiskErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAddLinodeDiskError = PostAddLinodeDiskErrors[keyof PostAddLinodeDiskErrors];

export type PostAddLinodeDiskResponses = {
    /**
     * Disk created.
     */
    200: {
        /**
         * __Read-only__ When this disk was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Displays if encryption is enabled on this disk. This setting is based on the `disk_encryption` setting of the Linode.
         */
        readonly disk_encryption: string;
        /**
         * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
         *
         * > 📘
         * >
         * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
         */
        filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
        /**
         * __Read-only__ This disk's ID. You need this value to run other operations that interact with the disk.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of the disk. This is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__ The size of the disk in MB.
         */
        size: number;
        /**
         * __Read-only__ The current state of the disk.
         */
        status: 'ready' | 'not ready' | 'deleting';
        /**
         * __Read-only__ When this disk was last updated.
         */
        readonly updated: string;
    };
};

export type PostAddLinodeDiskResponse = PostAddLinodeDiskResponses[keyof PostAddLinodeDiskResponses];

export type DeleteDiskData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * ID of the Disk to look up.
         */
        diskId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks/{diskId}';
};

export type DeleteDiskErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteDiskError = DeleteDiskErrors[keyof DeleteDiskErrors];

export type DeleteDiskResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteDiskResponse = DeleteDiskResponses[keyof DeleteDiskResponses];

export type GetLinodeDiskData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * ID of the Disk to look up.
         */
        diskId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks/{diskId}';
};

export type GetLinodeDiskErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeDiskError = GetLinodeDiskErrors[keyof GetLinodeDiskErrors];

export type GetLinodeDiskResponses = {
    /**
     * Returns a single Disk object.
     */
    200: {
        /**
         * __Read-only__ When this disk was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Displays if encryption is enabled on this disk. This setting is based on the `disk_encryption` setting of the Linode.
         */
        readonly disk_encryption: string;
        /**
         * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
         *
         * > 📘
         * >
         * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
         */
        filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
        /**
         * __Read-only__ This disk's ID. You need this value to run other operations that interact with the disk.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of the disk. This is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__ The size of the disk in MB.
         */
        size: number;
        /**
         * __Read-only__ The current state of the disk.
         */
        status: 'ready' | 'not ready' | 'deleting';
        /**
         * __Read-only__ When this disk was last updated.
         */
        readonly updated: string;
    };
};

export type GetLinodeDiskResponse = GetLinodeDiskResponses[keyof GetLinodeDiskResponses];

export type PutDiskData = {
    /**
     * Updates the parameters of a single Disk.
     */
    body: {
        /**
         * __Filterable__ The name of the disk. This is for display purposes only.
         */
        label: string;
    };
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * ID of the Disk to look up.
         */
        diskId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks/{diskId}';
};

export type PutDiskErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutDiskError = PutDiskErrors[keyof PutDiskErrors];

export type PutDiskResponses = {
    /**
     * The updated Disk.
     */
    200: {
        /**
         * __Read-only__ When this disk was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Displays if encryption is enabled on this disk. This setting is based on the `disk_encryption` setting of the Linode.
         */
        readonly disk_encryption: string;
        /**
         * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
         *
         * > 📘
         * >
         * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
         */
        filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
        /**
         * __Read-only__ This disk's ID. You need this value to run other operations that interact with the disk.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of the disk. This is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__ The size of the disk in MB.
         */
        size: number;
        /**
         * __Read-only__ The current state of the disk.
         */
        status: 'ready' | 'not ready' | 'deleting';
        /**
         * __Read-only__ When this disk was last updated.
         */
        readonly updated: string;
    };
};

export type PutDiskResponse = PutDiskResponses[keyof PutDiskResponses];

export type PostCloneLinodeDiskData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * ID of the Disk to clone.
         */
        diskId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks/{diskId}/clone';
};

export type PostCloneLinodeDiskErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCloneLinodeDiskError = PostCloneLinodeDiskErrors[keyof PostCloneLinodeDiskErrors];

export type PostCloneLinodeDiskResponses = {
    /**
     * Disk clone initiated.
     */
    200: {
        /**
         * __Read-only__ When this disk was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Displays if encryption is enabled on this disk. This setting is based on the `disk_encryption` setting of the Linode.
         */
        readonly disk_encryption: string;
        /**
         * The disk's format or file system. A value of `raw` indicates no file system, just a raw binary stream. A value of `swap` indicates a Linux swap area. The values `ext3` or `ext4` represent these Linux journaling file systems. The value `ext2` is the deprecated ext2 Linux file system. Finally, `initrd` indicates the disk is formatted as an uncompressed initial RAM disk.
         *
         * > 📘
         * >
         * > The `ext2` file system doesn't properly support timestamps and will be removed from Linux support in the near future. Also, `initrd` is a legacy format that no longer applies to most use cases. As a best practice, use the other supported formats or file systems instead.
         */
        filesystem: 'raw' | 'swap' | 'ext2' | 'ext3' | 'ext4' | 'initrd';
        /**
         * __Read-only__ This disk's ID. You need this value to run other operations that interact with the disk.
         */
        readonly id: number;
        /**
         * __Filterable__ The name of the disk. This is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__ The size of the disk in MB.
         */
        size: number;
        /**
         * __Read-only__ The current state of the disk.
         */
        status: 'ready' | 'not ready' | 'deleting';
        /**
         * __Read-only__ When this disk was last updated.
         */
        readonly updated: string;
    };
};

export type PostCloneLinodeDiskResponse = PostCloneLinodeDiskResponses[keyof PostCloneLinodeDiskResponses];

export type PostResetDiskPasswordData = {
    /**
     * The new password.
     */
    body: {
        /**
         * The new root password for the OS installed on this Disk. The password must meet the complexity strength validation requirements for a strong password.
         */
        password: string;
    };
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * ID of the Disk to look up.
         */
        diskId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks/{diskId}/password';
};

export type PostResetDiskPasswordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostResetDiskPasswordError = PostResetDiskPasswordErrors[keyof PostResetDiskPasswordErrors];

export type PostResetDiskPasswordResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostResetDiskPasswordResponse = PostResetDiskPasswordResponses[keyof PostResetDiskPasswordResponses];

export type PostResizeDiskData = {
    /**
     * The new size of the Disk.
     */
    body: {
        /**
         * The desired size, in MB, of the disk.
         */
        size: number;
    };
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * ID of the Disk to look up.
         */
        diskId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/disks/{diskId}/resize';
};

export type PostResizeDiskErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostResizeDiskError = PostResizeDiskErrors[keyof PostResizeDiskErrors];

export type PostResizeDiskResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostResizeDiskResponse = PostResizeDiskResponses[keyof PostResizeDiskResponses];

export type GetLinodeFirewallsData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode.
         */
        linodeId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/instances/{linodeId}/firewalls';
};

export type GetLinodeFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeFirewallsError = GetLinodeFirewallsErrors[keyof GetLinodeFirewallsErrors];

export type GetLinodeFirewallsResponses = {
    /**
     * Returns a paginated list of Firewalls assigned to this Linode.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this Firewall was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
             */
            readonly entities: Array<{
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a `linode`.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'linode_interface' | 'nodebalancer';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            }>;
            /**
             * __Filterable__, __Read-only__ The Firewall's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The Firewall's label, for display purposes only.
             *
             * Firewall labels have the following constraints:
             *
             * - Must begin and end with an alphanumeric character.
             * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             * - Must be between 3 and 32 characters.
             * - Must be unique.
             */
            label: string;
            /**
             * The inbound and outbound access rules to apply to the Firewall.
             *
             * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
             *
             * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
                 */
                readonly fingerprint: string;
                /**
                 * The inbound rules for the firewall, as a JSON array.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall, as a JSON array.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
                 */
                readonly version: number;
            };
            /**
             * __Read-only__ The status of this Firewall.
             *
             * - When a Firewall is first created its status is `enabled`.
             * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
             * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
             */
            status: 'enabled' | 'disabled' | 'deleted';
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Filterable__, __Read-only__ When this Firewall was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeFirewallsResponse = GetLinodeFirewallsResponses[keyof GetLinodeFirewallsResponses];

export type PutLinodeFirewallsData = {
    /**
     * Replace or remove firewalls IDs assigned to a Linode or NodeBalancer.
     */
    body: {
        /**
         * A complete list of firewall IDs to assign to this Linode or NodeBalancer. This operation replaces any existing assignments. To remove all firewalls, pass an empty list, `[]`.
         */
        firewall_ids: Array<number>;
    };
    path: {
        /**
         * The ID of the Linode.
         */
        linodeId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/instances/{linodeId}/firewalls';
};

export type PutLinodeFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeFirewallsError = PutLinodeFirewallsErrors[keyof PutLinodeFirewallsErrors];

export type PutLinodeFirewallsResponses = {
    /**
     * Returns a paginated list of Firewalls assigned to this Linode.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this Firewall was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
             */
            readonly entities: Array<{
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a `linode`.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'linode_interface' | 'nodebalancer';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            }>;
            /**
             * __Filterable__, __Read-only__ The Firewall's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The Firewall's label, for display purposes only.
             *
             * Firewall labels have the following constraints:
             *
             * - Must begin and end with an alphanumeric character.
             * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             * - Must be between 3 and 32 characters.
             * - Must be unique.
             */
            label: string;
            /**
             * The inbound and outbound access rules to apply to the Firewall.
             *
             * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
             *
             * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
                 */
                readonly fingerprint: string;
                /**
                 * The inbound rules for the firewall, as a JSON array.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall, as a JSON array.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
                 */
                readonly version: number;
            };
            /**
             * __Read-only__ The status of this Firewall.
             *
             * - When a Firewall is first created its status is `enabled`.
             * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
             * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
             */
            status: 'enabled' | 'disabled' | 'deleted';
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Filterable__, __Read-only__ When this Firewall was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type PutLinodeFirewallsResponse = PutLinodeFirewallsResponses[keyof PutLinodeFirewallsResponses];

export type PostApplyFirewallsData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/firewalls/apply';
};

export type PostApplyFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostApplyFirewallsError = PostApplyFirewallsErrors[keyof PostApplyFirewallsErrors];

export type PostApplyFirewallsResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostApplyFirewallsResponse = PostApplyFirewallsResponses[keyof PostApplyFirewallsResponses];

export type GetLinodeInterfacesData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces';
};

export type GetLinodeInterfacesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInterfacesError = GetLinodeInterfacesErrors[keyof GetLinodeInterfacesErrors];

export type GetLinodeInterfacesResponses = {
    /**
     * A list of all the interfaces available for a Linode.
     */
    200: {
        /**
         * Any of the following interface types: VPC, public, or VLAN.
         */
        interfaces: Array<{
            /**
             * When the interface was created.
             */
            created: string;
            /**
             * Indicates if the interface is used as a default route.
             */
            default_route: {
                /**
                 * Indicates if the interface is used for the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
                 */
                ipv4: boolean;
                /**
                 * Indicates if the interface is used for the IPv6 default route. Only one interface per Linode can have the IPv6 default route.
                 */
                ipv6: boolean;
            } | null;
            /**
             * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
             */
            readonly id: number;
            /**
             * A 48-bit MAC address used to identify the Linode’s interface. A public interface's `mac_address` does not change, even if the public interface is deleted and then recreated.
             */
            mac_address: string;
            /**
             * Public interface type.
             */
            public: {
                /**
                 * The interface's public IPv4 `addresses`.
                 */
                ipv4: {
                    /**
                     * The public IPv4 addresses and primary settings for this public interface.
                     */
                    addresses: Array<{
                        /**
                         * The public IPv4 address assigned to this interface.
                         */
                        address: string;
                        /**
                         * Indicates if the public IPv4 address serves as the source address for traffic routing within the Linode and other corresponding network interfaces and services.
                         */
                        primary: boolean;
                    }>;
                    /**
                     * The IPv4 address assigned to this Linode interface, which is also shared with another Linode.
                     */
                    shared: Array<{
                        /**
                         * Shared IPv4 address.
                         */
                        address: string;
                        /**
                         * The ID of the Linode this address currently belongs to. For IPv4 addresses, by default this is the Linode this address was assigned when created.
                         */
                        linode_id: string;
                    }>;
                };
                /**
                 * The interface's public IPv6 configuration.
                 */
                ipv6: {
                    /**
                     * List of IPv6 ranges assigned to this interface.
                     */
                    ranges: Array<{
                        /**
                         * IPv6 range in CIDR notation (`2600:0db8::1/64`) or prefix-only (`/64`).
                         */
                        range: string;
                        /**
                         * The public IPv6 address that the `range` is routed to.
                         */
                        route_target: string;
                    }>;
                    /**
                     * The IPv6 address assigned to this Linode interface, which is also shared with another Linode.
                     */
                    shared: Array<{
                        /**
                         * The IPv6 address range.
                         */
                        range: string;
                        /**
                         * The public IPv6 address that the `range` is routed to.
                         */
                        route_target: string;
                    }>;
                    /**
                     * The public `slaac` and subnet prefix settings for this public interface that is used to communicate over the public internet, and with other services in the same data center.
                     */
                    slaac: Array<{
                        /**
                         * Public IPv6 addresses assigned to this interface.
                         */
                        address: string;
                        /**
                         * The prefix length advertised for SLAAC to use. Only the specific (`/128`) EUI-64 address derived from the interface's MAC address is supported. To ensure the MAC-based EUI-64 address is used, privacy addressing needs to be disabled. Network Helper automatically configures the MAC-derived EUI-64 address. If you disable Network Helper or use an unsupported operating system, follow the specific instructions for your OS.
                         */
                        prefix: number;
                    }>;
                };
            };
            /**
             * When the interface was last updated.
             */
            updated: string;
            /**
             * Interface version number that increments when the interface updates.
             */
            version: number;
            /**
             * The value is `null` if this is not a VLAN interface.
             */
            vlan: {
                [key: string]: unknown;
            } | null;
            /**
             * The value is `null` if this is not a VPC interface.
             */
            vpc: {
                [key: string]: never;
            } | null;
        } | {
            /**
             * When the interface was created.
             */
            created: string;
            /**
             * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
             */
            readonly id: number;
            /**
             * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
             */
            mac_address: string;
            /**
             * The value is `null` if this isn't a public interface.
             */
            public: {
                [key: string]: unknown;
            } | null;
            /**
             * When the interface was last updated.
             */
            updated: string;
            /**
             * Interface version number that is incremented when the interface is updated.
             */
            version: number;
            /**
             * VLAN interface type.
             */
            vlan: {
                /**
                 * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
                 */
                ipam_address: string | null;
                /**
                 * The VLAN's label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
                 */
                vlan_label: string;
            };
            /**
             * The value is `null` if this isn't a VPC interface.
             */
            vpc: {
                [key: string]: unknown;
            } | null;
        } | {
            /**
             * When the interface was created.
             */
            created: string;
            /**
             * Indicates if the interface serves as a default route.
             */
            default_route: {
                /**
                 * Indicates if the interface serves as the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
                 */
                ipv4: boolean;
            };
            /**
             * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
             */
            readonly id: number;
            /**
             * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
             */
            mac_address: string;
            /**
             * The value is `null` if this is not a public interface.
             */
            public: {
                [key: string]: never;
            } | null;
            /**
             * When the interface last updated.
             */
            updated: string;
            /**
             * The version number of the interface configuration, incremented each time the interface is updated.
             */
            version: number;
            /**
             * The value is `null` if this is not a VLAN interface.
             */
            vlan: {
                [key: string]: never;
            } | null;
            /**
             * VPC interface type.
             */
            vpc: {
                /**
                 * The interface's IPv4 `addresses` and `ranges` configuration.
                 */
                ipv4: {
                    /**
                     * IPv4 address settings for this VPC interface.
                     */
                    addresses: Array<{
                        /**
                         * The VPC subnet IPv4 address.
                         */
                        address: string;
                        /**
                         * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                         */
                        nat_1_1_address: string | null;
                        /**
                         * Indicates if the IPv4 address is used to set up a source address for routes inside the Linode for the corresponding network interface.
                         */
                        primary: boolean;
                    }>;
                    /**
                     * VPC IPv4 ranges.
                     */
                    ranges: Array<{
                        /**
                         * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                         */
                        range: string;
                    }>;
                };
                /**
                 * VPC subnet's unique identifier.
                 */
                subnet_id: number;
                /**
                 * VPC's unique identifier.
                 */
                vpc_id: number;
            };
        }>;
    };
};

export type GetLinodeInterfacesResponse = GetLinodeInterfacesResponses[keyof GetLinodeInterfacesResponses];

export type PostLinodeInterfaceData = {
    /**
     * Defines Linode interfaces. You can configure interfaces for one of the following types: `vpc`, `public`, or `vlan`. Any other type must either be omitted or set to `null`.
     */
    body?: {
        /**
         * Indicates if the interface serves as the default route when multiple interfaces are eligible for this role. A public interface can have both an IPv4 `default_route` and an IPv6 `default_route`, provided it has at least one IP address of the corresponding type.
         */
        default_route: {
            /**
             * If set to `true`, the interface is used for the IPv4 `default_route`. Only one interface per Linode can be set as the IPv4 default route.
             */
            ipv4: boolean | null;
            /**
             * If set to `true`, the interface is used for the IPv6 `default_route`. Only one interface per Linode can have the IPv6 default route.
             */
            ipv6: boolean | null;
        };
        /**
         * The enabled firewall to secure a VPC or public interface. Not allowed for VLAN interfaces. If a `firewall_id` is not provided for a VPC or a public interface, then its [default interface firewall](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-firewalls) is used. If a default firewall is not available, and `null` is not specified, the request fails. Setting the `firewall_id` as `null` indicates that no firewall device will be attached to the interface.
         */
        firewall_id: number | null;
        /**
         * Public interface settings. A Linode can have only one public interface. A public interface can have both IPv4 and IPv6 configurations.
         */
        public: {
            /**
             * IPv4 address settings for this public interface. If omitted, a public IPv4 address is automatically allocated.
             */
            ipv4: {
                /**
                 * List of IPv4 addresses to assign to this interface. Setting any to `auto` allocates a public IPv4 address. To create a public interface that uses IPv6 and no IPv4, set the IPv4 address as an empty list `[]`.
                 */
                addresses: Array<{
                    /**
                     * The interface's public IPv4 address. You can specify which public IPv4 address to configure for the interface. Setting this to `auto` automatically allocates a public address. To create a public interface that uses IPv6 and no IPv4, set the IPv4 address as an empty list `[]`. If the address is a reserved or automatically assigned, it needs to be reserved or already assigned to a single Linode, and it can’t be assigned to any other interface.
                     */
                    address: string | 'auto' | string;
                    /**
                     * The IPv4 primary address configures the source address for routes within the Linode on the corresponding network interface.
                     * - Don't set this to `false` if there's only one address in the `addresses` array.
                     * - If more than one address is provided, primary can be set to `true` for one address.
                     * - If only one address is present in the `addresses` array, this address is automatically set as the primary address.
                     */
                    primary: boolean;
                }>;
            };
            /**
             * IPv6 address settings for the public interface.
             */
            ipv6: {
                /**
                 * IPv6 address ranges to assign to this interface. If omitted, no ranges are assigned.
                 */
                ranges: Array<{
                    /**
                     * Your assigned IPv6 range in CIDR notation (`2001:0db8::1/64`) or prefix (`/64`).
                     * - The prefix of `/64` or `/56` block of IPv6 addresses.
                     * - If provided in CIDR notation, the prefix must be within the assigned ranges for the Linode.
                     */
                    range: string;
                }>;
            };
        } | null;
    } | {
        /**
         * VLAN interface settings. A Linode can have up to three VLAN interfaces, with a unique `vlan_label` for each.
         */
        vlan: {
            /**
             * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
             */
            ipam_address: string;
            /**
             * The VLAN's unique label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
             */
            vlan_label: string;
        } | null;
    } | {
        /**
         * Indicates if the interface is used as the default route when multiple interfaces are eligible for this role. A VPC interface can have an IPv4 `default_route`.
         */
        default_route: {
            /**
             * If set to `true`, the interface is used for the IPv4 `default_route`. Only one interface per Linode can be set as the IPv4 default route.
             */
            ipv4: boolean | null;
        };
        /**
         * The enabled firewall to secure a VPC or public interface. Not allowed for VLAN interfaces. If a `firewall_id` is not provided for a VPC or a public interface, then its default interface firewall is used. If a default firewall is not available, and `null` is not specified, the request fails. Setting the `firewall_id` as `null` indicates that no firewall device will be attached to the interface.
         */
        firewall_id: number | null;
        /**
         * VPC interface settings. A Linode can have one VPC interface. The maximum number of interfaces allowed on a Linode is three.
         */
        vpc: {
            /**
             * Interfaces can be configured with IPv4 addresses or ranges.
             */
            ipv4?: {
                /**
                 * IPv4 address settings for this VPC interface.
                 */
                addresses: Array<{
                    /**
                     * Specifies which IPv4 address to use in the VPC subnet. You can specify which VPC Ipv4 address in the subnet to configure for the interface. You can't use an IPv4 address taken from another Linode or interface, or the first two or last two addresses in the VPC subnet. When `address` is set to `auto`, an IP address from the subnet is automatically assigned.
                     */
                    address: string | 'auto';
                    /**
                     * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                     * - You can set this to a specific public IPv4 address that's available on the Linode.
                     * - If set to `auto`, a public IPv4 address is automatically selected.
                     * - If a public IPv4 address or `auto` is specified, primary must be set to `true`.
                     * - The address can't be used on another Linode.
                     * - If the address is a reserved or an automatically assigned IP, the IP must be reserved or already assigned to a single Linode, and not assigned to an interface.
                     * - If this property is omitted or set to `null`, no 1:1 NAT configuration will be applied to the VPC interface.
                     */
                    nat_1_1_address: string | 'auto' | null;
                    /**
                     * The IPv4 primary address is used to configure the source address for routes within the Linode on the corresponding network interface.
                     * Should not be set to `false` if there is only one address present in the `addresses` array.
                     * If more than one address is provided, primary must be set to `true` for one address.
                     * If only one address is present in the `addresses` array, this address is automatically set as the primary address.
                     */
                    primary: boolean | null;
                }>;
                /**
                 * VPC IPv4 ranges.
                 */
                ranges: Array<{
                    /**
                     * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                     * - When only the prefix is provided, then an available range of that size within the VPC's subnet is automatically selected.
                     * - If specified as CIDR notation, it must belong to the VPC subnet. All addresses in the range must not be taken by any other Linode or interfaces in the VPC subnet and must not include any of the first two or last two addresses of the VPC subnet.
                     */
                    range: string;
                }>;
            };
            /**
             * The VPC subnet identifier for this interface. Your subnet’s VPC must be in the same data center (region) as the Linode.
             */
            subnet_id: number;
        } | null;
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces';
};

export type PostLinodeInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLinodeInterfaceError = PostLinodeInterfaceErrors[keyof PostLinodeInterfaceErrors];

export type PostLinodeInterfaceResponses = {
    /**
     * One of the following interface types: VPC, public, or VLAN.
     */
    200: {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * Indicates if the interface is used as a default route.
         */
        default_route: {
            /**
             * Indicates if the interface is used for the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
             */
            ipv4: boolean;
            /**
             * Indicates if the interface is used for the IPv6 default route. Only one interface per Linode can have the IPv6 default route.
             */
            ipv6: boolean;
        } | null;
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. A public interface's `mac_address` does not change, even if the public interface is deleted and then recreated.
         */
        mac_address: string;
        /**
         * Public interface type.
         */
        public: {
            /**
             * The interface's public IPv4 `addresses`.
             */
            ipv4: {
                /**
                 * The public IPv4 addresses and primary settings for this public interface.
                 */
                addresses: Array<{
                    /**
                     * The public IPv4 address assigned to this interface.
                     */
                    address: string;
                    /**
                     * Indicates if the public IPv4 address serves as the source address for traffic routing within the Linode and other corresponding network interfaces and services.
                     */
                    primary: boolean;
                }>;
                /**
                 * The IPv4 address assigned to this Linode interface, which is also shared with another Linode.
                 */
                shared: Array<{
                    /**
                     * Shared IPv4 address.
                     */
                    address: string;
                    /**
                     * The ID of the Linode this address currently belongs to. For IPv4 addresses, by default this is the Linode this address was assigned when created.
                     */
                    linode_id: string;
                }>;
            };
            /**
             * The interface's public IPv6 configuration.
             */
            ipv6: {
                /**
                 * List of IPv6 ranges assigned to this interface.
                 */
                ranges: Array<{
                    /**
                     * IPv6 range in CIDR notation (`2600:0db8::1/64`) or prefix-only (`/64`).
                     */
                    range: string;
                    /**
                     * The public IPv6 address that the `range` is routed to.
                     */
                    route_target: string;
                }>;
                /**
                 * The IPv6 address assigned to this Linode interface, which is also shared with another Linode.
                 */
                shared: Array<{
                    /**
                     * The IPv6 address range.
                     */
                    range: string;
                    /**
                     * The public IPv6 address that the `range` is routed to.
                     */
                    route_target: string;
                }>;
                /**
                 * The public `slaac` and subnet prefix settings for this public interface that is used to communicate over the public internet, and with other services in the same data center.
                 */
                slaac: Array<{
                    /**
                     * Public IPv6 addresses assigned to this interface.
                     */
                    address: string;
                    /**
                     * The prefix length advertised for SLAAC to use. Only the specific (`/128`) EUI-64 address derived from the interface's MAC address is supported. To ensure the MAC-based EUI-64 address is used, privacy addressing needs to be disabled. Network Helper automatically configures the MAC-derived EUI-64 address. If you disable Network Helper or use an unsupported operating system, follow the specific instructions for your OS.
                     */
                    prefix: number;
                }>;
            };
        };
        /**
         * When the interface was last updated.
         */
        updated: string;
        /**
         * Interface version number that increments when the interface updates.
         */
        version: number;
        /**
         * The value is `null` if this is not a VLAN interface.
         */
        vlan: {
            [key: string]: unknown;
        } | null;
        /**
         * The value is `null` if this is not a VPC interface.
         */
        vpc: {
            [key: string]: never;
        } | null;
    } | {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
         */
        mac_address: string;
        /**
         * The value is `null` if this isn't a public interface.
         */
        public: {
            [key: string]: unknown;
        } | null;
        /**
         * When the interface was last updated.
         */
        updated: string;
        /**
         * Interface version number that is incremented when the interface is updated.
         */
        version: number;
        /**
         * VLAN interface type.
         */
        vlan: {
            /**
             * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
             */
            ipam_address: string | null;
            /**
             * The VLAN's label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
             */
            vlan_label: string;
        };
        /**
         * The value is `null` if this isn't a VPC interface.
         */
        vpc: {
            [key: string]: unknown;
        } | null;
    } | {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * Indicates if the interface serves as a default route.
         */
        default_route: {
            /**
             * Indicates if the interface serves as the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
             */
            ipv4: boolean;
        };
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
         */
        mac_address: string;
        /**
         * The value is `null` if this is not a public interface.
         */
        public: {
            [key: string]: never;
        } | null;
        /**
         * When the interface last updated.
         */
        updated: string;
        /**
         * The version number of the interface configuration, incremented each time the interface is updated.
         */
        version: number;
        /**
         * The value is `null` if this is not a VLAN interface.
         */
        vlan: {
            [key: string]: never;
        } | null;
        /**
         * VPC interface type.
         */
        vpc: {
            /**
             * The interface's IPv4 `addresses` and `ranges` configuration.
             */
            ipv4: {
                /**
                 * IPv4 address settings for this VPC interface.
                 */
                addresses: Array<{
                    /**
                     * The VPC subnet IPv4 address.
                     */
                    address: string;
                    /**
                     * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                     */
                    nat_1_1_address: string | null;
                    /**
                     * Indicates if the IPv4 address is used to set up a source address for routes inside the Linode for the corresponding network interface.
                     */
                    primary: boolean;
                }>;
                /**
                 * VPC IPv4 ranges.
                 */
                ranges: Array<{
                    /**
                     * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                     */
                    range: string;
                }>;
            };
            /**
             * VPC subnet's unique identifier.
             */
            subnet_id: number;
            /**
             * VPC's unique identifier.
             */
            vpc_id: number;
        };
    };
};

export type PostLinodeInterfaceResponse = PostLinodeInterfaceResponses[keyof PostLinodeInterfaceResponses];

export type GetLinodeInterfaceHistoryData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/history';
};

export type GetLinodeInterfaceHistoryErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInterfaceHistoryError = GetLinodeInterfaceHistoryErrors[keyof GetLinodeInterfaceHistoryErrors];

export type GetLinodeInterfaceHistoryResponses = {
    /**
     * Returns a paginated list of network interface versions for a Linode.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this version was created. Use `created` to determine which settings an interface prevously had.
             */
            readonly created: string;
            /**
             * The JSON body returned in response to a successful `PUT`, `POST`, or `DELETE` operation on the interface.
             */
            interface_data: string;
            /**
             * __Filterable__, __Read-only__ The unique ID for this history version.
             */
            readonly interface_history_id: number;
            /**
             * __Filterable__, __Read-only__ The network interface defined in the `version`.
             */
            readonly interface_id: number;
            /**
             * __Filterable__, __Read-only__ The Linode the `interface_id` belongs to.
             */
            readonly linode_id: number;
            /**
             * __Filterable__, __Read-only__ The network interface's version. The first version from a `POST` is `1`. The version number is incremented when the network interface configuration is changed.
             */
            readonly version: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeInterfaceHistoryResponse = GetLinodeInterfaceHistoryResponses[keyof GetLinodeInterfaceHistoryResponses];

export type GetLinodeInterfaceSettingsData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/settings';
};

export type GetLinodeInterfaceSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInterfaceSettingsError = GetLinodeInterfaceSettingsErrors[keyof GetLinodeInterfaceSettingsErrors];

export type GetLinodeInterfaceSettingsResponses = {
    /**
     * Linode interface settings.
     */
    200: {
        /**
         * Interfaces used for the IPv4 `default_route` and IPv6 `default_route` when multiple interfaces are eligible for the role.
         */
        default_route: {
            /**
             * The VPC or public interface IDs that are eligible to be the IPv4 `default_route` for this Linode. If the `ipv4_eligible_interface_ids` array is empty, this means there are no eligible interfaces eligible for the IPv4 default route role.
             */
            ipv4_eligible_interface_ids: Array<number>;
            /**
             * The VPC or public interface ID assigned as the IPv4 `default_route`. The [List Linode interface settings](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-settings) operation provides eligible IPv4 interface IDs.
             */
            ipv4_interface_id: number | null;
            /**
             * The public interface IDs that are eligible to be the IPv6 `default_route` for this Linode. If the `ipv6_eligible_interface_ids` array is empty, this means there are no eligible interfaces eligible for the IPv6 default route role.
             */
            ipv6_eligible_interface_ids: Array<number>;
            /**
             * The public interface ID assigned as the IPv6 `default_route`. The [List Linode interface settings](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-settings) operation provides eligible IPv6 interface IDs.
             */
            ipv6_interface_id: number | null;
        };
        /**
         * Enables the Network Helper feature. The default value is determined by the `network_helper` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). [Power off the Linode](https://techdocs.akamai.com/linode-api/reference/post-shutdown-linode-instance) before disabling or enabling Network Helper.
         */
        network_helper: boolean;
    };
};

export type GetLinodeInterfaceSettingsResponse = GetLinodeInterfaceSettingsResponses[keyof GetLinodeInterfaceSettingsResponses];

export type PutLinodeInterfaceSettingsData = {
    /**
     * Linode interface settings.
     */
    body: {
        /**
         * Interfaces used for the IPv4 `default_route` and IPv6 `default_route` when multiple interfaces are eligible for the role.
         */
        default_route: {
            /**
             * The VPC or public interface IDs that are eligible to be the IPv4 `default_route` for this Linode. If the `ipv4_eligible_interface_ids` array is empty, this means there are no eligible interfaces eligible for the IPv4 default route role.
             */
            ipv4_eligible_interface_ids: Array<number>;
            /**
             * The VPC or public interface ID assigned as the IPv4 `default_route`. The [List Linode interface settings](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-settings) operation provides eligible IPv4 interface IDs.
             */
            ipv4_interface_id: number | null;
            /**
             * The public interface IDs that are eligible to be the IPv6 `default_route` for this Linode. If the `ipv6_eligible_interface_ids` array is empty, this means there are no eligible interfaces eligible for the IPv6 default route role.
             */
            ipv6_eligible_interface_ids: Array<number>;
            /**
             * The public interface ID assigned as the IPv6 `default_route`. The [List Linode interface settings](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-settings) operation provides eligible IPv6 interface IDs.
             */
            ipv6_interface_id: number | null;
        };
        /**
         * Enables the Network Helper feature. The default value is determined by the `network_helper` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). [Power off the Linode](https://techdocs.akamai.com/linode-api/reference/post-shutdown-linode-instance) before disabling or enabling Network Helper.
         */
        network_helper: boolean;
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/settings';
};

export type PutLinodeInterfaceSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeInterfaceSettingsError = PutLinodeInterfaceSettingsErrors[keyof PutLinodeInterfaceSettingsErrors];

export type PutLinodeInterfaceSettingsResponses = {
    /**
     * Linode interface settings.
     */
    200: {
        /**
         * Interfaces used for the IPv4 `default_route` and IPv6 `default_route` when multiple interfaces are eligible for the role.
         */
        default_route: {
            /**
             * The VPC or public interface IDs that are eligible to be the IPv4 `default_route` for this Linode. If the `ipv4_eligible_interface_ids` array is empty, this means there are no eligible interfaces eligible for the IPv4 default route role.
             */
            ipv4_eligible_interface_ids: Array<number>;
            /**
             * The VPC or public interface ID assigned as the IPv4 `default_route`. The [List Linode interface settings](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-settings) operation provides eligible IPv4 interface IDs.
             */
            ipv4_interface_id: number | null;
            /**
             * The public interface IDs that are eligible to be the IPv6 `default_route` for this Linode. If the `ipv6_eligible_interface_ids` array is empty, this means there are no eligible interfaces eligible for the IPv6 default route role.
             */
            ipv6_eligible_interface_ids: Array<number>;
            /**
             * The public interface ID assigned as the IPv6 `default_route`. The [List Linode interface settings](https://techdocs.akamai.com/linode-api/reference/get-linode-interface-settings) operation provides eligible IPv6 interface IDs.
             */
            ipv6_interface_id: number | null;
        };
        /**
         * Enables the Network Helper feature. The default value is determined by the `network_helper` setting in the [account settings](https://techdocs.akamai.com/linode-api/reference/get-account-settings). [Power off the Linode](https://techdocs.akamai.com/linode-api/reference/post-shutdown-linode-instance) before disabling or enabling Network Helper.
         */
        network_helper: boolean;
    };
};

export type PutLinodeInterfaceSettingsResponse = PutLinodeInterfaceSettingsResponses[keyof PutLinodeInterfaceSettingsResponses];

export type DeleteLinodeInterfaceData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Linode interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/{interfaceId}';
};

export type DeleteLinodeInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLinodeInterfaceError = DeleteLinodeInterfaceErrors[keyof DeleteLinodeInterfaceErrors];

export type DeleteLinodeInterfaceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLinodeInterfaceResponse = DeleteLinodeInterfaceResponses[keyof DeleteLinodeInterfaceResponses];

export type GetLinodeInterfaceData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Linode interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/{interfaceId}';
};

export type GetLinodeInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInterfaceError = GetLinodeInterfaceErrors[keyof GetLinodeInterfaceErrors];

export type GetLinodeInterfaceResponses = {
    /**
     * One of the following interface types: VPC, public, or VLAN.
     */
    200: {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * Indicates if the interface is used as a default route.
         */
        default_route: {
            /**
             * Indicates if the interface is used for the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
             */
            ipv4: boolean;
            /**
             * Indicates if the interface is used for the IPv6 default route. Only one interface per Linode can have the IPv6 default route.
             */
            ipv6: boolean;
        } | null;
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. A public interface's `mac_address` does not change, even if the public interface is deleted and then recreated.
         */
        mac_address: string;
        /**
         * Public interface type.
         */
        public: {
            /**
             * The interface's public IPv4 `addresses`.
             */
            ipv4: {
                /**
                 * The public IPv4 addresses and primary settings for this public interface.
                 */
                addresses: Array<{
                    /**
                     * The public IPv4 address assigned to this interface.
                     */
                    address: string;
                    /**
                     * Indicates if the public IPv4 address serves as the source address for traffic routing within the Linode and other corresponding network interfaces and services.
                     */
                    primary: boolean;
                }>;
                /**
                 * The IPv4 address assigned to this Linode interface, which is also shared with another Linode.
                 */
                shared: Array<{
                    /**
                     * Shared IPv4 address.
                     */
                    address: string;
                    /**
                     * The ID of the Linode this address currently belongs to. For IPv4 addresses, by default this is the Linode this address was assigned when created.
                     */
                    linode_id: string;
                }>;
            };
            /**
             * The interface's public IPv6 configuration.
             */
            ipv6: {
                /**
                 * List of IPv6 ranges assigned to this interface.
                 */
                ranges: Array<{
                    /**
                     * IPv6 range in CIDR notation (`2600:0db8::1/64`) or prefix-only (`/64`).
                     */
                    range: string;
                    /**
                     * The public IPv6 address that the `range` is routed to.
                     */
                    route_target: string;
                }>;
                /**
                 * The IPv6 address assigned to this Linode interface, which is also shared with another Linode.
                 */
                shared: Array<{
                    /**
                     * The IPv6 address range.
                     */
                    range: string;
                    /**
                     * The public IPv6 address that the `range` is routed to.
                     */
                    route_target: string;
                }>;
                /**
                 * The public `slaac` and subnet prefix settings for this public interface that is used to communicate over the public internet, and with other services in the same data center.
                 */
                slaac: Array<{
                    /**
                     * Public IPv6 addresses assigned to this interface.
                     */
                    address: string;
                    /**
                     * The prefix length advertised for SLAAC to use. Only the specific (`/128`) EUI-64 address derived from the interface's MAC address is supported. To ensure the MAC-based EUI-64 address is used, privacy addressing needs to be disabled. Network Helper automatically configures the MAC-derived EUI-64 address. If you disable Network Helper or use an unsupported operating system, follow the specific instructions for your OS.
                     */
                    prefix: number;
                }>;
            };
        };
        /**
         * When the interface was last updated.
         */
        updated: string;
        /**
         * Interface version number that increments when the interface updates.
         */
        version: number;
        /**
         * The value is `null` if this is not a VLAN interface.
         */
        vlan: {
            [key: string]: unknown;
        } | null;
        /**
         * The value is `null` if this is not a VPC interface.
         */
        vpc: {
            [key: string]: never;
        } | null;
    } | {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
         */
        mac_address: string;
        /**
         * The value is `null` if this isn't a public interface.
         */
        public: {
            [key: string]: unknown;
        } | null;
        /**
         * When the interface was last updated.
         */
        updated: string;
        /**
         * Interface version number that is incremented when the interface is updated.
         */
        version: number;
        /**
         * VLAN interface type.
         */
        vlan: {
            /**
             * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
             */
            ipam_address: string | null;
            /**
             * The VLAN's label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
             */
            vlan_label: string;
        };
        /**
         * The value is `null` if this isn't a VPC interface.
         */
        vpc: {
            [key: string]: unknown;
        } | null;
    } | {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * Indicates if the interface serves as a default route.
         */
        default_route: {
            /**
             * Indicates if the interface serves as the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
             */
            ipv4: boolean;
        };
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
         */
        mac_address: string;
        /**
         * The value is `null` if this is not a public interface.
         */
        public: {
            [key: string]: never;
        } | null;
        /**
         * When the interface last updated.
         */
        updated: string;
        /**
         * The version number of the interface configuration, incremented each time the interface is updated.
         */
        version: number;
        /**
         * The value is `null` if this is not a VLAN interface.
         */
        vlan: {
            [key: string]: never;
        } | null;
        /**
         * VPC interface type.
         */
        vpc: {
            /**
             * The interface's IPv4 `addresses` and `ranges` configuration.
             */
            ipv4: {
                /**
                 * IPv4 address settings for this VPC interface.
                 */
                addresses: Array<{
                    /**
                     * The VPC subnet IPv4 address.
                     */
                    address: string;
                    /**
                     * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                     */
                    nat_1_1_address: string | null;
                    /**
                     * Indicates if the IPv4 address is used to set up a source address for routes inside the Linode for the corresponding network interface.
                     */
                    primary: boolean;
                }>;
                /**
                 * VPC IPv4 ranges.
                 */
                ranges: Array<{
                    /**
                     * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                     */
                    range: string;
                }>;
            };
            /**
             * VPC subnet's unique identifier.
             */
            subnet_id: number;
            /**
             * VPC's unique identifier.
             */
            vpc_id: number;
        };
    };
};

export type GetLinodeInterfaceResponse = GetLinodeInterfaceResponses[keyof GetLinodeInterfaceResponses];

export type PutLinodeInterfaceData = {
    /**
     * Modifies an existing Linode interface. The interface type (`vpc`, `public`, `vlan`) can't be changed.
     */
    body: {
        /**
         * Defines whether IPv4 and IPv6 default routes are enabled for this interface. Public interfaces can have both an IPv4  and IPv6 `default_route`, provided they have at least one IP address of the corresponding type. If the `default_route` is omitted, (or set to `null`), no changes are made to the `default_route` configuration.
         */
        default_route: {
            /**
             * If set to `true`, and if the interface has at least one IPv4 address after the PUT request, the `default_route` role is assigned to the interface. If another interface is already configured as the default route, this setting removes that configuration. If the interface has an IPv4 address, the value cannot be set as `false`.
             */
            ipv4: boolean | null;
            /**
             * If set to `true`, and if the interface has at least one IPv6 address after the PUT request, the `default_route` role is assigned to the interface. If another interface is already configured as the default route, this setting removes that configuration. If the interface has an IPv6 address, the value cannot be set as `false`.
             */
            ipv6: boolean | null;
        };
        /**
         * Public interface settings.
         */
        public: {
            /**
             * IPv4 address settings for this public interface.
             */
            ipv4: {
                /**
                 * List of IPv4 addresses to assign to this interface. When updating the addresses, all addresses must be explicitly listed in the array, and any addresses not included in the update are removed. If omitted or set to `null`, no changes are made to the existing IPv4 configuration.
                 */
                addresses: Array<{
                    /**
                     * The public IPv4 address configuration for the interface. If set to `auto`, a public IPv4 address is allocated. If the address is a reserved or an automatically assigned public IP, the IP must be reserved or already assigned to a single Linode, and it can’t be already assigned to an interface.
                     */
                    address: string;
                    /**
                     * The IPv4 primary address for the interface that is used to set up a source address for routes inside the Linode for the corresponding network interface.
                     * Automatically set to `true` and should not be set to `false` if there is only one address present in the addresses array.
                     * If more than one address is provided, primary must be set to `true` for one address.
                     */
                    primary: boolean | null;
                }>;
            };
            /**
             * IPv6 address settings for the public interface.
             */
            ipv6: {
                /**
                 * List of IPv6 address ranges to assign to this interface. If range is omitted, or set to `null`, no changes are made to the IPv6 ranges of the interface. If range is updated, specify all ranges in this array, otherwise, the currently assigned ranges will be removed.
                 */
                ranges: Array<{
                    /**
                     * IPv6 range in CIDR notation (`2001:0db8::1/64`) or prefix-only (`/64`).
                     * - The prefix of /64 or /56 block of IPv6 addresses.
                     * - CIDR notation ranges must be within your assigned ranges.
                     */
                    range: string;
                }>;
            };
        } | null;
    } | {
        /**
         * VLAN interface settings.
         */
        vlan: {
            /**
             * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation. The `ipam_address` can't be updated. It needs to be the existing value, or `null`.
             */
            ipam_address: string | null;
            /**
             * The VLAN's label. Once you specify it, you can't update it.
             */
            vlan_label: string;
        };
    } | {
        /**
         * Indicates if the interface is used as the default route. A VPC interface can have an IPv4 `default_route`.
         */
        default_route: {
            /**
             * If set to `true`, and if the interface has at least one IPv4 address after the `PUT` request, the `default_route` role is assigned to the interface. If another interface is already configured as the default route, this setting removes that configuration. If the interface has an IPv4 address, the value can't be set to `false`. If this is omitted, or set to `null`, no changes are made to the interface's IPv4 configuration.
             */
            ipv4: boolean | null;
        };
        /**
         * VPC interface settings.
         */
        vpc: {
            /**
             * Interfaces can be configured with IPv4 `addresses` or `ranges`. If this is omitted, or set to `null`, no changes are made to the IPv4 configuration.
             */
            ipv4?: {
                /**
                 * IPv4 configuration for this VPC interface. When updating the addresses, all addresses must be explicitly listed in the array, and any addresses not included in the update are removed. If omitted or set to `null`, no changes are made to the existing IPv4 configuration. However, if the `subnet_id` is updated, new default address values are applied.
                 */
                addresses: Array<{
                    /**
                     * Specifies which IPv4 addresses to use in the VPC subnet. You can't use an IPv4 address taken from another Linode or interface, or the first two or last two addresses in the VPC subnet. Setting `address` to `auto` automatically assigns an IP address from the subnet.
                     */
                    address: string;
                    /**
                     * The 1:1 NAT IPv4 address that links a public IPv4 address with the interface's VPC subnet IPv4 address.
                     *
                     * - You can set this to a specific public IPv4 address that's available on the Linode.
                     *
                     * - If set to `auto`, a public IPv4 address is automatically selected.
                     *
                     * - If a public IPv4 address or `auto` is specified, `primary` must be set to `true`.
                     *
                     * - A specified address can't be used on another Linode.
                     *
                     * - If the address is a reserved or an automatically assigned public IP, the IP must be reserved or already assigned to a single Linode, and not assigned to an interface.
                     *
                     * - Omitting this or setting it to `null` results in no 1:1 NAT configuration for the VPC interface.
                     */
                    nat_1_1_address?: string;
                    /**
                     * The IPv4 primary address for the interface that sets up a source address for routes inside the Linode for the corresponding network interface.
                     * Automatically sets to `true` and should not be set to `false` if there is only one address present in the `addresses` array.
                     * If more than one `address` is provided, `primary` must be set to `true` for one address.
                     */
                    primary?: boolean | null;
                }>;
                /**
                 * VPC IPv4 ranges. If you omit `ranges` or set it to `null`, the interface's IPv4 ranges remain unchanged. But if you update `ranges`, you need to specify all of them, otherwise currently assigned ranges are removed.
                 */
                ranges: Array<{
                    /**
                     * CIDR notation of a range (`1.2.3.4/28`) or prefix only (`/28`):
                     * - When only the prefix is provided, then an available range of that size within the VPC's subnet is automatically selected.
                     * - If specified as CIDR notation, it must belong to the VPC subnet. All addresses in the range must not be taken by any other Linode or interfaces in the VPC subnet and must not include any of the first two or last two addresses of the VPC subnet.
                     */
                    range: string | null;
                }>;
            };
            /**
             * The VPC subnet identifier for this interface. Your subnet’s VPC must be in the same data center (region) as the Linode. If the subnet is updated, the IP configuration should also be updated. If the subnet is updated without updating the IP configuration, defaults are applied.
             */
            subnet_id: number;
        } | null;
    };
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Linode interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/{interfaceId}';
};

export type PutLinodeInterfaceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeInterfaceError = PutLinodeInterfaceErrors[keyof PutLinodeInterfaceErrors];

export type PutLinodeInterfaceResponses = {
    /**
     * One of the following interface types: VPC, public, or VLAN.
     */
    200: {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * Indicates if the interface is used as a default route.
         */
        default_route: {
            /**
             * Indicates if the interface is used for the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
             */
            ipv4: boolean;
            /**
             * Indicates if the interface is used for the IPv6 default route. Only one interface per Linode can have the IPv6 default route.
             */
            ipv6: boolean;
        } | null;
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. A public interface's `mac_address` does not change, even if the public interface is deleted and then recreated.
         */
        mac_address: string;
        /**
         * Public interface type.
         */
        public: {
            /**
             * The interface's public IPv4 `addresses`.
             */
            ipv4: {
                /**
                 * The public IPv4 addresses and primary settings for this public interface.
                 */
                addresses: Array<{
                    /**
                     * The public IPv4 address assigned to this interface.
                     */
                    address: string;
                    /**
                     * Indicates if the public IPv4 address serves as the source address for traffic routing within the Linode and other corresponding network interfaces and services.
                     */
                    primary: boolean;
                }>;
                /**
                 * The IPv4 address assigned to this Linode interface, which is also shared with another Linode.
                 */
                shared: Array<{
                    /**
                     * Shared IPv4 address.
                     */
                    address: string;
                    /**
                     * The ID of the Linode this address currently belongs to. For IPv4 addresses, by default this is the Linode this address was assigned when created.
                     */
                    linode_id: string;
                }>;
            };
            /**
             * The interface's public IPv6 configuration.
             */
            ipv6: {
                /**
                 * List of IPv6 ranges assigned to this interface.
                 */
                ranges: Array<{
                    /**
                     * IPv6 range in CIDR notation (`2600:0db8::1/64`) or prefix-only (`/64`).
                     */
                    range: string;
                    /**
                     * The public IPv6 address that the `range` is routed to.
                     */
                    route_target: string;
                }>;
                /**
                 * The IPv6 address assigned to this Linode interface, which is also shared with another Linode.
                 */
                shared: Array<{
                    /**
                     * The IPv6 address range.
                     */
                    range: string;
                    /**
                     * The public IPv6 address that the `range` is routed to.
                     */
                    route_target: string;
                }>;
                /**
                 * The public `slaac` and subnet prefix settings for this public interface that is used to communicate over the public internet, and with other services in the same data center.
                 */
                slaac: Array<{
                    /**
                     * Public IPv6 addresses assigned to this interface.
                     */
                    address: string;
                    /**
                     * The prefix length advertised for SLAAC to use. Only the specific (`/128`) EUI-64 address derived from the interface's MAC address is supported. To ensure the MAC-based EUI-64 address is used, privacy addressing needs to be disabled. Network Helper automatically configures the MAC-derived EUI-64 address. If you disable Network Helper or use an unsupported operating system, follow the specific instructions for your OS.
                     */
                    prefix: number;
                }>;
            };
        };
        /**
         * When the interface was last updated.
         */
        updated: string;
        /**
         * Interface version number that increments when the interface updates.
         */
        version: number;
        /**
         * The value is `null` if this is not a VLAN interface.
         */
        vlan: {
            [key: string]: unknown;
        } | null;
        /**
         * The value is `null` if this is not a VPC interface.
         */
        vpc: {
            [key: string]: never;
        } | null;
    } | {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
         */
        mac_address: string;
        /**
         * The value is `null` if this isn't a public interface.
         */
        public: {
            [key: string]: unknown;
        } | null;
        /**
         * When the interface was last updated.
         */
        updated: string;
        /**
         * Interface version number that is incremented when the interface is updated.
         */
        version: number;
        /**
         * VLAN interface type.
         */
        vlan: {
            /**
             * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
             */
            ipam_address: string | null;
            /**
             * The VLAN's label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
             */
            vlan_label: string;
        };
        /**
         * The value is `null` if this isn't a VPC interface.
         */
        vpc: {
            [key: string]: unknown;
        } | null;
    } | {
        /**
         * When the interface was created.
         */
        created: string;
        /**
         * Indicates if the interface serves as a default route.
         */
        default_route: {
            /**
             * Indicates if the interface serves as the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
             */
            ipv4: boolean;
        };
        /**
         * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
         */
        readonly id: number;
        /**
         * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
         */
        mac_address: string;
        /**
         * The value is `null` if this is not a public interface.
         */
        public: {
            [key: string]: never;
        } | null;
        /**
         * When the interface last updated.
         */
        updated: string;
        /**
         * The version number of the interface configuration, incremented each time the interface is updated.
         */
        version: number;
        /**
         * The value is `null` if this is not a VLAN interface.
         */
        vlan: {
            [key: string]: never;
        } | null;
        /**
         * VPC interface type.
         */
        vpc: {
            /**
             * The interface's IPv4 `addresses` and `ranges` configuration.
             */
            ipv4: {
                /**
                 * IPv4 address settings for this VPC interface.
                 */
                addresses: Array<{
                    /**
                     * The VPC subnet IPv4 address.
                     */
                    address: string;
                    /**
                     * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                     */
                    nat_1_1_address: string | null;
                    /**
                     * Indicates if the IPv4 address is used to set up a source address for routes inside the Linode for the corresponding network interface.
                     */
                    primary: boolean;
                }>;
                /**
                 * VPC IPv4 ranges.
                 */
                ranges: Array<{
                    /**
                     * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                     */
                    range: string;
                }>;
            };
            /**
             * VPC subnet's unique identifier.
             */
            subnet_id: number;
            /**
             * VPC's unique identifier.
             */
            vpc_id: number;
        };
    };
};

export type PutLinodeInterfaceResponse = PutLinodeInterfaceResponses[keyof PutLinodeInterfaceResponses];

export type GetLinodeInterfaceFirewallsData = {
    body?: never;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
        /**
         * The `id` of the Linode interface.
         */
        interfaceId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/interfaces/{interfaceId}/firewalls';
};

export type GetLinodeInterfaceFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeInterfaceFirewallsError = GetLinodeInterfaceFirewallsErrors[keyof GetLinodeInterfaceFirewallsErrors];

export type GetLinodeInterfaceFirewallsResponses = {
    /**
     * Returns a paginated list of firewalls assigned to an interface.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this Firewall was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
             */
            readonly entities: Array<{
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a `linode`.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'linode_interface' | 'nodebalancer';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            }>;
            /**
             * __Filterable__, __Read-only__ The Firewall's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The Firewall's label, for display purposes only.
             *
             * Firewall labels have the following constraints:
             *
             * - Must begin and end with an alphanumeric character.
             * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             * - Must be between 3 and 32 characters.
             * - Must be unique.
             */
            label: string;
            /**
             * The inbound and outbound access rules to apply to the Firewall.
             *
             * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
             *
             * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
                 */
                readonly fingerprint: string;
                /**
                 * The inbound rules for the firewall, as a JSON array.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall, as a JSON array.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
                 */
                readonly version: number;
            };
            /**
             * __Read-only__ The status of this Firewall.
             *
             * - When a Firewall is first created its status is `enabled`.
             * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
             * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
             */
            status: 'enabled' | 'disabled' | 'deleted';
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Filterable__, __Read-only__ When this Firewall was last updated.
             */
            readonly updated: string;
        }>;
    };
};

export type GetLinodeInterfaceFirewallsResponse = GetLinodeInterfaceFirewallsResponses[keyof GetLinodeInterfaceFirewallsResponses];

export type GetLinodeIpsData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/ips';
};

export type GetLinodeIpsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeIpsError = GetLinodeIpsErrors[keyof GetLinodeIpsErrors];

export type GetLinodeIpsResponses = {
    /**
     * Requested Linode's networking configuration.
     */
    200: {
        /**
         * __Read-only__ Information about this Linode's IPv4 addresses.
         */
        readonly ipv4: {
            /**
             * __Read-only__ A list of private IP Address objects belonging to this Linode.
             */
            readonly private: Array<{
                /**
                 * __Read-only__ The private IPv4 address.
                 */
                readonly address: string;
                /**
                 * __Read-only__ The default gateway for this address.
                 */
                readonly gateway: string | null;
                /**
                 * __Read-only__ The ID of the Linode this address currently belongs to.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The number of bits set in the subnet mask.
                 */
                readonly prefix: number;
                /**
                 * __Read-only__ Whether this is a public or private IP address.
                 */
                readonly public: boolean;
                /**
                 * The reverse DNS assigned to this address.
                 */
                rdns: string | null;
                /**
                 * __Read-only__ The Region this address resides in.
                 */
                readonly region: string;
                /**
                 * __Read-only__ The mask that separates host bits from network bits for this address.
                 */
                readonly subnet_mask: string;
                /**
                 * __Read-only__ The type of address this is.
                 */
                readonly type: string;
            }>;
            /**
             * __Read-only__ A list of public IP Address objects belonging to this Linode.
             */
            readonly public: Array<{
                /**
                 * __Read-only__ The IP address.
                 */
                readonly address: string;
                /**
                 * __Read-only__ The default gateway for this address.
                 */
                readonly gateway: string | null;
                /**
                 * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
                 */
                readonly interface_id: number | null;
                /**
                 * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The number of bits set in the subnet mask.
                 */
                readonly prefix: number;
                /**
                 * __Read-only__ Whether this is a public or private IP address.
                 */
                readonly public: boolean;
                /**
                 * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
                 */
                rdns: string | null;
                /**
                 * __Read-only__ The Region this IP address resides in.
                 */
                readonly region: string;
                /**
                 * __Read-only__ The mask that separates host bits from network bits for this address.
                 */
                readonly subnet_mask: string;
                /**
                 * __Read-only__ The type of address this is.
                 */
                type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
                /**
                 * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
                 *
                 * > 📘
                 * >
                 * > Only allowed for `vpc` type interfaces.
                 */
                vpc_nat_1_1: {
                    /**
                     * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                     */
                    address: string;
                    /**
                     * The `id` of the VPC Subnet for this interface.
                     */
                    subnet_id: number;
                    /**
                     * __Read-only__ The `id` of the VPC configured for this interface.
                     */
                    readonly vpc_id: number;
                };
            }>;
            /**
             * __Read-only__ A list of reserved IP Address objects belonging to this Linode.
             */
            readonly reserved: Array<{
                /**
                 * __Read-only__ The IP address.
                 */
                readonly address: string;
                /**
                 * __Read-only__ The default gateway for this address.
                 */
                readonly gateway: string | null;
                /**
                 * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
                 */
                readonly interface_id: number | null;
                /**
                 * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The number of bits set in the subnet mask.
                 */
                readonly prefix: number;
                /**
                 * __Read-only__ Whether this is a public or private IP address.
                 */
                readonly public: boolean;
                /**
                 * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
                 */
                rdns: string | null;
                /**
                 * __Read-only__ The Region this IP address resides in.
                 */
                readonly region: string;
                /**
                 * __Read-only__ The mask that separates host bits from network bits for this address.
                 */
                readonly subnet_mask: string;
                /**
                 * __Read-only__ The type of address this is.
                 */
                type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
                /**
                 * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
                 *
                 * > 📘
                 * >
                 * > Only allowed for `vpc` type interfaces.
                 */
                vpc_nat_1_1: {
                    /**
                     * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                     */
                    address: string;
                    /**
                     * The `id` of the VPC Subnet for this interface.
                     */
                    subnet_id: number;
                    /**
                     * __Read-only__ The `id` of the VPC configured for this interface.
                     */
                    readonly vpc_id: number;
                };
            }>;
            /**
             * __Read-only__ A list of shared IP Address objects assigned to this Linode.
             */
            readonly shared: Array<{
                /**
                 * __Read-only__ The IP address.
                 */
                readonly address: string;
                /**
                 * __Read-only__ The default gateway for this address.
                 */
                readonly gateway: string | null;
                /**
                 * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
                 */
                readonly interface_id: number | null;
                /**
                 * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The number of bits set in the subnet mask.
                 */
                readonly prefix: number;
                /**
                 * __Read-only__ Whether this is a public or private IP address.
                 */
                readonly public: boolean;
                /**
                 * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
                 */
                rdns: string | null;
                /**
                 * __Read-only__ The Region this IP address resides in.
                 */
                readonly region: string;
                /**
                 * __Read-only__ The mask that separates host bits from network bits for this address.
                 */
                readonly subnet_mask: string;
                /**
                 * __Read-only__ The type of address this is.
                 */
                type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
                /**
                 * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
                 *
                 * > 📘
                 * >
                 * > Only allowed for `vpc` type interfaces.
                 */
                vpc_nat_1_1: {
                    /**
                     * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                     */
                    address: string;
                    /**
                     * The `id` of the VPC Subnet for this interface.
                     */
                    subnet_id: number;
                    /**
                     * __Read-only__ The `id` of the VPC configured for this interface.
                     */
                    readonly vpc_id: number;
                };
            }>;
            /**
             * __Read-only__ A list of Virtual Private Cloud (VPC)-specific addresses or ranges for the Linode.
             */
            readonly vpc: Array<{
                /**
                 * __Filterable__, __Read-only__ Returns `true` if the VPC interface is in use, meaning that the Linode was powered on using the `config_id` to which the interface belongs. Otherwise returns `false`.
                 */
                readonly active: boolean;
                /**
                 * __Read-only__ An IPv4 address configured for this VPC interface. These follow the [RFC 1918](https://datatracker.ietf.org/doc/html/rfc1918) private address format. Displayed as `null` if an `address_range`.
                 */
                readonly address: string | null;
                /**
                 * __Read-only__ A range of IPv4 addresses configured for this VPC interface. Displayed as `null` if a single `address`.
                 */
                readonly address_range: string | null;
                /**
                 * __Filterable__, __Read-only__ The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                 */
                readonly config_id: number | null;
                /**
                 * __Read-only__ The default gateway for the VPC subnet that the IP or IP range belongs to.
                 */
                readonly gateway: string | null;
                /**
                 * __Beta__, __Read-only__ The globally general API entity identifier for the Linode interface.
                 */
                readonly interface_id: number;
                /**
                 * __Filterable__, __Read-only__ The identifier for the Linode the VPC interface currently belongs to.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The public IP address used for NAT 1:1 with the VPC. This is empty if NAT 1:1 isn't used.
                 */
                readonly nat_1_1: string;
                /**
                 * __Read-only__ The number of bits set in the `subnet_mask`.
                 */
                readonly prefix: number | null;
                /**
                 * __Filterable__, __Read-only__ The region of the VPC.
                 */
                readonly region: string;
                /**
                 * The `id` of the VPC Subnet for this interface.
                 */
                subnet_id: number;
                /**
                 * __Read-only__ The mask that separates host bits from network bits for the `address` or `address_range`.
                 */
                readonly subnet_mask: string;
                /**
                 * __Filterable__, __Read-only__ The unique globally general API entity identifier for the VPC.
                 */
                readonly vpc_id: number;
            }>;
        };
        /**
         * __Read-only__ Information about this Linode's IPv6 addresses.
         */
        readonly ipv6: {
            /**
             * A list of IPv6 range objects assigned to this Linode.
             */
            global: Array<{
                /**
                 * The prefix length of the address. The total number of addresses that can be assigned from this range is calculated as 2<sup>(128 - prefix length)</sup>.
                 */
                prefix: number;
                /**
                 * __Read-only__ The IPv6 address of this range.
                 */
                readonly range: string;
                /**
                 * __Read-only__ The region for this range of IPv6 addresses.
                 */
                readonly region: string;
                /**
                 * The IPv6 SLAAC address.
                 */
                route_target: string;
            }>;
            /**
             * A link-local IPv6 address that exists in Linode's system.
             */
            link_local: {
                /**
                 * __Read-only__ The IPv6 link-local address.
                 */
                readonly address: string;
                /**
                 * __Read-only__ The default gateway for this address.
                 */
                readonly gateway: string;
                /**
                 * __Read-only__ The ID of the Linode this address currently belongs to.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The network prefix.
                 */
                readonly prefix: number;
                /**
                 * __Read-only__ Whether this is a public or private IP address.
                 */
                readonly public: boolean;
                /**
                 * The reverse DNS assigned to this address.
                 */
                rdns: string | null;
                /**
                 * __Filterable__, __Read-only__ The Region this address resides in.
                 */
                readonly region: string;
                /**
                 * __Read-only__ The subnet mask.
                 */
                readonly subnet_mask: string;
                /**
                 * __Read-only__ The type of address this is.
                 */
                readonly type: string;
            };
            /**
             * A SLAAC IPv6 address that exists in Linode's system.
             */
            slaac: {
                /**
                 * __Read-only__ The address.
                 */
                readonly address: string;
                /**
                 * __Read-only__ The default gateway for this address.
                 */
                readonly gateway: string;
                /**
                 * __Read-only__ The ID of the Linode this address currently belongs to.
                 */
                readonly linode_id: number;
                /**
                 * __Read-only__ The network prefix.
                 */
                readonly prefix: number;
                /**
                 * __Read-only__ Whether this is a public or private IP address.
                 */
                readonly public: boolean;
                /**
                 * The reverse DNS assigned to this address.
                 */
                rdns: string | null;
                /**
                 * __Filterable__, __Read-only__ The Region this address resides in.
                 */
                readonly region: string;
                /**
                 * __Read-only__ The subnet mask.
                 */
                readonly subnet_mask: string;
                /**
                 * __Read-only__ The type of address this is.
                 */
                readonly type: string;
            };
        };
    };
};

export type GetLinodeIpsResponse = GetLinodeIpsResponses[keyof GetLinodeIpsResponses];

export type PostAddLinodeIpData = {
    /**
     * Information about the address you are creating.
     */
    body: {
        /**
         * Whether to create a public or private IPv4 address.
         */
        public: boolean;
        /**
         * The type of address you are allocating. Only IPv4 addresses may be allocated through this operation.
         */
        type: 'ipv4';
    };
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/ips';
};

export type PostAddLinodeIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAddLinodeIpError = PostAddLinodeIpErrors[keyof PostAddLinodeIpErrors];

export type PostAddLinodeIpResponses = {
    /**
     * An IP address that exists in Linode's system, either IPv4 or IPv6.
     */
    200: {
        /**
         * __Read-only__ The IP address.
         */
        readonly address: string;
        /**
         * __Read-only__ The default gateway for this address.
         */
        readonly gateway: string | null;
        /**
         * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
         */
        readonly interface_id: number | null;
        /**
         * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
         */
        readonly linode_id: number;
        /**
         * __Read-only__ The number of bits set in the subnet mask.
         */
        readonly prefix: number;
        /**
         * __Read-only__ Whether this is a public or private IP address.
         */
        readonly public: boolean;
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
        /**
         * __Read-only__ The Region this IP address resides in.
         */
        readonly region: string;
        /**
         * __Read-only__ The mask that separates host bits from network bits for this address.
         */
        readonly subnet_mask: string;
        /**
         * __Read-only__ The type of address this is.
         */
        type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
        /**
         * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
         *
         * > 📘
         * >
         * > Only allowed for `vpc` type interfaces.
         */
        vpc_nat_1_1: {
            /**
             * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
             */
            address: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface.
             */
            readonly vpc_id: number;
        };
    };
};

export type PostAddLinodeIpResponse = PostAddLinodeIpResponses[keyof PostAddLinodeIpResponses];

export type DeleteLinodeIpData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode.
         */
        linodeId: number;
        /**
         * The IP address.
         */
        address: string;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/ips/{address}';
};

export type DeleteLinodeIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLinodeIpError = DeleteLinodeIpErrors[keyof DeleteLinodeIpErrors];

export type DeleteLinodeIpResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLinodeIpResponse = DeleteLinodeIpResponses[keyof DeleteLinodeIpResponses];

export type GetLinodeIpData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode.
         */
        linodeId: number;
        /**
         * The IP address.
         */
        address: string;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/ips/{address}';
};

export type GetLinodeIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeIpError = GetLinodeIpErrors[keyof GetLinodeIpErrors];

export type GetLinodeIpResponses = {
    /**
     * An IP address that exists in Linode's system, either IPv4 or IPv6.
     */
    200: {
        /**
         * __Read-only__ The IP address.
         */
        readonly address: string;
        /**
         * __Read-only__ The default gateway for this address.
         */
        readonly gateway: string | null;
        /**
         * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
         */
        readonly interface_id: number | null;
        /**
         * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
         */
        readonly linode_id: number;
        /**
         * __Read-only__ The number of bits set in the subnet mask.
         */
        readonly prefix: number;
        /**
         * __Read-only__ Whether this is a public or private IP address.
         */
        readonly public: boolean;
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
        /**
         * __Read-only__ The Region this IP address resides in.
         */
        readonly region: string;
        /**
         * __Read-only__ The mask that separates host bits from network bits for this address.
         */
        readonly subnet_mask: string;
        /**
         * __Read-only__ The type of address this is.
         */
        type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
        /**
         * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
         *
         * > 📘
         * >
         * > Only allowed for `vpc` type interfaces.
         */
        vpc_nat_1_1: {
            /**
             * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
             */
            address: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface.
             */
            readonly vpc_id: number;
        };
    };
};

export type GetLinodeIpResponse = GetLinodeIpResponses[keyof GetLinodeIpResponses];

export type PutLinodeIpData = {
    /**
     * The information to update for the IP address.
     */
    body?: {
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
    };
    path: {
        /**
         * The ID of the Linode.
         */
        linodeId: number;
        /**
         * The IP address.
         */
        address: string;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/ips/{address}';
};

export type PutLinodeIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLinodeIpError = PutLinodeIpErrors[keyof PutLinodeIpErrors];

export type PutLinodeIpResponses = {
    /**
     * An IP address that exists in Linode's system, either IPv4 or IPv6.
     */
    200: {
        /**
         * __Read-only__ The IP address.
         */
        readonly address: string;
        /**
         * __Read-only__ The default gateway for this address.
         */
        readonly gateway: string | null;
        /**
         * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
         */
        readonly interface_id: number | null;
        /**
         * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
         */
        readonly linode_id: number;
        /**
         * __Read-only__ The number of bits set in the subnet mask.
         */
        readonly prefix: number;
        /**
         * __Read-only__ Whether this is a public or private IP address.
         */
        readonly public: boolean;
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
        /**
         * __Read-only__ The Region this IP address resides in.
         */
        readonly region: string;
        /**
         * __Read-only__ The mask that separates host bits from network bits for this address.
         */
        readonly subnet_mask: string;
        /**
         * __Read-only__ The type of address this is.
         */
        type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
        /**
         * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
         *
         * > 📘
         * >
         * > Only allowed for `vpc` type interfaces.
         */
        vpc_nat_1_1: {
            /**
             * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
             */
            address: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface.
             */
            readonly vpc_id: number;
        };
    };
};

export type PutLinodeIpResponse = PutLinodeIpResponses[keyof PutLinodeIpResponses];

export type PostMigrateLinodeInstanceData = {
    body?: {
        /**
         * Include this to assign this Linode to an existing [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) in the data center you're migrating to. These constraints apply:
         *
         * - If the target Linode is in a placement group, it will be removed from it when migrating.
         * - The target placement group needs to be in the same `region` you're migrating to.
         * - The placement group needs to have capacity. Run the [Get a region](https://techdocs.akamai.com/linode-api/reference/get-region) operation and note either the `maximum_linodes_per_pg` (strict) or `maximum_linodes_per_flexible_pg` (flexible), based on your selected `placement_group_policy`. These represent the Linode limit per placement group, for each `placement_group_policy` type. You can then run the [Get a placement group](https://techdocs.akamai.com/linode-api/reference/get-placement-group) operation to review the Linodes in that group.
         */
        placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
        };
        /**
         * The region to which the Linode will be migrated. Must be a valid region slug. A list of regions can be viewed by running the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation. A cross data center migration will cancel a pending migration that has not yet been initiated. A cross data center migration will initiate a `linode_migrate_datacenter_create` event.
         */
        region: string;
        /**
         * Type of migration used in moving to a new host or Linode type.
         *
         * `warm`: the Linode will not power down until the migration is complete.
         * Warm migrations are not available for DC migrations.
         *
         * `cold`: the Linode will be powered down and migrated. When the migration
         * is complete, the Linode will be powered on.
         */
        type: 'warm' | 'cold';
        /**
         * When initiating a cross DC migration, setting this value to `true` will also ensure that the Linode is upgraded to the latest generation of hardware that corresponds to your Linode's Type, if any free upgrades are available for it. If no free upgrades are available, and this value is set to `true`, then the endpoint will return a 400 error code and the migration will not be performed. If the data center set in the `region` field does not allow upgrades, then the endpoint will return a 400 error code and the migration will not be performed.
         */
        upgrade: boolean;
    };
    path: {
        /**
         * ID of the Linode to migrate.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/migrate';
};

export type PostMigrateLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostMigrateLinodeInstanceError = PostMigrateLinodeInstanceErrors[keyof PostMigrateLinodeInstanceErrors];

export type PostMigrateLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostMigrateLinodeInstanceResponse = PostMigrateLinodeInstanceResponses[keyof PostMigrateLinodeInstanceResponses];

export type PostMutateLinodeInstanceData = {
    /**
     * Whether to automatically resize disks or not.
     */
    body?: {
        /**
         * Automatically resize disks when resizing a Linode. When resizing down to a smaller plan your Linode's data must fit within the smaller disk size.
         */
        allow_auto_disk_resize: boolean;
    };
    path: {
        /**
         * ID of the Linode to mutate.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/mutate';
};

export type PostMutateLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostMutateLinodeInstanceError = PostMutateLinodeInstanceErrors[keyof PostMutateLinodeInstanceErrors];

export type PostMutateLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostMutateLinodeInstanceResponse = PostMutateLinodeInstanceResponses[keyof PostMutateLinodeInstanceResponses];

export type GetLinodeNodeBalancersData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/nodebalancers';
};

export type GetLinodeNodeBalancersResponses = {
    /**
     * Returns a paginated list of NodeBalancers.
     */
    200: {
        data: Array<{
            /**
             * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
             */
            client_conn_throttle: number;
            /**
             * __Read-only__ When this NodeBalancer was created.
             */
            readonly created: string;
            /**
             * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
             */
            readonly hostname: string;
            /**
             * __Read-only__ This NodeBalancer's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
             */
            readonly ipv4: string;
            /**
             * __Read-only__ This NodeBalancer's public IPv6 address.
             */
            readonly ipv6: string | null;
            /**
             * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
             */
            label: string;
            /**
             * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
             */
            readonly lke_cluster: {
                /**
                 * The ID of the related LKE cluster.
                 */
                id: string;
                /**
                 * The label of the related LKE cluster.
                 */
                label: string;
                /**
                 * __Read-only__ The type for LKE clusters.
                 */
                readonly type: string;
                /**
                 * The URL where you can access the related LKE cluster.
                 */
                url: string;
            } | null;
            /**
             * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
             */
            readonly region: string;
            /**
             * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
             */
            readonly transfer: {
                /**
                 * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
                 */
                readonly in: number | null;
                /**
                 * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
                 */
                readonly out: number | null;
                /**
                 * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                 */
                readonly total: number | null;
            };
            /**
             * __Read-only__ The type of NodeBalancer.
             */
            type: 'common' | 'premium';
            /**
             * __Read-only__ When this NodeBalancer was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeNodeBalancersResponse = GetLinodeNodeBalancersResponses[keyof GetLinodeNodeBalancersResponses];

export type PostResetLinodePasswordData = {
    /**
     * This Linode's new root password.
     */
    body?: {
        /**
         * The root user's password on this Linode. Linode passwords must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a Password does not meet strength requirement error.
         */
        root_pass: string;
    };
    path: {
        /**
         * ID of the Linode for which to reset its root password.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/password';
};

export type PostResetLinodePasswordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostResetLinodePasswordError = PostResetLinodePasswordErrors[keyof PostResetLinodePasswordErrors];

export type PostResetLinodePasswordResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostResetLinodePasswordResponse = PostResetLinodePasswordResponses[keyof PostResetLinodePasswordResponses];

export type PostRebootLinodeInstanceData = {
    /**
     * Optional reboot parameters.
     */
    body?: {
        /**
         * The Linode Config ID to reboot into.  If `null` or omitted, the last booted config will be used.  If there was no last booted config and this Linode only has one config, it will be used.  If a config cannot be determined, an error will be returned.
         */
        config_id: number | null;
    };
    path: {
        /**
         * ID of the linode to reboot.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/reboot';
};

export type PostRebootLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostRebootLinodeInstanceError = PostRebootLinodeInstanceErrors[keyof PostRebootLinodeInstanceErrors];

export type PostRebootLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostRebootLinodeInstanceResponse = PostRebootLinodeInstanceResponses[keyof PostRebootLinodeInstanceResponses];

export type PostRebuildLinodeInstanceData = {
    /**
     * The requested state your Linode will be rebuilt into.
     */
    body: {
        /**
         * __Write-only__ A list of public SSH keys that will be automatically appended to the root user's `~/.ssh/authorized_keys` file when deploying from an Image.
         */
        authorized_keys: Array<string>;
        /**
         * __Write-only__ A list of usernames for authorized users. Before you can add a user, it needs an SSH key assigned to its profile. See [Add an SSH key](https://techdocs.akamai.com/linode-api/reference/post-add-ssh-key) for more information. If the usernames already have associated SSH keys, the keys will be appended to the root users `~/.ssh/authorized_keys` file automatically when deploying from an image.
         */
        authorized_users: Array<string>;
        /**
         * __Write-only__ This field defaults to `true` if the Linode is created with an Image or from a Backup. If it is deployed from an Image or a Backup and you wish it to remain `offline` after deployment, set this to `false`.
         */
        booted: boolean;
        /**
         * Local disk encryption ensures that your data stored on Linodes is secured. Disk encryption protects against unauthorized data access by keeping the data encrypted if the disk is ever removed from the data center, decommissioned, or disposed of. The platform manages the encryption and decryption for you.
         *
         * By default, encryption is `enabled` on all Linodes. If you opted out of encryption or if the Linode was created prior to local disk encryption support, you can encrypt your data using [Rebuild](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance).
         */
        disk_encryption: 'enabled' | 'disabled';
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Beta__ Defines the maintenance policy for this Linode. Choose from the following policies:
         *
         * - `linode/migrate`: Migrates the Linode to a new host while it remains fully operational (with some performance degradation). Recommended for maximizing availability.
         * - `linode/power_off_on`: Powers off the Linode at the start of the maintenance event and reboots it once the maintenance finishes. Recommended for maximizing performance.
         *
         * Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Write-only__ An object containing user-defined data relevant to the creation of Linodes.
         */
        metadata: {
            /**
             * Base64-encoded [cloud-config](https://www.linode.com/docs/products/compute/compute-instances/guides/metadata-cloud-config/) data.
             *
             * Cannot be modified after provisioning. To update, use either the [Clone a Linode](https://techdocs.akamai.com/linode-api/reference/post-clone-linode-instance) or [Rebuild a Linode](https://techdocs.akamai.com/linode-api/reference/post-rebuild-linode-instance) operations.
             *
             * Must not be included when cloning to an existing Linode.
             *
             * Unencoded data must not exceed 65535 bytes, or about 16kb encoded.
             */
            user_data: string;
        };
        /**
         * __Write-only__ This sets the root user's password on a newly created Linode Disk when deploying from an Image.
         *
         * - __Required__ when creating a Linode Disk from an Image, including when using a StackScript.
         *
         * - Must meet a password strength score requirement that is calculated internally by the API. If the strength requirement is not met, you will receive a `Password does not meet strength requirement` error.
         */
        root_pass: string;
        /**
         * This field is required only if the StackScript being deployed requires input data from the User for successful completion. See [User Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more details.
         *
         * This field is required to be valid JSON.
         *
         * Total length cannot exceed 65,535 characters.
         */
        stackscript_data: {
            [key: string]: unknown;
        };
        /**
         * A StackScript ID that will cause the referenced StackScript to be run during deployment of this Linode. A compatible `image` is required to use a StackScript. To get a list of available StackScript and their permitted Images, run [List StackScripts](https://techdocs.akamai.com/linode-api/reference/get-stack-scripts). This field cannot be used when deploying from a Backup or a Private Image.
         */
        stackscript_id: number;
    } & {
        /**
         * The ID of the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) to resize to with this request.
         */
        type: string;
    };
    path: {
        /**
         * ID of the Linode to rebuild.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/rebuild';
};

export type PostRebuildLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostRebuildLinodeInstanceError = PostRebuildLinodeInstanceErrors[keyof PostRebuildLinodeInstanceErrors];

export type PostRebuildLinodeInstanceResponses = {
    /**
     * Linode
     *
     * Rebuild started.
     */
    200: {
        alerts: {
            /**
             * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
             *
             * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
             *
             * The default value is 90% multiplied by the number of cores.
             *
             * If the value is set to `0` (zero), the alert is disabled.
             */
            cpu: number;
            /**
             * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
             */
            io: number;
            /**
             * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_in: number;
            /**
             * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
             */
            network_out: number;
            /**
             * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
             */
            transfer_quota: number;
        };
        /**
         * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
         */
        backups: {
            /**
             * __Read-only__ Whether Backups for this Linode are available for restoration.
             *
             * Backups undergoing maintenance are not available for restoration.
             */
            readonly available: boolean;
            /**
             * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
             */
            readonly enabled: boolean;
            /**
             * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
             */
            readonly last_successful: string;
            schedule: {
                /**
                 * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                 *
                 * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                 */
                day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                /**
                 * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                 *
                 * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                 *
                 * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                 */
                window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
            };
        };
        /**
         * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
         */
        readonly capabilities: Array<string>;
        /**
         * __Read-only__ When this Linode was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
         */
        readonly disk_encryption: string | null;
        /**
         * __Deprecated__, __Filterable__ The group label for this Linode.
         *
         * @deprecated
         */
        group: string;
        /**
         * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
         */
        readonly has_user_data: boolean;
        /**
         * __Read-only__ The Linode's host machine, as a UUID.
         */
        readonly host_uuid: string;
        /**
         * __Read-only__ The virtualization software powering this Linode.
         */
        hypervisor: 'kvm';
        /**
         * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
         */
        readonly id: number;
        /**
         * An Image ID to deploy the Linode Disk from.
         *
         * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
         */
        image: string | null;
        /**
         * __Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
         */
        interface_generation: 'legacy_config' | 'linode';
        /**
         * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
         *
         * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
         */
        readonly ipv4: Array<string>;
        /**
         * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
         *
         * Linode labels have the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         */
        label: string;
        /**
         * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
         */
        readonly lke_cluster_id: number | null;
        /**
         * __Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
         */
        maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
        /**
         * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
         */
        readonly placement_group: {
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
        } | null;
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
         */
        readonly region: string;
        /**
         * __Read-only__ Information about the resources available to this Linode.
         */
        readonly specs: {
            /**
             * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
             */
            readonly disk: number;
            /**
             * __Read-only__ The number of GPUs this Linode has access to.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The amount of RAM, in MB, this Linode has access to.
             *
             * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
             */
            readonly memory: number;
            /**
             * __Read-only__ The amount of network transfer this Linode is allotted each month.
             */
            readonly transfer: number;
            /**
             * __Read-only__ The number of VCPUs this Linode has access to.
             */
            readonly vcpus: number;
        };
        /**
         * __Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
         *
         * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
         * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
         * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
         */
        status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
        /**
         * __Filterable__ Tags to help you organize your content.
         */
        tags: Array<string>;
        /**
         * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
         */
        readonly type: string;
        /**
         * __Read-only__ When this Linode was last updated.
         */
        readonly updated: string;
        /**
         * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
         */
        watchdog_enabled: boolean;
    };
};

export type PostRebuildLinodeInstanceResponse = PostRebuildLinodeInstanceResponses[keyof PostRebuildLinodeInstanceResponses];

export type PostRescueLinodeInstanceData = {
    /**
     * Optional object of devices to be mounted.
     */
    body?: {
        devices: {
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sda: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdb: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdc: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdd: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sde: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdf: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
            /**
             * Device can be either a Disk or Volume identified by `disk_id` or `volume_id`. Only one type per slot allowed. Can be `null`. Devices mapped from _sde_ through _sdh_ are unavailable in `fullvirt` virt_mode.
             */
            sdg: {
                /**
                 * The Disk ID, or `null` if a Volume is assigned to this slot.
                 */
                disk_id: number;
                /**
                 * The Volume ID, or `null` if a Disk is assigned to this slot.
                 */
                volume_id: number | null;
            };
        };
    };
    path: {
        /**
         * ID of the Linode to rescue.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/rescue';
};

export type PostRescueLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostRescueLinodeInstanceError = PostRescueLinodeInstanceErrors[keyof PostRescueLinodeInstanceErrors];

export type PostRescueLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostRescueLinodeInstanceResponse = PostRescueLinodeInstanceResponses[keyof PostRescueLinodeInstanceResponses];

export type PostResizeLinodeInstanceData = {
    /**
     * The Type your current Linode will resize to, and whether to attempt to automatically resize the Linode's disks.
     */
    body: {
        /**
         * Automatically resize disks when resizing a Linode. When resizing down to a smaller plan your Linode's data must fit within the smaller disk size.
         */
        allow_auto_disk_resize?: boolean;
        /**
         * Type of migration used in moving to a new host or Linode type.
         *
         * `warm`: the Linode will not power down until the migration is complete.
         * Warm migrations are not available for DC migrations.
         *
         * `cold`: the Linode will be powered down and migrated. When the migration
         * is complete, the Linode will be powered on.
         */
        migration_type?: 'warm' | 'cold';
        /**
         * The ID representing the Linode Type.
         */
        type: string;
    };
    path: {
        /**
         * ID of the Linode to resize.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/resize';
};

export type PostResizeLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostResizeLinodeInstanceError = PostResizeLinodeInstanceErrors[keyof PostResizeLinodeInstanceErrors];

export type PostResizeLinodeInstanceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostResizeLinodeInstanceResponse = PostResizeLinodeInstanceResponses[keyof PostResizeLinodeInstanceResponses];

export type PostShutdownLinodeInstanceData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to shutdown.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/shutdown';
};

export type PostShutdownLinodeInstanceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostShutdownLinodeInstanceError = PostShutdownLinodeInstanceErrors[keyof PostShutdownLinodeInstanceErrors];

export type PostShutdownLinodeInstanceResponses = {
    /**
     * Shutdown started.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostShutdownLinodeInstanceResponse = PostShutdownLinodeInstanceResponses[keyof PostShutdownLinodeInstanceResponses];

export type GetLinodeStatsData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/stats';
};

export type GetLinodeStatsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeStatsError = GetLinodeStatsErrors[keyof GetLinodeStatsErrors];

export type GetLinodeStatsResponses = {
    /**
     * __Read-only__ CPU, IO, IPv4, and IPv6 statistics. Graph data, if available, is in `[timestamp, reading]` array format. Timestamp is a UNIX timestamp in EST.
     */
    readonly 200: {
        /**
         * Percentage of CPU used.
         */
        cpu: Array<Array<number>>;
        /**
         * Input/Output statistics.
         */
        io: {
            /**
             * Block/s written.
             */
            io: Array<Array<number>>;
            /**
             * Block/s written.
             */
            swap: Array<Array<number>>;
        };
        /**
         * IPv4 statistics.
         */
        netv4: {
            /**
             * Input stats for IPv4, measured in bits/s (bits/second).
             */
            in: Array<Array<number>>;
            /**
             * Output stats for IPv4, measured in bits/s (bits/second).
             */
            out: Array<Array<number>>;
            /**
             * Private IPv4 input statistics, measured in bits/s (bits/second).
             */
            private_in: Array<Array<number>>;
            /**
             * Private IPv4 output statistics, measured in bits/s (bits/second).
             */
            private_out: Array<Array<number>>;
        };
        /**
         * IPv6 statistics.
         */
        netv6: {
            /**
             * Input stats for IPv6, measured in bits/s (bits/second).
             */
            in: Array<Array<number>>;
            /**
             * Output stats for IPv6, measured in bits/s (bits/second).
             */
            out: Array<Array<number>>;
            /**
             * Private IPv6 input statistics, measured in bits/s (bits/second).
             */
            private_in: Array<Array<number>>;
            /**
             * Private IPv6 output statistics, measured in bits/s (bits/second).
             */
            private_out: Array<Array<number>>;
        };
        /**
         * The title for this data set.
         */
        title: string;
    };
};

export type GetLinodeStatsResponse = GetLinodeStatsResponses[keyof GetLinodeStatsResponses];

export type GetLinodeStatsByYearMonthData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * Numeric value representing the year to look up.
         */
        year: number;
        /**
         * Numeric value representing the month to look up.
         */
        month: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/stats/{year}/{month}';
};

export type GetLinodeStatsByYearMonthErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeStatsByYearMonthError = GetLinodeStatsByYearMonthErrors[keyof GetLinodeStatsByYearMonthErrors];

export type GetLinodeStatsByYearMonthResponses = {
    /**
     * __Read-only__ CPU, IO, IPv4, and IPv6 statistics. Graph data, if available, is in `[timestamp, reading]` array format. Timestamp is a UNIX timestamp in EST.
     */
    readonly 200: {
        /**
         * Percentage of CPU used.
         */
        cpu: Array<Array<number>>;
        /**
         * Input/Output statistics.
         */
        io: {
            /**
             * Block/s written.
             */
            io: Array<Array<number>>;
            /**
             * Block/s written.
             */
            swap: Array<Array<number>>;
        };
        /**
         * IPv4 statistics.
         */
        netv4: {
            /**
             * Input stats for IPv4, measured in bits/s (bits/second).
             */
            in: Array<Array<number>>;
            /**
             * Output stats for IPv4, measured in bits/s (bits/second).
             */
            out: Array<Array<number>>;
            /**
             * Private IPv4 input statistics, measured in bits/s (bits/second).
             */
            private_in: Array<Array<number>>;
            /**
             * Private IPv4 output statistics, measured in bits/s (bits/second).
             */
            private_out: Array<Array<number>>;
        };
        /**
         * IPv6 statistics.
         */
        netv6: {
            /**
             * Input stats for IPv6, measured in bits/s (bits/second).
             */
            in: Array<Array<number>>;
            /**
             * Output stats for IPv6, measured in bits/s (bits/second).
             */
            out: Array<Array<number>>;
            /**
             * Private IPv6 input statistics, measured in bits/s (bits/second).
             */
            private_in: Array<Array<number>>;
            /**
             * Private IPv6 output statistics, measured in bits/s (bits/second).
             */
            private_out: Array<Array<number>>;
        };
        /**
         * The title for this data set.
         */
        title: string;
    };
};

export type GetLinodeStatsByYearMonthResponse = GetLinodeStatsByYearMonthResponses[keyof GetLinodeStatsByYearMonthResponses];

export type GetLinodeTransferData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/transfer';
};

export type GetLinodeTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeTransferError = GetLinodeTransferErrors[keyof GetLinodeTransferErrors];

export type GetLinodeTransferResponses = {
    /**
     * A collection of the specified Linode's network transfer statistics.
     */
    200: {
        /**
         * __Read-only__ The amount of network transfer this Linode has used, in GB, past your monthly quota.
         */
        readonly billable: number;
        /**
         * __Read-only__ The amount of network transfer this Linode adds to your transfer pool, in GB, for the current month's billing cycle.
         */
        readonly quota: number;
        /**
         * __Read-only__ The amount of network transfer used by this Linode, in bytes, for the current month's billing cycle.
         */
        readonly used: number;
    };
};

export type GetLinodeTransferResponse = GetLinodeTransferResponses[keyof GetLinodeTransferResponses];

export type GetLinodeTransferByYearMonthData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
        /**
         * Numeric value representing the year to look up.
         */
        year: number;
        /**
         * Numeric value representing the month to look up.
         */
        month: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/transfer/{year}/{month}';
};

export type GetLinodeTransferByYearMonthErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeTransferByYearMonthError = GetLinodeTransferByYearMonthErrors[keyof GetLinodeTransferByYearMonthErrors];

export type GetLinodeTransferByYearMonthResponses = {
    /**
     * A collection of the specified Linode's network transfer statistics for the requested month.
     */
    200: {
        /**
         * __Read-only__ The amount of inbound public network traffic received by this Linode, in bytes, for a specific year/month.
         */
        readonly bytes_in: number;
        /**
         * __Read-only__ The amount of outbound public network traffic sent by this Linode, in bytes, for a specific year/month.
         */
        readonly bytes_out: number;
        /**
         * __Read-only__ The total amount of public network traffic sent and received by this Linode, in bytes, for a specific year/month.
         */
        readonly bytes_total: number;
    };
};

export type GetLinodeTransferByYearMonthResponse = GetLinodeTransferByYearMonthResponses[keyof GetLinodeTransferByYearMonthResponses];

export type PostUpgradeLinodeInterfacesData = {
    /**
     * Upgrades a legacy configuration interface on a Linode to a matching Linode interface.
     */
    body?: {
        /**
         * The Linode's `config_id`. Only one `config_id` can be specified:
         * - If there are no legacy configuration interfaces or configurations defined in the config_id, a public interface is created using the Linode's automatically assigned public IPv4 address.
         * - If a config_id is not provided and the Linode has only one configuration, the upgrade automatically uses that config_id.
         * - If the Linode has multiple configurations and a config_id is not specified, an error is returned.
         */
        config_id: number | null;
        /**
         * Before you upgrade interfaces, you can preview the new Linode interface by performing a `dry_run`:
         * - Either omit `dry_run` or set it to `true` to simulate the upgrade process. The response data shows what the Linode interface will look like after the upgrade, but without committing any changes. Since the interface doesn't exist yet, the interface `id` value is 0.
         * - If `dry_run` is set to `false`, the Linode undergoes the actual upgrade, but note you need to first shut down the Linode.
         */
        dry_run: boolean;
    } | null;
    path: {
        /**
         * The `id` of the Linode.
         */
        linodeId: number;
    };
    query?: never;
    url: '/linode/instances/{linodeId}/upgrade-interfaces';
};

export type PostUpgradeLinodeInterfacesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostUpgradeLinodeInterfacesError = PostUpgradeLinodeInterfacesErrors[keyof PostUpgradeLinodeInterfacesErrors];

export type PostUpgradeLinodeInterfacesResponses = {
    /**
     * Upgraded interfaces
     */
    200: {
        /**
         * The Linode's `config_id` that was upgraded.
         */
        config_id: number;
        /**
         * Indicates if this was a `dry_run` upgrade that didn't commit any changes and didn't generate a unique interface ID. If `dry_run` is `false`, the upgrade completed.
         */
        dry_run: boolean;
        /**
         * One of the following interface types: VPC, public, or VLAN.
         */
        interfaces: Array<{
            /**
             * When the interface was created.
             */
            created: string;
            /**
             * Indicates if the interface is used as a default route.
             */
            default_route: {
                /**
                 * Indicates if the interface is used for the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
                 */
                ipv4: boolean;
                /**
                 * Indicates if the interface is used for the IPv6 default route. Only one interface per Linode can have the IPv6 default route.
                 */
                ipv6: boolean;
            } | null;
            /**
             * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
             */
            readonly id: number;
            /**
             * A 48-bit MAC address used to identify the Linode’s interface. A public interface's `mac_address` does not change, even if the public interface is deleted and then recreated.
             */
            mac_address: string;
            /**
             * Public interface type.
             */
            public: {
                /**
                 * The interface's public IPv4 `addresses`.
                 */
                ipv4: {
                    /**
                     * The public IPv4 addresses and primary settings for this public interface.
                     */
                    addresses: Array<{
                        /**
                         * The public IPv4 address assigned to this interface.
                         */
                        address: string;
                        /**
                         * Indicates if the public IPv4 address serves as the source address for traffic routing within the Linode and other corresponding network interfaces and services.
                         */
                        primary: boolean;
                    }>;
                    /**
                     * The IPv4 address assigned to this Linode interface, which is also shared with another Linode.
                     */
                    shared: Array<{
                        /**
                         * Shared IPv4 address.
                         */
                        address: string;
                        /**
                         * The ID of the Linode this address currently belongs to. For IPv4 addresses, by default this is the Linode this address was assigned when created.
                         */
                        linode_id: string;
                    }>;
                };
                /**
                 * The interface's public IPv6 configuration.
                 */
                ipv6: {
                    /**
                     * List of IPv6 ranges assigned to this interface.
                     */
                    ranges: Array<{
                        /**
                         * IPv6 range in CIDR notation (`2600:0db8::1/64`) or prefix-only (`/64`).
                         */
                        range: string;
                        /**
                         * The public IPv6 address that the `range` is routed to.
                         */
                        route_target: string;
                    }>;
                    /**
                     * The IPv6 address assigned to this Linode interface, which is also shared with another Linode.
                     */
                    shared: Array<{
                        /**
                         * The IPv6 address range.
                         */
                        range: string;
                        /**
                         * The public IPv6 address that the `range` is routed to.
                         */
                        route_target: string;
                    }>;
                    /**
                     * The public `slaac` and subnet prefix settings for this public interface that is used to communicate over the public internet, and with other services in the same data center.
                     */
                    slaac: Array<{
                        /**
                         * Public IPv6 addresses assigned to this interface.
                         */
                        address: string;
                        /**
                         * The prefix length advertised for SLAAC to use. Only the specific (`/128`) EUI-64 address derived from the interface's MAC address is supported. To ensure the MAC-based EUI-64 address is used, privacy addressing needs to be disabled. Network Helper automatically configures the MAC-derived EUI-64 address. If you disable Network Helper or use an unsupported operating system, follow the specific instructions for your OS.
                         */
                        prefix: number;
                    }>;
                };
            };
            /**
             * When the interface was last updated.
             */
            updated: string;
            /**
             * Interface version number that increments when the interface updates.
             */
            version: number;
            /**
             * The value is `null` if this is not a VLAN interface.
             */
            vlan: {
                [key: string]: unknown;
            } | null;
            /**
             * The value is `null` if this is not a VPC interface.
             */
            vpc: {
                [key: string]: never;
            } | null;
        } | {
            /**
             * When the interface was created.
             */
            created: string;
            /**
             * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
             */
            readonly id: number;
            /**
             * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
             */
            mac_address: string;
            /**
             * The value is `null` if this isn't a public interface.
             */
            public: {
                [key: string]: unknown;
            } | null;
            /**
             * When the interface was last updated.
             */
            updated: string;
            /**
             * Interface version number that is incremented when the interface is updated.
             */
            version: number;
            /**
             * VLAN interface type.
             */
            vlan: {
                /**
                 * This VLAN interface's private IPv4 address in classless inter-domain routing (CIDR) notation.
                 */
                ipam_address: string | null;
                /**
                 * The VLAN's label. VLAN interfaces on the same Linode must have a unique `vlan_label`.
                 */
                vlan_label: string;
            };
            /**
             * The value is `null` if this isn't a VPC interface.
             */
            vpc: {
                [key: string]: unknown;
            } | null;
        } | {
            /**
             * When the interface was created.
             */
            created: string;
            /**
             * Indicates if the interface serves as a default route.
             */
            default_route: {
                /**
                 * Indicates if the interface serves as the IPv4 default route. Only one interface per Linode can have the IPv4 default route.
                 */
                ipv4: boolean;
            };
            /**
             * __Read-only__ The unique ID for this interface. For `dry_run` [upgrades](https://techdocs.akamai.com/linode-api/reference/post-upgrade-linode-interfaces), a unique `id` is not generated for the interface and its value is set to 0.
             */
            readonly id: number;
            /**
             * A 48-bit MAC address used to identify the Linode’s interface. The `mac_address` of an interface remains constant and does not change.
             */
            mac_address: string;
            /**
             * The value is `null` if this is not a public interface.
             */
            public: {
                [key: string]: never;
            } | null;
            /**
             * When the interface last updated.
             */
            updated: string;
            /**
             * The version number of the interface configuration, incremented each time the interface is updated.
             */
            version: number;
            /**
             * The value is `null` if this is not a VLAN interface.
             */
            vlan: {
                [key: string]: never;
            } | null;
            /**
             * VPC interface type.
             */
            vpc: {
                /**
                 * The interface's IPv4 `addresses` and `ranges` configuration.
                 */
                ipv4: {
                    /**
                     * IPv4 address settings for this VPC interface.
                     */
                    addresses: Array<{
                        /**
                         * The VPC subnet IPv4 address.
                         */
                        address: string;
                        /**
                         * The 1:1 NAT IPv4 address used to associate a public IPv4 address with the interface's VPC subnet IPv4 address.
                         */
                        nat_1_1_address: string | null;
                        /**
                         * Indicates if the IPv4 address is used to set up a source address for routes inside the Linode for the corresponding network interface.
                         */
                        primary: boolean;
                    }>;
                    /**
                     * VPC IPv4 ranges.
                     */
                    ranges: Array<{
                        /**
                         * CIDR notation of a range (`1.2.3.4/24`) or prefix only (`/24`).
                         */
                        range: string;
                    }>;
                };
                /**
                 * VPC subnet's unique identifier.
                 */
                subnet_id: number;
                /**
                 * VPC's unique identifier.
                 */
                vpc_id: number;
            };
        }>;
    };
};

export type PostUpgradeLinodeInterfacesResponse = PostUpgradeLinodeInterfacesResponses[keyof PostUpgradeLinodeInterfacesResponses];

export type GetLinodeVolumesData = {
    body?: never;
    path: {
        /**
         * ID of the Linode to look up.
         */
        linodeId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/instances/{linodeId}/volumes';
};

export type GetLinodeVolumesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeVolumesError = GetLinodeVolumesErrors[keyof GetLinodeVolumesErrors];

export type GetLinodeVolumesResponses = {
    /**
     * Returns the Block Storage volumes attached to this Linode.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this volume was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Whether encryption is enabled on this volume.
             */
            encryption: 'enabled' | 'disabled';
            /**
             * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
             */
            readonly filesystem_path: string;
            /**
             * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
             */
            hardware_type: 'hdd' | 'nvme';
            /**
             * __Read-only__ The unique identifier for the volume.
             */
            readonly id: number;
            /**
             * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
             */
            readonly io_ready: boolean;
            /**
             * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
             */
            label: string;
            /**
             * The unique identifier of the Linode this volume is attached to, if applicable.
             */
            linode_id: number | null;
            /**
             * __Read-only__ The name of the Linode this volume is attached to, if applicable.
             */
            readonly linode_label: string | null;
            /**
             * The unique identifier for the region where the volume lives.
             */
            region: string;
            /**
             * The volume's size, in gigabytes.
             */
            size: number;
            /**
             * __Read-only__ The current status of the volume. This can be one of:
             *
             * - `creating`. The API is creating the volume and it's not ready for use.
             *
             * - `active`. The volume is online and ready for use.
             *
             * - `resizing`. The volume's capacity is being upgraded.
             *
             * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
             */
            status: 'creating' | 'active' | 'resizing' | 'key_rotating';
            /**
             * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
             */
            tags: Array<string>;
            /**
             * __Read-only__ When this volume was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeVolumesResponse = GetLinodeVolumesResponses[keyof GetLinodeVolumesResponses];

export type GetKernelsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/kernels';
};

export type GetKernelsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetKernelsError = GetKernelsErrors[keyof GetKernelsErrors];

export type GetKernelsResponses = {
    /**
     * Returns an array of Kernels.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ The architecture of this Kernel.
             */
            architecture: 'x86_64' | 'i386';
            /**
             * __Read-only__ The date on which this Kernel was built.
             */
            readonly built: string;
            /**
             * __Filterable__, __Read-only__ If this Kernel is marked as deprecated, this field has a value of `true`; otherwise, this field is `false`.
             */
            readonly deprecated: boolean;
            /**
             * __Read-only__ The unique ID of this Kernel.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ If this Kernel is suitable for KVM Linodes.
             */
            readonly kvm: boolean;
            /**
             * __Filterable__, __Read-only__ The friendly name of this Kernel.
             */
            readonly label: string;
            /**
             * __Filterable__, __Read-only__ If this Kernel is suitable for paravirtualized operations.
             */
            readonly pvops: boolean;
            /**
             * __Filterable__, __Read-only__ Linux Kernel version.
             */
            readonly version: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetKernelsResponse = GetKernelsResponses[keyof GetKernelsResponses];

export type GetKernelData = {
    body?: never;
    path: {
        /**
         * ID of the Kernel to look up.
         */
        kernelId: string;
    };
    query?: never;
    url: '/linode/kernels/{kernelId}';
};

export type GetKernelErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetKernelError = GetKernelErrors[keyof GetKernelErrors];

export type GetKernelResponses = {
    /**
     * Linux kernel object.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The architecture of this Kernel.
         */
        architecture: 'x86_64' | 'i386';
        /**
         * __Read-only__ The date on which this Kernel was built.
         */
        readonly built: string;
        /**
         * __Filterable__, __Read-only__ If this Kernel is marked as deprecated, this field has a value of `true`; otherwise, this field is `false`.
         */
        readonly deprecated: boolean;
        /**
         * __Read-only__ The unique ID of this Kernel.
         */
        readonly id: string;
        /**
         * __Filterable__, __Read-only__ If this Kernel is suitable for KVM Linodes.
         */
        readonly kvm: boolean;
        /**
         * __Filterable__, __Read-only__ The friendly name of this Kernel.
         */
        readonly label: string;
        /**
         * __Filterable__, __Read-only__ If this Kernel is suitable for paravirtualized operations.
         */
        readonly pvops: boolean;
        /**
         * __Filterable__, __Read-only__ Linux Kernel version.
         */
        readonly version: string;
    };
};

export type GetKernelResponse = GetKernelResponses[keyof GetKernelResponses];

export type GetStackScriptsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/linode/stackscripts';
};

export type GetStackScriptsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetStackScriptsError = GetStackScriptsErrors[keyof GetStackScriptsErrors];

export type GetStackScriptsResponses = {
    /**
     * A list of StackScripts available to the User, including private StackScripts owned by the User if the request is authenticated.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The date this StackScript was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
             */
            readonly deployments_active: number;
            /**
             * __Filterable__, __Read-only__ The total number of times this StackScript has been deployed.
             */
            readonly deployments_total: number;
            /**
             * __Filterable__ A description for the StackScript.
             */
            description: string;
            /**
             * __Read-only__ The unique ID of this StackScript.
             */
            readonly id: number;
            /**
             * An array of Image IDs. These are the Images that can be deployed with this StackScript.
             *
             * `any/all` indicates that all available Images, including private Images, are accepted.
             */
            images: Array<string>;
            /**
             * __Filterable__ This determines whether other users can use your StackScript. __Once a StackScript is made public, it cannot be made private.__
             */
            is_public: boolean;
            /**
             * __Filterable__ The StackScript's label is for display purposes only.
             */
            label: string;
            /**
             * __Filterable__, __Read-only__ Returns `true` if this StackScript is owned by the account of the user making the request, and the user making the request is unrestricted or has access to this StackScript.
             */
            readonly mine: boolean;
            /**
             * __Filterable__ This field allows you to add notes for the set of revisions made to this StackScript.
             */
            rev_note: string;
            /**
             * The script to execute when provisioning a new Linode with this StackScript.
             */
            script: string;
            /**
             * __Read-only__ The date this StackScript was last updated.
             */
            readonly updated: string;
            /**
             * __Read-only__ This is a list of fields defined with a special syntax inside this StackScript that allow for supplying customized parameters during deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more information.
             */
            readonly user_defined_fields: Array<{
                /**
                 * __Read-only__ The default value.  If not specified, this value will be used.
                 */
                readonly default?: string;
                /**
                 * __Read-only__ An example value for the field.
                 */
                readonly example: string;
                /**
                 * __Read-only__ A human-readable label for the field that will serve as the input prompt for entering the value during deployment.
                 */
                readonly label: string;
                /**
                 * __Read-only__ A list of acceptable values for the field in any quantity, combination or order.
                 */
                readonly manyOf?: string;
                /**
                 * __Read-only__ The name of the field.
                 */
                readonly name: string;
                /**
                 * __Read-only__ A list of acceptable single values for the field.
                 */
                readonly oneOf?: string;
            }>;
            /**
             * __Read-only__ The Gravatar ID for the User who created the StackScript.
             */
            readonly user_gravatar_id: string;
            /**
             * __Read-only__ The User who created the StackScript.
             */
            readonly username: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetStackScriptsResponse = GetStackScriptsResponses[keyof GetStackScriptsResponses];

export type PostAddStackScriptData = {
    /**
     * A StackScript enables you to quickly deploy a fully configured application in an automated manner.
     */
    body: {
        /**
         * __Read-only__ The date this StackScript was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
         */
        readonly deployments_active: number;
        /**
         * __Filterable__, __Read-only__ The total number of times this StackScript has been deployed.
         */
        readonly deployments_total: number;
        /**
         * __Filterable__ A description for the StackScript.
         */
        description: string;
        /**
         * __Read-only__ The unique ID of this StackScript.
         */
        readonly id: number;
        /**
         * An array of Image IDs. These are the Images that can be deployed with this StackScript.
         *
         * `any/all` indicates that all available Images, including private Images, are accepted.
         */
        images: Array<string>;
        /**
         * __Filterable__ This determines whether other users can use your StackScript. __Once a StackScript is made public, it cannot be made private.__
         */
        is_public: boolean;
        /**
         * __Filterable__ The StackScript's label is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__, __Read-only__ Returns `true` if this StackScript is owned by the account of the user making the request, and the user making the request is unrestricted or has access to this StackScript.
         */
        readonly mine: boolean;
        /**
         * __Filterable__ This field allows you to add notes for the set of revisions made to this StackScript.
         */
        rev_note: string;
        /**
         * The script to execute when provisioning a new Linode with this StackScript.
         */
        script: string;
        /**
         * __Read-only__ The date this StackScript was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ This is a list of fields defined with a special syntax inside this StackScript that allow for supplying customized parameters during deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more information.
         */
        readonly user_defined_fields: Array<{
            /**
             * __Read-only__ The default value.  If not specified, this value will be used.
             */
            readonly default?: string;
            /**
             * __Read-only__ An example value for the field.
             */
            readonly example: string;
            /**
             * __Read-only__ A human-readable label for the field that will serve as the input prompt for entering the value during deployment.
             */
            readonly label: string;
            /**
             * __Read-only__ A list of acceptable values for the field in any quantity, combination or order.
             */
            readonly manyOf?: string;
            /**
             * __Read-only__ The name of the field.
             */
            readonly name: string;
            /**
             * __Read-only__ A list of acceptable single values for the field.
             */
            readonly oneOf?: string;
        }>;
        /**
         * __Read-only__ The Gravatar ID for the User who created the StackScript.
         */
        readonly user_gravatar_id: string;
        /**
         * __Read-only__ The User who created the StackScript.
         */
        readonly username: string;
    };
    path?: never;
    query?: never;
    url: '/linode/stackscripts';
};

export type PostAddStackScriptErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAddStackScriptError = PostAddStackScriptErrors[keyof PostAddStackScriptErrors];

export type PostAddStackScriptResponses = {
    /**
     * A StackScript enables you to quickly deploy a fully configured application in an automated manner.
     */
    200: {
        /**
         * __Read-only__ The date this StackScript was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
         */
        readonly deployments_active: number;
        /**
         * __Filterable__, __Read-only__ The total number of times this StackScript has been deployed.
         */
        readonly deployments_total: number;
        /**
         * __Filterable__ A description for the StackScript.
         */
        description: string;
        /**
         * __Read-only__ The unique ID of this StackScript.
         */
        readonly id: number;
        /**
         * An array of Image IDs. These are the Images that can be deployed with this StackScript.
         *
         * `any/all` indicates that all available Images, including private Images, are accepted.
         */
        images: Array<string>;
        /**
         * __Filterable__ This determines whether other users can use your StackScript. __Once a StackScript is made public, it cannot be made private.__
         */
        is_public: boolean;
        /**
         * __Filterable__ The StackScript's label is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__, __Read-only__ Returns `true` if this StackScript is owned by the account of the user making the request, and the user making the request is unrestricted or has access to this StackScript.
         */
        readonly mine: boolean;
        /**
         * __Filterable__ This field allows you to add notes for the set of revisions made to this StackScript.
         */
        rev_note: string;
        /**
         * The script to execute when provisioning a new Linode with this StackScript.
         */
        script: string;
        /**
         * __Read-only__ The date this StackScript was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ This is a list of fields defined with a special syntax inside this StackScript that allow for supplying customized parameters during deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more information.
         */
        readonly user_defined_fields: Array<{
            /**
             * __Read-only__ The default value.  If not specified, this value will be used.
             */
            readonly default?: string;
            /**
             * __Read-only__ An example value for the field.
             */
            readonly example: string;
            /**
             * __Read-only__ A human-readable label for the field that will serve as the input prompt for entering the value during deployment.
             */
            readonly label: string;
            /**
             * __Read-only__ A list of acceptable values for the field in any quantity, combination or order.
             */
            readonly manyOf?: string;
            /**
             * __Read-only__ The name of the field.
             */
            readonly name: string;
            /**
             * __Read-only__ A list of acceptable single values for the field.
             */
            readonly oneOf?: string;
        }>;
        /**
         * __Read-only__ The Gravatar ID for the User who created the StackScript.
         */
        readonly user_gravatar_id: string;
        /**
         * __Read-only__ The User who created the StackScript.
         */
        readonly username: string;
    };
};

export type PostAddStackScriptResponse = PostAddStackScriptResponses[keyof PostAddStackScriptResponses];

export type DeleteStackScriptData = {
    body?: never;
    path: {
        /**
         * The ID of the StackScript to look up.
         */
        stackscriptId: string;
    };
    query?: never;
    url: '/linode/stackscripts/{stackscriptId}';
};

export type DeleteStackScriptErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteStackScriptError = DeleteStackScriptErrors[keyof DeleteStackScriptErrors];

export type DeleteStackScriptResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteStackScriptResponse = DeleteStackScriptResponses[keyof DeleteStackScriptResponses];

export type GetStackScriptData = {
    body?: never;
    path: {
        /**
         * The ID of the StackScript to look up.
         */
        stackscriptId: string;
    };
    query?: never;
    url: '/linode/stackscripts/{stackscriptId}';
};

export type GetStackScriptErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetStackScriptError = GetStackScriptErrors[keyof GetStackScriptErrors];

export type GetStackScriptResponses = {
    /**
     * A StackScript enables you to quickly deploy a fully configured application in an automated manner.
     */
    200: {
        /**
         * __Read-only__ The date this StackScript was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
         */
        readonly deployments_active: number;
        /**
         * __Filterable__, __Read-only__ The total number of times this StackScript has been deployed.
         */
        readonly deployments_total: number;
        /**
         * __Filterable__ A description for the StackScript.
         */
        description: string;
        /**
         * __Read-only__ The unique ID of this StackScript.
         */
        readonly id: number;
        /**
         * An array of Image IDs. These are the Images that can be deployed with this StackScript.
         *
         * `any/all` indicates that all available Images, including private Images, are accepted.
         */
        images: Array<string>;
        /**
         * __Filterable__ This determines whether other users can use your StackScript. __Once a StackScript is made public, it cannot be made private.__
         */
        is_public: boolean;
        /**
         * __Filterable__ The StackScript's label is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__, __Read-only__ Returns `true` if this StackScript is owned by the account of the user making the request, and the user making the request is unrestricted or has access to this StackScript.
         */
        readonly mine: boolean;
        /**
         * __Filterable__ This field allows you to add notes for the set of revisions made to this StackScript.
         */
        rev_note: string;
        /**
         * The script to execute when provisioning a new Linode with this StackScript.
         */
        script: string;
        /**
         * __Read-only__ The date this StackScript was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ This is a list of fields defined with a special syntax inside this StackScript that allow for supplying customized parameters during deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more information.
         */
        readonly user_defined_fields: Array<{
            /**
             * __Read-only__ The default value.  If not specified, this value will be used.
             */
            readonly default?: string;
            /**
             * __Read-only__ An example value for the field.
             */
            readonly example: string;
            /**
             * __Read-only__ A human-readable label for the field that will serve as the input prompt for entering the value during deployment.
             */
            readonly label: string;
            /**
             * __Read-only__ A list of acceptable values for the field in any quantity, combination or order.
             */
            readonly manyOf?: string;
            /**
             * __Read-only__ The name of the field.
             */
            readonly name: string;
            /**
             * __Read-only__ A list of acceptable single values for the field.
             */
            readonly oneOf?: string;
        }>;
        /**
         * __Read-only__ The Gravatar ID for the User who created the StackScript.
         */
        readonly user_gravatar_id: string;
        /**
         * __Read-only__ The User who created the StackScript.
         */
        readonly username: string;
    };
};

export type GetStackScriptResponse = GetStackScriptResponses[keyof GetStackScriptResponses];

export type PutStackScriptData = {
    /**
     * A StackScript enables you to quickly deploy a fully configured application in an automated manner.
     */
    body?: {
        /**
         * __Read-only__ The date this StackScript was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
         */
        readonly deployments_active: number;
        /**
         * __Filterable__, __Read-only__ The total number of times this StackScript has been deployed.
         */
        readonly deployments_total: number;
        /**
         * __Filterable__ A description for the StackScript.
         */
        description: string;
        /**
         * __Read-only__ The unique ID of this StackScript.
         */
        readonly id: number;
        /**
         * An array of Image IDs. These are the Images that can be deployed with this StackScript.
         *
         * `any/all` indicates that all available Images, including private Images, are accepted.
         */
        images: Array<string>;
        /**
         * __Filterable__ This determines whether other users can use your StackScript. __Once a StackScript is made public, it cannot be made private.__
         */
        is_public: boolean;
        /**
         * __Filterable__ The StackScript's label is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__, __Read-only__ Returns `true` if this StackScript is owned by the account of the user making the request, and the user making the request is unrestricted or has access to this StackScript.
         */
        readonly mine: boolean;
        /**
         * __Filterable__ This field allows you to add notes for the set of revisions made to this StackScript.
         */
        rev_note: string;
        /**
         * The script to execute when provisioning a new Linode with this StackScript.
         */
        script: string;
        /**
         * __Read-only__ The date this StackScript was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ This is a list of fields defined with a special syntax inside this StackScript that allow for supplying customized parameters during deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more information.
         */
        readonly user_defined_fields: Array<{
            /**
             * __Read-only__ The default value.  If not specified, this value will be used.
             */
            readonly default?: string;
            /**
             * __Read-only__ An example value for the field.
             */
            readonly example: string;
            /**
             * __Read-only__ A human-readable label for the field that will serve as the input prompt for entering the value during deployment.
             */
            readonly label: string;
            /**
             * __Read-only__ A list of acceptable values for the field in any quantity, combination or order.
             */
            readonly manyOf?: string;
            /**
             * __Read-only__ The name of the field.
             */
            readonly name: string;
            /**
             * __Read-only__ A list of acceptable single values for the field.
             */
            readonly oneOf?: string;
        }>;
        /**
         * __Read-only__ The Gravatar ID for the User who created the StackScript.
         */
        readonly user_gravatar_id: string;
        /**
         * __Read-only__ The User who created the StackScript.
         */
        readonly username: string;
    };
    path: {
        /**
         * The ID of the StackScript to look up.
         */
        stackscriptId: string;
    };
    query?: never;
    url: '/linode/stackscripts/{stackscriptId}';
};

export type PutStackScriptErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutStackScriptError = PutStackScriptErrors[keyof PutStackScriptErrors];

export type PutStackScriptResponses = {
    /**
     * A StackScript enables you to quickly deploy a fully configured application in an automated manner.
     */
    200: {
        /**
         * __Read-only__ The date this StackScript was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Count of currently active, deployed Linodes created from this StackScript.
         */
        readonly deployments_active: number;
        /**
         * __Filterable__, __Read-only__ The total number of times this StackScript has been deployed.
         */
        readonly deployments_total: number;
        /**
         * __Filterable__ A description for the StackScript.
         */
        description: string;
        /**
         * __Read-only__ The unique ID of this StackScript.
         */
        readonly id: number;
        /**
         * An array of Image IDs. These are the Images that can be deployed with this StackScript.
         *
         * `any/all` indicates that all available Images, including private Images, are accepted.
         */
        images: Array<string>;
        /**
         * __Filterable__ This determines whether other users can use your StackScript. __Once a StackScript is made public, it cannot be made private.__
         */
        is_public: boolean;
        /**
         * __Filterable__ The StackScript's label is for display purposes only.
         */
        label: string;
        /**
         * __Filterable__, __Read-only__ Returns `true` if this StackScript is owned by the account of the user making the request, and the user making the request is unrestricted or has access to this StackScript.
         */
        readonly mine: boolean;
        /**
         * __Filterable__ This field allows you to add notes for the set of revisions made to this StackScript.
         */
        rev_note: string;
        /**
         * The script to execute when provisioning a new Linode with this StackScript.
         */
        script: string;
        /**
         * __Read-only__ The date this StackScript was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ This is a list of fields defined with a special syntax inside this StackScript that allow for supplying customized parameters during deployment. See [Declare User-Defined Fields (UDFs)](https://www.linode.com/docs/products/tools/stackscripts/guides/write-a-custom-script/#declare-user-defined-fields-udfs) for more information.
         */
        readonly user_defined_fields: Array<{
            /**
             * __Read-only__ The default value.  If not specified, this value will be used.
             */
            readonly default?: string;
            /**
             * __Read-only__ An example value for the field.
             */
            readonly example: string;
            /**
             * __Read-only__ A human-readable label for the field that will serve as the input prompt for entering the value during deployment.
             */
            readonly label: string;
            /**
             * __Read-only__ A list of acceptable values for the field in any quantity, combination or order.
             */
            readonly manyOf?: string;
            /**
             * __Read-only__ The name of the field.
             */
            readonly name: string;
            /**
             * __Read-only__ A list of acceptable single values for the field.
             */
            readonly oneOf?: string;
        }>;
        /**
         * __Read-only__ The Gravatar ID for the User who created the StackScript.
         */
        readonly user_gravatar_id: string;
        /**
         * __Read-only__ The User who created the StackScript.
         */
        readonly username: string;
    };
};

export type PutStackScriptResponse = PutStackScriptResponses[keyof PutStackScriptResponses];

export type GetLinodeTypesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/linode/types';
};

export type GetLinodeTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeTypesError = GetLinodeTypesErrors[keyof GetLinodeTypesErrors];

export type GetLinodeTypesResponses = {
    /**
     * A collection of Linode Types.
     */
    200: {
        /**
         * The Linode types.
         */
        data: Array<{
            /**
             * __Read-only__ Optional add-on services for Linodes and their associated cost.
             */
            readonly addons: {
                /**
                 * __Read-only__ Details on the optional backup service available with a Linode.
                 */
                readonly backups: {
                    /**
                     * The default cost to enable the Backups service for this Linode type. Prices are in U.S. dollars, broken down into hourly and monthly charges. Different prices apply in different regions. For region-specific prices, see `region_prices`.
                     */
                    price: {
                        /**
                         * The hourly cost for the Backups service, in U.S. dollars.
                         */
                        hourly: number;
                        /**
                         * The monthly cost for the Backups service, in U.S. dollars.
                         */
                        monthly: number;
                    };
                    region_prices: Array<{
                        /**
                         * The hourly cost for the Backups service in this region, in U.S. dollars.
                         */
                        hourly: number;
                        /**
                         * The unique identifier for the region.
                         */
                        id: string;
                        /**
                         * The monthly cost for the Backups service in this region, in U.S. dollars.
                         */
                        monthly: number;
                    }>;
                };
            };
            /**
             * __Filterable__, __Read-only__ The class of the Linode type.
             *
             * - `nanode`. These instances are good for low-duty workloads, where performance isn't critical.
             *
             * - `standard`. These instances are good for medium-duty workloads, and offer a good mix of performance, resources, and price.
             *
             * - `dedicated`. These instances are good for full-duty workloads where consistent performance is important.
             *
             * - `premium` (limited regions). This includes the features of a `dedicated` instance as well as the latest AMD EPYC&trade; CPUs. This ensures your applications are running on the latest hardware with consistently high performance. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "Premium Plans" in their `capabilities`.
             *
             * - `gpu` (limited regions). Linodes with dedicated NVIDIA Quadro&reg; RTX 6000 GPUs accelerate highly specialized applications such as machine learning, AI, and video transcoding. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with `GPU Linodes` in their `capabilities`.
             *
             * - `accelerated` (limited regions). These leverage the power of dedicated, application-specific integrated circuits (ASIC), starting with NETINT Video Processing Units (VPUs). They're ideal for video transcoding, media processing, and other compute-heavy workloads. Designed to offload specialized tasks, these instances deliver faster processing times and greater efficiency than traditional CPU-based solutions. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with `Accelerated` in their `capabilities`.
             *
             * - `highmem`. High Memory instances favor RAM over other resources, and can be good for memory hungry use cases like caching and in-memory databases. All High Memory plans contain dedicated CPU cores.
             *
             * > 📘
             * >
             * > - A `nanode` class is listed as a 1 GB Linode in Cloud Manager. The API, the CLI, and billing continue to refer to these as a Nanode.
             * >
             * > - A `standard` class is listed as a Shared Linode in Cloud Manager. The API, the CLI, and billing still refer to these as Standard.
             */
            class: 'nanode' | 'standard' | 'dedicated' | 'premium' | 'gpu' | 'accelerated' | 'highmem';
            /**
             * __Filterable__, __Read-only__ The Linode type's disk size in MB.
             */
            readonly disk: number;
            /**
             * __Filterable__, __Read-only__ The number of GPUs this Linode type offers.
             */
            readonly gpus: number;
            /**
             * __Read-only__ The ID representing the Linode type.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The display name for the Linode type.
             */
            readonly label: string;
            /**
             * __Filterable__, __Read-only__ Amount of RAM included in this Linode type.
             */
            readonly memory: number;
            /**
             * __Filterable__, __Read-only__ The Mbits outbound bandwidth allocation.
             */
            readonly network_out: number;
            /**
             * __Read-only__ The default cost of provisioning this Linode type. Prices are in U.S. dollars, broken down into hourly and monthly charges. Certain regions have different prices. For region-specific pricing, see `region_prices`.
             */
            readonly price: {
                /**
                 * The cost per hour in U.S. dollars.
                 */
                hourly: number;
                /**
                 * The cost per month in U.S. dollars.
                 */
                monthly: number;
            };
            region_prices: Array<{
                /**
                 * The cost per hour for this region in U.S. dollars.
                 */
                hourly: number;
                /**
                 * The unique identifier for the region.
                 */
                id: string;
                /**
                 * The cost per month for this region in U.S. dollars.
                 */
                monthly: number;
            }>;
            /**
             * __Read-only__ After a [mutate](https://techdocs.akamai.com/linode-api/reference/post-mutate-linode-instance), the Linode is upgraded to this Linode type. If `null`, this Linode type can't be mutated.
             */
            readonly successor: string | null;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount in MB.
             */
            readonly transfer: number;
            /**
             * __Filterable__, __Read-only__ The number of VCPU cores this Linode type offers.
             */
            readonly vcpus: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLinodeTypesResponse = GetLinodeTypesResponses[keyof GetLinodeTypesResponses];

export type GetLinodeTypeData = {
    body?: never;
    path: {
        /**
         * The ID of the Linode Type to look up.
         */
        typeId: string;
    };
    query?: never;
    url: '/linode/types/{typeId}';
};

export type GetLinodeTypeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLinodeTypeError = GetLinodeTypeErrors[keyof GetLinodeTypeErrors];

export type GetLinodeTypeResponses = {
    /**
     * The available Linode types, including pricing and specifications for each. Use them when [creating](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) or [resizing](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance) Linodes.
     */
    200: {
        /**
         * __Read-only__ Optional add-on services for Linodes and their associated cost.
         */
        readonly addons: {
            /**
             * __Read-only__ Details on the optional backup service available with a Linode.
             */
            readonly backups: {
                /**
                 * The default cost to enable the Backups service for this Linode type. Prices are in U.S. dollars, broken down into hourly and monthly charges. Different prices apply in different regions. For region-specific prices, see `region_prices`.
                 */
                price: {
                    /**
                     * The hourly cost for the Backups service, in U.S. dollars.
                     */
                    hourly: number;
                    /**
                     * The monthly cost for the Backups service, in U.S. dollars.
                     */
                    monthly: number;
                };
                region_prices: Array<{
                    /**
                     * The hourly cost for the Backups service in this region, in U.S. dollars.
                     */
                    hourly: number;
                    /**
                     * The unique identifier for the region.
                     */
                    id: string;
                    /**
                     * The monthly cost for the Backups service in this region, in U.S. dollars.
                     */
                    monthly: number;
                }>;
            };
        };
        /**
         * __Filterable__, __Read-only__ The class of the Linode type.
         *
         * - `nanode`. These instances are good for low-duty workloads, where performance isn't critical.
         *
         * - `standard`. These instances are good for medium-duty workloads, and offer a good mix of performance, resources, and price.
         *
         * - `dedicated`. These instances are good for full-duty workloads where consistent performance is important.
         *
         * - `premium` (limited regions). This includes the features of a `dedicated` instance as well as the latest AMD EPYC&trade; CPUs. This ensures your applications are running on the latest hardware with consistently high performance. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with "Premium Plans" in their `capabilities`.
         *
         * - `gpu` (limited regions). Linodes with dedicated NVIDIA Quadro&reg; RTX 6000 GPUs accelerate highly specialized applications such as machine learning, AI, and video transcoding. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with `GPU Linodes` in their `capabilities`.
         *
         * - `accelerated` (limited regions). These leverage the power of dedicated, application-specific integrated circuits (ASIC), starting with NETINT Video Processing Units (VPUs). They're ideal for video transcoding, media processing, and other compute-heavy workloads. Designed to offload specialized tasks, these instances deliver faster processing times and greater efficiency than traditional CPU-based solutions. Only available in [regions](https://techdocs.akamai.com/linode-api/reference/get-regions) with `Accelerated` in their `capabilities`.
         *
         * - `highmem`. High Memory instances favor RAM over other resources, and can be good for memory hungry use cases like caching and in-memory databases. All High Memory plans contain dedicated CPU cores.
         *
         * > 📘
         * >
         * > - A `nanode` class is listed as a 1 GB Linode in Cloud Manager. The API, the CLI, and billing continue to refer to these as a Nanode.
         * >
         * > - A `standard` class is listed as a Shared Linode in Cloud Manager. The API, the CLI, and billing still refer to these as Standard.
         */
        class: 'nanode' | 'standard' | 'dedicated' | 'premium' | 'gpu' | 'accelerated' | 'highmem';
        /**
         * __Filterable__, __Read-only__ The Linode type's disk size in MB.
         */
        readonly disk: number;
        /**
         * __Filterable__, __Read-only__ The number of GPUs this Linode type offers.
         */
        readonly gpus: number;
        /**
         * __Read-only__ The ID representing the Linode type.
         */
        readonly id: string;
        /**
         * __Filterable__, __Read-only__ The display name for the Linode type.
         */
        readonly label: string;
        /**
         * __Filterable__, __Read-only__ Amount of RAM included in this Linode type.
         */
        readonly memory: number;
        /**
         * __Filterable__, __Read-only__ The Mbits outbound bandwidth allocation.
         */
        readonly network_out: number;
        /**
         * __Read-only__ The default cost of provisioning this Linode type. Prices are in U.S. dollars, broken down into hourly and monthly charges. Certain regions have different prices. For region-specific pricing, see `region_prices`.
         */
        readonly price: {
            /**
             * The cost per hour in U.S. dollars.
             */
            hourly: number;
            /**
             * The cost per month in U.S. dollars.
             */
            monthly: number;
        };
        region_prices: Array<{
            /**
             * The cost per hour for this region in U.S. dollars.
             */
            hourly: number;
            /**
             * The unique identifier for the region.
             */
            id: string;
            /**
             * The cost per month for this region in U.S. dollars.
             */
            monthly: number;
        }>;
        /**
         * __Read-only__ After a [mutate](https://techdocs.akamai.com/linode-api/reference/post-mutate-linode-instance), the Linode is upgraded to this Linode type. If `null`, this Linode type can't be mutated.
         */
        readonly successor: string | null;
        /**
         * __Filterable__, __Read-only__ The monthly outbound transfer amount in MB.
         */
        readonly transfer: number;
        /**
         * __Filterable__, __Read-only__ The number of VCPU cores this Linode type offers.
         */
        readonly vcpus: number;
    };
};

export type GetLinodeTypeResponse = GetLinodeTypeResponses[keyof GetLinodeTypeResponses];

export type GetLkeClustersData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/lke/clusters';
};

export type GetLkeClustersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClustersError = GetLkeClustersErrors[keyof GetLkeClustersErrors];

export type GetLkeClustersResponses = {
    /**
     * Returns an array of all Kubernetes clusters on your Account.
     */
    200: {
        data: Array<{
            /**
             * __Write-once__ Indicates whether the Akamai App Platform is installed during creation of the LKE cluster. It defaults to `false`. If set to `true`, `control_plane.high_availability` also needs to be `true`. Automatic installation of the App Platform is only possible when creating a new cluster (not when modifying existing clusters).
             */
            apl_enabled: boolean;
            /**
             * Defines settings for the Kubernetes control plane, including enabling High Availability (HA) for the control plane.
             */
            control_plane: {
                /**
                 * __Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.
                 *
                 * > 🚧
                 * >
                 * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
                 */
                audit_logs_enabled: boolean;
                /**
                 * Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change.
                 */
                high_availability: boolean;
            };
            /**
             * __Read-only__ When this Kubernetes cluster was created.
             */
            readonly created: string;
            /**
             * __Read-only__ This Kubernetes cluster's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The desired Kubernetes version for this Kubernetes cluster in the format of `<major>.<minor>`. The latest supported patch version is deployed.
             */
            k8s_version: string;
            /**
             * __Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:
             *
             * - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"カ"`).
             *
             * - 4 byte UTF-8 characters are not supported.
             *
             * - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
             */
            label: string;
            /**
             * __Filterable__ This Kubernetes cluster's location.
             */
            region: string;
            /**
             * __Beta__, __LKE Enterprise__ The networking stack type of the Kubernetes cluster. This specifies that the cluster is IPv4 only (default) or supports both IPv4 and IPv6 (dual-stack).
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            stack_type: 'ipv4' | 'ipv4-ipv6';
            /**
             * __Beta__, __LKE Enterprise__ The ID of the VPC subnet to use for the Kubernetes cluster. This subnet must have both IPv4 and IPv6 enabled (dual-stack). When this field is specified, the cluster is deployed to the given subnet and its corresponding VPC. To specify a VPC and have a subnet auto-allocated, use `vpc_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            subnet_id: number;
            /**
             * __Filterable__ An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Beta__, __Filterable__ The desired Kubernetes tier, either `standard` or `enterprise`.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            tier: 'standard' | 'enterprise';
            /**
             * __Read-only__ When this Kubernetes cluster was updated.
             */
            readonly updated: string;
            /**
             * __Beta__, __LKE Enterprise__ The ID of the VPC to use for the Kubernetes cluster. An appropriately sized subnet is auto-allocated. To specify an existing subnet, use `subnet_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            vpc_id: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLkeClustersResponse = GetLkeClustersResponses[keyof GetLkeClustersResponses];

export type PostLkeClusterData = {
    /**
     * Configuration for the Kubernetes cluster.
     */
    body?: {
        /**
         * __Write-once__ Indicates whether the Akamai App Platform is installed during creation of the LKE cluster. It defaults to `false`. If set to `true`, `control_plane.high_availability` also needs to be `true`. Automatic installation of the App Platform is only possible when creating a new cluster (not when modifying existing clusters).
         */
        apl_enabled?: boolean;
        /**
         * Defines settings for the Kubernetes control plane, including High Availability (HA) and an IP-based Access Control List (ACL) for the control plane components.
         */
        control_plane?: {
            /**
             * Defines settings related to the IP-based ACL of the LKE cluster. The object requires the `enabled` and `addresses` keys. It also supports the optional key `revision-id`. The default policy is set to `ALLOW`, so that access controls are disabled. An empty object value (`{}`) sets default elements.
             */
            acl: {
                /**
                 * Supports keys `ipv4` and `ipv6`. Defaults to `{}`.
                 */
                addresses: {
                    /**
                     * A list of individual ipv4 addresses or CIDRs to ALLOW. Defaults to `[]`.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of individual ipv6 addresses or CIDRs to ALLOW. Defaults to `[]`.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Defines a default policy. A value of `true` results in a default policy of `DENY`. A value of `false` results in a default policy of `ALLOW`, such as for disabled access controls. It defaults to `true`. Creating a cluster with ACL, or upgrading a cluster to use ACL for LKE, is an irreversible change. Once upgraded, you can only toggle access controls with this field.
                 */
                enabled: boolean;
                /**
                 * Enables clients to track events related to ACL update requests and enforcements. Optional field. If omitted, defaults to a randomly generated string.
                 */
                'revision-id': string;
            };
            /**
             * __Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            audit_logs_enabled: boolean;
            /**
             * Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change.
             */
            high_availability: boolean;
        };
        /**
         * __Filterable__ The desired Kubernetes version for this Kubernetes cluster in the format of `<major>.<minor>`. The latest supported patch version is deployed.
         */
        k8s_version: string;
        /**
         * __Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:
         *
         * - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"カ"`).
         *
         * - 4 byte UTF-8 characters are not supported.
         *
         * - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
         */
        label: string;
        node_pools: Array<{
            /**
             * When enabled, the number of nodes automatically scales within the defined minimum and maximum values. When making a request, `max` and `min` require each other.
             */
            autoscaler?: {
                /**
                 * Whether automatic scaling is enabled for this node pool. Defaults to `false`.
                 */
                enabled: boolean;
                /**
                 * The maximum number of nodes to automatically scale to. Defaults to the value provided by the `count` field.
                 */
                max: number;
                /**
                 * The minimum number of nodes to automatically scale to. Defaults to the node pool's `count`.
                 */
                min: number;
            };
            /**
             * The number of nodes in the node pool.
             */
            count: number;
            /**
             * Local disk encryption setting for this LKE node pool. Defaults to `disabled`.
             */
            disk_encryption?: 'enabled' | 'disabled';
            /**
             * This node pool's custom disk layout. Each item in this array will create a new disk partition for each node in this node pool.
             *
             * > 📘
             * >
             * > Omit this field, except for special use cases. The disks specified here are partitions in _addition_ to the primary partition and reduce the size of the primary partition. This can lead to stability problems for the node.
             *
             * - The custom disk layout is applied to each node in this node pool.
             *
             * - The maximum number of custom disk partitions that can be configured is 7.
             *
             * - Once the requested disk partitions are allocated, the remaining disk space is allocated to the node's boot disk.
             *
             * - A node pool's custom disk layout is immutable over the lifetime of the node pool.
             */
            disks?: Array<{
                /**
                 * The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.
                 */
                size: number;
                /**
                 * This custom disk partition's filesystem type.
                 */
                type: 'raw' | 'ext4';
            }>;
            /**
             * __Beta__, __LKE Enterprise__ The LKE-specific Kubernetes version to use for the worker nodes within this node pool. This field is required when creating node pools on LKE Enterprise clusters.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            k8s_version?: string;
            /**
             * __Beta__ An optional label for this node pool. Labels can be up to 32 characters long, containing lowercase alphanumeric characters optionally interspersed with dashes.
             *
             * A node pool's label also cascades down to its nodes (Linodes). If a label is defined on the node pool, the label for each node is `<cluster-id>-<nodepool-label>-<hash>`. If not, the label is `<cluster-id>-<nodepool-id>-<hash>`.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            label?: string;
            /**
             * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
             *
             * **Label key:**
             *
             * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
             *
             * - If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the maximum total length of the label key is 128 characters. Domain labels can be up to 62 characters after the '/'. The prefix needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions.
             *
             * - If the key doesn't begin with a DNS subdomain prefix, the maximum key length is 63 characters.
             *
             * Specifying an empty object removes all previously set labels.
             *
             * **Label value:**
             *
             * - The label's value can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
             *
             * - Can be specified as an empty string value with `""`.
             */
            labels?: {
                [key: string]: string;
            };
            /**
             * __Filterable__ Organizational tags to apply to this node pool. When you apply tags to LKE Enterprise node pools, they also apply to the nodes (Linodes) within the node pool. Tags don't cascade down to nodes in non-Enterprise node pools.
             */
            tags?: Array<string>;
            /**
             * Kubernetes taints to add to node pool nodes. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods. To learn more, review [Add labels and taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
             *
             * Specifying an empty array (`[]`) removes all previously set taints.
             */
            taints?: Array<{
                /**
                 * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
                 */
                effect: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
                /**
                 * The Kubernetes taint key.
                 *
                 * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
                 *
                 * - If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the prefix part needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions and be a maximum of 253 characters.
                 */
                key: string;
                /**
                 * The Kubernetes taint value.
                 *
                 * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
                 *
                 * - Can be specified as an empty string value with `""`.
                 */
                value: string;
            }>;
            /**
             * The Linode Type for all of the nodes in the node pool.
             */
            type: string;
            /**
             * __Beta__, __LKE Enterprise__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required when creating node pools on LKE Enterprise clusters.
             *
             * - `rolling_update`: Immediately triggers a recycle of this node pool when the Kubernetes version is updated.
             * - `on_recycle` (default): Does not trigger any immediate recycle. New worker nodes are created with the new Kubernetes version. Existing worker nodes will be upgraded when a recycle is manually triggered.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            update_strategy?: 'rolling_update' | 'on_recycle';
        }>;
        /**
         * __Filterable__ This Kubernetes cluster's location.
         */
        region: string;
        /**
         * __Beta__, __LKE Enterprise__ The networking stack type of the Kubernetes cluster. This specifies that the cluster is IPv4 only (default) or supports both IPv4 and IPv6 (dual-stack).
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        stack_type?: 'ipv4' | 'ipv4-ipv6';
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC subnet to use for the Kubernetes cluster. This subnet must have both IPv4 and IPv6 enabled (dual-stack). When this field is specified, the cluster is deployed to the given subnet and its corresponding VPC. To specify a VPC and have a subnet auto-allocated, use `vpc_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        subnet_id?: number;
        /**
         * __Filterable__ An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only.
         */
        tags?: Array<string>;
        /**
         * __Beta__, __Filterable__ The desired Kubernetes tier, either `standard` or `enterprise`.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        tier?: 'standard' | 'enterprise';
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC to use for the Kubernetes cluster. An appropriately sized subnet is auto-allocated. To specify an existing subnet, use `subnet_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        vpc_id?: number;
    };
    path?: never;
    query?: never;
    url: '/lke/clusters';
};

export type PostLkeClusterErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLkeClusterError = PostLkeClusterErrors[keyof PostLkeClusterErrors];

export type PostLkeClusterResponses = {
    /**
     * A Kubernetes cluster.
     */
    200: {
        /**
         * __Write-once__ Indicates whether the Akamai App Platform is installed during creation of the LKE cluster. It defaults to `false`. If set to `true`, `control_plane.high_availability` also needs to be `true`. Automatic installation of the App Platform is only possible when creating a new cluster (not when modifying existing clusters).
         */
        apl_enabled: boolean;
        /**
         * Defines settings for the Kubernetes control plane, including enabling High Availability (HA) for the control plane.
         */
        control_plane: {
            /**
             * __Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            audit_logs_enabled: boolean;
            /**
             * Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change.
             */
            high_availability: boolean;
        };
        /**
         * __Read-only__ When this Kubernetes cluster was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Kubernetes cluster's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The desired Kubernetes version for this Kubernetes cluster in the format of `<major>.<minor>`. The latest supported patch version is deployed.
         */
        k8s_version: string;
        /**
         * __Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:
         *
         * - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"カ"`).
         *
         * - 4 byte UTF-8 characters are not supported.
         *
         * - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
         */
        label: string;
        /**
         * __Filterable__ This Kubernetes cluster's location.
         */
        region: string;
        /**
         * __Beta__, __LKE Enterprise__ The networking stack type of the Kubernetes cluster. This specifies that the cluster is IPv4 only (default) or supports both IPv4 and IPv6 (dual-stack).
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        stack_type: 'ipv4' | 'ipv4-ipv6';
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC subnet to use for the Kubernetes cluster. This subnet must have both IPv4 and IPv6 enabled (dual-stack). When this field is specified, the cluster is deployed to the given subnet and its corresponding VPC. To specify a VPC and have a subnet auto-allocated, use `vpc_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        subnet_id: number;
        /**
         * __Filterable__ An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Beta__, __Filterable__ The desired Kubernetes tier, either `standard` or `enterprise`.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        tier: 'standard' | 'enterprise';
        /**
         * __Read-only__ When this Kubernetes cluster was updated.
         */
        readonly updated: string;
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC to use for the Kubernetes cluster. An appropriately sized subnet is auto-allocated. To specify an existing subnet, use `subnet_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        vpc_id: number;
    };
};

export type PostLkeClusterResponse = PostLkeClusterResponses[keyof PostLkeClusterResponses];

export type DeleteLkeClusterData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}';
};

export type DeleteLkeClusterErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLkeClusterError = DeleteLkeClusterErrors[keyof DeleteLkeClusterErrors];

export type DeleteLkeClusterResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLkeClusterResponse = DeleteLkeClusterResponses[keyof DeleteLkeClusterResponses];

export type GetLkeClusterData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}';
};

export type GetLkeClusterErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterError = GetLkeClusterErrors[keyof GetLkeClusterErrors];

export type GetLkeClusterResponses = {
    /**
     * A Kubernetes cluster.
     */
    200: {
        /**
         * __Write-once__ Indicates whether the Akamai App Platform is installed during creation of the LKE cluster. It defaults to `false`. If set to `true`, `control_plane.high_availability` also needs to be `true`. Automatic installation of the App Platform is only possible when creating a new cluster (not when modifying existing clusters).
         */
        apl_enabled: boolean;
        /**
         * Defines settings for the Kubernetes control plane, including enabling High Availability (HA) for the control plane.
         */
        control_plane: {
            /**
             * __Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            audit_logs_enabled: boolean;
            /**
             * Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change.
             */
            high_availability: boolean;
        };
        /**
         * __Read-only__ When this Kubernetes cluster was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Kubernetes cluster's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The desired Kubernetes version for this Kubernetes cluster in the format of `<major>.<minor>`. The latest supported patch version is deployed.
         */
        k8s_version: string;
        /**
         * __Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:
         *
         * - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"カ"`).
         *
         * - 4 byte UTF-8 characters are not supported.
         *
         * - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
         */
        label: string;
        /**
         * __Filterable__ This Kubernetes cluster's location.
         */
        region: string;
        /**
         * __Beta__, __LKE Enterprise__ The networking stack type of the Kubernetes cluster. This specifies that the cluster is IPv4 only (default) or supports both IPv4 and IPv6 (dual-stack).
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        stack_type: 'ipv4' | 'ipv4-ipv6';
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC subnet to use for the Kubernetes cluster. This subnet must have both IPv4 and IPv6 enabled (dual-stack). When this field is specified, the cluster is deployed to the given subnet and its corresponding VPC. To specify a VPC and have a subnet auto-allocated, use `vpc_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        subnet_id: number;
        /**
         * __Filterable__ An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Beta__, __Filterable__ The desired Kubernetes tier, either `standard` or `enterprise`.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        tier: 'standard' | 'enterprise';
        /**
         * __Read-only__ When this Kubernetes cluster was updated.
         */
        readonly updated: string;
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC to use for the Kubernetes cluster. An appropriately sized subnet is auto-allocated. To specify an existing subnet, use `subnet_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        vpc_id: number;
    };
};

export type GetLkeClusterResponse = GetLkeClusterResponses[keyof GetLkeClusterResponses];

export type PutLkeClusterData = {
    /**
     * The fields to update the Kubernetes cluster.
     */
    body?: {
        /**
         * Defines settings for the Kubernetes control plane, including High Availability (HA) and an IP-based Access Control List (ACL) for the control plane components.
         *
         * When upgrading pre-existing LKE clusters to use the HA Control Plane, the following changes will additionally occur:
         *
         * - All nodes will be deleted and new nodes will be created to replace them.
         *
         * - Any local storage (such as `hostPath` volumes) will be erased.
         *
         * - The upgrade process may take several minutes to complete, as nodes will be replaced on a rolling basis.
         *
         * When upgrading pre-existing LKE clusters to use the control plane ACL for the first time, it may take several hours for external clients to respect the access control settings. This is partly due to delays from DNS propagation.
         */
        control_plane: {
            /**
             * Defines settings related to the IP-based ACL of the LKE cluster. The object requires the `enabled` and `addresses` keys. It also supports the optional key `revision-id`. The default policy is set to `ALLOW`, so that access controls are disabled. An empty object value (`{}`) sets default elements.
             */
            acl: {
                /**
                 * Supports keys `ipv4` and `ipv6`. Defaults to `{}`.
                 */
                addresses: {
                    /**
                     * A list of individual ipv4 addresses or CIDRs to ALLOW. Defaults to `[]`.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of individual ipv6 addresses or CIDRs to ALLOW. Defaults to `[]`.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Defines a default policy. A value of `true` results in a default policy of `DENY`. A value of `false` results in a default policy of `ALLOW`, such as for disabled access controls. It defaults to `true`. Creating a cluster with ACL, or upgrading a cluster to use ACL for LKE, is an irreversible change. Once upgraded, you can only toggle access controls with this field.
                 */
                enabled: boolean;
                /**
                 * Enables clients to track events related to ACL update requests and enforcements. Optional field. If omitted, defaults to a randomly generated string.
                 */
                'revision-id': string;
            };
            /**
             * __Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            audit_logs_enabled: boolean;
            /**
             * Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change.
             */
            high_availability: boolean;
        };
        /**
         * The desired Kubernetes version for this Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;. New and recycled Nodes in this cluster will be installed with the latest available patch for the Cluster's Kubernetes version.
         *
         * When upgrading the Kubernetes version, only the next latest minor version following the current version can be deployed. For example, a cluster with Kubernetes version 1.29 can be upgraded to version 1.30, but not directly to 1.31.
         *
         * The Kubernetes version of a cluster can not be downgraded.
         */
        k8s_version: string;
        /**
         * __Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:
         *
         * - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"カ"`).
         *
         * - 4 byte UTF-8 characters are not supported.
         *
         * - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
         */
        label: string;
        /**
         * An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only. To delete a tag, exclude it from your `tags` array.
         */
        tags: Array<string>;
    };
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}';
};

export type PutLkeClusterResponses = {
    /**
     * A Kubernetes cluster.
     */
    200: {
        /**
         * __Write-once__ Indicates whether the Akamai App Platform is installed during creation of the LKE cluster. It defaults to `false`. If set to `true`, `control_plane.high_availability` also needs to be `true`. Automatic installation of the App Platform is only possible when creating a new cluster (not when modifying existing clusters).
         */
        apl_enabled: boolean;
        /**
         * Defines settings for the Kubernetes control plane, including enabling High Availability (HA) for the control plane.
         */
        control_plane: {
            /**
             * __Beta__, __LKE Enterprise__ Enables audit logs on the cluster's control plane. These logs provide detailed information about user access and the operations performed on the cluster, which can be useful for security and compliance purposes.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
             */
            audit_logs_enabled: boolean;
            /**
             * Enables High Availability for the cluster's control plane components. It defaults to `false`. Enabling High Availability for LKE is an irreversible change.
             */
            high_availability: boolean;
        };
        /**
         * __Read-only__ When this Kubernetes cluster was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Kubernetes cluster's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The desired Kubernetes version for this Kubernetes cluster in the format of `<major>.<minor>`. The latest supported patch version is deployed.
         */
        k8s_version: string;
        /**
         * __Filterable__ This Kubernetes cluster's unique label for display purposes only. Labels have the following constraints:
         *
         * - UTF-8 characters will be returned by the API using escape sequences of their Unicode code points. For example, the Japanese character _か_ is 3 bytes in UTF-8 (`0xE382AB`). Its Unicode code point is 2 bytes (`0x30AB`). APIv4 supports this character and the API will return it as the escape sequence using six 1 byte characters which represent 2 bytes of Unicode code point (`"カ"`).
         *
         * - 4 byte UTF-8 characters are not supported.
         *
         * - If the label is entirely composed of UTF-8 characters, the API response will return the code points using up to 193 1 byte characters.
         */
        label: string;
        /**
         * __Filterable__ This Kubernetes cluster's location.
         */
        region: string;
        /**
         * __Beta__, __LKE Enterprise__ The networking stack type of the Kubernetes cluster. This specifies that the cluster is IPv4 only (default) or supports both IPv4 and IPv6 (dual-stack).
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        stack_type: 'ipv4' | 'ipv4-ipv6';
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC subnet to use for the Kubernetes cluster. This subnet must have both IPv4 and IPv6 enabled (dual-stack). When this field is specified, the cluster is deployed to the given subnet and its corresponding VPC. To specify a VPC and have a subnet auto-allocated, use `vpc_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        subnet_id: number;
        /**
         * __Filterable__ An array of tags applied to the Kubernetes cluster. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Beta__, __Filterable__ The desired Kubernetes tier, either `standard` or `enterprise`.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        tier: 'standard' | 'enterprise';
        /**
         * __Read-only__ When this Kubernetes cluster was updated.
         */
        readonly updated: string;
        /**
         * __Beta__, __LKE Enterprise__ The ID of the VPC to use for the Kubernetes cluster. An appropriately sized subnet is auto-allocated. To specify an existing subnet, use `subnet_id` instead. If `subnet_id` and `vpc_id` are both unspecified, a new VPC and subnet are auto-allocated for the cluster.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        vpc_id: number;
    };
};

export type PutLkeClusterResponse = PutLkeClusterResponses[keyof PutLkeClusterResponses];

export type GetLkeClusterApiEndpointsData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/api-endpoints';
};

export type GetLkeClusterApiEndpointsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterApiEndpointsError = GetLkeClusterApiEndpointsErrors[keyof GetLkeClusterApiEndpointsErrors];

export type GetLkeClusterApiEndpointsResponses = {
    /**
     * Returns the Kubernetes API server endpoints for this cluster.
     */
    200: {
        /**
         * The Kubernetes API server endpoints for this cluster.
         */
        data: Array<{
            /**
             * __Read-only__ A Kubernetes API server endpoint for this cluster.
             */
            readonly endpoint: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLkeClusterApiEndpointsResponse = GetLkeClusterApiEndpointsResponses[keyof GetLkeClusterApiEndpointsResponses];

export type DeleteLkeClusterAclData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/control_plane_acl';
};

export type DeleteLkeClusterAclErrors = {
    /**
     * If the cluster was not created or updated to enable the ACL feature, this request returns a 400 (Bad Request) error with an appropriate message, such as `Cluster does not support Control Plane ACL`.
     */
    400: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * A string explaining that the cluster does not support Control Plane ACL.
             */
            reason: string;
        }>;
    };
    /**
     * If the cluster does not exist, this request returns a 404 (Not Found) error.
     */
    404: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * A string explaining that the cluster does not exist.
             */
            reason: string;
        }>;
    };
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLkeClusterAclError = DeleteLkeClusterAclErrors[keyof DeleteLkeClusterAclErrors];

export type DeleteLkeClusterAclResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLkeClusterAclResponse = DeleteLkeClusterAclResponses[keyof DeleteLkeClusterAclResponses];

export type GetLkeClusterAclData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/control_plane_acl';
};

export type GetLkeClusterAclErrors = {
    /**
     * If the cluster was not created or updated to enable the ACL feature, this request returns a 400 (Bad Request) error with an appropriate message, such as `Cluster does not support Control Plane ACL`.
     */
    400: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * A string explaining that the cluster does not support Control Plane ACL.
             */
            reason: string;
        }>;
    };
    /**
     * If the cluster does not exist, this request returns a 404 (Not Found) error.
     */
    404: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * A string explaining that the cluster does not exist.
             */
            reason: string;
        }>;
    };
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterAclError = GetLkeClusterAclErrors[keyof GetLkeClusterAclErrors];

export type GetLkeClusterAclResponses = {
    /**
     * Returns a single cluster's control plane access control list. The optional field `revision-id` provided will be reflected on GET response when (and only after) the ACL stanza is verified as enforced.
     */
    200: {
        /**
         * Defines settings related to the IP-based ACL of the LKE cluster. The object requires the `enabled` and `addresses` keys. It also supports the optional key `revision-id`. The default policy is set to `ALLOW`, so that access controls are disabled. An empty object value (`{}`) sets default elements.
         */
        acl?: {
            /**
             * Supports keys `ipv4` and `ipv6`. Defaults to `{}`.
             */
            addresses: {
                /**
                 * A list of individual ipv4 addresses or CIDRs to ALLOW. Defaults to `[]`.
                 */
                ipv4: Array<string>;
                /**
                 * A list of individual ipv6 addresses or CIDRs to ALLOW. Defaults to `[]`.
                 */
                ipv6: Array<string>;
            };
            /**
             * Defines a default policy. A value of `true` results in a default policy of `DENY`. A value of `false` results in a default policy of `ALLOW`, such as for disabled access controls. It defaults to `true`. Creating a cluster with ACL, or upgrading a cluster to use ACL for LKE, is an irreversible change. Once upgraded, you can only toggle access controls with this field.
             */
            enabled: boolean;
            /**
             * Enables clients to track events related to ACL update requests and enforcements. Optional field. If omitted, defaults to a randomly generated string.
             */
            'revision-id': string;
        };
    } & {
        acl: {
            addresses: {
                ipv4: unknown;
                ipv6: unknown;
            };
            'revision-id': unknown;
        };
    };
};

export type GetLkeClusterAclResponse = GetLkeClusterAclResponses[keyof GetLkeClusterAclResponses];

export type PutLkeClusterAclData = {
    /**
     * Defines settings for the Kubernetes control plane, including an IP-based Access Control List (ACL) for the control plane components.
     */
    body?: {
        /**
         * Defines settings related to the IP-based ACL of the LKE cluster. The object requires the `enabled` and `addresses` keys. It also supports the optional key `revision-id`. The default policy is set to `ALLOW`, so that access controls are disabled. An empty object value (`{}`) sets default elements.
         */
        acl: {
            /**
             * Supports keys `ipv4` and `ipv6`. Defaults to `{}`.
             */
            addresses: {
                /**
                 * A list of individual ipv4 addresses or CIDRs to ALLOW. Defaults to `[]`.
                 */
                ipv4: Array<string>;
                /**
                 * A list of individual ipv6 addresses or CIDRs to ALLOW. Defaults to `[]`.
                 */
                ipv6: Array<string>;
            };
            /**
             * Defines a default policy. A value of `true` results in a default policy of `DENY`. A value of `false` results in a default policy of `ALLOW`, such as for disabled access controls. It defaults to `true`. Creating a cluster with ACL, or upgrading a cluster to use ACL for LKE, is an irreversible change. Once upgraded, you can only toggle access controls with this field.
             */
            enabled: boolean;
            /**
             * Enables clients to track events related to ACL update requests and enforcements. Optional field. If omitted, defaults to a randomly generated string.
             */
            'revision-id': string;
        };
    };
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/control_plane_acl';
};

export type PutLkeClusterAclErrors = {
    /**
     * If the cluster was not created or updated to enable the ACL feature, this request returns a 400 (Bad Request) error with an appropriate message, such as `Cluster does not support Control Plane ACL`.
     */
    400: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * A string explaining that the cluster does not support Control Plane ACL.
             */
            reason: string;
        }>;
    };
    /**
     * If the cluster does not exist, this request returns a 404 (Not Found) error.
     */
    404: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * A string explaining that the cluster does not exist.
             */
            reason: string;
        }>;
    };
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLkeClusterAclError = PutLkeClusterAclErrors[keyof PutLkeClusterAclErrors];

export type PutLkeClusterAclResponses = {
    /**
     * Returns a single cluster's control plane access control list. The optional field `revision-id` provided will be reflected on GET response when (and only after) the ACL stanza is verified as enforced.
     */
    200: {
        /**
         * Defines settings related to the IP-based ACL of the LKE cluster. The object requires the `enabled` and `addresses` keys. It also supports the optional key `revision-id`. The default policy is set to `ALLOW`, so that access controls are disabled. An empty object value (`{}`) sets default elements.
         */
        acl?: {
            /**
             * Supports keys `ipv4` and `ipv6`. Defaults to `{}`.
             */
            addresses: {
                /**
                 * A list of individual ipv4 addresses or CIDRs to ALLOW. Defaults to `[]`.
                 */
                ipv4: Array<string>;
                /**
                 * A list of individual ipv6 addresses or CIDRs to ALLOW. Defaults to `[]`.
                 */
                ipv6: Array<string>;
            };
            /**
             * Defines a default policy. A value of `true` results in a default policy of `DENY`. A value of `false` results in a default policy of `ALLOW`, such as for disabled access controls. It defaults to `true`. Creating a cluster with ACL, or upgrading a cluster to use ACL for LKE, is an irreversible change. Once upgraded, you can only toggle access controls with this field.
             */
            enabled: boolean;
            /**
             * Enables clients to track events related to ACL update requests and enforcements. Optional field. If omitted, defaults to a randomly generated string.
             */
            'revision-id': string;
        };
    } & {
        acl: {
            addresses: {
                ipv4: unknown;
                ipv6: unknown;
            };
            'revision-id': unknown;
        };
    };
};

export type PutLkeClusterAclResponse = PutLkeClusterAclResponses[keyof PutLkeClusterAclResponses];

export type GetLkeClusterDashboardData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/dashboard';
};

export type GetLkeClusterDashboardErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterDashboardError = GetLkeClusterDashboardErrors[keyof GetLkeClusterDashboardErrors];

export type GetLkeClusterDashboardResponses = {
    /**
     * Returns a Kubernetes Cluster Dashboard URL.
     */
    200: {
        /**
         * The Cluster Dashboard access URL.
         */
        url: string;
    };
};

export type GetLkeClusterDashboardResponse = GetLkeClusterDashboardResponses[keyof GetLkeClusterDashboardResponses];

export type DeleteLkeClusterKubeconfigData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/kubeconfig';
};

export type DeleteLkeClusterKubeconfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLkeClusterKubeconfigError = DeleteLkeClusterKubeconfigErrors[keyof DeleteLkeClusterKubeconfigErrors];

export type DeleteLkeClusterKubeconfigResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLkeClusterKubeconfigResponse = DeleteLkeClusterKubeconfigResponses[keyof DeleteLkeClusterKubeconfigResponses];

export type GetLkeClusterKubeconfigData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/kubeconfig';
};

export type GetLkeClusterKubeconfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterKubeconfigError = GetLkeClusterKubeconfigErrors[keyof GetLkeClusterKubeconfigErrors];

export type GetLkeClusterKubeconfigResponses = {
    /**
     * Returns the Base64-encoded Kubeconfig file for this Kubernetes cluster.
     */
    200: {
        /**
         * __Read-only__ The Base64-encoded Kubeconfig file for this Cluster.
         */
        readonly kubeconfig: string;
    };
};

export type GetLkeClusterKubeconfigResponse = GetLkeClusterKubeconfigResponses[keyof GetLkeClusterKubeconfigResponses];

export type DeleteLkeClusterNodeData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster containing the Node.
         */
        clusterId: number;
        /**
         * The ID of the Node to access.
         */
        nodeId: string;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/nodes/{nodeId}';
};

export type DeleteLkeClusterNodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLkeClusterNodeError = DeleteLkeClusterNodeErrors[keyof DeleteLkeClusterNodeErrors];

export type DeleteLkeClusterNodeResponses = {
    /**
     * Delete successful.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLkeClusterNodeResponse = DeleteLkeClusterNodeResponses[keyof DeleteLkeClusterNodeResponses];

export type GetLkeClusterNodeData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster containing the Node.
         */
        clusterId: number;
        /**
         * The ID of the Node to access.
         */
        nodeId: string;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/nodes/{nodeId}';
};

export type GetLkeClusterNodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterNodeError = GetLkeClusterNodeErrors[keyof GetLkeClusterNodeErrors];

export type GetLkeClusterNodeResponses = {
    /**
     * __Read-only__ Status information for a Node which is a member of a Kubernetes cluster.
     */
    readonly 200: {
        /**
         * The Node's ID.
         */
        id: string;
        /**
         * The Linode's ID. When no Linode is currently provisioned for this Node, this will be `null`.
         */
        instance_id: number | null;
        /**
         * The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.
         *
         * `not_ready` indicates that the Linode is still being created.
         *
         * `ready` indicates that the Linode has successfully been created and is running Kubernetes software.
         */
        status: 'ready' | 'not_ready';
    };
};

export type GetLkeClusterNodeResponse = GetLkeClusterNodeResponses[keyof GetLkeClusterNodeResponses];

export type PostLkeClusterNodeRecycleData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster containing the Node.
         */
        clusterId: number;
        /**
         * ID of the Node to be recycled.
         */
        nodeId: string;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/nodes/{nodeId}/recycle';
};

export type PostLkeClusterNodeRecycleErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLkeClusterNodeRecycleError = PostLkeClusterNodeRecycleErrors[keyof PostLkeClusterNodeRecycleErrors];

export type PostLkeClusterNodeRecycleResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostLkeClusterNodeRecycleResponse = PostLkeClusterNodeRecycleResponses[keyof PostLkeClusterNodeRecycleResponses];

export type GetLkeClusterPoolsData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/pools';
};

export type GetLkeClusterPoolsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeClusterPoolsError = GetLkeClusterPoolsErrors[keyof GetLkeClusterPoolsErrors];

export type GetLkeClusterPoolsResponses = {
    /**
     * Returns all node pools in this Kubernetes cluster.
     */
    200: {
        data: Array<{
            /**
             * When enabled, the number of nodes autoscales within the defined minimum and maximum values.
             */
            autoscaler: {
                /**
                 * Whether autoscaling is enabled for this node pool. Defaults to `false`.
                 */
                enabled: boolean;
                /**
                 * The maximum number of nodes to autoscale to. Defaults to the node pool's `count`.
                 */
                max: number;
                /**
                 * The minimum number of nodes to autoscale to. Defaults to the node pool's `count`.
                 */
                min: number;
            };
            /**
             * The number of nodes in the node pool.
             */
            count: number;
            /**
             * Indicates the local disk encryption setting for this LKE node pool.
             */
            disk_encryption: 'enabled' | 'disabled';
            /**
             * This node pool's custom disk layout.
             */
            disks: Array<{
                /**
                 * The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.
                 */
                size: number;
                /**
                 * This custom disk partition's filesystem type.
                 */
                type: 'raw' | 'ext4';
            }>;
            /**
             * __Filterable__ This node pool's unique ID.
             */
            id: number;
            /**
             * __Beta__ The Kubernetes version used for the worker nodes within this node pool.
             * > 🚧
             * >
             * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
             */
            k8s_version: string;
            /**
             * __Beta__ The optional label defined for this node Pool.
             *
             * > 🚧
             * >
             * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
             */
            label: string;
            /**
             * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
             */
            labels: {
                [key: string]: string;
            };
            /**
             * Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`.
             */
            nodes: Array<{
                /**
                 * The Node's ID.
                 */
                id: string;
                /**
                 * The Linode's ID. When no Linode is currently provisioned for this Node, this will be `null`.
                 */
                instance_id: number | null;
                /**
                 * The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.
                 *
                 * `not_ready` indicates that the Linode is still being created.
                 *
                 * `ready` indicates that the Linode has successfully been created and is running Kubernetes software.
                 */
                status: 'ready' | 'not_ready';
            }>;
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods.
             */
            taints: Array<{
                /**
                 * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
                 */
                effect?: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
                /**
                 * The Kubernetes taint key.
                 */
                key?: string;
                /**
                 * The Kubernetes taint value.
                 */
                value?: string;
            }>;
            /**
             * The Linode Type for all of the nodes in the node pool.
             */
            type: string;
            /**
             * __Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.
             */
            update_strategy: 'rolling_update' | 'on_recycle';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLkeClusterPoolsResponse = GetLkeClusterPoolsResponses[keyof GetLkeClusterPoolsResponses];

export type PostLkeClusterPoolsData = {
    /**
     * Specifies a collection of Linodes to serve as members of a Kubernetes cluster.
     */
    body: {
        /**
         * When enabled, the number of nodes automatically scales within the defined minimum and maximum values. When making a request, `max` and `min` require each other.
         */
        autoscaler?: {
            /**
             * Whether automatic scaling is enabled for this node pool. Defaults to `false`.
             */
            enabled: boolean;
            /**
             * The maximum number of nodes to automatically scale to. Defaults to the value provided by the `count` field.
             */
            max: number;
            /**
             * The minimum number of nodes to automatically scale to. Defaults to the node pool's `count`.
             */
            min: number;
        };
        /**
         * The number of nodes in the node pool.
         */
        count: number;
        /**
         * Local disk encryption setting for this LKE node pool. Defaults to `disabled`.
         */
        disk_encryption?: 'enabled' | 'disabled';
        /**
         * This node pool's custom disk layout. Each item in this array will create a new disk partition for each node in this node pool.
         *
         * > 📘
         * >
         * > Omit this field, except for special use cases. The disks specified here are partitions in _addition_ to the primary partition and reduce the size of the primary partition. This can lead to stability problems for the node.
         *
         * - The custom disk layout is applied to each node in this node pool.
         *
         * - The maximum number of custom disk partitions that can be configured is 7.
         *
         * - Once the requested disk partitions are allocated, the remaining disk space is allocated to the node's boot disk.
         *
         * - A node pool's custom disk layout is immutable over the lifetime of the node pool.
         */
        disks?: Array<{
            /**
             * The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.
             */
            size: number;
            /**
             * This custom disk partition's filesystem type.
             */
            type: 'raw' | 'ext4';
        }>;
        /**
         * __Beta__, __LKE Enterprise__ The LKE-specific Kubernetes version to use for the worker nodes within this node pool. This field is required when creating node pools on LKE Enterprise clusters.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        k8s_version?: string;
        /**
         * __Beta__ An optional label for this node pool. Labels can be up to 32 characters long, containing lowercase alphanumeric characters optionally interspersed with dashes.
         *
         * A node pool's label also cascades down to its nodes (Linodes). If a label is defined on the node pool, the label for each node is `<cluster-id>-<nodepool-label>-<hash>`. If not, the label is `<cluster-id>-<nodepool-id>-<hash>`.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        label?: string;
        /**
         * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         *
         * **Label key:**
         *
         * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
         *
         * - If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the maximum total length of the label key is 128 characters. Domain labels can be up to 62 characters after the '/'. The prefix needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions.
         *
         * - If the key doesn't begin with a DNS subdomain prefix, the maximum key length is 63 characters.
         *
         * Specifying an empty object removes all previously set labels.
         *
         * **Label value:**
         *
         * - The label's value can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
         *
         * - Can be specified as an empty string value with `""`.
         */
        labels?: {
            [key: string]: string;
        };
        /**
         * __Filterable__ Organizational tags to apply to this node pool. When you apply tags to LKE Enterprise node pools, they also apply to the nodes (Linodes) within the node pool. Tags don't cascade down to nodes in non-Enterprise node pools.
         */
        tags?: Array<string>;
        /**
         * Kubernetes taints to add to node pool nodes. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods. To learn more, review [Add labels and taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         *
         * Specifying an empty array (`[]`) removes all previously set taints.
         */
        taints?: Array<{
            /**
             * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
             */
            effect: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
            /**
             * The Kubernetes taint key.
             *
             * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
             *
             * - If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the prefix part needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions and be a maximum of 253 characters.
             */
            key: string;
            /**
             * The Kubernetes taint value.
             *
             * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
             *
             * - Can be specified as an empty string value with `""`.
             */
            value: string;
        }>;
        /**
         * The Linode Type for all of the nodes in the node pool.
         */
        type: string;
        /**
         * __Beta__, __LKE Enterprise__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required when creating node pools on LKE Enterprise clusters.
         *
         * - `rolling_update`: Immediately triggers a recycle of this node pool when the Kubernetes version is updated.
         * - `on_recycle` (default): Does not trigger any immediate recycle. New worker nodes are created with the new Kubernetes version. Existing worker nodes will be upgraded when a recycle is manually triggered.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and can only be used with accounts that have been enrolled in the LKE Enterprise LA. Call the URL with the `apiVersion` path parameter set to `v4beta`.
         */
        update_strategy?: 'rolling_update' | 'on_recycle';
    };
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/pools';
};

export type PostLkeClusterPoolsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLkeClusterPoolsError = PostLkeClusterPoolsErrors[keyof PostLkeClusterPoolsErrors];

export type PostLkeClusterPoolsResponses = {
    /**
     * The set of node pools that are members of the Kubernetes cluster. Node pools consist of a Linode type, the number of Linodes to deploy of that type, and additional status information.
     */
    200: {
        /**
         * When enabled, the number of nodes autoscales within the defined minimum and maximum values.
         */
        autoscaler: {
            /**
             * Whether autoscaling is enabled for this node pool. Defaults to `false`.
             */
            enabled: boolean;
            /**
             * The maximum number of nodes to autoscale to. Defaults to the node pool's `count`.
             */
            max: number;
            /**
             * The minimum number of nodes to autoscale to. Defaults to the node pool's `count`.
             */
            min: number;
        };
        /**
         * The number of nodes in the node pool.
         */
        count: number;
        /**
         * Indicates the local disk encryption setting for this LKE node pool.
         */
        disk_encryption: 'enabled' | 'disabled';
        /**
         * This node pool's custom disk layout.
         */
        disks: Array<{
            /**
             * The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.
             */
            size: number;
            /**
             * This custom disk partition's filesystem type.
             */
            type: 'raw' | 'ext4';
        }>;
        /**
         * __Filterable__ This node pool's unique ID.
         */
        id: number;
        /**
         * __Beta__ The Kubernetes version used for the worker nodes within this node pool.
         * > 🚧
         * >
         * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
         */
        k8s_version: string;
        /**
         * __Beta__ The optional label defined for this node Pool.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
         */
        label: string;
        /**
         * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         */
        labels: {
            [key: string]: string;
        };
        /**
         * Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`.
         */
        nodes: Array<{
            /**
             * The Node's ID.
             */
            id: string;
            /**
             * The Linode's ID. When no Linode is currently provisioned for this Node, this will be `null`.
             */
            instance_id: number | null;
            /**
             * The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.
             *
             * `not_ready` indicates that the Linode is still being created.
             *
             * `ready` indicates that the Linode has successfully been created and is running Kubernetes software.
             */
            status: 'ready' | 'not_ready';
        }>;
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods.
         */
        taints: Array<{
            /**
             * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
             */
            effect?: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
            /**
             * The Kubernetes taint key.
             */
            key?: string;
            /**
             * The Kubernetes taint value.
             */
            value?: string;
        }>;
        /**
         * The Linode Type for all of the nodes in the node pool.
         */
        type: string;
        /**
         * __Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.
         */
        update_strategy: 'rolling_update' | 'on_recycle';
    };
};

export type PostLkeClusterPoolsResponse = PostLkeClusterPoolsResponses[keyof PostLkeClusterPoolsResponses];

export type DeleteLkeNodePoolData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
        /**
         * ID of the Pool to look up.
         */
        poolId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/pools/{poolId}';
};

export type DeleteLkeNodePoolErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLkeNodePoolError = DeleteLkeNodePoolErrors[keyof DeleteLkeNodePoolErrors];

export type DeleteLkeNodePoolResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLkeNodePoolResponse = DeleteLkeNodePoolResponses[keyof DeleteLkeNodePoolResponses];

export type GetLkeNodePoolData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
        /**
         * ID of the Pool to look up.
         */
        poolId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/pools/{poolId}';
};

export type GetLkeNodePoolResponses = {
    /**
     * The set of node pools that are members of the Kubernetes cluster. Node pools consist of a Linode type, the number of Linodes to deploy of that type, and additional status information.
     */
    200: {
        /**
         * When enabled, the number of nodes autoscales within the defined minimum and maximum values.
         */
        autoscaler: {
            /**
             * Whether autoscaling is enabled for this node pool. Defaults to `false`.
             */
            enabled: boolean;
            /**
             * The maximum number of nodes to autoscale to. Defaults to the node pool's `count`.
             */
            max: number;
            /**
             * The minimum number of nodes to autoscale to. Defaults to the node pool's `count`.
             */
            min: number;
        };
        /**
         * The number of nodes in the node pool.
         */
        count: number;
        /**
         * Indicates the local disk encryption setting for this LKE node pool.
         */
        disk_encryption: 'enabled' | 'disabled';
        /**
         * This node pool's custom disk layout.
         */
        disks: Array<{
            /**
             * The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.
             */
            size: number;
            /**
             * This custom disk partition's filesystem type.
             */
            type: 'raw' | 'ext4';
        }>;
        /**
         * __Filterable__ This node pool's unique ID.
         */
        id: number;
        /**
         * __Beta__ The Kubernetes version used for the worker nodes within this node pool.
         * > 🚧
         * >
         * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
         */
        k8s_version: string;
        /**
         * __Beta__ The optional label defined for this node Pool.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
         */
        label: string;
        /**
         * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         */
        labels: {
            [key: string]: string;
        };
        /**
         * Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`.
         */
        nodes: Array<{
            /**
             * The Node's ID.
             */
            id: string;
            /**
             * The Linode's ID. When no Linode is currently provisioned for this Node, this will be `null`.
             */
            instance_id: number | null;
            /**
             * The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.
             *
             * `not_ready` indicates that the Linode is still being created.
             *
             * `ready` indicates that the Linode has successfully been created and is running Kubernetes software.
             */
            status: 'ready' | 'not_ready';
        }>;
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods.
         */
        taints: Array<{
            /**
             * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
             */
            effect?: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
            /**
             * The Kubernetes taint key.
             */
            key?: string;
            /**
             * The Kubernetes taint value.
             */
            value?: string;
        }>;
        /**
         * The Linode Type for all of the nodes in the node pool.
         */
        type: string;
        /**
         * __Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.
         */
        update_strategy: 'rolling_update' | 'on_recycle';
    };
};

export type GetLkeNodePoolResponse = GetLkeNodePoolResponses[keyof GetLkeNodePoolResponses];

export type PutLkeNodePoolData = {
    /**
     * The fields to update.
     */
    body?: {
        /**
         * When enabled, the number of nodes automatically scales within the defined minimum and maximum values. When making a request, `max` and `min` require each other.
         */
        autoscaler: {
            /**
             * Whether automatic scaling is enabled for this node pool. Defaults to `false`.
             */
            enabled: boolean;
            /**
             * The maximum number of nodes to automatically scale to. Defaults to the value provided by the `count` field.
             */
            max: number;
            /**
             * The minimum number of nodes to automatically scale to. Defaults to the node pool's `count`.
             */
            min: number;
        };
        /**
         * The number of nodes in the node pool.
         */
        count: number;
        /**
         * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         *
         * **Label key:**
         *
         * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
         *
         * - If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the maximum total length of the label key is 128 characters. Domain labels can be up to 62 characters after the '/'. The prefix needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions.
         *
         * - If the key doesn't begin with a DNS subdomain prefix, the maximum key length is 63 characters.
         *
         * Specifying an empty object removes all previously set labels.
         *
         * **Label value:**
         *
         * - The label's value can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
         *
         * - Can be specified as an empty string value with `""`.
         */
        labels: {
            [key: string]: string;
        };
        /**
         * __Filterable__ Organizational tags to apply to this node pool. When you apply tags to LKE Enterprise node pools, they also apply to the nodes (Linodes) within the node pool. Tags don't cascade down to nodes in non-Enterprise node pools.
         */
        tags: Array<string>;
        /**
         * Kubernetes taints to add to node pool nodes. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods. To learn more, review [Add labels and taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         *
         * Specifying an empty array (`[]`) removes all previously set taints.
         */
        taints: Array<{
            /**
             * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
             */
            effect: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
            /**
             * The Kubernetes taint key.
             *
             * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
             *
             * - If the key begins with a DNS subdomain prefix followed by a single slash, for example `example.com/my-app`, the prefix part needs to adhere to [RFC 1123](https://datatracker.ietf.org/doc/html/rfc1123) DNS subdomain restrictions and be a maximum of 253 characters.
             */
            key: string;
            /**
             * The Kubernetes taint value.
             *
             * - A key can contain alphanumeric characters, dashes (`-`), underscores (`_`), or dots (`.`). Start and end it with an alphanumeric character.
             *
             * - Can be specified as an empty string value with `""`.
             */
            value: string;
        }>;
    };
    path: {
        /**
         * ID of the Kubernetes cluster to look up.
         */
        clusterId: number;
        /**
         * ID of the Pool to look up.
         */
        poolId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/pools/{poolId}';
};

export type PutLkeNodePoolResponses = {
    /**
     * The set of node pools that are members of the Kubernetes cluster. Node pools consist of a Linode type, the number of Linodes to deploy of that type, and additional status information.
     */
    200: {
        /**
         * When enabled, the number of nodes autoscales within the defined minimum and maximum values.
         */
        autoscaler: {
            /**
             * Whether autoscaling is enabled for this node pool. Defaults to `false`.
             */
            enabled: boolean;
            /**
             * The maximum number of nodes to autoscale to. Defaults to the node pool's `count`.
             */
            max: number;
            /**
             * The minimum number of nodes to autoscale to. Defaults to the node pool's `count`.
             */
            min: number;
        };
        /**
         * The number of nodes in the node pool.
         */
        count: number;
        /**
         * Indicates the local disk encryption setting for this LKE node pool.
         */
        disk_encryption: 'enabled' | 'disabled';
        /**
         * This node pool's custom disk layout.
         */
        disks: Array<{
            /**
             * The size of this custom disk partition in MB. The size of this disk partition can't exceed the capacity of the node's plan type.
             */
            size: number;
            /**
             * This custom disk partition's filesystem type.
             */
            type: 'raw' | 'ext4';
        }>;
        /**
         * __Filterable__ This node pool's unique ID.
         */
        id: number;
        /**
         * __Beta__ The Kubernetes version used for the worker nodes within this node pool.
         * > 🚧
         * >
         * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
         */
        k8s_version: string;
        /**
         * __Beta__ The optional label defined for this node Pool.
         *
         * > 🚧
         * >
         * > This field is available as part of the beta API and is only returned for accounts that have been enrolled in the LKE Enterprise LA.
         */
        label: string;
        /**
         * Key-value pairs added as labels to nodes in the node pool. Labels help classify your nodes and easily select subsets of objects. To learn more, review [Add Labels and Taints to your LKE node pools](https://www.linode.com/docs/products/compute/kubernetes/guides/deploy-and-manage-cluster-with-the-linode-api/#add-labels-and-taints-to-your-lke-node-pools).
         */
        labels: {
            [key: string]: string;
        };
        /**
         * Status information for the nodes that are members of this node pool. If a Linode has not been provisioned for a given node slot, the `instance_id` is `null`.
         */
        nodes: Array<{
            /**
             * The Node's ID.
             */
            id: string;
            /**
             * The Linode's ID. When no Linode is currently provisioned for this Node, this will be `null`.
             */
            instance_id: number | null;
            /**
             * The creation status of this Node. This status is distinct from this Node's readiness as a Kubernetes Node Object as determined by the command `kubectl get nodes`.
             *
             * `not_ready` indicates that the Linode is still being created.
             *
             * `ready` indicates that the Linode has successfully been created and is running Kubernetes software.
             */
            status: 'ready' | 'not_ready';
        }>;
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * Kubernetes taints added to nodes in the node pool. Taints help control how pods are scheduled onto nodes, specifically allowing them to repel certain pods.
         */
        taints: Array<{
            /**
             * The Kubernetes taint effect. For `NoSchedule`, `PreferNoSchedule` and `NoExecute` descriptions, see [Kubernetes Taints and Tolerations](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
             */
            effect?: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
            /**
             * The Kubernetes taint key.
             */
            key?: string;
            /**
             * The Kubernetes taint value.
             */
            value?: string;
        }>;
        /**
         * The Linode Type for all of the nodes in the node pool.
         */
        type: string;
        /**
         * __Beta__ Determines when the worker nodes within this node pool upgrade to the latest selected Kubernetes version, after the cluster has been upgraded. This field is required for LKE Enterprise clusters but should not be used for non-enterprise LKE clusters.
         */
        update_strategy: 'rolling_update' | 'on_recycle';
    };
};

export type PutLkeNodePoolResponse = PutLkeNodePoolResponses[keyof PutLkeNodePoolResponses];

export type PostLkeClusterPoolRecycleData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster this Node Pool is attached to.
         */
        clusterId: number;
        /**
         * ID of the Node Pool to be recycled.
         */
        poolId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/pools/{poolId}/recycle';
};

export type PostLkeClusterPoolRecycleErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLkeClusterPoolRecycleError = PostLkeClusterPoolRecycleErrors[keyof PostLkeClusterPoolRecycleErrors];

export type PostLkeClusterPoolRecycleResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostLkeClusterPoolRecycleResponse = PostLkeClusterPoolRecycleResponses[keyof PostLkeClusterPoolRecycleResponses];

export type PostLkeClusterRecycleData = {
    body?: never;
    path: {
        /**
         * ID of the Kubernetes cluster which contains nodes to be recycled.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/recycle';
};

export type PostLkeClusterRecycleErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLkeClusterRecycleError = PostLkeClusterRecycleErrors[keyof PostLkeClusterRecycleErrors];

export type PostLkeClusterRecycleResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostLkeClusterRecycleResponse = PostLkeClusterRecycleResponses[keyof PostLkeClusterRecycleResponses];

export type PostLkeClusterRegenerateData = {
    /**
     * The Kubernetes Cluster Regenerate request object.
     */
    body?: {
        /**
         * Whether to delete and regenerate the Kubeconfig file for this Cluster.
         */
        kubeconfig: boolean;
        /**
         * Whether to delete and regenerate the service access token for this Cluster.
         */
        servicetoken: boolean;
    };
    path: {
        /**
         * ID of the target Kubernetes cluster.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/regenerate';
};

export type PostLkeClusterRegenerateErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLkeClusterRegenerateError = PostLkeClusterRegenerateErrors[keyof PostLkeClusterRegenerateErrors];

export type PostLkeClusterRegenerateResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostLkeClusterRegenerateResponse = PostLkeClusterRegenerateResponses[keyof PostLkeClusterRegenerateResponses];

export type DeleteLkeServiceTokenData = {
    body?: never;
    path: {
        /**
         * ID of the target Kubernetes cluster.
         */
        clusterId: number;
    };
    query?: never;
    url: '/lke/clusters/{clusterId}/servicetoken';
};

export type DeleteLkeServiceTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLkeServiceTokenError = DeleteLkeServiceTokenErrors[keyof DeleteLkeServiceTokenErrors];

export type DeleteLkeServiceTokenResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLkeServiceTokenResponse = DeleteLkeServiceTokenResponses[keyof DeleteLkeServiceTokenResponses];

export type GetLkeTiersVersionsData = {
    body?: never;
    path: {
        /**
         * __Enum__ The LKE tier to use.
         */
        tier: 'standard' | 'enterprise';
    };
    query?: never;
    url: '/lke/tiers/{tier}/versions';
};

export type GetLkeTiersVersionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeTiersVersionsError = GetLkeTiersVersionsErrors[keyof GetLkeTiersVersionsErrors];

export type GetLkeTiersVersionsResponses = {
    /**
     * __Read-only__ LKE versions for a tier.
     */
    readonly 200: {
        /**
         * __Read-only__ LKE versions for the requested tier.
         */
        readonly data: Array<{
            /**
             * __Read-only__ A Kubernetes version number available for deployment to a Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;, and the latest supported patch version.
             */
            readonly id: string;
            /**
             * __Read-only__ An LKE tier.
             */
            tier: 'standard' | 'enterprise';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLkeTiersVersionsResponse = GetLkeTiersVersionsResponses[keyof GetLkeTiersVersionsResponses];

export type GetLkeTiersVersionData = {
    body?: never;
    path: {
        /**
         * __Enum__ The LKE tier to use.
         */
        tier: 'standard' | 'enterprise';
        /**
         * The LKE version to view.
         */
        version: string;
    };
    query?: never;
    url: '/lke/tiers/{tier}/versions/{version}';
};

export type GetLkeTiersVersionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeTiersVersionError = GetLkeTiersVersionErrors[keyof GetLkeTiersVersionErrors];

export type GetLkeTiersVersionResponses = {
    /**
     * __Read-only__ LKE version for a tier.
     */
    readonly 200: {
        /**
         * __Read-only__ A Kubernetes version number available for deployment to a Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;, and the latest supported patch version.
         */
        readonly id: string;
        /**
         * __Read-only__ An LKE tier.
         */
        tier: 'standard' | 'enterprise';
    };
};

export type GetLkeTiersVersionResponse = GetLkeTiersVersionResponses[keyof GetLkeTiersVersionResponses];

export type GetLkeTypesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/lke/types';
};

export type GetLkeTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeTypesError = GetLkeTypesErrors[keyof GetLkeTypesErrors];

export type GetLkeTypesResponses = {
    /**
     * A collection of Kubernetes types.
     */
    200: {
        /**
         * The Kubernetes types.
         */
        data: Array<{
            /**
             * __Read-only__ The ID representing the Kubernetes type.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The Kubernetes type label is for display purposes only.
             */
            readonly label: string;
            /**
             * __Read-only__ The default cost of this Kubernetes type. Prices are in US dollars, broken down into hourly and monthly charges.
             *
             * Certain regions have different prices from the default. For region-specific prices, see `region_prices`.
             */
            readonly price: {
                /**
                 * __Filterable__ Cost (in US dollars) per hour.
                 */
                hourly: number;
                /**
                 * __Filterable__ Cost per month, in US dollars.
                 */
                monthly: number;
            };
            region_prices: Array<{
                /**
                 * Cost per hour for this region, in US dollars.
                 */
                hourly: number;
                /**
                 * The Region ID for these prices.
                 */
                id: string;
                /**
                 * Cost per month for this region, in US dollars.
                 */
                monthly: number;
            }>;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount, in MB.
             */
            readonly transfer: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLkeTypesResponse = GetLkeTypesResponses[keyof GetLkeTypesResponses];

export type GetLkeVersionsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/lke/versions';
};

export type GetLkeVersionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeVersionsError = GetLkeVersionsErrors[keyof GetLkeVersionsErrors];

export type GetLkeVersionsResponses = {
    /**
     * LKE versions for standard tier.
     */
    200: {
        /**
         * __Read-only__ LKE versions for standard tier.
         */
        readonly data: Array<{
            /**
             * __Read-only__ A Kubernetes version number available for deployment to a Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;, and the latest supported patch version.
             */
            readonly id: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLkeVersionsResponse = GetLkeVersionsResponses[keyof GetLkeVersionsResponses];

export type GetLkeVersionData = {
    body?: never;
    path: {
        /**
         * The LKE version to view.
         */
        version: string;
    };
    query?: never;
    url: '/lke/versions/{version}';
};

export type GetLkeVersionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLkeVersionError = GetLkeVersionErrors[keyof GetLkeVersionErrors];

export type GetLkeVersionResponses = {
    /**
     * LKE version.
     */
    200: {
        /**
         * __Read-only__ A Kubernetes version number available for deployment to a Kubernetes cluster in the format of &lt;major&gt;.&lt;minor&gt;, and the latest supported patch version.
         */
        readonly id: string;
    };
};

export type GetLkeVersionResponse = GetLkeVersionResponses[keyof GetLkeVersionResponses];

export type GetLongviewClientsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/longview/clients';
};

export type GetLongviewClientsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLongviewClientsError = GetLongviewClientsErrors[keyof GetLongviewClientsErrors];

export type GetLongviewClientsResponses = {
    /**
     * A paginated list of Longview Clients.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.
             *
             * Returns as `[REDACTED]` if you do not have read-write access to this client.
             */
            readonly api_key: string;
            /**
             * __Read-only__ The apps this Client is monitoring on your Linode. This is configured when you install the Longview Client application, and is present here for information purposes only.
             */
            readonly apps: {
                /**
                 * __Read-only__ If `true`, the Apache Longview Client module is monitoring Apache on your server.
                 */
                readonly apache: boolean;
                /**
                 * __Read-only__ If `true`, the MySQL Longview Client modules is monitoring MySQL on your server.
                 */
                readonly mysql: boolean;
                /**
                 * __Read-only__ If `true`, the Nginx Longview Client module is monitoring Nginx on your server.
                 */
                readonly nginx: boolean;
            };
            /**
             * __Read-only__ When this Longview Client was created.
             */
            readonly created: string;
            /**
             * __Read-only__ This Client's unique ID.
             */
            readonly id: number;
            /**
             * __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.
             *
             * Returns as `[REDACTED]` if you do not have read-write access to this client.
             */
            readonly install_code: string;
            /**
             * __Filterable__ This Client's unique label. This is for display purposes only.
             */
            label: string;
            /**
             * __Read-only__ When this Longview Client was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLongviewClientsResponse = GetLongviewClientsResponses[keyof GetLongviewClientsResponses];

export type PostLongviewClientData = {
    /**
     * A LongviewClient is a single monitor set up to track statistics about one of your servers.
     */
    body: {
        /**
         * __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly api_key: string;
        /**
         * __Read-only__ The apps this Client is monitoring on your Linode. This is configured when you install the Longview Client application, and is present here for information purposes only.
         */
        readonly apps: {
            /**
             * __Read-only__ If `true`, the Apache Longview Client module is monitoring Apache on your server.
             */
            readonly apache: boolean;
            /**
             * __Read-only__ If `true`, the MySQL Longview Client modules is monitoring MySQL on your server.
             */
            readonly mysql: boolean;
            /**
             * __Read-only__ If `true`, the Nginx Longview Client module is monitoring Nginx on your server.
             */
            readonly nginx: boolean;
        };
        /**
         * __Read-only__ When this Longview Client was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Client's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly install_code: string;
        /**
         * __Filterable__ This Client's unique label. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ When this Longview Client was last updated.
         */
        readonly updated: string;
    };
    path?: never;
    query?: never;
    url: '/longview/clients';
};

export type PostLongviewClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostLongviewClientError = PostLongviewClientErrors[keyof PostLongviewClientErrors];

export type PostLongviewClientResponses = {
    /**
     * A LongviewClient is a single monitor set up to track statistics about one of your servers.
     */
    200: {
        /**
         * __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly api_key: string;
        /**
         * __Read-only__ The apps this Client is monitoring on your Linode. This is configured when you install the Longview Client application, and is present here for information purposes only.
         */
        readonly apps: {
            /**
             * __Read-only__ If `true`, the Apache Longview Client module is monitoring Apache on your server.
             */
            readonly apache: boolean;
            /**
             * __Read-only__ If `true`, the MySQL Longview Client modules is monitoring MySQL on your server.
             */
            readonly mysql: boolean;
            /**
             * __Read-only__ If `true`, the Nginx Longview Client module is monitoring Nginx on your server.
             */
            readonly nginx: boolean;
        };
        /**
         * __Read-only__ When this Longview Client was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Client's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly install_code: string;
        /**
         * __Filterable__ This Client's unique label. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ When this Longview Client was last updated.
         */
        readonly updated: string;
    };
};

export type PostLongviewClientResponse = PostLongviewClientResponses[keyof PostLongviewClientResponses];

export type DeleteLongviewClientData = {
    body?: never;
    path: {
        /**
         * The Longview Client ID to access.
         */
        clientId: number;
    };
    query?: never;
    url: '/longview/clients/{clientId}';
};

export type DeleteLongviewClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteLongviewClientError = DeleteLongviewClientErrors[keyof DeleteLongviewClientErrors];

export type DeleteLongviewClientResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteLongviewClientResponse = DeleteLongviewClientResponses[keyof DeleteLongviewClientResponses];

export type GetLongviewClientData = {
    body?: never;
    path: {
        /**
         * The Longview Client ID to access.
         */
        clientId: number;
    };
    query?: never;
    url: '/longview/clients/{clientId}';
};

export type GetLongviewClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLongviewClientError = GetLongviewClientErrors[keyof GetLongviewClientErrors];

export type GetLongviewClientResponses = {
    /**
     * A LongviewClient is a single monitor set up to track statistics about one of your servers.
     */
    200: {
        /**
         * __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly api_key: string;
        /**
         * __Read-only__ The apps this Client is monitoring on your Linode. This is configured when you install the Longview Client application, and is present here for information purposes only.
         */
        readonly apps: {
            /**
             * __Read-only__ If `true`, the Apache Longview Client module is monitoring Apache on your server.
             */
            readonly apache: boolean;
            /**
             * __Read-only__ If `true`, the MySQL Longview Client modules is monitoring MySQL on your server.
             */
            readonly mysql: boolean;
            /**
             * __Read-only__ If `true`, the Nginx Longview Client module is monitoring Nginx on your server.
             */
            readonly nginx: boolean;
        };
        /**
         * __Read-only__ When this Longview Client was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Client's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly install_code: string;
        /**
         * __Filterable__ This Client's unique label. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ When this Longview Client was last updated.
         */
        readonly updated: string;
    };
};

export type GetLongviewClientResponse = GetLongviewClientResponses[keyof GetLongviewClientResponses];

export type PutLongviewClientData = {
    /**
     * A LongviewClient is a single monitor set up to track statistics about one of your servers.
     */
    body: {
        /**
         * __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly api_key: string;
        /**
         * __Read-only__ The apps this Client is monitoring on your Linode. This is configured when you install the Longview Client application, and is present here for information purposes only.
         */
        readonly apps: {
            /**
             * __Read-only__ If `true`, the Apache Longview Client module is monitoring Apache on your server.
             */
            readonly apache: boolean;
            /**
             * __Read-only__ If `true`, the MySQL Longview Client modules is monitoring MySQL on your server.
             */
            readonly mysql: boolean;
            /**
             * __Read-only__ If `true`, the Nginx Longview Client module is monitoring Nginx on your server.
             */
            readonly nginx: boolean;
        };
        /**
         * __Read-only__ When this Longview Client was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Client's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly install_code: string;
        /**
         * __Filterable__ This Client's unique label. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ When this Longview Client was last updated.
         */
        readonly updated: string;
    };
    path: {
        /**
         * The Longview Client ID to access.
         */
        clientId: number;
    };
    query?: never;
    url: '/longview/clients/{clientId}';
};

export type PutLongviewClientErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLongviewClientError = PutLongviewClientErrors[keyof PutLongviewClientErrors];

export type PutLongviewClientResponses = {
    /**
     * A LongviewClient is a single monitor set up to track statistics about one of your servers.
     */
    200: {
        /**
         * __Read-only__ The API key for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly api_key: string;
        /**
         * __Read-only__ The apps this Client is monitoring on your Linode. This is configured when you install the Longview Client application, and is present here for information purposes only.
         */
        readonly apps: {
            /**
             * __Read-only__ If `true`, the Apache Longview Client module is monitoring Apache on your server.
             */
            readonly apache: boolean;
            /**
             * __Read-only__ If `true`, the MySQL Longview Client modules is monitoring MySQL on your server.
             */
            readonly mysql: boolean;
            /**
             * __Read-only__ If `true`, the Nginx Longview Client module is monitoring Nginx on your server.
             */
            readonly nginx: boolean;
        };
        /**
         * __Read-only__ When this Longview Client was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This Client's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The install code for this Client, used when configuring the Longview Client application on your Linode.
         *
         * Returns as `[REDACTED]` if you do not have read-write access to this client.
         */
        readonly install_code: string;
        /**
         * __Filterable__ This Client's unique label. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ When this Longview Client was last updated.
         */
        readonly updated: string;
    };
};

export type PutLongviewClientResponse = PutLongviewClientResponses[keyof PutLongviewClientResponses];

export type GetLongviewPlanData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/longview/plan';
};

export type GetLongviewPlanErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLongviewPlanError = GetLongviewPlanErrors[keyof GetLongviewPlanErrors];

export type GetLongviewPlanResponses = {
    /**
     * A Longview Subscription represents a tier of Longview service you can subscribe to.
     */
    200: {
        /**
         * __Read-only__ The number of Longview Clients that may be created with this Subscription tier.
         */
        readonly clients_included: number;
        /**
         * __Read-only__ The unique ID of this Subscription tier.
         */
        id: 'longview-3' | 'longview-10' | 'longview-40' | 'longview-100';
        /**
         * __Read-only__ A display name for this Subscription tier.
         */
        readonly label: string;
        /**
         * __Read-only__ Pricing information about this Subscription tier.
         */
        readonly price: {
            /**
             * __Read-only__ The hourly price, in US dollars, for this Subscription tier.
             */
            readonly hourly: number;
            /**
             * __Read-only__ The maximum monthly price in US Dollars for this Subscription tier. You will never be charged more than this amount per month for this subscription.
             */
            readonly monthly: number;
        };
    };
};

export type GetLongviewPlanResponse = GetLongviewPlanResponses[keyof GetLongviewPlanResponses];

export type PutLongviewPlanData = {
    /**
     * Longview Plan object.
     */
    body: {
        /**
         * The subscription ID for a particular Longview plan. A value of `null` corresponds to Longview Free. You can send a request to the [List Longview subscriptions](https://techdocs.akamai.com/linode-api/reference/get-longview-subscriptions) operation to receive the details of each plan.
         */
        longview_subscription: 'longview-3' | 'longview-10' | 'longview-40' | 'longview-100';
    };
    path?: never;
    query?: never;
    url: '/longview/plan';
};

export type PutLongviewPlanErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutLongviewPlanError = PutLongviewPlanErrors[keyof PutLongviewPlanErrors];

export type PutLongviewPlanResponses = {
    /**
     * A Longview Subscription represents a tier of Longview service you can subscribe to.
     */
    200: {
        /**
         * __Read-only__ The number of Longview Clients that may be created with this Subscription tier.
         */
        readonly clients_included: number;
        /**
         * __Read-only__ The unique ID of this Subscription tier.
         */
        id: 'longview-3' | 'longview-10' | 'longview-40' | 'longview-100';
        /**
         * __Read-only__ A display name for this Subscription tier.
         */
        readonly label: string;
        /**
         * __Read-only__ Pricing information about this Subscription tier.
         */
        readonly price: {
            /**
             * __Read-only__ The hourly price, in US dollars, for this Subscription tier.
             */
            readonly hourly: number;
            /**
             * __Read-only__ The maximum monthly price in US Dollars for this Subscription tier. You will never be charged more than this amount per month for this subscription.
             */
            readonly monthly: number;
        };
    };
};

export type PutLongviewPlanResponse = PutLongviewPlanResponses[keyof PutLongviewPlanResponses];

export type GetLongviewSubscriptionsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/longview/subscriptions';
};

export type GetLongviewSubscriptionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLongviewSubscriptionsError = GetLongviewSubscriptionsErrors[keyof GetLongviewSubscriptionsErrors];

export type GetLongviewSubscriptionsResponses = {
    /**
     * A paginated list of Longview subscriptions.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The number of Longview Clients that may be created with this Subscription tier.
             */
            readonly clients_included: number;
            /**
             * __Read-only__ The unique ID of this Subscription tier.
             */
            id: 'longview-3' | 'longview-10' | 'longview-40' | 'longview-100';
            /**
             * __Read-only__ A display name for this Subscription tier.
             */
            readonly label: string;
            /**
             * __Read-only__ Pricing information about this Subscription tier.
             */
            readonly price: {
                /**
                 * __Read-only__ The hourly price, in US dollars, for this Subscription tier.
                 */
                readonly hourly: number;
                /**
                 * __Read-only__ The maximum monthly price in US Dollars for this Subscription tier. You will never be charged more than this amount per month for this subscription.
                 */
                readonly monthly: number;
            };
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLongviewSubscriptionsResponse = GetLongviewSubscriptionsResponses[keyof GetLongviewSubscriptionsResponses];

export type GetLongviewSubscriptionData = {
    body?: never;
    path: {
        /**
         * The Longview Subscription to look up.
         */
        subscriptionId: string;
    };
    query?: never;
    url: '/longview/subscriptions/{subscriptionId}';
};

export type GetLongviewSubscriptionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLongviewSubscriptionError = GetLongviewSubscriptionErrors[keyof GetLongviewSubscriptionErrors];

export type GetLongviewSubscriptionResponses = {
    /**
     * A Longview Subscription represents a tier of Longview service you can subscribe to.
     */
    200: {
        /**
         * __Read-only__ The number of Longview Clients that may be created with this Subscription tier.
         */
        readonly clients_included: number;
        /**
         * __Read-only__ The unique ID of this Subscription tier.
         */
        id: 'longview-3' | 'longview-10' | 'longview-40' | 'longview-100';
        /**
         * __Read-only__ A display name for this Subscription tier.
         */
        readonly label: string;
        /**
         * __Read-only__ Pricing information about this Subscription tier.
         */
        readonly price: {
            /**
             * __Read-only__ The hourly price, in US dollars, for this Subscription tier.
             */
            readonly hourly: number;
            /**
             * __Read-only__ The maximum monthly price in US Dollars for this Subscription tier. You will never be charged more than this amount per month for this subscription.
             */
            readonly monthly: number;
        };
    };
};

export type GetLongviewSubscriptionResponse = GetLongviewSubscriptionResponses[keyof GetLongviewSubscriptionResponses];

export type GetLongviewTypesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/longview/types';
};

export type GetLongviewTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetLongviewTypesError = GetLongviewTypesErrors[keyof GetLongviewTypesErrors];

export type GetLongviewTypesResponses = {
    /**
     * A collection of Longview types.
     */
    200: {
        /**
         * The Longview types.
         */
        data: Array<{
            /**
             * __Read-only__ The ID representing the Longview type.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The Longview type label is for display purposes only.
             */
            readonly label: string;
            /**
             * __Read-only__ The default cost of this Longview type. Prices are in US dollars, broken down into hourly and monthly charges.
             *
             * Certain regions have different prices from the default. For region-specific prices, see `region_prices`.
             */
            readonly price: {
                /**
                 * __Filterable__ Cost (in US dollars) per hour.
                 */
                hourly: number;
                /**
                 * __Filterable__ Cost per month, in US dollars.
                 */
                monthly: number;
            };
            region_prices: Array<{
                /**
                 * Cost per hour for this region, in US dollars.
                 */
                hourly: number;
                /**
                 * The Region ID for these prices.
                 */
                id: string;
                /**
                 * Cost per month for this region, in US dollars.
                 */
                monthly: number;
            }>;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount, in MB.
             */
            readonly transfer: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetLongviewTypesResponse = GetLongviewTypesResponses[keyof GetLongviewTypesResponses];

export type GetMaintenancePoliciesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/maintenance/policies';
};

export type GetMaintenancePoliciesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetMaintenancePoliciesError = GetMaintenancePoliciesErrors[keyof GetMaintenancePoliciesErrors];

export type GetMaintenancePoliciesResponses = {
    /**
     * Returns a paginated list of maintenance policy objects.
     */
    200: {
        data: Array<{
            /**
             * A brief explanation of the maintenance policy's intended behavior.
             */
            description: string;
            /**
             * Indicates whether this policy is the default one applied when creating a Linode.
             */
            is_default: boolean;
            /**
             * The display name for the maintenance policy.
             */
            label: string;
            /**
             * __Filterable__ Number of seconds before the maintenance event triggers. A value of 0 means no prior notification.
             */
            notification_period_sec: number;
            /**
             * Unique identifier for the maintenance policy. System policies are prefixed with `linode/`.
             */
            slug: string;
            /**
             * The type of policy.
             */
            type: 'migrate' | 'power_off_on';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetMaintenancePoliciesResponse = GetMaintenancePoliciesResponses[keyof GetMaintenancePoliciesResponses];

export type GetManagedContactsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/managed/contacts';
};

export type GetManagedContactsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedContactsError = GetManagedContactsErrors[keyof GetManagedContactsErrors];

export type GetManagedContactsResponses = {
    /**
     * A paginated list of ManagedContacts.
     */
    200: {
        data: Array<{
            /**
             * The address to email this Contact to alert them of issues.
             */
            email: string;
            /**
             * __Filterable__ A grouping for this Contact. This is for display purposes only.
             */
            group: string | null;
            /**
             * __Read-only__ This Contact's unique ID.
             */
            readonly id: number;
            /**
             * The name of this Contact.
             */
            name: string;
            /**
             * Information about how to reach this Contact by phone.
             */
            phone: {
                /**
                 * This Contact's primary phone number.
                 */
                primary: string | null;
                /**
                 * This Contact's secondary phone number.
                 */
                secondary: string | null;
            };
            /**
             * __Read-only__ When this Contact was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetManagedContactsResponse = GetManagedContactsResponses[keyof GetManagedContactsResponses];

export type PostManagedContactData = {
    /**
     * Information about someone Linode's special forces may contact in case an issue is detected with a manager service.
     */
    body?: {
        /**
         * The address to email this Contact to alert them of issues.
         */
        email: string;
        /**
         * __Filterable__ A grouping for this Contact. This is for display purposes only.
         */
        group: string | null;
        /**
         * __Read-only__ This Contact's unique ID.
         */
        readonly id: number;
        /**
         * The name of this Contact.
         */
        name: string;
        /**
         * Information about how to reach this Contact by phone.
         */
        phone: {
            /**
             * This Contact's primary phone number.
             */
            primary: string | null;
            /**
             * This Contact's secondary phone number.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ When this Contact was last updated.
         */
        readonly updated: string;
    };
    path?: never;
    query?: never;
    url: '/managed/contacts';
};

export type PostManagedContactErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostManagedContactError = PostManagedContactErrors[keyof PostManagedContactErrors];

export type PostManagedContactResponses = {
    /**
     * Information about someone Linode's special forces may contact in case an issue is detected with a manager service.
     */
    200: {
        /**
         * The address to email this Contact to alert them of issues.
         */
        email: string;
        /**
         * __Filterable__ A grouping for this Contact. This is for display purposes only.
         */
        group: string | null;
        /**
         * __Read-only__ This Contact's unique ID.
         */
        readonly id: number;
        /**
         * The name of this Contact.
         */
        name: string;
        /**
         * Information about how to reach this Contact by phone.
         */
        phone: {
            /**
             * This Contact's primary phone number.
             */
            primary: string | null;
            /**
             * This Contact's secondary phone number.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ When this Contact was last updated.
         */
        readonly updated: string;
    };
};

export type PostManagedContactResponse = PostManagedContactResponses[keyof PostManagedContactResponses];

export type DeleteManagedContactData = {
    body?: never;
    path: {
        /**
         * The ID of the contact to access.
         */
        contactId: number;
    };
    query?: never;
    url: '/managed/contacts/{contactId}';
};

export type DeleteManagedContactErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteManagedContactError = DeleteManagedContactErrors[keyof DeleteManagedContactErrors];

export type DeleteManagedContactResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteManagedContactResponse = DeleteManagedContactResponses[keyof DeleteManagedContactResponses];

export type GetManagedContactData = {
    body?: never;
    path: {
        /**
         * The ID of the contact to access.
         */
        contactId: number;
    };
    query?: never;
    url: '/managed/contacts/{contactId}';
};

export type GetManagedContactErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedContactError = GetManagedContactErrors[keyof GetManagedContactErrors];

export type GetManagedContactResponses = {
    /**
     * Information about someone Linode's special forces may contact in case an issue is detected with a manager service.
     */
    200: {
        /**
         * The address to email this Contact to alert them of issues.
         */
        email: string;
        /**
         * __Filterable__ A grouping for this Contact. This is for display purposes only.
         */
        group: string | null;
        /**
         * __Read-only__ This Contact's unique ID.
         */
        readonly id: number;
        /**
         * The name of this Contact.
         */
        name: string;
        /**
         * Information about how to reach this Contact by phone.
         */
        phone: {
            /**
             * This Contact's primary phone number.
             */
            primary: string | null;
            /**
             * This Contact's secondary phone number.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ When this Contact was last updated.
         */
        readonly updated: string;
    };
};

export type GetManagedContactResponse = GetManagedContactResponses[keyof GetManagedContactResponses];

export type PutManagedContactData = {
    /**
     * Information about someone Linode's special forces may contact in case an issue is detected with a manager service.
     */
    body: {
        /**
         * The address to email this Contact to alert them of issues.
         */
        email: string;
        /**
         * __Filterable__ A grouping for this Contact. This is for display purposes only.
         */
        group: string | null;
        /**
         * __Read-only__ This Contact's unique ID.
         */
        readonly id: number;
        /**
         * The name of this Contact.
         */
        name: string;
        /**
         * Information about how to reach this Contact by phone.
         */
        phone: {
            /**
             * This Contact's primary phone number.
             */
            primary: string | null;
            /**
             * This Contact's secondary phone number.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ When this Contact was last updated.
         */
        readonly updated: string;
    };
    path: {
        /**
         * The ID of the contact to access.
         */
        contactId: number;
    };
    query?: never;
    url: '/managed/contacts/{contactId}';
};

export type PutManagedContactErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutManagedContactError = PutManagedContactErrors[keyof PutManagedContactErrors];

export type PutManagedContactResponses = {
    /**
     * Information about someone Linode's special forces may contact in case an issue is detected with a manager service.
     */
    200: {
        /**
         * The address to email this Contact to alert them of issues.
         */
        email: string;
        /**
         * __Filterable__ A grouping for this Contact. This is for display purposes only.
         */
        group: string | null;
        /**
         * __Read-only__ This Contact's unique ID.
         */
        readonly id: number;
        /**
         * The name of this Contact.
         */
        name: string;
        /**
         * Information about how to reach this Contact by phone.
         */
        phone: {
            /**
             * This Contact's primary phone number.
             */
            primary: string | null;
            /**
             * This Contact's secondary phone number.
             */
            secondary: string | null;
        };
        /**
         * __Read-only__ When this Contact was last updated.
         */
        readonly updated: string;
    };
};

export type PutManagedContactResponse = PutManagedContactResponses[keyof PutManagedContactResponses];

export type GetManagedCredentialsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/managed/credentials';
};

export type GetManagedCredentialsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedCredentialsError = GetManagedCredentialsErrors[keyof GetManagedCredentialsErrors];

export type GetManagedCredentialsResponses = {
    /**
     * A paginated list of ManagedCredentials.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ This Credential's unique ID.
             */
            readonly id: number;
            /**
             * The unique label for this Credential. This is for display purposes only.
             */
            label: string;
            /**
             * __Read-only__ The date this Credential was last decrypted by a member of Linode special forces.
             */
            readonly last_decrypted: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetManagedCredentialsResponse = GetManagedCredentialsResponses[keyof GetManagedCredentialsResponses];

export type PostManagedCredentialData = {
    /**
     * Information about the Credential to create.
     */
    body?: {
        /**
         * __Read-only__ This Credential's unique ID.
         */
        readonly id: number;
        /**
         * The unique label for this Credential. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ The date this Credential was last decrypted by a member of Linode special forces.
         */
        readonly last_decrypted: string;
    } & {
        /**
         * The password to use when accessing the Managed Service.
         */
        password: string;
        /**
         * The username to use when accessing the Managed Service.
         */
        username: string;
    };
    path?: never;
    query?: never;
    url: '/managed/credentials';
};

export type PostManagedCredentialErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostManagedCredentialError = PostManagedCredentialErrors[keyof PostManagedCredentialErrors];

export type PostManagedCredentialResponses = {
    /**
     * A securely stored Credential that allows Linode's special forces to access a Managed server to respond to Issues.
     */
    200: {
        /**
         * __Read-only__ This Credential's unique ID.
         */
        readonly id: number;
        /**
         * The unique label for this Credential. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ The date this Credential was last decrypted by a member of Linode special forces.
         */
        readonly last_decrypted: string;
    };
};

export type PostManagedCredentialResponse = PostManagedCredentialResponses[keyof PostManagedCredentialResponses];

export type GetManagedSshKeyData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/managed/credentials/sshkey';
};

export type GetManagedSshKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedSshKeyError = GetManagedSshKeyErrors[keyof GetManagedSshKeyErrors];

export type GetManagedSshKeyResponses = {
    /**
     * A unique SSH public key that allows Linode's special forces to access a Managed server to respond to Issues.
     */
    200: {
        /**
         * __Read-only__ The unique SSH public key assigned to your Linode account's Managed service.
         */
        readonly ssh_key: string;
    };
};

export type GetManagedSshKeyResponse = GetManagedSshKeyResponses[keyof GetManagedSshKeyResponses];

export type GetManagedCredentialData = {
    body?: never;
    path: {
        /**
         * The ID of the Credential to access.
         */
        credentialId: number;
    };
    query?: never;
    url: '/managed/credentials/{credentialId}';
};

export type GetManagedCredentialErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedCredentialError = GetManagedCredentialErrors[keyof GetManagedCredentialErrors];

export type GetManagedCredentialResponses = {
    /**
     * A securely stored Credential that allows Linode's special forces to access a Managed server to respond to Issues.
     */
    200: {
        /**
         * __Read-only__ This Credential's unique ID.
         */
        readonly id: number;
        /**
         * The unique label for this Credential. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ The date this Credential was last decrypted by a member of Linode special forces.
         */
        readonly last_decrypted: string;
    };
};

export type GetManagedCredentialResponse = GetManagedCredentialResponses[keyof GetManagedCredentialResponses];

export type PutManagedCredentialData = {
    /**
     * A securely stored Credential that allows Linode's special forces to access a Managed server to respond to Issues.
     */
    body: {
        /**
         * __Read-only__ This Credential's unique ID.
         */
        readonly id: number;
        /**
         * The unique label for this Credential. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ The date this Credential was last decrypted by a member of Linode special forces.
         */
        readonly last_decrypted: string;
    };
    path: {
        /**
         * The ID of the Credential to access.
         */
        credentialId: number;
    };
    query?: never;
    url: '/managed/credentials/{credentialId}';
};

export type PutManagedCredentialErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutManagedCredentialError = PutManagedCredentialErrors[keyof PutManagedCredentialErrors];

export type PutManagedCredentialResponses = {
    /**
     * A securely stored Credential that allows Linode's special forces to access a Managed server to respond to Issues.
     */
    200: {
        /**
         * __Read-only__ This Credential's unique ID.
         */
        readonly id: number;
        /**
         * The unique label for this Credential. This is for display purposes only.
         */
        label: string;
        /**
         * __Read-only__ The date this Credential was last decrypted by a member of Linode special forces.
         */
        readonly last_decrypted: string;
    };
};

export type PutManagedCredentialResponse = PutManagedCredentialResponses[keyof PutManagedCredentialResponses];

export type PostManagedCredentialRevokeData = {
    body?: never;
    path: {
        /**
         * The ID of the Credential to access.
         */
        credentialId: number;
    };
    query?: never;
    url: '/managed/credentials/{credentialId}/revoke';
};

export type PostManagedCredentialRevokeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostManagedCredentialRevokeError = PostManagedCredentialRevokeErrors[keyof PostManagedCredentialRevokeErrors];

export type PostManagedCredentialRevokeResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostManagedCredentialRevokeResponse = PostManagedCredentialRevokeResponses[keyof PostManagedCredentialRevokeResponses];

export type PostManagedCredentialUsernamePasswordData = {
    /**
     * The new username and password to assign to the Managed Credential.
     */
    body?: {
        /**
         * The password to use when accessing the Managed Service.
         */
        password: string;
        /**
         * The username to use when accessing the Managed Service.
         */
        username?: string;
    };
    path: {
        /**
         * The ID of the Credential to update.
         */
        credentialId: number;
    };
    query?: never;
    url: '/managed/credentials/{credentialId}/update';
};

export type PostManagedCredentialUsernamePasswordErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostManagedCredentialUsernamePasswordError = PostManagedCredentialUsernamePasswordErrors[keyof PostManagedCredentialUsernamePasswordErrors];

export type PostManagedCredentialUsernamePasswordResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostManagedCredentialUsernamePasswordResponse = PostManagedCredentialUsernamePasswordResponses[keyof PostManagedCredentialUsernamePasswordResponses];

export type GetManagedIssuesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/managed/issues';
};

export type GetManagedIssuesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedIssuesError = GetManagedIssuesErrors[keyof GetManagedIssuesErrors];

export type GetManagedIssuesResponses = {
    /**
     * A paginated list of open or ongoing Managed Issues.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this Issue was created. Issues are created in response to issues detected with Managed Services, so this is also when the Issue was detected.
             */
            readonly created: string;
            /**
             * __Read-only__ The ticket this Managed Issue opened.
             */
            readonly entity: {
                /**
                 * __Read-only__ This ticket's ID.
                 */
                readonly id: number;
                /**
                 * __Read-only__ The summary for this Ticket.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The type of entity this is. In this case, it is always a Ticket.
                 */
                type: 'ticket';
                /**
                 * __Read-only__ The relative URL where you can access this Ticket.
                 */
                readonly url: string;
            };
            /**
             * __Read-only__ This Issue's unique ID.
             */
            readonly id: number;
            /**
             * __Read-only__ An array of Managed Service IDs that were affected by this Issue.
             */
            readonly services: Array<number>;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetManagedIssuesResponse = GetManagedIssuesResponses[keyof GetManagedIssuesResponses];

export type GetManagedIssueData = {
    body?: never;
    path: {
        /**
         * The Issue to look up.
         */
        issueId: number;
    };
    query?: never;
    url: '/managed/issues/{issueId}';
};

export type GetManagedIssueErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedIssueError = GetManagedIssueErrors[keyof GetManagedIssueErrors];

export type GetManagedIssueResponses = {
    /**
     * An Issue that was detected with a service Linode is managing.
     */
    200: {
        /**
         * __Read-only__ When this Issue was created. Issues are created in response to issues detected with Managed Services, so this is also when the Issue was detected.
         */
        readonly created: string;
        /**
         * __Read-only__ The ticket this Managed Issue opened.
         */
        readonly entity: {
            /**
             * __Read-only__ This ticket's ID.
             */
            readonly id: number;
            /**
             * __Read-only__ The summary for this Ticket.
             */
            readonly label: string;
            /**
             * __Read-only__ The type of entity this is. In this case, it is always a Ticket.
             */
            type: 'ticket';
            /**
             * __Read-only__ The relative URL where you can access this Ticket.
             */
            readonly url: string;
        };
        /**
         * __Read-only__ This Issue's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ An array of Managed Service IDs that were affected by this Issue.
         */
        readonly services: Array<number>;
    };
};

export type GetManagedIssueResponse = GetManagedIssueResponses[keyof GetManagedIssueResponses];

export type GetManagedLinodeSettingsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/managed/linode-settings';
};

export type GetManagedLinodeSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedLinodeSettingsError = GetManagedLinodeSettingsErrors[keyof GetManagedLinodeSettingsErrors];

export type GetManagedLinodeSettingsResponses = {
    /**
     * A paginated list of Managed settings for your Linodes.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The group of the Linode these Settings are for. This is for display purposes only.
             *
             * @deprecated
             */
            readonly group: string;
            /**
             * __Read-only__ The ID of the Linode these Settings are for.
             */
            readonly id: number;
            /**
             * __Read-only__ The label of the Linode these Settings are for.
             */
            readonly label: string;
            /**
             * The SSH settings for this Linode.
             */
            ssh: {
                /**
                 * If `true`, Linode special forces may access this Linode over ssh to respond to Issues.
                 */
                access: boolean;
                /**
                 * The IP Linode special forces should use to access this Linode when responding to an Issue.
                 *
                 * By default, any of a Linode's IP addresses can be used for incident response access.
                 */
                ip: string;
                /**
                 * The port Linode special forces should use to access this Linode over ssh to respond to an Issue.
                 *
                 * The default `null` value corresponds to port 22.
                 */
                port: number | null;
                /**
                 * The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.
                 *
                 * The default `null` value corresponds to the root user.
                 */
                user: string | null;
            };
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetManagedLinodeSettingsResponse = GetManagedLinodeSettingsResponses[keyof GetManagedLinodeSettingsResponses];

export type GetManagedLinodeSettingData = {
    body?: never;
    path: {
        /**
         * The Linode ID whose settings we are accessing.
         */
        linodeId: number;
    };
    query?: never;
    url: '/managed/linode-settings/{linodeId}';
};

export type GetManagedLinodeSettingErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedLinodeSettingError = GetManagedLinodeSettingErrors[keyof GetManagedLinodeSettingErrors];

export type GetManagedLinodeSettingResponses = {
    /**
     * Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings object for each Linode on your Account.
     */
    200: {
        /**
         * __Read-only__ The group of the Linode these Settings are for. This is for display purposes only.
         *
         * @deprecated
         */
        readonly group: string;
        /**
         * __Read-only__ The ID of the Linode these Settings are for.
         */
        readonly id: number;
        /**
         * __Read-only__ The label of the Linode these Settings are for.
         */
        readonly label: string;
        /**
         * The SSH settings for this Linode.
         */
        ssh: {
            /**
             * If `true`, Linode special forces may access this Linode over ssh to respond to Issues.
             */
            access: boolean;
            /**
             * The IP Linode special forces should use to access this Linode when responding to an Issue.
             *
             * By default, any of a Linode's IP addresses can be used for incident response access.
             */
            ip: string;
            /**
             * The port Linode special forces should use to access this Linode over ssh to respond to an Issue.
             *
             * The default `null` value corresponds to port 22.
             */
            port: number | null;
            /**
             * The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.
             *
             * The default `null` value corresponds to the root user.
             */
            user: string | null;
        };
    };
};

export type GetManagedLinodeSettingResponse = GetManagedLinodeSettingResponses[keyof GetManagedLinodeSettingResponses];

export type PutManagedLinodeSettingData = {
    /**
     * Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings object for each Linode on your Account.
     */
    body: {
        /**
         * __Read-only__ The group of the Linode these Settings are for. This is for display purposes only.
         *
         * @deprecated
         */
        readonly group: string;
        /**
         * __Read-only__ The ID of the Linode these Settings are for.
         */
        readonly id: number;
        /**
         * __Read-only__ The label of the Linode these Settings are for.
         */
        readonly label: string;
        /**
         * The SSH settings for this Linode.
         */
        ssh: {
            /**
             * If `true`, Linode special forces may access this Linode over ssh to respond to Issues.
             */
            access: boolean;
            /**
             * The IP Linode special forces should use to access this Linode when responding to an Issue.
             *
             * By default, any of a Linode's IP addresses can be used for incident response access.
             */
            ip: string;
            /**
             * The port Linode special forces should use to access this Linode over ssh to respond to an Issue.
             *
             * The default `null` value corresponds to port 22.
             */
            port: number | null;
            /**
             * The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.
             *
             * The default `null` value corresponds to the root user.
             */
            user: string | null;
        };
    };
    path: {
        /**
         * The Linode ID whose settings we are accessing.
         */
        linodeId: number;
    };
    query?: never;
    url: '/managed/linode-settings/{linodeId}';
};

export type PutManagedLinodeSettingErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutManagedLinodeSettingError = PutManagedLinodeSettingErrors[keyof PutManagedLinodeSettingErrors];

export type PutManagedLinodeSettingResponses = {
    /**
     * Settings for a specific Linode related to Managed Services. There is one ManagedLinodeSettings object for each Linode on your Account.
     */
    200: {
        /**
         * __Read-only__ The group of the Linode these Settings are for. This is for display purposes only.
         *
         * @deprecated
         */
        readonly group: string;
        /**
         * __Read-only__ The ID of the Linode these Settings are for.
         */
        readonly id: number;
        /**
         * __Read-only__ The label of the Linode these Settings are for.
         */
        readonly label: string;
        /**
         * The SSH settings for this Linode.
         */
        ssh: {
            /**
             * If `true`, Linode special forces may access this Linode over ssh to respond to Issues.
             */
            access: boolean;
            /**
             * The IP Linode special forces should use to access this Linode when responding to an Issue.
             *
             * By default, any of a Linode's IP addresses can be used for incident response access.
             */
            ip: string;
            /**
             * The port Linode special forces should use to access this Linode over ssh to respond to an Issue.
             *
             * The default `null` value corresponds to port 22.
             */
            port: number | null;
            /**
             * The specific user, if any, Linode's special forces should use when accessing this Linode to respond to an issue.
             *
             * The default `null` value corresponds to the root user.
             */
            user: string | null;
        };
    };
};

export type PutManagedLinodeSettingResponse = PutManagedLinodeSettingResponses[keyof PutManagedLinodeSettingResponses];

export type GetManagedServicesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/managed/services';
};

export type GetManagedServicesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedServicesError = GetManagedServicesErrors[keyof GetManagedServicesErrors];

export type GetManagedServicesResponses = {
    /**
     * A paginated list of Managed Services.
     */
    200: {
        data: Array<{
            /**
             * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
             */
            address: string;
            /**
             * What to expect to find in the response body for the Service to be considered up.
             */
            body: string | null;
            /**
             * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
             */
            consultation_group: string;
            /**
             * __Read-only__ When this Managed Service was created.
             */
            readonly created: string;
            /**
             * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
             */
            credentials: Array<number>;
            /**
             * __Read-only__ This Service's unique ID.
             */
            readonly id: number;
            /**
             * The label for this Service. This is for display purposes only.
             */
            label: string;
            /**
             * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
             */
            notes: string | null;
            /**
             * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
             */
            region: string | null;
            /**
             * How this Service is monitored.
             */
            service_type: 'url' | 'tcp';
            /**
             * __Read-only__ The current status of this Service.
             */
            status: 'disabled' | 'pending' | 'ok' | 'problem';
            /**
             * How long to wait, in seconds, for a response before considering the Service to be down.
             */
            timeout: number;
            /**
             * __Read-only__ When this Managed Service was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetManagedServicesResponse = GetManagedServicesResponses[keyof GetManagedServicesResponses];

export type PostManagedServiceData = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    body?: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
    path?: never;
    query?: never;
    url: '/managed/services';
};

export type PostManagedServiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostManagedServiceError = PostManagedServiceErrors[keyof PostManagedServiceErrors];

export type PostManagedServiceResponses = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    200: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
};

export type PostManagedServiceResponse = PostManagedServiceResponses[keyof PostManagedServiceResponses];

export type DeleteManagedServiceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed Service to access.
         */
        serviceId: number;
    };
    query?: never;
    url: '/managed/services/{serviceId}';
};

export type DeleteManagedServiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteManagedServiceError = DeleteManagedServiceErrors[keyof DeleteManagedServiceErrors];

export type DeleteManagedServiceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteManagedServiceResponse = DeleteManagedServiceResponses[keyof DeleteManagedServiceResponses];

export type GetManagedServiceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed Service to access.
         */
        serviceId: number;
    };
    query?: never;
    url: '/managed/services/{serviceId}';
};

export type GetManagedServiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedServiceError = GetManagedServiceErrors[keyof GetManagedServiceErrors];

export type GetManagedServiceResponses = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    200: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
};

export type GetManagedServiceResponse = GetManagedServiceResponses[keyof GetManagedServiceResponses];

export type PutManagedServiceData = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    body: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
    path: {
        /**
         * The ID of the Managed Service to access.
         */
        serviceId: number;
    };
    query?: never;
    url: '/managed/services/{serviceId}';
};

export type PutManagedServiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutManagedServiceError = PutManagedServiceErrors[keyof PutManagedServiceErrors];

export type PutManagedServiceResponses = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    200: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
};

export type PutManagedServiceResponse = PutManagedServiceResponses[keyof PutManagedServiceResponses];

export type PostDisableManagedServiceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed Service to disable.
         */
        serviceId: number;
    };
    query?: never;
    url: '/managed/services/{serviceId}/disable';
};

export type PostDisableManagedServiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDisableManagedServiceError = PostDisableManagedServiceErrors[keyof PostDisableManagedServiceErrors];

export type PostDisableManagedServiceResponses = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    200: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
};

export type PostDisableManagedServiceResponse = PostDisableManagedServiceResponses[keyof PostDisableManagedServiceResponses];

export type PostEnableManagedServiceData = {
    body?: never;
    path: {
        /**
         * The ID of the Managed Service to enable.
         */
        serviceId: number;
    };
    query?: never;
    url: '/managed/services/{serviceId}/enable';
};

export type PostEnableManagedServiceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostEnableManagedServiceError = PostEnableManagedServiceErrors[keyof PostEnableManagedServiceErrors];

export type PostEnableManagedServiceResponses = {
    /**
     * A service that Linode is monitoring as part of your Managed services. If issues are detected with this service, a ManagedIssue will be opened and, optionally, Linode special forces will attempt to resolve the Issue.
     */
    200: {
        /**
         * The URL at which this Service is monitored. URL parameters such as `?no-cache=1` are preserved. URL fragments/anchors such as `#monitor` are __not__ preserved.
         */
        address: string;
        /**
         * What to expect to find in the response body for the Service to be considered up.
         */
        body: string | null;
        /**
         * The group of ManagedContacts who should be notified or consulted with when an Issue is detected.
         */
        consultation_group: string;
        /**
         * __Read-only__ When this Managed Service was created.
         */
        readonly created: string;
        /**
         * An array of ManagedCredential IDs that should be used when attempting to resolve issues with this Service.
         */
        credentials: Array<number>;
        /**
         * __Read-only__ This Service's unique ID.
         */
        readonly id: number;
        /**
         * The label for this Service. This is for display purposes only.
         */
        label: string;
        /**
         * Any information relevant to the Service that Linode special forces should know when attempting to resolve Issues.
         */
        notes: string | null;
        /**
         * The Region in which this Service is located. This is required if address is a private IP, and may not be set otherwise.
         */
        region: string | null;
        /**
         * How this Service is monitored.
         */
        service_type: 'url' | 'tcp';
        /**
         * __Read-only__ The current status of this Service.
         */
        status: 'disabled' | 'pending' | 'ok' | 'problem';
        /**
         * How long to wait, in seconds, for a response before considering the Service to be down.
         */
        timeout: number;
        /**
         * __Read-only__ When this Managed Service was last updated.
         */
        readonly updated: string;
    };
};

export type PostEnableManagedServiceResponse = PostEnableManagedServiceResponses[keyof PostEnableManagedServiceResponses];

export type GetManagedStatsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/managed/stats';
};

export type GetManagedStatsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetManagedStatsError = GetManagedStatsErrors[keyof GetManagedStatsErrors];

export type GetManagedStatsResponses = {
    /**
     * A list of Managed Stats from the last 24 hours.
     */
    200: {
        data: {
            /**
             * CPU usage stats from the last 24 hours.
             */
            cpu: Array<{
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly x: number;
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly y: number;
            }>;
            /**
             * Disk usage stats from the last 24 hours.
             */
            disk: Array<{
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly x: number;
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly y: number;
            }>;
            /**
             * Inbound network traffic stats from the last 24 hours.
             */
            net_in: Array<{
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly x: number;
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly y: number;
            }>;
            /**
             * Outbound network traffic stats from the last 24 hours.
             */
            net_out: Array<{
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly x: number;
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly y: number;
            }>;
            /**
             * Swap usage stats from the last 24 hours.
             */
            swap: Array<{
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly x: number;
                /**
                 * __Read-only__ A stats graph data point.
                 */
                readonly y: number;
            }>;
        } | Array<string>;
    };
};

export type GetManagedStatsResponse = GetManagedStatsResponses[keyof GetManagedStatsResponses];

export type GetAlertChannelsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/monitor/alert-channels';
};

export type GetAlertChannelsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAlertChannelsError = GetAlertChannelsErrors[keyof GetAlertChannelsErrors];

export type GetAlertChannelsResponses = {
    /**
     * Returns a paginated list of alerts channels.
     */
    200: {
        data: Array<{
            /**
             * Details about the alerts where you've applied this alert channel.
             */
            alerts: Array<{
                /**
                 * The unique identifier for the alert.
                 */
                id: number;
                /**
                 * The name of the alert.
                 */
                label: string;
                /**
                 * The entity type of the alert.
                 */
                type: string;
                /**
                 * The URL to access the alert.
                 */
                url: string;
            }>;
            /**
             * The type of notification used with the alert channel. Currently, only `email` is supported.
             */
            channel_type: 'email';
            /**
             * The configuration of the alert channel.
             */
            content: {
                /**
                 * An alert channel set up to deliver an email for an alert.
                 */
                email: {
                    /**
                     * Email addresses that receive the message for the alert. For a `user` alert, these are the email addresses you defined in the alert channel. For a `system` alert, this is returned as `Users-with-read-write-access-to-resources`. This represents all users on your account that have read-write access to the object that triggered the alert.
                     *
                     * > 📘
                     * >
                     * > Currently, only `system` alert channels are supported for use.
                     */
                    email_addresses: Array<string>;
                };
            };
            /**
             * When the alert channel was created.
             */
            created: string;
            /**
             * For a `user` alert channel, this is the user on your account that created it. For a `system` alert channel, this value is returned as `system`.
             *
             * > 📘
             * >
             * > Currently, only `system` alert channels are supported for use. This value will always return a value of `system`.
             */
            created_by: string;
            /**
             * __Filterable__ The unique identifier for the alert channel.
             */
            id: number;
            /**
             * __Filterable__ The name of the alert channel, used to display it in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The type of alert channel. This can be either `system` for a channel provided by Akamai, or `user` for one you've created.
             *
             * > 📘
             * >
             * > Currently, only `system` alert channels are supported for use. This value will always return a value of `system`.
             */
            type: 'system' | 'user';
            /**
             * When the alert channel was last updated. This is the same as `created` if the channel hasn't been updated.
             */
            updated: string;
            /**
             * For a `user` alert channel, this is the user on your account that last updated it. For a `system` alert channel, this is returned as `system`. If it hasn't been updated, this value is the same as `created_by`.
             *
             * > 📘
             * >
             * > Currently, only `system` alert channels are supported for use. This value will always return a value of `system`.
             */
            updated_by: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetAlertChannelsResponse = GetAlertChannelsResponses[keyof GetAlertChannelsResponses];

export type GetAlertDefinitionsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/monitor/alert-definitions';
};

export type GetAlertDefinitionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAlertDefinitionsError = GetAlertDefinitionsErrors[keyof GetAlertDefinitionsErrors];

export type GetAlertDefinitionsResponses = {
    /**
     * Returns a paginated list of all alert definitions.
     */
    200: {
        data: Array<{
            /**
             * The alert channels set up for use with this alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation to see all of the available channels.
             */
            alert_channels: Array<{
                /**
                 * The unique identifier for this alert channel on your account.
                 */
                id: number;
                /**
                 * The name of the channel, used to display it in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The type of notification used with the channel. For a `user` alert definition, only `email` is supported.
                 */
                type: 'email';
                /**
                 * The URL for the channel that ends in the channel's `id`.
                 */
                url: string;
            }>;
            /**
             * The plan type for the Managed Database cluster, either `shared` or `dedicated`. This only applies to a `system` alert for a `service_type` of `dbaas` (Managed Databases). For `user` alerts for `dbaas`, this is returned as `null`.
             */
            class: 'shared' | 'dedicated';
            /**
             * When the alert definition was created.
             */
            created: string;
            /**
             * For a `user` alert definition, this is the user on your account that [created](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) it. For a `system` alert definition, this is returned as `system`.
             */
            created_by: string;
            /**
             * An additional description for the alert definition.
             */
            description: string;
            /**
             * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
             *
             * > 📘
             * >
             * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`. Other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
             */
            entity_ids: Array<string>;
            /**
             * Whether there are additional `entity_ids` associated with the alert for which the user doesn't have at least `read-only` access.
             */
            has_more_resources: boolean;
            /**
             * __Filterable__ The unique identifier for the alert definition.
             */
            id: number;
            /**
             * __Filterable__ The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * Details for the rules required to trigger the alert.
             */
            rule_criteria: {
                /**
                 * The individual rules that make up the alert definition.
                 */
                rules: Array<{
                    /**
                     * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                     */
                    aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                    /**
                     * Dimension filters for the rule.
                     */
                    dimension_filters: Array<{
                        /**
                         * The name of the dimension to be used in the filter.
                         */
                        dimension_label: string;
                        /**
                         * The name of the dimension filter. Used for display purposes.
                         */
                        label: string;
                        /**
                         * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                         */
                        operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                        /**
                         * The value to compare the `dimension_label` against.
                         */
                        value: string;
                    }>;
                    /**
                     * The name of the individual rule. This is used for display purposes in Akamai Cloud Manager.
                     */
                    label: string;
                    /**
                     * The metric to query.
                     */
                    metric: string;
                    /**
                     * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than, `gte` for greater than or equal, and `lte` for less than or equal.
                     */
                    operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                    /**
                     * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                     */
                    threshold: number;
                    /**
                     * The unit of the metric. This can be values like `percent` for percentage or `GB` for gigabyte.
                     */
                    unit: 'number' | 'byte' | 'second' | 'percent' | 'bit_per_second' | 'millisecond' | 'KB' | 'MB' | 'GB';
                }>;
            };
            /**
             * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
             */
            service_type: string;
            /**
             * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
             */
            severity: 0 | 1 | 2 | 3;
            /**
             * __Filterable__ The current status of the alert. This can be either `enabled`, `disabled`, `in progress`, or `failed`.
             */
            status: 'enabled' | 'disabled' | 'in progress' | 'failed';
            /**
             * The conditions that need to be met to send a notification for the alert.
             */
            trigger_conditions: {
                /**
                 * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`.  Currently, only `ALL` is supported.
                 */
                criteria_condition: 'ALL';
                /**
                 * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
                 *
                 * > 📘
                 * >
                 * > During the beta, only a value of `300` seconds is supported.
                 */
                evaluation_period_seconds: number;
                /**
                 * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
                 *
                 * > 📘
                 * >
                 * > During the beta, only a value of `300` seconds is supported.
                 */
                polling_interval_seconds: number;
                /**
                 * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
                 */
                trigger_occurrences: number;
            };
            /**
             * __Filterable__ The type of alert. This can be either `user` for an alert specific to the current user, or `system` for one that applies to all users on your account.
             */
            type: 'user' | 'system';
            /**
             * When the alert definition was last updated. This is the same as `created` if the alert definition hasn't been updated.
             */
            updated: string;
            /**
             * For a `user` alert definition, this is the user on your account that last [updated](https://techdocs.akamai.com/linode-api/reference/put-alert-definition) it. For a `system` alert definition, this is returned as `system`. If it hasn't been updated, this value is the same as `created_by`.
             */
            updated_by: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetAlertDefinitionsResponse = GetAlertDefinitionsResponses[keyof GetAlertDefinitionsResponses];

export type GetDashboardsAllData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/monitor/dashboards';
};

export type GetDashboardsAllErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDashboardsAllError = GetDashboardsAllErrors[keyof GetDashboardsAllErrors];

export type GetDashboardsAllResponses = {
    /**
     * Returns a paginated list of dashboards.
     */
    200: {
        data: Array<{
            /**
             * When the dashboard was created.
             */
            created: string;
            /**
             * The unique identifier for the dashboard.
             */
            id: number;
            /**
             * The name of the dashboard. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The Akamai Cloud Computing service used by this dashboard.
             */
            service_type: string;
            /**
             * The type of dashboard. Currently, this can only be `standard` for a dashboard that uses default formatting.
             */
            type: 'standard';
            /**
             * When the dashboard was last updated.
             */
            updated: string;
            /**
             * The widgets used in the dashboard.
             */
            widgets: Array<{
                /**
                 * The aggregate function for the metric. This defaults to `avg` for average.
                 */
                aggregate_function: 'min' | 'max' | 'avg' | 'sum' | 'rate';
                /**
                 * The type of chart used in the widget. This can be `line` for a line chart or `area` for an area chart.
                 */
                chart_type: 'line' | 'area';
                /**
                 * The color used in the widget.
                 */
                color: string;
                /**
                 * The name of the widget. This is used for display purposes in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The metric to query.
                 */
                metric: string;
                /**
                 * The size of the widget. This can be `6` or `12` grid units, expressed as strings.
                 */
                size: '6' | '12';
                /**
                 * The unit of the metric. This can be values like `%` for percentage or `GB` for gigabyte.
                 */
                unit: '%' | 'Bytes' | 'sec' | 'bps' | 'msec' | 'Bps' | 'KB' | 'MB' | 'GB' | 'rate' | 'percentile' | 'ratio' | 'OPS' | 'IOPS';
                /**
                 * The label for the y-axis in the widget's chart.
                 */
                y_label: string;
            }>;
        }>;
    } & {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetDashboardsAllResponse = GetDashboardsAllResponses[keyof GetDashboardsAllResponses];

export type GetDashboardsByIdData = {
    body?: never;
    path: {
        /**
         * The unique identifier of the dashboard.
         */
        dashboardId: number;
    };
    query?: never;
    url: '/monitor/dashboards/{dashboardId}';
};

export type GetDashboardsByIdErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDashboardsByIdError = GetDashboardsByIdErrors[keyof GetDashboardsByIdErrors];

export type GetDashboardsByIdResponses = {
    /**
     * Returns a dashboard.
     */
    200: {
        /**
         * When the dashboard was created.
         */
        created: string;
        /**
         * The unique identifier for the dashboard.
         */
        id: number;
        /**
         * The name of the dashboard. This is used for display purposes in Akamai Cloud Manager.
         */
        label: string;
        /**
         * The Akamai Cloud Computing service used by this dashboard.
         */
        service_type: string;
        /**
         * The type of dashboard. Currently, this can only be `standard` for a dashboard that uses default formatting.
         */
        type: 'standard';
        /**
         * When the dashboard was last updated.
         */
        updated: string;
        /**
         * The widgets used in the dashboard.
         */
        widgets: Array<{
            /**
             * The aggregate function for the metric. This defaults to `avg` for average.
             */
            aggregate_function: 'min' | 'max' | 'avg' | 'sum' | 'rate';
            /**
             * The type of chart used in the widget. This can be `line` for a line chart or `area` for an area chart.
             */
            chart_type: 'line' | 'area';
            /**
             * The color used in the widget.
             */
            color: string;
            /**
             * The name of the widget. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The metric to query.
             */
            metric: string;
            /**
             * The size of the widget. This can be `6` or `12` grid units, expressed as strings.
             */
            size: '6' | '12';
            /**
             * The unit of the metric. This can be values like `%` for percentage or `GB` for gigabyte.
             */
            unit: '%' | 'Bytes' | 'sec' | 'bps' | 'msec' | 'Bps' | 'KB' | 'MB' | 'GB' | 'rate' | 'percentile' | 'ratio' | 'OPS' | 'IOPS';
            /**
             * The label for the y-axis in the widget's chart.
             */
            y_label: string;
        }>;
    };
};

export type GetDashboardsByIdResponse = GetDashboardsByIdResponses[keyof GetDashboardsByIdResponses];

export type GetMonitorServicesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/monitor/services';
};

export type GetMonitorServicesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetMonitorServicesError = GetMonitorServicesErrors[keyof GetMonitorServicesErrors];

export type GetMonitorServicesResponses = {
    /**
     * Returns a paginated list of metric definitions.
     */
    200: {
        data: Array<{
            /**
             * The full name of Akamai Cloud Computing service. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
             */
            service_type: string;
        }>;
        /**
         * The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        page: number;
        /**
         * The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        pages: number;
        /**
         * The total number of results.
         */
        results: number;
    };
};

export type GetMonitorServicesResponse = GetMonitorServicesResponses[keyof GetMonitorServicesResponses];

export type GetMonitorServicesForServiceTypeData = {
    body?: never;
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}';
};

export type GetMonitorServicesForServiceTypeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetMonitorServicesForServiceTypeError = GetMonitorServicesForServiceTypeErrors[keyof GetMonitorServicesForServiceTypeErrors];

export type GetMonitorServicesForServiceTypeResponses = {
    /**
     * Returns a paginated list of metric definitions.
     */
    200: {
        data: Array<{
            /**
             * The full name of Akamai Cloud Computing service. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
             */
            service_type: string;
        }>;
        /**
         * The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        page: number;
        /**
         * The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        pages: number;
        /**
         * The total number of results.
         */
        results: number;
    };
};

export type GetMonitorServicesForServiceTypeResponse = GetMonitorServicesForServiceTypeResponses[keyof GetMonitorServicesForServiceTypeResponses];

export type GetAlertDefinitionsForServiceTypeData = {
    body?: never;
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/alert-definitions';
};

export type GetAlertDefinitionsForServiceTypeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAlertDefinitionsForServiceTypeError = GetAlertDefinitionsForServiceTypeErrors[keyof GetAlertDefinitionsForServiceTypeErrors];

export type GetAlertDefinitionsForServiceTypeResponses = {
    /**
     * Returns a paginated list of alert definitions for the specified service type.
     */
    200: {
        data: Array<{
            /**
             * The alert channels set up for use with this alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation to see all of the available channels.
             */
            alert_channels: Array<{
                /**
                 * The unique identifier for this alert channel on your account.
                 */
                id: number;
                /**
                 * The name of the channel, used to display it in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The type of notification used with the channel. For a `user` alert definition, only `email` is supported.
                 */
                type: 'email';
                /**
                 * The URL for the channel that ends in the channel's `id`.
                 */
                url: string;
            }>;
            /**
             * The plan type for the Managed Database cluster, either `shared` or `dedicated`. This only applies to a `system` alert for a `service_type` of `dbaas` (Managed Databases). For `user` alerts for `dbaas`, this is returned as `null`.
             */
            class: 'shared' | 'dedicated';
            /**
             * When the alert definition was created.
             */
            created: string;
            /**
             * For a `user` alert definition, this is the user on your account that [created](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) it. For a `system` alert definition, this is returned as `system`.
             */
            created_by: string;
            /**
             * An additional description for the alert definition.
             */
            description: string;
            /**
             * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
             *
             * > 📘
             * >
             * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`. Other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
             */
            entity_ids: Array<string>;
            /**
             * Whether there are additional `entity_ids` associated with the alert for which the user doesn't have at least `read-only` access.
             */
            has_more_resources: boolean;
            /**
             * __Filterable__ The unique identifier for the alert definition.
             */
            id: number;
            /**
             * __Filterable__ The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * Details for the rules required to trigger the alert.
             */
            rule_criteria: {
                /**
                 * The individual rules that make up the alert definition.
                 */
                rules: Array<{
                    /**
                     * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                     */
                    aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                    /**
                     * Dimension filters for the rule.
                     */
                    dimension_filters: Array<{
                        /**
                         * The name of the dimension to be used in the filter.
                         */
                        dimension_label: string;
                        /**
                         * The name of the dimension filter. Used for display purposes.
                         */
                        label: string;
                        /**
                         * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                         */
                        operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                        /**
                         * The value to compare the `dimension_label` against.
                         */
                        value: string;
                    }>;
                    /**
                     * The name of the individual rule. This is used for display purposes in Akamai Cloud Manager.
                     */
                    label: string;
                    /**
                     * The metric to query.
                     */
                    metric: string;
                    /**
                     * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than, `gte` for greater than or equal, and `lte` for less than or equal.
                     */
                    operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                    /**
                     * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                     */
                    threshold: number;
                    /**
                     * The unit of the metric. This can be values like `percent` for percentage or `GB` for gigabyte.
                     */
                    unit: 'number' | 'byte' | 'second' | 'percent' | 'bit_per_second' | 'millisecond' | 'KB' | 'MB' | 'GB';
                }>;
            };
            /**
             * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
             */
            service_type: string;
            /**
             * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
             */
            severity: 0 | 1 | 2 | 3;
            /**
             * __Filterable__ The current status of the alert. This can be either `enabled`, `disabled`, `in progress`, or `failed`.
             */
            status: 'enabled' | 'disabled' | 'in progress' | 'failed';
            /**
             * The conditions that need to be met to send a notification for the alert.
             */
            trigger_conditions: {
                /**
                 * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`.  Currently, only `ALL` is supported.
                 */
                criteria_condition: 'ALL';
                /**
                 * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
                 *
                 * > 📘
                 * >
                 * > During the beta, only a value of `300` seconds is supported.
                 */
                evaluation_period_seconds: number;
                /**
                 * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
                 *
                 * > 📘
                 * >
                 * > During the beta, only a value of `300` seconds is supported.
                 */
                polling_interval_seconds: number;
                /**
                 * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
                 */
                trigger_occurrences: number;
            };
            /**
             * __Filterable__ The type of alert. This can be either `user` for an alert specific to the current user, or `system` for one that applies to all users on your account.
             */
            type: 'user' | 'system';
            /**
             * When the alert definition was last updated. This is the same as `created` if the alert definition hasn't been updated.
             */
            updated: string;
            /**
             * For a `user` alert definition, this is the user on your account that last [updated](https://techdocs.akamai.com/linode-api/reference/put-alert-definition) it. For a `system` alert definition, this is returned as `system`. If it hasn't been updated, this value is the same as `created_by`.
             */
            updated_by: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetAlertDefinitionsForServiceTypeResponse = GetAlertDefinitionsForServiceTypeResponses[keyof GetAlertDefinitionsForServiceTypeResponses];

export type PostAlertDefinitionForServiceTypeData = {
    body: {
        /**
         * The identifiers for the alert channels to use for the alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation and store the `id` for the applicable channels.
         */
        channel_ids: Array<number>;
        /**
         * An additional description for the alert definition.
         */
        description?: string;
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response. You also need `read_only` access to the [scope](https://techdocs.akamai.com/linode-api/reference/get-started#oauth-reference) for the `service_type` for each of the `entity_ids`.
         *
         * > 📘
         * >
         * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`, while other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
         */
        entity_ids?: Array<string>;
        /**
         * The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Details for the rules required to trigger the alert.
         */
        rule_criteria: {
            /**
             * The individual rules that make up the alert definition.
             */
            rules: Array<{
                /**
                 * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                 */
                aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                /**
                 * Individual objects that define dimension filters for the rule.
                 */
                dimension_filters: Array<{
                    /**
                     * The name of the dimension to be used in the filter.
                     */
                    dimension_label: string;
                    /**
                     * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                     */
                    operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                    /**
                     * The value to compare the `dimension_label` against.
                     */
                    value: string;
                }>;
                /**
                 * The metric to query.
                 */
                metric: string;
                /**
                 * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than `gte` for greater than or equal, and `lte` for less than or equal.
                 */
                operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                /**
                 * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                 */
                threshold: number;
            }>;
        };
        /**
         * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
         */
        severity: 0 | 1 | 2 | 3;
        /**
         * The conditions that need to be met to send a notification for the alert.
         */
        trigger_conditions: {
            /**
             * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`. Currently, only `ALL` is supported.
             */
            criteria_condition: 'ALL';
            /**
             * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            evaluation_period_seconds: number;
            /**
             * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            polling_interval_seconds: number;
            /**
             * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
             */
            trigger_occurrences: number;
        };
    };
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/alert-definitions';
};

export type PostAlertDefinitionForServiceTypeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAlertDefinitionForServiceTypeError = PostAlertDefinitionForServiceTypeErrors[keyof PostAlertDefinitionForServiceTypeErrors];

export type PostAlertDefinitionForServiceTypeResponses = {
    /**
     * The alert definition is created.
     */
    200: {
        /**
         * The alert channels set up for use with this alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation to see all of the available channels.
         */
        alert_channels: Array<{
            /**
             * The unique identifier for this alert channel on your account.
             */
            id: number;
            /**
             * The name of the channel, used to display it in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The type of notification used with the channel. For a `user` alert definition, only `email` is supported.
             */
            type: 'email';
            /**
             * The URL for the channel that ends in the channel's `id`.
             */
            url: string;
        }>;
        /**
         * The plan type for the Managed Database cluster, either `shared` or `dedicated`. This only applies to a `system` alert for a `service_type` of `dbaas` (Managed Databases). For `user` alerts for `dbaas`, this is returned as `null`.
         */
        class: 'shared' | 'dedicated';
        /**
         * When the alert definition was created.
         */
        created: string;
        /**
         * For a `user` alert definition, this is the user on your account that [created](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) it. For a `system` alert definition, this is returned as `system`.
         */
        created_by: string;
        /**
         * An additional description for the alert definition.
         */
        description: string;
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
         *
         * > 📘
         * >
         * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`. Other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
         */
        entity_ids: Array<string>;
        /**
         * Whether there are additional `entity_ids` associated with the alert for which the user doesn't have at least `read-only` access.
         */
        has_more_resources: boolean;
        /**
         * __Filterable__ The unique identifier for the alert definition.
         */
        id: number;
        /**
         * __Filterable__ The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Details for the rules required to trigger the alert.
         */
        rule_criteria: {
            /**
             * The individual rules that make up the alert definition.
             */
            rules: Array<{
                /**
                 * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                 */
                aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                /**
                 * Dimension filters for the rule.
                 */
                dimension_filters: Array<{
                    /**
                     * The name of the dimension to be used in the filter.
                     */
                    dimension_label: string;
                    /**
                     * The name of the dimension filter. Used for display purposes.
                     */
                    label: string;
                    /**
                     * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                     */
                    operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                    /**
                     * The value to compare the `dimension_label` against.
                     */
                    value: string;
                }>;
                /**
                 * The name of the individual rule. This is used for display purposes in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The metric to query.
                 */
                metric: string;
                /**
                 * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than, `gte` for greater than or equal, and `lte` for less than or equal.
                 */
                operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                /**
                 * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                 */
                threshold: number;
                /**
                 * The unit of the metric. This can be values like `percent` for percentage or `GB` for gigabyte.
                 */
                unit: 'number' | 'byte' | 'second' | 'percent' | 'bit_per_second' | 'millisecond' | 'KB' | 'MB' | 'GB';
            }>;
        };
        /**
         * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
         */
        service_type: string;
        /**
         * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
         */
        severity: 0 | 1 | 2 | 3;
        /**
         * __Filterable__ The current status of the alert. This can be either `enabled`, `disabled`, `in progress`, or `failed`.
         */
        status: 'enabled' | 'disabled' | 'in progress' | 'failed';
        /**
         * The conditions that need to be met to send a notification for the alert.
         */
        trigger_conditions: {
            /**
             * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`.  Currently, only `ALL` is supported.
             */
            criteria_condition: 'ALL';
            /**
             * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            evaluation_period_seconds: number;
            /**
             * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            polling_interval_seconds: number;
            /**
             * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
             */
            trigger_occurrences: number;
        };
        /**
         * __Filterable__ The type of alert. This can be either `user` for an alert specific to the current user, or `system` for one that applies to all users on your account.
         */
        type: 'user' | 'system';
        /**
         * When the alert definition was last updated. This is the same as `created` if the alert definition hasn't been updated.
         */
        updated: string;
        /**
         * For a `user` alert definition, this is the user on your account that last [updated](https://techdocs.akamai.com/linode-api/reference/put-alert-definition) it. For a `system` alert definition, this is returned as `system`. If it hasn't been updated, this value is the same as `created_by`.
         */
        updated_by: string;
    };
};

export type PostAlertDefinitionForServiceTypeResponse = PostAlertDefinitionForServiceTypeResponses[keyof PostAlertDefinitionForServiceTypeResponses];

export type DeleteAlertDefinitionData = {
    body?: never;
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
        /**
         * The unique identifier assigned to the alert definition. Run the [List alert definitions](https://techdocs.akamai.com/linode-api/reference/get-alert-definitions) operation and store the `id` for the applicable alert definition.
         */
        alertId: number;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/alert-definitions/{alertId}';
};

export type DeleteAlertDefinitionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteAlertDefinitionError = DeleteAlertDefinitionErrors[keyof DeleteAlertDefinitionErrors];

export type DeleteAlertDefinitionResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteAlertDefinitionResponse = DeleteAlertDefinitionResponses[keyof DeleteAlertDefinitionResponses];

export type GetAlertDefinitionData = {
    body?: never;
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
        /**
         * The unique identifier assigned to the alert definition. Run the [List alert definitions](https://techdocs.akamai.com/linode-api/reference/get-alert-definitions) operation and store the `id` for the applicable alert definition.
         */
        alertId: number;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/alert-definitions/{alertId}';
};

export type GetAlertDefinitionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetAlertDefinitionError = GetAlertDefinitionErrors[keyof GetAlertDefinitionErrors];

export type GetAlertDefinitionResponses = {
    /**
     * Returns the specified alert definition.
     */
    200: {
        /**
         * The alert channels set up for use with this alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation to see all of the available channels.
         */
        alert_channels: Array<{
            /**
             * The unique identifier for this alert channel on your account.
             */
            id: number;
            /**
             * The name of the channel, used to display it in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The type of notification used with the channel. For a `user` alert definition, only `email` is supported.
             */
            type: 'email';
            /**
             * The URL for the channel that ends in the channel's `id`.
             */
            url: string;
        }>;
        /**
         * The plan type for the Managed Database cluster, either `shared` or `dedicated`. This only applies to a `system` alert for a `service_type` of `dbaas` (Managed Databases). For `user` alerts for `dbaas`, this is returned as `null`.
         */
        class: 'shared' | 'dedicated';
        /**
         * When the alert definition was created.
         */
        created: string;
        /**
         * For a `user` alert definition, this is the user on your account that [created](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) it. For a `system` alert definition, this is returned as `system`.
         */
        created_by: string;
        /**
         * An additional description for the alert definition.
         */
        description: string;
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
         *
         * > 📘
         * >
         * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`. Other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
         */
        entity_ids: Array<string>;
        /**
         * Whether there are additional `entity_ids` associated with the alert for which the user doesn't have at least `read-only` access.
         */
        has_more_resources: boolean;
        /**
         * __Filterable__ The unique identifier for the alert definition.
         */
        id: number;
        /**
         * __Filterable__ The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Details for the rules required to trigger the alert.
         */
        rule_criteria: {
            /**
             * The individual rules that make up the alert definition.
             */
            rules: Array<{
                /**
                 * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                 */
                aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                /**
                 * Dimension filters for the rule.
                 */
                dimension_filters: Array<{
                    /**
                     * The name of the dimension to be used in the filter.
                     */
                    dimension_label: string;
                    /**
                     * The name of the dimension filter. Used for display purposes.
                     */
                    label: string;
                    /**
                     * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                     */
                    operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                    /**
                     * The value to compare the `dimension_label` against.
                     */
                    value: string;
                }>;
                /**
                 * The name of the individual rule. This is used for display purposes in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The metric to query.
                 */
                metric: string;
                /**
                 * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than, `gte` for greater than or equal, and `lte` for less than or equal.
                 */
                operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                /**
                 * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                 */
                threshold: number;
                /**
                 * The unit of the metric. This can be values like `percent` for percentage or `GB` for gigabyte.
                 */
                unit: 'number' | 'byte' | 'second' | 'percent' | 'bit_per_second' | 'millisecond' | 'KB' | 'MB' | 'GB';
            }>;
        };
        /**
         * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
         */
        service_type: string;
        /**
         * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
         */
        severity: 0 | 1 | 2 | 3;
        /**
         * __Filterable__ The current status of the alert. This can be either `enabled`, `disabled`, `in progress`, or `failed`.
         */
        status: 'enabled' | 'disabled' | 'in progress' | 'failed';
        /**
         * The conditions that need to be met to send a notification for the alert.
         */
        trigger_conditions: {
            /**
             * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`.  Currently, only `ALL` is supported.
             */
            criteria_condition: 'ALL';
            /**
             * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            evaluation_period_seconds: number;
            /**
             * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            polling_interval_seconds: number;
            /**
             * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
             */
            trigger_occurrences: number;
        };
        /**
         * __Filterable__ The type of alert. This can be either `user` for an alert specific to the current user, or `system` for one that applies to all users on your account.
         */
        type: 'user' | 'system';
        /**
         * When the alert definition was last updated. This is the same as `created` if the alert definition hasn't been updated.
         */
        updated: string;
        /**
         * For a `user` alert definition, this is the user on your account that last [updated](https://techdocs.akamai.com/linode-api/reference/put-alert-definition) it. For a `system` alert definition, this is returned as `system`. If it hasn't been updated, this value is the same as `created_by`.
         */
        updated_by: string;
    };
};

export type GetAlertDefinitionResponse = GetAlertDefinitionResponses[keyof GetAlertDefinitionResponses];

export type PutAlertDefinitionData = {
    body: {
        /**
         * The identifiers for the alert channels to use for the alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation and store the `id` for the applicable channels.
         */
        channel_ids: Array<number>;
        /**
         * An additional description for the alert definition.
         */
        description: string;
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response. You also need `read_only` access to the [scope](https://techdocs.akamai.com/linode-api/reference/get-started#oauth-reference) for the `service_type` for each of the `entity_ids`.
         *
         * > 📘
         * >
         * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`. Other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
         */
        entity_ids: Array<string>;
        /**
         * The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Details for the rules required to trigger the alert.
         */
        rule_criteria: {
            /**
             * The individual rules that make up the alert definition.
             */
            rules: Array<{
                /**
                 * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                 */
                aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                /**
                 * Individual objects that define dimension filters for the rule.
                 */
                dimension_filters: Array<{
                    /**
                     * The name of the dimension to be used in the filter.
                     */
                    dimension_label: string;
                    /**
                     * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                     */
                    operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                    /**
                     * The value to compare the `dimension_label` against.
                     */
                    value: string;
                }>;
                /**
                 * The metric to query.
                 */
                metric: string;
                /**
                 * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than, `gte` for greater than or equal, and `lte` for less than or equal.
                 */
                operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                /**
                 * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                 */
                threshold: number;
            }>;
        };
        /**
         * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
         */
        severity: 0 | 1 | 2 | 3;
        /**
         * The current status of the alert. You can set this to `enabled` or `disabled`.
         */
        status: 'enabled' | 'disabled';
        /**
         * The conditions that need to be met to send a notification for the alert.
         */
        trigger_conditions: {
            /**
             * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`.  Currently, only `ALL` is supported.
             */
            criteria_condition: 'ALL';
            /**
             * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            evaluation_period_seconds: number;
            /**
             * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            polling_interval_seconds: number;
            /**
             * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
             */
            trigger_occurrences: number;
        };
    };
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
        /**
         * The unique identifier assigned to the alert definition. Run the [List alert definitions](https://techdocs.akamai.com/linode-api/reference/get-alert-definitions) operation and store the `id` for the applicable alert definition.
         */
        alertId: number;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/alert-definitions/{alertId}';
};

export type PutAlertDefinitionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutAlertDefinitionError = PutAlertDefinitionErrors[keyof PutAlertDefinitionErrors];

export type PutAlertDefinitionResponses = {
    /**
     * The alert definition is updated.
     */
    200: {
        /**
         * The alert channels set up for use with this alert. Run the [List alert channels](https://techdocs.akamai.com/linode-api/reference/get-alert-channels) operation to see all of the available channels.
         */
        alert_channels: Array<{
            /**
             * The unique identifier for this alert channel on your account.
             */
            id: number;
            /**
             * The name of the channel, used to display it in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The type of notification used with the channel. For a `user` alert definition, only `email` is supported.
             */
            type: 'email';
            /**
             * The URL for the channel that ends in the channel's `id`.
             */
            url: string;
        }>;
        /**
         * The plan type for the Managed Database cluster, either `shared` or `dedicated`. This only applies to a `system` alert for a `service_type` of `dbaas` (Managed Databases). For `user` alerts for `dbaas`, this is returned as `null`.
         */
        class: 'shared' | 'dedicated';
        /**
         * When the alert definition was created.
         */
        created: string;
        /**
         * For a `user` alert definition, this is the user on your account that [created](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) it. For a `system` alert definition, this is returned as `system`.
         */
        created_by: string;
        /**
         * An additional description for the alert definition.
         */
        description: string;
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
         *
         * > 📘
         * >
         * > The format `type` for an `entity_id` may vary, based on the Akamai Cloud `service_type`. For example, the `dbaas` service returns an integer value for an `id`, that you'd use for the `entity_id`. Other services may return a string for their `id`. With the Alerts operations, all of these formats are recognized as an `entity_id`, when you include them as a `string`.
         */
        entity_ids: Array<string>;
        /**
         * Whether there are additional `entity_ids` associated with the alert for which the user doesn't have at least `read-only` access.
         */
        has_more_resources: boolean;
        /**
         * __Filterable__ The unique identifier for the alert definition.
         */
        id: number;
        /**
         * __Filterable__ The name of the alert definition. This is used for display purposes in Akamai Cloud Manager.
         */
        label: string;
        /**
         * Details for the rules required to trigger the alert.
         */
        rule_criteria: {
            /**
             * The individual rules that make up the alert definition.
             */
            rules: Array<{
                /**
                 * The aggregation function applied to the `metric`. Available values are `avg`, `sum`, `min`, and `max`.
                 */
                aggregate_function: 'avg' | 'sum' | 'min' | 'max';
                /**
                 * Dimension filters for the rule.
                 */
                dimension_filters: Array<{
                    /**
                     * The name of the dimension to be used in the filter.
                     */
                    dimension_label: string;
                    /**
                     * The name of the dimension filter. Used for display purposes.
                     */
                    label: string;
                    /**
                     * The operator to apply to the dimension filter. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
                     */
                    operator: 'eq' | 'neq' | 'startswith' | 'endswith';
                    /**
                     * The value to compare the `dimension_label` against.
                     */
                    value: string;
                }>;
                /**
                 * The name of the individual rule. This is used for display purposes in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The metric to query.
                 */
                metric: string;
                /**
                 * The operator to apply to the metric. Available values are `eq` for equal, `gt` for greater than, `lt` for less than, `gte` for greater than or equal, and `lte` for less than or equal.
                 */
                operator: 'eq' | 'gt' | 'lt' | 'gte' | 'lte';
                /**
                 * The predefined value or condition that triggers an alert when met or exceeded. For example, for `avg(cpu_usage) > 80`, the `threshold` is 80.
                 */
                threshold: number;
                /**
                 * The unit of the metric. This can be values like `percent` for percentage or `GB` for gigabyte.
                 */
                unit: 'number' | 'byte' | 'second' | 'percent' | 'bit_per_second' | 'millisecond' | 'KB' | 'MB' | 'GB';
            }>;
        };
        /**
         * The identifier for the Akamai Cloud Computing service. Use this value to call out the service in other Monitor operations in the API.
         */
        service_type: string;
        /**
         * The severity of the alert. Supported values include `3` for info, `2` for low, `1` for medium, and `0` for severe.
         */
        severity: 0 | 1 | 2 | 3;
        /**
         * __Filterable__ The current status of the alert. This can be either `enabled`, `disabled`, `in progress`, or `failed`.
         */
        status: 'enabled' | 'disabled' | 'in progress' | 'failed';
        /**
         * The conditions that need to be met to send a notification for the alert.
         */
        trigger_conditions: {
            /**
             * Signifies the logical operation applied when multiple metrics are set for an alert definition. For example, if you wanted to apply both `cpu_usage > 90` and `memory_usage > 80`, `ALL` is the `criteria_condition`.  Currently, only `ALL` is supported.
             */
            criteria_condition: 'ALL';
            /**
             * The time period over which data is collected before evaluating whether the alert definition's `threshold` has been met or exceeded.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            evaluation_period_seconds: number;
            /**
             * The frequency at which the `metric` is checked for a change in state. For example, with `cpu_usage` set as your `metric` and this set to `300`, your `cpu_usage` is checked every 5 minutes for some change in its state.
             *
             * > 📘
             * >
             * > During the beta, only a value of `300` seconds is supported.
             */
            polling_interval_seconds: number;
            /**
             * The minimum number of consecutive `polling_interval_seconds` periods that the `threshold` needs to be breached to trigger the alert.
             */
            trigger_occurrences: number;
        };
        /**
         * __Filterable__ The type of alert. This can be either `user` for an alert specific to the current user, or `system` for one that applies to all users on your account.
         */
        type: 'user' | 'system';
        /**
         * When the alert definition was last updated. This is the same as `created` if the alert definition hasn't been updated.
         */
        updated: string;
        /**
         * For a `user` alert definition, this is the user on your account that last [updated](https://techdocs.akamai.com/linode-api/reference/put-alert-definition) it. For a `system` alert definition, this is returned as `system`. If it hasn't been updated, this value is the same as `created_by`.
         */
        updated_by: string;
    };
};

export type PutAlertDefinitionResponse = PutAlertDefinitionResponses[keyof PutAlertDefinitionResponses];

export type GetDashboardsData = {
    body?: never;
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/dashboards';
};

export type GetDashboardsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDashboardsError = GetDashboardsErrors[keyof GetDashboardsErrors];

export type GetDashboardsResponses = {
    /**
     * Returns a paginated list of dashboards.
     */
    200: {
        data: Array<{
            /**
             * When the dashboard was created.
             */
            created: string;
            /**
             * The unique ID assigned to the dashboard.
             */
            id: number;
            /**
             * The name of the dashboard. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The Akamai Cloud Computing service used by this dashboard.
             */
            service_type: string;
            /**
             * The type of dashboard. Currently, this can only be `standard` for a dashboard, which uses default formatting.
             */
            type: 'standard';
            /**
             * When the dashboard was last updated.
             */
            updated: string;
            /**
             * The widgets used in the dashboard.
             */
            widgets: Array<{
                /**
                 * The aggregate function for the metric. This defaults to `avg` for average.
                 */
                aggregate_function: 'min' | 'max' | 'avg' | 'sum' | 'rate' | 'increase';
                /**
                 * The type of chart used in the widget. This can be `line` for a line chart or `area` for an area chart.
                 */
                chart_type: 'line' | 'area';
                /**
                 * The color used in the widget.
                 */
                color: string;
                /**
                 * The name of the widget. This is used for display purposes in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * The metric used in the widget.
                 */
                metric: string;
                /**
                 * The size of the widget. This can be `6` or `12` grid units, expressed as strings.
                 */
                size: '6' | '12';
                /**
                 * The unit of the metric. This can be values like `%` for percentage or `GB` for gigabyte.
                 */
                unit: '%' | 'Bytes' | 'sec' | 'bps' | 'msec' | 'Bps' | 'KB' | 'MB' | 'GB' | 'rate' | 'percentile' | 'ratio' | 'OPS' | 'IOPS';
                /**
                 * The label for the y-axis in the widget's chart.
                 */
                y_label: string;
            }>;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetDashboardsResponse = GetDashboardsResponses[keyof GetDashboardsResponses];

export type GetMonitorInformationData = {
    body?: never;
    headers?: {
        /**
         * Specifies the optional `X-Filter` header JSON object's filtering and sort criteria.
         */
        'X-Filter'?: {
            /**
             * Filter by whether the metric is alertable (`true` or `false`).
             */
            is_alertable?: boolean;
        } | {
            /**
             * Filter by the metric type. Available values are `counter`, `histogram`, `gauge`, or `summary`.
             */
            metric_type?: 'counter' | 'histogram' | 'gauge' | 'summary';
        } | {
            /**
             * All conditions need to be true. Include both `is_alertable` and `metric_type` conditions.
             */
            '+and'?: Array<{
                is_alertable: boolean;
                metric_type: 'counter' | 'histogram' | 'gauge' | 'summary';
            }>;
        } | {
            /**
             * At least one condition needs to be true. Include both `is_alertable` and `metric_type` conditions.
             */
            '+or'?: Array<{
                is_alertable: boolean;
                metric_type: 'counter' | 'histogram' | 'gauge' | 'summary';
            }>;
        };
    };
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/metric-definitions';
};

export type GetMonitorInformationErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetMonitorInformationError = GetMonitorInformationErrors[keyof GetMonitorInformationErrors];

export type GetMonitorInformationResponses = {
    /**
     * Returns a paginated list of metric information.
     */
    200: {
        data: Array<{
            /**
             * Available aggregate functions for the metric.
             */
            available_aggregate_functions: Array<'min' | 'max' | 'avg' | 'sum' | 'rate' | 'increase'>;
            /**
             * Dimensions for the metric.
             */
            dimensions: Array<{
                /**
                 * The label used to represent the dimension in metrics. Use this value to call out the service in other Monitor operations in the API.
                 */
                dimension_label: string;
                /**
                 * The label of the dimension. This is used for display purposes in Akamai Cloud Manager.
                 */
                label: string;
                /**
                 * Available values for the `dimension_label`.
                 */
                values: Array<string>;
            }>;
            /**
             * Whether the metric is alertable.
             */
            is_alertable: boolean;
            /**
             * The name of the metric. This is used for display purposes in Akamai Cloud Manager.
             */
            label: string;
            /**
             * The identifier for the metric. This is how the metric is called out in other Monitor operations in the API.
             */
            metric: string;
            /**
             * The type of metric. A value of `counter` represents variable values and a set target for the count. A `histogram` represents the frequency distribution of data points across a continuous range of numerical values. A `gauge` represents the performance against a target goal, and `summary` presents and summarizes data from multiple sources.
             */
            metric_type: 'counter' | 'histogram' | 'gauge' | 'summary';
            /**
             * How frequently a metric is scraped to gather data, using `s` for seconds, `m` for minutes, or `h` for hours. Set to `60s`, the metric would be scraped every 60 seconds. Set to `2m`, the scrape occurs every two minutes.
             */
            scrape_interval: string;
            /**
             * The unit of measurement for the metric.
             */
            unit: '%' | 'Bytes' | 'sec' | 'bps' | 'msec' | 'Bps' | 'KB' | 'MB' | 'GB' | 'rate' | 'percentile' | 'ratio' | 'OPS' | 'IOPS';
        }>;
        /**
         * The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        page: number;
        /**
         * The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        pages: number;
        /**
         * The total number of results.
         */
        results: number;
    };
};

export type GetMonitorInformationResponse = GetMonitorInformationResponses[keyof GetMonitorInformationResponses];

export type PostReadMetricData = {
    /**
     * Settings used for data queries.
     */
    body: ({
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
         */
        entity_ids: Array<number>;
        /**
         * Individual objects that define dimension filters for the query.
         */
        filters?: Array<{
            /**
             * The name of the dimension label to filter on.
             */
            dimension_label: string;
            /**
             * The operator to apply to the dimension label. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
             */
            operator: 'eq' | 'neq' | 'startswith' | 'endswith';
            /**
             * The value to compare the dimension label against.
             */
            value: string;
        }>;
        /**
         * Categorizes response data based on the specified fields:
         *
         * * `entity_id`. Include this parameter by name to group the response data using the `entity_id` values included in the request.
         *
         * * `dimension_label`. This represents the desired dimension from a monitoring metric. To get a `dimension_label`, run the [List metrics for a service type](https://techdocs.akamai.com/linode-api/reference/get-monitor-information) operation, review the available `dimensions`, and store the applicable `dimension_label` values.
         *
         * You can group these fields using these combinations:
         *
         * * Group by `entity_id` values and one or more `dimensions_label` values. For example, `[entity_id, node_type]` groups response data based on values set for the `entity_id` parameter and the `node_type` dimension that's available for a `service_type` of `dbaas`.
         *
         * * Group by only the `entity_id` values. For example, `[entity_id]` exclusively groups response data based on values set for the `entity_id` parameter.
         *
         * * Group by one or more `dimension_label` values. For example, `[node_type]` groups response data based on the `node_type` dimension that's available for a `service_type` of `dbaas`.
         *
         * If you leave this out of a request, data is aggregated across all series.
         */
        group_by?: Array<string>;
        /**
         * A list of metric objects, each specifying a metric name and its corresponding aggregation function.
         */
        metrics: Array<{
            /**
             * The aggregation function applied to the metric. Available values are `min`, `max`, `avg`, and `sum`.
             */
            aggregate_function: 'min' | 'max' | 'avg' | 'sum';
            /**
             * The metric to query.
             */
            name: string;
        }>;
        /**
         * Allows for an optional time granularity setting for metric data.
         */
        time_granularity?: {
            /**
             * The unit of time granularity for the metric data. Available values are `sec`, `min`, `hr`, and `days`.
             */
            unit: 'sec' | 'min' | 'hr' | 'days';
            /**
             * The value that corresponds to the `unit`. Set to `10` with a `unit` of `hr` for 10 hours.
             */
            value: number;
        };
    } & {
        /**
         * Specifies a relative time duration for data queries. Queries begin immediately for the specified amount of time. You can specify a `relative_time_duration` or an `absolute_time_duration`, but not both.
         */
        relative_time_duration: {
            /**
             * The unit of time used for the relative duration to query metric data. Available values are `sec`, `min`, `hr`, and `days`.
             */
            unit: 'sec' | 'min' | 'hr' | 'days';
            /**
             * The value that corresponds to the `unit`. Set to `30` with a `unit` of `min` for 30 minutes.
             */
            value: number;
        };
    }) | ({
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
         */
        entity_ids: Array<number>;
        /**
         * Individual objects that define dimension filters for the query.
         */
        filters?: Array<{
            /**
             * The name of the dimension label to filter on.
             */
            dimension_label: string;
            /**
             * The operator to apply to the dimension label. Available values are `eq` for equal, `neq` for not equal, `startswith`, and `endswith`.
             */
            operator: 'eq' | 'neq' | 'startswith' | 'endswith';
            /**
             * The value to compare the dimension label against.
             */
            value: string;
        }>;
        /**
         * Categorizes response data based on the specified fields:
         *
         * * `entity_id`. Include this parameter by name to group the response data using the `entity_id` values included in the request.
         *
         * * `dimension_label`. This represents the desired dimension from a monitoring metric. To get a `dimension_label`, run the [List metrics for a service type](https://techdocs.akamai.com/linode-api/reference/get-monitor-information) operation, review the available `dimensions`, and store the applicable `dimension_label` values.
         *
         * You can group these fields using these combinations:
         *
         * * Group by `entity_id` values and one or more `dimensions_label` values. For example, `[entity_id, node_type]` groups response data based on values set for the `entity_id` parameter and the `node_type` dimension that's available for a `service_type` of `dbaas`.
         *
         * * Group by only the `entity_id` values. For example, `[entity_id]` exclusively groups response data based on values set for the `entity_id` parameter.
         *
         * * Group by one or more `dimension_label` values. For example, `[node_type]` groups response data based on the `node_type` dimension that's available for a `service_type` of `dbaas`.
         *
         * If you leave this out of a request, data is aggregated across all series.
         */
        group_by?: Array<string>;
        /**
         * A list of metric objects, each specifying a metric name and its corresponding aggregation function.
         */
        metrics: Array<{
            /**
             * The aggregation function applied to the metric. Available values are `min`, `max`, `avg`, and `sum`.
             */
            aggregate_function: 'min' | 'max' | 'avg' | 'sum';
            /**
             * The metric to query.
             */
            name: string;
        }>;
        /**
         * Allows for an optional time granularity setting for metric data.
         */
        time_granularity?: {
            /**
             * The unit of time granularity for the metric data. Available values are `sec`, `min`, `hr`, and `days`.
             */
            unit: 'sec' | 'min' | 'hr' | 'days';
            /**
             * The value that corresponds to the `unit`. Set to `10` with a `unit` of `hr` for 10 hours.
             */
            value: number;
        };
    } & {
        /**
         * Specifies an absolute time range for data queries. Queries begin and end at a specific time.
         *
         * > 📘
         * >
         * > - You can query data for up to 31 days.
         * >
         * > - You can specify a `relative_time_duration` or an `absolute_time_duration`, but not both.
         */
        absolute_time_duration: {
            /**
             * The end time for querying data, in ISO 8601 format, using the UTC time zone.
             */
            end: string;
            /**
             * The start time for querying data, in ISO 8601 format, using the UTC time zone.
             */
            start: string;
        };
    });
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/metrics';
};

export type PostReadMetricErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostReadMetricError = PostReadMetricErrors[keyof PostReadMetricErrors];

export type PostReadMetricResponses = {
    /**
     * Metrics returned.
     */
    200: {
        /**
         * A collection of information for the metric.
         */
        data: {
            /**
             * Pairs of a specific `metric` and its corresponding `values`.
             */
            result: Array<{
                /**
                 * A mapping of labels where keys and values are strings representing details for the metric.
                 */
                metric: {
                    [key: string]: string;
                };
                /**
                 * Pairs that consist of an epoch timestamp and a corresponding metric value.
                 */
                values: Array<Array<number | string>>;
            }>;
            /**
             * The type of result, which will always be `matrix` in this context.
             */
            resultType: 'matrix';
        };
        /**
         * Indicates whether the result is partial. A result of `false` indicates the response is complete, while `true` indicates a partial response.
         */
        isPartial: boolean;
        /**
         * Statistics for a query against a `metric`.
         */
        stats: {
            /**
             * The time taken to execute the query, in milliseconds.
             */
            executionTimeMsec: number;
            /**
             * The number of metric series fetched in the query.
             */
            seriesFetched: string;
        };
        /**
         * The status of the query execution, such as `success`.
         */
        status: string;
    };
};

export type PostReadMetricResponse = PostReadMetricResponses[keyof PostReadMetricResponses];

export type PostGetTokenData = {
    body: {
        /**
         * The `id` for each individual entity from a `service_type`. Get this value by running the list operation for the appropriate entity. For example, if your entity is one of your PostgreSQL databases, run the [List PostgreSQL Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-postgre-sql-instances) operation and store the `id` for the appropriate database from the response.
         *
         * > 📘
         * >
         * > - This field is required and supports up to 100 total entries.
         * >
         * > - You need read access permission to all of the entities you include in the token. Run the [List a user's grants](https://techdocs.akamai.com/linode-api/reference/get-user-grants) operation to see the permission level for each entity in your account.
         */
        entity_ids: Array<number>;
    };
    path: {
        /**
         * The Akamai Cloud Computing service being monitored. To see your currently supported services, run the [List supported service types](https://techdocs.akamai.com/linode-api/reference/get-monitor-services) operation and store the appropriate `service_type`.
         */
        serviceType: string;
    };
    query?: never;
    url: '/monitor/services/{serviceType}/token';
};

export type PostGetTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostGetTokenError = PostGetTokenErrors[keyof PostGetTokenErrors];

export type PostGetTokenResponses = {
    /**
     * The response provides the token.
     */
    200: {
        /**
         * A string representing the authentication token. This token is required for subsequent requests.
         */
        token: string;
    };
};

export type PostGetTokenResponse = PostGetTokenResponses[keyof PostGetTokenResponses];

export type GetNetworkTransferPricesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/network-transfer/prices';
};

export type GetNetworkTransferPricesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNetworkTransferPricesError = GetNetworkTransferPricesErrors[keyof GetNetworkTransferPricesErrors];

export type GetNetworkTransferPricesResponses = {
    /**
     * A collection of network transfer prices.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The ID representing the network transfer price.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The network transfer price label is for display purposes only.
             */
            readonly label: string;
            /**
             * __Read-only__ The default cost of this network transfer. Prices are in US dollars, broken down into hourly and monthly charges.
             *
             * Certain regions have different prices from the default. For region-specific prices, see `region_prices`.
             */
            readonly price: {
                /**
                 * __Filterable__ Cost (in US dollars) per hour.
                 */
                hourly: number;
                /**
                 * __Filterable__ Cost per month, in US dollars.
                 */
                monthly: number | null;
            };
            region_prices: Array<{
                /**
                 * Cost per hour for this region, in US dollars.
                 */
                hourly: number;
                /**
                 * The Region ID for these prices.
                 */
                id: string;
                /**
                 * Cost per month for this region, in US dollars.
                 */
                monthly: number | null;
            }>;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount, in MB.
             */
            readonly transfer: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetNetworkTransferPricesResponse = GetNetworkTransferPricesResponses[keyof GetNetworkTransferPricesResponses];

export type GetFirewallsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/firewalls';
};

export type GetFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallsError = GetFirewallsErrors[keyof GetFirewallsErrors];

export type GetFirewallsResponses = {
    /**
     * Returns an array of Firewalls.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this Firewall was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
             */
            readonly entities: Array<{
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a `linode`.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'linode_interface' | 'nodebalancer';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            }>;
            /**
             * __Filterable__, __Read-only__ The Firewall's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The Firewall's label, for display purposes only.
             *
             * Firewall labels have the following constraints:
             *
             * - Must begin and end with an alphanumeric character.
             * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             * - Must be between 3 and 32 characters.
             * - Must be unique.
             */
            label: string;
            /**
             * The inbound and outbound access rules to apply to the Firewall.
             *
             * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
             *
             * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
                 */
                readonly fingerprint: string;
                /**
                 * The inbound rules for the firewall, as a JSON array.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall, as a JSON array.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
                 */
                readonly version: number;
            };
            /**
             * __Read-only__ The status of this Firewall.
             *
             * - When a Firewall is first created its status is `enabled`.
             * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
             * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
             */
            status: 'enabled' | 'disabled' | 'deleted';
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Filterable__, __Read-only__ When this Firewall was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetFirewallsResponse = GetFirewallsResponses[keyof GetFirewallsResponses];

export type PostFirewallsData = {
    /**
     * Creates a firewall object that can be applied to a service to filter the service's network traffic.
     */
    body?: {
        /**
         * __Filterable__, __Read-only__ When this Firewall was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
         */
        readonly entities: Array<{
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a `linode`.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'linode_interface' | 'nodebalancer';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        }>;
        /**
         * __Filterable__, __Read-only__ The Firewall's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The inbound and outbound access rules to apply to the Firewall.
         *
         * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
         *
         * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
             */
            readonly fingerprint: string;
            /**
             * The inbound rules for the firewall, as a JSON array.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall, as a JSON array.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
            /**
             * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
             */
            readonly version: number;
        };
        /**
         * __Read-only__ The status of this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled' | 'deleted';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Filterable__, __Read-only__ When this Firewall was last updated.
         */
        readonly updated: string;
    } & {
        /**
         * Devices to create for this firewall. When a device is created, the firewall is assigned to its associated service. Currently, devices can be created for Linodes using legacy configuration profiles, Linode interfaces, and NodeBalancers. Firewall devices can't be created for individual legacy configuration profile interfaces.
         *
         * Additional devices can be assigned after Firewall creation by using the [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) operation.
         */
        devices?: {
            /**
             * An array of Linode interface IDs. A firewall device is created for each ID. For Linodes using Linode interfaces, firewalls need to be assigned to Linode interfaces and not the Linode.
             */
            linode_interfaces: Array<number>;
            /**
             * An array of Linode IDs. A firewall device is created for each ID. These Linodes can't use Linode interfaces.
             */
            linodes: Array<number>;
            /**
             * An array containing a NodeBalancer ID. A Firewall device is created for the ID.
             *
             * - A NodeBalancer can have only one Firewall assigned to it.
             * - Firewalls only apply to inbound TCP traffic to NodeBalancers.
             */
            nodebalancers: Array<number>;
        };
        rules: {
            inbound?: unknown;
            outbound?: unknown;
        };
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
    };
    path?: never;
    query?: never;
    url: '/networking/firewalls';
};

export type PostFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostFirewallsError = PostFirewallsErrors[keyof PostFirewallsErrors];

export type PostFirewallsResponses = {
    /**
     * A resource that controls incoming and outgoing network traffic to a compute service. Only one enabled Firewall can be attached to a particular service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) to assign a Firewall to a service. Currently, Firewalls can assigned to Linode compute instances and NodeBalancers.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this Firewall was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
         */
        readonly entities: Array<{
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a `linode`.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'linode_interface' | 'nodebalancer';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        }>;
        /**
         * __Filterable__, __Read-only__ The Firewall's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The inbound and outbound access rules to apply to the Firewall.
         *
         * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
         *
         * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
             */
            readonly fingerprint: string;
            /**
             * The inbound rules for the firewall, as a JSON array.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall, as a JSON array.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
            /**
             * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
             */
            readonly version: number;
        };
        /**
         * __Read-only__ The status of this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled' | 'deleted';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Filterable__, __Read-only__ When this Firewall was last updated.
         */
        readonly updated: string;
    };
};

export type PostFirewallsResponse = PostFirewallsResponses[keyof PostFirewallsResponses];

export type GetFirewallSettingsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/firewalls/settings';
};

export type GetFirewallSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallSettingsError = GetFirewallSettingsErrors[keyof GetFirewallSettingsErrors];

export type GetFirewallSettingsResponses = {
    /**
     * Default firewalls.
     */
    200: {
        /**
         * The default firewall ID for a `linode`, `nodebalancer`, `public_interface`, or `vpc_interface`. Default firewalls can't be deleted or disabled.
         */
        default_firewall_ids: {
            /**
             * The Linode's default firewall.
             */
            linode: number;
            /**
             * The NodeBalancer's default firewall.
             */
            nodebalancer: number;
            /**
             * The public interface's default firewall.
             */
            public_interface: number;
            /**
             * The VPC interface's default firewall.
             */
            vpc_interface: number;
        };
    };
};

export type GetFirewallSettingsResponse = GetFirewallSettingsResponses[keyof GetFirewallSettingsResponses];

export type PutFirewallSettingsData = {
    /**
     * Default firewalls.
     */
    body: {
        /**
         * The default firewall ID for a `linode`, `nodebalancer`, `public_interface`, or `vpc_interface`. Default firewalls can't be deleted or disabled.
         */
        default_firewall_ids: {
            /**
             * The Linode's default firewall.
             */
            linode: number;
            /**
             * The NodeBalancer's default firewall.
             */
            nodebalancer: number;
            /**
             * The public interface's default firewall.
             */
            public_interface: number;
            /**
             * The VPC interface's default firewall.
             */
            vpc_interface: number;
        };
    };
    path?: never;
    query?: never;
    url: '/networking/firewalls/settings';
};

export type PutFirewallSettingsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutFirewallSettingsError = PutFirewallSettingsErrors[keyof PutFirewallSettingsErrors];

export type PutFirewallSettingsResponses = {
    /**
     * Default firewalls.
     */
    200: {
        /**
         * The default firewall ID for a `linode`, `nodebalancer`, `public_interface`, or `vpc_interface`. Default firewalls can't be deleted or disabled.
         */
        default_firewall_ids: {
            /**
             * The Linode's default firewall.
             */
            linode: number;
            /**
             * The NodeBalancer's default firewall.
             */
            nodebalancer: number;
            /**
             * The public interface's default firewall.
             */
            public_interface: number;
            /**
             * The VPC interface's default firewall.
             */
            vpc_interface: number;
        };
    };
};

export type PutFirewallSettingsResponse = PutFirewallSettingsResponses[keyof PutFirewallSettingsResponses];

export type GetFirewallTemplatesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/firewalls/templates';
};

export type GetFirewallTemplatesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallTemplatesError = GetFirewallTemplatesErrors[keyof GetFirewallTemplatesErrors];

export type GetFirewallTemplatesResponses = {
    /**
     * Returns a paginated list of firewall templates.
     */
    200: {
        data: Array<{
            /**
             * The inbound and outbound access rules for the VPC firewall template.
             *
             * A firewall can have up to 25 rules across its inbound and outbound rule sets. Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic from the same address, the first rule applies, and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * The inbound rules for the firewall.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. You can override this setting by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound` object's `action` field.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. You can override this setting by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound` object's `action` fields.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
            };
            /**
             * __Read-only__ The firewall template types available for VPC and public Linode interfaces.
             */
            slug: 'vpc' | 'public';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetFirewallTemplatesResponse = GetFirewallTemplatesResponses[keyof GetFirewallTemplatesResponses];

export type GetFirewallTemplateData = {
    body?: never;
    path: {
        /**
         * __Enum__ The firewall template type, available for either `vpc` or `public` interfaces.
         */
        slug: 'vpc' | 'public';
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/firewalls/templates/{slug}';
};

export type GetFirewallTemplateErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallTemplateError = GetFirewallTemplateErrors[keyof GetFirewallTemplateErrors];

export type GetFirewallTemplateResponses = {
    /**
     * A list of firewall templates and their `rules`.
     */
    200: {
        /**
         * The inbound and outbound access rules for the VPC firewall template.
         *
         * A firewall can have up to 25 rules across its inbound and outbound rule sets. Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic from the same address, the first rule applies, and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * The inbound rules for the firewall.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. You can override this setting by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound` object's `action` field.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. You can override this setting by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound` object's `action` fields.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
        };
        /**
         * __Read-only__ The firewall template types available for VPC and public Linode interfaces.
         */
        slug: 'vpc' | 'public';
    };
};

export type GetFirewallTemplateResponse = GetFirewallTemplateResponses[keyof GetFirewallTemplateResponses];

export type DeleteFirewallData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}';
};

export type DeleteFirewallErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteFirewallError = DeleteFirewallErrors[keyof DeleteFirewallErrors];

export type DeleteFirewallResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteFirewallResponse = DeleteFirewallResponses[keyof DeleteFirewallResponses];

export type GetFirewallData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}';
};

export type GetFirewallErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallError = GetFirewallErrors[keyof GetFirewallErrors];

export type GetFirewallResponses = {
    /**
     * A resource that controls incoming and outgoing network traffic to a compute service. Only one enabled Firewall can be attached to a particular service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) to assign a Firewall to a service. Currently, Firewalls can assigned to Linode compute instances and NodeBalancers.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this Firewall was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
         */
        readonly entities: Array<{
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a `linode`.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'linode_interface' | 'nodebalancer';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        }>;
        /**
         * __Filterable__, __Read-only__ The Firewall's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The inbound and outbound access rules to apply to the Firewall.
         *
         * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
         *
         * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
             */
            readonly fingerprint: string;
            /**
             * The inbound rules for the firewall, as a JSON array.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall, as a JSON array.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
            /**
             * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
             */
            readonly version: number;
        };
        /**
         * __Read-only__ The status of this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled' | 'deleted';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Filterable__, __Read-only__ When this Firewall was last updated.
         */
        readonly updated: string;
    };
};

export type GetFirewallResponse = GetFirewallResponses[keyof GetFirewallResponses];

export type PutFirewallData = {
    body?: {
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The status to be applied to this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - An assigned default firewall can't be disabled.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
    };
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}';
};

export type PutFirewallErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutFirewallError = PutFirewallErrors[keyof PutFirewallErrors];

export type PutFirewallResponses = {
    /**
     * A resource that controls incoming and outgoing network traffic to a compute service. Only one enabled Firewall can be attached to a particular service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) to assign a Firewall to a service. Currently, Firewalls can assigned to Linode compute instances and NodeBalancers.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this Firewall was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
         */
        readonly entities: Array<{
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a `linode`.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'linode_interface' | 'nodebalancer';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        }>;
        /**
         * __Filterable__, __Read-only__ The Firewall's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The inbound and outbound access rules to apply to the Firewall.
         *
         * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
         *
         * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
             */
            readonly fingerprint: string;
            /**
             * The inbound rules for the firewall, as a JSON array.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall, as a JSON array.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
            /**
             * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
             */
            readonly version: number;
        };
        /**
         * __Read-only__ The status of this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled' | 'deleted';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Filterable__, __Read-only__ When this Firewall was last updated.
         */
        readonly updated: string;
    };
};

export type PutFirewallResponse = PutFirewallResponses[keyof PutFirewallResponses];

export type GetFirewallDevicesData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/firewalls/{firewallId}/devices';
};

export type GetFirewallDevicesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallDevicesError = GetFirewallDevicesErrors[keyof GetFirewallDevicesErrors];

export type GetFirewallDevicesResponses = {
    /**
     * A paginated list of Firewall Devices.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this device was created.
             */
            readonly created: string;
            /**
             * The Linode, NodeBalancer, or Linode interface this firewall is assigned to.
             */
            entity: {
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a Linode.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'nodebalancer' | 'linode_interface';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            };
            /**
             * __Filterable__ The device's unique ID.
             */
            id: number;
            /**
             * __Filterable__, __Read-only__ When this device was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetFirewallDevicesResponse = GetFirewallDevicesResponses[keyof GetFirewallDevicesResponses];

export type PostFirewallDeviceData = {
    /**
     * The Linode, NodeBalancer, or Linode interface this firewall is assigned to.
     */
    body?: {
        /**
         * The entity's ID.
         */
        id: number;
        /**
         * __Read-only__ The entity's label.
         */
        readonly label: string;
        /**
         * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
         */
        readonly parent_entity: {
            /**
             * The ID of the Linode assigned to this `linode_interface`.
             */
            id: number;
            /**
             * The name of the Linode this `linode_interface` is assigned to.
             */
            label: string;
            /**
             * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
             */
            readonly parent_entity: string | null;
            /**
             * A Linode interface’s `parent_entity` is a Linode.
             */
            type: 'linode';
            /**
             * The API URL path you can use to access this entity.
             */
            url: string;
        } | null;
        /**
         * The entity's type.
         */
        type: 'linode' | 'nodebalancer' | 'linode_interface';
        /**
         * __Read-only__ The API URL path you can use to access this entity.
         */
        readonly url: string;
    };
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/devices';
};

export type PostFirewallDeviceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostFirewallDeviceError = PostFirewallDeviceErrors[keyof PostFirewallDeviceErrors];

export type PostFirewallDeviceResponses = {
    /**
     * Associates a firewall with a Linode, a Linode interface, or a NodeBalancer service.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this device was created.
         */
        readonly created: string;
        /**
         * The Linode, NodeBalancer, or Linode interface this firewall is assigned to.
         */
        entity: {
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a Linode.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'nodebalancer' | 'linode_interface';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        };
        /**
         * __Filterable__ The device's unique ID.
         */
        id: number;
        /**
         * __Filterable__, __Read-only__ When this device was last updated.
         */
        readonly updated: string;
    };
};

export type PostFirewallDeviceResponse = PostFirewallDeviceResponses[keyof PostFirewallDeviceResponses];

export type DeleteFirewallDeviceData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
        /**
         * ID of the Firewall Device to access.
         */
        deviceId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/devices/{deviceId}';
};

export type DeleteFirewallDeviceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteFirewallDeviceError = DeleteFirewallDeviceErrors[keyof DeleteFirewallDeviceErrors];

export type DeleteFirewallDeviceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteFirewallDeviceResponse = DeleteFirewallDeviceResponses[keyof DeleteFirewallDeviceResponses];

export type GetFirewallDeviceData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
        /**
         * ID of the Firewall Device to access.
         */
        deviceId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/devices/{deviceId}';
};

export type GetFirewallDeviceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallDeviceError = GetFirewallDeviceErrors[keyof GetFirewallDeviceErrors];

export type GetFirewallDeviceResponses = {
    /**
     * Associates a firewall with a Linode, a Linode interface, or a NodeBalancer service.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this device was created.
         */
        readonly created: string;
        /**
         * The Linode, NodeBalancer, or Linode interface this firewall is assigned to.
         */
        entity: {
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a Linode.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'nodebalancer' | 'linode_interface';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        };
        /**
         * __Filterable__ The device's unique ID.
         */
        id: number;
        /**
         * __Filterable__, __Read-only__ When this device was last updated.
         */
        readonly updated: string;
    };
};

export type GetFirewallDeviceResponse = GetFirewallDeviceResponses[keyof GetFirewallDeviceResponses];

export type GetFirewallRuleVersionsData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/history';
};

export type GetFirewallRuleVersionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallRuleVersionsError = GetFirewallRuleVersionsErrors[keyof GetFirewallRuleVersionsErrors];

export type GetFirewallRuleVersionsResponses = {
    /**
     * A resource that controls incoming and outgoing network traffic to a compute service. Only one enabled Firewall can be attached to a particular service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) to assign a Firewall to a service. Currently, Firewalls can assigned to Linode compute instances and NodeBalancers.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this Firewall was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
         */
        readonly entities: Array<{
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a `linode`.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'linode_interface' | 'nodebalancer';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        }>;
        /**
         * __Filterable__, __Read-only__ The Firewall's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The inbound and outbound access rules to apply to the Firewall.
         *
         * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
         *
         * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
             */
            readonly fingerprint: string;
            /**
             * The inbound rules for the firewall, as a JSON array.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall, as a JSON array.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
            /**
             * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
             */
            readonly version: number;
        };
        /**
         * __Read-only__ The status of this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled' | 'deleted';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Filterable__, __Read-only__ When this Firewall was last updated.
         */
        readonly updated: string;
    };
};

export type GetFirewallRuleVersionsResponse = GetFirewallRuleVersionsResponses[keyof GetFirewallRuleVersionsResponses];

export type GetFirewallRuleVersionData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
        /**
         * The firewall rule version to view.
         */
        version: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/history/rules/{version}';
};

export type GetFirewallRuleVersionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallRuleVersionError = GetFirewallRuleVersionErrors[keyof GetFirewallRuleVersionErrors];

export type GetFirewallRuleVersionResponses = {
    /**
     * A resource that controls incoming and outgoing network traffic to a compute service. Only one enabled Firewall can be attached to a particular service at any given time. [Create a firewall device](https://techdocs.akamai.com/linode-api/reference/post-firewall-device) to assign a Firewall to a service. Currently, Firewalls can assigned to Linode compute instances and NodeBalancers.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this Firewall was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
         */
        readonly entities: Array<{
            /**
             * The entity's ID.
             */
            id: number;
            /**
             * __Read-only__ The entity's label.
             */
            readonly label: string;
            /**
             * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
             */
            readonly parent_entity: {
                /**
                 * The ID of the Linode assigned to this `linode_interface`.
                 */
                id: number;
                /**
                 * The name of the Linode this `linode_interface` is assigned to.
                 */
                label: string;
                /**
                 * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                 */
                readonly parent_entity: string | null;
                /**
                 * A Linode interface’s `parent_entity` is a `linode`.
                 */
                type: 'linode';
                /**
                 * The API URL path you can use to access this entity.
                 */
                url: string;
            } | null;
            /**
             * The entity's type.
             */
            type: 'linode' | 'linode_interface' | 'nodebalancer';
            /**
             * __Read-only__ The API URL path you can use to access this entity.
             */
            readonly url: string;
        }>;
        /**
         * __Filterable__, __Read-only__ The Firewall's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__ The Firewall's label, for display purposes only.
         *
         * Firewall labels have the following constraints:
         *
         * - Must begin and end with an alphanumeric character.
         * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
         * - Must be between 3 and 32 characters.
         * - Must be unique.
         */
        label: string;
        /**
         * The inbound and outbound access rules to apply to the Firewall.
         *
         * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
         *
         * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
         */
        rules: {
            /**
             * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
             */
            readonly fingerprint: string;
            /**
             * The inbound rules for the firewall, as a JSON array.
             */
            inbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
             */
            inbound_policy: 'ACCEPT' | 'DROP';
            /**
             * The outbound rules for the firewall, as a JSON array.
             */
            outbound: Array<{
                /**
                 * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                 */
                action: 'ACCEPT' | 'DROP';
                /**
                 * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                 *
                 * Must contain `ipv4`, `ipv6`, or both.
                 */
                addresses: {
                    /**
                     * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                     *
                     * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                     */
                    ipv4: Array<string>;
                    /**
                     * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                     *
                     * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                     */
                    ipv6: Array<string>;
                };
                /**
                 * Used to describe this rule. For display purposes only.
                 */
                description: string;
                /**
                 * Used to identify this rule. For display purposes only.
                 */
                label: string;
                /**
                 * A string representing the port or ports affected by this rule:
                 *
                 * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                 * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                 * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                 * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                 * - If no ports are configured, all ports are affected.
                 * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                 */
                ports: string | null;
                /**
                 * The type of network traffic affected by this rule.
                 */
                protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
            }>;
            /**
             * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
             */
            outbound_policy: 'ACCEPT' | 'DROP';
            /**
             * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
             */
            readonly version: number;
        };
        /**
         * __Read-only__ The status of this Firewall.
         *
         * - When a Firewall is first created its status is `enabled`.
         * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
         * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
         */
        status: 'enabled' | 'disabled' | 'deleted';
        /**
         * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Filterable__, __Read-only__ When this Firewall was last updated.
         */
        readonly updated: string;
    };
};

export type GetFirewallRuleVersionResponse = GetFirewallRuleVersionResponses[keyof GetFirewallRuleVersionResponses];

export type GetFirewallRulesData = {
    body?: never;
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/rules';
};

export type GetFirewallRulesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetFirewallRulesError = GetFirewallRulesErrors[keyof GetFirewallRulesErrors];

export type GetFirewallRulesResponses = {
    /**
     * The inbound and outbound access rules to apply to the Firewall.
     *
     * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
     *
     * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
     */
    200: {
        /**
         * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
         */
        readonly fingerprint: string;
        /**
         * The inbound rules for the firewall, as a JSON array.
         */
        inbound: Array<{
            /**
             * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
             */
            action: 'ACCEPT' | 'DROP';
            /**
             * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
             *
             * Must contain `ipv4`, `ipv6`, or both.
             */
            addresses: {
                /**
                 * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                 *
                 * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                 */
                ipv4: Array<string>;
                /**
                 * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                 *
                 * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                 */
                ipv6: Array<string>;
            };
            /**
             * Used to describe this rule. For display purposes only.
             */
            description: string;
            /**
             * Used to identify this rule. For display purposes only.
             */
            label: string;
            /**
             * A string representing the port or ports affected by this rule:
             *
             * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
             * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
             * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
             * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
             * - If no ports are configured, all ports are affected.
             * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
             */
            ports: string | null;
            /**
             * The type of network traffic affected by this rule.
             */
            protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
        }>;
        /**
         * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
         */
        inbound_policy: 'ACCEPT' | 'DROP';
        /**
         * The outbound rules for the firewall, as a JSON array.
         */
        outbound: Array<{
            /**
             * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
             */
            action: 'ACCEPT' | 'DROP';
            /**
             * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
             *
             * Must contain `ipv4`, `ipv6`, or both.
             */
            addresses: {
                /**
                 * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                 *
                 * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                 */
                ipv4: Array<string>;
                /**
                 * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                 *
                 * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                 */
                ipv6: Array<string>;
            };
            /**
             * Used to describe this rule. For display purposes only.
             */
            description: string;
            /**
             * Used to identify this rule. For display purposes only.
             */
            label: string;
            /**
             * A string representing the port or ports affected by this rule:
             *
             * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
             * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
             * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
             * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
             * - If no ports are configured, all ports are affected.
             * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
             */
            ports: string | null;
            /**
             * The type of network traffic affected by this rule.
             */
            protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
        }>;
        /**
         * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
         */
        outbound_policy: 'ACCEPT' | 'DROP';
        /**
         * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
         */
        readonly version: number;
    };
};

export type GetFirewallRulesResponse = GetFirewallRulesResponses[keyof GetFirewallRulesResponses];

export type PutFirewallRulesData = {
    /**
     * The Firewall Rules information to update.
     */
    body?: {
        /**
         * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
         */
        readonly fingerprint: string;
        /**
         * The inbound rules for the firewall, as a JSON array.
         */
        inbound: Array<{
            /**
             * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
             */
            action: 'ACCEPT' | 'DROP';
            /**
             * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
             *
             * Must contain `ipv4`, `ipv6`, or both.
             */
            addresses: {
                /**
                 * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                 *
                 * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                 */
                ipv4: Array<string>;
                /**
                 * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                 *
                 * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                 */
                ipv6: Array<string>;
            };
            /**
             * Used to describe this rule. For display purposes only.
             */
            description: string;
            /**
             * Used to identify this rule. For display purposes only.
             */
            label: string;
            /**
             * A string representing the port or ports affected by this rule:
             *
             * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
             * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
             * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
             * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
             * - If no ports are configured, all ports are affected.
             * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
             */
            ports: string | null;
            /**
             * The type of network traffic affected by this rule.
             */
            protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
        }>;
        /**
         * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
         */
        inbound_policy: 'ACCEPT' | 'DROP';
        /**
         * The outbound rules for the firewall, as a JSON array.
         */
        outbound: Array<{
            /**
             * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
             */
            action: 'ACCEPT' | 'DROP';
            /**
             * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
             *
             * Must contain `ipv4`, `ipv6`, or both.
             */
            addresses: {
                /**
                 * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                 *
                 * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                 */
                ipv4: Array<string>;
                /**
                 * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                 *
                 * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                 */
                ipv6: Array<string>;
            };
            /**
             * Used to describe this rule. For display purposes only.
             */
            description: string;
            /**
             * Used to identify this rule. For display purposes only.
             */
            label: string;
            /**
             * A string representing the port or ports affected by this rule:
             *
             * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
             * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
             * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
             * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
             * - If no ports are configured, all ports are affected.
             * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
             */
            ports: string | null;
            /**
             * The type of network traffic affected by this rule.
             */
            protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
        }>;
        /**
         * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
         */
        outbound_policy: 'ACCEPT' | 'DROP';
        /**
         * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
         */
        readonly version: number;
    } & {
        inbound?: unknown;
        outbound?: unknown;
    };
    path: {
        /**
         * ID of the Firewall to access.
         */
        firewallId: number;
    };
    query?: never;
    url: '/networking/firewalls/{firewallId}/rules';
};

export type PutFirewallRulesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutFirewallRulesError = PutFirewallRulesErrors[keyof PutFirewallRulesErrors];

export type PutFirewallRulesResponses = {
    /**
     * The inbound and outbound access rules to apply to the Firewall.
     *
     * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
     *
     * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
     */
    200: {
        /**
         * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
         */
        readonly fingerprint: string;
        /**
         * The inbound rules for the firewall, as a JSON array.
         */
        inbound: Array<{
            /**
             * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
             */
            action: 'ACCEPT' | 'DROP';
            /**
             * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
             *
             * Must contain `ipv4`, `ipv6`, or both.
             */
            addresses: {
                /**
                 * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                 *
                 * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                 */
                ipv4: Array<string>;
                /**
                 * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                 *
                 * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                 */
                ipv6: Array<string>;
            };
            /**
             * Used to describe this rule. For display purposes only.
             */
            description: string;
            /**
             * Used to identify this rule. For display purposes only.
             */
            label: string;
            /**
             * A string representing the port or ports affected by this rule:
             *
             * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
             * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
             * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
             * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
             * - If no ports are configured, all ports are affected.
             * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
             */
            ports: string | null;
            /**
             * The type of network traffic affected by this rule.
             */
            protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
        }>;
        /**
         * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
         */
        inbound_policy: 'ACCEPT' | 'DROP';
        /**
         * The outbound rules for the firewall, as a JSON array.
         */
        outbound: Array<{
            /**
             * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
             */
            action: 'ACCEPT' | 'DROP';
            /**
             * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
             *
             * Must contain `ipv4`, `ipv6`, or both.
             */
            addresses: {
                /**
                 * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                 *
                 * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                 */
                ipv4: Array<string>;
                /**
                 * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                 *
                 * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                 */
                ipv6: Array<string>;
            };
            /**
             * Used to describe this rule. For display purposes only.
             */
            description: string;
            /**
             * Used to identify this rule. For display purposes only.
             */
            label: string;
            /**
             * A string representing the port or ports affected by this rule:
             *
             * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
             * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
             * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
             * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
             * - If no ports are configured, all ports are affected.
             * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
             */
            ports: string | null;
            /**
             * The type of network traffic affected by this rule.
             */
            protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
        }>;
        /**
         * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
         */
        outbound_policy: 'ACCEPT' | 'DROP';
        /**
         * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
         */
        readonly version: number;
    };
};

export type PutFirewallRulesResponse = PutFirewallRulesResponses[keyof PutFirewallRulesResponses];

export type GetIpsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * When `true`, the `rdns` property for any `ipv6` type addresses always returns `null` regardless of whether RDNS data exists for the address.
         */
        skip_ipv6_rdns?: boolean;
    };
    url: '/networking/ips';
};

export type GetIpsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetIpsError = GetIpsErrors[keyof GetIpsErrors];

export type GetIpsResponses = {
    /**
     * The response data for the [List IP addresses](https://techdocs.akamai.com/linode-api/reference/get-ips) operation.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        /**
         * IP addresses that exist in Linode's system, either IPv4 or IPv6, specific to the response for the [List IP addresses](https://techdocs.akamai.com/linode-api/reference/get-ips) operation.
         */
        data: Array<{
            /**
             * __Filterable__, __Read-only__ The IP address.
             */
            readonly address: string;
            /**
             * __Read-only__ The default gateway for this address.
             */
            readonly gateway: string | null;
            /**
             * __Beta__, __Read-only__ The Linode interface ID this public IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
             */
            readonly interface_id: number | null;
            /**
             * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this defaults to the Linode that this address was assigned to on creation. IPv4 addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value may not be changed.
             */
            readonly linode_id: number;
            /**
             * __Filterable__, __Read-only__ The number of bits set in the subnet mask.
             */
            readonly prefix: number;
            /**
             * __Read-only__ Whether this is a public or private IP address.
             */
            readonly public: boolean;
            /**
             * __Filterable__ The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
             */
            rdns: string | null;
            /**
             * __Filterable__, __Read-only__ The Region this IP address resides in.
             */
            readonly region: string;
            /**
             * __Read-only__ The mask that separates host bits from network bits for this address.
             */
            readonly subnet_mask: string;
            /**
             * __Filterable__, __Read-only__ The type of address this is.
             */
            type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
            /**
             * IPv4 address configured as a 1:1 NAT for this interface. Empty if no address is configured as a 1:1 NAT.
             *
             * > 📘
             * >
             * > Only allowed for `vpc` type interfaces.
             */
            vpc_nat_1_1: {
                /**
                 * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
                 */
                address: string;
                /**
                 * The `id` of the VPC Subnet for this interface.
                 */
                subnet_id: number;
                /**
                 * __Read-only__ The `id` of the VPC configured for this Interface.
                 */
                readonly vpc_id: number;
            };
        }>;
    };
};

export type GetIpsResponse = GetIpsResponses[keyof GetIpsResponses];

export type PostAllocateIpData = {
    /**
     * Information about the address you are creating.
     */
    body: {
        /**
         * The ID of a Linode you have access to that this address will be allocated to.
         */
        linode_id: number;
        /**
         * Whether to create a public or private IPv4 address.
         */
        public: boolean;
        /**
         * The type of address you are requesting. Only IPv4 addresses may be allocated through this operation.
         */
        type: 'ipv4';
    };
    path?: never;
    query?: never;
    url: '/networking/ips';
};

export type PostAllocateIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAllocateIpError = PostAllocateIpErrors[keyof PostAllocateIpErrors];

export type PostAllocateIpResponses = {
    /**
     * An IP address that exists in Linode's system, either IPv4 or IPv6.
     */
    200: {
        /**
         * __Read-only__ The IP address.
         */
        readonly address: string;
        /**
         * __Read-only__ The default gateway for this address.
         */
        readonly gateway: string | null;
        /**
         * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
         */
        readonly interface_id: number | null;
        /**
         * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
         */
        readonly linode_id: number;
        /**
         * __Read-only__ The number of bits set in the subnet mask.
         */
        readonly prefix: number;
        /**
         * __Read-only__ Whether this is a public or private IP address.
         */
        readonly public: boolean;
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
        /**
         * __Read-only__ The Region this IP address resides in.
         */
        readonly region: string;
        /**
         * __Read-only__ The mask that separates host bits from network bits for this address.
         */
        readonly subnet_mask: string;
        /**
         * __Read-only__ The type of address this is.
         */
        type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
        /**
         * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
         *
         * > 📘
         * >
         * > Only allowed for `vpc` type interfaces.
         */
        vpc_nat_1_1: {
            /**
             * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
             */
            address: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface.
             */
            readonly vpc_id: number;
        };
    };
};

export type PostAllocateIpResponse = PostAllocateIpResponses[keyof PostAllocateIpResponses];

export type PostAssignIpsData = {
    /**
     * Request object for IP Addresses Assign (POST /networking/ips/assign).
     */
    body: {
        /**
         * The list of assignments to make. You must have read_write access to all IPs being assigned and all Linodes being assigned to in order for the assignments to succeed.
         */
        assignments: Array<{
            /**
             * The IPv4 address or IPv6 range for this assignment.
             *
             * - Must be an IPv4 address or an IPv6 range you can access in the Region specified.
             * - IPv6 ranges must include a prefix length of `/56` or `/64`, for example: `2001:db8:3c4d:15::/64`.
             * - Assignment of an IPv6 range to a Linode updates the route target of the range to the assigned Linode's SLAAC address.
             * - Can be a public or private address.
             * - For Linode interfaces, public IPv4 addresses or public IPv6 ranges can be exchanged on public interfaces. Private IPv4 addresses are not supported.
             */
            address: string;
            /**
             * The ID of the Linode to assign this address to. The IP's previous Linode will lose this address, and must end up with at least one public address and no more than one private address once all assignments have been made.
             */
            linode_id: number;
        }>;
        /**
         * The ID of the Region in which these assignments are to take place. All IPs and Linodes must exist in this Region.
         */
        region: string;
    };
    path?: never;
    query?: never;
    url: '/networking/ips/assign';
};

export type PostAssignIpsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAssignIpsError = PostAssignIpsErrors[keyof PostAssignIpsErrors];

export type PostAssignIpsResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostAssignIpsResponse = PostAssignIpsResponses[keyof PostAssignIpsResponses];

export type PostShareIpsData = {
    /**
     * A request object IP Addresses Share (POST /networking/ips/share).
     */
    body: {
        /**
         * A list of secondary Linode IPs to share with the primary Linode.
         *
         * - Can include both IPv4 addresses and IPv6 ranges (omit /56 and /64 prefix lengths)
         * - Can include both private and public IPv4 addresses.
         * - You must have access to all of these addresses and they must be in the same Region as the primary Linode.
         * - Enter an empty array to remove all shared IP addresses.
         */
        ips: Array<string>;
        /**
         * The ID of the primary Linode that the addresses will be shared with.
         */
        linode_id: number;
    };
    path?: never;
    query?: never;
    url: '/networking/ips/share';
};

export type PostShareIpsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostShareIpsError = PostShareIpsErrors[keyof PostShareIpsErrors];

export type PostShareIpsResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostShareIpsResponse = PostShareIpsResponses[keyof PostShareIpsResponses];

export type GetIpData = {
    body?: never;
    path: {
        /**
         * The address to operate on.
         */
        address: string;
    };
    query?: never;
    url: '/networking/ips/{address}';
};

export type GetIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetIpError = GetIpErrors[keyof GetIpErrors];

export type GetIpResponses = {
    /**
     * An IP address that exists in Linode's system, either IPv4 or IPv6.
     */
    200: {
        /**
         * __Read-only__ The IP address.
         */
        readonly address: string;
        /**
         * __Read-only__ The default gateway for this address.
         */
        readonly gateway: string | null;
        /**
         * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
         */
        readonly interface_id: number | null;
        /**
         * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
         */
        readonly linode_id: number;
        /**
         * __Read-only__ The number of bits set in the subnet mask.
         */
        readonly prefix: number;
        /**
         * __Read-only__ Whether this is a public or private IP address.
         */
        readonly public: boolean;
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
        /**
         * __Read-only__ The Region this IP address resides in.
         */
        readonly region: string;
        /**
         * __Read-only__ The mask that separates host bits from network bits for this address.
         */
        readonly subnet_mask: string;
        /**
         * __Read-only__ The type of address this is.
         */
        type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
        /**
         * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
         *
         * > 📘
         * >
         * > Only allowed for `vpc` type interfaces.
         */
        vpc_nat_1_1: {
            /**
             * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
             */
            address: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface.
             */
            readonly vpc_id: number;
        };
    };
};

export type GetIpResponse = GetIpResponses[keyof GetIpResponses];

export type PutIpData = {
    /**
     * The information to update.
     */
    body: {
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
    };
    path: {
        /**
         * The address to operate on.
         */
        address: string;
    };
    query?: never;
    url: '/networking/ips/{address}';
};

export type PutIpErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutIpError = PutIpErrors[keyof PutIpErrors];

export type PutIpResponses = {
    /**
     * An IP address that exists in Linode's system, either IPv4 or IPv6.
     */
    200: {
        /**
         * __Read-only__ The IP address.
         */
        readonly address: string;
        /**
         * __Read-only__ The default gateway for this address.
         */
        readonly gateway: string | null;
        /**
         * __Beta__, __Read-only__ The Linode interface ID that this IP address is assigned to. This value is `null` if a Linode interface is not assigned, or if the IP is assigned to a legacy configuration profile interface.
         */
        readonly interface_id: number | null;
        /**
         * __Read-only__ The ID of the Linode this address currently belongs to. For IPv4 addresses, this is by default the Linode that this address was assigned to on creation, and these addresses may be moved using the [Assign IPv4s to Linodes](https://techdocs.akamai.com/linode-api/reference/post-assign-ipv4s) operation. For SLAAC and link-local addresses, this value can't be changed.
         */
        readonly linode_id: number;
        /**
         * __Read-only__ The number of bits set in the subnet mask.
         */
        readonly prefix: number;
        /**
         * __Read-only__ Whether this is a public or private IP address.
         */
        readonly public: boolean;
        /**
         * The reverse DNS assigned to this address. For public IPv4 addresses, this will be set to a default value provided by Linode if not explicitly set.
         */
        rdns: string | null;
        /**
         * __Read-only__ The Region this IP address resides in.
         */
        readonly region: string;
        /**
         * __Read-only__ The mask that separates host bits from network bits for this address.
         */
        readonly subnet_mask: string;
        /**
         * __Read-only__ The type of address this is.
         */
        type: 'ipv4' | 'ipv6' | 'ipv6/pool' | 'ipv6/range';
        /**
         * IPv4 address configured as a 1:1 NAT for this Interface. If no address is configured as a 1:1 NAT, `null` is returned.
         *
         * > 📘
         * >
         * > Only allowed for `vpc` type interfaces.
         */
        vpc_nat_1_1: {
            /**
             * The IPv4 address that is configured as a 1:1 NAT for this VPC interface.
             */
            address: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this interface.
             */
            readonly vpc_id: number;
        };
    };
};

export type PutIpResponse = PutIpResponses[keyof PutIpResponses];

export type PostAssignIpv4sData = {
    /**
     * Request object for IP Addresses Assign (POST /networking/ips/assign).
     */
    body: {
        /**
         * The list of assignments to make. You must have read_write access to all IPs being assigned and all Linodes being assigned to in order for the assignments to succeed.
         */
        assignments: Array<{
            /**
             * The IPv4 address or IPv6 range for this assignment.
             *
             * - Must be an IPv4 address or an IPv6 range you can access in the Region specified.
             * - IPv6 ranges must include a prefix length of `/56` or `/64`, for example: `2001:db8:3c4d:15::/64`.
             * - Assignment of an IPv6 range to a Linode updates the route target of the range to the assigned Linode's SLAAC address.
             * - Can be a public or private address.
             * - For Linode interfaces, public IPv4 addresses or public IPv6 ranges can be exchanged on public interfaces. Private IPv4 addresses are not supported.
             */
            address: string;
            /**
             * The ID of the Linode to assign this address to. The IP's previous Linode will lose this address, and must end up with at least one public address and no more than one private address once all assignments have been made.
             */
            linode_id: number;
        }>;
        /**
         * The ID of the Region in which these assignments are to take place. All IPs and Linodes must exist in this Region.
         */
        region: string;
    };
    path?: never;
    query?: never;
    url: '/networking/ipv4/assign';
};

export type PostAssignIpv4sErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAssignIpv4sError = PostAssignIpv4sErrors[keyof PostAssignIpv4sErrors];

export type PostAssignIpv4sResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostAssignIpv4sResponse = PostAssignIpv4sResponses[keyof PostAssignIpv4sResponses];

export type PostShareIpv4sData = {
    /**
     * A request object IP Addresses Share (POST /networking/ips/share).
     */
    body: {
        /**
         * A list of secondary Linode IPs to share with the primary Linode.
         *
         * - Can include both IPv4 addresses and IPv6 ranges (omit /56 and /64 prefix lengths)
         * - Can include both private and public IPv4 addresses.
         * - You must have access to all of these addresses and they must be in the same Region as the primary Linode.
         * - Enter an empty array to remove all shared IP addresses.
         */
        ips: Array<string>;
        /**
         * The ID of the primary Linode that the addresses will be shared with.
         */
        linode_id: number;
    };
    path?: never;
    query?: never;
    url: '/networking/ipv4/share';
};

export type PostShareIpv4sErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostShareIpv4sError = PostShareIpv4sErrors[keyof PostShareIpv4sErrors];

export type PostShareIpv4sResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostShareIpv4sResponse = PostShareIpv4sResponses[keyof PostShareIpv4sResponses];

export type GetIpv6PoolsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/ipv6/pools';
};

export type GetIpv6PoolsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetIpv6PoolsError = GetIpv6PoolsErrors[keyof GetIpv6PoolsErrors];

export type GetIpv6PoolsResponses = {
    /**
     * The IPv6 pools on your Account.
     */
    200: {
        data: Array<{
            /**
             * The prefix length of the address. The total number of addresses that can be assigned from this range is calculated as 2<sup>(128 - prefix length)</sup>.
             */
            prefix: number;
            /**
             * __Read-only__ The IPv6 range of addresses in this pool.
             */
            readonly range: string;
            /**
             * __Filterable__, __Read-only__ The region for this pool of IPv6 addresses.
             */
            readonly region: string;
            /**
             * The last address in this block of IPv6 addresses.
             */
            route_target: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetIpv6PoolsResponse = GetIpv6PoolsResponses[keyof GetIpv6PoolsResponses];

export type GetIpv6RangesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/ipv6/ranges';
};

export type GetIpv6RangesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetIpv6RangesError = GetIpv6RangesErrors[keyof GetIpv6RangesErrors];

export type GetIpv6RangesResponses = {
    /**
     * The IPv6 ranges on your Account.
     */
    200: {
        data: Array<{
            /**
             * The prefix length of the address. The total number of addresses that can be assigned from this range is calculated as 2<sup>(128 - prefix length)</sup>.
             */
            prefix: number;
            /**
             * __Read-only__ The IPv6 address of this range.
             */
            readonly range: string;
            /**
             * __Read-only__ The region for this range of IPv6 addresses.
             */
            readonly region: string;
            /**
             * The IPv6 SLAAC address.
             */
            route_target: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetIpv6RangesResponse = GetIpv6RangesResponses[keyof GetIpv6RangesResponses];

export type PostIpv6RangeData = {
    /**
     * Information about the IPv6 range to create.
     */
    body: {
        /**
         * The ID of the Linode to assign this range to. The SLAAC address for the provided Linode is used as the range's `route_target`.
         *
         * - __Required__ if `route_target` is omitted from the request.
         *
         * - Mutually exclusive with `route_target`. Submitting values for both properties in a request results in an error.
         */
        linode_id?: number;
        /**
         * The prefix length of the IPv6 range.
         */
        prefix_length: 56 | 64;
        /**
         * The IPv6 SLAAC address to assign this range to.
         *
         * - __Required__ if `linode_id` is omitted from the request.
         *
         * - Mutually exclusive with `linode_id`. Submitting values for both properties in a request results in an error.
         *
         * > 📘
         * >
         * > You need to omit the `/128` prefix length of the SLAAC address when using this property.
         */
        route_target?: string;
    };
    path?: never;
    query?: never;
    url: '/networking/ipv6/ranges';
};

export type PostIpv6RangeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostIpv6RangeError = PostIpv6RangeErrors[keyof PostIpv6RangeErrors];

export type PostIpv6RangeResponses = {
    /**
     * IPv6 range created successfully.
     */
    200: {
        /**
         * The IPv6 network range, including subnet and prefix length.
         */
        range: string;
        /**
         * The route target IPV6 SLAAC address for this range. Does not include the prefix length.
         */
        route_target: string;
    };
};

export type PostIpv6RangeResponse = PostIpv6RangeResponses[keyof PostIpv6RangeResponses];

export type DeleteIpv6RangeData = {
    body?: never;
    path: {
        /**
         * The IPv6 range to access. Corresponds to the `range` property of objects returned from the [List IPv6 ranges](https://techdocs.akamai.com/linode-api/reference/get-ipv6-ranges) operation.
         *
         * > 📘
         * >
         * > You need to omit the prefix length of the IPv6 range.
         */
        range: string;
    };
    query?: never;
    url: '/networking/ipv6/ranges/{range}';
};

export type DeleteIpv6RangeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteIpv6RangeError = DeleteIpv6RangeErrors[keyof DeleteIpv6RangeErrors];

export type DeleteIpv6RangeResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteIpv6RangeResponse = DeleteIpv6RangeResponses[keyof DeleteIpv6RangeResponses];

export type GetIpv6RangeData = {
    body?: never;
    path: {
        /**
         * The IPv6 range to access. Corresponds to the `range` property of objects returned from the [List IPv6 ranges](https://techdocs.akamai.com/linode-api/reference/get-ipv6-ranges) operation.
         *
         * > 📘
         * >
         * > You need to omit the prefix length of the IPv6 range.
         */
        range: string;
    };
    query?: never;
    url: '/networking/ipv6/ranges/{range}';
};

export type GetIpv6RangeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetIpv6RangeError = GetIpv6RangeErrors[keyof GetIpv6RangeErrors];

export type GetIpv6RangeResponses = {
    /**
     * An object representing an IPv6 range.
     */
    200: {
        /**
         * __Read-only__ Whether this IPv6 range is shared.
         */
        readonly is_bgp: boolean;
        /**
         * __Read-only__ A list of Linodes targeted by this IPv6 range. Includes Linodes with IP sharing.
         */
        readonly linodes: Array<number>;
        /**
         * The prefix length of the address. The total number of addresses that can be assigned from this range is calculated as 2<sup>(128 - prefix length)</sup>.
         */
        prefix: number;
        /**
         * __Read-only__ The IPv6 address of this range.
         */
        readonly range: string;
        /**
         * __Read-only__ The region for this range of IPv6 addresses.
         */
        readonly region: string;
    };
};

export type GetIpv6RangeResponse = GetIpv6RangeResponses[keyof GetIpv6RangeResponses];

export type GetVlansData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/networking/vlans';
};

export type GetVlansErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVlansError = GetVlansErrors[keyof GetVlansErrors];

export type GetVlansResponses = {
    /**
     * The VLANs available on this Account.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The date this VLAN was created.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ The name of this VLAN.
             */
            readonly label: string;
            /**
             * __Read-only__ An array of Linode IDs attached to this VLAN.
             */
            readonly linodes: Array<number>;
            /**
             * __Filterable__, __Read-only__ This VLAN's data center region.
             *
             * > 📘
             * >
             * > Currently, a VLAN can only be assigned to a Linode within the same data center region.
             */
            readonly region: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetVlansResponse = GetVlansResponses[keyof GetVlansResponses];

export type DeleteVlanData = {
    body?: never;
    path: {
        /**
         * The label of the VLAN to be deleted.
         */
        label: string;
        /**
         * The VLAN's region.
         */
        regionId: string;
    };
    query?: never;
    url: '/networking/vlans/{regionId}/{label}';
};

export type DeleteVlanErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteVlanError = DeleteVlanErrors[keyof DeleteVlanErrors];

export type DeleteVlanResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteVlanResponse = DeleteVlanResponses[keyof DeleteVlanResponses];

export type GetNodeBalancersData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/nodebalancers';
};

export type GetNodeBalancersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancersError = GetNodeBalancersErrors[keyof GetNodeBalancersErrors];

export type GetNodeBalancersResponses = {
    /**
     * A paginated list of NodeBalancers.
     */
    200: {
        data: Array<{
            /**
             * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
             */
            client_conn_throttle: number;
            /**
             * __Read-only__ When this NodeBalancer was created.
             */
            readonly created: string;
            /**
             * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
             */
            readonly hostname: string;
            /**
             * __Read-only__ This NodeBalancer's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
             */
            readonly ipv4: string;
            /**
             * __Read-only__ This NodeBalancer's public IPv6 address.
             */
            readonly ipv6: string | null;
            /**
             * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
             */
            label: string;
            /**
             * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
             */
            readonly lke_cluster: {
                /**
                 * The ID of the related LKE cluster.
                 */
                id: string;
                /**
                 * The label of the related LKE cluster.
                 */
                label: string;
                /**
                 * __Read-only__ The type for LKE clusters.
                 */
                readonly type: string;
                /**
                 * The URL where you can access the related LKE cluster.
                 */
                url: string;
            } | null;
            /**
             * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
             */
            readonly region: string;
            /**
             * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
             */
            readonly transfer: {
                /**
                 * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
                 */
                readonly in: number | null;
                /**
                 * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
                 */
                readonly out: number | null;
                /**
                 * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                 */
                readonly total: number | null;
            };
            /**
             * __Read-only__ The type of NodeBalancer.
             */
            type: 'common' | 'premium';
            /**
             * __Read-only__ When this NodeBalancer was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetNodeBalancersResponse = GetNodeBalancersResponses[keyof GetNodeBalancersResponses];

export type PostNodeBalancerData = {
    /**
     * Information about the NodeBalancer to create.
     */
    body: {
        /**
         * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
         */
        client_conn_throttle?: number;
        /**
         * The port configs to create for this NodeBalancer. Each config needs a unique port and at least one node.
         */
        configs?: Array<{
            /**
             * The algorithm this UDP NodeBalancer uses to route traffic to backends.
             */
            algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
            /**
             * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
             *
             * - If `none`, no check is performed.
             * - `connection` requires only a connection to the backend to succeed.
             * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
             */
            check?: 'none' | 'connection' | 'http' | 'http_body';
            /**
             * How many times to attempt a check before considering a backend to be down.
             */
            check_attempts?: number;
            /**
             * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
             */
            check_body?: string;
            /**
             * How often, in seconds, to check that backends are up and serving requests.
             *
             * Must be greater than `check_timeout`.
             */
            check_interval?: number;
            /**
             * __Read-only__ Must be `false` for UDP.
             */
            readonly check_passive?: boolean;
            /**
             * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
             */
            check_path?: string;
            /**
             * How long, in seconds, to wait for a check attempt before considering it failed.
             *
             * Must be less than `check_interval`.
             */
            check_timeout?: number;
            /**
             * __Read-only__ Not applicable for UDP configs.
             */
            readonly cipher_suite?: string;
            /**
             * __Read-only__ This config's unique ID.
             */
            readonly id?: number;
            /**
             * __Read-only__ The ID for the NodeBalancer this config belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * The NodeBalancer nodes that serve this configuration.
             */
            nodes: Array<{
                /**
                 * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
                 * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
                 * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
                 * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
                 */
                address?: string;
                /**
                 * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                 */
                readonly config_id?: number;
                /**
                 * __Read-only__ This node's unique ID.
                 */
                readonly id?: number;
                /**
                 * The label for this node.  This is for display purposes only.
                 */
                label?: string;
                /**
                 * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
                 */
                mode?: 'none';
                /**
                 * __Read-only__ The NodeBalancer ID that this Node belongs to.
                 */
                readonly nodebalancer_id?: number;
                /**
                 * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
                 */
                status?: 'unknown' | 'UP' | 'DOWN';
                /**
                 * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
                 */
                subnet_id?: number;
                /**
                 * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
                 */
                weight?: number;
            }>;
            /**
             * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
             */
            readonly nodes_status?: {
                /**
                 * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
                 */
                readonly down: number;
                /**
                 * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
                 */
                readonly up: number;
            };
            /**
             * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
             */
            port?: number;
            /**
             * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
             */
            protocol?: 'udp';
            /**
             * __Read-only__ Must be `none` for UDP.
             */
            readonly proxy_protocol?: string;
            /**
             * __Read-only__ Not applicable for UDP configs.
             */
            readonly ssl_cert?: string | null;
            /**
             * __Read-only__ Not applicable for UPD configs.
             */
            readonly ssl_commonname?: string;
            /**
             * __Read-only__ Not applicable for UDP configs.
             */
            readonly ssl_fingerprint?: string;
            /**
             * __Read-only__ Not applicable for UDP configs.
             */
            readonly ssl_key?: string | null;
            /**
             * Controls how session stickiness is handled on this port.
             *
             * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
             * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
             * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
             */
            stickiness?: 'none' | 'session' | 'source_ip';
            /**
             * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
             */
            udp_check_port?: number;
            /**
             * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
             */
            readonly udp_session_timeout?: number;
        } | {
            /**
             * The algorithm this TCP NodeBalancer uses to route traffic to backends.
             */
            algorithm?: 'roundrobin' | 'leastconn' | 'source';
            /**
             * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
             *
             * - If `none`, no check is performed.
             * - `connection` requires only a connection to the backend to succeed.
             * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
             */
            check?: 'none' | 'connection' | 'http' | 'http_body';
            /**
             * How many times to attempt a check before considering a backend to be down.
             */
            check_attempts?: number;
            /**
             * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
             */
            check_body?: string;
            /**
             * How often, in seconds, to check that backends are up and serving requests.
             *
             * Must be greater than `check_timeout`.
             */
            check_interval?: number;
            /**
             * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
             */
            check_passive?: boolean;
            /**
             * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
             */
            check_path?: string;
            /**
             * How long, in seconds, to wait for a check attempt before considering it failed.
             *
             * Must be less than `check_interval`.
             */
            check_timeout?: number;
            /**
             * __Read-only__ Not applicable for `tcp` configs.
             */
            readonly cipher_suite?: string;
            /**
             * __Read-only__ This config's unique ID.
             */
            readonly id?: number;
            /**
             * __Read-only__ The ID for the NodeBalancer this config belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * The NodeBalancer nodes that serve this configuration.
             */
            nodes: Array<{
                /**
                 * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
                 * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
                 * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
                 * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
                 */
                address?: string;
                /**
                 * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                 */
                readonly config_id?: number;
                /**
                 * __Read-only__ This node's unique ID.
                 */
                readonly id?: number;
                /**
                 * The label for this node.  This is for display purposes only.
                 */
                label?: string;
                /**
                 * The mode this NodeBalancer should use when sending traffic to this backend.
                 *
                 * - If set to `accept` this backend is accepting traffic.
                 * - If set to `reject` this backend will not receive traffic.
                 * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                 * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                 */
                mode?: 'accept' | 'reject' | 'drain' | 'backup';
                /**
                 * __Read-only__ The NodeBalancer ID that this Node belongs to.
                 */
                readonly nodebalancer_id?: number;
                /**
                 * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
                 */
                status?: 'unknown' | 'UP' | 'DOWN';
                /**
                 * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
                 */
                subnet_id?: number;
                /**
                 * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
                 */
                weight?: number;
            }>;
            /**
             * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
             */
            readonly nodes_status?: {
                /**
                 * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
                 */
                readonly down: number;
                /**
                 * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
                 */
                readonly up: number;
            };
            /**
             * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
             */
            port?: number;
            /**
             * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
             */
            protocol?: 'tcp';
            /**
             * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
             *
             * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
             * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
             * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
             */
            proxy_protocol?: 'none' | 'v1' | 'v2';
            /**
             * __Read-only__ Not applicable for TCP configs.
             */
            readonly ssl_cert?: string | null;
            /**
             * __Read-only__ Not applicable for TCP configs.
             */
            readonly ssl_commonname?: string;
            /**
             * __Read-only__ Not applicable for TCP configs.
             */
            readonly ssl_fingerprint?: string;
            /**
             * __Read-only__ Not applicable for HTTP configs.
             */
            readonly ssl_key?: string | null;
            /**
             * __Read-only__ Controls how session stickiness is handled on this port.
             *
             * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
             * - If set to `table`, sessions from the same remote address will be routed to the same backend.
             */
            stickiness?: 'none' | 'table';
        } | {
            /**
             * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
             */
            algorithm?: 'roundrobin' | 'leastconn' | 'source';
            /**
             * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
             *
             * - If `none`, no check is performed.
             * - `connection` requires only a connection to the backend to succeed.
             * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
             */
            check?: 'none' | 'connection' | 'http' | 'http_body';
            /**
             * How many times to attempt a check before considering a backend to be down.
             */
            check_attempts?: number;
            /**
             * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
             */
            check_body?: string;
            /**
             * How often, in seconds, to check that backends are up and serving requests.
             *
             * Must be greater than `check_timeout`.
             */
            check_interval?: number;
            /**
             * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
             */
            check_passive?: boolean;
            /**
             * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
             */
            check_path?: string;
            /**
             * How long, in seconds, to wait for a check attempt before considering it failed.
             *
             * Must be less than `check_interval`.
             */
            check_timeout?: number;
            /**
             * __Read-only__ Not applicable for `http` configs.
             */
            readonly cipher_suite?: string;
            /**
             * __Read-only__ This config's unique ID.
             */
            readonly id?: number;
            /**
             * __Read-only__ Identifies the NodeBalancer this config belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * The NodeBalancer nodes that serve this configuration.
             */
            nodes: Array<{
                /**
                 * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
                 * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
                 * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
                 * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
                 */
                address?: string;
                /**
                 * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                 */
                readonly config_id?: number;
                /**
                 * __Read-only__ This node's unique ID.
                 */
                readonly id?: number;
                /**
                 * The label for this node.  This is for display purposes only.
                 */
                label?: string;
                /**
                 * The mode this NodeBalancer should use when sending traffic to this backend.
                 *
                 * - If set to `accept` this backend is accepting traffic.
                 * - If set to `reject` this backend will not receive traffic.
                 * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                 * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                 */
                mode?: 'accept' | 'reject' | 'drain' | 'backup';
                /**
                 * __Read-only__ The NodeBalancer ID that this Node belongs to.
                 */
                readonly nodebalancer_id?: number;
                /**
                 * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
                 */
                status?: 'unknown' | 'UP' | 'DOWN';
                /**
                 * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
                 */
                subnet_id?: number;
                /**
                 * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
                 */
                weight?: number;
            }>;
            /**
             * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
             */
            readonly nodes_status?: {
                /**
                 * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
                 */
                readonly down: number;
                /**
                 * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
                 */
                readonly up: number;
            };
            /**
             * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
             */
            port?: number;
            /**
             * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
             */
            protocol?: 'http';
            /**
             * __Read-only__ Not applicable for HTTP configs.
             */
            readonly proxy_protocol?: string;
            /**
             * __Read-only__ Not applicable for HTTP configs.
             */
            readonly ssl_cert?: string | null;
            /**
             * __Read-only__ Not applicable for HTTP configs.
             */
            readonly ssl_commonname?: string;
            /**
             * __Read-only__ Not applicable for HTTP configs.
             */
            readonly ssl_fingerprint?: string;
            /**
             * __Read-only__ Not applicable for HTTP configs.
             */
            readonly ssl_key?: string | null;
            /**
             * Controls how session stickiness is handled on this port.
             *
             * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
             * - If set to `table`, sessions from the same remote address will be routed to the same backend.
             * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
             */
            stickiness?: 'none' | 'table' | 'http_cookie';
        } | {
            /**
             * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
             */
            algorithm?: 'roundrobin' | 'leastconn' | 'source';
            /**
             * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
             *
             * - If `none` no check is performed.
             * - `connection` requires only a connection to the backend to succeed.
             * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
             */
            check?: 'none' | 'connection' | 'http' | 'http_body';
            /**
             * How many times to attempt a check before considering a backend to be down.
             */
            check_attempts?: number;
            /**
             * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
             */
            check_body?: string;
            /**
             * How often, in seconds, to check that backends are up and serving requests.
             *
             * Must be greater than `check_timeout`.
             */
            check_interval?: number;
            /**
             * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
             */
            check_passive?: boolean;
            /**
             * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
             */
            check_path?: string;
            /**
             * How long, in seconds, to wait for a check attempt before considering it failed.
             *
             * Must be less than `check_interval`.
             */
            check_timeout?: number;
            /**
             * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
             */
            cipher_suite?: 'recommended' | 'legacy';
            /**
             * __Read-only__ This config's unique ID.
             */
            readonly id?: number;
            /**
             * __Read-only__ The ID for the NodeBalancer this config belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * The NodeBalancer nodes that serve this configuration.
             */
            nodes: Array<{
                /**
                 * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
                 * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
                 * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
                 * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
                 */
                address?: string;
                /**
                 * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
                 */
                readonly config_id?: number;
                /**
                 * __Read-only__ This node's unique ID.
                 */
                readonly id?: number;
                /**
                 * The label for this node.  This is for display purposes only.
                 */
                label?: string;
                /**
                 * The mode this NodeBalancer should use when sending traffic to this backend.
                 *
                 * - If set to `accept` this backend is accepting traffic.
                 * - If set to `reject` this backend will not receive traffic.
                 * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
                 * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
                 */
                mode?: 'accept' | 'reject' | 'drain' | 'backup';
                /**
                 * __Read-only__ The NodeBalancer ID that this Node belongs to.
                 */
                readonly nodebalancer_id?: number;
                /**
                 * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
                 */
                status?: 'unknown' | 'UP' | 'DOWN';
                /**
                 * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
                 */
                subnet_id?: number;
                /**
                 * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
                 */
                weight?: number;
            }>;
            /**
             * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
             */
            readonly nodes_status?: {
                /**
                 * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
                 */
                readonly down: number;
                /**
                 * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
                 */
                readonly up: number;
            };
            /**
             * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
             */
            port?: number;
            /**
             * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
             */
            protocol?: 'https';
            /**
             * __Read-only__ Not applicable for HTTPS configs.
             */
            readonly proxy_protocol?: string;
            /**
             *
             * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
             *
             * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
             *
             * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
             *
             * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
             *
             * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
             */
            ssl_cert?: string | null;
            /**
             * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
             */
            readonly ssl_commonname?: string;
            /**
             * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
             */
            readonly ssl_fingerprint?: string;
            /**
             * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
             *
             * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
             *
             * The contents of this field will not be shown in any responses that display
             * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
             * appears.
             *
             * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
             * response are automatically derived from your certificate. Please refer to these fields to
             * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
             */
            ssl_key?: string | null;
            /**
             * Controls how session stickiness is handled on this port.
             *
             * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
             * - If set to `table`, sessions from the same remote address will be routed to the same backend.
             * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
             */
            stickiness?: 'none' | 'table' | 'http_cookie';
        }>;
        /**
         * The ID of the Firewall to assign to the NodeBalancer.
         *
         * - A NodeBalancer can have only one Firewall assigned to it.
         * - Firewalls control inbound network traffic to NodeBalancers.
         */
        firewall_id?: number;
        /**
         * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
         */
        label?: string;
        /**
         * The ID of the Region to create this NodeBalancer in.
         */
        region: string;
        /**
         * An array of Tags applied to this object. Tags are for organizational purposes only.
         */
        tags?: Array<string>;
        /**
         * You can have only one `vpcs` in a NodeBalancer configuration. If any of your backend nodes are VPC Linodes, specify the VPC subnet and CIDR range. NodeBalancer routes traffic to backend VPC nodes through this subnet. The specified VPC subnet must exist within the same data center as the NodeBalancer, and the provided IP range must be contained within the subnet's CIDR block. All IP addresses within the specified range must be free and available for assignment. Once the NodeBalancer is created, its VPC cannot be changed.
         */
        vpcs?: Array<{
            /**
             * __Read-only__ Identifies the VPC configuration for this NodeBalancer.
             */
            readonly id?: number;
            /**
             * A CIDR range for the VPC's IPv4 addresses. The NodeBalancer sources IP addresses from this range when routing traffic to the backend VPC nodes.
             */
            ipv4_range?: string;
            /**
             * Enables the use of a larger `ipv4_range` subnet for multiple NodeBalancers within the same VPC by allocating smaller `/30` subnets for each NodeBalancer's backends.
             *
             * - When set to `true`, the system automatically allocates the smallest possible subnet (`/30`) from the provided `ipv4_range` for the NodeBalancer's backend interface. If the specified range doesn't have enough available IPs to allocate a `/30` subnet, the creation fails.
             *
             * - When set to `false`, the NodeBalancer is created using the entire `ipv4_range` as specified, without attempting to allocate a `/30` subnet.
             */
            ipv4_range_auto_assign?: boolean | null;
            /**
             * __Read-only__ Identifies the NodeBalancer.
             */
            readonly nodebalancer_id?: number;
            /**
             * The VPC's subnet. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation provides data for your VPCs and their subnets.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this NodeBalancer.
             */
            readonly vpc_id?: number | null;
        }>;
    };
    path?: never;
    query?: never;
    url: '/nodebalancers';
};

export type PostNodeBalancerErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostNodeBalancerError = PostNodeBalancerErrors[keyof PostNodeBalancerErrors];

export type PostNodeBalancerResponses = {
    /**
     * NodeBalancer
     *
     * Linode's load balancing solution. Can handle multiple ports, SSL termination, and any number of backends. NodeBalancer ports are configured with NodeBalancer configs, and each config is given one or more NodeBalancer nodes that accepts traffic.  The traffic should be routed to the NodeBalancer's IP address, for the NodeBalancer to handle routing individual requests to backends.
     */
    200: {
        /**
         * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
         */
        client_conn_throttle: number;
        /**
         * __Read-only__ When this NodeBalancer was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
         */
        readonly hostname: string;
        /**
         * __Read-only__ This NodeBalancer's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
         */
        readonly ipv4: string;
        /**
         * __Read-only__ This NodeBalancer's public IPv6 address.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
         */
        label: string;
        /**
         * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
         */
        readonly lke_cluster: {
            /**
             * The ID of the related LKE cluster.
             */
            id: string;
            /**
             * The label of the related LKE cluster.
             */
            label: string;
            /**
             * __Read-only__ The type for LKE clusters.
             */
            readonly type: string;
            /**
             * The URL where you can access the related LKE cluster.
             */
            url: string;
        } | null;
        /**
         * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
         */
        readonly region: string;
        /**
         * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
         */
        readonly transfer: {
            /**
             * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly in: number | null;
            /**
             * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly out: number | null;
            /**
             * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
             */
            readonly total: number | null;
        };
        /**
         * __Read-only__ The type of NodeBalancer.
         */
        type: 'common' | 'premium';
        /**
         * __Read-only__ When this NodeBalancer was last updated.
         */
        readonly updated: string;
    };
};

export type PostNodeBalancerResponse = PostNodeBalancerResponses[keyof PostNodeBalancerResponses];

export type GetNodeBalancerTypesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/nodebalancers/types';
};

export type GetNodeBalancerTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerTypesError = GetNodeBalancerTypesErrors[keyof GetNodeBalancerTypesErrors];

export type GetNodeBalancerTypesResponses = {
    /**
     * A collection of NodeBalancer types.
     */
    200: {
        /**
         * The NodeBalancer types.
         */
        data: Array<{
            /**
             * __Read-only__ The ID representing the NodeBalancer type.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The NodeBalancer type label is for display purposes only.
             */
            readonly label: string;
            /**
             * __Read-only__ The default cost of this NodeBalancer type. Prices are in US dollars, broken down into hourly and monthly charges.
             *
             * Certain regions have different prices from the default. For region-specific prices, see `region_prices`.
             */
            readonly price: {
                /**
                 * __Filterable__ Cost (in US dollars) per hour.
                 */
                hourly: number;
                /**
                 * __Filterable__ Cost per month, in US dollars.
                 */
                monthly: number;
            };
            region_prices: Array<{
                /**
                 * Cost per hour for this region, in US dollars.
                 */
                hourly: number;
                /**
                 * The Region ID for these prices.
                 */
                id: string;
                /**
                 * Cost per month for this region, in US dollars.
                 */
                monthly: number;
            }>;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount, in MB.
             */
            readonly transfer: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetNodeBalancerTypesResponse = GetNodeBalancerTypesResponses[keyof GetNodeBalancerTypesResponses];

export type DeleteNodeBalancerData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}';
};

export type DeleteNodeBalancerErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteNodeBalancerError = DeleteNodeBalancerErrors[keyof DeleteNodeBalancerErrors];

export type DeleteNodeBalancerResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteNodeBalancerResponse = DeleteNodeBalancerResponses[keyof DeleteNodeBalancerResponses];

export type GetNodeBalancerData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}';
};

export type GetNodeBalancerErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerError = GetNodeBalancerErrors[keyof GetNodeBalancerErrors];

export type GetNodeBalancerResponses = {
    /**
     * NodeBalancer
     *
     * Linode's load balancing solution. Can handle multiple ports, SSL termination, and any number of backends. NodeBalancer ports are configured with NodeBalancer configs, and each config is given one or more NodeBalancer nodes that accepts traffic.  The traffic should be routed to the NodeBalancer's IP address, for the NodeBalancer to handle routing individual requests to backends.
     */
    200: {
        /**
         * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
         */
        client_conn_throttle: number;
        /**
         * __Read-only__ When this NodeBalancer was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
         */
        readonly hostname: string;
        /**
         * __Read-only__ This NodeBalancer's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
         */
        readonly ipv4: string;
        /**
         * __Read-only__ This NodeBalancer's public IPv6 address.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
         */
        label: string;
        /**
         * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
         */
        readonly lke_cluster: {
            /**
             * The ID of the related LKE cluster.
             */
            id: string;
            /**
             * The label of the related LKE cluster.
             */
            label: string;
            /**
             * __Read-only__ The type for LKE clusters.
             */
            readonly type: string;
            /**
             * The URL where you can access the related LKE cluster.
             */
            url: string;
        } | null;
        /**
         * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
         */
        readonly region: string;
        /**
         * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
         */
        readonly transfer: {
            /**
             * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly in: number | null;
            /**
             * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly out: number | null;
            /**
             * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
             */
            readonly total: number | null;
        };
        /**
         * __Read-only__ The type of NodeBalancer.
         */
        type: 'common' | 'premium';
        /**
         * __Read-only__ When this NodeBalancer was last updated.
         */
        readonly updated: string;
    };
};

export type GetNodeBalancerResponse = GetNodeBalancerResponses[keyof GetNodeBalancerResponses];

export type PutNodeBalancerData = {
    /**
     * NodeBalancer
     *
     * Linode's load balancing solution. Can handle multiple ports, SSL termination, and any number of backends. NodeBalancer ports are configured with NodeBalancer configs, and each config is given one or more NodeBalancer nodes that accepts traffic.  The traffic should be routed to the NodeBalancer's IP address, for the NodeBalancer to handle routing individual requests to backends.
     */
    body: {
        /**
         * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
         */
        client_conn_throttle: number;
        /**
         * __Read-only__ When this NodeBalancer was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
         */
        readonly hostname: string;
        /**
         * __Read-only__ This NodeBalancer's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
         */
        readonly ipv4: string;
        /**
         * __Read-only__ This NodeBalancer's public IPv6 address.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
         */
        label: string;
        /**
         * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
         */
        readonly lke_cluster: {
            /**
             * The ID of the related LKE cluster.
             */
            id: string;
            /**
             * The label of the related LKE cluster.
             */
            label: string;
            /**
             * __Read-only__ The type for LKE clusters.
             */
            readonly type: string;
            /**
             * The URL where you can access the related LKE cluster.
             */
            url: string;
        } | null;
        /**
         * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
         */
        readonly region: string;
        /**
         * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
         */
        readonly transfer: {
            /**
             * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly in: number | null;
            /**
             * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly out: number | null;
            /**
             * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
             */
            readonly total: number | null;
        };
        /**
         * __Read-only__ The type of NodeBalancer.
         */
        type: 'common' | 'premium';
        /**
         * __Read-only__ When this NodeBalancer was last updated.
         */
        readonly updated: string;
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}';
};

export type PutNodeBalancerErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutNodeBalancerError = PutNodeBalancerErrors[keyof PutNodeBalancerErrors];

export type PutNodeBalancerResponses = {
    /**
     * NodeBalancer
     *
     * Linode's load balancing solution. Can handle multiple ports, SSL termination, and any number of backends. NodeBalancer ports are configured with NodeBalancer configs, and each config is given one or more NodeBalancer nodes that accepts traffic.  The traffic should be routed to the NodeBalancer's IP address, for the NodeBalancer to handle routing individual requests to backends.
     */
    200: {
        /**
         * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
         */
        client_conn_throttle: number;
        /**
         * __Read-only__ When this NodeBalancer was created.
         */
        readonly created: string;
        /**
         * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
         */
        readonly hostname: string;
        /**
         * __Read-only__ This NodeBalancer's unique ID.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
         */
        readonly ipv4: string;
        /**
         * __Read-only__ This NodeBalancer's public IPv6 address.
         */
        readonly ipv6: string | null;
        /**
         * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
         */
        label: string;
        /**
         * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
         */
        readonly lke_cluster: {
            /**
             * The ID of the related LKE cluster.
             */
            id: string;
            /**
             * The label of the related LKE cluster.
             */
            label: string;
            /**
             * __Read-only__ The type for LKE clusters.
             */
            readonly type: string;
            /**
             * The URL where you can access the related LKE cluster.
             */
            url: string;
        } | null;
        /**
         * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
         */
        readonly region: string;
        /**
         * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
         */
        tags: Array<string>;
        /**
         * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
         */
        readonly transfer: {
            /**
             * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly in: number | null;
            /**
             * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
             */
            readonly out: number | null;
            /**
             * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
             */
            readonly total: number | null;
        };
        /**
         * __Read-only__ The type of NodeBalancer.
         */
        type: 'common' | 'premium';
        /**
         * __Read-only__ When this NodeBalancer was last updated.
         */
        readonly updated: string;
    };
};

export type PutNodeBalancerResponse = PutNodeBalancerResponses[keyof PutNodeBalancerResponses];

export type GetNodeBalancerConfigsData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/nodebalancers/{nodeBalancerId}/configs';
};

export type GetNodeBalancerConfigsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerConfigsError = GetNodeBalancerConfigsErrors[keyof GetNodeBalancerConfigsErrors];

export type GetNodeBalancerConfigsResponses = {
    /**
     * NodeBalancer `config` options for each protocol.
     */
    200: {
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `udp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * Not applicable to TCP configurations.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are always assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address are routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * The number of seconds between each check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    };
};

export type GetNodeBalancerConfigsResponse = GetNodeBalancerConfigsResponses[keyof GetNodeBalancerConfigsResponses];

export type PostNodeBalancerConfigData = {
    /**
     * NodeBalancer configuration details for the port based on the routing protocol.
     */
    body?: ({
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
             */
            mode?: 'none';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         */
        stickiness?: 'none' | 'table';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    }) & {
        /**
         * The NodeBalancer nodes that serve this config.
         *
         * Some considerations for Nodes when rebuilding a config:
         *
         * - Current Nodes excluded from the request body will be deleted from the Config.
         * - Current Nodes (identified by their Node ID) will be updated.
         * - New Nodes (included without a Node ID) will be created.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address: string;
            /**
             * The unique ID of the Node to update.
             */
            id: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight: number;
        }>;
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs';
};

export type PostNodeBalancerConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostNodeBalancerConfigError = PostNodeBalancerConfigErrors[keyof PostNodeBalancerConfigErrors];

export type PostNodeBalancerConfigResponses = {
    /**
     * NodeBalancer `config` options for each protocol.
     */
    200: {
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `udp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * Not applicable to TCP configurations.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are always assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address are routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * The number of seconds between each check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    };
};

export type PostNodeBalancerConfigResponse = PostNodeBalancerConfigResponses[keyof PostNodeBalancerConfigResponses];

export type DeleteNodeBalancerConfigData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}';
};

export type DeleteNodeBalancerConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteNodeBalancerConfigError = DeleteNodeBalancerConfigErrors[keyof DeleteNodeBalancerConfigErrors];

export type DeleteNodeBalancerConfigResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteNodeBalancerConfigResponse = DeleteNodeBalancerConfigResponses[keyof DeleteNodeBalancerConfigResponses];

export type GetNodeBalancerConfigData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}';
};

export type GetNodeBalancerConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerConfigError = GetNodeBalancerConfigErrors[keyof GetNodeBalancerConfigErrors];

export type GetNodeBalancerConfigResponses = {
    /**
     * NodeBalancer `config` options for each protocol.
     */
    200: {
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `udp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * Not applicable to TCP configurations.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are always assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address are routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * The number of seconds between each check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    };
};

export type GetNodeBalancerConfigResponse = GetNodeBalancerConfigResponses[keyof GetNodeBalancerConfigResponses];

export type PutNodeBalancerConfigData = {
    /**
     * NodeBalancer `config` options for each protocol.
     */
    body: {
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they're serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts: number;
        /**
         * This value must be present in the check's response body for it to pass. Otherwise the backend is considered to be down.
         */
        check_body: string;
        /**
         * Number of seconds between checks to verify that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path: string;
        /**
         * Number of seconds to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout: number;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly cipher_suite: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case.
         *
         * Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key: string | null;
        /**
         * Controls how session stickiness is handled on this port:
         *
         * - If set to `none`, connections are assigned a back-end server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port: number;
        /**
         * __Read-only__ The maximum number of seconds a UDP session can be idle before it's closed.
         */
        readonly udp_session_timeout: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - Setting this to `none` skips the check.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body: string;
        /**
         * Number of seconds between checks to verify that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request it is considered to be down.
         */
        check_path: string;
        /**
         * Number of seconds to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout: number;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly cipher_suite: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case.
         *
         * Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port:
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         */
        stickiness: 'none' | 'table';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they're serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body: string;
        /**
         * Number of seconds between checks to verify that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path: string;
        /**
         * Number of seconds to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout: number;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly cipher_suite: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port: number;
        /**
         * The protocol the port is configured to serve, `http` in this case.
         *
         * Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key: string | null;
        /**
         * Controls how session stickiness is handled on this port:
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - Setting this to `none` skips the check.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body: string;
        /**
         * Number of seconds between checks to verify that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path: string;
        /**
         * Number of seconds to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer.
         *
         * - `legacy` is considered insecure and should only be used if necessary.
         */
        cipher_suite: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These aren't in rotation, and aren't serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, currently serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally assigned specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port: number;
        /**
         * The protocol the port is configured to serve, `https` in this case.
         *
         * - The `https` protocol is mutually required with `ssl_cert` and `ssl_key`.
         *
         * Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol: string;
        /**
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key: string | null;
        /**
         * Controls how session stickiness is handled on this port:
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness: 'none' | 'table' | 'http_cookie';
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}';
};

export type PutNodeBalancerConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutNodeBalancerConfigError = PutNodeBalancerConfigErrors[keyof PutNodeBalancerConfigErrors];

export type PutNodeBalancerConfigResponses = {
    /**
     * NodeBalancer `config` options for each protocol.
     */
    200: {
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `udp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * Not applicable to TCP configurations.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are always assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address are routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * The number of seconds between each check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    };
};

export type PutNodeBalancerConfigResponse = PutNodeBalancerConfigResponses[keyof PutNodeBalancerConfigResponses];

export type GetNodeBalancerConfigNodesData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the NodeBalancer config to access.
         */
        configId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}/nodes';
};

export type GetNodeBalancerConfigNodesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerConfigNodesError = GetNodeBalancerConfigNodesErrors[keyof GetNodeBalancerConfigNodesErrors];

export type GetNodeBalancerConfigNodesResponses = {
    /**
     * A paginated list of NodeBalancer nodes.
     */
    200: {
        /**
         * A NodeBalancer node represents a single backend serving requests for a single port on a NodeBalancer. Nodes are tied to NodeBalancer configurations, and serve traffic over their private IPv4 address, IPv6 address, or VPC IPv4 address. If the same Linode is serving traffic for more than one port on the same NodeBalancer, one NodeBalancer node is required for each config (port) it should serve requests on.  For example, if you have four backends, and each should respond to both HTTP and HTTPS requests, you will need two NodeBalancer configs (port 80 and port 443) and four backends each, one for each of the Linodes serving requests for that port.
         */
        data: Array<{
            /**
             * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'Unknown' | 'UP' | 'DOWN';
            /**
             * __Read-only__ For VPC backends, you can use the `vpc_config_id` to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only in `{apiVersion}` `v4beta`.
             */
            readonly vpc_config_id?: number | null;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        } | {
            /**
             * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
             */
            mode?: 'none';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
             */
            status?: 'Unknown' | 'UP' | 'DOWN';
            /**
             * __Read-only__ For VPC backends, the `vpc_config_id` can be used to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only when the `apiVersion` is `v4beta`.
             */
            readonly vpc_config_id?: number | null;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetNodeBalancerConfigNodesResponse = GetNodeBalancerConfigNodesResponses[keyof GetNodeBalancerConfigNodesResponses];

export type PostNodeBalancerNodeData = {
    /**
     * A NodeBalancer node represents a single backend serving requests for a single port of a NodeBalancer. Nodes are tied to individual NodeBalancer configurations and route traffic over their private IPv4 address, IPv6 address, or VPC IPv4 address. If the same Linode is serving traffic for more than one port on the same NodeBalancer, one NodeBalancer node is required for each config (port) it should serve requests on.  For example, if you have four backends, and each should respond to both HTTP and HTTPS requests, you will need two NodeBalancer configs (port 80 and port 443) and four backends each, one for each of the Linodes serving requests for that port.
     */
    body: {
        /**
         * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
         * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
         * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
         * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
         */
        mode?: 'none';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
         */
        status?: 'unknown' | 'UP' | 'DOWN';
        /**
         * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         */
        subnet_id?: number;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    } | {
        /**
         * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
         * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
         * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
         * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * The mode this NodeBalancer should use when sending traffic to this backend.
         *
         * - If set to `accept` this backend is accepting traffic.
         * - If set to `reject` this backend will not receive traffic.
         * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
         * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         */
        mode?: 'accept' | 'reject' | 'drain' | 'backup';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         */
        status?: 'unknown' | 'UP' | 'DOWN';
        /**
         * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         */
        subnet_id?: number;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the NodeBalancer config to access.
         */
        configId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}/nodes';
};

export type PostNodeBalancerNodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostNodeBalancerNodeError = PostNodeBalancerNodeErrors[keyof PostNodeBalancerNodeErrors];

export type PostNodeBalancerNodeResponses = {
    /**
     * A NodeBalancer node represents a single backend serving requests for a single port on a NodeBalancer. Nodes are tied to NodeBalancer configurations, and serve traffic over their private IPv4 address, IPv6 address, or VPC IPv4 address. If the same Linode is serving traffic for more than one port on the same NodeBalancer, one NodeBalancer node is required for each config (port) it should serve requests on.  For example, if you have four backends, and each should respond to both HTTP and HTTPS requests, you will need two NodeBalancer configs (port 80 and port 443) and four backends each, one for each of the Linodes serving requests for that port.
     */
    200: {
        /**
         * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * The mode this NodeBalancer should use when sending traffic to this backend.
         *
         * - If set to `accept` this backend is accepting traffic.
         * - If set to `reject` this backend will not receive traffic.
         * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
         * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         */
        mode?: 'accept' | 'reject' | 'drain' | 'backup';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         */
        status?: 'Unknown' | 'UP' | 'DOWN';
        /**
         * __Read-only__ For VPC backends, you can use the `vpc_config_id` to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only in `{apiVersion}` `v4beta`.
         */
        readonly vpc_config_id?: number | null;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    } | {
        /**
         * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
         */
        mode?: 'none';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
         */
        status?: 'Unknown' | 'UP' | 'DOWN';
        /**
         * __Read-only__ For VPC backends, the `vpc_config_id` can be used to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only when the `apiVersion` is `v4beta`.
         */
        readonly vpc_config_id?: number | null;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    };
};

export type PostNodeBalancerNodeResponse = PostNodeBalancerNodeResponses[keyof PostNodeBalancerNodeResponses];

export type DeleteNodeBalancerConfigNodeData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
        /**
         * The ID of the Node to access.
         */
        nodeId: string;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}/nodes/{nodeId}';
};

export type DeleteNodeBalancerConfigNodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteNodeBalancerConfigNodeError = DeleteNodeBalancerConfigNodeErrors[keyof DeleteNodeBalancerConfigNodeErrors];

export type DeleteNodeBalancerConfigNodeResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteNodeBalancerConfigNodeResponse = DeleteNodeBalancerConfigNodeResponses[keyof DeleteNodeBalancerConfigNodeResponses];

export type GetNodeBalancerNodeData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
        /**
         * The ID of the Node to access.
         */
        nodeId: string;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}/nodes/{nodeId}';
};

export type GetNodeBalancerNodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerNodeError = GetNodeBalancerNodeErrors[keyof GetNodeBalancerNodeErrors];

export type GetNodeBalancerNodeResponses = {
    /**
     * A NodeBalancer node represents a single backend serving requests for a single port on a NodeBalancer. Nodes are tied to NodeBalancer configurations, and serve traffic over their private IPv4 address, IPv6 address, or VPC IPv4 address. If the same Linode is serving traffic for more than one port on the same NodeBalancer, one NodeBalancer node is required for each config (port) it should serve requests on.  For example, if you have four backends, and each should respond to both HTTP and HTTPS requests, you will need two NodeBalancer configs (port 80 and port 443) and four backends each, one for each of the Linodes serving requests for that port.
     */
    200: {
        /**
         * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * The mode this NodeBalancer should use when sending traffic to this backend.
         *
         * - If set to `accept` this backend is accepting traffic.
         * - If set to `reject` this backend will not receive traffic.
         * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
         * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         */
        mode?: 'accept' | 'reject' | 'drain' | 'backup';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         */
        status?: 'Unknown' | 'UP' | 'DOWN';
        /**
         * __Read-only__ For VPC backends, you can use the `vpc_config_id` to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only in `{apiVersion}` `v4beta`.
         */
        readonly vpc_config_id?: number | null;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    } | {
        /**
         * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
         */
        mode?: 'none';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
         */
        status?: 'Unknown' | 'UP' | 'DOWN';
        /**
         * __Read-only__ For VPC backends, the `vpc_config_id` can be used to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only when the `apiVersion` is `v4beta`.
         */
        readonly vpc_config_id?: number | null;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    };
};

export type GetNodeBalancerNodeResponse = GetNodeBalancerNodeResponses[keyof GetNodeBalancerNodeResponses];

export type PutNodeBalancerNodeData = {
    /**
     * A NodeBalancer node represents a single backend serving requests for a single port of a NodeBalancer. Nodes are tied to individual NodeBalancer configurations and route traffic over their private IPv4 address, IPv6 address, or VPC IPv4 address. If the same Linode is serving traffic for more than one port on the same NodeBalancer, one NodeBalancer node is required for each config (port) it should serve requests on.  For example, if you have four backends, and each should respond to both HTTP and HTTPS requests, you will need two NodeBalancer configs (port 80 and port 443) and four backends each, one for each of the Linodes serving requests for that port.
     */
    body: {
        /**
         * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
         * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
         * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
         * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
         */
        mode?: 'none';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
         */
        status?: 'unknown' | 'UP' | 'DOWN';
        /**
         * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         */
        subnet_id?: number;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    } | {
        /**
         * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
         * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
         * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
         * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * The mode this NodeBalancer should use when sending traffic to this backend.
         *
         * - If set to `accept` this backend is accepting traffic.
         * - If set to `reject` this backend will not receive traffic.
         * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
         * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         */
        mode?: 'accept' | 'reject' | 'drain' | 'backup';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         */
        status?: 'unknown' | 'UP' | 'DOWN';
        /**
         * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
         */
        subnet_id?: number;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
        /**
         * The ID of the Node to access.
         */
        nodeId: string;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}/nodes/{nodeId}';
};

export type PutNodeBalancerNodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutNodeBalancerNodeError = PutNodeBalancerNodeErrors[keyof PutNodeBalancerNodeErrors];

export type PutNodeBalancerNodeResponses = {
    /**
     * A NodeBalancer node represents a single backend serving requests for a single port on a NodeBalancer. Nodes are tied to NodeBalancer configurations, and serve traffic over their private IPv4 address, IPv6 address, or VPC IPv4 address. If the same Linode is serving traffic for more than one port on the same NodeBalancer, one NodeBalancer node is required for each config (port) it should serve requests on.  For example, if you have four backends, and each should respond to both HTTP and HTTPS requests, you will need two NodeBalancer configs (port 80 and port 443) and four backends each, one for each of the Linodes serving requests for that port.
     */
    200: {
        /**
         * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * The mode this NodeBalancer should use when sending traffic to this backend.
         *
         * - If set to `accept` this backend is accepting traffic.
         * - If set to `reject` this backend will not receive traffic.
         * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
         * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
         */
        mode?: 'accept' | 'reject' | 'drain' | 'backup';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
         */
        status?: 'Unknown' | 'UP' | 'DOWN';
        /**
         * __Read-only__ For VPC backends, you can use the `vpc_config_id` to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only in `{apiVersion}` `v4beta`.
         */
        readonly vpc_config_id?: number | null;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    } | {
        /**
         * The address and port where this backend can be reached: either the Linode’s private IPv4, public IPv6, or the VPC's IPv4 address.
         */
        address?: string;
        /**
         * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
         */
        readonly config_id?: number;
        /**
         * __Read-only__ This node's unique ID.
         */
        readonly id?: number;
        /**
         * The label for this node.  This is for display purposes only.
         */
        label?: string;
        /**
         * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
         */
        mode?: 'none';
        /**
         * __Read-only__ The NodeBalancer ID that this Node belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
         */
        status?: 'Unknown' | 'UP' | 'DOWN';
        /**
         * __Read-only__ For VPC backends, the `vpc_config_id` can be used to [get VPC IDs and VPC subnet IDs](https://techdocs.akamai.com/linode-api/reference/get-node-balancer-vpc-config). Returns a `null` value if the backend is not a VPC node. Included only when the `apiVersion` is `v4beta`.
         */
        readonly vpc_config_id?: number | null;
        /**
         * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
         */
        weight?: number;
    };
};

export type PutNodeBalancerNodeResponse = PutNodeBalancerNodeResponses[keyof PutNodeBalancerNodeResponses];

export type PostRebuildNodeBalancerConfigData = {
    /**
     * Information about the NodeBalancer configuration to rebuild.
     */
    body: ({
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * __Read-only__ The mode this NodeBalancer should use when sending traffic to this backend. For backend nodes with a UDP config, the `mode` doesn't apply, so the value is `none`.
             */
            mode?: 'none';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         */
        stickiness?: 'none' | 'table';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * The NodeBalancer nodes that serve this configuration.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address?: string;
            /**
             * __Read-only__ The NodeBalancer Config ID that this Node belongs to.
             */
            readonly config_id?: number;
            /**
             * __Read-only__ This node's unique ID.
             */
            readonly id?: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label?: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode?: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * __Read-only__ The NodeBalancer ID that this Node belongs to.
             */
            readonly nodebalancer_id?: number;
            /**
             * __Read-only__ The current status of this node, based on the configured checks of its NodeBalancer Config.
             */
            status?: 'unknown' | 'UP' | 'DOWN';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id?: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight?: number;
        }>;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    }) & {
        /**
         * The NodeBalancer nodes that serve this config.
         *
         * Some considerations for Nodes when rebuilding a config:
         *
         * - Current Nodes excluded from the request body will be deleted from the Config.
         * - Current Nodes (identified by their Node ID) will be updated.
         * - New Nodes (included without a Node ID) will be created.
         */
        nodes: Array<{
            /**
             * Backend nodes can be Linodes and Linodes within a VPC. The following IP types are supported:
             * - For non-VPC backend nodes, the private IPv4 address and port where this backend can be reached.
             * - For non-VPC backend nodes, the public IPv6 address and port where this backend can be reached.
             * - For backend nodes within a VPC, the IPv4 address and port where this backend can be reached.
             */
            address: string;
            /**
             * The unique ID of the Node to update.
             */
            id: number;
            /**
             * The label for this node.  This is for display purposes only.
             */
            label: string;
            /**
             * The mode this NodeBalancer should use when sending traffic to this backend.
             *
             * - If set to `accept` this backend is accepting traffic.
             * - If set to `reject` this backend will not receive traffic.
             * - If set to `drain` this backend will not receive _new_ traffic, but connections already pinned to it will continue to be routed to it.
             * - If set to `backup`, this backend will only receive traffic if all `accept` nodes are down.
             */
            mode: 'accept' | 'reject' | 'drain' | 'backup';
            /**
             * Required for VPC backend nodes only. The VPC's subnet. To display information about your VPCs and their subnets, run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation.
             */
            subnet_id: number;
            /**
             * Used when picking a backend to serve a request and is not pinned to a single backend yet. Nodes with a higher weight will receive more traffic.
             */
            weight: number;
        }>;
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The ID of the Config to access.
         */
        configId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/configs/{configId}/rebuild';
};

export type PostRebuildNodeBalancerConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostRebuildNodeBalancerConfigError = PostRebuildNodeBalancerConfigErrors[keyof PostRebuildNodeBalancerConfigErrors];

export type PostRebuildNodeBalancerConfigResponses = {
    /**
     * NodeBalancer `config` options for each protocol.
     */
    200: {
        /**
         * The algorithm this UDP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'ring_hash';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value isn't present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * __Read-only__ Must be `false` for UDP.
         */
        readonly check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `udp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `udp` in this case. Review our guide on [Available Protocols](https://www.linode.com/docs/products/networking/nodebalancers/guides/protocols/) for information on protocol features.
         */
        protocol?: 'udp';
        /**
         * __Read-only__ Must be `none` for UDP.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for UPD configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for UDP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are assigned a backend server based on the algorithm configured.
         * - If set to `session`, all packets with the same session identifiers are routed to the same backend server. Two packets are considered part of the same session if they share the same source and destination IP addresses or ports, and are received within a short time window.
         * - If set to `source_ip`, the NodeBalancer uses the client's source IP address to route all packets from the same client to the same backend server.
         */
        stickiness?: 'none' | 'session' | 'source_ip';
        /**
         * UDP NodeBalancers use TCP and HTTP active health checks to ensure back-end nodes can receive traffic. You can specify the health check port that the backend node listens on, which may differ from the UDP port used to serve traffic.
         */
        udp_check_port?: number;
        /**
         * __Read-only__ The maximum duration in seconds, a UDP session can be idle before it is closed.
         */
        readonly udp_session_timeout?: number;
    } | {
        /**
         * The algorithm this TCP NodeBalancer uses to route traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code is enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. If the backend does not respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `tcp` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `tcp` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'tcp';
        /**
         * Proxy protocol is a TCP extension that sends initial TCP connection information such as source or destination IPs and ports to backend devices. Proxy protocol preserves initial TCP information that would be lost otherwise. Backend devices must be configured to work with `proxy_protocol` if enabled.
         *
         * - If omitted, or set to `none`, the NodeBalancer doesn't send any auxiliary data over TCP connections. This is the default.
         * - If set to `v1`, the human-readable header format (Version 1) is used. Requires `tcp` protocol.
         * - If set to `v2`, the binary header format (Version 2) is used. Requires `tcp` protocol.
         */
        proxy_protocol?: 'none' | 'v1' | 'v2';
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for TCP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * __Read-only__ Controls how session stickiness is handled on this port.
         *
         * Not applicable to TCP configurations.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTP NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. This determines if backends are up or down.
         *
         * - If `none`, no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value needs to be in the response body of the check in order for it to pass. Otherwise the backend is considered to be down.
         */
        check_body?: string;
        /**
         * How often, in seconds, to check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * __Read-only__ Not applicable for `http` configs.
         */
        readonly cipher_suite?: string;
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ Identifies the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers need to be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced. You may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `http` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features.
         */
        protocol?: 'http';
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly proxy_protocol?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_cert?: string | null;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_fingerprint?: string;
        /**
         * __Read-only__ Not applicable for HTTP configs.
         */
        readonly ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections are always assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address are routed to the same backend.
         * - If set to `http_cookie`, sessions are routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    } | {
        /**
         * The algorithm this HTTPS NodeBalancer uses for routing traffic to backends.
         */
        algorithm?: 'roundrobin' | 'leastconn' | 'source';
        /**
         * The type of check to perform against backends to ensure they are serving requests. The `check` is used to determine if backends are up or down.
         *
         * - If `none` no check is performed.
         * - `connection` requires only a connection to the backend to succeed.
         * - `http` and `http_body` rely on the backend serving HTTP, and that the response returned matches what is expected.
         */
        check?: 'none' | 'connection' | 'http' | 'http_body';
        /**
         * How many times to attempt a check before considering a backend to be down.
         */
        check_attempts?: number;
        /**
         * Use when the active health `check` type is `http_body`. This value must be present in the response body of the check in order for it to pass. If this value is not present in the response body of a check request, the backend is considered to be down.
         */
        check_body?: string;
        /**
         * The number of seconds between each check that backends are up and serving requests.
         *
         * Must be greater than `check_timeout`.
         */
        check_interval?: number;
        /**
         * If `true`, any response from this backend with a `5xx` status code will be enough for it to be considered unhealthy and taken out of rotation.
         */
        check_passive?: boolean;
        /**
         * The URL path to check on each backend. Use when the active health `check` type is `http`. If the backend doesn't respond to this request, it's considered to be down.
         */
        check_path?: string;
        /**
         * How long, in seconds, to wait for a check attempt before considering it failed.
         *
         * Must be less than `check_interval`.
         */
        check_timeout?: number;
        /**
         * What ciphers to use for SSL connections served by this NodeBalancer. The `legacy` cipher is considered insecure and should only be used if necessary. For information on recommended and legacy ciphers, see [TLS cipher suites](https://techdocs.akamai.com/cloud-computing/docs/tls-ssl-termination-on-nodebalancers#tls-cipher-suites).
         */
        cipher_suite?: 'recommended' | 'legacy';
        /**
         * __Read-only__ This config's unique ID.
         */
        readonly id?: number;
        /**
         * __Read-only__ The ID for the NodeBalancer this config belongs to.
         */
        readonly nodebalancer_id?: number;
        /**
         * __Read-only__ Describes the health of the backends for this port. This data updates periodically as checks are performed against backends.
         */
        readonly nodes_status?: {
            /**
             * __Read-only__ The number of backends considered to be `DOWN` and unhealthy.  These are not in rotation, and not serving requests.
             */
            readonly down: number;
            /**
             * __Read-only__ The number of backends considered to be `UP` and healthy, and that are serving requests.
             */
            readonly up: number;
        };
        /**
         * This is the port the NodeBalancer listens on for this configuration. Port numbers must be unique across TCP, HTTP, and HTTPS configurations on a single NodeBalancer. However, ports assigned to TCP, HTTP, or HTTPS configurations can also be reused for UDP configurations. For example, Port 80 can simultaneously serve a TCP and a UDP configuration on the same NodeBalancer, but it can't be shared by both a TCP and an HTTP configuration. Although certain ports are traditionally associated with specific protocols, this isn't strictly enforced, and you may configure your NodeBalancer however you find useful.
         */
        port?: number;
        /**
         * The protocol the port is configured to serve, `https` in this case. Review our guide on [Available protocols](https://techdocs.akamai.com/cloud-computing/docs/available-protocols) for information on protocol features. The `https` protocol needs to be specified with both `ssl_cert` and `ssl_key`.
         */
        protocol?: 'https';
        /**
         * __Read-only__ Not applicable for HTTPS configs.
         */
        readonly proxy_protocol?: string;
        /**
         *
         * The PEM-formatted public SSL certificate (or the combined PEM-formatted SSL certificate and Certificate Authority chain) that should be served on this NodeBalancerConfig's port.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * [Diffie-Hellman Parameters](https://www.linode.com/docs/products/networking/nodebalancers/guides/ssl-termination/#diffie-hellman-parameters) can be included in this value to enable forward secrecy.
         *
         * The contents of this field will not be shown in any responses that display the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig response are automatically derived from your certificate. Please refer to these fields to verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_cert?: string | null;
        /**
         * __Read-only__ The read-only common name automatically derived from the SSL certificate assigned to this NodeBalancerConfig. Please refer to this field to verify that the appropriate certificate is assigned to your NodeBalancerConfig.
         */
        readonly ssl_commonname?: string;
        /**
         * __Read-only__ The read-only SHA1-encoded fingerprint automatically derived from the SSL certificate assigned to this NodeBalancer configuration. Please refer to the `ssl_fingerprint` field to verify that the appropriate certificate is assigned to your NodeBalancer configuration.
         */
        readonly ssl_fingerprint?: string;
        /**
         * The PEM-formatted private key for the SSL certificate set in the `ssl_cert` field.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI).
         *
         * The contents of this field will not be shown in any responses that display
         * the NodeBalancerConfig. Instead, `<REDACTED>` will be printed where the field
         * appears.
         *
         * The read-only `ssl_commonname` and `ssl_fingerprint` fields in a NodeBalancerConfig
         * response are automatically derived from your certificate. Please refer to these fields to
         * verify that the appropriate certificate was assigned to your NodeBalancerConfig.
         */
        ssl_key?: string | null;
        /**
         * Controls how session stickiness is handled on this port.
         *
         * - If set to `none`, connections will always be assigned a backend based on the algorithm configured.
         * - If set to `table`, sessions from the same remote address will be routed to the same backend.
         * - For HTTP or HTTPS clients, `http_cookie` allows sessions to be routed to the same backend based on a cookie set by the NodeBalancer.
         */
        stickiness?: 'none' | 'table' | 'http_cookie';
    };
};

export type PostRebuildNodeBalancerConfigResponse = PostRebuildNodeBalancerConfigResponses[keyof PostRebuildNodeBalancerConfigResponses];

export type GetNodeBalancerFirewallsData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/firewalls';
};

export type GetNodeBalancerFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerFirewallsError = GetNodeBalancerFirewallsErrors[keyof GetNodeBalancerFirewallsErrors];

export type GetNodeBalancerFirewallsResponses = {
    /**
     * Returns a paginated list of Firewalls assigned to this NodeBalancer.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this Firewall was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
             */
            readonly entities: Array<{
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a `linode`.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'linode_interface' | 'nodebalancer';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            }>;
            /**
             * __Filterable__, __Read-only__ The Firewall's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The Firewall's label, for display purposes only.
             *
             * Firewall labels have the following constraints:
             *
             * - Must begin and end with an alphanumeric character.
             * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             * - Must be between 3 and 32 characters.
             * - Must be unique.
             */
            label: string;
            /**
             * The inbound and outbound access rules to apply to the Firewall.
             *
             * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
             *
             * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
                 */
                readonly fingerprint: string;
                /**
                 * The inbound rules for the firewall, as a JSON array.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall, as a JSON array.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
                 */
                readonly version: number;
            };
            /**
             * __Read-only__ The status of this Firewall.
             *
             * - When a Firewall is first created its status is `enabled`.
             * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
             * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
             */
            status: 'enabled' | 'disabled' | 'deleted';
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Filterable__, __Read-only__ When this Firewall was last updated.
             */
            readonly updated: string;
        }>;
    };
};

export type GetNodeBalancerFirewallsResponse = GetNodeBalancerFirewallsResponses[keyof GetNodeBalancerFirewallsResponses];

export type PutNodeBalancerFirewallsData = {
    /**
     * Replace or remove firewalls IDs assigned to a Linode or NodeBalancer.
     */
    body: {
        /**
         * A complete list of firewall IDs to assign to this Linode or NodeBalancer. This operation replaces any existing assignments. To remove all firewalls, pass an empty list, `[]`.
         */
        firewall_ids: Array<number>;
    };
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/nodebalancers/{nodeBalancerId}/firewalls';
};

export type PutNodeBalancerFirewallsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutNodeBalancerFirewallsError = PutNodeBalancerFirewallsErrors[keyof PutNodeBalancerFirewallsErrors];

export type PutNodeBalancerFirewallsResponses = {
    /**
     * Returns a paginated list of Firewalls assigned to this NodeBalancer.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this Firewall was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The Linodes, NodeBalancers, and Linode interfaces this firewall is assigned to.
             */
            readonly entities: Array<{
                /**
                 * The entity's ID.
                 */
                id: number;
                /**
                 * __Read-only__ The entity's label.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The Linode this `linode_interface` is assigned to. Returns `null` when the entity `type` is `linode` or `nodebalancer`.
                 */
                readonly parent_entity: {
                    /**
                     * The ID of the Linode assigned to this `linode_interface`.
                     */
                    id: number;
                    /**
                     * The name of the Linode this `linode_interface` is assigned to.
                     */
                    label: string;
                    /**
                     * __Read-only__ Currently, no parent entity has a parent of its own. Each `parent_entity` will have its own `parent_entity` set to `null`.
                     */
                    readonly parent_entity: string | null;
                    /**
                     * A Linode interface’s `parent_entity` is a `linode`.
                     */
                    type: 'linode';
                    /**
                     * The API URL path you can use to access this entity.
                     */
                    url: string;
                } | null;
                /**
                 * The entity's type.
                 */
                type: 'linode' | 'linode_interface' | 'nodebalancer';
                /**
                 * __Read-only__ The API URL path you can use to access this entity.
                 */
                readonly url: string;
            }>;
            /**
             * __Filterable__, __Read-only__ The Firewall's unique ID.
             */
            readonly id: number;
            /**
             * __Filterable__ The Firewall's label, for display purposes only.
             *
             * Firewall labels have the following constraints:
             *
             * - Must begin and end with an alphanumeric character.
             * - May only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
             * - Must be between 3 and 32 characters.
             * - Must be unique.
             */
            label: string;
            /**
             * The inbound and outbound access rules to apply to the Firewall.
             *
             * A Firewall may have up to 25 rules across its inbound and outbound rulesets.
             *
             * Multiple rules are applied in order. If two rules conflict, the first rule takes precedence. For example, if the first rule accepts inbound traffic from an address, and the second rule drops inbound traffic the same address, the first rule applies and inbound traffic from that address is accepted.
             */
            rules: {
                /**
                 * __Read-only__ The fingerprint is a 32-bit hash. It represents the firewall rules as an 8 character hex string. You can use `fingerprint` to compare rule versions.
                 */
                readonly fingerprint: string;
                /**
                 * The inbound rules for the firewall, as a JSON array.
                 */
                inbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for inbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `inbound.action` property of the Firewall Rule.
                 */
                inbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * The outbound rules for the firewall, as a JSON array.
                 */
                outbound: Array<{
                    /**
                     * Controls whether traffic is accepted or dropped by this rule. Overrides the Firewall's `inbound_policy` if this is an inbound rule, or the `outbound_policy` if this is an outbound rule.
                     */
                    action: 'ACCEPT' | 'DROP';
                    /**
                     * The IPv4 or IPv6 addresses affected by this rule. A rule can have up to 255 total addresses or networks listed across its `ipv4` and `ipv6` arrays. A network and a single IP are treated as equivalent when accounting for this limit.
                     *
                     * Must contain `ipv4`, `ipv6`, or both.
                     */
                    addresses: {
                        /**
                         * A list of IPv4 addresses or networks. Addresses must be in IP/mask format. Must not be an empty list.
                         *
                         * If `0.0.0.0/0` is included in this list, all IPv4 addresses are affected by this rule.
                         */
                        ipv4: Array<string>;
                        /**
                         * A list of IPv6 addresses or networks. Addresses must be in IP/mask format and must not include zone_id notation as described in [RFC 4007](https://www.rfc-editor.org/rfc/rfc4007). Must not be an empty list.
                         *
                         * If `::/0` is included in this list, all IPv6 addresses are affected by this rule.
                         */
                        ipv6: Array<string>;
                    };
                    /**
                     * Used to describe this rule. For display purposes only.
                     */
                    description: string;
                    /**
                     * Used to identify this rule. For display purposes only.
                     */
                    label: string;
                    /**
                     * A string representing the port or ports affected by this rule:
                     *
                     * - The string may be a single port, a range of ports, or a comma-separated list of single ports and port ranges. A space is permitted following each comma.
                     * - A range of ports is inclusive of the start and end values for the range. The end value of the range must be greater than the start value.
                     * - Ports must be within 1 and 65535, and may not contain any leading zeroes. For example, port `080` is not allowed.
                     * - The ports string can have up to 15 _pieces_, where a single port is treated as one piece, and a port range is treated as two pieces. For example, the string "22-24, 80, 443" has four pieces.
                     * - If no ports are configured, all ports are affected.
                     * - Only allowed for the TCP and UDP protocols. Ports are not allowed for the ICMP and IPENCAP protocols.
                     */
                    ports: string | null;
                    /**
                     * The type of network traffic affected by this rule.
                     */
                    protocol: 'TCP' | 'UDP' | 'ICMP' | 'IPENCAP';
                }>;
                /**
                 * The default behavior for outbound traffic. This setting can be overridden by [updating](https://techdocs.akamai.com/linode-api/reference/put-firewall-rules) the `outbound.action` property of the Firewall Rule.
                 */
                outbound_policy: 'ACCEPT' | 'DROP';
                /**
                 * __Read-only__ The firewall's rule version. The first version is `1`. The version number is incremented when the firewall's rules change.
                 */
                readonly version: number;
            };
            /**
             * __Read-only__ The status of this Firewall.
             *
             * - When a Firewall is first created its status is `enabled`.
             * - Run the [Update a firewall](https://techdocs.akamai.com/linode-api/reference/put-firewall) operation to set a Firewall's status to `enabled` or `disabled`.
             * - Run the [Delete a firewall](https://techdocs.akamai.com/linode-api/reference/delete-firewall) operation to delete a Firewall.
             */
            status: 'enabled' | 'disabled' | 'deleted';
            /**
             * __Filterable__ An array of tags applied to this object. Tags are for organizational purposes only.
             */
            tags: Array<string>;
            /**
             * __Filterable__, __Read-only__ When this Firewall was last updated.
             */
            readonly updated: string;
        }>;
    };
};

export type PutNodeBalancerFirewallsResponse = PutNodeBalancerFirewallsResponses[keyof PutNodeBalancerFirewallsResponses];

export type GetNodeBalancerStatsData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/stats';
};

export type GetNodeBalancerStatsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerStatsError = GetNodeBalancerStatsErrors[keyof GetNodeBalancerStatsErrors];

export type GetNodeBalancerStatsResponses = {
    /**
     * Stats for this NodeBalancer.
     */
    200: {
        /**
         * The data returned about this NodeBalancers.
         */
        data: {
            /**
             * An array of key/value pairs representing unix timestamp and reading for connections to this NodeBalancer.
             */
            connections: Array<number>;
            /**
             * Traffic statistics for this NodeBalancer.
             */
            traffic: {
                /**
                 * An array of key/value pairs representing unix timestamp and reading for inbound traffic.
                 */
                in: Array<number>;
                /**
                 * An array of key/value pairs representing unix timestamp and reading for outbound traffic.
                 */
                out: Array<number>;
            };
        };
        /**
         * The title for the statistics generated in this response.
         */
        title: string;
    };
};

export type GetNodeBalancerStatsResponse = GetNodeBalancerStatsResponses[keyof GetNodeBalancerStatsResponses];

export type GetNodeBalancerVpcsData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/nodebalancers/{nodeBalancerId}/vpcs';
};

export type GetNodeBalancerVpcsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerVpcsError = GetNodeBalancerVpcsErrors[keyof GetNodeBalancerVpcsErrors];

export type GetNodeBalancerVpcsResponses = {
    /**
     * A paginated list of NodeBalancer VPC configurations.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ Identifies the VPC configuration for this NodeBalancer.
             */
            readonly id?: number;
            /**
             * A CIDR range for the VPC's IPv4 addresses. The NodeBalancer sources IP addresses from this range when routing traffic to the backend VPC nodes.
             */
            ipv4_range?: string;
            /**
             * Enables the use of a larger `ipv4_range` subnet for multiple NodeBalancers within the same VPC by allocating smaller `/30` subnets for each NodeBalancer's backends.
             *
             * - When set to `true`, the system automatically allocates the smallest possible subnet (`/30`) from the provided `ipv4_range` for the NodeBalancer's backend interface. If the specified range doesn't have enough available IPs to allocate a `/30` subnet, the creation fails.
             *
             * - When set to `false`, the NodeBalancer is created using the entire `ipv4_range` as specified, without attempting to allocate a `/30` subnet.
             */
            ipv4_range_auto_assign?: boolean | null;
            /**
             * __Read-only__ Identifies the NodeBalancer.
             */
            readonly nodebalancer_id?: number;
            /**
             * The VPC's subnet. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation provides data for your VPCs and their subnets.
             */
            subnet_id: number;
            /**
             * __Read-only__ The `id` of the VPC configured for this NodeBalancer.
             */
            readonly vpc_id?: number | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetNodeBalancerVpcsResponse = GetNodeBalancerVpcsResponses[keyof GetNodeBalancerVpcsResponses];

export type GetNodeBalancerVpcConfigData = {
    body?: never;
    path: {
        /**
         * The ID of the NodeBalancer.
         */
        nodeBalancerId: number;
        /**
         * The VPC configuration `id` for the NodeBalancer.
         */
        nodeBalancerVpcConfigId: number;
    };
    query?: never;
    url: '/nodebalancers/{nodeBalancerId}/vpcs/{nodeBalancerVpcConfigId}';
};

export type GetNodeBalancerVpcConfigErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetNodeBalancerVpcConfigError = GetNodeBalancerVpcConfigErrors[keyof GetNodeBalancerVpcConfigErrors];

export type GetNodeBalancerVpcConfigResponses = {
    /**
     * You can have only one VPC in a NodeBalancer configuration. If any of your backend nodes are VPC Linodes, specify the VPC subnet and CIDR range. NodeBalancer routes traffic to backend VPC nodes through this subnet. Once the NodeBalancer is created, its VPC cannot be changed.
     */
    200: {
        /**
         * __Read-only__ Identifies the VPC configuration for this NodeBalancer.
         */
        readonly id?: number;
        /**
         * A CIDR range for the VPC's IPv4 addresses. The NodeBalancer sources IP addresses from this range when routing traffic to the backend VPC nodes.
         */
        ipv4_range?: string;
        /**
         * Enables the use of a larger `ipv4_range` subnet for multiple NodeBalancers within the same VPC by allocating smaller `/30` subnets for each NodeBalancer's backends.
         *
         * - When set to `true`, the system automatically allocates the smallest possible subnet (`/30`) from the provided `ipv4_range` for the NodeBalancer's backend interface. If the specified range doesn't have enough available IPs to allocate a `/30` subnet, the creation fails.
         *
         * - When set to `false`, the NodeBalancer is created using the entire `ipv4_range` as specified, without attempting to allocate a `/30` subnet.
         */
        ipv4_range_auto_assign?: boolean | null;
        /**
         * __Read-only__ Identifies the NodeBalancer.
         */
        readonly nodebalancer_id?: number;
        /**
         * The VPC's subnet. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation provides data for your VPCs and their subnets.
         */
        subnet_id: number;
        /**
         * __Read-only__ The `id` of the VPC configured for this NodeBalancer.
         */
        readonly vpc_id?: number | null;
    };
};

export type GetNodeBalancerVpcConfigResponse = GetNodeBalancerVpcConfigResponses[keyof GetNodeBalancerVpcConfigResponses];

export type GetObjectStorageBucketsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/buckets';
};

export type GetObjectStorageBucketsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageBucketsError = GetObjectStorageBucketsErrors[keyof GetObjectStorageBucketsErrors];

export type GetObjectStorageBucketsResponses = {
    /**
     * A paginated list of buckets you own.
     */
    200: {
        data: Array<{
            /**
             * __Deprecated__ The legacy `clusterId` equivalent for the `regionId` where this bucket lives. The API maintains this for backward compatibility.
             *
             * > 📘
             * >
             * > - This value and the `regionId` are interchangeable when used in requests. Best practice is to use the `regionId`.
             * >
             * > - This value is empty for newer regions that don't have a legacy `clusterId`.
             *
             * @deprecated
             */
            cluster: string;
            /**
             * When this bucket was created.
             */
            created: string;
            /**
             * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * The hostname where this bucket can be accessed. This hostname can be accessed through a browser if the bucket is made public.
             */
            hostname: string;
            /**
             * The name of this bucket.
             */
            label: string;
            /**
             * The number of objects stored in this bucket.
             */
            objects: number;
            /**
             * The `id` of the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where this Object Storage bucket lives.
             */
            region: string;
            /**
             * The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`.
             */
            s3_endpoint: string;
            /**
             * The size of the bucket in bytes.
             */
            size: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageBucketsResponse = GetObjectStorageBucketsResponses[keyof GetObjectStorageBucketsResponses];

export type PostObjectStorageBucketData = {
    /**
     * Information about the bucket you want to create.
     */
    body?: {
        /**
         * The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).
         */
        acl?: 'private' | 'public-read' | 'authenticated-read' | 'public-read-write';
        /**
         * If set to `false`, cross-origin resource sharing (CORS) is disabled for all origins in the bucket.
         */
        cors_enabled?: boolean;
        /**
         * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
         */
        endpoint_type?: 'E0' | 'E1' | 'E2' | 'E3';
        /**
         * The name for this bucket.
         *
         * * A bucket name can contain from 3 to 63 alphanumeric characters, dashes (`-`), or dots (`.`).
         * * A bucket name can't end in a dash and you can't use two consecutive dashes.
         * * A bucket name can't start or end in a dot, and you can't use two consecutive dots. As a best practice, only use dots if a certificate you're using with your bucket requires it. (For example, if you're using a custom TLS certificate.)
         * * A bucket name needs to be unique in the `region` where you're creating the bucket. The API only reserves labels for the `region` where active buckets are created and stored. If you want to reserve this bucket's label in another `region`, create a new bucket with the same label in the new `region`.
         */
        label: string;
        /**
         * The `id` assigned to the data center ([region](https://techdocs.akamai.com/linode-api/reference/get-regions)) where this Object Storage bucket should be created.
         *
         * > 📘
         * >
         * > This supports legacy `clusterId` values that represented a specific region. For example, `us-east-1` is the legacy reference for the `us-east` region.
         */
        region?: string;
        /**
         * The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`.
         */
        s3_endpoint?: string;
    };
    path?: never;
    query?: never;
    url: '/object-storage/buckets';
};

export type PostObjectStorageBucketErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostObjectStorageBucketError = PostObjectStorageBucketErrors[keyof PostObjectStorageBucketErrors];

export type PostObjectStorageBucketResponses = {
    /**
     * An Object Storage bucket.
     */
    200: {
        /**
         * __Deprecated__ The legacy `clusterId` equivalent for the `regionId` where this bucket lives. The API maintains this for backward compatibility.
         *
         * > 📘
         * >
         * > - This value and the `regionId` are interchangeable when used in requests. Best practice is to use the `regionId`.
         * >
         * > - This value is empty for newer regions that don't have a legacy `clusterId`.
         *
         * @deprecated
         */
        cluster: string;
        /**
         * When this bucket was created.
         */
        created: string;
        /**
         * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
         */
        endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
        /**
         * The hostname where this bucket can be accessed. This hostname can be accessed through a browser if the bucket is made public.
         */
        hostname: string;
        /**
         * The name of this bucket.
         */
        label: string;
        /**
         * The number of objects stored in this bucket.
         */
        objects: number;
        /**
         * The `id` of the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where this Object Storage bucket lives.
         */
        region: string;
        /**
         * The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`.
         */
        s3_endpoint: string;
        /**
         * The size of the bucket in bytes.
         */
        size: number;
    };
};

export type PostObjectStorageBucketResponse = PostObjectStorageBucketResponses[keyof PostObjectStorageBucketResponses];

export type GetObjectStorageBucketinClusterData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}';
};

export type GetObjectStorageBucketinClusterErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageBucketinClusterError = GetObjectStorageBucketinClusterErrors[keyof GetObjectStorageBucketinClusterErrors];

export type GetObjectStorageBucketinClusterResponses = {
    /**
     * A paginated list of buckets on your account in this region.
     */
    200: {
        data: Array<{
            /**
             * __Deprecated__ The legacy `clusterId` equivalent for the `regionId` where this bucket lives. The API maintains this for backward compatibility.
             *
             * > 📘
             * >
             * > - This value and the `regionId` are interchangeable when used in requests. Best practice is to use the `regionId`.
             * >
             * > - This value is empty for newer regions that don't have a legacy `clusterId`.
             *
             * @deprecated
             */
            cluster: string;
            /**
             * When this bucket was created.
             */
            created: string;
            /**
             * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * The hostname where this bucket can be accessed. This hostname can be accessed through a browser if the bucket is made public.
             */
            hostname: string;
            /**
             * The name of this bucket.
             */
            label: string;
            /**
             * The number of objects stored in this bucket.
             */
            objects: number;
            /**
             * The `id` of the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where this Object Storage bucket lives.
             */
            region: string;
            /**
             * The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`.
             */
            s3_endpoint: string;
            /**
             * The size of the bucket in bytes.
             */
            size: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageBucketinClusterResponse = GetObjectStorageBucketinClusterResponses[keyof GetObjectStorageBucketinClusterResponses];

export type DeleteObjectStorageBucketData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}';
};

export type DeleteObjectStorageBucketErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteObjectStorageBucketError = DeleteObjectStorageBucketErrors[keyof DeleteObjectStorageBucketErrors];

export type DeleteObjectStorageBucketResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteObjectStorageBucketResponse = DeleteObjectStorageBucketResponses[keyof DeleteObjectStorageBucketResponses];

export type GetObjectStorageBucketData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}';
};

export type GetObjectStorageBucketErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageBucketError = GetObjectStorageBucketErrors[keyof GetObjectStorageBucketErrors];

export type GetObjectStorageBucketResponses = {
    /**
     * An Object Storage bucket.
     */
    200: {
        /**
         * __Deprecated__ The legacy `clusterId` equivalent for the `regionId` where this bucket lives. The API maintains this for backward compatibility.
         *
         * > 📘
         * >
         * > - This value and the `regionId` are interchangeable when used in requests. Best practice is to use the `regionId`.
         * >
         * > - This value is empty for newer regions that don't have a legacy `clusterId`.
         *
         * @deprecated
         */
        cluster: string;
        /**
         * When this bucket was created.
         */
        created: string;
        /**
         * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
         */
        endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
        /**
         * The hostname where this bucket can be accessed. This hostname can be accessed through a browser if the bucket is made public.
         */
        hostname: string;
        /**
         * The name of this bucket.
         */
        label: string;
        /**
         * The number of objects stored in this bucket.
         */
        objects: number;
        /**
         * The `id` of the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where this Object Storage bucket lives.
         */
        region: string;
        /**
         * The active user's S3-compatible endpoint URL, based on the `endpoint_type` and `region`.
         */
        s3_endpoint: string;
        /**
         * The size of the bucket in bytes.
         */
        size: number;
    };
};

export type GetObjectStorageBucketResponse = GetObjectStorageBucketResponses[keyof GetObjectStorageBucketResponses];

export type GetObjectStorageBucketAccessData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/access';
};

export type GetObjectStorageBucketAccessErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageBucketAccessError = GetObjectStorageBucketAccessErrors[keyof GetObjectStorageBucketAccessErrors];

export type GetObjectStorageBucketAccessResponses = {
    /**
     * Access settings for the specific bucket.
     */
    200: {
        /**
         * The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).
         */
        acl: 'private' | 'public-read' | 'authenticated-read' | 'public-read-write' | 'custom';
        /**
         * The full XML of the object's ACL policy.
         */
        acl_xml: string;
        /**
         * If `true`, cross-origin resource sharing (CORS) is enabled for all origins in the bucket. Returned as `null` for `E2` and `E3` [endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) because CORS is not supported.
         */
        cors_enabled: boolean | null;
        /**
         * The full XML of the bucket's CORS policy. Returned as an empty object if `cors_enabled` is `false`, and `null` for `E2` and `E3` [endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types). CORS is not supported with these endpoint types.
         */
        cors_xml: string | null;
    };
};

export type GetObjectStorageBucketAccessResponse = GetObjectStorageBucketAccessResponses[keyof GetObjectStorageBucketAccessResponses];

export type PostObjectStorageBucketAccessData = {
    body?: {
        /**
         * The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).
         */
        acl: 'private' | 'public-read' | 'authenticated-read' | 'public-read-write' | 'custom';
        /**
         * If `true`, cross-origin resource sharing (CORS) is enabled for all origins in the bucket.
         */
        cors_enabled: boolean;
    };
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/access';
};

export type PostObjectStorageBucketAccessErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostObjectStorageBucketAccessError = PostObjectStorageBucketAccessErrors[keyof PostObjectStorageBucketAccessErrors];

export type PostObjectStorageBucketAccessResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostObjectStorageBucketAccessResponse = PostObjectStorageBucketAccessResponses[keyof PostObjectStorageBucketAccessResponses];

export type PutStorageBucketAccessData = {
    body?: {
        /**
         * The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).
         */
        acl: 'private' | 'public-read' | 'authenticated-read' | 'public-read-write' | 'custom';
        /**
         * If `true`, the API enables cross-origin resource sharing for all origins on the bucket.
         */
        cors_enabled: boolean;
    };
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/access';
};

export type PutStorageBucketAccessErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutStorageBucketAccessError = PutStorageBucketAccessErrors[keyof PutStorageBucketAccessErrors];

export type PutStorageBucketAccessResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PutStorageBucketAccessResponse = PutStorageBucketAccessResponses[keyof PutStorageBucketAccessResponses];

export type GetObjectStorageBucketAclData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query: {
        /**
         * The name of a specific object to get its access control list (ACL) details. Run the [List Object Storage bucket contents](https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket-content) operation to access all object names in a bucket.
         */
        name: string;
    };
    url: '/object-storage/buckets/{regionId}/{bucket}/object-acl';
};

export type GetObjectStorageBucketAclErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageBucketAclError = GetObjectStorageBucketAclErrors[keyof GetObjectStorageBucketAclErrors];

export type GetObjectStorageBucketAclResponses = {
    /**
     * The Object's canned ACL and policy.
     */
    200: {
        /**
         * The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).
         */
        acl: 'private' | 'public-read' | 'authenticated-read' | 'public-read-write' | 'custom';
        /**
         * The full XML of the object's ACL policy.
         */
        acl_xml: string;
    };
};

export type GetObjectStorageBucketAclResponse = GetObjectStorageBucketAclResponses[keyof GetObjectStorageBucketAclResponses];

export type PutObjectStorageBucketAclData = {
    body?: {
        /**
         * The S3 predefined collection of grantees and permissions set for the bucket, also referred to as a [Canned ACL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/acl-overview.html#canned-acl).
         */
        acl: 'private' | 'public-read' | 'authenticated-read' | 'public-read-write' | 'custom';
        /**
         * The name of the object where access control list (ACL) settings will be updated. Run the [List Object Storage bucket contents](https://techdocs.akamai.com/linode-api/reference/get-object-storage-bucket-content) operation to access all object names in a bucket.
         */
        name: string;
    };
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/object-acl';
};

export type PutObjectStorageBucketAclErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutObjectStorageBucketAclError = PutObjectStorageBucketAclErrors[keyof PutObjectStorageBucketAclErrors];

export type PutObjectStorageBucketAclResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PutObjectStorageBucketAclResponse = PutObjectStorageBucketAclResponses[keyof PutObjectStorageBucketAclResponses];

export type GetObjectStorageBucketContentData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: {
        /**
         * The "marker" for this request, which can be used to paginate through large buckets. Its value should be the value of the `next_marker` property returned with the last page. Listing bucket contents _does not_ support arbitrary page access. See the `next_marker` property in the responses section for more details.
         */
        marker?: string;
        /**
         * The delimiter for object names; if given, object names will be returned up to the first occurrence of this character. This is most commonly used with the `/` character to allow bucket transversal in a manner similar to a filesystem, however any delimiter may be used. Use in conjunction with `prefix` to see object names past the first occurrence of the delimiter.
         */
        delimiter?: string;
        /**
         * Filters objects returned to only those whose name start with the given prefix. Commonly used in conjunction with `delimiter` to allow transversal of bucket contents in a manner similar to a filesystem.
         */
        prefix?: string;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/object-storage/buckets/{regionId}/{bucket}/object-list';
};

export type GetObjectStorageBucketContentErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageBucketContentError = GetObjectStorageBucketContentErrors[keyof GetObjectStorageBucketContentErrors];

export type GetObjectStorageBucketContentResponses = {
    /**
     * One page of the requested bucket's contents.
     */
    200: {
        data: Array<{
            /**
             * An MD-5 hash of the object. `null` if this object represents a prefix.
             */
            etag: string;
            /**
             * The date and time this object was last modified. `null` if this object represents a prefix.
             */
            last_modified: string;
            /**
             * The name of this object or prefix.
             */
            name: string;
            /**
             * The owner of this object, as a UUID. `null` if this object represents a prefix.
             */
            owner: string;
            /**
             * The size of this object, in bytes. `null` if this object represents a prefix.
             */
            size: number;
        }>;
        /**
         * Designates if there is another page of bucket objects.
         */
        is_truncated: boolean;
        /**
         * Returns the value you should pass to the `marker` query parameter to get the next page of objects. If there is no next page, `null` will be returned.
         */
        next_marker: string | null;
    };
};

export type GetObjectStorageBucketContentResponse = GetObjectStorageBucketContentResponses[keyof GetObjectStorageBucketContentResponses];

export type PostObjectStorageObjectUrlData = {
    /**
     * Information about the request to sign.
     */
    body?: {
        /**
         * The expected `Content-type` header of the request this signed URL will be valid for.  If provided, the `Content-type` header _must_ be sent with the request when this URL is used, and _must_ be the same as it was when the signed URL was created. Required for all methods _except_ `GET` or `DELETE`.
         */
        content_type?: string | null;
        /**
         * How long this signed URL will be valid for, in seconds.  If omitted, the URL will be valid for 3600 seconds (1 hour).
         */
        expires_in?: number | null;
        /**
         * The HTTP method allowed to be used with the pre-signed URL.
         */
        method: string;
        /**
         * The name of the object that will be accessed with the pre-signed URL. This object need not exist, and no error will be returned if it doesn't. This behavior is useful for generating pre-signed URLs to upload new objects to by setting the `method` to `PUT`.
         */
        name: string;
    };
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/object-url';
};

export type PostObjectStorageObjectUrlErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostObjectStorageObjectUrlError = PostObjectStorageObjectUrlErrors[keyof PostObjectStorageObjectUrlErrors];

export type PostObjectStorageObjectUrlResponses = {
    /**
     * The URL with which to access your object.
     */
    200: {
        /**
         * The signed URL to perform the request at.
         */
        url: string;
    };
};

export type PostObjectStorageObjectUrlResponse = PostObjectStorageObjectUrlResponses[keyof PostObjectStorageObjectUrlResponses];

export type DeleteObjectStorageSslData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/ssl';
};

export type DeleteObjectStorageSslErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteObjectStorageSslError = DeleteObjectStorageSslErrors[keyof DeleteObjectStorageSslErrors];

export type DeleteObjectStorageSslResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteObjectStorageSslResponse = DeleteObjectStorageSslResponses[keyof DeleteObjectStorageSslResponses];

export type GetObjectStorageSslData = {
    body?: never;
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/ssl';
};

export type GetObjectStorageSslErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageSslError = GetObjectStorageSslErrors[keyof GetObjectStorageSslErrors];

export type GetObjectStorageSslResponses = {
    /**
     * If this Object Storage bucket has a corresponding TLS/SSL Certificate.
     */
    200: {
        /**
         * __Read-only__ A boolean indicating if this Bucket has a corresponding TLS/SSL certificate that was uploaded by an Account user.
         */
        readonly ssl: boolean;
    };
};

export type GetObjectStorageSslResponse = GetObjectStorageSslResponses[keyof GetObjectStorageSslResponses];

export type PostObjectStorageSslData = {
    /**
     * Upload a TLS/SSL certificate and private key to be served when you visit your Object Storage bucket via HTTPS.
     */
    body?: {
        /**
         * Your Base64 encoded and PEM formatted SSL certificate.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI)
         */
        certificate: string;
        /**
         * The private key associated with this TLS/SSL certificate.
         *
         * Line breaks must be represented as `\n` in the string for requests (but not when using the Linode CLI)
         */
        private_key: string;
    };
    path: {
        /**
         * Identifies a region where this bucket lives.
         *
         * > 📘
         * >
         * > You can use a `clusterId` in place of `regionId` in requests for buckets that you created using the legacy version of the API. Run [List clusters](https://techdocs.akamai.com/linode-api/reference/get-object-storage-clusters) to see each cluster `id`.
         */
        regionId: string;
        /**
         * The bucket name.
         */
        bucket: string;
    };
    query?: never;
    url: '/object-storage/buckets/{regionId}/{bucket}/ssl';
};

export type PostObjectStorageSslErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostObjectStorageSslError = PostObjectStorageSslErrors[keyof PostObjectStorageSslErrors];

export type PostObjectStorageSslResponses = {
    /**
     * If this Object Storage bucket has a corresponding TLS/SSL Certificate.
     */
    200: {
        /**
         * __Read-only__ A boolean indicating if this Bucket has a corresponding TLS/SSL certificate that was uploaded by an Account user.
         */
        readonly ssl: boolean;
    };
};

export type PostObjectStorageSslResponse = PostObjectStorageSslResponses[keyof PostObjectStorageSslResponses];

export type PostCancelObjectStorageData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/cancel';
};

export type PostCancelObjectStorageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCancelObjectStorageError = PostCancelObjectStorageErrors[keyof PostCancelObjectStorageErrors];

export type PostCancelObjectStorageResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostCancelObjectStorageResponse = PostCancelObjectStorageResponses[keyof PostCancelObjectStorageResponses];

export type GetObjectStorageClustersData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/clusters';
};

export type GetObjectStorageClustersErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageClustersError = GetObjectStorageClustersErrors[keyof GetObjectStorageClustersErrors];

export type GetObjectStorageClustersResponses = {
    /**
     * A paginated list of available clusters.
     */
    200: {
        data: Array<{
            /**
             * The base URL for this cluster, used for connecting with third-party clients.
             */
            domain: string;
            /**
             * The unique ID for this cluster.
             */
            id: string;
            /**
             * The region where this cluster is located.
             */
            region: string;
            /**
             * The base URL for this cluster used when hosting static sites.
             */
            static_site_domain: string;
            /**
             * This cluster's status.
             */
            status: 'available' | 'unavailable';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageClustersResponse = GetObjectStorageClustersResponses[keyof GetObjectStorageClustersResponses];

export type GetObjectStorageClusterData = {
    body?: never;
    path: {
        /**
         * Identifies a cluster where this bucket lives. For backward compatibility with Object Storage in this API.
         *
         * > 📘
         * >
         * > You can use the applicable `regionId`, for example `us-west`, in place of the `clusterId`, for example, `us-west-1`. Run [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) to see all regions.
         */
        clusterId: string;
    };
    query?: never;
    url: '/object-storage/clusters/{clusterId}';
};

export type GetObjectStorageClusterErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageClusterError = GetObjectStorageClusterErrors[keyof GetObjectStorageClusterErrors];

export type GetObjectStorageClusterResponses = {
    /**
     * An Object Storage Cluster.
     */
    200: {
        /**
         * The base URL for this cluster, used for connecting with third-party clients.
         */
        domain: string;
        /**
         * The unique ID for this cluster.
         */
        id: string;
        /**
         * The region where this cluster is located.
         */
        region: string;
        /**
         * The base URL for this cluster used when hosting static sites.
         */
        static_site_domain: string;
        /**
         * This cluster's status.
         */
        status: 'available' | 'unavailable';
    };
};

export type GetObjectStorageClusterResponse = GetObjectStorageClusterResponses[keyof GetObjectStorageClusterResponses];

export type GetObjectStorageEndpointsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/endpoints';
};

export type GetObjectStorageEndpointsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageEndpointsError = GetObjectStorageEndpointsErrors[keyof GetObjectStorageEndpointsErrors];

export type GetObjectStorageEndpointsResponses = {
    /**
     * A paginated list of endpoints you can access.
     */
    200: {
        data: Array<{
            /**
             * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * The Akamai cloud computing region, represented by its slug value. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view all regions and their slugs.
             */
            region: string;
            /**
             * Your S3-compatible endpoint URL, based on the `endpoint_type` and `region`. Displayed as `null` if you haven't assigned an endpoint for your user.
             */
            s3_endpoint: string | null;
        }>;
    } & {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageEndpointsResponse = GetObjectStorageEndpointsResponses[keyof GetObjectStorageEndpointsResponses];

export type GetObjectStorageKeysData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/keys';
};

export type GetObjectStorageKeysErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageKeysError = GetObjectStorageKeysErrors[keyof GetObjectStorageKeysErrors];

export type GetObjectStorageKeysResponses = {
    /**
     * A paginated list of Object Storage Keys.
     */
    200: {
        data: Array<{
            /**
             * A unique string chosen by the API to identify this key. Used as a username to identify this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
             */
            access_key: string;
            /**
             * Settings that limit access to specific buckets, each with a specific permission level. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
             */
            bucket_access: Array<{
                /**
                 * The name of the bucket the key can access in the `region`.
                 */
                bucket_name: string;
                /**
                 * __Deprecated__ For backwards compatibility, this is included to identify the legacy cluster equivalent of the `region` where this key can be used. The key grants access to each specified `bucket_name`, based on the `permissions` set.
                 *
                 * > 📘
                 * >
                 * > Use of clusters in a limited access key has been deprecated.
                 */
                cluster: string;
                /**
                 * The level of access the key grants to the `bucket_name`. Keys with `read_write` access can manage content in the `bucket_name`, while `read_only` can be used to view content.
                 */
                permissions: 'read_write' | 'read_only';
                /**
                 * The region where the `bucket_name` resides.
                 */
                region: string;
            }>;
            /**
             * This Object Storage key's unique ID.
             */
            id: number;
            /**
             * The label given to this key. For display purposes only.
             */
            label: string;
            /**
             * Whether this Object Storage key limits access to specific buckets and permissions. Returns `false` if this key grants full access. Specific limitations are set in `bucket_access`.
             */
            limited: boolean;
            /**
             * The key can be used in these regions to manage buckets.
             */
            regions: Array<{
                /**
                 * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
                 */
                endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
                /**
                 * Identifies each region where you can use the Object Storage key.
                 *
                 * - **For an unlimited key**. Each `id` represents an individual region you set in the `regions` array, when you set up the key. See [Create an unlimited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
                 *
                 * - **For a limited access key**. The API server populates each object in this array with each of the individual instances of the `region` parameter you set in the `bucket_access` array. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
                 */
                id: string;
                /**
                 * The S3-compatible hostname you can use to access the Object Storage buckets in this region.
                 */
                s3_endpoint: string;
            }>;
            /**
             * This Object Storage key's secret key. Used as a password to validate this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
             *
             * > 📘
             * >
             * > This value is listed as `[REDACTED]` for this operation, to protect it. It's only revealed in a response after [creating](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) a key.
             */
            secret_key: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageKeysResponse = GetObjectStorageKeysResponses[keyof GetObjectStorageKeysResponses];

export type PostObjectStorageKeysData = {
    /**
     * The settings necessary to create a new Object Storage access key.
     */
    body?: {
        /**
         * Settings that limit access to specific buckets, each with a specific permission level. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
         */
        bucket_access: Array<{
            /**
             * The name of the bucket the key can access in the `region`.
             */
            bucket_name: string;
            /**
             * The level of access the key grants to the `bucket_name`. Keys with `read_write` access can manage content in the `bucket_name`, while `read_only` can be used to view content.
             */
            permissions: 'read_write' | 'read_only';
            /**
             * The region where the Object Storage `bucket_name` resides.
             */
            region: string;
        } | {
            /**
             * The name of the bucket the key can access in the `region`.
             */
            bucket_name: string;
            /**
             * __Deprecated__ For backwards compatibility, this is included to identify the legacy cluster equivalent of the `region` where this key can be used. The key grants access to each specified `bucket_name`, based on the `permissions` set.
             *
             * > 📘
             * >
             * > Use of clusters in a limited access key has been deprecated. You should use the `region` parameter instead.
             */
            cluster: string;
            /**
             * The level of access the key grants to the `bucket_name`. Keys with `read_write` access can manage content in the `bucket_name`, while `read_only` can be used to view content.
             */
            permissions: 'read_write' | 'read_only';
        }>;
        /**
         * The name for the key. For display purposes only.
         */
        label: string;
    } | {
        /**
         * The name for the key. For display purposes only.
         */
        label: string;
        /**
         * The key can be used in these regions to manage all buckets. See [Create an unlimited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
         */
        regions: Array<string>;
    };
    path?: never;
    query?: never;
    url: '/object-storage/keys';
};

export type PostObjectStorageKeysErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostObjectStorageKeysError = PostObjectStorageKeysErrors[keyof PostObjectStorageKeysErrors];

export type PostObjectStorageKeysResponses = {
    /**
     * The settings necessary to create a new Object Storage access key.
     */
    200: {
        /**
         * __Read-only__ A unique string chosen by the API to identify this key. Used as a username to identify this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
         */
        readonly access_key: string;
        /**
         * Settings that limit access to specific buckets, each with a specific permission level. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
         */
        bucket_access: Array<{
            /**
             * The name of the bucket the key can access in the `region`.
             */
            bucket_name: string;
            /**
             * __Deprecated__ For backwards compatibility, this is included to identify the legacy cluster equivalent of the `region` where this key can be used. The key grants access to each specified `bucket_name`, based on the `permissions` set. Returned as an empty object for newer regions that don't support the legacy `cluster` parameter.
             *
             * > 📘
             * >
             * > Use of clusters in a limited access key has been deprecated. You should use the `region` parameter instead.
             */
            cluster: string;
            /**
             * The level of access the key grants to the `bucket_name`. Keys with `read_write` access can manage content in the `bucket_name`, while `read_only` can be used to view content.
             */
            permissions: 'read_write' | 'read_only';
            /**
             * The region where the Object Storage `bucket_name` resides.
             */
            region: string;
        }>;
        /**
         * __Read-only__ This Object Storage key's unique ID.
         */
        readonly id: number;
        /**
         * The label given to this key. For display purposes only.
         */
        label: string;
        /**
         * __Read-only__ Whether this Object Storage key limits access to specific buckets and permissions. Returns `false` if this key grants full access. Specific limitations are set in `bucket_access`.
         */
        readonly limited: boolean;
        /**
         * Identifies each region where you can use the Object Storage key.
         */
        regions: Array<{
            /**
             * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * The individual region where this key is valid.
             *
             * - **For an unlimited key**. Each `id` represents an individual region you set in the `regions` array, when you set up the key. See [Create an unlimited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
             *
             * - **For a limited access key**. The API server populates each object in this array with each of the individual instances of the `region` parameter you set in the `bucket_access` array. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
             */
            id: string;
            /**
             * The S3-compatible hostname you can use to access the Object Storage buckets in this region.
             */
            s3_endpoint: string;
        }>;
        /**
         * __Read-only__ This Object Storage key's secret key. Used as a password to validate this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
         *
         * > 🚧
         * >
         * > The `secret_key` is only revealed in the response for this operation. Make sure to store it for later use.
         */
        readonly secret_key: string;
    };
};

export type PostObjectStorageKeysResponse = PostObjectStorageKeysResponses[keyof PostObjectStorageKeysResponses];

export type DeleteObjectStorageKeyData = {
    body?: never;
    path: {
        /**
         * The key to look up.
         */
        keyId: number;
    };
    query?: never;
    url: '/object-storage/keys/{keyId}';
};

export type DeleteObjectStorageKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteObjectStorageKeyError = DeleteObjectStorageKeyErrors[keyof DeleteObjectStorageKeyErrors];

export type DeleteObjectStorageKeyResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteObjectStorageKeyResponse = DeleteObjectStorageKeyResponses[keyof DeleteObjectStorageKeyResponses];

export type GetObjectStorageKeyData = {
    body?: never;
    path: {
        /**
         * The key to look up.
         */
        keyId: number;
    };
    query?: never;
    url: '/object-storage/keys/{keyId}';
};

export type GetObjectStorageKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageKeyError = GetObjectStorageKeyErrors[keyof GetObjectStorageKeyErrors];

export type GetObjectStorageKeyResponses = {
    /**
     * A key used to validate requests to an S3 API.
     */
    200: {
        /**
         * A unique string chosen by the API to identify this key. Used as a username to identify this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
         */
        access_key: string;
        /**
         * Settings that limit access to specific buckets, each with a specific permission level. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
         */
        bucket_access: Array<{
            /**
             * The name of the bucket the key can access in the `region`.
             */
            bucket_name: string;
            /**
             * __Deprecated__ For backwards compatibility, this is included to identify the legacy cluster equivalent of the `region` where this key can be used. The key grants access to each specified `bucket_name`, based on the `permissions` set.
             *
             * > 📘
             * >
             * > Use of clusters in a limited access key has been deprecated.
             */
            cluster: string;
            /**
             * The level of access the key grants to the `bucket_name`. Keys with `read_write` access can manage content in the `bucket_name`, while `read_only` can be used to view content.
             */
            permissions: 'read_write' | 'read_only';
            /**
             * The region where the `bucket_name` resides.
             */
            region: string;
        }>;
        /**
         * This Object Storage key's unique ID.
         */
        id: number;
        /**
         * The label given to this key. For display purposes only.
         */
        label: string;
        /**
         * Whether this Object Storage key limits access to specific buckets and permissions. Returns `false` if this key grants full access. Specific limitations are set in `bucket_access`.
         */
        limited: boolean;
        /**
         * The key can be used in these regions to manage buckets.
         */
        regions: Array<{
            /**
             * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * Identifies each region where you can use the Object Storage key.
             *
             * - **For an unlimited key**. Each `id` represents an individual region you set in the `regions` array, when you set up the key. See [Create an unlimited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
             *
             * - **For a limited access key**. The API server populates each object in this array with each of the individual instances of the `region` parameter you set in the `bucket_access` array. See [Create a limited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
             */
            id: string;
            /**
             * The S3-compatible hostname you can use to access the Object Storage buckets in this region.
             */
            s3_endpoint: string;
        }>;
        /**
         * This Object Storage key's secret key. Used as a password to validate this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
         *
         * > 📘
         * >
         * > This value is listed as `[REDACTED]` for this operation, to protect it. It's only revealed in a response after [creating](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) a key.
         */
        secret_key: string;
    };
};

export type GetObjectStorageKeyResponse = GetObjectStorageKeyResponses[keyof GetObjectStorageKeyResponses];

export type PutObjectStorageKeyData = {
    /**
     * The fields to update.
     */
    body?: {
        /**
         * The label for the Object Storage key. Used for display purposes. Omit this to leave the `label` unchanged.
         */
        label: string;
        /**
         * Replace the current list of `regions` set in a specific key. Include an existing region to keep it, or leave it out to remove it. Omit the `regions` array to leave the existing list unchanged. If you include new `regions` in the key, you can't immediately use the key to manage content in buckets in those regions. You need to grant these keys this access using [bucket policies](https://www.linode.com/docs/products/storage/object-storage/guides/bucket-policies/).
         *
         * > 🚧 Limited keys have restrictions
         * >
         * > These points apply to keys you've [created](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) using the `bucket_access` array:
         * >
         * > - You can't remove a `region` from a limited key's original `bucket_access` list. If you include the `regions` array in this operation and target a limited key, you need to include all existing `region` entries from the `bucket_access` array. Otherwise, the operation fails with an error. Run [Get an Object Storage key](https://techdocs.akamai.com/linode-api/reference/get-object-storage-key) to review current `region` entries in a limited key.
         * >
         * > - If you need to remove a specific region from an active limited key, you can [revoke the key](https://techdocs.akamai.com/linode-api/reference/delete-object-storage-key) and create a new Object Storage key, without the specific region.
         */
        regions: Array<string>;
    };
    path: {
        /**
         * The key to look up.
         */
        keyId: number;
    };
    query?: never;
    url: '/object-storage/keys/{keyId}';
};

export type PutObjectStorageKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutObjectStorageKeyError = PutObjectStorageKeyErrors[keyof PutObjectStorageKeyErrors];

export type PutObjectStorageKeyResponses = {
    /**
     * An updated Object Storage key used for authentication.
     */
    200: {
        /**
         * A unique string chosen by the API to identify this key. Used as a username to identify this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
         */
        access_key: string;
        /**
         * This Object Storage key's unique numeric identifier.
         */
        id: number;
        /**
         * The label given to this key. For display purposes only.
         */
        label: string;
        /**
         * Whether this Object Storage key limits access to specific buckets and permissions. Returns `false` if this key grants full access.
         *
         * > 📘
         * >
         * > The `bucket_access` array that contains limited Object Storage key settings doesn't appear in this response. Store this key's `id` from the response and run [Get an Object Storage key](https://techdocs.akamai.com/linode-api/reference/get-object-storage-key) to view these settings.
         */
        limited: boolean;
        /**
         * The key can be used in these regions to list buckets and create new ones, but it can't be used to manage content in the newly created buckets. See [Create an unlimited access key](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) for more information.
         */
        regions: Array<{
            /**
             * The type of `s3_endpoint` available to the active `user` in this `region`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * Identifies each region where you can use the Object Storage key.
             */
            id: string;
            /**
             * The S3-compatible hostname you can use to access your Object Storage buckets in this region.
             */
            s3_endpoint: string;
        }>;
        /**
         * This Object Storage key's secret key. Used as a password to validate this key when making requests to an S3 API, such as the Amazon S3 API or Ceph Object Gateway S3 API.
         *
         * > 📘
         * >
         * > This value is listed as `[REDACTED]` for this operation, to protect it. It's only revealed in a response after [creating](https://techdocs.akamai.com/linode-api/reference/post-object-storage-keys) a key.
         */
        secret_key: string;
    };
};

export type PutObjectStorageKeyResponse = PutObjectStorageKeyResponses[keyof PutObjectStorageKeyResponses];

export type GetObjectStorageQuotasData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/quotas';
};

export type GetObjectStorageQuotasErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageQuotasError = GetObjectStorageQuotasErrors[keyof GetObjectStorageQuotasErrors];

export type GetObjectStorageQuotasResponses = {
    /**
     * A paginated list of Object Storage-related quotas applied to your account.
     */
    200: {
        data: Array<{
            /**
             * A description for the Object Storage-related quota.
             */
            description: string;
            /**
             * The type of `s3_endpoint`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
             */
            endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
            /**
             * Identifies the Object Storage-related quota. Formatted as `obj-<quota_type>-<s3_endpoint>`, where `<quota_type>` is the `resource_metric` in use: `buckets`, `objects` or `bytes`.
             */
            quota_id: string;
            /**
             * The maximum quantity of the `resource_metric` allowed by the quota.
             */
            quota_limit: number;
            /**
             * __Filterable__ The name of the Object Storage-related quota. This is how the quota displays in Akamai Cloud Manager. This can be `Number of Buckets`, `Number of Objects`, or `Total Capacity`.
             */
            quota_name: 'Number of Objects' | 'Number of Buckets' | 'Total Capacity';
            /**
             * The specific Object Storage-based resource for the quota. A quota maximum may apply as follows:
             *
             * - The Object Storage `bucket` quota for a single `s3_endpoint`
             *
             * - The `object` quota for a single `s3_endpoint`
             *
             * - The `byte` count quota for content in a single `s3_endpoint`
             */
            resource_metric: 'bucket' | 'object' | 'byte';
            /**
             * __Filterable__ The URL for the s3 endpoint where the quota applies. Every `s3_endpoint` exists in a specific Akamai Cloud Computing data center (`region`). Run the [List Object Storage endpoints](https://techdocs.akamai.com/linode-api/reference/get-object-storage-endpoints) operation to see more specifics on this `s3_endpoint`.
             */
            s3_endpoint: string;
        }>;
    } & {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageQuotasResponse = GetObjectStorageQuotasResponses[keyof GetObjectStorageQuotasResponses];

export type GetObjectStorageQuotaData = {
    body?: never;
    path: {
        /**
         * The unique string that identifies the specific Object Storage-related quota to look up. This follows the pattern, `obj-<quota_type>-<s3_endpoint>`, for example, `obj-buckets-eu-central-1.linodeobjects.com`.
         */
        objQuotaId: string;
    };
    query?: never;
    url: '/object-storage/quotas/{objQuotaId}';
};

export type GetObjectStorageQuotaErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageQuotaError = GetObjectStorageQuotaErrors[keyof GetObjectStorageQuotaErrors];

export type GetObjectStorageQuotaResponses = {
    /**
     * The current Object Storage-related quotas on your account.
     */
    200: {
        /**
         * A description for the Object Storage-related quota.
         */
        description: string;
        /**
         * The type of `s3_endpoint`. See [Endpoint types](https://techdocs.akamai.com/cloud-computing/docs/object-storage#endpoint-types) for more information.
         */
        endpoint_type: 'E0' | 'E1' | 'E2' | 'E3';
        /**
         * Identifies the Object Storage-related quota. Formatted as `obj-<quota_type>-<s3_endpoint>`, where `<quota_type>` is the `resource_metric` in use: `buckets`, `objects` or `bytes`.
         */
        quota_id: string;
        /**
         * The maximum quantity of the `resource_metric` allowed by the quota.
         */
        quota_limit: number;
        /**
         * __Filterable__ The name of the Object Storage-related quota. This is how the quota displays in Akamai Cloud Manager. This can be `Number of Buckets`, `Number of Objects`, or `Total Capacity`.
         */
        quota_name: 'Number of Objects' | 'Number of Buckets' | 'Total Capacity';
        /**
         * The specific Object Storage-based resource for the quota. A quota maximum may apply as follows:
         *
         * - The Object Storage `bucket` quota for a single `s3_endpoint`
         *
         * - The `object` quota for a single `s3_endpoint`
         *
         * - The `byte` count quota for content in a single `s3_endpoint`
         */
        resource_metric: 'bucket' | 'object' | 'byte';
        /**
         * __Filterable__ The URL for the s3 endpoint where the quota applies. Every `s3_endpoint` exists in a specific Akamai Cloud Computing data center (`region`). Run the [List Object Storage endpoints](https://techdocs.akamai.com/linode-api/reference/get-object-storage-endpoints) operation to see more specifics on this `s3_endpoint`.
         */
        s3_endpoint: string;
    };
};

export type GetObjectStorageQuotaResponse = GetObjectStorageQuotaResponses[keyof GetObjectStorageQuotaResponses];

export type GetObjectStorageQuotaUsageData = {
    body?: never;
    path: {
        /**
         * The unique string that identifies the specific Object Storage-related quota to look up. This follows the pattern, `obj-<quota_type>-<s3_endpoint>`, for example, `obj-buckets-eu-central-1.linodeobjects.com`.
         */
        objQuotaId: string;
    };
    query?: never;
    url: '/object-storage/quotas/{objQuotaId}/usage';
};

export type GetObjectStorageQuotaUsageErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageQuotaUsageError = GetObjectStorageQuotaUsageErrors[keyof GetObjectStorageQuotaUsageErrors];

export type GetObjectStorageQuotaUsageResponses = {
    /**
     * Usage data for a specific Object Storage-related quota on your account.
     */
    200: {
        /**
         * The availability limit for a specific Object Storage resource (`objQuotaId`) for a single endpoint.
         */
        quota_limit: number;
        /**
         * The quantity of the Object Storage resource currently in use on an endpoint. Displayed as `null` if no resources are in use.
         */
        usage: number | null;
    };
};

export type GetObjectStorageQuotaUsageResponse = GetObjectStorageQuotaUsageResponses[keyof GetObjectStorageQuotaUsageResponses];

export type GetObjectStorageTransferData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/transfer';
};

export type GetObjectStorageTransferErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageTransferError = GetObjectStorageTransferErrors[keyof GetObjectStorageTransferErrors];

export type GetObjectStorageTransferResponses = {
    /**
     * Returns the amount of outbound data transfer used by your account's Object Storage buckets.
     */
    200: {
        /**
         * __Read-only__ The amount of outbound data transfer used by your account's Object Storage buckets, in bytes, for the current month's billing cycle.
         */
        readonly used: number;
    };
};

export type GetObjectStorageTransferResponse = GetObjectStorageTransferResponses[keyof GetObjectStorageTransferResponses];

export type GetObjectStorageTypesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/object-storage/types';
};

export type GetObjectStorageTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetObjectStorageTypesError = GetObjectStorageTypesErrors[keyof GetObjectStorageTypesErrors];

export type GetObjectStorageTypesResponses = {
    /**
     * A collection of Object Storage types.
     */
    200: {
        /**
         * The Object Storage types.
         */
        data: Array<{
            /**
             * __Read-only__ The ID representing the Object Storage type.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The Object Storage type label is for display purposes only.
             */
            readonly label: string;
            /**
             * __Read-only__ The default cost of this Object Storage type. Prices are in US dollars, broken down into hourly and monthly charges.
             *
             * Certain regions have different prices from the default. For region-specific prices, see `region_prices`.
             */
            readonly price: {
                /**
                 * __Filterable__ Cost (in US dollars) per hour.
                 */
                hourly: number;
                /**
                 * __Filterable__ Cost per month, in US dollars.
                 */
                monthly: number;
            };
            region_prices: Array<{
                /**
                 * Cost per hour for this region, in US dollars.
                 */
                hourly: number;
                /**
                 * The Region ID for these prices.
                 */
                id: string;
                /**
                 * Cost per month for this region, in US dollars.
                 */
                monthly: number;
            }>;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount, in MB.
             */
            readonly transfer: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetObjectStorageTypesResponse = GetObjectStorageTypesResponses[keyof GetObjectStorageTypesResponses];

export type GetPlacementGroupsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/placement/groups';
};

export type GetPlacementGroupsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPlacementGroupsError = GetPlacementGroupsErrors[keyof GetPlacementGroupsErrors];

export type GetPlacementGroupsResponses = {
    /**
     * Returns an array of all placement groups on your Account.
     */
    200: {
        data: Array<{
            /**
             * The placement group's ID. You need to provide it for all operations that affect it.
             */
            id: number;
            /**
             * Whether all of the compute instances in your placement group are compliant. If `true`, all compute instances meet either the grouped-together or spread-apart model, which you determine through your selected `placement_group_type`. If `false`, a compute instance is out of this compliance. For example, assume you've set `anti-affinity:local` as your `placement_group_type` and your group already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, the placement group is non-compliant. Enforce compliance in your group by setting a `placement_group_policy`.
             *
             * > 📘
             * >
             * > Fixing compliance is not self-service. You need to wait for our assistance to physically move compute instances to make the group compliant again.
             */
            is_compliant: boolean;
            /**
             * __Filterable__ The unique name set for the placement group. A label has these constraints:
             *
             * - It needs to begin and end with an alphanumeric character.
             * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
             */
            label: string;
            /**
             * An array of compute instances included in the placement group.
             */
            members: Array<{
                /**
                 * The compliance status of each individual compute instance in the placement group.
                 */
                is_compliant: boolean;
                /**
                 * __Read-only__ The unique identifier for a compute instance included in the placement group.
                 */
                readonly linode_id: number;
            }>;
            /**
             * Any compute instances that are being migrated to or from the placement group. Returned as `null` if no migrations are taking place.
             */
            migrations: {
                /**
                 * The individual compute instances the system is migrating into the placement group.
                 */
                inbound: Array<{
                    /**
                     * __Read-only__ The unique identifier for a compute instance being migrated into the placement group.
                     */
                    readonly linode_id: number;
                }>;
                /**
                 * The individual compute instances the system is migrating out of the placement group.
                 */
                outbound: Array<{
                    /**
                     * __Read-only__ The unique identifier for a compute instance being migrated out of the placement group.
                     */
                    readonly linode_id: number;
                }>;
            } | null;
            /**
             * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
             *
             * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
             * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
             *
             * <<LB>>
             *
             * > 📘
             * >
             * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
             */
            placement_group_policy: 'strict' | 'flexible';
            /**
             * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
             *
             * > 📘
             * >
             * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
             */
            placement_group_type: 'anti_affinity:local';
            /**
             * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
             */
            readonly region: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetPlacementGroupsResponse = GetPlacementGroupsResponses[keyof GetPlacementGroupsResponses];

export type PostPlacementGroupData = {
    /**
     * Common properties for creating and rebuilding placement groups.
     */
    body: {
        /**
         * A unique name for the placement group. A placement group `label` has the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`), or periods (`.`).
         */
        label: string;
        /**
         * How requests to add future compute instances to your placement group are handled:
         *
         * - `strict`. Don't add a compute instance if it breaks the grouped-together or spread-apart model set by your `placement_group_type`. For example, with `anti-affinity:local` as your `placement_group_type`, assume it already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, an error is thrown and the system won't add it. Ensures the placement group stays compliant with your selected model.
         * - `flexible`. Add a new compute instance, even if it breaks the grouped-together or spread-apart model set by your `placement_group_type`. Breaking the model makes the placement group non-compliant. You need to wait for Akamai to move the offending compute instances to make the group compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
         *
         * > 📘
         * >
         * > Once you create a placement group, you can't change its `placement_group_policy` setting.
         */
        placement_group_policy: 'strict' | 'flexible';
        /**
         * How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
         *
         * > 📘
         * >
         * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
         */
        placement_group_type: 'anti_affinity:local';
        /**
         * The data center that houses the compute instances you want to add to your placement group. Run the [List Linodes](https://techdocs.akamai.com/linode-api/reference/get-linode-instances) operation to view your compute instances and store the `region` identifier.
         */
        region: string;
    };
    path?: never;
    query?: never;
    url: '/placement/groups';
};

export type PostPlacementGroupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostPlacementGroupError = PostPlacementGroupErrors[keyof PostPlacementGroupErrors];

export type PostPlacementGroupResponses = {
    /**
     * A new placement group is being created.
     */
    200: {
        /**
         * The placement group's ID. You need to provide it for all operations that affect it.
         */
        id: number;
        /**
         * Whether all of the compute instances in your placement group are compliant. If `true`, all compute instances meet either the grouped-together or spread-apart model, which you determine through your selected `placement_group_type`. If `false`, a compute instance is out of this compliance. For example, assume you've set `anti-affinity:local` as your `placement_group_type` and your group already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, the placement group is non-compliant. Enforce compliance in your group by setting a `placement_group_policy`.
         *
         * > 📘
         * >
         * > Fixing compliance is not self-service. You need to wait for our assistance to physically move compute instances to make the group compliant again.
         */
        is_compliant: boolean;
        /**
         * __Filterable__ The unique name set for the placement group. A label has these constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         */
        label: string;
        /**
         * An array of compute instances included in the placement group.
         */
        members: Array<{
            /**
             * The compliance status of each individual compute instance in the placement group.
             */
            is_compliant: boolean;
            /**
             * __Read-only__ The unique identifier for a compute instance included in the placement group.
             */
            readonly linode_id: number;
        }>;
        /**
         * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
         *
         * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
         * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
         */
        placement_group_policy: 'strict' | 'flexible';
        /**
         * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
         *
         * > 📘
         * >
         * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
         */
        placement_group_type: 'anti_affinity:local';
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
         */
        readonly region: string;
    };
};

export type PostPlacementGroupResponse = PostPlacementGroupResponses[keyof PostPlacementGroupResponses];

export type DeletePlacementGroupData = {
    body?: never;
    path: {
        /**
         * ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups) operation and store the `id` for the applicable placement group.
         */
        groupId: number;
    };
    query?: never;
    url: '/placement/groups/{groupId}';
};

export type DeletePlacementGroupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeletePlacementGroupError = DeletePlacementGroupErrors[keyof DeletePlacementGroupErrors];

export type DeletePlacementGroupResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeletePlacementGroupResponse = DeletePlacementGroupResponses[keyof DeletePlacementGroupResponses];

export type GetPlacementGroupData = {
    body?: never;
    path: {
        /**
         * ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups) operation and store the `id` for the applicable placement group.
         */
        groupId: number;
    };
    query?: never;
    url: '/placement/groups/{groupId}';
};

export type GetPlacementGroupResponses = {
    /**
     * Returns a single placement group object.
     */
    200: {
        /**
         * The placement group's ID. You need to provide it for all operations that affect it.
         */
        id: number;
        /**
         * Whether all of the compute instances in your placement group are compliant. If `true`, all compute instances meet either the grouped-together or spread-apart model, which you determine through your selected `placement_group_type`. If `false`, a compute instance is out of this compliance. For example, assume you've set `anti-affinity:local` as your `placement_group_type` and your group already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, the placement group is non-compliant. Enforce compliance in your group by setting a `placement_group_policy`.
         *
         * > 📘
         * >
         * > Fixing compliance is not self-service. You need to wait for our assistance to physically move compute instances to make the group compliant again.
         */
        is_compliant: boolean;
        /**
         * __Filterable__ The unique name set for the placement group. A label has these constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         */
        label: string;
        /**
         * An array of compute instances included in the placement group.
         */
        members: Array<{
            /**
             * The compliance status of each individual compute instance in the placement group.
             */
            is_compliant: boolean;
            /**
             * __Read-only__ The unique identifier for a compute instance included in the placement group.
             */
            readonly linode_id: number;
        }>;
        /**
         * Any compute instances that are being migrated to or from the placement group. Returned as `null` if no migrations are taking place.
         */
        migrations: {
            /**
             * The individual compute instances the system is migrating into the placement group.
             */
            inbound: Array<{
                /**
                 * __Read-only__ The unique identifier for a compute instance being migrated into the placement group.
                 */
                readonly linode_id: number;
            }>;
            /**
             * The individual compute instances the system is migrating out of the placement group.
             */
            outbound: Array<{
                /**
                 * __Read-only__ The unique identifier for a compute instance being migrated out of the placement group.
                 */
                readonly linode_id: number;
            }>;
        } | null;
        /**
         * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
         *
         * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
         * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
         */
        placement_group_policy: 'strict' | 'flexible';
        /**
         * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
         *
         * > 📘
         * >
         * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
         */
        placement_group_type: 'anti_affinity:local';
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
         */
        readonly region: string;
    };
};

export type GetPlacementGroupResponse = GetPlacementGroupResponses[keyof GetPlacementGroupResponses];

export type PutPlacementGroupData = {
    body: {
        /**
         * A unique name for the placement group. A placement group `label` has the following constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`), or periods (`.`).
         */
        label: string;
    };
    path: {
        /**
         * ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups) operation and store the `id` for the applicable placement group.
         */
        groupId: number;
    };
    query?: never;
    url: '/placement/groups/{groupId}';
};

export type PutPlacementGroupErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutPlacementGroupError = PutPlacementGroupErrors[keyof PutPlacementGroupErrors];

export type PutPlacementGroupResponses = {
    /**
     * The updated placement group.
     */
    200: {
        /**
         * The placement group's ID. You need to provide it for all operations that affect it.
         */
        id: number;
        /**
         * Whether all of the compute instances in your placement group are compliant. If `true`, all compute instances meet either the grouped-together or spread-apart model, which you determine through your selected `placement_group_type`. If `false`, a compute instance is out of this compliance. For example, assume you've set `anti-affinity:local` as your `placement_group_type` and your group already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, the placement group is non-compliant. Enforce compliance in your group by setting a `placement_group_policy`.
         *
         * > 📘
         * >
         * > Fixing compliance is not self-service. You need to wait for our assistance to physically move compute instances to make the group compliant again.
         */
        is_compliant: boolean;
        /**
         * __Filterable__ The unique name set for the placement group. A label has these constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         */
        label: string;
        /**
         * An array of compute instances included in the placement group.
         */
        members: Array<{
            /**
             * The compliance status of each individual compute instance in the placement group.
             */
            is_compliant: boolean;
            /**
             * __Read-only__ The unique identifier for a compute instance included in the placement group.
             */
            readonly linode_id: number;
        }>;
        /**
         * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
         *
         * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
         * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
         */
        placement_group_policy: 'strict' | 'flexible';
        /**
         * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
         *
         * > 📘
         * >
         * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
         */
        placement_group_type: 'anti_affinity:local';
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
         */
        readonly region: string;
    };
};

export type PutPlacementGroupResponse = PutPlacementGroupResponses[keyof PutPlacementGroupResponses];

export type PostGroupAddLinodeData = {
    /**
     * The compute instances included in a placement group.
     */
    body: {
        /**
         * The `linodeId` values for individual compute instances included in the placement group.
         */
        linodes: Array<number>;
    };
    path: {
        /**
         * ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups) operation and store the `id` for the applicable placement group.
         */
        groupId: number;
    };
    query?: never;
    url: '/placement/groups/{groupId}/assign';
};

export type PostGroupAddLinodeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostGroupAddLinodeError = PostGroupAddLinodeErrors[keyof PostGroupAddLinodeErrors];

export type PostGroupAddLinodeResponses = {
    /**
     * The Linode was added successfully.
     */
    200: {
        /**
         * The placement group's ID. You need to provide it for all operations that affect it.
         */
        id: number;
        /**
         * Whether all of the compute instances in your placement group are compliant. If `true`, all compute instances meet either the grouped-together or spread-apart model, which you determine through your selected `placement_group_type`. If `false`, a compute instance is out of this compliance. For example, assume you've set `anti-affinity:local` as your `placement_group_type` and your group already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, the placement group is non-compliant. Enforce compliance in your group by setting a `placement_group_policy`.
         *
         * > 📘
         * >
         * > Fixing compliance is not self-service. You need to wait for our assistance to physically move compute instances to make the group compliant again.
         */
        is_compliant: boolean;
        /**
         * __Filterable__ The unique name set for the placement group. A label has these constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         */
        label: string;
        /**
         * An array of compute instances included in the placement group.
         */
        members: Array<{
            /**
             * The compliance status of each individual compute instance in the placement group.
             */
            is_compliant: boolean;
            /**
             * __Read-only__ The unique identifier for a compute instance included in the placement group.
             */
            readonly linode_id: number;
        }>;
        /**
         * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
         *
         * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
         * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
         */
        placement_group_policy: 'strict' | 'flexible';
        /**
         * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
         *
         * > 📘
         * >
         * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
         */
        placement_group_type: 'anti_affinity:local';
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
         */
        readonly region: string;
    };
};

export type PostGroupAddLinodeResponse = PostGroupAddLinodeResponses[keyof PostGroupAddLinodeResponses];

export type PostGroupUnassignData = {
    /**
     * The compute instances included in a placement group.
     */
    body: {
        /**
         * The `linodeId` values for individual compute instances included in the placement group.
         */
        linodes: Array<number>;
    };
    path: {
        /**
         * ID of the placement group to look up. Run the [List placement groups](https://techdocs.akamai.com/linode-api/reference/get-placement-groups) operation and store the `id` for the applicable placement group.
         */
        groupId: number;
    };
    query?: never;
    url: '/placement/groups/{groupId}/unassign';
};

export type PostGroupUnassignErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostGroupUnassignError = PostGroupUnassignErrors[keyof PostGroupUnassignErrors];

export type PostGroupUnassignResponses = {
    /**
     * The Linode was removed successfully.
     */
    200: {
        /**
         * The placement group's ID. You need to provide it for all operations that affect it.
         */
        id: number;
        /**
         * Whether all of the compute instances in your placement group are compliant. If `true`, all compute instances meet either the grouped-together or spread-apart model, which you determine through your selected `placement_group_type`. If `false`, a compute instance is out of this compliance. For example, assume you've set `anti-affinity:local` as your `placement_group_type` and your group already has three qualifying compute instances on separate hosts, to support the spread-apart model. If a fourth compute instance is assigned that's on the same host as one of the existing three, the placement group is non-compliant. Enforce compliance in your group by setting a `placement_group_policy`.
         *
         * > 📘
         * >
         * > Fixing compliance is not self-service. You need to wait for our assistance to physically move compute instances to make the group compliant again.
         */
        is_compliant: boolean;
        /**
         * __Filterable__ The unique name set for the placement group. A label has these constraints:
         *
         * - It needs to begin and end with an alphanumeric character.
         * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
         */
        label: string;
        /**
         * An array of compute instances included in the placement group.
         */
        members: Array<{
            /**
             * The compliance status of each individual compute instance in the placement group.
             */
            is_compliant: boolean;
            /**
             * __Read-only__ The unique identifier for a compute instance included in the placement group.
             */
            readonly linode_id: number;
        }>;
        /**
         * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
         *
         * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
         * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
         *
         * <<LB>>
         *
         * > 📘
         * >
         * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
         */
        placement_group_policy: 'strict' | 'flexible';
        /**
         * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
         *
         * > 📘
         * >
         * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
         */
        placement_group_type: 'anti_affinity:local';
        /**
         * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the placement group was deployed.
         */
        readonly region: string;
    };
};

export type PostGroupUnassignResponse = PostGroupUnassignResponses[keyof PostGroupUnassignResponses];

export type GetProfileData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile';
};

export type GetProfileErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetProfileError = GetProfileErrors[keyof GetProfileErrors];

export type GetProfileResponses = {
    /**
     * A profile represents your user in our system. This is where you can change information about your user. This information is available to any OAuth client regardless of requested scopes, and you can use it to populate user information in third-party applications.
     */
    200: {
        /**
         * __Read-only__ This account's Cloud Manager authentication type. You choose an authentication type in Cloud Manager and Akamai authorizes it when you log into your account. Authentication types include your user's password (in conjunction with your username), or the name of your identity provider, such as GitHub. Here are some examples:
         *
         * - If a user has never used third-party authentication, the authentication type will be `password`.
         *
         * - If a user is using third-party authentication, the name of their identity provider is used for the authentication type, for example, `github`.
         *
         * - If a user has used third-party authentication and has since revoked it, the authentication type is `password`.
         */
        authentication_type: 'password' | 'github';
        /**
         * Your user can use these SSH Keys to access Lish. This value is ignored if `lish_auth_method` is `disabled`.
         */
        authorized_keys: Array<string> | null;
        /**
         * Your email address. We use this address for Akamai Cloud Computing-related communication.
         */
        email: string;
        /**
         * When set to `true`, you will receive email notifications about account activity. When `false`, you may still receive business-critical communications through email.
         */
        email_notifications: boolean;
        /**
         * When set to `true`, your user logins are only allowed from whitelisted IPs. This setting is deprecated, and can't be enabled. If you disable this setting, you won't be able to re-enable it.
         *
         * @deprecated
         */
        ip_whitelist_enabled: boolean;
        /**
         * The authentication methods that you can use when connecting to the [Linode Shell (Lish)](https://www.linode.com/docs/guides/lish/).
         *
         * - `keys_only` is the most secure if you intend to use Lish.
         *
         * - `disabled` is recommended if you don't want to use Lish.
         *
         * - If this account's Cloud Manager authentication type is set to a third-party authentication method, you can't use `password_keys` as your Lish authentication method. Run the [Get a profile](https://techdocs.akamai.com/linode-api/reference/get-profile) operation to view your account's Cloud Manager `authentication_type` field.
         */
        lish_auth_method: 'password_keys' | 'keys_only' | 'disabled';
        /**
         * __Read-only__ Information about your status in our referral program. The API makes this information available after this profile's account has established at least $25.00 USD of total payments.
         */
        readonly referrals: {
            /**
             * __Read-only__ Your referral code. If others use this when signing up for Linode, you receive an account credit.
             */
            readonly code: string;
            /**
             * __Read-only__ The number of completed sign-ups that used your referral code.
             */
            readonly completed: number;
            /**
             * __Read-only__ Your referral program account credit in US dollars.
             */
            readonly credit: number;
            /**
             * __Read-only__ The number of pending sign-ups that used your referral code. Akamai gives you credit for these sign-ups once they've completed.
             */
            readonly pending: number;
            /**
             * __Read-only__ The number of users who have signed up with your referral code.
             */
            readonly total: number;
            /**
             * __Read-only__ The referral URL that Akamai uses to direct others to sign up for Akamai Cloud Computing with your referral code.
             */
            readonly url: string;
        };
        /**
         * When set to `true`, there are restrictions on what your user can access on your account. Run [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants) to get details on what entities and actions you can access and perform.
         */
        restricted: boolean;
        /**
         * The time zone you want to display for your Linode assets. This API doesn't directly use this time zone. It's provided for the benefit of clients such as the Akamai Cloud Manager and other clients built on the API. All times returned by the API are in UTC.
         */
        timezone: string;
        /**
         * When set to `true`, a login from an untrusted computer requires two-factor authentication. You also need to run [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) to enable two-factor authentication.
         */
        two_factor_auth: boolean;
        /**
         * __Read-only__ Your unique ID in our system. This value will never change, and can safely be used to identify your user.
         */
        readonly uid: number;
        /**
         * __Read-only__ Your username, used for logging in to our system.
         */
        readonly username: string;
        /**
         * __Read-only__ The phone number verified for this profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation. Displayed as `null` if the profile doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    };
};

export type GetProfileResponse = GetProfileResponses[keyof GetProfileResponses];

export type PutProfileData = {
    /**
     * A profile represents your user in our system. This is where you can change information about your user. This information is available to any OAuth client regardless of requested scopes, and you can use it to populate user information in third-party applications.
     */
    body: {
        /**
         * __Read-only__ This account's Cloud Manager authentication type. You choose an authentication type in Cloud Manager and Akamai authorizes it when you log into your account. Authentication types include your user's password (in conjunction with your username), or the name of your identity provider, such as GitHub. Here are some examples:
         *
         * - If a user has never used third-party authentication, the authentication type will be `password`.
         *
         * - If a user is using third-party authentication, the name of their identity provider is used for the authentication type, for example, `github`.
         *
         * - If a user has used third-party authentication and has since revoked it, the authentication type is `password`.
         */
        authentication_type: 'password' | 'github';
        /**
         * Your user can use these SSH Keys to access Lish. This value is ignored if `lish_auth_method` is `disabled`.
         */
        authorized_keys: Array<string> | null;
        /**
         * Your email address. We use this address for Akamai Cloud Computing-related communication.
         */
        email: string;
        /**
         * When set to `true`, you will receive email notifications about account activity. When `false`, you may still receive business-critical communications through email.
         */
        email_notifications: boolean;
        /**
         * When set to `true`, your user logins are only allowed from whitelisted IPs. This setting is deprecated, and can't be enabled. If you disable this setting, you won't be able to re-enable it.
         *
         * @deprecated
         */
        ip_whitelist_enabled: boolean;
        /**
         * The authentication methods that you can use when connecting to the [Linode Shell (Lish)](https://www.linode.com/docs/guides/lish/).
         *
         * - `keys_only` is the most secure if you intend to use Lish.
         *
         * - `disabled` is recommended if you don't want to use Lish.
         *
         * - If this account's Cloud Manager authentication type is set to a third-party authentication method, you can't use `password_keys` as your Lish authentication method. Run the [Get a profile](https://techdocs.akamai.com/linode-api/reference/get-profile) operation to view your account's Cloud Manager `authentication_type` field.
         */
        lish_auth_method: 'password_keys' | 'keys_only' | 'disabled';
        /**
         * __Read-only__ Information about your status in our referral program. The API makes this information available after this profile's account has established at least $25.00 USD of total payments.
         */
        readonly referrals: {
            /**
             * __Read-only__ Your referral code. If others use this when signing up for Linode, you receive an account credit.
             */
            readonly code: string;
            /**
             * __Read-only__ The number of completed sign-ups that used your referral code.
             */
            readonly completed: number;
            /**
             * __Read-only__ Your referral program account credit in US dollars.
             */
            readonly credit: number;
            /**
             * __Read-only__ The number of pending sign-ups that used your referral code. Akamai gives you credit for these sign-ups once they've completed.
             */
            readonly pending: number;
            /**
             * __Read-only__ The number of users who have signed up with your referral code.
             */
            readonly total: number;
            /**
             * __Read-only__ The referral URL that Akamai uses to direct others to sign up for Akamai Cloud Computing with your referral code.
             */
            readonly url: string;
        };
        /**
         * When set to `true`, there are restrictions on what your user can access on your account. Run [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants) to get details on what entities and actions you can access and perform.
         */
        restricted: boolean;
        /**
         * The time zone you want to display for your Linode assets. This API doesn't directly use this time zone. It's provided for the benefit of clients such as the Akamai Cloud Manager and other clients built on the API. All times returned by the API are in UTC.
         */
        timezone: string;
        /**
         * When set to `true`, a login from an untrusted computer requires two-factor authentication. You also need to run [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) to enable two-factor authentication.
         */
        two_factor_auth: boolean;
        /**
         * __Read-only__ Your unique ID in our system. This value will never change, and can safely be used to identify your user.
         */
        readonly uid: number;
        /**
         * __Read-only__ Your username, used for logging in to our system.
         */
        readonly username: string;
        /**
         * __Read-only__ The phone number verified for this profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation. Displayed as `null` if the profile doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    };
    path?: never;
    query?: never;
    url: '/profile';
};

export type PutProfileErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutProfileError = PutProfileErrors[keyof PutProfileErrors];

export type PutProfileResponses = {
    /**
     * A profile represents your user in our system. This is where you can change information about your user. This information is available to any OAuth client regardless of requested scopes, and you can use it to populate user information in third-party applications.
     */
    200: {
        /**
         * __Read-only__ This account's Cloud Manager authentication type. You choose an authentication type in Cloud Manager and Akamai authorizes it when you log into your account. Authentication types include your user's password (in conjunction with your username), or the name of your identity provider, such as GitHub. Here are some examples:
         *
         * - If a user has never used third-party authentication, the authentication type will be `password`.
         *
         * - If a user is using third-party authentication, the name of their identity provider is used for the authentication type, for example, `github`.
         *
         * - If a user has used third-party authentication and has since revoked it, the authentication type is `password`.
         */
        authentication_type: 'password' | 'github';
        /**
         * Your user can use these SSH Keys to access Lish. This value is ignored if `lish_auth_method` is `disabled`.
         */
        authorized_keys: Array<string> | null;
        /**
         * Your email address. We use this address for Akamai Cloud Computing-related communication.
         */
        email: string;
        /**
         * When set to `true`, you will receive email notifications about account activity. When `false`, you may still receive business-critical communications through email.
         */
        email_notifications: boolean;
        /**
         * When set to `true`, your user logins are only allowed from whitelisted IPs. This setting is deprecated, and can't be enabled. If you disable this setting, you won't be able to re-enable it.
         *
         * @deprecated
         */
        ip_whitelist_enabled: boolean;
        /**
         * The authentication methods that you can use when connecting to the [Linode Shell (Lish)](https://www.linode.com/docs/guides/lish/).
         *
         * - `keys_only` is the most secure if you intend to use Lish.
         *
         * - `disabled` is recommended if you don't want to use Lish.
         *
         * - If this account's Cloud Manager authentication type is set to a third-party authentication method, you can't use `password_keys` as your Lish authentication method. Run the [Get a profile](https://techdocs.akamai.com/linode-api/reference/get-profile) operation to view your account's Cloud Manager `authentication_type` field.
         */
        lish_auth_method: 'password_keys' | 'keys_only' | 'disabled';
        /**
         * __Read-only__ Information about your status in our referral program. The API makes this information available after this profile's account has established at least $25.00 USD of total payments.
         */
        readonly referrals: {
            /**
             * __Read-only__ Your referral code. If others use this when signing up for Linode, you receive an account credit.
             */
            readonly code: string;
            /**
             * __Read-only__ The number of completed sign-ups that used your referral code.
             */
            readonly completed: number;
            /**
             * __Read-only__ Your referral program account credit in US dollars.
             */
            readonly credit: number;
            /**
             * __Read-only__ The number of pending sign-ups that used your referral code. Akamai gives you credit for these sign-ups once they've completed.
             */
            readonly pending: number;
            /**
             * __Read-only__ The number of users who have signed up with your referral code.
             */
            readonly total: number;
            /**
             * __Read-only__ The referral URL that Akamai uses to direct others to sign up for Akamai Cloud Computing with your referral code.
             */
            readonly url: string;
        };
        /**
         * When set to `true`, there are restrictions on what your user can access on your account. Run [List grants](https://techdocs.akamai.com/linode-api/reference/get-profile-grants) to get details on what entities and actions you can access and perform.
         */
        restricted: boolean;
        /**
         * The time zone you want to display for your Linode assets. This API doesn't directly use this time zone. It's provided for the benefit of clients such as the Akamai Cloud Manager and other clients built on the API. All times returned by the API are in UTC.
         */
        timezone: string;
        /**
         * When set to `true`, a login from an untrusted computer requires two-factor authentication. You also need to run [Create a two factor secret](https://techdocs.akamai.com/linode-api/reference/post-tfa-enable) to enable two-factor authentication.
         */
        two_factor_auth: boolean;
        /**
         * __Read-only__ Your unique ID in our system. This value will never change, and can safely be used to identify your user.
         */
        readonly uid: number;
        /**
         * __Read-only__ Your username, used for logging in to our system.
         */
        readonly username: string;
        /**
         * __Read-only__ The phone number verified for this profile with the [Verify a phone number](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number-verify) operation. Displayed as `null` if the profile doesn't have a verified phone number.
         */
        readonly verified_phone_number: string | null;
    };
};

export type PutProfileResponse = PutProfileResponses[keyof PutProfileResponses];

export type GetProfileAppsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/profile/apps';
};

export type GetProfileAppsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetProfileAppsError = GetProfileAppsErrors[keyof GetProfileAppsErrors];

export type GetProfileAppsResponses = {
    /**
     * A paginated list of apps you've authorized.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ When this app was authorized.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ When the app's access to your account expires. If `null`, the app's access must be revoked manually.
             */
            readonly expiry: string | null;
            /**
             * __Read-only__ This authorization's ID, used for revoking access.
             */
            readonly id: number;
            /**
             * __Filterable__, __Read-only__ The name of the application you've authorized.
             */
            readonly label: string;
            /**
             * __Read-only__ The OAuth scopes this app was authorized with.  This defines what parts of your Account the app is allowed to access.
             */
            readonly scopes: string;
            /**
             * __Read-only__ The URL at which this app's thumbnail may be accessed.
             */
            readonly thumbnail_url: string | null;
            /**
             * __Read-only__ The website where you can get more information about this app.
             */
            readonly website: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetProfileAppsResponse = GetProfileAppsResponses[keyof GetProfileAppsResponses];

export type DeleteProfileAppData = {
    body?: never;
    path: {
        /**
         * The authorized app ID to manage.
         */
        appId: number;
    };
    query?: never;
    url: '/profile/apps/{appId}';
};

export type DeleteProfileAppErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteProfileAppError = DeleteProfileAppErrors[keyof DeleteProfileAppErrors];

export type DeleteProfileAppResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteProfileAppResponse = DeleteProfileAppResponses[keyof DeleteProfileAppResponses];

export type GetProfileAppData = {
    body?: never;
    path: {
        /**
         * The authorized app ID to manage.
         */
        appId: number;
    };
    query?: never;
    url: '/profile/apps/{appId}';
};

export type GetProfileAppErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetProfileAppError = GetProfileAppErrors[keyof GetProfileAppErrors];

export type GetProfileAppResponses = {
    /**
     * An application you have authorized access to your Account through OAuth.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ When this app was authorized.
         */
        readonly created: string;
        /**
         * __Filterable__, __Read-only__ When the app's access to your account expires. If `null`, the app's access must be revoked manually.
         */
        readonly expiry: string | null;
        /**
         * __Read-only__ This authorization's ID, used for revoking access.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ The name of the application you've authorized.
         */
        readonly label: string;
        /**
         * __Read-only__ The OAuth scopes this app was authorized with.  This defines what parts of your Account the app is allowed to access.
         */
        readonly scopes: string;
        /**
         * __Read-only__ The URL at which this app's thumbnail may be accessed.
         */
        readonly thumbnail_url: string | null;
        /**
         * __Read-only__ The website where you can get more information about this app.
         */
        readonly website: string;
    };
};

export type GetProfileAppResponse = GetProfileAppResponses[keyof GetProfileAppResponses];

export type GetDevicesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/devices';
};

export type GetDevicesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetDevicesError = GetDevicesErrors[keyof GetDevicesErrors];

export type GetDevicesResponses = {
    /**
     * Returns a paginated list of TrustedDevice objects.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this Remember Me session was started.  This corresponds to the time of login with the "Remember Me" box checked.
             */
            readonly created: string;
            /**
             * __Read-only__ When this TrustedDevice session expires.  Sessions typically last 30 days.
             */
            readonly expiry: string;
            /**
             * __Read-only__ The unique ID for this TrustedDevice.
             */
            readonly id: number;
            /**
             * __Read-only__ The last time this TrustedDevice was successfully used to authenticate to [login.linode.com](https://login.linode.com).
             */
            readonly last_authenticated: string;
            /**
             * __Read-only__ The last IP Address to successfully authenticate with this TrustedDevice.
             */
            readonly last_remote_addr: string;
            /**
             * __Read-only__ The User Agent of the browser that created this TrustedDevice session.
             */
            readonly user_agent: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetDevicesResponse = GetDevicesResponses[keyof GetDevicesResponses];

export type DeleteTrustedDeviceData = {
    body?: never;
    path: {
        /**
         * The ID of the TrustedDevice.
         */
        deviceId: number;
    };
    query?: never;
    url: '/profile/devices/{deviceId}';
};

export type DeleteTrustedDeviceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteTrustedDeviceError = DeleteTrustedDeviceErrors[keyof DeleteTrustedDeviceErrors];

export type DeleteTrustedDeviceResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteTrustedDeviceResponse = DeleteTrustedDeviceResponses[keyof DeleteTrustedDeviceResponses];

export type GetTrustedDeviceData = {
    body?: never;
    path: {
        /**
         * The ID of the TrustedDevice.
         */
        deviceId: number;
    };
    query?: never;
    url: '/profile/devices/{deviceId}';
};

export type GetTrustedDeviceErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTrustedDeviceError = GetTrustedDeviceErrors[keyof GetTrustedDeviceErrors];

export type GetTrustedDeviceResponses = {
    /**
     * A trusted device object represents an active Remember Me session with [login.linode.com](https://login.linode.com).
     */
    200: {
        /**
         * __Read-only__ When this Remember Me session was started.  This corresponds to the time of login with the "Remember Me" box checked.
         */
        readonly created: string;
        /**
         * __Read-only__ When this TrustedDevice session expires.  Sessions typically last 30 days.
         */
        readonly expiry: string;
        /**
         * __Read-only__ The unique ID for this TrustedDevice.
         */
        readonly id: number;
        /**
         * __Read-only__ The last time this TrustedDevice was successfully used to authenticate to [login.linode.com](https://login.linode.com).
         */
        readonly last_authenticated: string;
        /**
         * __Read-only__ The last IP Address to successfully authenticate with this TrustedDevice.
         */
        readonly last_remote_addr: string;
        /**
         * __Read-only__ The User Agent of the browser that created this TrustedDevice session.
         */
        readonly user_agent: string;
    };
};

export type GetTrustedDeviceResponse = GetTrustedDeviceResponses[keyof GetTrustedDeviceResponses];

export type GetProfileGrantsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/grants';
};

export type GetProfileGrantsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetProfileGrantsError = GetProfileGrantsErrors[keyof GetProfileGrantsErrors];

export type GetProfileGrantsResponses = {
    /**
     * GrantsResponse.
     */
    200: {
        /**
         * The grants this user has for individual Managed Databases on this account.
         */
        database: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual domains on this account.
         */
        domain: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual firewalls on this account.
         */
        firewall: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants the user has to all resources on your account.
         */
        global: {
            /**
             * The level of access this user has to account-level actions, like billing information and user management.
             *
             * > 📘
             * >
             * > A `restricted` user can't be used to manage users, even if this is set to `read-write`. Only unrestricted users can manage other users on an account.
             *
             * __Parent and child accounts__
             *
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this grant can be added to a child account user, to give the user `read-write` access. This gives the child user unrestricted access to expected management operations, such as creating other child users. However, child users don't have write access to billing operations. The API issues a specific error message if a write operation is attempted by a child user.
             */
            account_access: 'read_only' | 'read_write';
            /**
             * Whether this user can add Managed Databases on the account.
             */
            add_databases: boolean;
            /**
             * Whether this user can add domains on the account.
             */
            add_domains: boolean;
            /**
             * Whether this user can add Firewalls on the account.
             */
            add_firewalls: boolean;
            /**
             * Whether this user can create images from disks on your Linodes, on the account.
             */
            add_images: boolean;
            /**
             * Whether this user can create Linodes.
             */
            add_linodes: boolean;
            /**
             * Whether this user can create Longview clients and view the current plan.
             */
            add_longview: boolean;
            /**
             * Whether this user can add NodeBalancers on the account.
             */
            add_nodebalancers: boolean;
            /**
             * Whether this user can add StackScripts on the account.
             */
            add_stackscripts: boolean;
            /**
             * Whether this user can add volumes on the account.
             */
            add_volumes: boolean;
            /**
             * Whether this user can add Virtual Private Clouds (VPCs) on the account.
             */
            add_vpcs: boolean;
            /**
             * Whether this user can cancel the entire account.
             */
            cancel_account: boolean;
            /**
             * In a [parent and child account](https://www.linode.com/docs/guides/parent-child-accounts/) environment, this gives a parent account access to operations that can be used to manage child accounts. Unrestricted parent account users have access to this grant, while restricted parent users don't. An unrestricted parent user can set this to `true` to add this grant to a restricted parent user. Displayed as `null` for all non-parent accounts.
             */
            child_account_access: boolean | null;
            /**
             * Whether this user can manage your account's Longview subscription.
             */
            longview_subscription: boolean;
        };
        /**
         * The grants this user has for individual images on this account.
         */
        image: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Linodes on this account.
         */
        linode: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual Longview Clients on this account.
         */
        longview: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has for individual NodeBalancers on this account.
         */
        nodebalancer: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this User has for individual StackScripts on this account.
         */
        stackscript: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Block Storage Volumes on this account.
         */
        volume: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
        /**
         * The grants this user has individual Virtual Private Clouds (VPCs) on this account.
         */
        vpc: Array<{
            /**
             * The unique identifier of the resource this grant applies to.
             */
            id: number;
            /**
             * __Read-only__ The name of the entity this grant applies to. This is only for display purposes.
             */
            readonly label: string;
            /**
             * The level of access this user has to this entity. If `null`, this user has no access.
             */
            permissions: 'read_only' | 'read_write';
        }>;
    };
    /**
     * This is an unrestricted User, who has no grants. This User can access everything on the Account.
     */
    204: void;
};

export type GetProfileGrantsResponse = GetProfileGrantsResponses[keyof GetProfileGrantsResponses];

export type GetProfileLoginsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/logins';
};

export type GetProfileLoginsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetProfileLoginsError = GetProfileLoginsErrors[keyof GetProfileLoginsErrors];

export type GetProfileLoginsResponses = {
    /**
     * An array of successful account logins from this user during the last 90 days.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When the login was initiated.
             */
            readonly datetime: string;
            /**
             * __Read-only__ The unique ID of this login object.
             */
            readonly id: number;
            /**
             * __Read-only__ The remote IP address that requested the login.
             */
            readonly ip: string;
            /**
             * __Read-only__ True if the User that attempted the login was a restricted User, false otherwise.
             */
            readonly restricted: boolean;
            /**
             * __Read-only__ Whether the login attempt succeeded or failed.
             */
            status: 'successful' | 'failed';
            /**
             * __Read-only__ The username of the User that attempted the login.
             */
            readonly username: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetProfileLoginsResponse = GetProfileLoginsResponses[keyof GetProfileLoginsResponses];

export type GetProfileLoginData = {
    body?: never;
    path: {
        /**
         * The ID of the login object to access.
         */
        loginId: number;
    };
    query?: never;
    url: '/profile/logins/{loginId}';
};

export type GetProfileLoginErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetProfileLoginError = GetProfileLoginErrors[keyof GetProfileLoginErrors];

export type GetProfileLoginResponses = {
    /**
     * An object representing a previous successful login for a User.
     */
    200: {
        /**
         * __Read-only__ When the login was initiated.
         */
        readonly datetime: string;
        /**
         * __Read-only__ The unique ID of this login object.
         */
        readonly id: number;
        /**
         * __Read-only__ The remote IP address that requested the login.
         */
        readonly ip: string;
        /**
         * __Read-only__ True if the User that attempted the login was a restricted User, false otherwise.
         */
        readonly restricted: boolean;
        /**
         * __Read-only__ Whether the login attempt succeeded or failed.
         */
        status: 'successful' | 'failed';
        /**
         * __Read-only__ The username of the User that attempted the login.
         */
        readonly username: string;
    };
};

export type GetProfileLoginResponse = GetProfileLoginResponses[keyof GetProfileLoginResponses];

export type DeleteProfilePhoneNumberData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/phone-number';
};

export type DeleteProfilePhoneNumberErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteProfilePhoneNumberError = DeleteProfilePhoneNumberErrors[keyof DeleteProfilePhoneNumberErrors];

export type DeleteProfilePhoneNumberResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteProfilePhoneNumberResponse = DeleteProfilePhoneNumberResponses[keyof DeleteProfilePhoneNumberResponses];

export type PostProfilePhoneNumberData = {
    /**
     * Enter a phone number and country code for verification.
     */
    body?: {
        /**
         * The two-letter ISO 3166 country code associated with the phone number.
         */
        iso_code: string;
        /**
         * A valid phone number.
         */
        phone_number: string;
    };
    path?: never;
    query?: never;
    url: '/profile/phone-number';
};

export type PostProfilePhoneNumberErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostProfilePhoneNumberError = PostProfilePhoneNumberErrors[keyof PostProfilePhoneNumberErrors];

export type PostProfilePhoneNumberResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostProfilePhoneNumberResponse = PostProfilePhoneNumberResponses[keyof PostProfilePhoneNumberResponses];

export type PostProfilePhoneNumberVerifyData = {
    /**
     * Enter a phone verification code for confirmation.
     */
    body?: {
        /**
         * The one-time code received via SMS message after running the [Send a phone number verification code](https://techdocs.akamai.com/linode-api/reference/post-profile-phone-number) operation.
         */
        otp_code: string;
    };
    path?: never;
    query?: never;
    url: '/profile/phone-number/verify';
};

export type PostProfilePhoneNumberVerifyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostProfilePhoneNumberVerifyError = PostProfilePhoneNumberVerifyErrors[keyof PostProfilePhoneNumberVerifyErrors];

export type PostProfilePhoneNumberVerifyResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostProfilePhoneNumberVerifyResponse = PostProfilePhoneNumberVerifyResponses[keyof PostProfilePhoneNumberVerifyResponses];

export type GetUserPreferencesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/preferences';
};

export type GetUserPreferencesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetUserPreferencesError = GetUserPreferencesErrors[keyof GetUserPreferencesErrors];

export type GetUserPreferencesResponses = {
    /**
     * A dictionary of user preferences.
     */
    200: {
        [key: string]: unknown;
    };
};

export type GetUserPreferencesResponse = GetUserPreferencesResponses[keyof GetUserPreferencesResponses];

export type PutUserPreferencesData = {
    /**
     * Arbitrary JSON of your choosing. Overwrites any existing preferences for this user.
     *
     * Total length cannot exceed 65,535 characters.
     */
    body: {
        [key: string]: unknown;
    };
    path?: never;
    query?: never;
    url: '/profile/preferences';
};

export type PutUserPreferencesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutUserPreferencesError = PutUserPreferencesErrors[keyof PutUserPreferencesErrors];

export type PutUserPreferencesResponses = {
    /**
     * An object of user preferences.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PutUserPreferencesResponse = PutUserPreferencesResponses[keyof PutUserPreferencesResponses];

export type GetSecurityQuestionsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/security-questions';
};

export type GetSecurityQuestionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSecurityQuestionsError = GetSecurityQuestionsErrors[keyof GetSecurityQuestionsErrors];

export type GetSecurityQuestionsResponses = {
    /**
     * Security questions and responses object for GET operation.
     */
    200: {
        /**
         * Security questions and response objects.
         */
        security_questions: Array<{
            /**
             * The ID representing the security question.
             */
            id: number;
            /**
             * __Read-only__ The security question.
             */
            readonly question: string;
            /**
             * The security question response.
             */
            response: string;
        }>;
    };
};

export type GetSecurityQuestionsResponse = GetSecurityQuestionsResponses[keyof GetSecurityQuestionsResponses];

export type PostSecurityQuestionsData = {
    /**
     * Security questions and responses object for POST operation.
     */
    body?: {
        /**
         * Security questions and response objects.
         */
        security_questions: Array<{
            /**
             * The ID representing the security question.
             */
            question_id: number;
            /**
             * The security question response.
             */
            response: string;
            /**
             * __Read-only__ The security question.
             */
            readonly security_question: string;
        }>;
    };
    path?: never;
    query?: never;
    url: '/profile/security-questions';
};

export type PostSecurityQuestionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostSecurityQuestionsError = PostSecurityQuestionsErrors[keyof PostSecurityQuestionsErrors];

export type PostSecurityQuestionsResponses = {
    /**
     * Security questions and responses object for POST operation.
     */
    200: {
        /**
         * Security questions and response objects.
         */
        security_questions: Array<{
            /**
             * The ID representing the security question.
             */
            question_id: number;
            /**
             * The security question response.
             */
            response: string;
            /**
             * __Read-only__ The security question.
             */
            readonly security_question: string;
        }>;
    };
};

export type PostSecurityQuestionsResponse = PostSecurityQuestionsResponses[keyof PostSecurityQuestionsResponses];

export type GetSshKeysData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/profile/sshkeys';
};

export type GetSshKeysErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSshKeysError = GetSshKeysErrors[keyof GetSshKeysErrors];

export type GetSshKeysResponses = {
    /**
     * Returns a paginated list of SSH Key objects.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ The date this key was added.
             */
            readonly created: string;
            /**
             * __Read-only__ The unique identifier of an SSH Key object.
             */
            readonly id: number;
            /**
             * A label for the SSH Key.
             */
            label: string;
            /**
             * The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.
             *
             * Accepted formats:
             *
             * - ssh-dss
             * - ssh-rsa
             * - ecdsa-sha2-nistp
             * - ssh-ed25519
             * - sk-ecdsa-sha2-nistp256 (Akamai-specific)
             */
            ssh_key: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetSshKeysResponse = GetSshKeysResponses[keyof GetSshKeysResponses];

export type PostAddSshKeyData = {
    /**
     * A credential object for authenticating a User's secure shell connection to a Linode.
     */
    body?: {
        /**
         * __Read-only__ The date this key was added.
         */
        readonly created: string;
        /**
         * __Read-only__ The unique identifier of an SSH Key object.
         */
        readonly id: number;
        /**
         * A label for the SSH Key.
         */
        label: string;
        /**
         * The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.
         *
         * Accepted formats:
         *
         * - ssh-dss
         * - ssh-rsa
         * - ecdsa-sha2-nistp
         * - ssh-ed25519
         * - sk-ecdsa-sha2-nistp256 (Akamai-specific)
         */
        ssh_key: string;
    };
    path?: never;
    query?: never;
    url: '/profile/sshkeys';
};

export type PostAddSshKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAddSshKeyError = PostAddSshKeyErrors[keyof PostAddSshKeyErrors];

export type PostAddSshKeyResponses = {
    /**
     * A credential object for authenticating a User's secure shell connection to a Linode.
     */
    200: {
        /**
         * __Read-only__ The date this key was added.
         */
        readonly created: string;
        /**
         * __Read-only__ The unique identifier of an SSH Key object.
         */
        readonly id: number;
        /**
         * A label for the SSH Key.
         */
        label: string;
        /**
         * The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.
         *
         * Accepted formats:
         *
         * - ssh-dss
         * - ssh-rsa
         * - ecdsa-sha2-nistp
         * - ssh-ed25519
         * - sk-ecdsa-sha2-nistp256 (Akamai-specific)
         */
        ssh_key: string;
    };
};

export type PostAddSshKeyResponse = PostAddSshKeyResponses[keyof PostAddSshKeyResponses];

export type DeleteSshKeyData = {
    body?: never;
    path: {
        /**
         * The ID of the SSHKey.
         */
        sshKeyId: number;
    };
    query?: never;
    url: '/profile/sshkeys/{sshKeyId}';
};

export type DeleteSshKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteSshKeyError = DeleteSshKeyErrors[keyof DeleteSshKeyErrors];

export type DeleteSshKeyResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteSshKeyResponse = DeleteSshKeyResponses[keyof DeleteSshKeyResponses];

export type GetSshKeyData = {
    body?: never;
    path: {
        /**
         * The ID of the SSHKey.
         */
        sshKeyId: number;
    };
    query?: never;
    url: '/profile/sshkeys/{sshKeyId}';
};

export type GetSshKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetSshKeyError = GetSshKeyErrors[keyof GetSshKeyErrors];

export type GetSshKeyResponses = {
    /**
     * A credential object for authenticating a User's secure shell connection to a Linode.
     */
    200: {
        /**
         * __Read-only__ The date this key was added.
         */
        readonly created: string;
        /**
         * __Read-only__ The unique identifier of an SSH Key object.
         */
        readonly id: number;
        /**
         * A label for the SSH Key.
         */
        label: string;
        /**
         * The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.
         *
         * Accepted formats:
         *
         * - ssh-dss
         * - ssh-rsa
         * - ecdsa-sha2-nistp
         * - ssh-ed25519
         * - sk-ecdsa-sha2-nistp256 (Akamai-specific)
         */
        ssh_key: string;
    };
};

export type GetSshKeyResponse = GetSshKeyResponses[keyof GetSshKeyResponses];

export type PutSshKeyData = {
    /**
     * The fields to update.
     */
    body: {
        /**
         * A label for the SSH Key.
         */
        label: string;
    };
    path: {
        /**
         * The ID of the SSHKey.
         */
        sshKeyId: number;
    };
    query?: never;
    url: '/profile/sshkeys/{sshKeyId}';
};

export type PutSshKeyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutSshKeyError = PutSshKeyErrors[keyof PutSshKeyErrors];

export type PutSshKeyResponses = {
    /**
     * A credential object for authenticating a User's secure shell connection to a Linode.
     */
    200: {
        /**
         * __Read-only__ The date this key was added.
         */
        readonly created: string;
        /**
         * __Read-only__ The unique identifier of an SSH Key object.
         */
        readonly id: number;
        /**
         * A label for the SSH Key.
         */
        label: string;
        /**
         * The public SSH Key, which is used to authenticate to the root user of the Linodes you deploy.
         *
         * Accepted formats:
         *
         * - ssh-dss
         * - ssh-rsa
         * - ecdsa-sha2-nistp
         * - ssh-ed25519
         * - sk-ecdsa-sha2-nistp256 (Akamai-specific)
         */
        ssh_key: string;
    };
};

export type PutSshKeyResponse = PutSshKeyResponses[keyof PutSshKeyResponses];

export type PostTfaDisableData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/tfa-disable';
};

export type PostTfaDisableErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTfaDisableError = PostTfaDisableErrors[keyof PostTfaDisableErrors];

export type PostTfaDisableResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostTfaDisableResponse = PostTfaDisableResponses[keyof PostTfaDisableResponses];

export type PostTfaEnableData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/tfa-enable';
};

export type PostTfaEnableErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTfaEnableError = PostTfaEnableErrors[keyof PostTfaEnableErrors];

export type PostTfaEnableResponses = {
    /**
     * Secret key generated.
     */
    200: {
        /**
         * When this Two Factor secret expires.
         */
        expiry: string;
        /**
         * Your Two Factor secret. This is used to generate time-based two factor codes required for logging in. Doing this will be required to confirm TFA an actually enable it.
         */
        secret: string;
    };
};

export type PostTfaEnableResponse = PostTfaEnableResponses[keyof PostTfaEnableResponses];

export type PostTfaConfirmData = {
    /**
     * The one-time code you generated with your 2FA secret key.
     */
    body: {
        /**
         * The Two Factor code you generated with your Two Factor secret. These codes are time-based, so be sure it is current.
         */
        tfa_code: string;
    };
    path?: never;
    query?: never;
    url: '/profile/tfa-enable-confirm';
};

export type PostTfaConfirmErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTfaConfirmError = PostTfaConfirmErrors[keyof PostTfaConfirmErrors];

export type PostTfaConfirmResponses = {
    /**
     * TFA enabled successfully.
     */
    200: {
        /**
         * A one-use code that can be used in place of your Two Factor code, in case you are unable to generate one.  Keep this in a safe place to avoid being locked out of your Account.
         */
        scratch: string;
    };
};

export type PostTfaConfirmResponse = PostTfaConfirmResponses[keyof PostTfaConfirmResponses];

export type GetPersonalAccessTokensData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/profile/tokens';
};

export type GetPersonalAccessTokensErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPersonalAccessTokensError = GetPersonalAccessTokensErrors[keyof GetPersonalAccessTokensErrors];

export type GetPersonalAccessTokensResponses = {
    /**
     * A paginated list of active tokens.
     */
    200: {
        data: Array<{
            /**
             * __Filterable__, __Read-only__ The date and time this token was created.
             */
            readonly created: string;
            /**
             * __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed, so after this time the token will be completely unusable and a new token will need to be generated.  Tokens may be created with `null` as their expiry and will never expire unless revoked.
             */
            readonly expiry: string;
            /**
             * __Read-only__ This token's unique ID, which can be used to revoke it.
             */
            readonly id: number;
            /**
             * __Filterable__ This token's label.  This is for display purposes only, but can be used to more easily track what you're using each token for.
             */
            label: string;
            /**
             * __Read-only__ The scopes this token was created with. These define what parts of the Account the token can be used to access. Many command-line tools, such as the [Linode CLI](https://github.com/linode/linode-cli), require tokens with access to `*`. Tokens with more restrictive scopes are generally more secure.
             */
            readonly scopes: string;
            /**
             * __Read-only__ The token used to access the API.  When the token is created, the full token is returned here.  Otherwise, only the first 16 characters are returned.
             */
            readonly token: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetPersonalAccessTokensResponse = GetPersonalAccessTokensResponses[keyof GetPersonalAccessTokensResponses];

export type PostPersonalAccessTokenData = {
    /**
     * Information about the requested token.
     */
    body: {
        /**
         * When this token should be valid until.  If omitted, the new token will be valid until it is manually revoked.
         */
        expiry: string | null;
        /**
         * __Filterable__ This token's label.  This is for display purposes only, but can be used to more easily track what you're using each token for.
         */
        label: string;
        /**
         * The access [scopes](https://techdocs.akamai.com/linode-api/reference/get-started#oauth-reference) to grant to the created token. These cannot be changed after creation, and may not exceed the scopes of the acting token.
         *
         * If omitted or entered with a wildcard character (`*`), the new token will have the same scopes as the acting token.
         *
         * Multiple scopes are separated by a space character (` `).
         *
         * For example, `linodes:read_write account:read_only`.
         */
        scopes: string;
    };
    path?: never;
    query?: never;
    url: '/profile/tokens';
};

export type PostPersonalAccessTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostPersonalAccessTokenError = PostPersonalAccessTokenErrors[keyof PostPersonalAccessTokenErrors];

export type PostPersonalAccessTokenResponses = {
    /**
     * A Personal Access Token is a token generated manually to access the API without going through an OAuth login.  Personal Access Tokens can have scopes just like OAuth tokens do, and are commonly used to give access to command-line tools like the Linode CLI, or when writing your own integrations.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date and time this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed, so after this time the token will be completely unusable and a new token will need to be generated.  Tokens may be created with `null` as their expiry and will never expire unless revoked.
         */
        readonly expiry: string;
        /**
         * __Read-only__ This token's unique ID, which can be used to revoke it.
         */
        readonly id: number;
        /**
         * __Filterable__ This token's label.  This is for display purposes only, but can be used to more easily track what you're using each token for.
         */
        label: string;
        /**
         * __Read-only__ The scopes this token was created with. These define what parts of the Account the token can be used to access. Many command-line tools, such as the [Linode CLI](https://github.com/linode/linode-cli), require tokens with access to `*`. Tokens with more restrictive scopes are generally more secure.
         */
        readonly scopes: string;
        /**
         * __Read-only__ The token used to access the API.  When the token is created, the full token is returned here.  Otherwise, only the first 16 characters are returned.
         */
        readonly token: string;
    };
};

export type PostPersonalAccessTokenResponse = PostPersonalAccessTokenResponses[keyof PostPersonalAccessTokenResponses];

export type DeletePersonalAccessTokenData = {
    body?: never;
    path: {
        /**
         * The ID of the token to access.
         */
        tokenId: number;
    };
    query?: never;
    url: '/profile/tokens/{tokenId}';
};

export type DeletePersonalAccessTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeletePersonalAccessTokenError = DeletePersonalAccessTokenErrors[keyof DeletePersonalAccessTokenErrors];

export type DeletePersonalAccessTokenResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeletePersonalAccessTokenResponse = DeletePersonalAccessTokenResponses[keyof DeletePersonalAccessTokenResponses];

export type GetPersonalAccessTokenData = {
    body?: never;
    path: {
        /**
         * The ID of the token to access.
         */
        tokenId: number;
    };
    query?: never;
    url: '/profile/tokens/{tokenId}';
};

export type GetPersonalAccessTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetPersonalAccessTokenError = GetPersonalAccessTokenErrors[keyof GetPersonalAccessTokenErrors];

export type GetPersonalAccessTokenResponses = {
    /**
     * A Personal Access Token is a token generated manually to access the API without going through an OAuth login.  Personal Access Tokens can have scopes just like OAuth tokens do, and are commonly used to give access to command-line tools like the Linode CLI, or when writing your own integrations.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date and time this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed, so after this time the token will be completely unusable and a new token will need to be generated.  Tokens may be created with `null` as their expiry and will never expire unless revoked.
         */
        readonly expiry: string;
        /**
         * __Read-only__ This token's unique ID, which can be used to revoke it.
         */
        readonly id: number;
        /**
         * __Filterable__ This token's label.  This is for display purposes only, but can be used to more easily track what you're using each token for.
         */
        label: string;
        /**
         * __Read-only__ The scopes this token was created with. These define what parts of the Account the token can be used to access. Many command-line tools, such as the [Linode CLI](https://github.com/linode/linode-cli), require tokens with access to `*`. Tokens with more restrictive scopes are generally more secure.
         */
        readonly scopes: string;
        /**
         * __Read-only__ The token used to access the API.  When the token is created, the full token is returned here.  Otherwise, only the first 16 characters are returned.
         */
        readonly token: string;
    };
};

export type GetPersonalAccessTokenResponse = GetPersonalAccessTokenResponses[keyof GetPersonalAccessTokenResponses];

export type PutPersonalAccessTokenData = {
    /**
     * A Personal Access Token is a token generated manually to access the API without going through an OAuth login.  Personal Access Tokens can have scopes just like OAuth tokens do, and are commonly used to give access to command-line tools like the Linode CLI, or when writing your own integrations.
     */
    body: {
        /**
         * __Filterable__, __Read-only__ The date and time this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed, so after this time the token will be completely unusable and a new token will need to be generated.  Tokens may be created with `null` as their expiry and will never expire unless revoked.
         */
        readonly expiry: string;
        /**
         * __Read-only__ This token's unique ID, which can be used to revoke it.
         */
        readonly id: number;
        /**
         * __Filterable__ This token's label.  This is for display purposes only, but can be used to more easily track what you're using each token for.
         */
        label: string;
        /**
         * __Read-only__ The scopes this token was created with. These define what parts of the Account the token can be used to access. Many command-line tools, such as the [Linode CLI](https://github.com/linode/linode-cli), require tokens with access to `*`. Tokens with more restrictive scopes are generally more secure.
         */
        readonly scopes: string;
        /**
         * __Read-only__ The token used to access the API.  When the token is created, the full token is returned here.  Otherwise, only the first 16 characters are returned.
         */
        readonly token: string;
    };
    path: {
        /**
         * The ID of the token to access.
         */
        tokenId: number;
    };
    query?: never;
    url: '/profile/tokens/{tokenId}';
};

export type PutPersonalAccessTokenErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutPersonalAccessTokenError = PutPersonalAccessTokenErrors[keyof PutPersonalAccessTokenErrors];

export type PutPersonalAccessTokenResponses = {
    /**
     * A Personal Access Token is a token generated manually to access the API without going through an OAuth login.  Personal Access Tokens can have scopes just like OAuth tokens do, and are commonly used to give access to command-line tools like the Linode CLI, or when writing your own integrations.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date and time this token was created.
         */
        readonly created: string;
        /**
         * __Read-only__ When this token will expire.  Personal Access Tokens cannot be renewed, so after this time the token will be completely unusable and a new token will need to be generated.  Tokens may be created with `null` as their expiry and will never expire unless revoked.
         */
        readonly expiry: string;
        /**
         * __Read-only__ This token's unique ID, which can be used to revoke it.
         */
        readonly id: number;
        /**
         * __Filterable__ This token's label.  This is for display purposes only, but can be used to more easily track what you're using each token for.
         */
        label: string;
        /**
         * __Read-only__ The scopes this token was created with. These define what parts of the Account the token can be used to access. Many command-line tools, such as the [Linode CLI](https://github.com/linode/linode-cli), require tokens with access to `*`. Tokens with more restrictive scopes are generally more secure.
         */
        readonly scopes: string;
        /**
         * __Read-only__ The token used to access the API.  When the token is created, the full token is returned here.  Otherwise, only the first 16 characters are returned.
         */
        readonly token: string;
    };
};

export type PutPersonalAccessTokenResponse = PutPersonalAccessTokenResponses[keyof PutPersonalAccessTokenResponses];

export type GetRegionsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/regions';
};

export type GetRegionsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetRegionsError = GetRegionsErrors[keyof GetRegionsErrors];

export type GetRegionsResponses = {
    /**
     * Returns an array of regions.
     */
    200: {
        data: Array<{
            /**
             * A list of capabilities of this region.
             */
            capabilities: Array<string>;
            /**
             * __Filterable__ The country where this region resides.
             */
            country: string;
            /**
             * The unique ID of this Region.
             */
            id: string;
            /**
             * Detailed location information for this region, including city, state or region, and country.
             */
            label: string;
            /**
             * __Beta__ Lists the services in this region that support metrics and alerts use with Akamai Cloud Pulse (ACLP).
             *
             * > 📘
             * >
             * > The ACLP service is currently beta. This object is only returned in a response if you're participating in the ACLP beta. Contact your account team for more information.
             */
            monitors: {
                /**
                 * Each `service_type` supported for use in managing ACLP [alerts](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) in this region, for your account. A `service_type` identifies the Akamai Cloud Computing service.
                 */
                alerts: Array<string>;
                /**
                 * Each `service_type` supported for use in managing ACLP [metrics](https://techdocs.akamai.com/linode-api/reference/get-dashboards-all) in this region, for your account. A `service_type` identifies the Akamai Cloud Computing service.
                 */
                metrics: Array<string>;
            };
            /**
             * The limits for [placement groups](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) in this region.
             */
            placement_group_limits: {
                /**
                 * The maximum number of Linodes you can include in a placement group, when that placement group uses a `placement_group_policy` of `flexible`.  Displayed as `null` if you don't have a limit. See [Create placement group](https://techdocs.akamai.com/linode-api/reference/post-placement-group) for more information on `placement_group_policy`.
                 */
                maximum_linodes_per_flexible_pg: number | null;
                /**
                 * The maximum number of Linodes you can include in a placement group, when that placement group uses a `placement _group_policy` of `strict`. Displayed as `null` if you don't have a limit. See [Create placement group](https://techdocs.akamai.com/linode-api/reference/post-placement-group) for more information on `placement_group_policy`.
                 */
                maximum_linodes_per_pg: number | null;
                /**
                 * The maximum number of placement groups you can have in this region. Displayed as `null` if you don't have a limit.
                 */
                maximum_pgs_per_customer: number | null;
            };
            resolvers: {
                /**
                 * The IPv4 addresses for this region's DNS resolvers, separated by commas.
                 */
                ipv4: string;
                /**
                 * The IPv6 addresses for this region's DNS resolvers, separated by commas.
                 */
                ipv6: string;
            };
            /**
             * __Filterable__ This region's site type. A `core` region indicates a traditional cloud computing [region](https://www.linode.com/docs/products/platform/get-started/guides/choose-a-data-center/#product-availability) that offers all compute services. A `distributed` region indicates sites that are globally dispersed to be closer to end users and workloads. These regions offer limited services.
             */
            site_type: 'core' | 'distributed';
            /**
             * This region's current operational status.
             */
            status: 'ok' | 'outage';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetRegionsResponse = GetRegionsResponses[keyof GetRegionsResponses];

export type GetRegionsAvailabilityData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/regions/availability';
};

export type GetRegionsAvailabilityErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetRegionsAvailabilityError = GetRegionsAvailabilityErrors[keyof GetRegionsAvailabilityErrors];

export type GetRegionsAvailabilityResponses = {
    /**
     * Returns a Region Availability object.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data?: Array<{
            /**
             * __Filterable__ Whether the compute instance type is available in the region.
             */
            available: boolean;
            /**
             * __Filterable__ The compute instance [Type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) ID.
             */
            plan: string;
            /**
             * __Filterable__ The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID.
             */
            region: string;
        }>;
    };
};

export type GetRegionsAvailabilityResponse = GetRegionsAvailabilityResponses[keyof GetRegionsAvailabilityResponses];

export type GetRegionData = {
    body?: never;
    path: {
        /**
         * The abbreviated value ("slug") for the applicable data center. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the slug for each data center.
         */
        regionId: string;
    };
    query?: never;
    url: '/regions/{regionId}';
};

export type GetRegionErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetRegionError = GetRegionErrors[keyof GetRegionErrors];

export type GetRegionResponses = {
    /**
     * An area where Linode services are available.
     */
    200: {
        /**
         * A list of capabilities of this region.
         */
        capabilities: Array<string>;
        /**
         * __Filterable__ The country where this region resides.
         */
        country: string;
        /**
         * The unique ID of this Region.
         */
        id: string;
        /**
         * Detailed location information for this region, including city, state or region, and country.
         */
        label: string;
        /**
         * __Beta__ Lists the services in this region that support metrics and alerts use with Akamai Cloud Pulse (ACLP).
         *
         * > 📘
         * >
         * > The ACLP service is currently beta. This object is only returned in a response if you're participating in the ACLP beta. Contact your account team for more information.
         */
        monitors: {
            /**
             * Each `service_type` supported for use in managing ACLP [alerts](https://techdocs.akamai.com/linode-api/reference/post-alert-definition-for-service-type) in this region, for your account. A `service_type` identifies the Akamai Cloud Computing service.
             */
            alerts: Array<string>;
            /**
             * Each `service_type` supported for use in managing ACLP [metrics](https://techdocs.akamai.com/linode-api/reference/get-dashboards-all) in this region, for your account. A `service_type` identifies the Akamai Cloud Computing service.
             */
            metrics: Array<string>;
        };
        /**
         * The limits for [placement groups](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) in this region.
         */
        placement_group_limits: {
            /**
             * The maximum number of Linodes you can include in a placement group, when that placement group uses a `placement_group_policy` of `flexible`.  Displayed as `null` if you don't have a limit. See [Create placement group](https://techdocs.akamai.com/linode-api/reference/post-placement-group) for more information on `placement_group_policy`.
             */
            maximum_linodes_per_flexible_pg: number | null;
            /**
             * The maximum number of Linodes you can include in a placement group, when that placement group uses a `placement _group_policy` of `strict`. Displayed as `null` if you don't have a limit. See [Create placement group](https://techdocs.akamai.com/linode-api/reference/post-placement-group) for more information on `placement_group_policy`.
             */
            maximum_linodes_per_pg: number | null;
            /**
             * The maximum number of placement groups you can have in this region. Displayed as `null` if you don't have a limit.
             */
            maximum_pgs_per_customer: number | null;
        };
        resolvers: {
            /**
             * The IPv4 addresses for this region's DNS resolvers, separated by commas.
             */
            ipv4: string;
            /**
             * The IPv6 addresses for this region's DNS resolvers, separated by commas.
             */
            ipv6: string;
        };
        /**
         * __Filterable__ This region's site type. A `core` region indicates a traditional cloud computing [region](https://www.linode.com/docs/products/platform/get-started/guides/choose-a-data-center/#product-availability) that offers all compute services. A `distributed` region indicates sites that are globally dispersed to be closer to end users and workloads. These regions offer limited services.
         */
        site_type: 'core' | 'distributed';
        /**
         * This region's current operational status.
         */
        status: 'ok' | 'outage';
    };
};

export type GetRegionResponse = GetRegionResponses[keyof GetRegionResponses];

export type GetRegionAvailabilityData = {
    body?: never;
    path: {
        /**
         * The abbreviated value ("slug") for the applicable data center. Run the [List regions](https://techdocs.akamai.com/linode-api/reference/get-regions) operation to view the slug for each data center.
         */
        regionId: string;
    };
    query?: never;
    url: '/regions/{regionId}/availability';
};

export type GetRegionAvailabilityErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetRegionAvailabilityError = GetRegionAvailabilityErrors[keyof GetRegionAvailabilityErrors];

export type GetRegionAvailabilityResponses = {
    /**
     * The availability data for a single Region.
     */
    200: Array<{
        /**
         * __Filterable__ Whether the compute instance type is available in the region.
         */
        available: boolean;
        /**
         * __Filterable__ The compute instance [Type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) ID.
         */
        plan: string;
        /**
         * __Filterable__ The [Region](https://techdocs.akamai.com/linode-api/reference/get-regions) ID.
         */
        region: string;
    }>;
};

export type GetRegionAvailabilityResponse = GetRegionAvailabilityResponses[keyof GetRegionAvailabilityResponses];

export type GetTicketsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/support/tickets';
};

export type GetTicketsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTicketsError = GetTicketsErrors[keyof GetTicketsErrors];

export type GetTicketsResponses = {
    /**
     * Returns a paginated list of support tickets.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ A list of filenames representing attached files associated with this ticket.
             */
            readonly attachments: Array<string>;
            /**
             * Whether the ticket can be closed.
             */
            closable: boolean;
            /**
             * __Filterable__, __Read-only__ When this ticket was closed.
             */
            readonly closed: string | null;
            /**
             * __Read-only__ The full details of the issue or question.
             */
            readonly description: string;
            /**
             * __Read-only__ The ticket was opened for this entity. An entity represents a specific object you've created, such as a Linode or a Managed Database.
             */
            readonly entity: {
                /**
                 * __Read-only__ The unique ID for this ticket's entity. Empty if the targeted entity doesn't use an `id`.
                 */
                readonly id: number;
                /**
                 * __Read-only__ The current label of this entity.
                 */
                readonly label: string;
                /**
                 * __Read-only__ The type of entity.
                 */
                type: 'database' | 'domain' | 'firewall' | 'linode' | 'lkecluster' | 'managed_service' | 'nodebalancer' | 'vlan' | 'volume' | 'vpc';
                /**
                 * __Read-only__ The URL where you can access the `entity`. If this is a relative URL, it's relative to the domain for the entity.
                 */
                readonly url: string;
            } | null;
            /**
             * __Read-only__ The Gravatar ID of the user who opened this ticket.
             */
            readonly gravatar_id: string;
            /**
             * __Read-only__ The ID of the support ticket.
             */
            readonly id: number;
            /**
             * __Filterable__, __Read-only__ When this ticket was created.
             */
            readonly opened: string;
            /**
             * __Filterable__, __Read-only__ The user who opened this ticket.
             */
            readonly opened_by: string;
            /**
             * __Read-only__ The severity of this ticket.
             */
            severity: 'low' | 'moderate' | 'major';
            /**
             * __Filterable__, __Read-only__ The current status of this ticket.
             */
            status: 'closed' | 'new' | 'open';
            /**
             * __Read-only__ The summary or title for this ticket.
             */
            readonly summary: string;
            /**
             * __Filterable__, __Read-only__ When this ticket was last updated.
             */
            readonly updated: string;
            /**
             * __Read-only__ The user who last updated this ticket.
             */
            readonly updated_by: string | null;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetTicketsResponse = GetTicketsResponses[keyof GetTicketsResponses];

export type PostTicketData = {
    /**
     * An object representing a created support ticket that contains a question or issue and request for help from the Linode support team.
     */
    body?: {
        /**
         * The name of an Object Storage bucket entity for this ticket. Run the [List Object Storage buckets](https://techdocs.akamai.com/linode-api/reference/get-object-storage-buckets) operation and store the `label` for the target bucket. You also need to provide the specific `region` where the bucket is located.
         */
        bucket?: string | null;
        /**
         * The ID of the Managed Database entity for the ticket. Run the [List Managed Databases](https://techdocs.akamai.com/linode-api/reference/get-databases-instances) operation and store the `id` for the target database.
         */
        database_id?: number | null;
        /**
         * The full details of the issue or question.
         */
        description: string;
        /**
         * The ID of the domain entity for the ticket. Run the [List domains](https://techdocs.akamai.com/linode-api/reference/get-domains) operation and store the `id` for the target domain.
         */
        domain_id?: number | null;
        /**
         * The ID of the Firewall entity for the ticket. Run the [List a Linode's firewalls](https://techdocs.akamai.com/linode-api/reference/get-linode-firewalls) operation and store the `id` for the target Linode firewall.
         */
        firewall_id?: number | null;
        /**
         * The ID of the Linode entity for the ticket. Run the [List Linodes](https://techdocs.akamai.com/linode-api/reference/get-linode-instances) operation and store the `id` for the target Linode.
         */
        linode_id?: number | null;
        /**
         * The ID of the Linode Kubernetes Engine (LKE) cluster entity for the ticket. Run the [List Kubernetes clusters](https://techdocs.akamai.com/linode-api/reference/get-lke-clusters) operation and store the `id` for the target LKE cluster.
         */
        lkecluster_id?: number | null;
        /**
         * The ID of the Longview client entity for the ticket. Run the [List Longview clients](https://techdocs.akamai.com/linode-api/reference/get-longview-clients) operation and store the `id` for the target client.
         */
        longviewclient_id?: number | null;
        /**
         * Whether this ticket is related to a [managed service](https://www.linode.com/products/managed/). If `true`, the following constraints apply:
         *
         * - You can't provide an entity, such as a `linode_id` or `bucket` with this request.
         *
         * - Your account needs a managed service [enabled](https://techdocs.akamai.com/linode-api/reference/post-enable-managed-service).
         */
        managed_issue?: boolean;
        /**
         * The ID of the NodeBalancer entity for the ticket. Run the [List NodeBalancers](https://techdocs.akamai.com/linode-api/reference/get-node-balancers) operation and store the `id` for the target NodeBalancer.
         */
        nodebalancer_id?: number | null;
        /**
         * The ID of the [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where this ticket's target entity resides. This only applies to tickets for a `vlan` or an Object Storage  `bucket`.
         *
         * > 📘
         * >
         * > Set this to the `clusterId` for a legacy Object Storage `bucket`.
         */
        region?: string | null;
        /**
         * The severity of the issue. A value of `1` indicates a major issue, `2` indicates a moderate priority issue, and `3` is a low priority issue. Your account may not have access to set this value. Talk to your Akamai account team for more details.
         */
        severity?: 1 | 2 | 3;
        /**
         * The summary or title for this support ticket.
         */
        summary: string;
        /**
         * The label of the VLAN entity for the ticket. Run the [List VLANs](https://techdocs.akamai.com/linode-api/reference/get-vlans) operation and store the `id` for the target VLAN. You also need to provide the specific `region` where the VLAN is located.
         */
        vlan?: string | null;
        /**
         * The ID of the volume entity for the ticket. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume.
         */
        volume_id?: number | null;
        /**
         * The ID of the VPC entity for the ticket. Run the [List VPCs](https://techdocs.akamai.com/linode-api/reference/get-vpcs) operation and store the `id` for the target VPC.
         */
        vpc_id?: number | null;
    };
    path?: never;
    query?: never;
    url: '/support/tickets';
};

export type PostTicketErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTicketError = PostTicketErrors[keyof PostTicketErrors];

export type PostTicketResponses = {
    /**
     * A support ticket opened from your account.
     */
    200: {
        /**
         * __Read-only__ A list of filenames representing attached files associated with this ticket.
         */
        readonly attachments: Array<string>;
        /**
         * Whether the ticket can be closed.
         */
        closable: boolean;
        /**
         * __Filterable__, __Read-only__ When this ticket was closed.
         */
        readonly closed: string | null;
        /**
         * __Read-only__ The full details of the issue or question.
         */
        readonly description: string;
        /**
         * __Read-only__ The ticket was opened for this entity. An entity represents a specific object you've created, such as a Linode or a Managed Database.
         */
        readonly entity: {
            /**
             * __Read-only__ The unique ID for this ticket's entity. Empty if the targeted entity doesn't use an `id`.
             */
            readonly id: number;
            /**
             * __Read-only__ The current label of this entity.
             */
            readonly label: string;
            /**
             * __Read-only__ The type of entity.
             */
            type: 'database' | 'domain' | 'firewall' | 'linode' | 'lkecluster' | 'managed_service' | 'nodebalancer' | 'vlan' | 'volume' | 'vpc';
            /**
             * __Read-only__ The URL where you can access the `entity`. If this is a relative URL, it's relative to the domain for the entity.
             */
            readonly url: string;
        } | null;
        /**
         * __Read-only__ The Gravatar ID of the user who opened this ticket.
         */
        readonly gravatar_id: string;
        /**
         * __Read-only__ The ID of the support ticket.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ When this ticket was created.
         */
        readonly opened: string;
        /**
         * __Filterable__, __Read-only__ The user who opened this ticket.
         */
        readonly opened_by: string;
        /**
         * __Read-only__ The severity of this ticket.
         */
        severity: 'low' | 'moderate' | 'major';
        /**
         * __Filterable__, __Read-only__ The current status of this ticket.
         */
        status: 'closed' | 'new' | 'open';
        /**
         * __Read-only__ The summary or title for this ticket.
         */
        readonly summary: string;
        /**
         * __Filterable__, __Read-only__ When this ticket was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ The user who last updated this ticket.
         */
        readonly updated_by: string | null;
    };
};

export type PostTicketResponse = PostTicketResponses[keyof PostTicketResponses];

export type GetTicketData = {
    body?: never;
    path: {
        /**
         * The ID of the support ticket.
         */
        ticketId: number;
    };
    query?: never;
    url: '/support/tickets/{ticketId}';
};

export type GetTicketErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTicketError = GetTicketErrors[keyof GetTicketErrors];

export type GetTicketResponses = {
    /**
     * A support ticket opened from your account.
     */
    200: {
        /**
         * __Read-only__ A list of filenames representing attached files associated with this ticket.
         */
        readonly attachments: Array<string>;
        /**
         * Whether the ticket can be closed.
         */
        closable: boolean;
        /**
         * __Filterable__, __Read-only__ When this ticket was closed.
         */
        readonly closed: string | null;
        /**
         * __Read-only__ The full details of the issue or question.
         */
        readonly description: string;
        /**
         * __Read-only__ The ticket was opened for this entity. An entity represents a specific object you've created, such as a Linode or a Managed Database.
         */
        readonly entity: {
            /**
             * __Read-only__ The unique ID for this ticket's entity. Empty if the targeted entity doesn't use an `id`.
             */
            readonly id: number;
            /**
             * __Read-only__ The current label of this entity.
             */
            readonly label: string;
            /**
             * __Read-only__ The type of entity.
             */
            type: 'database' | 'domain' | 'firewall' | 'linode' | 'lkecluster' | 'managed_service' | 'nodebalancer' | 'vlan' | 'volume' | 'vpc';
            /**
             * __Read-only__ The URL where you can access the `entity`. If this is a relative URL, it's relative to the domain for the entity.
             */
            readonly url: string;
        } | null;
        /**
         * __Read-only__ The Gravatar ID of the user who opened this ticket.
         */
        readonly gravatar_id: string;
        /**
         * __Read-only__ The ID of the support ticket.
         */
        readonly id: number;
        /**
         * __Filterable__, __Read-only__ When this ticket was created.
         */
        readonly opened: string;
        /**
         * __Filterable__, __Read-only__ The user who opened this ticket.
         */
        readonly opened_by: string;
        /**
         * __Read-only__ The severity of this ticket.
         */
        severity: 'low' | 'moderate' | 'major';
        /**
         * __Filterable__, __Read-only__ The current status of this ticket.
         */
        status: 'closed' | 'new' | 'open';
        /**
         * __Read-only__ The summary or title for this ticket.
         */
        readonly summary: string;
        /**
         * __Filterable__, __Read-only__ When this ticket was last updated.
         */
        readonly updated: string;
        /**
         * __Read-only__ The user who last updated this ticket.
         */
        readonly updated_by: string | null;
    };
};

export type GetTicketResponse = GetTicketResponses[keyof GetTicketResponses];

export type PostTicketAttachmentData = {
    body: {
        /**
         * The local, absolute path to the file you want to attach to your support ticket.
         */
        file: string;
    };
    path: {
        /**
         * The ID of the support ticket.
         */
        ticketId: number;
    };
    query?: never;
    url: '/support/tickets/{ticketId}/attachments';
};

export type PostTicketAttachmentErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTicketAttachmentError = PostTicketAttachmentErrors[keyof PostTicketAttachmentErrors];

export type PostTicketAttachmentResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostTicketAttachmentResponse = PostTicketAttachmentResponses[keyof PostTicketAttachmentResponses];

export type PostCloseTicketData = {
    body?: never;
    path: {
        /**
         * The ID of the support ticket.
         */
        ticketId: number;
    };
    query?: never;
    url: '/support/tickets/{ticketId}/close';
};

export type PostCloseTicketErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCloseTicketError = PostCloseTicketErrors[keyof PostCloseTicketErrors];

export type PostCloseTicketResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostCloseTicketResponse = PostCloseTicketResponses[keyof PostCloseTicketResponses];

export type GetTicketRepliesData = {
    body?: never;
    path: {
        /**
         * The ID of the support ticket.
         */
        ticketId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/support/tickets/{ticketId}/replies';
};

export type GetTicketRepliesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTicketRepliesError = GetTicketRepliesErrors[keyof GetTicketRepliesErrors];

export type GetTicketRepliesResponses = {
    /**
     * Returns a paginated list of support ticket replies.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this ticket reply was created.
             */
            readonly created: string;
            /**
             * __Read-only__ The user who submitted this reply.
             */
            readonly created_by: string;
            /**
             * __Read-only__ The body of this support ticket reply.
             */
            readonly description: string;
            /**
             * __Read-only__ If `true`, this reply came from a Linode employee.
             */
            readonly from_linode: boolean;
            /**
             * __Read-only__ The Gravatar ID of the user who created this reply.
             */
            readonly gravatar_id: string;
            /**
             * __Read-only__ The unique ID of this support ticket reply.
             */
            readonly id: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetTicketRepliesResponse = GetTicketRepliesResponses[keyof GetTicketRepliesResponses];

export type PostTicketReplyData = {
    body: {
        /**
         * The content of your reply.
         */
        description: string;
    };
    path: {
        /**
         * The ID of the support ticket.
         */
        ticketId: number;
    };
    query?: never;
    url: '/support/tickets/{ticketId}/replies';
};

export type PostTicketReplyErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTicketReplyError = PostTicketReplyErrors[keyof PostTicketReplyErrors];

export type PostTicketReplyResponses = {
    /**
     * An object representing a reply to a support ticket.
     */
    200: {
        /**
         * __Read-only__ When this ticket reply was created.
         */
        readonly created: string;
        /**
         * __Read-only__ The user who submitted this reply.
         */
        readonly created_by: string;
        /**
         * __Read-only__ The body of this support ticket reply.
         */
        readonly description: string;
        /**
         * __Read-only__ If `true`, this reply came from a Linode employee.
         */
        readonly from_linode: boolean;
        /**
         * __Read-only__ The Gravatar ID of the user who created this reply.
         */
        readonly gravatar_id: string;
        /**
         * __Read-only__ The unique ID of this support ticket reply.
         */
        readonly id: number;
    };
};

export type PostTicketReplyResponse = PostTicketReplyResponses[keyof PostTicketReplyResponses];

export type GetTagsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/tags';
};

export type GetTagsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTagsError = GetTagsErrors[keyof GetTagsErrors];

export type GetTagsResponses = {
    /**
     * A paginated list of tags.
     */
    200: {
        data: Array<{
            /**
             * The name of the tag used for organization of objects on your account.
             */
            label: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetTagsResponse = GetTagsResponses[keyof GetTagsResponses];

export type PostTagData = {
    body?: {
        /**
         * The `id` values for the domains where you want to apply the tag. You need `read_write` access to each domain. If you don't, the API won't create the tag and you'll receive an error. You can run the [List domains](https://techdocs.akamai.com/linode-api/reference/get-domains) operation to store the `id` for desired domains and to review any `tags` currently applied.
         */
        domains?: Array<number>;
        /**
         * The name of your tag. This is used for display purposes.
         */
        label: string;
        /**
         * The `id` values for the Linodes where you want to apply the tag. You need `read_write` access to each Linode. If you don't, the API won't create the tag and you'll receive an error. You can run the [List Linodes](https://techdocs.akamai.com/linode-api/reference/get-linode-instances) operation to store the `id` for desired Linodes and to review any `tags` currently applied.
         */
        linodes?: Array<number>;
        /**
         * The `id` values for the NodeBalancers where you want to apply the tag. You need `read_write` access to each NodeBalancer. If you don't, the API won't create the tag and you'll receive an error. You can run the [List NodeBalancers](https://techdocs.akamai.com/linode-api/reference/get-node-balancers) operation to store the `id` for desired NodeBalancers and to review any `tags` currently applied.
         */
        nodebalancers?: Array<number>;
        /**
         * The `id` values for the Linode volumes where you want to apply the tag. You need `read_write` access to each volume. If you don't, the API won't create the tag and you'll receive an error. You can run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation to store the `id` for desired Linode volumes and to review any `tags` currently applied.
         */
        volumes?: Array<number>;
    };
    path?: never;
    query?: never;
    url: '/tags';
};

export type PostTagErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostTagError = PostTagErrors[keyof PostTagErrors];

export type PostTagResponses = {
    /**
     * A tag you've created to apply to objects on your account. Tags are for organizational purposes only.
     */
    200: {
        /**
         * The name of the tag used for organization of objects on your account.
         */
        label: string;
    };
};

export type PostTagResponse = PostTagResponses[keyof PostTagResponses];

export type DeleteTagData = {
    body?: never;
    path: {
        /**
         * The `label` of the tag to access.
         */
        tagLabel: string;
    };
    query?: never;
    url: '/tags/{tagLabel}';
};

export type DeleteTagErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteTagError = DeleteTagErrors[keyof DeleteTagErrors];

export type DeleteTagResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteTagResponse = DeleteTagResponses[keyof DeleteTagResponses];

export type GetTaggedObjectsData = {
    body?: never;
    path: {
        /**
         * The `label` of the tag to access.
         */
        tagLabel: string;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/tags/{tagLabel}';
};

export type GetTaggedObjectsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetTaggedObjectsError = GetTaggedObjectsErrors[keyof GetTaggedObjectsErrors];

export type GetTaggedObjectsResponses = {
    /**
     * A paginated list of objects, organized by type, that have been tagged with the requested tag.
     */
    200: {
        data: Array<{
            /**
             * Details on the the specific object `type` the tag is assigned to.
             */
            data: {
                alerts: {
                    /**
                     * The percentage of CPU usage required to trigger an alert. If the average CPU usage over two hours exceeds this value, we'll send you an alert. Your Linode's total CPU capacity is represented as 100%, multiplied by its number of cores.
                     *
                     * For example, a two core Linode's CPU capacity is represented as 200%. If you want to be alerted at 90% of a two core Linode's CPU capacity, set the alert value to `180`.
                     *
                     * The default value is 90% multiplied by the number of cores.
                     *
                     * If the value is set to `0` (zero), the alert is disabled.
                     */
                    cpu: number;
                    /**
                     * The amount of disk IO operation per second required to trigger an alert. If the average disk IO over two hours exceeds this value, we'll send you an alert. If set to `0` (zero), this alert is disabled.
                     */
                    io: number;
                    /**
                     * The amount of incoming traffic, in Mbit/s, required to trigger an alert. If the average incoming traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
                     */
                    network_in: number;
                    /**
                     * The amount of outbound traffic, in Mbit/s, required to trigger an alert. If the average outbound traffic over two hours exceeds this value, we'll send you an alert. If this is set to `0` (zero), the alert is disabled.
                     */
                    network_out: number;
                    /**
                     * The percentage of network transfer that may be used before an alert is triggered. When this value is exceeded, we'll alert you. If this is set to `0` (zero), the alert is disabled.
                     */
                    transfer_quota: number;
                };
                /**
                 * Information about this Linode's backups status. For information about available backups, run [List backups](https://techdocs.akamai.com/linode-api/reference/get-backups).
                 */
                backups: {
                    /**
                     * __Read-only__ Whether Backups for this Linode are available for restoration.
                     *
                     * Backups undergoing maintenance are not available for restoration.
                     */
                    readonly available: boolean;
                    /**
                     * __Read-only__ If this Linode has the Backup service enabled. To enable backups, run [Enable backups](https://techdocs.akamai.com/linode-api/reference/post-enable-backups).
                     */
                    readonly enabled: boolean;
                    /**
                     * __Read-only__ The last successful backup time. Displayed as `null` if there was no previous backup.
                     */
                    readonly last_successful: string;
                    schedule: {
                        /**
                         * The day of the week that your Linode's weekly backup is taken. If not set manually, a day will be chosen for you. Backups are taken every day, but backups taken on this day are preferred when selecting backups to retain for a longer period.
                         *
                         * If not set manually, then when backups are initially enabled, this may come back as `Scheduling` until the `day` is automatically selected.
                         */
                        day: 'Scheduling' | 'Sunday' | 'Monday' | 'Tuesday' | 'Wednesday' | 'Thursday' | 'Friday' | 'Saturday';
                        /**
                         * When your backups will be taken, in UTC. A backups window is a two-hour span of time in which the backup may occur.
                         *
                         * For example, `W10` indicates that your backups should be taken between 10:00 and 12:00. If you don't choose a backup window, the API automatically assigns one.
                         *
                         * If not set manually, when backups are initially enabled this may come back as `Scheduling` until the `window` is automatically selected.
                         */
                        window: 'Scheduling' | 'W0' | 'W2' | 'W4' | 'W6' | 'W8' | 'W10' | 'W12' | 'W14' | 'W16' | 'W18' | 'W20' | 'W22';
                    };
                };
                /**
                 * __Limited availability__, __Read-only__ A list of capabilities this compute instance supports.
                 */
                readonly capabilities: Array<string>;
                /**
                 * __Read-only__ When this Linode was created.
                 */
                readonly created: string;
                /**
                 * __Read-only__ Indicates the local disk encryption setting for this Linode. If the Linode is part of an LKE cluster, the value is `null`.
                 */
                readonly disk_encryption: string | null;
                /**
                 * __Deprecated__, __Filterable__ The group label for this Linode.
                 *
                 * @deprecated
                 */
                group: string;
                /**
                 * __Read-only__ Whether this compute instance was provisioned with `user_data` provided via the Metadata service. See the [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance) description for more information on Metadata.
                 */
                readonly has_user_data: boolean;
                /**
                 * __Read-only__ The Linode's host machine, as a UUID.
                 */
                readonly host_uuid: string;
                /**
                 * __Read-only__ The virtualization software powering this Linode.
                 */
                hypervisor: 'kvm';
                /**
                 * __Filterable__, __Read-only__ This Linode's ID which must be provided for all operations impacting this Linode.
                 */
                readonly id: number;
                /**
                 * An Image ID to deploy the Linode Disk from.
                 *
                 * Run the [List images](https://techdocs.akamai.com/linode-api/reference/get-images) operation with authentication to view all available Images. Official Linode Images start with `linode/`, while your Account's Images start with `private/`. Creating a disk from a Private Image requires `read_only` or `read_write` permissions for that Image. Run the [Update a user's grants](https://techdocs.akamai.com/linode-api/reference/put-user-grants) operation to adjust permissions for an Account Image.
                 */
                image: string | null;
                /**
                 * __Filterable__, __Read-only__ Indicates if the Linode is configured to use Linode interfaces (`linode`) or legacy configuration profile interfaces (`legacy_config`).
                 */
                interface_generation: 'legacy_config' | 'linode';
                /**
                 * __Filterable__, __Read-only__ This Linode's IPv4 Addresses. Each Linode is assigned a single public IPv4 address upon creation, and may get a single private IPv4 address if needed. You may need to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) to get additional IPv4 addresses.
                 *
                 * IPv4 addresses may be reassigned between your Linodes, or shared with other Linodes. See the [networking](https://techdocs.akamai.com/linode-api/reference/post-firewalls) operations for details.
                 */
                readonly ipv4: Array<string>;
                /**
                 * __Read-only__ This Linode's IPv6 SLAAC address. This address is specific to a Linode, and may not be shared. If the Linode has not been assigned an IPv6 address, the return value will be `null`.
                 */
                readonly ipv6: string | null;
                /**
                 * __Filterable__ Provides a name for the Linode. If not provided, the API generates one for it.
                 *
                 * Linode labels have the following constraints:
                 *
                 * - It needs to begin and end with an alphanumeric character.
                 * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                 * - Cannot have two hyphens (`--`), underscores (`__`) or periods (`..`) in a row.
                 */
                label: string;
                /**
                 * __Read-only__ The ID of the Kubernetes cluster if the Linode is part of cluster.
                 */
                readonly lke_cluster_id: number | null;
                /**
                 * __Beta__ The maintenance policy configured by the user for this Linode. Review [maintenance policy](https://techdocs.akamai.com/cloud-computing/docs/host-maintenance-policy) documentation for more details.
                 */
                maintenance_policy: 'linode/migrate' | 'linode/power_off_on';
                /**
                 * __Read-only__ Details on the [placement group](https://www.linode.com/docs/products/compute/compute-instances/guides/placement-groups/) that this Linode belongs to. Empty if the Linode isn't in a placement group.
                 */
                readonly placement_group: {
                    /**
                     * The placement group's ID. You need to provide it for all operations that affect it.
                     */
                    id: number;
                    /**
                     * __Filterable__ The unique name set for the placement group. A label has these constraints:
                     *
                     * - It needs to begin and end with an alphanumeric character.
                     * - It can only consist of alphanumeric characters, hyphens (`-`), underscores (`_`) or periods (`.`).
                     */
                    label: string;
                    /**
                     * How requests to add future compute instances to your placement group are handled, and whether it remains compliant:
                     *
                     * - `strict`. Don't assign a new compute instance if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. Use this to ensure the placement group stays compliant (`is_compliant: true`).
                     * - `flexible`. Assign a new compute instance, even if it breaks the grouped-together or spread-apart model set by the `placement_group_type`. This makes the group non-compliant (`is_compliant: false`). You need to wait for Akamai to move the offending compute instance to make it compliant again, once the necessary capacity is available in the region. Offers flexibility to add future compute instances if compliance isn't an immediate concern.
                     *
                     * <<LB>>
                     *
                     * > 📘
                     * >
                     * > In rare cases, non-compliance can occur with a `strict` placement group if Akamai needs to failover or migrate your compute instances for maintenance. Fixing non-compliance for a `strict` placement group is prioritized over a `flexible` group.
                     */
                    placement_group_policy: 'strict' | 'flexible';
                    /**
                     * __Filterable__, __Read-only__ How compute instances are distributed in your placement group. A `placement_group_type` using anti-affinity (`anti-affinity:local`) places compute instances in separate hosts, but still in the same region. This best supports the spread-apart model for high availability. A `placement_group_type` using affinity places compute instances physically close together, possibly on the same host. This supports the grouped-together model for low-latency.
                     *
                     * > 📘
                     * >
                     * > Currently, only `anti_affinity:local` is available for `placement_group_type`.
                     */
                    placement_group_type: 'anti_affinity:local';
                } | null;
                /**
                 * __Filterable__, __Read-only__ The [region](https://techdocs.akamai.com/linode-api/reference/get-regions) where the Linode deployed. A Linode's region can only be changed by initiating a [cross data center migration](https://techdocs.akamai.com/linode-api/reference/post-migrate-linode-instance).
                 */
                readonly region: string;
                /**
                 * __Read-only__ Information about the resources available to this Linode.
                 */
                readonly specs: {
                    /**
                     * __Read-only__ The amount of storage space, in MB, this Linode has access to. A typical Linode divides this space between a primary disk with an `image` deployed to it, and a swap disk, usually 512 MB. This is the default configuration created when deploying a Linode with an `image` through [Create a Linode](https://techdocs.akamai.com/linode-api/reference/post-linode-instance). While this configuration is suitable for 99% of use cases, if you need finer control over your Linode's disks, see the [List disks](https://techdocs.akamai.com/linode-api/reference/get-linode-disks) operations.
                     */
                    readonly disk: number;
                    /**
                     * __Read-only__ The number of GPUs this Linode has access to.
                     */
                    readonly gpus: number;
                    /**
                     * __Read-only__ The amount of RAM, in MB, this Linode has access to.
                     *
                     * Typically, a Linode boots with all of its available RAM, but this can be configured in a config profile. See the [List config profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation for more information.
                     */
                    readonly memory: number;
                    /**
                     * __Read-only__ The amount of network transfer this Linode is allotted each month.
                     */
                    readonly transfer: number;
                    /**
                     * __Read-only__ The number of VCPUs this Linode has access to.
                     */
                    readonly vcpus: number;
                };
                /**
                 * __Read-only__ A brief description of this Linode's current state. This field may change without direct action from you. For example, when a compute instance goes into maintenance mode, its status is `stopped`. Status is generally self-explanatory, based on its name.
                 *
                 * - `busy` indicates you've assigned the compute instance to a [placement group](https://techdocs.akamai.com/cloud-computing/docs/work-with-placement-groups), but the compute instance is currently booting. Once the boot completes, the API completes the assignment and updates the compute instance's `status` accordingly.
                 * - `provisioning` indicates that the API is applying operating system or Marketplace applications on the compute instance.
                 * - `billing_suspension` indicates that payment is past due on the compute instance, so we've suspended its use.
                 */
                status: 'running' | 'offline' | 'booting' | 'busy' | 'rebooting' | 'shutting_down' | 'provisioning' | 'deleting' | 'migrating' | 'rebuilding' | 'cloning' | 'restoring' | 'stopped' | 'billing_suspension';
                /**
                 * __Filterable__ Tags to help you organize your content.
                 */
                tags: Array<string>;
                /**
                 * __Read-only__ This is the [Linode type](https://techdocs.akamai.com/linode-api/reference/get-linode-types) that this Linode was deployed with. To change a Linode's type, use [Resize a Linode](https://techdocs.akamai.com/linode-api/reference/post-resize-linode-instance).
                 */
                readonly type: string;
                /**
                 * __Read-only__ When this Linode was last updated.
                 */
                readonly updated: string;
                /**
                 * The watchdog, named Lassie, is a Shutdown Watchdog that monitors your Linode and reboots it if it powers off unexpectedly. It works by issuing a boot job when your Linode powers off without a shutdown job being responsible. To prevent a loop, Lassie gives up if there have been more than 5 boot jobs issued within 15 minutes.
                 */
                watchdog_enabled: boolean;
            } | {
                /**
                 * The list of IPs that may perform a zone transfer for this domain. The total combined length of all data within this array cannot exceed 1000 characters.
                 *
                 * > 📘
                 * >
                 * > This is potentially dangerous, and should be set to an empty list unless you intend to use it.
                 */
                axfr_ips: Array<string>;
                /**
                 * A description for this domain. This is for display purposes only.
                 */
                description: string | null;
                /**
                 * __Filterable__ The domain this domain represents. domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two domains representing the same domain.
                 */
                domain: string;
                /**
                 * The amount of time in seconds that may pass before this domain is no longer authoritative.
                 *
                 * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                 *
                 * - Any other value is rounded up to the nearest valid value.
                 *
                 * - A value of 0 is equivalent to the default value of 1209600.
                 */
                expire_sec: number;
                /**
                 * __Filterable__ The group this domain belongs to.  This is for display purposes only.
                 *
                 * @deprecated
                 */
                group: string | null;
                /**
                 * __Read-only__ This domain's unique ID.
                 */
                readonly id: number;
                /**
                 * The IP addresses representing the master DNS for this domain. At least one value is required for `type` slave domains. The total combined length of all data within this array cannot exceed 1000 characters.
                 */
                master_ips: Array<string>;
                /**
                 * The amount of time in seconds before this domain should be refreshed.
                 *
                 * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                 *
                 * - Any other value is rounded up to the nearest valid value.
                 *
                 * - A value of 0 is equivalent to the default value of 14400.
                 */
                refresh_sec: number;
                /**
                 * The interval, in seconds, at which a failed refresh should be retried.
                 *
                 * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                 *
                 * - Any other value is rounded up to the nearest valid value.
                 *
                 * - A value of 0 is equivalent to the default value of 14400.
                 */
                retry_sec: number;
                /**
                 * Start of Authority email address. This is required for `type` master domains.
                 */
                soa_email: string;
                /**
                 * Used to control whether this domain is currently being rendered.
                 */
                status: 'disabled' | 'active';
                /**
                 * __Filterable__ An array of tags applied to this object.  Tags are for organizational purposes only.
                 */
                tags: Array<string>;
                /**
                 * "Time to Live" - the amount of time in seconds that this domain's records may be cached by resolvers or other domain servers.
                 *
                 * - Valid values are 0, 30, 120, 300, 3600, 7200, 14400, 28800, 57600, 86400, 172800, 345600, 604800, 1209600, and 2419200.
                 *
                 * - Any other value is rounded up to the nearest valid value.
                 *
                 * - A value of 0 is equivalent to the default value of 86400.
                 */
                ttl_sec: number;
                /**
                 * Whether this domain represents the authoritative source of information for the domain it describes (`master`), or whether it is a read-only copy of a master (`slave`).
                 */
                type: 'master' | 'slave';
            } | {
                /**
                 * __Read-only__ When this volume was created.
                 */
                readonly created: string;
                /**
                 * __Read-only__ Whether encryption is enabled on this volume.
                 */
                encryption: 'enabled' | 'disabled';
                /**
                 * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
                 */
                readonly filesystem_path: string;
                /**
                 * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
                 */
                hardware_type: 'hdd' | 'nvme';
                /**
                 * __Read-only__ The unique identifier for the volume.
                 */
                readonly id: number;
                /**
                 * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
                 */
                readonly io_ready: boolean;
                /**
                 * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
                 */
                label: string;
                /**
                 * The unique identifier of the Linode this volume is attached to, if applicable.
                 */
                linode_id: number | null;
                /**
                 * __Read-only__ The name of the Linode this volume is attached to, if applicable.
                 */
                readonly linode_label: string | null;
                /**
                 * The unique identifier for the region where the volume lives.
                 */
                region: string;
                /**
                 * The volume's size, in gigabytes.
                 */
                size: number;
                /**
                 * __Read-only__ The current status of the volume. This can be one of:
                 *
                 * - `creating`. The API is creating the volume and it's not ready for use.
                 *
                 * - `active`. The volume is online and ready for use.
                 *
                 * - `resizing`. The volume's capacity is being upgraded.
                 *
                 * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
                 */
                status: 'creating' | 'active' | 'resizing' | 'key_rotating';
                /**
                 * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
                 */
                tags: Array<string>;
                /**
                 * __Read-only__ When this volume was last updated.
                 */
                readonly updated: string;
            } | {
                /**
                 * Throttle TCP connections per second for TCP, HTTP, and HTTPS configurations.  Set to `0` (zero) to disable throttling.
                 */
                client_conn_throttle: number;
                /**
                 * __Read-only__ When this NodeBalancer was created.
                 */
                readonly created: string;
                /**
                 * __Read-only__ This NodeBalancer's hostname, beginning with its IP address and ending with _.ip.linodeusercontent.com_.
                 */
                readonly hostname: string;
                /**
                 * __Read-only__ This NodeBalancer's unique ID.
                 */
                readonly id: number;
                /**
                 * __Filterable__, __Read-only__ This NodeBalancer's public IPv4 address.
                 */
                readonly ipv4: string;
                /**
                 * __Read-only__ This NodeBalancer's public IPv6 address.
                 */
                readonly ipv6: string | null;
                /**
                 * __Filterable__ This NodeBalancer's label. These must be unique on your Account.
                 */
                label: string;
                /**
                 * __Read-only__ This NodeBalancer's related LKE cluster, if any. The value is `null` if this NodeBalancer isn't related to an LKE cluster.
                 */
                readonly lke_cluster: {
                    /**
                     * The ID of the related LKE cluster.
                     */
                    id: string;
                    /**
                     * The label of the related LKE cluster.
                     */
                    label: string;
                    /**
                     * __Read-only__ The type for LKE clusters.
                     */
                    readonly type: string;
                    /**
                     * The URL where you can access the related LKE cluster.
                     */
                    url: string;
                } | null;
                /**
                 * __Filterable__, __Read-only__ The Region where this NodeBalancer is located. NodeBalancers only support backends in the same Region.
                 */
                readonly region: string;
                /**
                 * __Filterable__ An array of Tags applied to this object.  Tags are for organizational purposes only.
                 */
                tags: Array<string>;
                /**
                 * __Read-only__ Information about the amount of transfer this NodeBalancer has had so far this month.
                 */
                readonly transfer: {
                    /**
                     * __Read-only__ The total outbound transfer, in MB, used for this NodeBalancer this month.
                     */
                    readonly in: number | null;
                    /**
                     * __Read-only__ The total inbound transfer, in MB, used for this NodeBalancer this month.
                     */
                    readonly out: number | null;
                    /**
                     * __Read-only__ The total transfer, in MB, used by this NodeBalancer this month.
                     */
                    readonly total: number | null;
                };
                /**
                 * __Read-only__ The type of NodeBalancer.
                 */
                type: 'common' | 'premium';
                /**
                 * __Read-only__ When this NodeBalancer was last updated.
                 */
                readonly updated: string;
            };
            /**
             * The type of object the tag is applied to.
             */
            type: 'domain' | 'linode' | 'nodebalancer' | 'volume';
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetTaggedObjectsResponse = GetTaggedObjectsResponses[keyof GetTaggedObjectsResponses];

export type GetVolumesData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/volumes';
};

export type GetVolumesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVolumesError = GetVolumesErrors[keyof GetVolumesErrors];

export type GetVolumesResponses = {
    /**
     * The Block Storage volumes on your account are returned.
     */
    200: {
        data: Array<{
            /**
             * __Read-only__ When this volume was created.
             */
            readonly created: string;
            /**
             * __Read-only__ Whether encryption is enabled on this volume.
             */
            encryption: 'enabled' | 'disabled';
            /**
             * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
             */
            readonly filesystem_path: string;
            /**
             * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
             */
            hardware_type: 'hdd' | 'nvme';
            /**
             * __Read-only__ The unique identifier for the volume.
             */
            readonly id: number;
            /**
             * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
             */
            readonly io_ready: boolean;
            /**
             * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
             */
            label: string;
            /**
             * The unique identifier of the Linode this volume is attached to, if applicable.
             */
            linode_id: number | null;
            /**
             * __Read-only__ The name of the Linode this volume is attached to, if applicable.
             */
            readonly linode_label: string | null;
            /**
             * The unique identifier for the region where the volume lives.
             */
            region: string;
            /**
             * The volume's size, in gigabytes.
             */
            size: number;
            /**
             * __Read-only__ The current status of the volume. This can be one of:
             *
             * - `creating`. The API is creating the volume and it's not ready for use.
             *
             * - `active`. The volume is online and ready for use.
             *
             * - `resizing`. The volume's capacity is being upgraded.
             *
             * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
             */
            status: 'creating' | 'active' | 'resizing' | 'key_rotating';
            /**
             * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
             */
            tags: Array<string>;
            /**
             * __Read-only__ When this volume was last updated.
             */
            readonly updated: string;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetVolumesResponse = GetVolumesResponses[keyof GetVolumesResponses];

export type PostVolumeData = {
    body: {
        /**
         * When creating a volume attached to a Linode, this is the identifier of the Linode configuration profile (config) where the volume will live. Run the [List configuration profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation and store the `id` for the applicable config. The following apply when adding a `config_id`:
         *
         * - The selected config needs to belong to the Linode referenced by `linode_id` in this request.
         *
         * - You can't provide a `config_id` if you don't also provide a `linode_id` in the request.
         *
         * - If you send a `linode_id` without a `config_id` in the request, the API attaches the volume to that Linode's last used config, or to the only config in that Linode. If there isn't a config available for attachment, the API returns an error.
         */
        config_id?: number;
        /**
         * Enables encryption on the volume. Full disk encryption ensures the data stored on a block storage volume drive is secure. It protects against unauthorized access by keeping the data encrypted if the volume drive is removed from the data center, decommissioned, or disposed of.
         *
         * The platform automatically manages the encryption and decryption process for you. You can use an encrypted volume the same way you use a non-encrypted volume.
         *
         * > 📘
         * >
         * > You can enable or disable disk encryption only when creating new block storage volumes. After a volume is created, the encryption setting can't be changed.
         */
        encryption?: 'enabled' | 'disabled';
        /**
         * The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The Linode this volume should be attached to after it's created. If not given, the volume will be created without an attachment.
         */
        linode_id?: number;
        /**
         * The region where the API deploys the volume. This is only required if you didn't provide a `linode_id` for the volume.
         */
        region?: string | null;
        /**
         * The initial size of this volume, in GB. Volumes can only be resized after the creation completes.
         */
        size?: number;
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags?: Array<string>;
    };
    path?: never;
    query?: never;
    url: '/volumes';
};

export type PostVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostVolumeError = PostVolumeErrors[keyof PostVolumeErrors];

export type PostVolumeResponses = {
    /**
     * Volume
     *
     * A Block Storage volume on your account.
     */
    200: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    };
};

export type PostVolumeResponse = PostVolumeResponses[keyof PostVolumeResponses];

export type GetVolumeTypesData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/volumes/types';
};

export type GetVolumeTypesErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVolumeTypesError = GetVolumeTypesErrors[keyof GetVolumeTypesErrors];

export type GetVolumeTypesResponses = {
    /**
     * A collection of volume types.
     */
    200: {
        /**
         * The volume types.
         */
        data: Array<{
            /**
             * __Read-only__ The ID representing the volume type.
             */
            readonly id: string;
            /**
             * __Filterable__, __Read-only__ The volume type label is for display purposes only.
             */
            readonly label: string;
            /**
             * __Read-only__ The default cost of this volume type. Prices are in US dollars, broken down into hourly and monthly charges.
             *
             * Certain regions have different prices from the default. For region-specific prices, see `region_prices`.
             */
            readonly price: {
                /**
                 * __Filterable__ Cost (in US dollars) per hour.
                 */
                hourly: number;
                /**
                 * __Filterable__ Cost per month, in US dollars.
                 */
                monthly: number;
            };
            region_prices: Array<{
                /**
                 * Cost per hour for this region, in US dollars.
                 */
                hourly: number;
                /**
                 * The Region ID for these prices.
                 */
                id: string;
                /**
                 * Cost per month for this region, in US dollars.
                 */
                monthly: number;
            }>;
            /**
             * __Filterable__, __Read-only__ The monthly outbound transfer amount, in MB.
             */
            readonly transfer: number;
        }>;
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    };
};

export type GetVolumeTypesResponse = GetVolumeTypesResponses[keyof GetVolumeTypesResponses];

export type DeleteVolumeData = {
    body?: never;
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: never;
    url: '/volumes/{volumeId}';
};

export type DeleteVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteVolumeError = DeleteVolumeErrors[keyof DeleteVolumeErrors];

export type DeleteVolumeResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteVolumeResponse = DeleteVolumeResponses[keyof DeleteVolumeResponses];

export type GetVolumeData = {
    body?: never;
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/volumes/{volumeId}';
};

export type GetVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVolumeError = GetVolumeErrors[keyof GetVolumeErrors];

export type GetVolumeResponses = {
    /**
     * Volume
     *
     * A Block Storage volume on your account.
     */
    200: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    };
};

export type GetVolumeResponse = GetVolumeResponses[keyof GetVolumeResponses];

export type PutVolumeData = {
    body: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    } & {
        readonly linode_id: unknown;
        readonly size: unknown;
    };
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: never;
    url: '/volumes/{volumeId}';
};

export type PutVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutVolumeError = PutVolumeErrors[keyof PutVolumeErrors];

export type PutVolumeResponses = {
    /**
     * Volume
     *
     * A Block Storage volume on your account.
     */
    200: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    };
};

export type PutVolumeResponse = PutVolumeResponses[keyof PutVolumeResponses];

export type PostAttachVolumeData = {
    body: {
        /**
         * The unique identifier assigned to the Linode configuration where you want to include this volume. Run the [List configuration profiles](https://techdocs.akamai.com/linode-api/reference/get-linode-configs) operation and store the `id` for the target configuration.
         *
         * > 📘
         * >
         * > - The configuration needs to belong to the Linode referenced by `linode_id`.
         * >
         * > - If you don't include this value, the API uses the last booted configuration.
         */
        config_id?: number;
        /**
         * The unique identifier assigned to the Linode where you want to attach the volume. Run the [List Linodes](https://techdocs.akamai.com/linode-api/reference/get-linode-instances) operation and store the `id` for the target Linode.
         */
        linode_id: number;
        /**
         * If set to `false`, the volume won't be attached to the Linode configuration. In this case, more than eight volumes may be attached to a Linode if a Linode has 16GB of RAM or more. The number of volumes you can attach is equal to the number of GB of RAM that the Linode has, up to a maximum of 64. If you set this to `false`, you can't include a `config_id` and only the `linode_id` should be passed.
         *
         * > 📘
         * >
         * > To use this object, the target `linode_id` needs to have a status of `running`. Run the [List Linodes](https://techdocs.akamai.com/linode-api/reference/get-linode-instances) operation to check the `status` of each Linode.
         */
        persist_across_boots?: boolean;
    };
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: never;
    url: '/volumes/{volumeId}/attach';
};

export type PostAttachVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostAttachVolumeError = PostAttachVolumeErrors[keyof PostAttachVolumeErrors];

export type PostAttachVolumeResponses = {
    /**
     * Volume
     *
     * A Block Storage volume on your account.
     */
    200: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    };
};

export type PostAttachVolumeResponse = PostAttachVolumeResponses[keyof PostAttachVolumeResponses];

export type PostCloneVolumeData = {
    body: {
        /**
         * The name of the new volume after the clone. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
    };
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: never;
    url: '/volumes/{volumeId}/clone';
};

export type PostCloneVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostCloneVolumeError = PostCloneVolumeErrors[keyof PostCloneVolumeErrors];

export type PostCloneVolumeResponses = {
    /**
     * Volume
     *
     * A Block Storage volume on your account.
     */
    200: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    };
};

export type PostCloneVolumeResponse = PostCloneVolumeResponses[keyof PostCloneVolumeResponses];

export type PostDetachVolumeData = {
    body?: never;
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: never;
    url: '/volumes/{volumeId}/detach';
};

export type PostDetachVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostDetachVolumeError = PostDetachVolumeErrors[keyof PostDetachVolumeErrors];

export type PostDetachVolumeResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type PostDetachVolumeResponse = PostDetachVolumeResponses[keyof PostDetachVolumeResponses];

export type PostResizeVolumeData = {
    /**
     * The requested size to increase your Volume to.
     */
    body: {
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
    };
    path: {
        /**
         * The unique identifier for the target volume. Run the [List volumes](https://techdocs.akamai.com/linode-api/reference/get-volumes) operation and store the `id` for the target volume as your `volumeId`.
         */
        volumeId: number;
    };
    query?: never;
    url: '/volumes/{volumeId}/resize';
};

export type PostResizeVolumeErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostResizeVolumeError = PostResizeVolumeErrors[keyof PostResizeVolumeErrors];

export type PostResizeVolumeResponses = {
    /**
     * Volume
     *
     * A Block Storage volume on your account.
     */
    200: {
        /**
         * __Read-only__ When this volume was created.
         */
        readonly created: string;
        /**
         * __Read-only__ Whether encryption is enabled on this volume.
         */
        encryption: 'enabled' | 'disabled';
        /**
         * __Read-only__ The full file system path for the volume, based on its `label`. The path is `/dev/disk/by-id/scsi-0Linode_Volume_label`.
         */
        readonly filesystem_path: string;
        /**
         * __Read-only__ The storage type of this volume. This can be either `hdd` to emulate a hard disk drive for the volume, or `nvme` to emulate a non-volatile memory express solid state drive.
         */
        hardware_type: 'hdd' | 'nvme';
        /**
         * __Read-only__ The unique identifier for the volume.
         */
        readonly id: number;
        /**
         * __Read-only__ Indicates whether the volume is successfully attached to a Linode and ready for read and write operations.
         */
        readonly io_ready: boolean;
        /**
         * __Filterable__ The name of the volume. A `label` can be up to 32 characters long and contain alphanumeric characters, hyphens, and underscores. This value is also used in the volume's `filesystem_path`.
         */
        label: string;
        /**
         * The unique identifier of the Linode this volume is attached to, if applicable.
         */
        linode_id: number | null;
        /**
         * __Read-only__ The name of the Linode this volume is attached to, if applicable.
         */
        readonly linode_label: string | null;
        /**
         * The unique identifier for the region where the volume lives.
         */
        region: string;
        /**
         * The volume's size, in gigabytes.
         */
        size: number;
        /**
         * __Read-only__ The current status of the volume. This can be one of:
         *
         * - `creating`. The API is creating the volume and it's not ready for use.
         *
         * - `active`. The volume is online and ready for use.
         *
         * - `resizing`. The volume's capacity is being upgraded.
         *
         * - `key_rotating`. The volume's encryption keys are being rotated to new values. Requests to resize, delete, or clone a volume fail during encryption key rotation.
         */
        status: 'creating' | 'active' | 'resizing' | 'key_rotating';
        /**
         * __Filterable__ Any tags applied to this object. Use [tags](https://techdocs.akamai.com/linode-api/reference/post-tag) to label and organize your cloud computing resources.
         */
        tags: Array<string>;
        /**
         * __Read-only__ When this volume was last updated.
         */
        readonly updated: string;
    };
};

export type PostResizeVolumeResponse = PostResizeVolumeResponses[keyof PostResizeVolumeResponses];

export type GetVpcsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/vpcs';
};

export type GetVpcsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVpcsError = GetVpcsErrors[keyof GetVpcsErrors];

export type GetVpcsResponses = {
    /**
     * A paginated list of VPC objects.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data?: Array<{
            /**
             * __Filterable__, __Read-only__ The date-time of VPC creation.
             */
            readonly created: string;
            /**
             * A written description to help distinguish the VPC.
             */
            description: string;
            /**
             * __Filterable__, __Read-only__ The unique ID of the VPC.
             */
            readonly id: number;
            /**
             * __Filterable__ The VPC's label, for display purposes only.
             *
             * - Needs to be unique among the Account's VPCs.
             * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
             */
            label: string;
            /**
             * __Filterable__ The Region for the VPC.
             */
            region: string;
            /**
             * A list of subnets associated with the VPC.
             */
            subnets: Array<{
                /**
                 * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
                 */
                readonly created: string;
                /**
                 * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
                 */
                readonly id: number;
                /**
                 * IPv4 range in CIDR canonical form.
                 *
                 * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
                 * - Allowed prefix lengths: 1-29.
                 * - The range must not overlap with 192.168.128.0/17.
                 * - The range must not overlap with other Subnets on the same VPC.
                 */
                ipv4: string;
                /**
                 * __Filterable__ The VPC Subnet's label, for display purposes only.
                 *
                 * - Must be unique among the VPC's Subnets.
                 * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
                 */
                label: string;
                /**
                 * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
                 *
                 * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
                 */
                readonly linodes: Array<{
                    /**
                     * ID of a Linode assigned to the VPC Subnet.
                     */
                    id: number;
                    /**
                     * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
                     */
                    interfaces: Array<{
                        /**
                         * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                         */
                        active: boolean;
                        /**
                         * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                         */
                        config_id: number | null;
                        /**
                         * ID of the interface.
                         */
                        id: number;
                    }>;
                }>;
                /**
                 * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
                 */
                readonly updated: string | null;
            }>;
            /**
             * __Filterable__, __Read-only__ The date-time of the most recent VPC update.
             */
            readonly updated: string | null;
        }>;
    };
};

export type GetVpcsResponse = GetVpcsResponses[keyof GetVpcsResponses];

export type PostVpcData = {
    /**
     * VPC Create request object.
     */
    body: unknown & {
        subnets?: unknown;
    } & {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC creation.
         */
        readonly created: string;
        /**
         * A written description to help distinguish the VPC.
         */
        description: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC.
         */
        readonly id: number;
        /**
         * __Filterable__ The VPC's label, for display purposes only.
         *
         * - Needs to be unique among the Account's VPCs.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Filterable__ The Region for the VPC.
         */
        region: string;
        /**
         * A list of subnets associated with the VPC.
         */
        subnets: Array<{
            /**
             * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
             */
            readonly id: number;
            /**
             * IPv4 range in CIDR canonical form.
             *
             * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
             * - Allowed prefix lengths: 1-29.
             * - The range must not overlap with 192.168.128.0/17.
             * - The range must not overlap with other Subnets on the same VPC.
             */
            ipv4: string;
            /**
             * __Filterable__ The VPC Subnet's label, for display purposes only.
             *
             * - Must be unique among the VPC's Subnets.
             * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
             */
            label: string;
            /**
             * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
             *
             * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
             */
            readonly linodes: Array<{
                /**
                 * ID of a Linode assigned to the VPC Subnet.
                 */
                id: number;
                /**
                 * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
                 */
                interfaces: Array<{
                    /**
                     * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                     */
                    active: boolean;
                    /**
                     * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                     */
                    config_id: number | null;
                    /**
                     * ID of the interface.
                     */
                    id: number;
                }>;
            }>;
            /**
             * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
             */
            readonly updated: string | null;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC update.
         */
        readonly updated: string | null;
    };
    path?: never;
    query?: never;
    url: '/vpcs';
};

export type PostVpcErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostVpcError = PostVpcErrors[keyof PostVpcErrors];

export type PostVpcResponses = {
    /**
     * An object describing a VPC belonging to the Account.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC creation.
         */
        readonly created: string;
        /**
         * A written description to help distinguish the VPC.
         */
        description: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC.
         */
        readonly id: number;
        /**
         * __Filterable__ The VPC's label, for display purposes only.
         *
         * - Needs to be unique among the Account's VPCs.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Filterable__ The Region for the VPC.
         */
        region: string;
        /**
         * A list of subnets associated with the VPC.
         */
        subnets: Array<{
            /**
             * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
             */
            readonly id: number;
            /**
             * IPv4 range in CIDR canonical form.
             *
             * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
             * - Allowed prefix lengths: 1-29.
             * - The range must not overlap with 192.168.128.0/17.
             * - The range must not overlap with other Subnets on the same VPC.
             */
            ipv4: string;
            /**
             * __Filterable__ The VPC Subnet's label, for display purposes only.
             *
             * - Must be unique among the VPC's Subnets.
             * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
             */
            label: string;
            /**
             * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
             *
             * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
             */
            readonly linodes: Array<{
                /**
                 * ID of a Linode assigned to the VPC Subnet.
                 */
                id: number;
                /**
                 * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
                 */
                interfaces: Array<{
                    /**
                     * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                     */
                    active: boolean;
                    /**
                     * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                     */
                    config_id: number | null;
                    /**
                     * ID of the interface.
                     */
                    id: number;
                }>;
            }>;
            /**
             * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
             */
            readonly updated: string | null;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC update.
         */
        readonly updated: string | null;
    };
};

export type PostVpcResponse = PostVpcResponses[keyof PostVpcResponses];

export type GetVpcsIpsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/vpcs/ips';
};

export type GetVpcsIpsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVpcsIpsError = GetVpcsIpsErrors[keyof GetVpcsIpsErrors];

export type GetVpcsIpsResponses = {
    /**
     * A paginated list of VPC interface IP addresses.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data?: Array<{
            /**
             * __Filterable__, __Read-only__ Returns `true` if the VPC interface is in use, meaning that the Linode was powered on using the `config_id` to which the interface belongs. Otherwise returns `false`.
             */
            readonly active: boolean;
            /**
             * __Read-only__ An IPv4 address configured for this VPC interface. These follow the [RFC 1918](https://datatracker.ietf.org/doc/html/rfc1918) private address format. Displayed as `null` if an `address_range`.
             */
            readonly address: string | null;
            /**
             * __Read-only__ A range of IPv4 addresses configured for this VPC interface. Displayed as `null` if a single `address`.
             */
            readonly address_range: string | null;
            /**
             * __Filterable__, __Read-only__ The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
             */
            readonly config_id: number | null;
            /**
             * __Read-only__ The default gateway for the VPC subnet that the IP or IP range belongs to.
             */
            readonly gateway: string | null;
            /**
             * __Beta__, __Read-only__ The globally general API entity identifier for the Linode interface.
             */
            readonly interface_id: number;
            /**
             * __Filterable__, __Read-only__ The identifier for the Linode the VPC interface currently belongs to.
             */
            readonly linode_id: number;
            /**
             * __Read-only__ The public IP address used for NAT 1:1 with the VPC. This is empty if NAT 1:1 isn't used.
             */
            readonly nat_1_1: string;
            /**
             * __Read-only__ The number of bits set in the `subnet_mask`.
             */
            readonly prefix: number | null;
            /**
             * __Filterable__, __Read-only__ The region of the VPC.
             */
            readonly region: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The mask that separates host bits from network bits for the `address` or `address_range`.
             */
            readonly subnet_mask: string;
            /**
             * __Filterable__, __Read-only__ The unique globally general API entity identifier for the VPC.
             */
            readonly vpc_id: number;
        }>;
    };
};

export type GetVpcsIpsResponse = GetVpcsIpsResponses[keyof GetVpcsIpsResponses];

export type DeleteVpcData = {
    body?: never;
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}';
};

export type DeleteVpcErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteVpcError = DeleteVpcErrors[keyof DeleteVpcErrors];

export type DeleteVpcResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteVpcResponse = DeleteVpcResponses[keyof DeleteVpcResponses];

export type GetVpcData = {
    body?: never;
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}';
};

export type GetVpcErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVpcError = GetVpcErrors[keyof GetVpcErrors];

export type GetVpcResponses = {
    /**
     * An object describing a VPC belonging to the Account.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC creation.
         */
        readonly created: string;
        /**
         * A written description to help distinguish the VPC.
         */
        description: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC.
         */
        readonly id: number;
        /**
         * __Filterable__ The VPC's label, for display purposes only.
         *
         * - Needs to be unique among the Account's VPCs.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Filterable__ The Region for the VPC.
         */
        region: string;
        /**
         * A list of subnets associated with the VPC.
         */
        subnets: Array<{
            /**
             * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
             */
            readonly id: number;
            /**
             * IPv4 range in CIDR canonical form.
             *
             * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
             * - Allowed prefix lengths: 1-29.
             * - The range must not overlap with 192.168.128.0/17.
             * - The range must not overlap with other Subnets on the same VPC.
             */
            ipv4: string;
            /**
             * __Filterable__ The VPC Subnet's label, for display purposes only.
             *
             * - Must be unique among the VPC's Subnets.
             * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
             */
            label: string;
            /**
             * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
             *
             * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
             */
            readonly linodes: Array<{
                /**
                 * ID of a Linode assigned to the VPC Subnet.
                 */
                id: number;
                /**
                 * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
                 */
                interfaces: Array<{
                    /**
                     * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                     */
                    active: boolean;
                    /**
                     * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                     */
                    config_id: number | null;
                    /**
                     * ID of the interface.
                     */
                    id: number;
                }>;
            }>;
            /**
             * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
             */
            readonly updated: string | null;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC update.
         */
        readonly updated: string | null;
    };
};

export type GetVpcResponse = GetVpcResponses[keyof GetVpcResponses];

export type PutVpcData = {
    /**
     * A VPC Update request object.
     */
    body: {
        /**
         * A written description to help distinguish the VPC.
         */
        description: string;
        /**
         * __Filterable__ The VPC's label, for display purposes only.
         *
         * - Needs to be unique among the Account's VPCs.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
    };
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}';
};

export type PutVpcErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutVpcError = PutVpcErrors[keyof PutVpcErrors];

export type PutVpcResponses = {
    /**
     * An object describing a VPC belonging to the Account.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC creation.
         */
        readonly created: string;
        /**
         * A written description to help distinguish the VPC.
         */
        description: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC.
         */
        readonly id: number;
        /**
         * __Filterable__ The VPC's label, for display purposes only.
         *
         * - Needs to be unique among the Account's VPCs.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Filterable__ The Region for the VPC.
         */
        region: string;
        /**
         * A list of subnets associated with the VPC.
         */
        subnets: Array<{
            /**
             * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
             */
            readonly id: number;
            /**
             * IPv4 range in CIDR canonical form.
             *
             * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
             * - Allowed prefix lengths: 1-29.
             * - The range must not overlap with 192.168.128.0/17.
             * - The range must not overlap with other Subnets on the same VPC.
             */
            ipv4: string;
            /**
             * __Filterable__ The VPC Subnet's label, for display purposes only.
             *
             * - Must be unique among the VPC's Subnets.
             * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
             */
            label: string;
            /**
             * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
             *
             * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
             */
            readonly linodes: Array<{
                /**
                 * ID of a Linode assigned to the VPC Subnet.
                 */
                id: number;
                /**
                 * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
                 */
                interfaces: Array<{
                    /**
                     * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                     */
                    active: boolean;
                    /**
                     * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                     */
                    config_id: number | null;
                    /**
                     * ID of the interface.
                     */
                    id: number;
                }>;
            }>;
            /**
             * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
             */
            readonly updated: string | null;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC update.
         */
        readonly updated: string | null;
    };
};

export type PutVpcResponse = PutVpcResponses[keyof PutVpcResponses];

export type GetVpcIpsData = {
    body?: never;
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/vpcs/{vpcId}/ips';
};

export type GetVpcIpsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVpcIpsError = GetVpcIpsErrors[keyof GetVpcIpsErrors];

export type GetVpcIpsResponses = {
    /**
     * The IP addresses for the requested VPC.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data?: Array<{
            /**
             * __Filterable__, __Read-only__ Returns `true` if the VPC interface is in use, meaning that the Linode was powered on using the `config_id` to which the interface belongs. Otherwise returns `false`.
             */
            readonly active: boolean;
            /**
             * __Read-only__ An IPv4 address configured for this VPC interface. These follow the [RFC 1918](https://datatracker.ietf.org/doc/html/rfc1918) private address format. Displayed as `null` if an `address_range`.
             */
            readonly address: string | null;
            /**
             * __Read-only__ A range of IPv4 addresses configured for this VPC interface. Displayed as `null` if a single `address`.
             */
            readonly address_range: string | null;
            /**
             * __Filterable__, __Read-only__ The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
             */
            readonly config_id: number | null;
            /**
             * __Read-only__ The default gateway for the VPC subnet that the IP or IP range belongs to.
             */
            readonly gateway: string | null;
            /**
             * __Beta__, __Read-only__ The globally general API entity identifier for the Linode interface.
             */
            readonly interface_id: number;
            /**
             * __Filterable__, __Read-only__ The identifier for the Linode the VPC interface currently belongs to.
             */
            readonly linode_id: number;
            /**
             * __Read-only__ The public IP address used for NAT 1:1 with the VPC. This is empty if NAT 1:1 isn't used.
             */
            readonly nat_1_1: string;
            /**
             * __Read-only__ The number of bits set in the `subnet_mask`.
             */
            readonly prefix: number | null;
            /**
             * __Filterable__, __Read-only__ The region of the VPC.
             */
            readonly region: string;
            /**
             * The `id` of the VPC Subnet for this interface.
             */
            subnet_id: number;
            /**
             * __Read-only__ The mask that separates host bits from network bits for the `address` or `address_range`.
             */
            readonly subnet_mask: string;
            /**
             * __Filterable__, __Read-only__ The unique globally general API entity identifier for the VPC.
             */
            readonly vpc_id: number;
        }>;
    };
};

export type GetVpcIpsResponse = GetVpcIpsResponses[keyof GetVpcIpsResponses];

export type GetVpcSubnetsData = {
    body?: never;
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
    };
    query?: {
        /**
         * The page of a collection to return.
         */
        page?: number;
        /**
         * The number of items to return per page.
         */
        page_size?: number;
    };
    url: '/vpcs/{vpcId}/subnets';
};

export type GetVpcSubnetsErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVpcSubnetsError = GetVpcSubnetsErrors[keyof GetVpcSubnetsErrors];

export type GetVpcSubnetsResponses = {
    /**
     * A paginated list of VPC Subnet objects.
     */
    200: {
        /**
         * __Read-only__ The current [page](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly page: number;
        /**
         * __Read-only__ The total number of [pages](https://techdocs.akamai.com/linode-api/reference/pagination).
         */
        readonly pages: number;
        /**
         * __Read-only__ The total number of results.
         */
        readonly results: number;
    } & {
        data?: Array<{
            /**
             * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
             */
            readonly created: string;
            /**
             * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
             */
            readonly id: number;
            /**
             * IPv4 range in CIDR canonical form.
             *
             * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
             * - Allowed prefix lengths: 1-29.
             * - The range must not overlap with 192.168.128.0/17.
             * - The range must not overlap with other Subnets on the same VPC.
             */
            ipv4: string;
            /**
             * __Filterable__ The VPC Subnet's label, for display purposes only.
             *
             * - Must be unique among the VPC's Subnets.
             * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
             */
            label: string;
            /**
             * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
             *
             * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
             */
            readonly linodes: Array<{
                /**
                 * ID of a Linode assigned to the VPC Subnet.
                 */
                id: number;
                /**
                 * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
                 */
                interfaces: Array<{
                    /**
                     * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                     */
                    active: boolean;
                    /**
                     * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                     */
                    config_id: number | null;
                    /**
                     * ID of the interface.
                     */
                    id: number;
                }>;
            }>;
            /**
             * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
             */
            readonly updated: string | null;
        }>;
    };
};

export type GetVpcSubnetsResponse = GetVpcSubnetsResponses[keyof GetVpcSubnetsResponses];

export type PostVpcSubnetData = {
    /**
     * VPC Subnet Create request object.
     */
    body: {
        /**
         * IPv4 range in CIDR canonical form.
         *
         * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
         * - Allowed prefix lengths: 1-29.
         * - The range must not overlap with 192.168.128.0/17.
         * - The range must not overlap with other Subnets on the same VPC.
         */
        ipv4: string;
        /**
         * __Filterable__ The VPC Subnet's label, for display purposes only.
         *
         * - Must be unique among the VPC's Subnets.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
    };
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}/subnets';
};

export type PostVpcSubnetErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PostVpcSubnetError = PostVpcSubnetErrors[keyof PostVpcSubnetErrors];

export type PostVpcSubnetResponses = {
    /**
     * An object describing a VPC Subnet.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
         */
        readonly created: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
         */
        readonly id: number;
        /**
         * IPv4 range in CIDR canonical form.
         *
         * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
         * - Allowed prefix lengths: 1-29.
         * - The range must not overlap with 192.168.128.0/17.
         * - The range must not overlap with other Subnets on the same VPC.
         */
        ipv4: string;
        /**
         * __Filterable__ The VPC Subnet's label, for display purposes only.
         *
         * - Must be unique among the VPC's Subnets.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
         *
         * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
         */
        readonly linodes: Array<{
            /**
             * ID of a Linode assigned to the VPC Subnet.
             */
            id: number;
            /**
             * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
             */
            interfaces: Array<{
                /**
                 * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                 */
                active: boolean;
                /**
                 * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                 */
                config_id: number | null;
                /**
                 * ID of the interface.
                 */
                id: number;
            }>;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
         */
        readonly updated: string | null;
    };
};

export type PostVpcSubnetResponse = PostVpcSubnetResponses[keyof PostVpcSubnetResponses];

export type DeleteVpcSubnetData = {
    body?: never;
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
        /**
         * The `id` of the VPC Subnet.
         */
        vpcSubnetId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}/subnets/{vpcSubnetId}';
};

export type DeleteVpcSubnetErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type DeleteVpcSubnetError = DeleteVpcSubnetErrors[keyof DeleteVpcSubnetErrors];

export type DeleteVpcSubnetResponses = {
    /**
     * The API responds with an empty object.
     */
    200: {
        [key: string]: unknown;
    };
};

export type DeleteVpcSubnetResponse = DeleteVpcSubnetResponses[keyof DeleteVpcSubnetResponses];

export type GetVpcSubnetData = {
    body?: never;
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
        /**
         * The `id` of the VPC Subnet.
         */
        vpcSubnetId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}/subnets/{vpcSubnetId}';
};

export type GetVpcSubnetErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type GetVpcSubnetError = GetVpcSubnetErrors[keyof GetVpcSubnetErrors];

export type GetVpcSubnetResponses = {
    /**
     * An object describing a VPC Subnet.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
         */
        readonly created: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
         */
        readonly id: number;
        /**
         * IPv4 range in CIDR canonical form.
         *
         * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
         * - Allowed prefix lengths: 1-29.
         * - The range must not overlap with 192.168.128.0/17.
         * - The range must not overlap with other Subnets on the same VPC.
         */
        ipv4: string;
        /**
         * __Filterable__ The VPC Subnet's label, for display purposes only.
         *
         * - Must be unique among the VPC's Subnets.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
         *
         * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
         */
        readonly linodes: Array<{
            /**
             * ID of a Linode assigned to the VPC Subnet.
             */
            id: number;
            /**
             * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
             */
            interfaces: Array<{
                /**
                 * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                 */
                active: boolean;
                /**
                 * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                 */
                config_id: number | null;
                /**
                 * ID of the interface.
                 */
                id: number;
            }>;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
         */
        readonly updated: string | null;
    };
};

export type GetVpcSubnetResponse = GetVpcSubnetResponses[keyof GetVpcSubnetResponses];

export type PutVpcSubnetData = {
    /**
     * A VPC Subnet Update request object.
     */
    body: {
        /**
         * __Filterable__ The VPC Subnet's label, for display purposes only.
         *
         * - Must be unique among the VPC's Subnets.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
    };
    path: {
        /**
         * The `id` of the VPC.
         */
        vpcId: number;
        /**
         * The `id` of the VPC Subnet.
         */
        vpcSubnetId: number;
    };
    query?: never;
    url: '/vpcs/{vpcId}/subnets/{vpcSubnetId}';
};

export type PutVpcSubnetErrors = {
    /**
     * See [Errors](https://techdocs.akamai.com/linode-api/reference/errors) for the range of possible error response codes.
     */
    default: {
        errors: Array<{
            /**
             * The field in the request that caused this error. This may be a path, separated by periods in the case of nested fields. In some cases this may come back as `null` if the error is not specific to any single element of the request.
             */
            field: string;
            /**
             * What happened to cause this error. In most cases, this can be fixed immediately by changing the data you sent in the request, but in some cases you will be instructed to [Open a support ticket](https://techdocs.akamai.com/linode-api/reference/post-ticket) or perform some other action before you can complete the request successfully.
             */
            reason: string;
        }>;
    };
};

export type PutVpcSubnetError = PutVpcSubnetErrors[keyof PutVpcSubnetErrors];

export type PutVpcSubnetResponses = {
    /**
     * An object describing a VPC Subnet.
     */
    200: {
        /**
         * __Filterable__, __Read-only__ The date-time of VPC Subnet creation.
         */
        readonly created: string;
        /**
         * __Filterable__, __Read-only__ The unique ID of the VPC Subnet.
         */
        readonly id: number;
        /**
         * IPv4 range in CIDR canonical form.
         *
         * - The range must belong to a private address space as defined in [RFC1918](https://datatracker.ietf.org/doc/html/rfc1918).
         * - Allowed prefix lengths: 1-29.
         * - The range must not overlap with 192.168.128.0/17.
         * - The range must not overlap with other Subnets on the same VPC.
         */
        ipv4: string;
        /**
         * __Filterable__ The VPC Subnet's label, for display purposes only.
         *
         * - Must be unique among the VPC's Subnets.
         * - Can only contain ASCII letters, numbers, and hyphens (`-`). You can't use two consecutive hyphens (`--`).
         */
        label: string;
        /**
         * __Read-only__ An array of Linode IDs assigned to the VPC Subnet.
         *
         * A Linode is assigned to a VPC Subnet if it has a Configuration Profile with a `vpc` purpose interface with the subnet's `subnet_id`. Even if the Configuration Profile is not active, meaning the Linode does not have access to the Subnet, the Linode still appears in this array.
         */
        readonly linodes: Array<{
            /**
             * ID of a Linode assigned to the VPC Subnet.
             */
            id: number;
            /**
             * VPC purpose interfaces with the subnet's `subnet_id` assigned to the Linode.
             */
            interfaces: Array<{
                /**
                 * Returns `true` if the Interface is in use, meaning that the Linode was powered on using the Configuration Profile to which the Interface belongs. Otherwise returns `false`.
                 */
                active: boolean;
                /**
                 * The globally general entity identifier for the Linode configuration profile that includes the VPC. If this is a VPC Linode interface, the value is `null`.
                 */
                config_id: number | null;
                /**
                 * ID of the interface.
                 */
                id: number;
            }>;
        }>;
        /**
         * __Filterable__, __Read-only__ The date-time of the most recent VPC Subnet update.
         */
        readonly updated: string | null;
    };
};

export type PutVpcSubnetResponse = PutVpcSubnetResponses[keyof PutVpcSubnetResponses];
